[package]
name = "gunbc-exec"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Execution engine for gunbc DAGs with boundary interception"

[dependencies]
gunbc-ir = { path = "../ir" }
serde_json = { workspace = true }
thiserror = "1.0"
