[package]
name = "gunbc-codegen"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared code generation utilities for gunbc"

[dependencies]
gunbc-ir = { path = "../ir" }
gunbc-clippy = { path = "../../lib/tools/clippy" }

[[bin]]
name = "gunbc-codegen"
path = "src/main.rs"
