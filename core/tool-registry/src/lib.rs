//! Tool registry (deprecated).
//!
//! Tool discovery is now driven by DSL `@binary` annotations via
//! `discover_tool_defs_from_dsl()` in `gunbc-dag/src/dsl_registry.rs`.
//!
//! This crate is retained as an empty shell. It will be removed entirely
//! once downstream references in documentation are cleaned up.
