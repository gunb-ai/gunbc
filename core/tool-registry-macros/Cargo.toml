[package]
name = "gunbc-tool-registry-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Proc macros for auto-registered tool targets (deprecated — see gunbc-dag/src/dsl_registry.rs)"

[lib]
proc-macro = true

[dependencies]
