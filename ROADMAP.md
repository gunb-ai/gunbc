# gunbc — Roadmap

`DESIGN.md` is the authority for *why*. This is the **shape of the work** — a scannable, dependency-ordered checklist. **Checkboxes are authoritative for progress**; detail lives in the linked plan docs — don't restate it here (no dual representations). A task's real state is its branch/PR.

Legend: `[x]` done · `[ ]` todo · ~~struck~~ **superseded →** successor (the named carrier/lane now owns the territory; the row is kept as the pointer, never dispatched) · **indentation = depends on the item it sits under**. Each section opens with a **◆ Milestones** spine — the *verifiable* checkpoints in dependency order (`✓` reached · `▸` now · `○` ahead); the checklist below is the work toward them. Read L→R = the path.

**Reset 2026-07-01 (operator-directed).** The roadmap is collapsed to **two pillars** — **§1 get off v1** and **§2 own the compute fabric** — plus **§3 the floor + audit**: the walls that keep both pillars honest, and an execution-grounded audit of what the existing machinery *actually does* (green by execution, red by perturbation). Everything else is **§4 shelved**: parked, not deleted — plan docs and carriers stay, no work dispatches from it, and un-shelving is a PR that moves a node into §1–§3 with a displaced-cost justification (DESIGN §6). The prior roadmap with full detail is in git history. Priority order, top = now.

## ✦ Operator TODO — consolidated (2026-07-22 · CI audit + merge wave)

The operator's real TODO, organized (2026-07-22): six numbered items, each grounded in the lanes that already exist — pointers dispatch work, rows track it. Each row points at its carrier (PR · branch · issue · lane node); check a row when its lane merges or its issue closes, delete it when the pointer goes stale. DISSOLUTION: this lane is a dated scaffold — delete it when every row is checked or re-homed into §1–§4.

**1 · Main to actual green (audit receipts, 2026-07-22):**

- [ ] **falsifier dark since 2026-07-16 — roster burn-down** — every 4-hourly cold run red on `non_fold_residue_clean_holds` + `non_fold_residue_no_unrostered_or_stale`, logged as predicted=unaffected / actual=red divergences. Root-caused 2026-07-22 by local receipt (`nfr_roster_receipt`): 3 unrostered sites, 0 stale — `dag/gunbc/host_converge_slice1.dag::host_converge_slice1_host_result_all_valid` + `::host_converge_slice1_host_result_applied_count` (landed #7009) and `dag/gunbc/host_hygiene_reaper.dag::action_kills_build_cache_server` (landed #7026). LANDED with this sheet update: the 3 roster rows (reason + dissolution trigger each, the #6691 shape) are in `cli_run.rs`; receipt `nfr_roster_receipt` unrostered=0 stale=0 and the lens fn evaluates true by execution. The falsifier goes green on its next scheduled run and #7032 auto-closes — if it reds on anything else, that is a NEW find, not this one. — ⏳ awaiting sign-off
- [ ] **srv1 deploy keeps breaking and reds main while the floor is green** — 13 of 2026-07-22's 21 main-run failures are `deploy_dashboard_srv1`, not the floor: `gunbc-tree-sync.service` restart control-process failure (fix in flight: `session/sunny-owl-513`, receipt run 29928424523) after earlier `gunbc-roadmap.service` /healthz ServiceNotReady. Two halves: converge the srv1 units, and decide whether the deploy verdict should share the ci run's conclusion — main-green must mean code-green. The deeper fix is §2's CD-transport node; note `gunbc-roadmap.service` is also item 3's dispatch surface, so this fix is its prerequisite. UPDATE 2026-07-22 late: both halves of the unit fix are in — the tree-sync fix (16:34) and #7077's measured readiness bound (30s→120s; four srv1 starts at 35–39s prove the old bound red every SUCCESSFUL start). The tail executes for the first time on the first main run whose floor passes (post-#7080); check this row off on the first green deploy job, and keep the verdict-sharing question open. FIRST EXECUTIONS (21:49–21:50, both red, DIFFERENT causes — receipts from runs 29957321270/29957141552): (a) the #7077 run died at tree-sync — the .git/ rsync exited 23 (partial transfer) while a SECOND deploy job ran concurrently 18s later on another runner slot against the same /opt/gunbc target; deploy jobs have no concurrency group and ran out of commit order (newer commit deployed first, older second — even on success srv1 converges to the OLDER tree, last-writer-wins). NEW work: serialize the deploy (workflow concurrency group or host-side lock) and stop rsyncing .git/ from a live reusable runner checkout — the CD-transport node's case sharpens. (b) the #7080 run got past tree-sync, restarted the unit (active, 34s into its ~35–39s startup) and probed with the OLD 30s bound — its tree predates #7077; the known class, already fixed. The first honest 120s-bound deploy is the next main run carrying both fixes (#7058 merge, in flight at write time). THIRD EXECUTION (run 29961193892, 22:51): floor green a THIRD consecutive time, deploy red AGAIN at the same tree-sync .git rsync exit 23 — with NO concurrent deploy this time, which RECLASSIFIES the failure from race to a deterministic per-source-checkout condition: succeeded from srv1-03's checkout (21:49), failed twice from srv1-05's (21:49, 22:51). Leading suspect: service-user-unreadable files inside the runner checkout's .git/ (the oneshot reads ghrunner's checkout as User=service-user; a 0600 .git/config carrying the actions auth extraheader would explain exit 23 exactly — and note syncing .git/config to /opt/gunbc would be a CREDENTIAL LEAK if it ever succeeded, so excluding config from the .git sync is both the likely fix and a hardening). LOUDNESS gap, same class the status-tail was added for: the diagnosis shows exit 23 but never the per-file rsync errors (they live in the unit journal) — extend the capture to a journalctl tail so the failing paths are NAMED. The 120s readiness bound remains unexercised until tree-sync converges. RESOLUTION 2026-07-22 late: journal receipt named the root — ONE ghrunner-owned 0600 fetch marker (objects/pack/pack-25d0…keep) unreadable to the unit principal, deterministic on srv1-05; AND /opt/gunbc/gunbc/.git/config was carrying an actions extraheader auth token from the blanket sync (a real credential leak). Operator hand-scrubbed the token and removed the stale marker; the construction fix (whitelist .git sync — HEAD, objects minus fetch transients, packed-refs, refs, terminal exclude wall) MERGED with #7086, making both classes unwritable. Dashboard STAYS on srv1 (operator decision 2026-07-22); the srv3 move was assessed — redeploy-not-migration, ~1–2 days via HostIdentity spec parameterization shared with #7027 G1, plus service-account + claude-auth seeding — and parked. — ⏳ awaiting sign-off
- [ ] **first NativeRouted flip reverted by receipt — the frontier working as designed** — main run 29963619401 (#7069 merge, srv2-05) redded `emit_on_demand_family_crate_pr_native_agreement_holds` WARM (compile_skipped=true ×3, false in 496ms): a genuine native-vs-eval divergence or member-run failure, NOT cold cache. Remedy per design: complement + meet_join back to `InterpretedRetained agreement_red_on_main` — a two-row data edit, receipt on the carrier (`routed_flip_reverted_note`), investigation off the merge-critical path, falsifier cold control still exercising both legs. Named loudness gap to close BEFORE re-flip: the agreement fn's false carries no located member and no value pair — a divergence receipt must name which member and both answers. UPDATE 2026-07-23: the divergence carrier LANDED (v2.std.native_agreement — the receipt names the member and both runtime answers). REVIEW CORRECTION (adversarial pass 2026-07-23): the re-flip is NOT gated on the store alone — the frontier pair's own dissolve_on requires the divergence ROOT-CAUSED (member + both values named via the landed carrier) plus a fresh warm per-host agreement receipt first; the store (2b · ts-store-econ) gates the other nine families' FIRST flip, not this pair's re-flip.
- [ ] **queue + zombie hygiene** — ~18 runs/hour arriving against a 60–97-min green wall; main pushes queue behind PR traffic; cancel zombie queued run 50073 (`session/clever-hawk-862`, queued 2026-07-13); revisit main-push priority.
- [ ] **overnight main floor breakage healed** — the 00:44–02:04 cluster (DAG compile failure + ci_deploy_sudoers typecheck) was fixed on main by ~02:04; recorded so the day's red accounting separates it from the deploy-tail reds. — ⏳ awaiting sign-off
- [ ] **ROADMAP.md hand-edit drift healed** — #7071 hand-added its §0 lane-snapshot paragraph to the generated ROADMAP.md without touching `roadmap_authority.dag`, leaving main committed ≠ expected (the drift gate reds whenever it executes). Re-homed verbatim into the authority in this change; the open question — why the per-PR floor let a generated-artifact hand edit through — folds into the predict-skip row in item 2. — ⏳ awaiting sign-off
- [ ] **merge-wave floor reds root-caused and hotfixed** — the 16:11–17:31 wall of 15 main failures had two roots: (a) `generated_artifact_drift_gate` red from #7071 hand-editing the generated ROADMAP.md (healed by #7081 re-homing the paragraph), and (b) a stale `CommitWitnessClaim` roster row in `gunbc.commit_workflow` still enrolling the witness #7060 had moved to the long lane — every main run from 16:33Z failed with no-main-function-found (hotfixed #7079, which also cleared #7064 orphaned script-module import). THIRD collision: #7079 (branch cut before #7081) hand-deleted the same paragraph from the generated md as its own drift cleanup, so post-merge main drifts the OPPOSITE way — authority has the paragraph, committed md does not; #7082 closed it by regeneration (the freshness-block class exactly: green-on-branch is not green-on-main). Remaining for a green main push: the deploy tail verifies on the next run that reaches it, and the falsifier needs the nfr roster burn-down above. — ⏳ awaiting sign-off

**1a · Host state frontier — machines are stateful; declare the boundary (operator, 2026-07-22):**

- [ ] **the gap, named**: our deploy/subsumption workflows are causal scripts over host state we neither guarantee nor model — and host state IS effectively infinitely permutable, so any workflow whose correctness depends on unmodeled state is untenable. The doctrine already half-exists (membership-reconcile observe→diff→converge, gunbc.ownership, fail-closed observation à la belt_observe); the gap is that THE BOUNDARY OF THE MODELED STATE IS UNDECLARED — both 2026-07-22 tail incidents happened exactly there (reading foreign state without a contract: the runner checkout's live .git; blanket-copying foreign state into owned state: the leaked extraheader). Resolution is NOT model-everything (observe-all is the absorbing trap in reverse) — it is shrink-the-state-that-matters: content-addressed desired state + idempotent converge + readback, so accumulated host state becomes irrelevant wherever the spine applies. Children are the three mechanisms. RECEIPTS (2026-07-23, the srv4 incident — the gap fired live): srv4 OOM-killed PR CI (the runner save_state class). Diagnosis, two layers: (1) srv4 was NOT a modeled fleet member — no fleet_intent row, so the entire converge derivation chain could not name it (membership by hand-action; #7027 carries the enrollment); (2) the converge model has NO live actuator — KnobUnimplemented refusal at PerSlotMemoryCap, and the bash-emit path is unprovable standalone with zero executing CI consumers ('every fabric model is paper', now priced). Interim hand-fix applied with derived-equal values (swap 127G file, per-slot caps 16G/32G, oomd, reclaim — receipts in the gaps doc on #7027), every row owed a modeled axis with dissolution. The 0-to-3 dispatch brief issued 2026-07-23: kill the bash-emit transport (runtime-present hosts converge through typed effects only), close the loop vertically on PerSlotMemoryCap (idempotence + perturb-and-heal + foreign-occupant receipts on srv1), enroll srv4 + the enrollment wall (a host takes no fleet work without an intent row), and the axis-bundle framework — with interference DERIVED from provider fact rows on the one shared memory model (claims as verb-by-subtree facts, conflict = computed claim intersection; never a named domain or hand candidate roster — operator ruling 2026-07-23). Unmodeled occupants of a claimed resource position refuse as typed findings; TakeoverRuling is the only signed path from foreign occupant to owned teardown.
  - [ ] **declared state frontier per host-touching workflow** — every state input/output classified `Derived` (regenerable from spec; delete-and-recreate always legal) | `OwnedMember` (observed + reconciled by identity — the membership spine) | `ForeignWithContract` (someone else's process, read only through a NAMED contract); unclassified refuses. The tree-sync whitelist (#7086) is the case study: an implicit read of all of .git became a four-item declared contract — but by incident, not at authoring time. This is the Group-of-units taxonomy (5a) applied to host state; extends gunbc.ownership.
  - [ ] **anti-entropy cadence — the host falsifier** — drift is currently found only when a deploy trips on it (the stale .keep sat for weeks). Mirror the affected-set falsifier: a scheduled pass re-observes every enrolled member vs desired (readback) plus unowned-state-inside-owned-subtrees detection (would have caught the leaked config AND the stale server.js), divergences typed/located/counted. Seeds exist: host_hygiene_reaper, live_deploy readback.
  - [ ] **CD transport — stop deploying from a live runner workspace** — the shared root of both tail incidents: the runner checkout is a maximally stateful foreign entity and rsync-from-it is state coupling by construction. Deploy from the merged commit's CONTENT (fetch-by-hash from a store, or a clean host-side git fetch) — the whitelist narrowed the foreign-state contract; CD transport deletes it. Elevates §2's existing CD-transport node with this week's receipts.
  - [ ] **generation guard + subtree lease** — ordering and interference for concurrent effects on one host: apply refuses when the candidate desired-state is an ancestor of what is deployed (`StaleDesiredState` — the SAME refusal class as §2 merge-admission freshness: an effect admitted against observation O refuses if the world advanced past O — one authority, two consumers), and an effectful realization holds an exclusive write lease on its target subtree for the frame duration (effect-grants ⊑, modeled — not a shell flock). Interim: a workflow concurrency group on the deploy job (receipt: 21:49 two deploys overlapped, out of commit order).

**2 · CI actually functional — what it takes, what the roadmap already claims:**

- [ ] **definition — CI is functional when (a) a red means a real defect, (b) a green is trustworthy corpus-wide, and (c) the verdict arrives before merge.** Today none of the three hold: deploy reds mask code health (item 1), predict-skip hides corpus reds until the falsifier, and merges land 0–17 min after open while a green run takes 60–97 min.
- [ ] **kill the recurrence class — corpus-scan witnesses predict-skipped** — 4th occurrence per the nfr roster's own dated blocks: hygiene witnesses stamped `SubstrateInputsOnly` while their counters walk the whole corpus, so per-PR selection skips them and only the falsifier reds, days later. Decide once: re-stamp `ReadsLiveTree` (never-predict-skip; sub-ms at floor scale) vs falsifier-red as a merge blocker. Census machinery landed #7070. 5TH OCCURRENCE (2026-07-23): #7088 landed gate_in_cheap_floor_membership + spec_enrolls_gate unrostered in ci_floor_plan.dag; caught by an unrelated dag-touching PR running the whole-corpus nfr receipt (#7104), backfilled per the roster precedent.
- [ ] **CI is not a merge gate (re-evidence)** — 0–17-minute creation→merge latencies observed in the merge wave while a green run takes 60+ min, so verdicts land post-merge and main is the integration testbed; re-evidences §2's merge-admission HELD-state node — while it stays HELD and the falsifier is dark, the corpus has no pre-merge wall at all.
- [ ] **floor memory ceiling** — falsifier floor pinned at the 16 GiB cgroup memory.high with ~33 GB swap, sustained PSI stalls, and ~18 min of post-failure residual burn; #7000 (srv3 MemoryHigh/MemoryMax caps) landed 2026-07-22 — confirm the next falsifier runs reflect the caps; residual lineage #6848 → #7030.
- [ ] **already claimed in the roadmap — dispatch from these lanes, do not re-plan them**: §0 ① compile wall + ③ affected-set live in CI; §2 floor-throughput group (serial compile-clean wall · shard A ✓ / shard B · within-run double-resolve · resolve-cache default-on) targeting floor <1min; §2 workload-class admission + cap/admission de-conflation; §3 audit rows (affected-set soundness receipts · one tree one verdict · cache honesty re-receipt); the CI-onto-v2 native-routing lane (#7069, parity-gated). What the audit adds is only sequencing: falsifier green first (item 1), then selection honesty, then speed — a fast CI that lies is worse than a slow one.
- [ ] **the merge-gate options — pick one deliberately**: (i) GitHub-required check + merge queue, once the floor is fast enough to gate (needs §2 floor-throughput); (ii) keep post-merge CI but close §2 merge-admission — freshness block un-HELD — so admission is modeled rather than GitHub-native; (iii) interim, available today: falsifier-red blocks merges by convention. Doing none is the current state, and the last two weeks are its receipt.
- [ ] **CI ergonomics — the operator loop is clunky (operator, 2026-07-22)** — using CI today means digging raw run logs for the red witness, hand-correlating roster/selection/receipt state, ad-hoc local claim_batch incantations, and minutes-to-hours of latency before any signal. Open the lane by inventorying the operator touchpoints and pricing each (§6 displaced cost): where is the red — one link from the PR to the failing witness and its receipt, never a log dig · one local command == CI floor (same selection, same receipts) · floor/falsifier/deploy state visible at a glance (a dashboard tile on the srv1 roadmap — rides item 3 serving surface) · re-run and bisect ergonomics (re-kick one witness, not the whole floor). Existing machinery to feed on: logs-on-failure #7055, the gantt/receipt emissions, the belt dashboard.

**2a · Witness corpus onto v2-native — the bulk arc (operator re-priced 2026-07-22):**

- [ ] **the arc: rail → census → seams → one bulk flip PR.** Today 0% of the ~2,200-witness corpus executes native — all of it runs through the v1 interpreter (~30 of the 41-min ci job). Operator re-pricing (2026-07-22): parity windows demote from flip-precondition to post-flip monitoring — no customers, so a divergence should land immediately and loudly, never drip-feed. The in-PR receipt is ONE agreement run (both legs, hard equality), and a native-vs-interpreted disagreement FAILS THE FLOOR loudly — never a counted-and-continue row (the §5 wall that replaces the windows). Cache posture: staleness is a cache-key bug with a standing detector (the 4-hourly falsifier cold control, unchanged), not a reason to fear the cache; R2 (real toolchain identity in the `.native_ready` key) rides #7069 and lands before the flip. Sizing (sharp-bee-290, 2026-07-22): ~4–6 working days after #7069 merges; owner session started by operator.
  - [ ] **eligibility census — measure, never guess**: grain is the ENTRY CLOSURE — 744 entry files (337 dag/test/claim + 407 src/v2/test/claim), not 2,200 witnesses. Hard ceiling ~90%: 77 ReadsLiveTree + 35 host-effect entries stay InterpretedRetained host_fed — native host-effect transport is its own later lane, deliberately uncoupled. The mechanical sweep exists (curated_cargo_probe_one.sh spine); 16-entry stratified sample first, full 744 sweep wide on srvN (~2–4h) → per-entry first-error TSV = the enrollment census, whose first-error classes become the EmitIneligible refusal vocabulary. UPDATE (review 2026-07-23): the 744 = 337 + 407 literals are STALE — the live census carrier reads 876 entries (docs/probes/witness_entry_eligibility_histogram.txt: 698 bulk-pending · 146 EmitIneligible · 24 EmitOnDemandFamilyGrain · 6 HostFedCeiling · 2 RetainedReadsLiveTreeCarrier), the 77/35 ReadsLiveTree/host-effect split predates the classifier, and first_error_class is still CensusPending on essentially every row — the full srvN sweep has NOT landed. The denominator must be the DERIVED census count, never a literal (group 5a), else the bulk-flip totality wall pins to a stale number.
  - [ ] **executor seams** — family cold-build as a scheduled node the governor prices separately from member runs; per-witness leg attribution in floor receipts (native family/member vs interpreted — without it the flip is unobservable); typed EmitIneligible first-error refusal rows so residue is a prioritized list, never a silent skip. Crate grain is measurement-settled (#6879): per-witness LOSES (~17s cargo apiece); one-corpus-crate barred by real fn-name collisions; ~15–30 family crates clustered by shared closure, durable via GUNBC_NATIVE_CACHE_ROOT so srvN pays each cold build approximately once ever.
  - [ ] **the bulk flip PR — one PR, three sections**: (1) census as data — all 744 entries classified on the routing frontier, no entry unlisted (totality wall), rows landed in DERIVED Group form (DeclarationRef subjects, closure-derived membership — never 744 fresh string nicknames; group 5a); (2) the executor seams; (3) the disposition flip with the in-PR agreement receipt and the falsifier cold control untouched. Expected effect: the ~30-min interpreter leg shrinks toward the residue share (~10% host-fed + sweep-named ineligibles); family builds amortize to ~zero on the durable cache. UPDATE (review 2026-07-23): the totality denominator is the derived census count (876 entries at review — see the census row), never the 744 literal.

**2b · Interpreter deletion endgame — Track B past strong self-host (operator-ruled 2026-07-23):**

- [ ] **Track B — delete the v1 interpreter; the three deletions named apart** — the phrase delete-v1 conflated three finish lines (a §3 state-space conflation in our own vocabulary), now split: (a) COMPILER — the §1 gates lane (Weak → Strong Self Host), undisturbed, removes only the interpreter's emitter-host role; (b) INTERPRETER — this lane: the floor's eval + host-dispatch roles peeled family-by-family onto emit-on-demand (2a is the bulk arc; §0-④ execution-as-realization is the doctrine — interpreted-vs-compiled is a per-node realization policy, never an architecture); (c) HOST-PHYSICS — a pinned v2-EMITTED bootstrap kernel (seed census: compiler logic emitted, harness pinned, never hand-maintained). Terminal receipt for (b): the execution-routing frontier at zero — every family NativeRouted or typed-retained with a live dissolve trigger. REVIEW CORRECTIONS (adversarial pass 2026-07-23): (1) the deletion bar's authority is dag/gunbc/v1_deletion_plan.dag (prereq_drift_ruling_2026_07_23: the three gates ARE the interpreter-deletion bar) — this lane's new conditions land INTO that carrier as PlanMilestone bricks, never a second prose bar; and Gate 1 there is GateEmitterFixedPoint (the emitter re-emits itself, behavioral 2-generation), NOT 'nothing needs the tree-walker to emit' — regen already runs generated Rust; the tree-walker's emit-side residue is the compile-time-eval role + the emit_host transport builtins. (2) the roles carrier is dag/std/emit_on_demand.dag interpreter_surviving_roles — 3 declared roles under a ceiling of 8, not four prose roles — and the wet-workflow surfaces (gunbc serve/belt · main_wet regen · gunbc ci · pre-push · probe bins) are live interpreter consumers with NO roster row today: enroll them as a counted fourth role so the delete bar counts every execution surface, never only the floor. v1_interpreter.rs (10,523 LOC) deletes when a+b+c hold; the counted frontier + the roles roster are the only progress metric, never prose.
  - [ ] **operator ruling 2026-07-23: hand-MAINTAINED Rust goes to exactly zero** — the pinned kernel is emitted from cited models; hand-maintenance is a transitional frontier, never a terminus; the external trust residue stays cited, never owned (rustc, stdlib, OS). Consequences: HAND_MAINTAINED_STAGE0_FILES (25 files + module_path_index, currently GROWING per cli-run-hollowing managed-seed-growth) becomes a counted burn-down roster, and a NEW hand-Rust addition lands only with a named emitted-or-collapsed dissolution trigger — a review bar, same class as group-5a registry admission. Two doc contradictions to fix on their carriers: interpreter-kernel-d section 2's collapse-vs-pin either-or RESOLVES to collapse-onto-the-materialization-kernel (one CacheLookupResult fold, §3 one authority; pin-as-emitted is the fallback only if the fold proves unfeasible), and witness-realization P4's claim that claim_executor is not-a-self-host-emit-target with 0 rows is a TRANSITIONAL status misstated as identity — the census already rules the kernel v2-emitted, so the sentence corrects to not-YET and the executor eventually gains emit rows (cli_run hollowing is that runway). THIRD carrier contradiction (review 2026-07-23): v1_deletion_plan.dag ^hand_queue_drain still reads '7 files + module_path_index (~22.3k LOC)' against the live 25-file roster AND rules the host-physics pair 'terminal-kernel pins, NOT drain targets' — under this ruling that brick re-words to collapse-first with pin-as-EMITTED fallback; fix on that carrier with the other two.
  - [ ] **operator ruling 2026-07-23: effects are per-language library models — effect providers, emitted like everything else** — nothing dramatic: each target language's libraries are modeled (extdeps.languages rows; HostTransportDescriptor is the existing instance class) and the emitter renders effect calls against them; TargetModel (the runtime_row class, #5075) or an equivalent abstraction carries the provider interface. Status: filesystem_read + shell_exec_run landed as emit_host transports with executing byte-equality receipts (#7099); each remaining family is a modeling row + transport wiring, never new architecture — but the '~9 families' denominator is prose on no carrier (EmittedEffectFamily is a closed 2-variant sum today): per group 5a the family roster must be DERIVED (a census over the service-op families the witness corpus actually consumes — the eligibility census's host-effect classes) before 'remaining ~7' is a countable burn-down. This is lane (b)'s physics half; the hermetic story rides the same rows — fixture replay realized against the store instead of interpreter machinery.
  - [ ] **the durable BUDGETED store gates the 2a bulk flip (the nine economic families) — the lane's critical-path head** — 9 of 11 InterpretedRetained families' reason is literally no_cached_no_evict_carrier (the frontier's own rows; complement + meet_join sit at agreement_red_on_main — the reverted pair, whose re-flip additionally needs its divergence root-caused, see the flip-revert row): the equals_eval legs are uncached, so routing them native would rebuild every PR run. Today the DEFAULT native cache root is /tmp scratch; ci.yml pins GUNBC_NATIVE_CACHE_ROOT to the runner tool-cache (durable per-runner, the R2 toolchain key) but the tier is LifecycleByConvention, unbudgeted, per-workspace-evict only — and claim_executor consults NO store before choosing to interpret an entry (the consult lives inside the wet transport, never the executor's routing decision). Work, in order: (i) the artifact store's persistent SpacePacked tier gets a real budget + root — the P2 fold is landed and green IN-PROCESS, but the persistent tier is unrealizable by P2's own named gap (the cited Filesystem authority has no Delete/List, so the disk tier grows unbounded): modeling Filesystem Delete/List + realizing counted eviction is IN this step — modeling plus wiring, not wiring alone; (ii) claim_executor consults it: hit = run cached native, miss = emit-build-store once per content hash — which also makes the interpreted emitter's slowness a one-time cost, decoupling this lane from strong self-host; (iii) equals_eval agreement receipts become per-PR-affordable warm. Accept (T2→T5): claim_executor consults the budgeted tier on a real PR run (hit = cached native with zero cargo; miss = emit-build-store once); an over-budget put triggers counted typed eviction, never unbounded growth; RED: delete the store root → the next run rebuilds and restores with byte-identical cached-vs-cold (the purity oracle). Convergence receipt owed: resolved_graph_cache + recorded_fixture dissolve INTO this carrier per kernel-D's CacheLookupResult fold; the schedule-retention eviction (#7129 — in flight, NOT on main) and the compile-wall-endgame W3 cross-process typed-module store land INTO the same carrier, never beside it — one cache authority, not four.
  - [ ] **materialization running in CI — the kernel both substrates consume** — the realize/CacheLookupResult fold consumed by the v1 handlers AND v2 05_eval (kernel-D's named dissolution point for the pinned caches), the ComputationIdentity qualifier live inside materialize (duplicate-work lane), and scheduling from derived space bounds (realize_pack landed; the executor cutover seam is priced in witness-realization P4 — the executor already interprets .dag, so it calls realize_pack through the bridge it has). Sequencing note (review 2026-07-23): kernel-D's dissolution trigger is still written either-or (collapse OR pin) — land the ts-zero-hand carrier fix first so this row dispatches against collapse-first, not the stale either-or. Watch-flag CORRECTION (review 2026-07-23): the in-tree carrier still reads unkeyed demand at 47.6 percent of calls (docs/probes/ci_floor_redundancy_ledger_skeleton_2026-07-23.tsv, run 29976989996); the near-zero partial-run figures circulated 2026-07-23 (0.6, ~0.2) landed on NO carrier — land the fresh receipt with the first clean full run BEFORE repricing the identity lane.
  - [ ] **the delete itself** — v1_interpreter.rs deletes when: the v1_deletion_plan three gates read done (Gate 1 = the emitter re-emits itself — the carrier's definition, see the anchor correction), the 2a flip plus the effects families are native (eval + dispatch roles peeled), the NON-FLOOR wet surfaces are routed or retired (gunbc serve/belt · main_wet regen · gunbc ci · pre-push · probe bins — the roles-roster enrollment from the anchor row; a surface with no roster row blocks the delete by construction), the materialization kernel is live in CI (the caches collapsed), and the hand roster is burned down to the emitted kernel. NOTE (review 2026-07-23): the plan carrier has no interpreter-file-only milestone today — src/v1 deletes wholesale at ^v1_delete via ^ci_floor_cutover + ^v1_quarantine; if the interpreter file is meant to go FIRST, add its brick to v1_deletion_plan.dag explicitly. Not a switch-flip, by construction: parity evidence rides the 2a re-pricing (one loud in-PR agreement receipt; falsifier cold control unchanged; a divergence fails the floor, never counted-and-continue). cli_run hollowing (27,877 lines, the largest hand file) proceeds in parallel on the zero-hand path — it is NOT on the interpreter's critical path and must not be sequenced as if it were.

**2c · Lenses live in CI — the v2-door dependency + enforcement economics (operator milestone 2026-07-23):**

- [ ] **all lenses working in CI, enforcing actively — the v2-door dependency named precisely** — a lens is a v2-door construct: `always_required_root_lenses` (accumulator_copy · determinism · machine_shape · mandatory_tag) plus the per-subtree pair (fact_density · unit_modeling) fire inside `v2.compiler.compile` validate_then_compile — while the CI floor's corpus gate (`dag_compile_clean_gate`) compiles via the v1 seed path (the cli_run.rs floor receipt literally stamps empty_complexity_report), so TODAY NO lens enforces on the corpus compile gate; that is the whole 'complexity lens only enables via v2' dependency. Three stacked gates, each its own row below: the DOOR (the floor compile gate goes through validate_then_compile at the changed-closure grain), the ECONOMICS (whole-tree/cold v2-door compile is the pay-every-run shape §0-④ kills — it rides 2b ts-store-econ so warm runs are content-hash hits), and the RECEIPTS (complexity repo-wide's scope leg is red-by-design until fn-body reflection lands via the v2 ingest lane and the cost lens prices real callees — today every call prices unit_cost, so a quadratic loop reads linear; general-body-producer is the carrier). ◆ Milestones: M-L1 v2-door per-PR on the changed closure, six always-required lenses enforcing, planted-defect RED by execution → M-L2 whole-tree v2-door baseline on the falsifier cadence, store-priced → M-L3 every registered lens Blocking-with-executing-consumer or a typed Frontier row with dissolve trigger → M-L4 complexity repo-wide StandingIntent scope leg GREEN → TERMINAL: the enforcement-intent gate green over the whole lens registry — every lens wired (a discovered fail-closed witness) or deleted, DESIGN §6.
  - [ ] **M-L1 — the floor compile gate goes through the v2 door (changed-closure grain)** — route `dag_compile_clean` through validate_then_compile so the six always-required lenses fire on every PR's affected closure with typed located refusals; the v1 seed gate stays as the falsifier cold control until M-L2 retires it (two doors one verdict, divergence a counted red — the same both-legs pattern as the native flip). Accept (T2→T5): a planted accumulator-copy defect in a scratch module REDS the per-PR floor by execution; an unrelated PR stays green; wall-clock delta receipt on a scoped PR; RED control: de-enroll one always-required lens → the coverage witness fails. Economics note: the changed-closure grain may be affordable interpreted today (receipt decides); the whole-tree grain waits on the store (the M-L2 edge).
    - [ ] **M-L2 — whole-tree v2-door baseline on the falsifier cadence** — the corpus-wide lens verdict as the cold control, priced by the durable store: v2-door compile of the whole tree is the pay-every-run shape until compile results Share by content-hash (2b ts-store-econ + the compile-wall-endgame W3 typed-module store are the same carrier convergence); enrolling it un-priced re-creates the 100-min wall §0-① exists to kill. Lands as a scheduled falsifier-cadence node first (never per-PR), with per-lens verdict counts in the receipt so 'enforcing corpus-wide' is a counted fact, never prose.
    - [ ] **M-L4 — complexity repo-wide scope leg green** — standing_intent_complexity_repo_wide claims Blocking/FloorGate over dag + src/v1 + src/v2 while the live witness deliberately EXPECTS leg_fails on scope_covered — red-by-design until (a) fn-body reflection lands via the v2 front-end ingest lane (fn_body_reflection_blocker: decl_facts marshals only the fn output skeleton today; bodies arrive via ingest, not a new host builtin) and (b) the cost lens prices real callees (general-body-producer: today every call prices unit_cost, so a loop over list_append reads linear even when quadratic). The flip is a counted un-quarantine event (the expected-red inverts deliberately), never a silent green.
  - [ ] **M-L3 — contract truth: no inert lens, no AuditOnly-by-default** — the registry's LensContract roster reads ~2 Blocking-with-consumer against 46 AuditOnly/NoConsumerWitness rows (an enrolled-but-unreceipted frontier by construction, gated on fn_body_reflection_blocker — contract.dag's own pending note); the #5433 inert-lens hygiene witness lives in test/claim/long (excluded from the per-PR floor; its Rust twin left CI with nextest), and enforcement_live + the accumulator-copy + identity-captured-navigation roster gates are operator-ruled OFFLINE (15–22min corpus-denominated walks, ci_layer_roots exclusion rows). Work: re-home the hygiene backstop to an enforcing cadence (falsifier or per-PR ReadsLiveTree); bound + re-enroll (or deliberately delete) each OFFLINE row; flip each contract to Blocking with a named executing consumer or leave it a typed Frontier row with reason + dissolve-on (the #7089 shape) — an unrostered AuditOnly contract becomes the review reject.

**3 · Work intake — dispatch from the roadmap, not ad hoc:**

- [ ] **roadmap-driven dispatch — the machinery is LANDED; three gaps to go-live (assessed 2026-07-22)**: `gunbc serve` on srv1 serves the LIVE roadmap with `POST /dispatch/NODE_ID` invoking `belt_dispatch_node` in-process; the belt observes fail-closed (tmux ls), reconciles the ready frontier, and spawns `git worktree add` + tmux running claude with the FILLED dispatch brief as appended system prompt (effort mapped from node Intricacy), deterministic session UUID, typed refusals throughout; `belt_run_once_json` is the CLI tick for receipts. Gaps: (1) TREE FRESHNESS — srv1's tree/binary have been stale since belt B merged because every deploy failed; unblocks with the first green deploy post-#7086; (2) ONE LIVE END-TO-END RECEIPT — never collected with real claude (tests used a fake): ssh srv1, run belt_run_once_json once, converge whatever typed refusals it names (likely tmux/claude/claude-auth for the service user); (3) SIZED NODES — dispatchability requires intricacy/volume + Accept line + deps-done; the task-sheet authored() rows are unsized tracking rows BY DESIGN, so size the lane nodes you actually want dispatched. Auth precondition: 3b's dispatch-auth row before the button is exposed beyond the tailnet. GO-LIVE RECEIPT (2026-07-23, live on srv1): gap 2 CLOSED by execution — `POST /dispatch/2-dispatch-actuator` → `spawned`; independent read-back: `tmux ls` shows `gunbc-dispatch-2-dispatch-actuator`, worktree at `/opt/gunbc/dispatch-worktrees/2-dispatch-actuator` on branch `dispatch/2-dispatch-actuator`, REAL claude v2.1.170 running with the filled brief (`--effort high`, deterministic session UUID) and its transcript file under `~/.claude/projects/-opt-gunbc-dispatch-worktrees-2-dispatch-actuator/`; second POST → `already_live` (idempotent); bogus node id → `node_not_found` (typed RED, live). One refusal converged: claude's per-path workspace-trust dialog blocked the first spawn (`--dangerously-skip-permissions` does NOT cover it — same finding as ctrl's container entrypoint trust seed); ancestor-dir trust inheritance was proven by execution (seeded parent → child skips the dialog), so the ONE root `/opt/gunbc/dispatch-worktrees` is now seeded in srv1's `~/.claude.json` — interim hand-provisioning of the same class as the host_layout paths, dissolves into the live_deploy membership. Gap 3 receipt: the live ready-set serves 13 sized ready nodes. Gap 1 CLOSED by execution: the #7091 main run (29967907137) went green through the deploy tail — the first green `deploy_dashboard_srv1` post-#7086 — and srv1 converged by independent read: `/opt/gunbc/gunbc` HEAD == main HEAD 960bc8548, binary refreshed, unit active, `/healthz` ok. NEW GAP found live: re-dispatch after Stop is broken — own row below. CLAUDE DEPENDENCY MODELED (2026-07-23, same PR): the trust hand-seed now has a modeled home — `extdeps.llm.cli` carries claude's trust-store facts (`~/.claude.json` projects map, flag `hasTrustDialogAccepted`, ancestor-inheritance; classifier reuses the JSON grammar's string-aware value parser, not a brace scan) and the belt PREFLIGHTS claude-on-PATH + trust-seed-for-worktree-root before any actuation (typed `spawn_failed` steps `claude-cli`/`claude-trust-*` — previously a missing claude spawned a tmux pane that died instantly behind a fabricated `spawned`, and an unseeded root HUNG the session on the dialog); refuse-with-instructions, not auto-seed (the store is claude's own runtime-mutable file — a read-modify-write races the upstream writer), so seeding itself still dissolves into live_deploy. UI FORK FOUND LIVE + FIXED (2026-07-23): the dispatch button read the retired belt-A `ok`/`node` response shape while belt B answers `status`, so EVERY successful click rendered 'failed' (the operator's `5-regen-cutover` click had in fact spawned); the page now derives its ok-predicate from `belt_dispatch_ok_status_labels` — one wire vocabulary with the serve JSON (`belt_dispatch_status_label`), witness-glued to `belt_dispatch_result_ok`, page witness holds a RED on `j.ok`. — ⏳ awaiting sign-off
- [ ] **re-dispatch after Stop is broken — the dispatch loop is one-shot per node (found live 2026-07-23)**: two failure classes, both proven by execution on srv1. (a) DEBRIS — Stop (`tmux kill-session`) leaves the worktree and the `dispatch/<node_id>` branch behind, so the next POST is a typed `spawn_failed` (step git, `a branch named 'dispatch/…' already exists`); teardown/spawn must own the worktree+branch lifecycle (observe-then-decide, the same upsert_decision shape as session_lease — a worktree with uncommitted work must REFUSE removal, never silently delete). (b) SESSION-ID REUSE — after hand-cleaning worktree+branch, re-dispatch spawns tmux but claude exits 1: `Session ID <uuid> is already in use` — the deterministic per-node UUID collides with the archived transcript, so a node is one-shot per host at the claude layer too. Fix direction: dedup does NOT depend on UUID determinism (observe keys on the tmux session NAME), so either resume the prior session on re-dispatch (`--resume <uuid>` — continues the node's prior work, arguably the RIGHT semantic) or derive a per-attempt UUID; either way the choice must be a typed observation arm, not a fabricated fresh-spawn. RED controls: re-POST after Stop must not be `spawn_failed`; a worktree with uncommitted changes must refuse teardown.
- [ ] **UI modeling migration — kill the hand layer (string-JS, raw values, concat microcopy); unlock the component register; land /sandbox** (built on #7096's wire vocabulary). VERDICT (per ts-dispatch-verdict — the operator's review sets the actual status; this row presents the fields, MERGE IS NOT DONE): candidate **AcceptedWithResidue** — all five phases shipped green by execution, the three named residues below are the counted follow-on rows so the item STAYS OPEN. (1) product review: accomplishes the brief's P0–P5 and acceptance 1–6; nothing blocked — the residues are deliberate deferrals, not gaps. (2) modeling audit: adds the component concept, the scale token axis, and the wire-label + query authorities, all grounded (no anemic leaf minted). (3) planning update: the residues are the follow-ons. P0 MODEL: the component concept (`gunbc.design.component`) as a register sibling of the surface archetype — element × role/material × interaction STATES × wire-contract binding; DispatchButton (`gunbc.roadmap_component`) with states DERIVED from `belt_dispatch_all_status_labels` (a new authority grounded one-exemplar-per-variant in `belt_dispatch_status_label`), totality cross-checked both ways with a located RED; spacing/type/font + dimension token scales added (`gunbc.design.scale`). P1 STRING-JS→TSPROGRAM: `dispatch_client_program` is one modeled TsProgram (ok-predicate a ts_binop `||` chain over `belt_dispatch_ok_status_labels`), EMITTED as an external served asset referenced by <script src> (operator steer: inline is a hack) — the string `dispatch_client_js` + its scaffold row deleted; script census wall refuses inline hand-JS + rogue src (two REDs); #7096's j.ok RED preserved on the served asset. P2 TOKENS: every roadmap_style px/font routes through a scale var, proven visually-neutral by execution (resolving the tokens reproduces the pre-P2 CSS byte-for-byte), gated by an untokened-length census (mirror of unthemed-color) with a RED; existing register walls stay green. P3 PRESENTATION: reserved button min-width is a projection of the longest wire label (kills the layout jump), ok/refusal/requested chip materials paint the P0 state's role (BoundaryRole added to the archetype demand), located fields (step/detail) surface to a title attr not console-only. P4 SANDBOX: GET /sandbox renders the register at every depth + the component gallery (one static instance per wire label, totality RED) + one live instance per arm; POST /sandbox/echo?status=LABEL answers any wire arm with the production belt JSON (fail-closed 400 on unknown). P5 DISPOSITION: the dead belt-A roadmap node-http emit + its smuggled-exec dispatch route DELETED; the module survives only for the gunbhub static emit, stamped with an explicit dissolution row. LOCAL RECEIPT (gunbc serve, scratch port): GET / /roadmap /sandbox render 200; /assets/*.js 200 text/javascript; POST /sandbox/echo for all six arms returns the belt JSON (spawn_failed carries step+detail), bogus → 400. RESIDUE (named, within shipped phases — not un-shipped phases): P3 separators stay tokened per-child margins, the flex-container 'by construction' refactor deferred (it changes node-head title-wrap, unverifiable without a browser — §5); P2 length census scopes to roadmap_instance_rules, so `gunbc.site.moodboard.figure_rules` border/radius px is moodboard's own dimension-token lift (sibling of the color lift); P5 gunbhub's node_http emit remains, its belt-B migration the named follow-on. Instances of `ts-concat-class` (the dispatch string-JS) and the unconsumed-component/single-authority classes killed here. ANEMIA DISSOLUTION (D1-D5, operator + audit 2026-07-23, same PR; PURE RE-GROUNDING, emitted bytes and wire strings unchanged, each item with its receipt): D1 MediaType — the fused HttpContentType enum (media-type x charset flattened into variant NAMES) DELETED; extdeps.http.server models it as RFC 9110 section 8.3 says (top_level a closed RFC 6838 registry enum, subtype an upstream string, parameters a list, charset a cited IANA parameter row), and http_content_type_wire_label becomes media_type_wire_label serializing over the structure (five presets pinned byte-exact, perturbed-parameter and perturbed-subtype REDs; ApplicationOctetStream's charset-less form falls out of an empty parameter list). D2 typed HTTP status — belt_dispatch_status and serve_text_response re-grounded on HttpStatus, the bare Int surviving only at the ServeWireResponse wire seam; per-variant codes 200/200/404/409/503/502 pinned. D3 CssLength — new extdeps.languages.css.values (cited CSS Values and Units + Fonts 4) types ScaleToken.value as CssLength (magnitude and unit) or CssFontStack (a family list), and the value-encoded token NAME is derived from the magnitude by length_token (space-4 from px 4) so the name-vs-value fork is unwritable; dispatch_reserved_width migrates to css_ch. D4 exemplar roster — belt_dispatch_result_exemplars enrolled DeclaredFrontier in gunbc.roster_registry (reason plus dissolve_on), with a reconciliation witness pinning the exemplar-derived label set against the wire-contract and a missing-variant RED. D5 CSS grain — new extdeps.languages.css.properties (CssPropertyName over the register's emitted set, plus CustomProperty for author vars and a counted RawProperty escape) and .selectors (SelectorSubject = ClassSelector or ElementSelector or PseudoSuffix or a counted RawSelector frontier); material.CssDecl.prop and BuildRule subject re-grounded, every consumer migrated, stylesheet byte-identical. RESIDUE (counted, honest): RawSelector 18 (descendant/compound/functional-pseudo, awaiting the combinator grammar) and RawProperty 1 (accent-study code-spacing); the CSS VALUE grammar (decl right-hand sides), Content-Type forward-parse, and gunbhub's node_http belt-B migration stay named arcs. LOCAL RECEIPT (gunbc serve scratch port): GET / and GET /sandbox answer 200 text/html; charset=utf-8; POST /sandbox/echo?status=spawn_failed answers 200 application/json; charset=utf-8 carrying the belt shape with step and detail; POST /dispatch/bogus answers 404 application/json; charset=utf-8.
- [ ] **until the actuator lands, this sheet is the intake queue** — new work enters as a row here or as a sized lane node with an Accept line, never ad hoc mid-session; dispatch top-down by the numbering; a session that discovers work files a row instead of chasing it.
- [ ] **each dispatched item is a real workflow, not a single hop (operator, 2026-07-23)** — stages: MODEL first (the concept/authority delta plus the acceptance claims, in a scratch file or its own PR), BRIEF generated from the row + model with the acceptance bar as checkable claims, IMPLEMENT, ANTAGONISTIC REVIEW against the brief (never against the PR's self-description), LAND with the verdict updating this sheet. The #7087 incident is the pattern to kill: worker hits design gaps, does half, flags the gaps honestly, the PR merges — and the item silently reads done. That is the absorbing fallback at the management layer: cannot satisfy the brief, so land what exists and let merge stand in for completion. The wall: MERGE IS NOT DONE — a merge certifies the diff is safe to land; only a review verdict moves an item's status. OPERATOR SHARPENING (2026-07-23): the MODEL stage is unskippable and FIRST — thorough modeling, DFS-associated to authorities that already exist, created holistically where nothing exists, to the citation bar set 2026-07-23 (the ed25519 precedent: every referenced upstream entity carries its OWN authority, never only its consumer's); a brief without the model stage's output is not dispatchable. Group 3c is this discipline's machine.
  - [ ] **typed review verdict is the only status authority** — every dispatched item closes with the operator's three questions as the verdict's fields (operator, 2026-07-23): (1) antagonistic product review — did the PR accomplish what the brief asked; if not, what remains, what blocked it, what context/planning updates follow; (2) modeling audit — what models it adds, whether it should add more, anemia check; (3) planning update — the verdict lands on this sheet so the next session inherits it (sessions are goldfish; the sheet is the anchor). Vocabulary: Accepted | AcceptedWithResidue — residue becomes counted rows and the item STAYS OPEN | NeedsRework — return-to stage + located debts | Abandoned — reason. AcceptedWithResidue is the honest form of the half-done merge; merged residue with no row is the violation.
  - [ ] **rework is a first-class transition, designed against LLM defensiveness (operator concern, priced up front)** — backing up from step 10 to step 2 must be cheap and expected, never a negotiation. Three mechanisms: (a) the BRIEF declares rework a normal outcome, so the expectation is set at dispatch, not sprung at review; (b) a NeedsRework verdict dispatches a FRESH session carrying the original brief + the prior attempt's diff + the verdict's located debts — never ask the context that produced the work to unwind it (in-context sunk cost is the defensiveness mechanism; the goldfish property becomes the asset); (c) stage artifacts make backup a re-derivation — the model artifact is the authority and later stages derive from it, so returning to step 2 means edit the model and regenerate downstream, the house model-then-derive posture applied to the workflow itself (the same reason regen heals drift).

**3b · Access + compute fabric — no hidden transport dependencies (operator, 2026-07-22):**

- [ ] **access is currently a transport accident, not a model** — the dashboard's only wall is tailnet membership (the unit binds 0.0.0.0:8080, so any LAN peer reaches it; `POST /dispatch/NODE_ID` spawns a permission-skipping claude session with NO auth of its own), and the tailnet is the operator's personal one — a hidden dependency of exactly the class the operator ruled against. Holistic shape: model access as grants on the compute fabric — tailscale is FINE as a realization, but it must be a MODELED access pattern (who can reach which serving surface / press which actuation) with the transport as one handler, never the implicit wall. Grounds in the visibility-grants thread (`Publish` verb, AudienceScopeTree) and effect-grants (verb × subtree admission on the dispatch route); the belt's own note already names the destination ('nicer later as compute fabric', operator 2026-07-18).
  - [ ] **org tailnet** — operator creates it (not done yet, 2026-07-22); repo side = re-point `fleet_intent_network.tailnet_domain` from the personal tailnet and model the tailnet as a cited access realization row, the same personal→infra decoupling as retiring `service_user: briansrls`. Until then srv1.tailecbe08.ts.net stands, and the LAN IP:port works for the operator directly (the unit already binds 0.0.0.0).
  - [ ] **dispatch actuation auth is a go-live precondition** — before the Dispatch button matters, who can press it must be a modeled fact: the POST route gains its own admission check (grant-based, audience-scoped), never inherits whatever the network provides. Do NOT drop the tailscale front before this lands — it is the only wall in front of an actuation endpoint.

**3c · Workflow shape — dispatch items as typed workflows (operator-gated: opens after the first live dispatch round-trips):**

- [ ] **the lane: mechanize the dispatch lifecycle** (operator directive 2026-07-23) — the lifecycle prose rows in group 3 (ts-dispatch-lifecycle/verdict/rework) are the DISCIPLINE; this lane is the MACHINE: typed stages and verdicts on the roadmap node carrier, enforced at dispatch time by the belt and at floor time by lenses, with status and progress DERIVED, never written. Reconciliation seams (checked against in-flight work 2026-07-23): the UI-model branch claude/ui-modeling-hand-layer-o0mhyn (#7113 P0-P5 landed + the D1-D5 dissolution addendum pending) owns the PRESENTATION half — component register, sandbox, tokens, typed wire — and this lane's surfaces render THROUGH that machinery, never beside it. ts-dispatch-redispatch (group 3) is the stage-0 dependency: repeated dispatch must work before workflow state on top of it means anything. Terminal absorption: the standing rules here dissolve into enforcement-intent StandingIntent rows when that thread lands (DESIGN open thread); verdict capture from text rides the md-to-dag delta thread (module-identity lane). Neither is built here — declared convergences, not forks.
- [ ] **node schema decomposition — kill the prose blob at the model grain** — RoadmapNode content decomposes into typed fields: title / outcome / acceptance / receipts / deps / authored-at + last-verified. Freshness falls out: dispatch revalidates a row whose receipts predate its lane's last landing (priced by the 5-regen-cutover stale dispatch, 2026-07-23: a three-week-stale row reached a live session). The #7113 branch fixes PRESENTATION anemia; this row fixes the MODEL anemia under it. Accept: fields modeled with the blob deleted or derived (no dual carrier); page + ROADMAP.md project from fields; witnesses green.
  - [ ] **the DAG progress bar as pure projection** — verdict-owned done bits, Volume-weighted, rolled up the existing RoadmapEdge fan-in; per-subtree completion with fan-out/fan-in visible (operator UI direction 2026-07-22). No stored completion percentage anywhere — a derived read on the same tree the belt walks, rendered via the register/component machinery the UI-model branch lands. Accept: sandbox gallery shows the projection on a synthetic subtree with known fractions; live page renders it for the real tree.
- [ ] **the typed stage chain, status derived** — ModelArtifact (the DFS-association output: per-concept refs to the authorities the work grounds on, created holistically at the citation bar where nothing exists) then Brief (GENERATED from row + model, declaring rework a normal outcome) then Attempt (PR ref) then Verdict (Accepted | AcceptedWithResidue with counted rows | NeedsRework with return-to stage + located debts | Abandoned with reason). THE VERDICT IS THE ONLY STATUS MOVER — done is derived from the verdict chain, and backing up to an earlier stage is a re-derivation because everything downstream is generated. Accept: chain typed on the carrier; a done node without an Accepted verdict is unrepresentable or lens-red; the three group-3 prose rows re-point at this machinery as their realization.
  - [ ] **dispatch-time walls in belt_dispatch_node** — refuse dispatch on a node without a ModelArtifact; refuse on stale receipts (the freshness check from ts-wf-node-schema); a NeedsRework verdict dispatches a FRESH session carrying brief + prior diff + located debts (never the producing context unwinding its own work). Typed refusal arms on the existing BeltDispatchResult vocabulary — the wire/UI pick them up for free through the component register. Accept: each refusal arm witnessed with a RED control; live receipt on srv1 for at least the no-model-artifact arm.
- [ ] **floor lenses over the roadmap value** — pure readers (the v2 read interface): done-has-verdict; AcceptedWithResidue-has-counted-rows; dispatched-has-model-artifact. Each enrolled live with a RED control per lens hygiene (an inert lens is a lie); each carries the declared dissolution into enforcement-intent StandingIntent rows so the wall never lives twice.

**3a · The stabilization loop — why item 3 exists (receipts):**

- [ ] **name the loop: CI hurts → quick patch at the symptom layer → the patch regresses something else → repeat** — the instances below are receipts, not blame lines; the common shape is a fix landed under pressure without its own red-control, verified against a CI that was itself too slow or too red to check it (and merged before its verdict landed). The house rules already cover this (§5 factory model — analyze the stopped line before restart; §6 — root-cause, never fork-patch); the ask is that the loop's next iteration goes through a lane with a receipt instead of another hotfix, starting with the acute rows above.
  - [ ] pre-push hooks grew a CI-scale artifact/witness gate because CI was slow → local pushes broke and stalled → #7076 right-sized the hook back down (MERGED 2026-07-22). — ⏳ awaiting sign-off
  - [ ] nextest (37 GiB, red on main) removed from CI 2026-07-11 with fmt riding along, coverage claimed by the pre-push hook → the hook is absent in fresh clones → a fmt violation landed on main (#6658) → fmt re-enrolled as a standalone fast gate (#6691). — ⏳ awaiting sign-off
  - [ ] docs-only skip added for speed → silently bypassed the whole witness corpus including the doc-graph wall → retired #7023/#7039; docs-only PRs now run normal selection. — ⏳ awaiting sign-off
  - [ ] affected-set selection made PRs fast → corpus-read witnesses predict-skip → the falsifier ran red 29+ consecutive times unnoticed → falsifier-alert #7022 made it loud → the roster burn-down landed with this sheet update; the exit is the next scheduled green.
  - [ ] the release-build step's EAGAIN retry ladder (jobs=1, then drop RUSTC_WRAPPER) keeps builds green while masking the sccache/host deficit — the srvN build-cache provisioning lane owns the root fix (DESIGN open thread; STEP 2 fallback removal is operator-sequenced after T4).
  - [ ] #7060 moved a witness to the long lane but left its explicit roster row enrolled → every main run red for ~3 hours (no-main-function-found) → hotfix #7079; enrollment-vs-definition living in two places is the same single-authority class the unconsumed-component row names. — ⏳ awaiting sign-off
  - [ ] deploy-to-srv1 folded into the ci run for CD → srv1 host state now reds main daily independent of code health — the deploy-tail row above is the acute half; the CD-transport §2 node is the root.
  - [ ] **structural exit — the DERIVABLE red class now heals itself on main (ci-chores lane, operator-directed 2026-07-23: 'the priority is getting heal working').** A generated-artifact drift is a red whose remedy is a pure function of the tree: regenerate the projection from its `.dag` authority. So a new `heal_generated_artifacts` job (main pushes only, the same guard the srv1 deploy job uses) runs `main_wet` — which rewrites each `committed_generated_artifacts()` projection — then commits+pushes ONLY when git diff shows a change, healing the drift with no human. It needs only `build` (the release gunbc binary), NOT `ci`: the drift gate lives inside `ci`, so a heal gated on ci passing would skip exactly when needed. `contents:write` is scoped to this one job via a new job-level `Job.permissions` field (least privilege, §5 — the workflow default stays `contents:read`, so the whole-corpus floor never runs with a push-capable token). Loop-safety is by construction: the byte-idempotent fixed point (`generated_artifact_drift_test.witness_committed_is_fixed_point` — committed bytes == `artifact_generate`, with a RED control) is PROVEN pre-merge, so a second heal would find no diff and push nothing. A push made with the workflow's `GITHUB_TOKEN` does not trigger a new run (GitHub cascade-prevention), so the heal fires no follow-on CI at all — the loop cannot recur; the healed artifacts are re-validated by the batch-0 drift gate on the next unrelated PR/push, not on the heal commit (which is correct-by-construction as main_wet's own output). main is unprotected today (verified 2026-07-23), so the push needs no branch-protection grant; protecting main later is a separate deliberate step that must add this job's identity to the push allowance. Judgment-class reds (producer bugs #7080, roster staleness #7079) are untouched — this job only re-derives what the registry already declares generatable, never a human decision; they stay the fail-fast + PR-time-detector lanes' job.

**4 · Antagonistic product review — the last two weeks of merges:**

- [ ] **sweep the ~300 PRs merged since 2026-07-15 with the operator's checklist (authored 2026-07-22)**: does this PR accomplish what it was supposed to? if not, what remains? what issues happened? what context should pass to the next session? Output = per-PR disposition rows (finish · consume · revert · accept); prioritize merges whose mechanisms have zero executing consumers, and merges landed while the falsifier was dark (2026-07-16 onward — the whole wave).
- [ ] **the concat-composition class — shortcuts that calcify and spread** — string-building `concat` chains as the easiest path to the objective: 2,798 `concat(` sites in dag+src/v2, 112 nested `concat(concat(` in dag/** alone, densest in host/shell-adjacent modules (`srv3_os_install_reconcile_receipt` · `host_build_cache_provision` · `cli_invoke`) — exactly where the shell→intent rework fights them, and visible in open PRs now. The ruling already exists (operator 2026-07-14, #6656: a string literal is an ATOM, never a COMPOSITION; `TargetText` carrier at the emit seam) — the work is the census + wall: classify each chain (emit-seam-internal = fine · smuggled composition = migrate to its typed carrier) and gate new ones at the boundary so the class stops spreading.
- [ ] **unconsumed-component sweep** — mechanisms merged without an executing consumer are coverage-by-illusion (§6); the admission invariant (every witness row names an executing consumer — module-identity thread Phase 0) is the wall. Sweep the wave for landed-but-inert mechanisms and either wire a consumer or revert.
- [ ] **redefine done for PRs — finishing the task is not finishing the work**: a PR closes sustainably when the antagonistic review passes AND the modeling audit (item 5) passes AND its context-for-next-session lands on the carrier (item 6), not in the chat.

**4a · Open-PR audit — disposition every open PR (12 at snapshot):**

- [ ] **audit the remaining open PRs and decide each one's disposition** — merge · review-then-merge · fold into another lane · close; the 12 open at snapshot (16:30 UTC) are the children below; re-sweep after the merge wave settles, applying item 4's checklist and item 5's modeling audit to each.
  - [ ] #7080 layering gate false edges — the reference-derived producer misattributed prelude bare names, binders, and record-literal labels as cross-layer import edges (the one floor red left after #7082); fix verified pre-merge by a local red/green pair (gate witness false on main's tree, true with the PR applied, same tree same witness) — MERGED 2026-07-22 (7c222fc). Also the enabler for closure-derived Group<U> membership (group 5a). — ⏳ awaiting sign-off
  - [ ] #7078 §13 alias APPLY — CLOSED unmerged 2026-07-23 by the namespace lane as premature: dispatched off the design doc's stale import→alias sequencing; the fresh execution receipt shows the flip is a cheap safe substitution that goes first, making the alias tooling premature. Disposition executed; the doc reconcile rides the namespace lane. — ⏳ awaiting sign-off
  - [ ] #7077 srv1 deploy readiness fix (was mistitled `dag roadmap work`) — poll bound 30s→120s grounded in measurement: four srv1 starts listening at 35–39s against a 30s bound, so every prior deploy red was the probe quitting ~4s early on a successful start; expectation (40s) and bound (×3 margin) split into separate decls with the demand-scoped-load dissolution trigger — MERGED 2026-07-22 (fec005aa). Residual: `witness_poll_max_attempts_is_thirty` now asserts 120 — rename on next touch. — ⏳ awaiting sign-off
  - [ ] #7076 right-size the emitted pre-push hook — CI-scale artifact drift gate dropped from GitPrePushHook — MERGED 2026-07-22 (d50e25b). — ⏳ awaiting sign-off
  - [ ] #7074 cli_run.rs hollowing ledger — CLOSED by operator 2026-07-22, reframed as roadmap description: the census stands as the drain map (867 fns; LensHostBridge 225 + NamespaceResolutionSymbolIndex 214 + AffectedSetSelection 141 = 580/867 — two-thirds of the file on two v2 lanes), while the Python generator and its §3 second-producer question dissolve with the PR. Seed management continues in group 7. — ⏳ awaiting sign-off
  - [ ] #7072 finish #7043 floor budget refusal on FloorOutcome + plan-roster fact (`session/tidy-ibex-424`) — MERGED 2026-07-22 (44e538d). — ⏳ awaiting sign-off
  - [ ] #7069 CI onto v2 — routing frontier + parity gate + consumption P4/P2/R1/R2 (`session/native-routing-consumption`) — operator merging 2026-07-22 after rebase+regen (ci.yml drift collision with the heal merges). Parity windows DEMOTED from flip-precondition to post-flip monitoring (operator re-pricing 2026-07-22: no customers — prefer immediate loud divergence over drip-feed); R2 toolchain-identity key rides its commit 4. Immediate follow-on so it does not pile up: the family re-key row (group 5a); opens the bulk-native arc (group 2a). — ⏳ awaiting sign-off
  - [ ] #7068 dissolve cssl_emit_artifact_sanitize v1_rt-strip — dead rule deleted, pub-use-dedupe gap named for crisp-fox-839 — MERGED 2026-07-22 (46127b9). — ⏳ awaiting sign-off
  - [ ] #7066 Gate-1 receipt lane B1 — MERGED 2026-07-23: landed as the full per-module behavioral-receipt producer (one roster + one dispatch; 16 hand-authored transports dissolved). — ⏳ awaiting sign-off
  - [ ] #7058 post-merge honest frontier re-sweep — generic-T dead, type-ref-import-missing named successor class (`session/sharp-bee-290-roadmap-converge`) — MERGED 2026-07-22 (e89f076); its main run is the first to exercise the deploy tail with both unit fixes in tree. — ⏳ awaiting sign-off
  - [ ] #7050 computation-identity de-fork census R1–R8 (`session/computation-identity-defork`) — doc + bind row only, clean vs main; merge. R2 (durable native cache toolchain identity) now lands via #7069 commit 4 — the census row stays as its receipt.
  - [ ] #7027 srv4 BMC onboarding G1–G4 — parameterize per-host, gcloud self-provision, operator-token handler (`session/srv4-onboarding-credential-rotation-gap`) — ready; unblocks srv4.
  - [ ] #7008 Phase-A design-library lift — gunbc.design.* extraction + census frontier — MERGED 2026-07-22 (cadb472). — ⏳ awaiting sign-off

**5 · Modeling audit — a standing pass, not a one-off:**

- [ ] **per-PR modeling questions**: what models does this add — and should it add more? any anemic leaves (a `String` hiding named parts, a bare alias, an Option meaning three things)? does each new concept ground in an existing authority or mint a nickname (§2/§3)? Run it on the wave's merges alongside item 4's sweep; the §3 enforcement-intent lane's model-quality consumers (anemia/consolidation, node E) are its mechanization.
- [ ] **model items 4+5 as StandingIntent rows — ask once, compile forever** — the antagonistic-review checklist and the modeling-audit questions are operator standing directives being re-paid each conversation; DESIGN's enforcement-intent thread exists precisely to make them durable gated rows (intent ⇄ LensContract ⇄ coverage receipt). Until that lands, both checklists live HERE as the review template for every PR close.

**5a · Group of units — pluralities get standard representations (operator, 2026-07-22):**

- [ ] **a group of units is never a nickname** — taxonomy by how membership is KNOWN: positional (containment — module, the gold standard: being-in IS being-there, wrong membership unwritable) · derived (a computed relation — closure reachability, inhabitance, measured cost: membership is a query) · frontier (asserted per-row WITH reason + dissolve-on — sanctioned countable debt, the self-host-frontier shape) · nickname (asserted, string-keyed, no trigger — the violation; `family: String` and the closed #7074 feature enum are the type specimens). Migration pipeline: mint → frontier → query. Formalization home is the enforcement-intent carrier (its subject_roster IS this concept — no new lane): register every unit-naming roster; the gate = each registered group is Derived or Frontier-with-trigger; a bare unit-naming List of strings outside the registry is a review reject; a storage-grain proxy (dir/substring membership) must name its construction backstop (the long/ lane's 5s eval deadline is the blessed example — the dir cannot lie) or convert to Frontier rows.
  - [ ] **family re-key — the immediate #7069 follow-on (operator: figure out where this dissolves immediately, else these pile up)**: `family: String` → the subject construct's DeclarationRef; membership derived as closure-reaches-subject from the EXISTING closure authority — needs no namespace terminal (#7080's false-edge fix was the blocker and is on main; the containment SymbolIndex re-points the derivation later as a one-projection swap). Precondition for the bulk flip PR (2a) landing its 744 rows Derived rather than as 744 fresh nicknames.
  - [ ] **the census to burn down (swept 2026-07-22)**: `witness_exclusion_substrings` + its offline/fixture projections LANDED #7089 (typed frontier rows carry consumer classification; the parallel lists deleted) · the lens exception rosters (meta_exec_confinement · realization_vocab · inert_carrier · complexity_linearity_migration_debt · lens_registry_completeness) LANDED #7089 as typed Frontier rows, their prose reasons now typed fields · NON_FOLD_RESIDUE_ROSTER LANDED (this change, the group-7 data-out coupling): 125 typed FrontierRow rows on `gunbc.non_fold_residue` (123 legacy, deduped from the const's 127, + 2 fresh #7088 ci_floor_plan sites the re-home receipt caught), host census reads through the record-row reader, enrolled in the registry · remaining: discovery_scan_dirs / pool_roots (storage-grain proxies needing declared backstops). Burn down opportunistically when a PR touches the neighborhood; new instances blocked at review by the ts-group-u rule.
  - [ ] **stage0 partition crates rot without a consumer — the incident receipt for ts-group-u (2026-07-23)**: the crate-partition module lists (`stage0_std_primitive_modules` and siblings in `v2.workflow.rust_crate_partition`) are hand rosters NOT enrolled in any registry, and nothing in CI builds the partition crate libs — so they drifted silently twice: #6977 (Jul 21) added the measure→currency closure edge and `v1-stage0-std-core` stopped building standalone (std_measure imports a std_currency the crate list never gained), and the partition allow-block lacked the monolith's recursion_limit so `v1-stage0-extdeps-languages` failed on serde json_internal expansion. Both FIXED by authority edit + two-generation regen (rust_crate_partition.dag + stage0_crates.dag → regen_stage0 rebuild → re-emit; `cargo build -p v1-stage0-std-core` green). Remaining, the class not the instance: (a) DONE this change — partition module rosters enrolled in `gunbc.roster_registry` (DeclaredFrontier; terminal dissolution = the emitted-crate-partition thread deriving the layout ByDerivation from the module graph); (b) give the partition crates an EXECUTING consumer — a per-crate cargo check in the build job (ci_spec decision) — else the class rots again with zero signal; (c) side receipt: regen_stage0 WRITE mode was also broken since #7059 for stale-binary reasons (compiled-in hand-maintained roster) — the two-generation lag means every regen-affecting change needs build→regen→rebuild→regen, worth a note on the carrier.
  - [ ] **typed dissolution triggers — couple dissolve_on to the roadmap DAG for real (operator, 2026-07-23)**: prose `dissolve_on: String` becomes a typed variant naming the blocking roadmap node (`OnRoadmapNode` carrying a RoadmapNodeId, or a DeclarationRef where the trigger is a carrier fact), with prose retained only as the counted residue form. Then dissolution work is DERIVED, never hand-filed: every frontier/registry row with a typed trigger projects a dissolution node into the roadmap as a CHILD of its blocking item — the existing spawn-frontier deps-done machinery does the rest, so the moment the blocker closes (signoff), the dissolution item surfaces in Ready automatically. Enforcement: a lens reds a dangling node reference (trigger names a node that does not exist) and counts prose-trigger residue as the burn-down metric; no new scheduling mechanism, the coupling IS the edge. Natural follow-on to #7089 (FrontierRow is the carrier); shell→intent phase 3, meta-exec sequestration, and the belt-B interim scaffolds are the first typed references.

**6 · Planning + documentation — the stable anchor:**

- [ ] **every session starts and ends at a carrier, not a chat** — sessions drift context (more goldfish than they appear); the plan doc / lane node is the stable anchor with explicit expectations. So: dispatch always seeds from a brief bound to a plan (`dispatch_brief_template`, item 3); work-in-progress marks land on the carrier just-in-time (DESIGN §6 — the mark IS the authority, no parallel ledgers); and item 4's context-to-pass-forward question writes back to the plan doc at close. Existing machinery to lean on: the plan registry (#7012 — the registered PlanArtifact IS the doc binding) and the md↔dag delta-first thread.

**7 · Seed interim — cli_run.rs managed, not generated (yet):**

- [ ] **cli_run.rs cannot be generated from .dag today, and pretending otherwise is the specification-without-execution trap** — it IS the host seam (filesystem · git · process · cgroups · CLI), exactly the effect-transport layer the emitter learns last (the same fact that caps the native corpus at ~90%, group 2a). Receipt for why interim management is needed NOW: 26,686 lines, 988 fns; +2,747/−629 across 16 commits in the 33h of the merge wave — the §7 seed that should shrink toward zero grew ~8% in a day and a half, unpriced and invisible per-PR, and the operator observes the dumping ACCELERATES as v2 nears. The bulk-native arc (2a) is the biggest single hollowing step available; the rows below stop the inflow and move the movable.
  - [ ] **growth ratchet as a gate (§5 construction, not vigilance)**: cli_run.rs fn count becomes a floor receipt; growth is admissible only with a declared SeedGrowthJustification row (reason + owning dissolution lane — the frontier pattern applied to the seed itself). Every dump becomes a counted decision instead of an invisible default.
  - [ ] **data out now (zero emitter work, real shrink)**: rosters living in Rust move to .dag carriers the host reads — NON_FOLD_RESIDUE_ROSTER LANDED (group 5a): the const deleted, 125 typed rows on `gunbc.non_fold_residue` (reasons + dissolve triggers as typed fields; 123 legacy + 2 fresh #7088 sites), the host census reads through `non_fold_residue_units_from_module_source`, fail-closed with duplicate-unit refusal; remaining in this row: the exclusion-list mirror whose own note says it must stay byte-identical to the .dag authority (a live §3 dual representation — single-source it, host reads through).
  - [ ] **intake policy**: new host capability lands as extdeps interface shape + a THIN generic transport (the host_effect_apply funnel, per the shell→intent routing authority) — a cli_run fn carrying business logic rather than a transport shim is a review reject. Most of the wave's +2.7k lines had a .dag home available; the ratchet row makes this checkable rather than aspirational.

**Session branches running CI with no open PR at snapshot:**

- [ ] **each should end as a PR row above or be abandoned deliberately** — all 8 dispositioned: 7 merged same-day (#7075 · #7060 · #7061 · #7064 · #7059 · #7062 · #7067); fix/tidy-ibex-merge closed via #7073 as a deliberate duplicate-close of the merged #7072 (both pointed at the identical commit; operator-confirmed 2026-07-23). — ⏳ awaiting sign-off

## 0. Priorities — current execution order (rearranged 2026-07-06)

The two pillars — §1 get off v1, §2 own the compute fabric — stand as the strategic goal. This section is the **current priority ordering** over them and the walls that keep them honest, rearranged 2026-07-06 to **frontload the compile wall and complexity enforcement**: fix what blocks CI now, then make the regression classes *unwritable* before they recur (construction over after-the-fact catching, DESIGN §5). Each item points into the detailed lane; work dispatches from the lane, not from the pointer. Lanes marked *un-shelved* here are moved out of §4 by this ordering.

**① Compile wall** — the acute blocker. The `.dag` compile-clean floor runs **100+min** (#6239), past the CI job kills, so every PR is red on the floor and the rust-gate timeout thrash (the 10-min-drift #6339 + witness #6342, 2026-07-06) is a *downstream symptom* of the wall outgrowing the step budgets. Levers, in leverage order: affected-set-scope the compile-clean to the changed node-closure (→ ③), cross-run resolve memoization, per-module shard so `spawn_width` finally applies to the dominant node. Detail: §2 *kill the serial compile-clean wall*.

**② Complexity enforcement, whole codebase** *(frontloaded — un-shelved from §4)*. Make the regression classes unwritable, not caught-after: the **repo-wide complexity budget**, the **enforcement-intent gate** (`StandingIntent` ⇄ `LensContract` ⇄ `CoverageReceipt`, fail-closed — a mechanism *claiming* enforcement is complete only when the gate proves it from receipts), **intent-linearity** (model the axioms A1–A3 and enforce the syllogism — every claim a consequence-chain back to an axiom, no orphan and no cycle; [draft](docs/plans/intent-linearity-design-draft.md)), and the **gap analysis** — which regression classes *cannot* be caught yet (the expressibility frontier: wall vs lens vs review; [frontier](docs/plans/expressibility-frontier.md)). Detail: §3 *enforcement intent + model-quality walls*; §4 *complexity budget whole-codebase* + *fail-closed meta band* (promoted here).

**③ Affected set live in CI** — per-PR selection that shrinks the floor to the changed node-closure and returns witness enrollment to affected-set-shrunk discovery (today enrollment is opt-in *because* selection is not live — itself a compile-wall multiplier). Obligations: soundness (a skipped-but-affected witness is the fail-open catastrophe), effectiveness receipts on real scoped diffs, fail-closed default on a provenance gap. Detail: §2 *affected-set de-fork*; §3 *affected-set lens*.

**④ Realization** — inhabit the content-addressed carrier so caching, memoization, and provisioning all *derive* from one content-hash instead of N hand-rolled caches (v2 still hand-rolls `ParseTable`; floor shared-compute memoization is a Realization arm; DESIGN §2). Includes the **dag↔v2 de-fork grounding** — the fixed point must be well-defined over ONE algebra. Includes **execution-as-realization** (operator-affirmed 2026-07-07): `run ≜ realize ∘ materialize ∘ dependency_view` ([spine](docs/plans/execution-spine-design.md)) — interpreted-vs-compiled is a per-node realization *policy*, not an architecture. The interpreter is the pay-every-run realizer; the emitted native artifact is the pay-once-per-content-hash one, so **emit-on-demand** (emit closure → build → run, artifact `Share`d by content-hash) is the canonical execution path for a closed static substrate — no bytecode VM, no JIT (nothing dynamic left to profile; DESIGN §4). The tree-walker's surviving roles (bootstrap S1 receipts · compile-time eval) are bounded scaffolds dissolving with §1 self-hosting. **Materialization placement is derived, never hand-added (operator, 2026-07-09 — the state×decision ladder, `std.materialization_ladder`):** demand is read off ONE *nested* DependencyView — scopes nest eval → plan → process → emitted shell → CI run, a 'run' at any layer being a frame one scope up, never a different kind of thing (every layer is emitted from the same substrate, so the qualifier reaches all of them: it injects a content-hash skip into emitted shell as readily as a `Share` inside the plan). The law: plurality of demand per identity per frame decides — 0×pure = dead code, 0×effect = the effect is the use, 1 = Recompute, ≥2 with shared-state LCA = authored duplication (ERROR, rewire), ≥2 across isolation = memo obligation at the LCA that MUST be discharged by a covering content-keyed provider with declared eviction (`ScopeExit` scoped / `SpacePacked` persistent — dropping pure facts is always sound); *declared-emergent* frames (retry replay, unbounded siblings: server loops, CI-runs-over-time) obligate UP FRONT — expected emergent redundancy unprepared-for is an error, unexpected emergence is typed acceptance that converts to a declared row next run; an `ExistenceKeyed` provider is refused (existence ≠ identity — the build-if-absent red, #6352); a redundant `WorldRead` without a declared staleness envelope is refused (a TTL is a confession: unmodeled dependency or undeclared staleness). Un-shelves §4 *caching completion*'s 'one Materialization kernel'; subsumes ①'s 'cross-run resolve memoization' and §2's 'NOT cross-run caching' into per-node derived verdicts; §3-convergence: `v1.compiler.ownership`'s `binding_fan_out` is the eval-frame instance (Threaded-excluded plurality; SoleOwner/SharedError), retiring with the seed. Displaced cost already paid: the 9 GB retain-forever resolve store · build-if-absent stale binary · the ~275s double-resolve · the 3× CI release build. Detail: [duplicate-work](docs/plans/duplicate-work-graph-lens-design.md). Detail: §1 *de-fork grounding cluster* + *emit-on-demand execution*; §2 shared-compute memoization.

**⑤ Everything linear / efficient / minimal — dev UX** *(un-shelved from §4)*. The authoring experience is minimal and non-redundant: **fold-ergonomics** (inert-abstraction lens, non-fold-residue audit, ban source comments), **namespace-only resolution** (minimal naming, delete `import`, no ambiguity — operator-signed 2026-07-06, its two rules *are* the §2/§4 minimality + no-ambiguity walls; gated on the `SymbolIndex` substrate; [design](docs/plans/namespace-resolution-design.md)), and local iteration speed. Detail: §4 *fold-ergonomics lane* (promoted); the namespace-only lane.

**In-flight lane snapshot (2026-07-22, silent-ibex-417 — effect-grants):** dissolving the forked `Hermetic | Wet | Record` bit into the (frame × verb × subtree) grant model (DESIGN open thread; not a pillar item). P-A/P-B landed; **P-C slice 1 MERGED (#6987)** (derived axes + presets), **P-C slice 2 in review (#7071)** (per-grant `HandlerBinding` selection — 2 approvals, mergeable, CI running). Remaining: `LiveTreeDisposition` stamp-retire (blocked on an operator path-A/B decision), `Hermetic|Wet|Record` enum deletion (gated on slice 2 + stamp-retire), hermetic-native run + network isolation (deferred behind a target-env trigger). Full PR ledger + blockers: [effect-namespace-grants §4](docs/plans/effect-namespace-grants.md).

## 1. Get off v1 — v2 self-hosts (TERMINAL: `src/v1` deleted)

Anchor (do not flip-flop): `.dag` = truth; v2 emits its own seed (no stage0 hand-edits); the trust chain is discharged by execution; then the hand-written seed is deleted. Terminal is **hand-written compiler logic → 0**, not zero bytes of Rust: a pinned v2-emitted bootstrap kernel (~8–15k LOC — `claim_executor`, evaluator host-physics, the regen oracle) survives as the content-addressed seed. → [plan](docs/plans/v2-self-hosting.md) · [de-fork audit](docs/plans/dag-v2-defork-audit.md) · [seed census](docs/plans/seed-shrink-census.md)

Ground truth (2026-07-05): `src/v1` = ~174k hand-written `.rs` LOC (the ~154k figure in older docs is stale) + 44 `.dag` files of v1's own modeling; `src/v2` = 874 `.dag`, zero `.rs`. HAND_MAINTAINED roster went 24 → 7 files and has REGROWN to 25 (re-measured 2026-07-23: native-routing shims + governor/pre-push seeds, each with its own dissolution trigger) + `module_path_index` (~22.3k LOC: the three lens projections drained #6158/#6211/#6212; `phase_profile.rs` ADDED 2026-07-04 with its own dissolution trigger); `patch_*` is fully dead in-tree; the `dag_collect` pair briefly went HAND-only in #6262's regen unbreak, then RETURNED to GENERATED in #6239 (registry authority verified 2026-07-05: `regen_stage0.rs` GENERATED includes the pair, HAND count 7, witness pins 92). The regen gate now proves fresh-vs-committed byte-identity unconditionally in CI (#6957/#6958, 98/98 files), so emitter improvements are no longer latent and cargo-green is continuous by composition (see 5-regen-cutover / 5-cargo-green-continuous, closed 2026-07-23) — today's walls are that identity + well-typed emit. INTERIM (operator-accepted, 2026-07-05): the CI floor's compile-clean step cannot complete inside its 10-minute step budget (#6232; restructured #6273 so a breach fails the STEP loudly instead of cancelling the job silently, and `rust_tests` fits at 30min) while compile-clean runs ~40+min, — DISCHARGED since (2026-07-23): compile-clean is CI-scoped per import closure and regen `--verify` is a required CI step, so the walls are CI-proven now.

**◆ Milestones:** front-end ✓ · emit-rust well-typed ✓ · cross-tree import ✓ (#5473) · emitted crate cargo-green, 0 rustc errors ✓ (#5777/#5873, re-verified #6099; fresh emit re-greened and byte-identical to the committed seed since #6958) · `regen --verify` in CI ✓ (#5873) · interim fixpoint gate, 2-of-92 roster ✓ (#6009) · full-roster 92-file byte-fold + host-grounded digest ✓ (#6218) · v1 self-resolution: #6235 arm fixed ✓ (#6250) · #6241 `get`-registry arm ✓ (#6255, operator-resolved collision) · #6242 comment/registry break ✓ (#6262 — third dark-regen break in four days) · emitter mass fix ✓ (#6243 merged 2026-07-05: the 1667-error fresh-emit red root-caused to ONE `field_is_boxed` predicate bug = 1482 of 1667, plus the 139-error alias-brand/use-line import class; #6266 = byte-identical template hygiene) → regen `--verify` required in CI ✓ (#6957/#6958 — 98/98 byte-identical fresh-vs-committed) → **▸ NOW: the three-gate v1-deletion bar (dag/gunbc/v1_deletion_plan.dag — emitter fixed point · honest frontier · ledger green)** → seed-honesty (DDC — see the prereq-drift flag on 5-collapse-v1) → **TERMINAL: `src/v1` deleted**

- [ ] **regen-cutover cadence — closed by construction (audit 2026-07-23)**: `regen --verify` is a required CI step, green on every main push; #6957/#6958 moved rustfmt normalization to production emit, so all 98 generated files are byte-identical fresh-vs-committed and the two-generation latency this cadence existed to absorb is unwritable. The July-5 cargo-RED/cutover-blocked receipts died with #6243. Forward fixed-point work lives in the v1-deletion plan (^regen_fixed_point_receipt). — ⏳ awaiting sign-off
  - [x] ~~**real fixed point** — re-based to the behavioral 2-generation bar (byte-fixpoint an explicit non-goal, #6922/#7092); the landed byte-fold half (#6218) lives on as the required ci_regen gate. Residual the successor does not carry: generate_stage_candidate_from_ingest has zero consumers (candidate_generation.dag).~~ **superseded →** dag/gunbc/v1_deletion_plan.dag ^emitter_fixed_point + ^regen_fixed_point_receipt
    - [ ] **seed-honesty discharge** (Diverse Double-Compiling) — modeled in `bootstrap.dag` (`SeedHonestyDischarge`), no execution exists; worse, the modeled witness is fail-open by construction (its evidence argument is the same atom it checks against — Holds trivially, no red case constructible) until Stage 3's executing consumer lands. Needs a reproducible artifact to double-compile. Design proposal (second-compiler candidate `ddc_reference_compiler` + computed fixed-point digest, reusing `self_host.dag`'s `canonical_emitted_bytes_digest`; FLAGs A–D open for operator sign-off): [seed-honesty discharge design](docs/plans/seed-honesty-discharge-design.md)
    - [x] ~~**TERMINAL — collapse src/v1** — the deletion bar lives in the three-gate plan (GateEmitterFixedPoint · GateHonestFrontier · GateLedgerGreen) with frontier.dag (#7058) carrying per-module dispositions. PREREQ DRIFT ruled 2026-07-23 (prereq_drift_ruling_2026_07_23 in v1_deletion_plan.dag, PR #7110): the three gates are the interpreter-deletion bar and correctly omit all three; the terminal claims restore them as GateDeletion bricks — hand_queue_drain before v1_quarantine, test_migration + seed_honesty_decision before v1_delete.~~ **superseded →** dag/gunbc/v1_deletion_plan.dag ^v1_quarantine + ^v1_delete (three-gate bar, #6922/#7092)
- [ ] **drain the HAND_MAINTAINED queue** — re-measured 2026-07-23: 25 files + `module_path_index` (regrown from 7 by pre_push, memory_governor, census_exclude_derive, std_lens_verdict, 3 usv_pilot files, and 10 v2_compiler_* native-routing shims — each landed with its own dissolution trigger; drain as the triggers fire). The cli_run.rs drain has its own plan: [cli_run.rs hollowing, #7084](docs/plans/cli-run-hollowing-plan.md).
  - [ ] **no-dual-representation-test lens rebuilt pure-v2** (#6104) — lens verdict moves off v1 Rust, the lens-E retirement pattern continued (landed #6104) — ⏳ awaiting sign-off
- [x] ~~**emit-on-demand execution** — landed there by execution: kernel #6879, family fanout #6893/#6894/#6895/#6917, native path 9/9 families green + falsifier wet-cadence enrollment #6990, one-build-grain #7042, artifact store #6738; carrier src/v2/std/emit_on_demand.dag. The S1-parse-census first-customer framing is obsolete.~~ **superseded →** witness-realization lane — v1_deletion_plan ^witness_realization_kernel / ^witness_family_fanout · docs/plans/witness-realization-plan.md
- [ ] **de-fork dag ↔ v2 grounding cluster** (one std authority — the fixed point must be well-defined over ONE algebra): `algebra` first (LIVE fail-open, 75 floor entries) → effects/float/integer/logic → `nat` LAST. Operator rulings stand: coproduct = structural authority; grounded-realization wins. The cost of not de-forking is no longer theoretical: the `extdeps.shell` dag↔v2 fork silently shadowed `shell.Which` in the two-root module index and kept main red from #6097 until the de-fork hotfix. [brief](docs/plans/dag-v2-defork-audit.md)
  - [ ] **Root B repoints** — def-unification (coproduct authority + aliases) → repoints (algebra/nat/integer/float/logic/effects/verification) → 🟡-marker dissolution (keystone #5552 merged)
  - [ ] v1-coupled `coercion`/`node` renames — deferred to v1-delete
- [ ] **test-migration lane** — live debt per the `v2.lens.test_migration_debt` shrink-only ratchet: 73 v1 test modules / 731 `#[test]` fns / ~23k LOC with no exact-stem floor witness (baselines 77/912/28546 set 2026-07-02; `pipeline.rs` alone is 417 fns, the dominant unit); the delete-guard blocks removing a v1 test module without its floor witness (hard gate per module, bulk-delete forbidden). **Scrutinize before migrating (operator, 2026-07-05): tests are not inherently valuable — triage each module first (migrate · delete-as-redundant · delete-as-low-value); the guard currently requires an exact-stem witness for ANY delete, so the lane's first deliverable is a typed retirement path through the guard, never a bypass.** Parallelizable now; gates the terminal collapse per module. NOTE: migrated witnesses run on the local discovery path — CI enrollment is opt-in (#6232) until affected-set selection lands. UPDATE (2026-07-05): the typed retirement path LANDED (#6261 — `RetirementDisposition` model, guard union, import-bound `covered_by`), and the first drain tranche merged the same day (#6268/#6270/#6271/#6272: complexity-bounds, transport_emit, scrambled_name, self_gen8 clusters; baselines ratcheted 912→881 fns / 28546→28054 LOC, tightening further at each rebase). Triage finding so far: the delete mass concentrates in the `pipeline.rs`/`parse.rs` batteries (~64% of debt fns); the small-module tail mostly guards real v1 behavior whose dag/std concept exists but is UNWITNESSED — genuine migration work, not deadweight.
- [ ] **emitted crate partition** — derived partition LANDED #6747: v2.workflow.rust_crate_partition grounds on the self-host frontier roster → stage0_crate_partition_generated.dag → consumed by src/v1/stage0_crates.dag, proven by build + regen_verify. Remaining open: the rows are a HandExplicit interim (dissolve-on: PolicyPartition) and interface Node grounding is deferred (stage0_module_dag_interface_grounding debt note). [design](docs/plans/emitted-crate-partition-design.md)
- [ ] **make cargo-green continuous (or cheap)** — resolved by construction, neither named option: post-#6958 the fresh emit is byte-identical to the committed seed (98/98, production-side normalization), the required ci_regen step proves that identity on every main push, and CI builds the committed seed every run — so fresh-emit cargo-green is continuous by composition; the named failure mode (bytes stable per the old seed, fresh build broken) is unwritable. Residual cleanup: route_a_emit_fresh_cargo_green_test still #[ignore] — keep-as-local or delete. — ⏳ awaiting sign-off

## 2. Compute fabric — own the infra (CI · deploy · control plane)

State (2026-07-01): required CI is back on the fleet after the Ubicloud detour (#6107, reverted #6111). The detour was the **diagnostic and emergency valve, not a failure**: it proved raw VM isolation alone does not solve a serial floor (same ~50min wall on both runner types), and it stays the **break-glass/burst option**, just not the active required path. So the active work is (1) reduce floor recompute, (2) make fleet admission/caps safe — the dominant wall-clock is inside the floor, not host placement alone. The fleet — srv1/srv2 (128c/125GiB Ampere) + srv3 (greenfield, OpenBMC) — carries agent sessions, the dashboard, and all runners, with live cgroup caps, runner registration, sudoers, and tailscale-ACL actuation still **hand-managed**; the fleet-era OOM/sccache exposure returned with the traffic (operator-accepted interim). #6096 (fleet converge control plane) was **closed unmerged**; its review gap-list is the re-land spec, and the findings apply to the committed `.github/fleet-converge.sh` too (mutate-then-read, `|| true` swallowing, no hostname dispatch) — the re-land is an architecture change, not a rebase. → [CI humming](docs/plans/ci-humming.md) · [host-effect orchestration](docs/plans/host-effect-orchestration.md) · [compute envelope](docs/plans/compute-envelope-model.md) · [charter](docs/plans/ci-process-end-to-end.md) · [host-converge inventory](docs/plans/host-converge-inventory.md)

**The core design rule (operator-signed, 2026-07-01):** provider-specific power is earned behind a `ProviderOffer` adapter; the core fabric only knows `RunShape → Allocation → Receipt`; default allocation is **strict/ephemeral**; oversubscription is an optimization proven by receipts, never assumed by placement. Not "Ubicloud vs fleet" — GitHub Actions asks for a RunShape, the fabric chooses a ProviderOffer (srv1/srv2 fixed-host slice, Ubicloud ephemeral VM, or later others). srv1/srv2 are modeled as little Ubiclouds first (N strict slots, hard caps, read-back receipts); their richer allocation opportunity (stranded cores, burst, co-residency) is Phase 2, earned by receipts.

**◆ Milestones:** OOM root-caused (hand-set caps vs measured peaks + budget double-count) ✓ · Ubicloud stabilization trialed ✓ (#6107) → reverted, operator call (#6111 — no wall-clock win; width=9 was live by construction and never touched the serial wall) · converge shell emitted ✓ (#5725/#5827) · `apply()` Phase A ✓ (#5756) · CD to srv1 ✓ (#6030/#6060/#6093) · witness-level affected-set pruning LIVE ✓ (#6061) → **▸ NOW: kill the serial compile-clean wall · floor <1min · CD wrong-host fix · stateless-frontend MVP (first non-CI fabric consumer) · converge re-land to the signed design** → srv1/srv2 runner-allocation v0 (hand-config retired) → StrictLease + shape labels → dormant-Ubicloud provider row (design-break probe) → periodic actuation with receipts → srv3 OsInstalled · `apply()` Phases B–D → **TERMINAL: required CI runs on the compute fabric — the model can choose any admitted ProviderOffer, the receipt proves the shape was honored, and install/manage/decom all ride `apply()` with fail-closed receipts**

**Acceptance rule (operator, 2026-07-02): an item is not complete because its algebra exists — it is complete only when a NAMED consumer uses it on the LIVE path and produces a receipt** (DESIGN §5: a typecheck or a grep is not a consumer). Completion tiers: **T0** modeled · **T1** fixture/synthetic witness · **T2** wired to the intended consumer (proof by consumption, never grep) · **T3** live dry-run receipt · **T4** live apply + independent read-back receipt · **T5** a periodic/CI consumer keeps it alive. Every dispatchable node states **Accept:** — its required tier plus an operator-checkable end state on the real fleet (what is different on srv1/srv2 or in CI afterward) plus a discriminating RED; control-plane/fabric items close at **T4/T5**, and anything below the named tier is a subtask, never completion. A sized node without an explicit Accept is not ready to dispatch; algebra, carriers, and lenses are mechanisms and never their own acceptance. The bar for real progress: *can srv1/srv2 runner allocation be changed through the model, read back, re-run safely, and retracted?* Until yes, the work is scaffolding. Full per-node checklists: [fleet acceptance criteria](docs/plans/fleet-acceptance-criteria.md) — the node's Accept line is the summary, the doc is the checklist, and edits to one land with the other.

**Now — correctness of the current posture:**

- [ ] **fix the CD transport premise — a deploy needs a target-host proof** — `deploy_dashboard_srv1` is a *srv1 effect*, but job placement is not an srv1 *proof*: `LocalShell` with principal `runner` assumed the runner is ON srv1 (#6093), while `[self-hosted, linux, arm64]` can land on srv2 (and during the Ubicloud window it was a rented VM). A `LocalShell` deploy is valid ONLY when the runner identity proves it is already on srv1 — either pin the job to an srv1-specific trusted runner label, or make the effect `SshShell`-over-tailnet with an explicit srv1 target + typed credential (#6066 Phase 2). Until then every main push gambles the deploy host. — ⏳ awaiting sign-off
  - [ ] **stateless frontend MVP on fabric** — milestone (A) LANDED: roadmap static site served on srv1 (#6128) + healthz REST GET readiness edge (#6753) + bounded read-back (#6769) + roadmap_site_healthz_validate; live_deploy apply/retract on the membership spine (#6814/#6940/#7011). REMAINING: milestone (B) fabric SiteLease on the same allocation model as CI (waits on the control-plane read-back); the public-domain discharge (gunb.ai Pages cutover) is assigned to the site-subsumption lane and has NOT merged yet.
- [ ] **roadmap dispatch actuator** — machinery LANDED and live: first end-to-end dispatch on srv1 by execution (#7096 go-live receipt; belt A #6942 · belt B gunbc serve #6940 · single dispatch authority #6914 · spawn/observe/reap #6836; Dispatch button reads the belt-B status contract; claude preflight typed refusals; independent read-back = tmux ls). REMAINING vs the T4 accept: no Stop verb and no GET /sessions surface (roadmap_serve routes carry only POST /dispatch + statics), and re-dispatch after Stop is one-shot-broken (filed as ts-dispatch-redispatch, sized ready row). [plan](docs/plans/dispatch-maintain-cc.md)
- [ ] **workload-class admission model** *(operator directive, 2026-07-01: 1 core → 3 GiB + swap)* — runners are capacity surfaces, not concurrency proof. Define CI workload classes (`floor_light` · `rust_heavy` · `deploy` · `session`) with measured RAM/swap/pids/CPU-token demand; **admitted concurrency per host derives from HostSupply × WorkloadClassDemand, never from runner-slot count**. Initial 128c/125GiB policy: ~42 build tokens per host (≈1 core : 3 GiB, the `ubicloud-standard-16-arm` shape generalized), with intentionally-stranded cores reserved for sessions and low-memory work — stranded is not wasted. Measured anchors: ci ~11GiB · rust_tests ~37GiB peaks vs the old 8G slot caps. **Accept:** admitted concurrency for srv1/srv2 derives by execution from HostSupply × WorkloadClassDemand (witnessed, not hand-set to match), and a proposed allocation whose Σ exceeds supply is REFUSED with a typed verdict — perturb: inflate one class's measured demand and admission shrinks.
- [ ] **cap/admission de-conflation** — re-based 2026-07-23: the three facts now live in three mechanisms — per-slot containment = cgroup caps via converge (#7009/#7000) · host admission = fleet_host_budget conservation (host_allocation_conserves) · build width = the runtime AIMD memory governor (2026-07-12 adaptive-width ruling, which retired the width folds and per-shard byte predictions the original framing assumed). Remaining ask: a receipt that the three mechanisms cover the original knobs with no conflated survivor, or re-home the residue.
- [ ] **merge-admission gate: close the HELD state** — wired (#5974/#6080) but the freshness block is HELD pending a real green-on-main receipt, and the #6080→#6101 accidental re-apply/re-revert needs a clean landing. Evidence it is needed: stale-base merges landed BOTH `.gitignore` drift (healed #6110/#6111) AND the `extdeps.shell` collision that kept main red — green-on-branch is not green-on-main. [decision record](docs/plans/ci-merge-freshness.md)

**Fabric-hostable rule (operator-signed):** a service is fabric-hostable when it is expressible as **immutable storage + stateless compute + explicit route + read-back receipt**; anything requiring local mutable state is not fabric-portable until its state moves into a `StorageBinding`. "Connect the right users to the right resources" is the fabric's future value, not active work: `RoutePolicy` (nearest/cheapest/least-loaded/trust), multi-replica routes, cert/DNS automation land later, when they hurt — the first version connects ONE domain to ONE admitted allocation and proves it did.

**Floor throughput — reduce work, then schedule it (26m → <1min on a typical PR):**

- [ ] **kill the serial compile-clean wall** — re-based 2026-07-23: lever (a) LANDED — per-PR import-closure scoping via tools.dag_compile_clean_scope (2026-07-16) + loader-closure de-fork #6950; the cost picture moved under it (#7001 memory-thrash-not-compute · #6808 sigs-env 2461s→65s, 37.6× · #6999/#7030 namespace-walk fixes; width regime = adaptive governor). Remaining: lever (c) — shard A's compose proof landed but shards were never enrolled in the floor plan (2-compile-clean-shard-b, re-price first); whole-tree baseline runs still pay the serial wall on the falsifier/cold cadence.
  - [ ] **affected-set de-fork — `v2.lens.affected_set` as single authority** — witness-level run/skip live (#6061, v1 `force_run_all` hack retired); remaining: precompute-skip wired to the same one `.dag` query, the Rust parallel (`NodeFrontierSeeds`) deleted (N→1), and a wall-clock+RSS receipt on a scoped diff. [plan](docs/plans/affected-set-precompute-pruning.md)
    - [ ] **provenance ingest live at floor runtime** (#6105) — successor lane: resolver S2a + skip-before-resolve remaining fix (PR #6105 closed 2026-07-02 measured work-neutral); node-closure grounding for `v2.lens.affected_set` to be live during the floor pass; gates node-level pruning + the Rust-bridge deletion [plan](docs/plans/affected-set-precompute-pruning.md)
    - [ ] **host-scaffold witness classifier de-fork — DONE** (#6224/#6479) — the claim_executor Rust text classifier (`witness_test_fn_uses_live_host_scan`) and the interim `floor:host_scaffold` marker are dissolved into the substrate-declared `LiveTreeDisposition` row (#6479). Placement correction vs this row's original wording: the disposition lives as a `data live_tree_disposition` row in the WITNESS ENTRY FILE (`v2.std.live_tree`), not as a field on `TestClaim` rows — a claim row asserting a foreign entry's liveness would be a second carrier of the same fact (§3 dual representation); the entry's own row is the single authority. Undeclared = ReadsLiveTree = never predict-skip (fail-closed); the nightly affected-set falsifier is the enforcement for lying declarations; call-reachability grade re-derivation is the later lane [plan](docs/plans/affected-set-precompute-pruning.md) — ⏳ awaiting sign-off
- [ ] **floor wall-clock on a scoped PR — target <1min** — the original headline (the floor is a full recompute every run) is discharged: witness-level affected-set selection is LIVE in CI (discovery flip, operator acceptance 2026-07-09; fail-closed selection + falsifier cold control) plus compile-clean closure scoping. The 2026-06-24 profile numbers predate #6848/#6808/#6999 and are void. Target <1min still unmet on cold/whole-tree paths — remaining lever is resolve-phase incrementality, re-based on current receipts.
- [ ] **compile-clean shard A — partition boundary + ONE shard + compose proof** — the direct attack on the serial wall (lever c above): identify the module partition boundary for `dag_compile_clean_gate`, run ONE module shard (manually or via minimal wrapper), and prove the shard verdict composes with the existing whole-tree gate (a shard-green ∧ … ∧ shard-green tree is whole-tree green, and a broken module reds its shard). Non-goals: NO floor-plan/scheduler integration, no width tuning. The partition receipt is the deliverable even if enrollment never follows. — ⏳ awaiting sign-off
  - [ ] **compile-clean shard B — enroll shards in the floor plan** — batch-1 becomes N shard nodes so `spawn_width` finally applies to the dominant cost; before/after batch-1 wall-clock receipt on a real run. Gated on shard A's compose proof. **Accept ([checklist](docs/plans/fleet-acceptance-criteria.md)):** the floor plan CONSUMES the shards (shard A's exemplar is not sharding); every shard derives from the same source-root authority as the whole-tree gate; compose(shards) ≡ the whole-tree verdict on a green tree, a planted bad module fails the composed verdict, an empty roster is Unknown/fail-closed; receipt = before/after batch-1 wall-clock + shard count; RED: drop one shard from the roster → the coverage witness fails.
- [ ] **kill the within-run double-resolve** — discovery and execution each resolve the corpus (~275s second pass); execution piggybacks the discovery resolve. Pure within-run win (M1 within-walk memo landed #6008 — this is the cross-phase half).
- [x] **resolver pathology A — profile receipt only** — the 518s resolve: reproducible `scripts/profile-cold-resolve.sh`; top-N entry offenders; pair `budget_roster_completeness_test` vs `fold_list_generic_instantiation` (34.7× cold wall); hypothesis: `typecheck_module` in `reconcile_with_typed_cache`. — ✓ signed off: operator
  - [ ] **resolver pathology B — fix one confirmed pathology** — RED control on the pathological pair from the profile receipt, the fix, and a before/after resolve-count or wall-clock receipt.
- [ ] **G5 rust-gate selection** — rust fmt/clippy/run-all is all-or-nothing on `.rs` PRs (the ~530s hot spot); no affected-set, while the `.dag` floor already has one. Matters until v1 is gone. [plan](docs/plans/ci-selection-vs-scheduling.md)
- [ ] **floor runs the right things** — SELECTION (what changed) vs SCHEDULING (by cost); per-PR = run-all sound baseline (#5427) shrunk to the affected set; cost never drives selection. Scheduling authority: [bounded-input / cost-envelope design](docs/plans/bounded-input-cost-envelope-scheduling.md). [plan](docs/plans/ci-selection-vs-scheduling.md)
- [x] **bounded-input / cost-envelope scheduling — operator-signed design** *(2026-07-02 capture)* — unified design record: `InputEnvelope` (data ceiling) → `CostEnvelope` (symbolic Predicted cost at envelope) → scheduling (width/placement); Predicted authority / Measured falsifier; cost → scheduling never selection. P0 InputEnvelope landed; P1–P5 dispatch separately (no scheduler types in this capture). [plan](docs/plans/bounded-input-cost-envelope-scheduling.md) — ✓ signed off: operator
- [ ] **resolve-cache default-on** — purity proven (616/616) but opt-in: whole-file JSON IO buffers ~11× the packed graph and OOMed the concurrent floor (#5789 reverted); gated on a streaming IO realization. The warm ~18% lever, downstream of incrementality.
- [ ] **G4 dispatch dup** — `workflow_dispatch`+PR fire two same-SHA runs; the concurrency fallback won't collapse them → wasted runners and OOM risk (live instance: #6110's draft-attach + ready-flip fired two runs, the cancelled one reporting as a failing check). Superseded runs also linger `in_progress` for 12–37min — audit cancel-in-progress coverage; leaked runner minutes. [decision record](docs/plans/ci-merge-freshness.md)

Receipts + adjacent levers: [fractal Gantt](docs/plans/ci-floor-fractal-gantt.md) · [memory chronicle](docs/plans/interpreter-memory-chronicle.md) · [resolver pathology profile](docs/plans/resolver-pathology-profile-receipt.md) · [representation minimization](docs/plans/representation-minimization.md) · [shared-compute memoization](docs/plans/floor-shared-compute-memoization.md) *(M1 ✓ #6008; M2 gated on determinism #5941)* · [OOM reclassification](docs/plans/ci-oom-reclassification.md) · [post-engine deferred ledger + algorithm audit](docs/plans/post-engine-pr-roadmap.md) · [CI-performance tanking evidence — 2026-07-10/11 main-red incident](docs/plans/ci-performance-tanking-evidence.md).

**Control plane — the #6096 re-land, done right (G2/G3):**

- [ ] **G2 runner deployment derived from `fleet_intent`** — runners/host + registration from the modeled envelope; the gunbc-EMITTED converge shell is the host-apply carrier (operator-set). The umbrella for the re-land below. **Accept:** a runner-count/allocation change on srv1/srv2 lands by editing the modeled envelope only — registration + slots + caps converge from the model, independent read-back proves the live units match, and a hand-registered rogue runner is detected as drift (RED), never absorbed.
- [ ] **sound host admission — Σ-accounting with two modes** — post-patch values feed Σ-accounting over RAM, swap, pids, and CPU tokens. **Guaranteed mode**: Σ(memory_max) ≤ RAM budget ∧ Σ(swap_max) ≤ swap budget (safe, conservative). **Burst mode** (the CI target — we trust our jobs): admission from measured class demand + margin, caps as containment ceilings not reservations, and the receipt window must prove admitted×ram_margin+headroom ≤ host_ram, admitted×swap_margin+reserve ≤ host_swap, and oom_kill/swap_fail counters did NOT increase. A plan that passes only by assuming every slot simultaneously consumes its burst ceiling is rejected or explicitly marked Guaranteed-mode. Replaces the too-weak single-slot swap compare that helped sink #6096. **Accept ([checklist](docs/plans/fleet-acceptance-criteria.md)):** admission computes from POST-PATCH desired values (feeding base values instead is a RED); an overcommitted Guaranteed plan fails Σ-accounting by execution; a Burst plan proves its margins; live receipt = one real srv2 window with pre/post `memory.events` showing oom_kill and swap-fail counters did NOT increase.
- [ ] **live read, first slice: one runner unit's memory knobs** — read `MemoryMax`/`MemorySwapMax`/`MemoryHigh` for ONE live actions-runner instance and ground the result into the ConvergeTarget semantics (no string flattening). Non-goals: no patch/apply engine, no runner-count reconciliation, no timer. RED: a fixture read that differs from base refuses convergence (`NotConverged`); green receipt: one real srv2 unit read grounded into the model. Stop-and-return if systemctl read output cannot be represented typed. — ⏳ awaiting sign-off
  - [ ] **live host read seam (full)** — `fleet_show_effective_read` replaces `ReadAbsent` for cgroup caps, runner count, jobserver tokens, swap, and pinned-tree status; synthetic-read witnesses are NOT enough for periodic actuation (the reconciler substrate exists — the live host read is the load-bearing missing piece). The effective base for converge is `host_converge`'s `ConvergeKnob` rows, not `fleet_intent` (physical inventory). Grows knob-by-knob from the first slice above. **Accept (T3/T4 read-side; [checklist](docs/plans/fleet-acceptance-criteria.md)):** live typed reads on BOTH srv1+srv2 covering the runner units (`actions-runner@<host>-*.service`), `MemoryMax`/`MemoryHigh`/`MemorySwapMax`/`TasksMax`/`CPUWeight`, runner count, and the jobserver token value; fixture REDs (absent property → typed Absent · `infinity` where bytes expected → drift · wrong bytes → drift); full-host receipts list EVERY runner unit's effective knobs; NO mutation anywhere in this node; the converge precondition consumes these reads. The first slice does not close this node.
    - [ ] **closed-loop converge — T4 acceptance on the spine** — re-based 2026-07-23: the three-way-diff carrier EXISTS (host_converge_delta.dag + srv2 ConvergeKnob fixture witness); converge thin-run landed (#6572/#6585/#6598, re-verified 2026-07-20); per-slot MemoryMax convergence generalized multi-unit (#7009) and srv3 caps via host-standup converge (#7000); apply/teardown poles unified on the membership_reconcile spine (#6814) — deploy = one fleet reconcile spine (operator). REMAINING (this node's bar, unchanged): the full T4 accept — reconfigure srv1/srv2 to a NEW runner allocation from the model alone, plan shows exactly the delta, independent read-back (systemctl show, never our own write echoed back), hand-edit reds the next converge, retract restores, values survive daemon-reload ([12-step checklist](docs/plans/fleet-acceptance-criteria.md)); the live read seam is still ReadAbsent (fleet_show_effective_read) — bind ReadObserved at the apply increment.
      - [ ] **periodic actuation with receipts** — a systemd timer (or ctrl thin-run) executes the emitted converge per-host under the privilege model; receipts land where a hand-edit REDS (G3's unmet promise). Until this exists the emitted shell is spec-without-execution. **Accept (T5; [checklist](docs/plans/fleet-acceptance-criteria.md)):** installed timer on BOTH hosts under the privilege model; per-run receipt (host · model hash · observed hash · applied/noop/conflict · changed knobs) lands where CI/dashboard reads it; a no-change run is a green noop; a hand-edit to a managed cap is DETECTED — auto-correct knobs restore with read-back proof, verify-only knobs (`VerifyOnlyCap` semantics) refuse with a conflict and do not mutate; RED: broken sudo → typed refusal · removed read access → `NotConverged`. A timer existing is not acceptance.
      - [ ] **runner allocation v0 — srv1/srv2 as StrictLease runner capacity, hand-config retired** *(the operational milestone this control plane exists for — operator, 2026-07-02)* — desired allocation declared in the model per host (runner slots + labels · per-slot `MemoryMax`/`MemoryHigh`/`MemorySwapMax`/`TasksMax` · jobserver tokens), applied by converge, proven by read-back. **Accept (T4, held alive by T5; [checklist](docs/plans/fleet-acceptance-criteria.md)):** every active runner unit on BOTH hosts matches desired effective values by independent read and a freshly started unit inherits the template drop-ins; admitted heavy concurrency derives from the admission model and GitHub cannot schedule past it (labels/slots ARE the backpressure — generic self-hosted labels cannot bypass heavy admission); receipt records host · unit count · labels · effective values · model hash; RED: a hand-edited srv2 cap reds the next converge/receipt · desired heavy concurrency above admission is rejected · wrong/missing label fails the dispatch witness · unprivileged apply refused before mutation; workload proof: one real CI run on the intended shape with no oom_kill increase and peak memory recorded as a measurement row. Not complete while ANY runner-allocation knob on srv1/srv2 requires a hand edit.
- [ ] **privilege model for host mutation** — converge writes `/etc/systemd/system/*.d` and runs `systemctl set-property` (root), but #6096's service ran as the operator user with `|| true` swallowing EPERM; extend `DeployAccess`/`host_effect` so an unprivileged apply is a **typed refusal BEFORE mutation**, never a swallowed failure. RED: unprivileged mutation emits no shell / no `host_effect_apply`; green: the privileged modeled path applies in a fixture. — ⏳ awaiting sign-off
- [ ] **three-way diff = the core fold with a lattice codomain** — accepts (a)+(b) MET by execution: generic keyed fold attached to std.change (#6129, keyed_two_way_diff in dag/std/change.dag; 30 law witnesses incl. identity/reorder/conflict-join) and srv2 ConvergeKnob fixture rows + leaf algebra (host_converge_delta.dag). REMAINING: accept (c) proof-by-consumption — host_converge does not import the carrier (only the witness consumes it); live consumption instead arrived via membership_reconcile (#6814 live_deploy poles, #6820 belt R9) — either re-point (c) to the spine deliberately or wire host_converge through the carrier.
- [ ] **service allocation receipt** — independent read-back for frontend services: process running, effective port, route installed, artifact digest served; retract removes route+process. Reuses `reconcile_grounded` (apply → read host back → converge only on observed evidence); failure to read route/process/artifact is `NotConverged`, never success. Generalizes the stateless-frontend MVP's receipt into the fabric's service story. **Accept (T4; [checklist](docs/plans/fleet-acceptance-criteria.md)):** one real service on srv1/srv2 applied AND retracted — read-back proves process · listening port · route · served digest == desired; re-apply is a noop; after retract, read-back proves ABSENCE; RED: right process wrong digest → `NotConverged` · route present but port dead → `NotConverged` · shell exit-0 with failed read-back → `NotConverged`.
- [ ] **G3 cgroup caps derived + reconciled** — `TasksMax`/`MemoryMax` are host-set by hand and only *read* live; derive from the budget tree + reconcile via converge so a hand-edit reds.
- [ ] **G1 placement** — job→host is GitHub-native, demand-blind, first-idle → heavy runs co-reside while the other host idles. LIVE again since #6111 put required CI back on the fleet; the admission model above is its demand side. [plan](docs/plans/compute-envelope-model.md)
- [ ] **shell emission model** — `emit(intent, Bash)`; scoped by the bash-minimization rule (operator 2026-07-03): bash is emitted only into foreign executors + bootstrap windows; where the runtime is present, effects are typed argv/REST or binary-interpreted plans (the tail slices are shelved). [plan](docs/plans/shell-emission-model.md)
  - [ ] **orchestration emission → agnostic registry dispatch** (#6106) — de-bash the compiler stage (in flight) — ⏳ awaiting sign-off
    - [ ] **emit partition cleanup — shell/json/yaml as grammar rows, zero language knowledge in the wrong layer** *(operator directive, 2026-07-01)* — one grammar per language, read in both directions (DESIGN §4); emission is rows in `extdeps/languages/`, never stage code. Four leftovers: (a) `src/v2/compiler/05_emit_orchestration.dag` still carries orchestration→bash lowering IN the compiler stage (the retry 2-level expansion, seq-join) — the #6106 registry dispatch is the exit mechanism; the former env-weld half is DISCHARGED (#5868 grew the bash AST `EnvUnset` + multi-binding `EnvPrefixed`; #6137 dissolved `orch_emit_run_env_welded`); the stage keeps ONE agnostic fold. (b) the bash grammar itself is FORKED across trees — `dag/extdeps/languages/bash/` vs `src/v2/extdeps/languages/bash.dag`+`bash_command_fold.dag` — the same §3 class as the `extdeps.shell` fork (hotfixed #6112), one authority must win. (c) JSON has no grammar authority: N hand-rolled emitters (`roadmap_spawner`'s `json_escape`/`json_str` is a marked scaffold dissolving to `std.primitives.to_json`; `bmc_onboard` and `tailscale_acl_emit` carry their own). (d) YAML: `ci_yaml_emit`/`ci_yaml_validate`/`gha_yaml_fold_pilot` sit in `dag/gunbc/` (workflow layer) while the fold pilot is `src/v2/extdeps/languages/gha_workflow_yaml_fold.dag` — YAML-the-language rows belong in `extdeps/languages/` with GHA-workflow as rows on top, retiring the `yaml_check` Rust scaffold once parse is grammar-owned. The construction wall that makes the splice class unwritable (a string literal is an ATOM, never a COMPOSITION — joins live in `extdeps/languages` rows, never workflow code): [no-smuggled-programs wall dissolved with program.dag FULL DELETE — shell → intent design](docs/plans/shell-intent-emit-realization-design.md).
  - [x] **slice 0 — CI EAGAIN-retry** ✓ (#6467) — `ci_cargo_eagain_retry_core` → `emit(Retry, Bash)`; byte-oracle = committed `ci.yml` [plan](docs/plans/shell-emission-model.md) — ✓ signed off: operator
    - [x] **slice 1 — control-flow emit (census-scoped)** ✓ (#6475; tier-2 Procedure/Let band #6566) — the `If` band only (`orch_emit_step::If` + else + condition forms + pipes/cmdsubst/`$?`/AndOr/redirect/env words, byte goldens); `For`/`While` NOT in scope — the pre-runtime census (2026-07-03) found zero pre-runtime sites needing them [plan](docs/plans/shell-emission-model.md) — ✓ signed off: operator
      - [ ] **slice 2 — converge thin-run** — implementation MERGED (#6572 keystone `ConvergePlan`+`EmitArtifactThenThinRun`+`converge_apply` · #6585 golden shrink 323→21 lines · #6598 `gunbc converge --host` CLI receiver), operator sign-off PENDING — fleet_converge steady-state interpreted in the binary as a typed plan via `apply()`; emitted bash is the fresh-standup bootstrap fragment + thin invocation lines (supersedes 'emit the whole `.github/fleet-converge.sh`'). FLAGs 2a(i)/2b/2c signed/discharged 2026-07-14 ([census](docs/plans/shell-to-dag-residual-census-and-arc-completion.md) §2); receipts re-verified by execution 2026-07-20 (`fleet_converge_emit`/`fleet_converge_apply` witnesses). Flip to `done: true` + `sign(operator)` on sign-off (mirrors `6-shell-intent-phase1`). [plan](docs/plans/shell-emission-model.md)
  - [ ] **shell→intent Phase 1 — agnostic orchestration emit** — MERGED (#6832), operator sign-off PENDING — `While`/`BoundedPoll`/general `Retry` (N-level escalation) emit via `05_emit_orchestration` + bash grammar rows; byte goldens `orchestration_*_emit_test`; production consumer `ci_floor_peak_emit.dag`. Flip to `done: true` + `sign(operator)` on sign-off (mirrors `6-shell-slice2`). [plan](docs/plans/shell-intent-emit-realization-design.md)
- [ ] **`apply()` Phases B–F** — B `host_exec`→`apply()` (gated on srv3 OsInstalled) · C Redfish live (partially via #6097) · D converge lane = EmitArtifactThenThinRun handler (first ctrl LOC deleted) · E pull-mode self-converge (autoinstall plants the on-host agent; the push star retires) · F decom. Phase A landed (#5756). [plan](docs/plans/host-effect-orchestration.md)
  - [ ] **temporal-effect-spine-a — temporal realization spine (T1 vocabulary)** *(operator 2026-07-02; not a workflow engine)* — `std.temporal_effect`: durable facts + `plan_next_step_from_prior_receipt_and_lease` (single prior+lease; list fold is consumer-side); 🟡 markers on stringly receipt labels + derived step id. RED: approval/lease/read-back gates. **Non-goals:** live mutation, DB, scheduler. **Accept (T1):** witness suite green. — ⏳ awaiting sign-off
    - [ ] **srv3-install-reconcile-a — dry-run reconcile entrypoint** *(queued; gated on temporal-effect-spine-a + os-install-deduction-a)* — `InstallAttemptIntent` + workflow steps as data; `srv3_os_install_reconcile` folds preflight + diagnostic + temporal spine; dry-run first. **Accept (T3):** dry-run receipt on srv1 actuator host. — ⏳ awaiting sign-off
  - [ ] **os-install-deduction-a — preflight + KVM diagnostic vocabulary (T1)** *(operator 2026-07-02; deduction half; NOT wired to srv3_os_install_diagnostic until reconcile-a)* — `gunbc.os_install_deduction`: `fold_nbd_proxy_os_install_preflight_verdict` (NBD-proxy/on-ISO actuator scope — NoCloudNet incomplete for this path only), storage policy on payload, `TargetDiskState`, weak KVM verdicts (`KvmFirmwareIdleObserved`, `KvmSuggestsOsBooted`; `OsInstalled` reserved for router read-back). RED: remove storage flips ReadyToBoot; KVM login ≠ OsInstalled. **Non-goals:** live mutation, monitoring product, DB. **Accept (T1):** witness suite green. — ⏳ awaiting sign-off
- [ ] **srv3 → OsInstalled** — finish the greenfield install over the solve-driven NBD-proxy actuator (#6097, live BMC); proves the install band end-to-end. [plan](docs/plans/srv3-webui-kvm-virtual-media.md)
  - [ ] **resource-namespace-upsert-a — vocabulary only** *(#6134 srv3 bringup follow-up; operator reshape 2026-07-03)* — `std.upsert_decision` (`ObservationVerdict` + `UpsertDecision<P>`); domain modules `gunbc.file_access`, `gunbc.session_lease`, `gunbc.install_media`. RED: foreign process on 10809=Conflict not kill; outside-namespace chmod=Refuse; drifted seeded ISO plans Remaster not Converged. Non-goal: live srv3 mutation. — ⏳ awaiting sign-off
  - [ ] **install-media-generic-layer — extract Ubuntu flavor from process** — split `extdeps/os/install_media.dag` generic shapes (fetch/remaster/serve verdicts) from `ubuntu_*` flavor rows; move `.gunbc-content-hash` sidecar + remaster policy to `gunbc/install_media_*`; mandatory before second OS/distro. Post-#6134.
  - [ ] **nbd-serve-held-session-lease** — NbdProxyServeSession intent with port/pid/cmdline/ISO/token fingerprint; classification + plan INSTANTIATE `std.upsert_decision` (`ObservationVerdict` + `UpsertDecision<P>`; domain facts as the P payload — do NOT mint a parallel lease vocabulary, cf. the fork-census 2026-07-03). RED: foreign 10809 no kill; stale matching session drains only. Live consumer: srv3 actuator. — ⏳ awaiting sign-off
  - [ ] **srv3-boot-action-diagnostic — Redfish vs ipmitool fallback state machine** — separate transports for BmcBootAction; boot refused unless serve RunningExpected; NoRebootObserved → optional IpmiResetFallback with separate approval; OutOfBandHostActionReceipt until modeled.
  - [ ] **os-install generic naming cleanup** — rename generic behavior tests/modules away from `srv3_*` where testing credential resolution, remaster classification, workflow escalation; keep srv3 only in instance fixtures (`srv3_pre_install_lease_table`, `srv3_seeded_install_media_artifact`). Post-#6134.

**Fabric dispatch — RunShape → Allocation → Receipt (GitHub is the queue, not the scheduler):**

- [ ] **StrictLease allocation model (Policy 0)** — every provider exposes strict, non-oversubscribed `RunShape` offers first: one allocation consumes its declared RAM/swap/CPU budget, period. srv1/srv2 are modeled like ephemeral providers (N fixed slots, hard cgroup caps, read-back receipts before work); Ubicloud is a provider offer with `EphemeralVm` isolation. Burst/oversubscription explicitly out of scope. Four nouns, no scheduler: `RunShape` (arch/cpu_tokens/ram/swap/disk/duration/trust/workload_class) · `ProviderOffer` (provider/isolation/limits/oversubscription_policy/cost) · `Allocation` (shape×offer→target+lease) · `Receipt` (effective limits observed, peak usage, pressure/OOM events, release result). **Accept ([checklist](docs/plans/fleet-acceptance-criteria.md)):** one real CI job runs under a `StrictLease` allocation on srv1/srv2 whose `Receipt` proves effective limits == the offer's declared limits (read back from the live cgroup, never asserted), and a second job requesting more than remaining capacity is refused a lease rather than co-scheduled.
  - [ ] **CI shape labels — `runs-on` as a projection of the fabric model** — `ci.yml` emits `runs-on` labels derived from `RunShape` (e.g. `gunbc-shape-ci-42c-128g`), never host names; the runner registration/converge layer ensures the right runners carry those labels. GitHub remains the queue and **runner availability is the backpressure**: register slots per shape at safe strict capacity (e.g. 2 `shape-rust-heavy` + more light), NOT 10 equivalent heavy runners when only 2 heavy jobs are safe. A fabric-control-plane allocator (a `.dag` control server minting leases/JIT runners before dispatch) is the LATER architecture — only after srv1/srv2 are safely mutable and readable.
  - [ ] **ProviderOffer rows + the dormant-Ubicloud design-break probe** — srv1/srv2 fixed-host slots and Ubicloud ephemeral VMs inhabit the same offer type; add the Ubicloud row `Dormant` (16 vCPU/48GiB arm64, oversubscription: None) **without running it**. Acceptance = adding the offer changes no scheduler/control-plane invariant: witnesses prove (a) allocation accepts both `SrvHostSlot` and `UbicloudVmOffer`, (b) host-mutation code REFUSES Ubicloud offers (no cgroup target to mutate), (c) deploy-to-srv1 refuses `LocalShell` unless the allocation target proves host=srv1, (d) emitted labels encode shape, not provider host details. If adding Ubicloud forces a special case anywhere, the abstraction is wrong — this probe answers "does our design break?" before it can cost anything.
    - [ ] **BurstLease (Policy 1) — oversubscription earned by receipts** — co-resident allocations on one host only after pressure/OOM/swap receipts prove the measured peaks allow it; StrictLease stays the default and the fallback. This is where stranded cores come back (sessions, compression, IO, smaller jobs) — on 128c/125GiB, strict memory allocation strands CPU and that is correct: **the first goal is non-death, not full utilization**. Gated on the sound-host-admission receipts and the live read seam.

**Ordering (operator-signed):** ① CD target-host proof → ② stateless-frontend MVP receipt (milestone A, on the existing deploy path) → ③ live read seam + privilege model → ④ one converge hunk closed-loop end-to-end (the narrow srv2 acceptance case) → ⑤ service allocation generalized from the frontend MVP → ⑥ StrictLease + shape labels; register srv1/srv2 slots by shape → ⑦ dormant Ubicloud row proves no design break → ⑧ CI jobs move to shape labels → ⑨ receipts (effective limits · peaks · OOM/swap pressure) → ⑩ BurstLease / dynamic routing / richer placement. ①–④ make infra changeable (everything else is paper until then); ⑤ proves the fabric is not just CI; ⑥ fixes backpressure with no scheduler; ⑦ validates the abstraction before we depend on it; ⑧–⑨ make CI consume the model; ⑩ recovers stranded cores.

**Fleet hardening — make our metal safe for the load it carries:**

- [ ] **the return happened ungated** — #6111 put required CI back on srv1/srv2 by operator call, accepting interim OOM/sccache exposure; the items below are the gates the return *should* have had, now owed as hardening. Ubicloud remains the burst/fallback offer (and the slot-shape template the admission model generalizes).
  - [ ] **SessionSliceEnforcement + verified-effective caps** — agent-session cgroups have no citable verified-effective cap (the residual over-commit vector on srv1/srv2); the humming apply-rule stands: runner caps must not tighten while sessions are uncapped and oomd is absent.
  - [ ] **OOM classification consumer** — `ClusteredOutOfMemoryKill` is modeled but absent from the active infra-retry signature roster; build the consumer so an OOM is first-class, never EAGAIN-shaped. [plan](docs/plans/ci-oom-reclassification.md)
- [ ] **resource-aware scheduler (implementation)** — re-base required: the signed design's authored widths + per-shard peak predictions were deleted by the 2026-07-12 adaptive-width ruling (gunbc_ci_adaptive_width_note) in favor of the runtime AIMD memory governor; the governor's declared dissolve-on is graph-derived per-node demand (CostAccount.space measured — witness-realization measured-space lane, #6738 P1–P6 + #6855 P4 advisory packing width landed). Re-base phases A–C onto governor + measurement lane before dispatching. [tracker](docs/plans/resource-aware-scheduler.md)
- [ ] **resource budget tree** — admission (construction) · conserve-lens (honest residue) · runtime reconcile (fail-closed handler), three verdicts never conflated; subsumes spawn-width/placement/compile-jobs as consumer leaves. Protective only when paired with an enforcement actuator (cgroup `memory.max`, operator-fenced). [carrier](dag/product/budget_tree.dag) [grounding](docs/plans/budget-tree.md)
- [ ] **one Placement authority** — jobs (GitHub) · threads (`spawn_width`) · sessions (ctrl capacity) are 3 forks of "put work on a host"; `Placement`/`Materialization` are modeled but inert — CI consuming them is the lane's real deliverable.

## 3. The floor — audit what we have (does it actually do what it claims?)

Both pillars ride the CI floor: tree-wide marker-driven witness discovery · compile-clean gate · generated-artifact drift gates (this document is one) · `regen --verify` (#5873) · doc-graph reachability · emit-determinism (#5941) · the interim self-host comparison (#6009). Those stand. The floor's *expansion* lanes (testgen, wiring-liveness, complexity gates, lens meta-walls) are shelved — a new wall must displace a measured cost on a pillar path to un-shelve (the **enforcement-intent gate** below is the first such un-shelve, 2026-07-02: displaced cost = the operator's repeated manual *what-is-enforced-by-what-lens-over-what-corpus* join, plus CI-killing quadratics and repeated dual-representation/anemia rediscovery).

**Audit directive (operator, 2026-07-01): thoroughly audit what we have and what it actually does.** For each load-bearing mechanism, prove **by execution** that it does what it claims, with a **discriminating perturb** that goes RED when the behavior is wrong — a typecheck or a grep is not a consumer (DESIGN §5), and a wired gate can still be vacuous. A mechanism claim without a red-control is *unverified*, whatever its docs say. The reset's own mapping found the pattern live: the committed converge shell is spec-without-execution (§2), the fixpoint gate self-declares interim (2-of-92 files), cargo-green is an ignored test. Highest-stakes mechanisms first:

**Audit — green by execution, red by perturbation:**

- [ ] **affected-set lens — discharged by a standing mechanism (audit 2026-07-23)**: (a) soundness = the 4-hourly falsifier runs the corpus cold with predictions recorded, counted divergence is the RED — and it discriminated live (the 07-16..07-21 red period was real roster gaps, healed #7083; alert loudness #7022); (b) effectiveness = discovery-flip receipt 8→~1,721 witnesses/PR (ci_spec_discovery_flip_note, operator acceptance 2026-07-09) + regen affected-set ~4m45s/PR (#6732) + compile-clean closure scoping (#6658); (c) fail-closed = #6525 ReadsLiveTree never-skip and a provenance gap REFUSES, never widens (the original run-all wording predates the §5 absorbing-fallback ruling). — ⏳ awaiting sign-off
- [x] **mechanism inventory with red-controls** — census every wired gate/lens/cache (floor-plan roster + `commit_workflow`): what it claims · its discriminating RED witness · its last execution receipt. Anything without a red-control gets one or gets demoted/deleted. The inert-lens backstop (#5433) covers *wiring*; this covers *claim-vs-behavior*. [plan](docs/plans/mechanism-inventory-red-controls.md) — ✓ signed off: operator
- [ ] **cache honesty re-receipt** — re-run the warm==cold purity oracle against CURRENT main (616/616 was pre-reset), and close the sccache exit-0-no-binary class (below); a cache that lies is worse than no cache.
- [ ] **artifact-freshness gate** *(operator ask: "we need a freshness gate, if we don't already have one")* — we half-do: the per-PR drift gate proves committed==emitted for the PR's OWN tree, but drift still LANDED on main (`.gitignore` stale vs its authority since #6097; found and healed by #6110/#6111) because admission never re-validates against CURRENT main — green-on-branch is not green-on-main. The closing mechanism is the §2 merge-admission freshness block (HELD); this item is its receipt pair: (a) reproduce the landed-drift path as a RED witness, (b) confirm the freshness block refuses it once un-HELD.
- [x] ~~**one tree, one verdict** — the concrete forks dissolved: #6950 gate loader runs both closures (the exact gate-green/floor-red fork), #6935 reference_resolution_facts builtin de-forked the module-graph host; the systemic one-resolution-authority ask IS the namespace lane. Residual to carry: the gate-equivalent local invocation is still flag-sensitive (--dependency-pool-index primary-precedence).~~ **superseded →** namespace-only resolution lane (operator-signed 2026-07-06) — docs/plans/namespace-resolution-design.md
- [ ] **enforcement intent + model-quality walls** — substantially LANDED, re-based 2026-07-23: carrier family src/v2/lens/enforcement/ (standing_intent, contract, gate, receipts) + enforcement_live.dag on main; children A+B via #6756 (Question-0 lens-module gate, StandingIntent fail-closed); C carrier standing_intent_complexity_repo_wide exists; D's object rule effectively landed (#6527 accumulator-copy as required root-grain compile lens + #6540); E first consumer merged (model_anemia lens). Post-landing churn: #7070 de-enrolled lens_module_gate from the compile door (corpus-grain invariant, tree-grain roster). REMAINING: fold that churn in, finish the C receipts, close the DESIGN open thread. [design](docs/plans/enforcement-intent-design.md)

**Cost / risk / benefit — the floor's unmodeled optimization:**

- [ ] **model cost/risk/benefit — "what do we actually run" is one optimization, not a pile of policies** *(operator working session 2026-07-12)* — affected-set, wet/dry (mocks), receipt/`long`, `ReadsLiveTree` never-skip, and the test-kind names (`test`/`receipt`/`demo`/`integration`, `WitnessKind`, `TestClassification`) are all **unmodeled proxies** for `argmax(benefit − cost)` under a budget — hand-tuned stand-ins (mostly astrology) that exist because cost/risk/benefit were never first-class. Root anemia: `test fn` bakes in `cost ≈ 0, run always`, which is false (`s1_closure` ≈ 10min wet). **Invariant that keeps it fail-closed:** you may trade fidelity for cost (mocks, skips, deferrals), but the risk you trade away must stay **observable on a cadence** — deferred-and-detected, never silent (§5; a silent widen is the absorbing fallback). This inverts the tree's current *wet-is-sacred* default: wet is just the highest-cost fidelity point, usually not worth it. **Bootstrappable now:** a claim's expected internal work is inspectable (inputs/expected-output node shape) and its effects enumerable (which service ops, how many times via fold/loop bounds), each effect carrying a latency expectation; the floor already MEASURES `wall_nanos`/cgroup-peak per claim, so cost = measured receipt now → graph-derived later (§4). **Landed as approximation in #6506 (Stages 0–2):** hermetic-by-default floor, a flat hand-set **5s** per-witness budget (fail-closed `EvalBudgetExceeded`), and a `test/claim/long/` cadence — right shape, interim stand-ins; forward work is Stages 3–5. **Missing pieces (highlighted, not papered over):** no per-effect cost model; cost only bounded by the flat 5s budget, not derived from the graph (fail-closed = expensive on unknown fold bounds); the budget is flat/hand-set, not a per-run wall-clock allocated by value/cost; **benefit unmodeled** — only footholds are the §5 lower bound (no discriminating red ⟹ ~0 gate-value ⟹ drop, this is what a pure `demo` is) and observed red-frequency; the proxies aren't yet reconciled to the axes. **Sequence:** cost first (compute/resources, from what's measurable now) with budget-aware admission and *counted typed deferrals* (never silent skip) → benefit second (§5 lower bound + red-frequency, declared interim for the rest) → then affected-set/wet-dry/cadence/test-kinds collapse into derived readings and the qualitative vocabulary is deleted, not extended. [cost/risk/benefit model](docs/plans/cost-risk-benefit-floor-model.md)

**Open floor holes:**

- [ ] **full-corpus selector-backstop — live at 4-hourly cadence (audit 2026-07-23)**: falsifier.yml cron runs the corpus cold with predictions recorded (the flip note names it the selection's soundness governor); falsifier-alert (#7022) closes the silent-red loudness gap; green again per #7083. Regen's cold control is separate (unconditional main-push regen). — ⏳ awaiting sign-off
- [ ] **tree-scoped builtin registry** (fail-closed) — the global seed registry leaks intrinsics into the substrate compile, making compile-clean falsely green; instance fix #5452, class fix (partition) open [force-check plan](docs/plans/compile-clean-forcecheck.md)
- [ ] **kill sccache false-greens** — exit-0-no-binary CLOSED: claim_executor --verify-build-artifacts asserts every declared release binary exists + is executable at 3 ci.yml sites (#6472-era); deploy rebuild-flake killed by artifact reuse (#6810). REMAINING residue: freshness not asserted; sccache local-disk content_verified_on_read=false with live repro (#7031 — the cache-lies class is confirmed live); the ci_spec absorbing retry-fallback removal is srvN build-cache STEP-2, gated on srv1 (STEP-1 #6835).

## 4. Shelved (parked 2026-07-01 — not deleted)

Every lane below was live roadmap before the reset. Shelved = plan docs and carriers stay in place, nothing dispatches from here, and the git-history ROADMAP.md holds the full pre-reset detail (structure, milestones, sign-off states). Un-shelve by PR into §1–§3, priced in displaced cost. **(2026-07-06: the *complexity budget whole-codebase*, *fail-closed meta band*, and *fold-ergonomics* lanes are un-shelved into §0's priority order ② and ⑤ — this reshuffle is that PR; their nodes stay catalogued here until relocated into their §2/§3 lanes.)**

- **fold-ergonomics lane** — inert-abstraction lens · non-fold-residue audit · staging combinator · ban source comments [charter](docs/plans/fold-ergonomics.md) [residue catalogue](docs/plans/non-fold-irreducible-residue-catalogue.md) [debt sample audit](docs/plans/real-debt-syntactic-sample-audit.md) [eval-bind PROPOSE](docs/plans/eval-bind-node-eval-propose.md)
- **fail-closed meta band** — `Value::Null` split (the deep root) · Disposition carrier · reference grounding · cardinality refinement · expressibility frontier · reachability-completeness lens · axiom+syllogism lens · self-applying lenses · inert-lens promotion + gate-hygiene residue [lockdown](docs/plans/fail-closed-lockdown.md) [fork plan](docs/plans/model-realization-fork.md) [Disposition](docs/plans/disposition-carrier.md) [reference grounding](docs/plans/reference-grounding-migration.md) [cardinality](docs/plans/cardinality-refinement.md) [P1 where-lowering](docs/plans/p1-where-clause-lowering.md) [frontier](docs/plans/expressibility-frontier.md) [inert-layer lens](docs/plans/inert-layer-lens.md) [axiom lens](docs/plans/axiom-syllogism-lens.md) [self-applying](docs/plans/self-applying-lenses.md) [construction rule](docs/plans/construction-justification-rule.md)
- **rust-gate `.dag`→rust coverage wall (edge-b)** + stage0 clone-census ratchet [brief](docs/plans/edge-b-rust-dag-provenance-brief.md)

- [x] ~~**complexity budget whole-codebase** — un-shelved and live in tree; the algebraic-rewrite construction engine remains parked ([rewrite plan](docs/plans/algebraic-rewrite-optimization.md)).~~ **superseded →** live complexity-enforcement carriers — #6527 accumulator-copy required compile lens · #6540 affected-set floor enforcement · #6632 per-op cost derivation

- **testgen oracle + wiring-liveness family** (compile-time wiring lens ✓ #5679 · pre-send guard ✓ #5683 stand; the generator/preflight expansion parks) [method](docs/plans/testgen-oracle.md) [wiring](docs/plans/wiring-liveness-preflight.md)
- **caching completion** — `realize(subject)` one door (P2) · M5 fixture-store · native `content(T)` (P5) · B1 generic-instantiation / B2 cross-tree content-hash blockers · one Materialization kernel · shared secrets model [loop](docs/plans/realization-measurement-loop.md) [M4](docs/plans/m4-universal-hermetic-corpus.md) [M5](docs/plans/m5-fixture-store-consolidation.md)
- **CI inline-shell de-fork · interpreter terminal-output de-fork** (seed forks of modeled authorities; both shrink as v1 burns down) [emission-ingestion](docs/plans/emission-ingestion-inverse.md)
- **idea machine** — ingest-at-large · English closure/round-trip · cross-media (JSON/react/diagram) · `Medium` homomorphisms · format-model C4–C6 · fidelity compose-up · eval generalization · invert-hand-maintained residue [plan](docs/plans/idea-machine.md) [format reconciliation](docs/plans/format-model-reconciliation.md) [invert](docs/plans/invert-hand-maintained.md)

- [x] ~~**shell-emission slices 3–4** — absorbed: #6750 routed bmc_token_federation Do rows + ci_workflow RunSteps through orch_emit_step; githooks thin shim in tree; live_deploy re-modeled on the fleet-reconcile spine (#6820/#7086), not a frozen golden.~~ **superseded →** shell→intent arc — DESIGN open thread + docs/plans/shell-to-dag-residual-census-and-arc-completion.md

- **TypeScript first-class emit + TS self-host** — END GOAL UNCHANGED (both realizations, per plan do-not-relitigate); resumes after the Rust fixed point [gap census](docs/plans/typescript-gap-census.md)
- **language-target self-host frontier** — the get-to-self-host path for all emission languages (Rust as model; Verilog/SPICE/LLVM as design-stress falsifiers); solve resolved as higher-order not a primitive [frontier](docs/plans/language-target-self-host-frontier.md) [solve rationale](docs/plans/solve-higher-order-design.md)

- [x] ~~**HTML/React + live dashboard lane** — both halves now owned elsewhere: the page/frontend by the site-subsumption lane; the blessed keep-running JS server is a named §3 dual representation slated for deletion by belt B.~~ **superseded →** gunb.ai site-subsumption lane (#6927 Phase-0) + gunbc-served dashboard (belt B, #6940)
- [x] ~~**session dashboard + roadmap-as-spawner** — the operator-clicked dispatch actuator became the conveyor belt and is LIVE. Still true from this row: the automatic spawn bridge stays fail-closed paused.~~ **superseded →** conveyor-belt lane — belt A+B merged (#6940), belt_dispatch_node single dispatch authority (#6914), go-live #7096

- **gunbhub** (own the Git/CI engine, closes G6) · internal repo model · compute fabric as a sellable piece
- **privacy / isolation model for the compute fabric** — trust boundaries, tenant isolation, what the fabric may observe about a workload; was compute-fabric downstream work pre-reset, parked here so it is not lost (`trust_class` on `RunShape` is its active §2 seam). Includes committed-secret references (operator 2026-07-02, not urgent): a typed `SecretRef` — GCP Secret Manager resource name + version/content-hash — so sensitive facts (router serial, WAN MAC, public prefixes, credentials) commit as model-legible references while values stay in Secret Manager; redaction by construction (public shapes carry no raw-value field); first consumer: the network-identity observation fixtures.
- **structural correctness walls (standing, not active)** — generated-output gate · coproduct exhaustiveness · cross-representation equality · oracle-method map; they keep gating, no expansion work dispatches from here
- **design sketches without an active lane** [orchestration-as-intent](docs/plans/orchestration-as-intent-design.md) [model-grounding extract](docs/plans/model-grounding-lens-extract.md) [node-minimal representation](docs/plans/node-minimal-representation-sketch.md) [func-env sigs](docs/plans/func-env-sigs-single-authority.md) [resolved-graph minimization](docs/plans/resolved-graph-representation-minimization.md) [emit-host batch isolation](docs/plans/emit-host-batch-isolation.md) [accelerator roundtrip](docs/plans/accelerator-demo-roundtrip.md) [commit workflow](docs/plans/commit-workflow.md) [input envelope](docs/plans/input-envelope-roadmap.md) [seed-debt bundle 2](docs/plans/seed-debt-bundle-item-2.md) [resolver collision wall](docs/plans/resolver-type-name-collision-wall.md) [regime-2 shared emission](docs/plans/regime2-shared-emission-fold.md)
