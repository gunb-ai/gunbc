module fixture.import_binding_homonym.bystander

import std.types { Int }
import v2.std.integer { UInt8 }

// THE BYSTANDER. It names the OTHER declaration of the same short type name, and the consumer
// beside it does not import this module at all. Its only role is to be IN THE CLOSURE, so that
// two authorities for the bare spelling `UInt8` reach emission together.
fn bystander_octet_width(x: UInt8) -> Int {
  0
}
