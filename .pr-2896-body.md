## Summary

Gate **#87** (`lens_cementing_test_discipline_complete`) cementing harness sweep (G87-D2):

- **Registry ↔ runner drift:** landed `t_r3_gate_87_cementing_regen_parallelism.dag` and a matching `R3_GATE_87_CEMENTING_REGEN_SUITES` row so `regen.dag`’s `parallelism` lens has the same PB-B-1 inventory as the other regen lenses (post-#2795 `lens_parallelism_entry` co-land).
- **`variant_payload`:** replaced the authorable `Compiles` placeholder with a **`LensOutputEquals`** receipt using the existing **Int projection** pattern: claim program authors `Gate87EmptyPayload` (empty product); `eval_gate_87_cementing_projection` asserts `variant_payload_shape` returns `Found(Empty)`.
- **`infer_helpers` / `lower_helpers`:** remain explicit **`Compiles`** rows (behaviorally N/A / no honest single carrier per register + lens comments); **refreshed named blockers** in harness headers (`r3-structure.md`, `v3-lens-capability-register.md`, lens STOP boundaries).
- **`test_runner.rs`:** added `gate87_parallelism_anchor_no_lane2_workflow_projection` and `gate87_variant_payload_empty_product_found` arms (fail-closed on missing anchor / declaration; typed `matches!` on canonical reports).
- **Docs:** `r3_gate_87_lens_cementing_regen_receipts_test` module comment aligned with the new `variant_payload` `.dag` receipt shape.

## Test plan

Local / remote BuildBuddy (session default):

- `cargo test -p v3-compiler r3_gate_87` — **pass** (gate-87 integration + PB-B-1 runner sweep).
- `cargo test -p v3-compiler r3_gate_87_regen_lens_registry_names_match_fixture_inventory` — **pass** (registry name set vs `R3_GATE_87_CEMENTING_REGEN_SUITES`).
- `cargo clippy -p v3-compiler --all-targets -- -D warnings` — run on touched crate before push where feasible; **CI is source of truth** for full matrix.

## Worker attestation

- [x] Title describes the change (not the session id or branch).
- [x] PR body summarises *what* and *why* (this section replaces the prior TODO).
- [x] Tests run: `cargo test -p v3-compiler r3_gate_87` (and targeted inventory test above); green at last author push; full `v3` / CI job set tracked on GitHub Actions.
- [ ] Internal dashboard work item (not a GitHub issue); no `Closes #N`.
- [x] Branch commits are session-owned work + merge of `origin/main` + doc alignment.
- [x] No secrets / credentials / large binaries.
