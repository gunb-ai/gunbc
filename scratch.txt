CI-REWORK SCRATCH — modeling smells (temporary; delete before merge)

RUBRIC: path is the discriminator; right altitude, own clear place; no catch-alls; a high concept owns a registration concept, not instances; personal inventory seeds genuine vendor models, it does not live in std/ concepts.

LAYERS: std = standard library (universal primitives). extdeps = external vendors (Ampere, Hetzner). compute_fabric = a PRODUCT domain model (not std). operator's ACTUAL instantiation (srv1/srv2 real machines) = private repo (ctrl), out of gunbc.

ITEMS ([x]=done  [~]=partial  [ ]=todo  [P]=parked for a dedicated design pass)
[~] 1. compute_fabric owns the exact computers. DONE: dead throttle constants removed (cleared gate-1 :541); srv1/srv2+Hetzner instances shed -> generic example_shared_host/example_vm fixtures; wrong-direction `import extdeps.cloud.hetzner` removed; repro witness repointed. LEFT: real instances re-land in ctrl (item 6); memory/operator_fleet delete (item 2).
[x] 2. std/memory/operator_fleet.dag DELETED from gunbc (srv1/srv2 DIMM+RAM instances re-homed to ctrl operator_fleet.dag, item 6). No .dag consumed it; integration.rs parser-smoke + placement_supply/comment updated. gate-1 PASS.
[x] 3. std/cpu/ampere.dag -> extdeps/cpu/ampere.dag (vendor catalog as CpuModelCatalogRow values; imports std.cpu shape). std file deleted; integration.rs include_str repointed.
[x] 4. std/cpu/types.dag — AmpereCpuModel + CpuIdentity DISSOLVED; CpuModelCatalogRow gains `vendor: CpuVendor`; CpuFacts now { catalog: CpuModelCatalogRow, deployment }.
[x] 5. std/cpu.dag — cpu_catalog_row dispatch removed; cpu_vendor(cpu)=cpu.catalog.vendor, cpu_facts_* read cpu.catalog.*; std/cpu is the general CPU concept only. gate-1 PASS.
[~] 6. operator fleet actual config -> WRITTEN at ctrl plans/models/fabric/operator_fleet.dag (real srv1/srv2 Ampere offers + DIMM populations + Hetzner CAX41), importing gunbc std.compute_fabric concept + extdeps.cpu.ampere + extdeps.cloud.hetzner. container_vm_consumer.dag repointed off the (now-shed) gunbc supply_srv*_offer to the local file. LEFT: bump ctrl third_party/gunbc submodule past #4960 merge + open ctrl PR — BLOCKED on #4960 merging (ctrl imports the new extdeps.cpu.ampere / vendor-agnostic CpuFacts that only exist post-merge; cannot validate against the pinned old gunbc).
[x] 7. compute_fabric moved std/ -> product/ (dsl/product/compute_fabric.dag, module product.compute_fabric). It's a DOMAIN MODEL not a std primitive; imports std/ only, sits as a product peer alongside gunbc/, demos/, ctrl/ (already outside the §P2 core std->extdeps->compiler->workflow DAG, so NO invariant change). Functional blast radius was TINY: 1 real import (probe repro) + 1 Rust include_str; rest were comments (updated for coherence, skipped child corpus file + dated docs/planning). gate-1 PASS. NOTE: ctrl branch imports std.compute_fabric in 4 files (operator_fleet, container_vm_consumer, agent_mapreduce, review_rotation_laws/gate) — repoint to product.compute_fabric when the ctrl PR lands (rides the same #4960-blocked submodule bump).
[~] 8. hand-maintained corpus/test row LISTS + individual ROW datums + COUNT constants. COUNTS deleted (crisp-bear-37 #5005, merged). LISTS + individual `data ci_runner_gate_row_*: AffectedSetNodeFrontierClaimRunRow` + `ci_runner_node_frontier_claim_run_rows` aggregate STILL PRESENT — operator wants ALL of them gone. ROOT CAUSE of "can't delete": the consumer is a BASH gate (scripts/v4-affected-set-node-frontier-gate.sh) that TEXT-SCRAPES the hand-typed List literal out of the .dag (awk on `/data ci_runner_node_frontier_claim_run_rows:/`). Delete the list -> the scraper has nothing to read -> gate breaks. To replace enrollment with reflection (discover_owned_data, default --scan-dir src/v4/test/claim), TWO capability gaps block it (20/54 rows uncovered): (1) ~9 claims rooted in dsl/ are OUTSIDE the discoverer's scan-dir; (2) ~11 claims are fn `..._holds() -> Bool` BoolWitness-shaped, but the discoverer enumerates owned DATA decls of a resolved type, NOT function-witnesses -> its transport misses them. Deleting the list before closing both = silently drop 20 claims = fabricated-green (violates fail-closed). REAL FIX = item 14 (same work): extend discoverer (scan union of claim roots incl dsl/; enumerate _holds fn-witnesses) + rewrite the gate .dag-native reflection-enrolled; THEN list + rows + bash script all dissolve together. NOT a piecemeal delete. (operator: keep open for now.)
[ ] 9. LENS: error on unused / consumerless ANYTHING — unused imports (v2 is silent today), dead data/types/concepts. A "no consumerless concept" gate.
[x] 11. socket "LGA4926" DECOMPOSED -> CpuSocket { package: SocketPackage, contact_count: Int } in std/cpu/types.dag (SocketPackage = LandGridArray | PinGridArray | BallGridArray; grounded w/ Anchor: en.wikipedia.org/wiki/CPU_socket). ampere rows construct CpuSocket { package: LandGridArray, contact_count: 4926 }. Nobody read .socket; 3 sites. gate-1 PASS. STILL OPEN as a LENS (item 9-adjacent): sweep other stringly closed-sets (model_number/oem_listing_id, ddr4_pc4_class, rank_label, provider strings) + a "no stringly-atom" gate.
[ ] 11b. STRINGLY ATOMS sweep (general lens, from item 11) — every leaf must be a genuine atom; anything decomposable decomposes; decomposition wording must be grounded/cited (if we can't name a token from a cited source, the model is wrong). Remaining String candidates: DramModuleCatalogRow.{ddr4_pc4_class (JEDEC speed bin), rank_label (-> rank count + organization)}, model_number, oem_listing_id, ProviderIdentity/HostIdentity. Pairs w/ item 9 as a "no stringly-atom" lens/gate.
[x] 10. SK Hynix DONE (mirror of items 3-5): std/memory/sk_hynix.dag -> extdeps/memory/sk_hynix.dag (catalog rows as DramModuleCatalogRow values); dissolved DramModuleIdentity + SkHynixDdr4Module SKU enums + std.memory.dram_module_catalog_row dispatch; MemoryPopulationRow now carries `module: DramModuleCatalogRow` (was dimm_identity); std.memory no longer imports any vendor catalog. integration.rs include_str repointed; ctrl operator_fleet.dag rows reconstructed. gate-1 PASS.
PARKED — STD/EXTDEPS BOUNDARY design pass (items 12 + 13). Deferred deliberately (operator + agent both unsure; load-bearing INVARIANTS line, not safe to improvise). Do as its OWN focused pass with a short design note that fixes the rule BEFORE touching code; not blocking, do AFTER the CI PR. Working heuristic so far: commercial/third-party/vendoring -> extdeps (CLEAR); daglang's own primitives/substrate -> std; the grey middle (grounded-universal concepts like ISA/math/units) decide case-by-case, NOT by hard rule. Foundational carriers daglang is built ON (math/algebra, measure, core types) stay std, external authority honored by Anchor citation not relocation (extdeps imports std only, so relocating them inverts the DAG). MOTIVATION EXAMPLES that triggered this (keep — they're the test cases the rule must classify): CpuVendor lists Intel/AMD/Apple/Arm/Qualcomm but only Ampere is modeled (speculative members); CpuArchitecture vs Arch vs TargetArchitecture all enumerate X86_64/Aarch64 (triplication); CpuVendor/DramManufacturer are closed company-enums while GpuFacts.vendor/AcceleratorFacts.vendor are open strings (inconsistent); ISA-family = grounded-but-external (the hard case the rule must adjudicate).

[P] 12. ARCHITECTURE enum TRIPLICATED in std (M9 DFS / dedup smell): `Arch` (std/types.dag, 11 members, "canonical platform/target vocab, aligned core/ir::platform"), `CpuArchitecture` (std/cpu/types.dag, 5), `TargetArchitecture` (std/architecture_profile.dag, 5) — all enumerate X86_64/Aarch64/Riscv64/Wasm32/... architecture_profile.dag imports ALL THREE. ISA family IS a grounded CS primitive so std is the right LAYER — the smell is THREE parallel authorities, not location. Fix: DFS to the canonical (Arch) and dissolve the other two (or promote one shared). Cross-layer (cpu, compute_fabric, extdeps/runtime/architecture/*, abi, platform).
[P] 13. HARDWARE-VENDOR identity: closed enums of COMPANIES in std + INCONSISTENT model. CpuVendor (Intel|Amd|Ampere|Apple|Arm|Qualcomm, std/cpu/types.dag) and DramManufacturer (SkHynix|Samsung|Micron, std/memory/types.dag) are closed enums of external companies — but GpuFacts.vendor / AcceleratorFacts.vendor (compute_fabric) are OPEN NonEmptyStr, and ProviderIdentity is an open branded NonEmptyStr. So "who made this hardware" is modeled 2 different ways. A company is an external entity, NOT a grounded primitive (unlike an ISA) — operator's instinct: vendor identity is extdeps-flavored. ALSO: of CpuVendor's 6 members only Ampere is modeled (Intel/AMD/Apple/Arm/Qualcomm consumerless/speculative — item 9). DECISION NEEDED: (a) open branded `HardwareVendor` identity in std, each vendor registers in extdeps (extensible, no std edit per vendor; loses exhaustiveness) vs (b) closed enum trimmed to modeled-only + treat growth as extdeps-driven. NOT std's job to enumerate every CPU/DRAM maker on earth. NOTE distinct concept: std/types.dag `Vendor` (UnknownVendor|Pc|Apple|W64) is the Rust TARGET-TRIPLE vendor field, NOT a manufacturer — name collision with CpuVendor; keep separate, maybe rename TargetTripleVendor.
[ ] 14. CI GATES ARE BASH SCRIPTS that text-scrape hand-typed .dag lists (operator: "our ci.yml is running a shell script"). The slim ci.yml floor shells out to scripts/v4-affected-set-node-frontier-gate.sh, v2-dsl-full-tree-parse-gate.sh, v4-claim-witness-corpus-gate.sh, v4-layering-imports-gate.sh, v4-affected-tests-gate.sh — each greps/awks .dag files for enrollment + runs claims. This is the ORIGINAL CI-floor brief (gate->.dag dissolution + script deletion, my task #4) AND the same root cause as item 8: the gates can't go .dag-native until enrollment comes from reflection/discovery instead of text-scraping literals, which needs the discoverer extended (dsl/ scan-root + BoolWitness `_holds` fn-witness enumeration). Do items 8 + 14 together: extend discover_owned_data, rewrite gates as .dag-native reflection-enrolled gate runners, then the hand-lists + the bash scripts both die. Sequence AFTER #4960 floor lands (don't churn ci.yml mid-review).

NOTES
- item 8 has been operator-requested for ~3-4 days with NO movement before this dispatch (crisp-bear-37) — long-stale; track that it actually lands this time.
- verified clean (no change): std/placement_supply.dag (concept-only).
- ctrl cloned at .ctrl-fleet-work (git-excluded); nodes.json is the fleet source-of-truth.
- stale doc-comment ref to hetzner_cax41_cost_quote in dsl/examples/cost_estimate/cost_estimate.dag (cosmetic).
