CI-REWORK SCRATCH — modeling smells (temporary; delete before merge)

RUBRIC: path is the discriminator; right altitude, own clear place; no catch-alls; a high concept owns a registration concept, not instances; personal inventory seeds genuine vendor models, it does not live in std/ concepts.

LAYERS: extdeps = external vendors (Ampere, Hetzner, ...). std = concepts. compute_fabric = high concept; imports vendor models only if genuinely needed (probably too early now).

ITEMS (verified vs branch HEAD)
1. std/compute_fabric.dag:460-635 — sheds concrete srv1/srv2+Hetzner instances; stay a pure concept (keep only the general CpuFacts shape), do not import vendor models yet.
2. std/memory/operator_fleet.dag — whole file is srv1/srv2 DIMM+RAM instances; belongs in an operator/deployment layer, not std/memory.
3. std/cpu/ampere.dag — Ampere is an external dependency; move to extdeps (e.g. extdeps/cpu/ampere/ or extdeps/hardware/ampere/altra/). Blast radius = only this 4-file cluster.
4. std/cpu/types.dag:13 — AmpereCpuModel enum is narrowed to the two owned SKUs; a concept type should not equal a purchase order. Ampere specifics follow item 3 into extdeps.
5. std/cpu.dag — cpu_model_catalog maps only the owned models; follows once 3+4 move; std/cpu keeps the general CPU concept only.

NOTES
- verified clean (no change): std/placement_supply.dag (concept-only).
- live CI blocker: compute_fabric.dag:541 multi-line `data =` fails gate-1 parse (decide: one-line now vs fix as part of item 1).
