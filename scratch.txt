CI-REWORK SCRATCH — modeling smells (temporary; delete before merge)

RUBRIC: path is the discriminator; right altitude, own clear place; no catch-alls; a high concept owns a registration concept, not instances; personal inventory seeds genuine vendor models, it does not live in std/ concepts.

LAYERS: std = standard library (universal primitives). extdeps = external vendors (Ampere, Hetzner). compute_fabric = a PRODUCT domain model (not std). operator's ACTUAL instantiation (srv1/srv2 real machines) = private repo (ctrl), out of gunbc.

ITEMS ([x]=done  [~]=partial  [ ]=todo)
[~] 1. compute_fabric owns the exact computers. DONE: dead throttle constants removed (cleared gate-1 :541); srv1/srv2+Hetzner instances shed -> generic example_shared_host/example_vm fixtures; wrong-direction `import extdeps.cloud.hetzner` removed; repro witness repointed. LEFT: real instances re-land in ctrl (item 6); memory/operator_fleet delete (item 2).
[ ] 2. std/memory/operator_fleet.dag — whole file is srv1/srv2 DIMM+RAM instances; delete from gunbc once item 6 lands them in ctrl.
[x] 3. std/cpu/ampere.dag -> extdeps/cpu/ampere.dag (vendor catalog as CpuModelCatalogRow values; imports std.cpu shape). std file deleted; integration.rs include_str repointed.
[x] 4. std/cpu/types.dag — AmpereCpuModel + CpuIdentity DISSOLVED; CpuModelCatalogRow gains `vendor: CpuVendor`; CpuFacts now { catalog: CpuModelCatalogRow, deployment }.
[x] 5. std/cpu.dag — cpu_catalog_row dispatch removed; cpu_vendor(cpu)=cpu.catalog.vendor, cpu_facts_* read cpu.catalog.*; std/cpu is the general CPU concept only. gate-1 PASS.
[ ] 6. operator fleet actual config (srv1/srv2 real machines, source-of-truth ctrl nodes.json) -> ctrl PR at plans/models/fabric/operator_fleet.dag, importing gunbc concept + extdeps vendor models.
[ ] 7. compute_fabric does not belong in std/. std is a standard library; the fabric is a product. Move out of std/ into a product/ layer (folder + module rename).
[ ] 8. hand-maintained corpus/test row LISTS + COUNT constants (claim_witness_corpus_*). COUNTS already unconsumed (gate folds the typed List; #4959 retired the cross-check) -> delete now. LISTS blocked only on wiring existing discovery (discover_owned_data / src/v4/compiler/discovery_enumeration.dag) as the enrollment source instead of a hand-typed List literal.
[ ] 9. LENS: error on unused / consumerless ANYTHING — unused imports (v2 is silent today), dead data/types/concepts. A "no consumerless concept" gate.

NOTES
- item 8 has been operator-requested for ~3-4 days with NO movement before this dispatch (crisp-bear-37) — long-stale; track that it actually lands this time.
- verified clean (no change): std/placement_supply.dag (concept-only).
- ctrl cloned at .ctrl-fleet-work (git-excluded); nodes.json is the fleet source-of-truth.
- stale doc-comment ref to hetzner_cax41_cost_quote in dsl/examples/cost_estimate/cost_estimate.dag (cosmetic).
