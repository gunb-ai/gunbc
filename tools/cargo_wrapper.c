// Generated by gunbc-toolsgen
// Hash: 356d1704e1e39b80

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifdef _WIN32
#include <process.h>

int main(int argc, char **argv) {
    if (argc < 2) {
        fprintf(stderr, "usage: cargo_wrapper <args>\n");
        return 2;
    }

    char **args = (char **)calloc((size_t)argc + 1, sizeof(char *));
    if (!args) {
        fprintf(stderr, "cargo_wrapper: allocation failed\n");
        return 2;
    }

    args[0] = "cargo";
    for (int i = 1; i < argc; i++) {
        args[i] = argv[i];
    }
    args[argc] = NULL;

    int rc = _spawnvp(_P_WAIT, "cargo", (const char *const *)args);
    if (rc == -1) {
        fprintf(stderr, "cargo_wrapper: failed to launch cargo (%d)\n", errno);
        return 127;
    }

    return rc;
}
#else
#include <unistd.h>

int main(int argc, char **argv) {
    if (argc < 2) {
        fprintf(stderr, "usage: cargo_wrapper <args>\n");
        return 2;
    }

    char **args = (char **)calloc((size_t)argc + 1, sizeof(char *));
    if (!args) {
        fprintf(stderr, "cargo_wrapper: allocation failed\n");
        return 2;
    }

    args[0] = "cargo";
    for (int i = 1; i < argc; i++) {
        args[i] = argv[i];
    }
    args[argc] = NULL;

    execvp("cargo", args);
    fprintf(stderr, "cargo_wrapper: failed to exec cargo (%s)\n", strerror(errno));
    return 127;
}
#endif
