# Subject-map receipt: (file, annotation block first line, bound subject authored name), PRODUCED BY EXECUTION.
# Recipe: a throwaway probe module importing v1.compiler.annotation_bind + v1.compiler.tokenize reads each file
# with filesystem_read, runs the real tokenizer -> parser -> binder, and renders every admitted graph row plus a
# per-file refusal count. Run via: gunbc run --source-root dag --source-root src/v1 --entry <probe> --function dump.
# The probe is not committed; this file is its captured output for the M1C-1 self-application + jwt.dag, regenerated
# after the module-header attachment law landed (a prior revision of this receipt was hand-authored and claimed
# MODULE bindings the binder did not produce).
dag/std/source_annotation.dag	The language-agnostic authority for source annotations, per DESIGN 4c. It names no .dag syntax and no lexical rule: a realization decides what its annotation syntax looks like and routes it through its own annotation channel, then lands rows of THIS shape. Both the v1 seed tokenizer and the v2 modeled tokenizer import from here rather than each minting a placement enum, because two spellings of one concept is the nicknaming defect this whole lane exists to remove.	std.source_annotation
dag/std/source_annotation.dag	Placement is OBSERVED at capture, not reconstructed later, because it cannot be reconstructed later. Whitespace and line structure are consumed before the parser sees anything, so once they are gone `fn a() -> Int { 1 } // text` followed by `fn b()` leaves residue identical to a leading annotation on b — two opposite meanings, one byte pattern. LeadingAfterLineIndent means only indentation preceded the annotation on its line; TrailingAfterSemanticToken means a semantic token did. Only the leading form is eligible for attachment in the first realization; trailing refuses until its attachment rule is separately modeled.	AnnotationPlacement
dag/std/source_annotation.dag	What a tokenizer produces before any structural subject is known — hence UNBOUND. It is the shape that crosses the lexer-to-parser seam, and it deliberately carries no subject and no identity: binding is the parser's job, and minting an identity here would draw from the semantic occurrence allocator.	UnboundAnnotationCapture
dag/std/source_annotation.dag	WHY THIS LIVES IN std RATHER THAN IN A TOKENIZER: the v1 seed tokenizer cannot import v2 modules, so a capture shape declared inside either realization forces the other to mint a second spelling of one concept. v2's lexing module additionally carries an annotation_class Symbol on its own capture row because that lexer is generic over many languages with many annotation syntaxes; the v1 .dag tokenizer has exactly one, so it uses this shape directly. That is a real difference in what the two realizations must distinguish, not a fork — and both converge on SourceAnnotationDebt once a subject is known, which is the row that actually persists.	UnboundAnnotationCapture
dag/std/source_annotation.dag	THE FIELD IS `lexeme`, NOT `text`, AND THE DISTINCTION IS LOAD-BEARING. A capture carries RAW SYNTAX exactly as it appeared, delimiter included: the lexer reports what it saw, and the language-specific attachment realization owns normalization into SourceAnnotationDebt.text. Naming it `text` while storing a `//`-prefixed lexeme would make the two indistinguishable at the type level, and the failure is not hypothetical — if one realization strips the delimiter and another does not, parse then render then parse either doubles the delimiter or silently changes annotation content, and the D-C round-trip control is what would catch it. Every realization capturing into this field stores raw; every realization reading it for durable text strips.	UnboundAnnotationCapture
dag/std/source_annotation.dag	Every inhabitant is MODELING DEBT — that is the type's standing law, not a field on it. A comment is prose that the executable model does not currently represent, retained honestly rather than deleted or promoted. There is deliberately no rationale variant: a permanent category with no authoring path, no distinct consumer, and no lifecycle would be speculative vocabulary, and offered early every unmodeled invariant acquires a why-prefix and declares itself permanent. A second population earns its own carrier when migration produces one and its consumer is modeled.	SourceAnnotationDebt
dag/std/source_annotation.dag	NOTE WHAT IS ABSENT: this row has no identity of its own. Giving an annotation an OccurrenceId would draw from the graph-scoped semantic occurrence allocator, so inserting a comment could shift the identities of declarations later in the same source and in later-parsed modules — the exact hazard DESIGN 4c forbids. `subject` REFERS to a semantic occurrence; it never mints one. Multiplicity and source order are carried by the ordered graph below, not by per-row ids, and a dedicated SourceAnnotationId arrives only when a real consumer must address one annotation independently.	SourceAnnotationDebt
dag/std/source_annotation.dag	An ORDERED sequence, because order and multiplicity are the whole of an annotation's identity here. Two annotations with the same text on the same subject are two annotations, and the one written first is written first — a set would silently merge them and a map would need the per-row key this carrier deliberately does not have. Consecutive leading lines group into one block at attachment, so a block is one row whose text carries its own line breaks, not N rows a consumer must re-associate.	SourceAnnotationGraph
dag/std/source_annotation.dag	Joins the graphs of separately parsed sources, preserving order. It exists so a consumer accumulating across a compilation never reaches into `rows` to append — the ordered-sequence law lives with the carrier, and a caller that rebuilt the record itself would be free to sort, dedupe, or reverse it and silently lose the multiplicity the order encodes.	source_annotation_graph_concat
dag/std/source_annotation.dag	Refusals are typed and located rather than dropped, because a dropped annotation is indistinguishable from a source that never carried one — the empty-observation narrow DESIGN names. UnattachedAtScopeEnd is the pending-block-at-EOF case: prose that names no subject. TrailingNotModeled and BodyGrainNotModeled are the two deliberate first-cut narrowings; each names what would have to be modeled to admit it, so neither reads as an oversight.	AnnotationAttachmentRefusal
dag/std/source_annotation.dag	Every refusal carries where it happened, so a diagnostic reporting one does not need to carry a second copy of the span beside it. A consumer asks this rather than storing the origin again — a duplicated span is a second representation that can disagree with the first.	annotation_attachment_refusal_origin
dag/std/source_annotation.dag	The human-facing sentence for each refusal, single-authored here beside the variants so a new refusal cannot be added without one. Each says what was seen and what would have to be modeled to admit it, because the reader's next question after `this is refused` is always `then where do I put it`.	annotation_attachment_refusal_message
dag/std/source_annotation.dag	The gate between analysing attachment and CONTINUING with it. AnnotationAttachmentResult is an analysis surface — it reports what attached and what did not, and a reader may legitimately inspect both. But a caller that takes `graph` from that record and carries on has done exactly what admitting `//` and discarding the capture would have done: the prose is dropped and compilation proceeds as though the source never carried it.	annotation_admission_note
dag/std/source_annotation.dag	The first realization admits leading annotations only, and this is the single predicate that says so. A realization asking whether to attach consults this rather than re-deciding, so widening the admitted grain later is one edit here plus the attachment rule it enables, never a sweep over every call site.	annotation_placement_is_attachable
dag/std/source_annotation.dag	Placement is decided by the PHYSICAL LINE PREFIX — the raw text between the last line feed and the annotation — and never by which lexical category the preceding lexeme belonged to.	advance_line_prefix_indent_only
dag/std/source_annotation.dag	The category guess is wrong, and a real grammar disproves it. Deciding `leading` by `no semantic token seen on this line yet` assumes line boundaries live in trivia; the Python model's own token lexemes include a closing-paren-plus-newline-plus-indent sequence, which ENDS on a fresh line carrying only indentation, and a newline-plus-indent-plus-if sequence, which ends on a fresh line carrying semantic text. An annotation after the first is leading and after the second is trailing, yet a category rule classifies both as trailing because in both cases the last thing consumed was a semantic token. Any grammar whose tokens may span lines breaks the same way.	advance_line_prefix_indent_only
dag/std/source_annotation.dag	So the state is `line prefix is indent only`, advanced by the RAW TEXT of every consumed lexeme regardless of category. One fold expresses the whole rule: a line feed resets the prefix to empty (vacuously indent-only), and every character after it must be indentation for the prefix to stay so. A lexeme with no line feed therefore preserves the previous state only while it is itself indentation, and a lexeme containing one is judged entirely by what follows its LAST line feed — both cases the fold produces without a special arm. An empty lexeme preserves the state.	advance_line_prefix_indent_only
dag/std/source_annotation.dag	A lexer that walks lexemes threads this forward; a lexer with random access to the source feeds it the raw slice it consumed between steps. Both consume THIS function, because two spellings of one placement rule would let the realizations disagree about the same source.	advance_line_prefix_indent_only
dag/std/source_annotation.dag	The rule is stated over CODE POINTS, not over a String, because the two realizations do not share a string representation: the seed's String is a host string while the modeled compiler's is already a sequence of code points. Taking the sequence as the parameter puts the one representation-specific step — how to get code points out of whatever this realization calls a lexeme — at each call site, and keeps the rule itself single-authored. The text-shaped convenience below exists for host-string callers and is the same fold, never a second rule.	advance_line_prefix_indent_only_text
dag/std/source_annotation.dag	What a realization offers as attachable subjects, and deliberately NOT DeclarationOccurrence. Two reasons, both structural. First, this module would otherwise depend on the whole occurrence transport to express a rule that needs exactly two facts per subject — an identity to refer to and a span to compare against. Second, the roster IS where the module-item narrowing lives: a realization admitting only module-scope declarations supplies only those, and widening to a finer grain later is a change in what the caller enumerates, not a change to the fold. The occurrence is REFERRED to, never minted; every id in a roster was already allocated by the semantic parser before attachment runs.	AnnotationSubject
dag/std/source_annotation.dag	The capture after the realization has stripped its delimiter and before a subject is known. It exists as a distinct type from UnboundAnnotationCapture precisely because the `lexeme` versus `text` distinction is load-bearing: normalization is language-specific and happens exactly once, at the boundary between the realization's capture and this shared fold, so the fold can never be handed raw syntax by accident. A realization that forgot to strip would have to construct this type to call at all, and the field name is what tells it.	NormalizedAnnotationCapture
dag/std/source_annotation.dag	Both halves of the outcome, because either alone is a lie. The graph alone hides that prose was dropped; the refusals alone hide what was kept. A consumer reading only `graph` still gets a correct graph — it simply is not entitled to conclude the source carried nothing else, and the refusal list is what makes that conclusion checkable rather than assumed.	AnnotationAttachmentResult
dag/std/source_annotation.dag	Resolves one capture against the subject roster in a single pass, answering both questions the attachment rule asks: is the capture INSIDE some subject (body grain, refused), and what is the nearest subject STARTING AFTER it (its leading subject). Containment is checked before proximity because the two overlap — prose inside a function body always has some later declaration after it, and picking that one would silently reattach body commentary to the next unrelated declaration. That is the specific misattachment BodyGrainNotModeled exists to prevent, and checking proximity first would produce a plausible wrong answer rather than a refusal.	annotation_subject_pick_note
dag/std/source_annotation.dag	POST-MODULE HEADER GAP: prose on the line immediately after `module p.q` attaches to the module, not the first depth-1 member. A blank line between the module declaration and the comment starts a new block that follows the ordinary nearest-following rule instead. Without this arm, a line-2 comment would skip the module (its span ends before the capture) and bind to the first import or declaration — the wrong subject for module-level rationale while still satisfying line-1 CI layout.	annotation_subject_pick_note
dag/std/source_annotation.dag	Adjacency to the module line needs no width constant. The two observed bits pin the shape exactly: a capture that is neither blank-preceded nor annotation-preceded has NON-COMMENT CONTENT on the line above it, and if it also starts after the module subject and ends before the first member starts, the only thing that line can be is the module declaration itself — a member there would put the capture past `first_member_start`. A capture that IS annotation-preceded is a continuation line; its block's subject was decided by the block's first line, so the arm refuses it and the attach fold merges it instead. An earlier revision used a tolerance window (`span.end + 12`) in place of the annotation-line bit; that both under-covered long dotted module names (the window is a guess about how many characters a module declaration spans) and mis-fired on the adjacent second line of a blank-separated block below the module, which the window happened to exclude only when the module name was short.	annotation_subject_pick_note
dag/std/source_annotation.dag	The roster is scanned rather than indexed, so resolution is captures times subjects. That is a join without a sorted index, not a copied accumulator, and it is bounded by one source's declarations.	annotation_subject_pick_note
dag/std/source_annotation.dag	WHY AGREEMENT IS KEYED ON A STRING AND NOT ON OccurrenceId. An occurrence identity is unique within ONE graph-scoped allocator. Two separately parsed sources have two allocators, so equal id VALUES across them establish equal allocation ordinals and nothing else — a round trip comparing them directly would report `same subject` for two declarations that merely occupy the same position in their respective files.	annotation_subject_key_note
dag/std/source_annotation.dag	That is not a hypothetical. Identical prose above `data alpha` in one source and above `data beta` in another source of the same shape yields the same ordinal for both, so an id-valued comparison agrees while the annotations name different declarations. The failure is a FALSE POSITIVE in the oracle, which is the direction that never gets noticed.	annotation_subject_key_note
dag/std/source_annotation.dag	So agreement is stated over a caller-supplied key that is stable ACROSS parses. The realization decides what that key is, because only the realization knows: for the first .dag cut it is the authored declaration name, which is unique among one module's items and is exactly what a reader means by `the same subject`. A realization whose subjects are not uniquely named must supply a qualified key instead.	annotation_subject_key_note
dag/std/source_annotation.dag	WHAT AGREEMENT MEANS, and deliberately what it does not. Parse a source, render its graph, parse the result: the two graphs agree when they have the same rows in the same order, each row naming the same subject and carrying the same normalized text.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	EXACT SOURCE BYTES ARE NOT THE TARGET, and cannot be. Normalization strips the delimiter and at most one following space, so `//tight` and `// tight` both land as `tight` and the authored difference is gone before any renderer runs. A property demanding original bytes would be unsatisfiable against this carrier — not hard, unsatisfiable — so it would either never pass or be quietly weakened until it passed. The declared canonical form replaces it: the renderer emits ONE spelling, and the property asserts the graph survives, not the keystrokes.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	FOUR THINGS ARE THEREFORE EXCLUDED ON PURPOSE. Original delimiter spacing, because normalization already discarded it. Original byte offsets, because rendered source has its own. Raw node equality, because v1 nodes carry spans and a span-carrying comparison reports PROVENANCE differences as semantic ones. And origin spans on the rows themselves — this predicate compares subject and text only, for the same reason: a rendered source legitimately places the same annotation at a different offset, and a comparison that failed on that would be measuring the renderer's line arithmetic rather than whether the prose survived.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	ORDER AND MULTIPLICITY ARE IN, not incidentally but because the carrier says they are its identity: two rows with the same text on the same subject are two annotations, and the first-written is first.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	It is a FOLD carrying the unconsumed right-hand rows, not a recursion over both. Graphs concatenate across compilation units, so row count scales with the corpus's whole prose population and a recursive comparison would make stack depth a function of how much prose the repository holds. The positional guarantee is identical either way; only the depth dependency differs.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	Length mismatch falls out of the walk rather than being checked separately: a right side that runs dry marks disagreement, and a right side with rows left over fails the final emptiness check.	keyed_annotation_rows_agree
dag/std/source_annotation.dag	`pending_adjacent` records whether the immediately previous capture opened or extended `pending`. A continuation line (annotation-preceded, not blank-preceded) merges into `pending` only when this is true: captures arrive in source order, so the standalone comment line above a continuation IS the previous capture, and if that capture was refused rather than admitted, the continuation must not ride into whatever OLDER block still sits open — it falls through to the ordinary rule and is judged on its own.	AnnotationAttachAcc
dag/std/source_annotation.dag	A BLANK LINE SPLITS A BLOCK. Two facts decide grouping — same following subject, and physical adjacency — and both are required.	annotation_attach_step
dag/std/source_annotation.dag	An earlier revision used same-subject alone and claimed those captures were `by construction consecutive`. They are not. Two comment lines directly above a declaration and two comment BLOCKS separated by a blank line above the SAME declaration both resolve to that one subject, so same-subject alone merged them into a single row joined by a single newline and the blank line ceased to exist. That is not a formatting nicety: the authored structure is gone, and once a renderer exists it would render one block where the author wrote two, permanently.	annotation_attach_step
dag/std/source_annotation.dag	Adjacency cannot be recovered here — the fold sees spans, not source, and the whitespace between two captures was consumed by the lexer — so it is OBSERVED at capture, exactly like placement and for exactly the same reason. `preceded_by_blank_line` is that observation, and this fold reads it rather than re-deriving it.	annotation_attach_step
dag/std/source_annotation.dag	The merged row's text carries its own line breaks and its origin spans first capture start to last capture end, so a block is addressable as one authored unit. `pending` holds the block still being extended; it is flushed when a capture resolves to a different subject, when a capture opens a new block after a blank line, and once more at the end.	annotation_attach_step
dag/std/source_annotation.dag	That is not a formatting preference — it is what makes the round trip mean anything. The grouping fold reads a blank line as `these are two authored blocks` and adjacency as `this is one block spanning two lines`. A renderer that emitted the rows adjacently would produce source that re-parses as ONE row where the author wrote two, and the round trip would then certify the merge the grouping rule exists to prevent. The failure is silent and it is permanent, because the merged form is what gets written back.	annotation_attach_step
dag/std/source_annotation.dag	What this law does NOT fix is the delimiter: how an annotation line is spelled is a realization fact, so each realization owns the inverse of its own normalization step and states it beside that step.	annotation_attach_step
dag/std/source_annotation.dag	The ordinary (non-continuation) resolution: the capture's own pick decides its subject. A continuation line never reaches this — its subject is its block's first line's subject, held in `pending` — except when the line it continues was refused, in which case it is judged here on its own and typically shares that refusal.	annotation_attach_resolve
dag/std/source_annotation.dag	The single attachment authority both realizations call. It is a PURE FOLD over captures and an already-allocated subject roster: it reads occurrence identities and never mints one, so inserting, editing, or deleting a comment cannot shift any declaration's identity here or in any later-parsed module. That property is not a convention this function happens to honor — there is no allocator in scope for it to violate.	attach_annotations
dag/std/source_annotation.dag	Captures must arrive in source order, which is the order the lexer produced them; the block grouping above is the only thing that depends on it.	attach_annotations
dag/std/source_annotation.dag	refusals	0
src/v1/annotation_bind.dag	The .dag realization of annotation attachment: it turns what the tokenizer captured into subject-bound rows by calling std.source_annotation.attach_annotations. Everything here is either .dag-specific — what the comment delimiter looks like, how a module item is recognized in this parser's transport — or a projection of facts the parser already produced. The RULE itself is not here; a second copy of the rule beside the shared one is the fork this lane exists to prevent.	v1.compiler.annotation_bind
src/v1/annotation_bind.dag	The roster comes from the occurrence INDEX, not from OccurrenceTransport.declarations, and the difference is measured rather than assumed. Parsing `module p.q` with one data row, one type, and one fn yields four declaration-role rows — the module, the fn, and two lexical binders inside the fn body — while the index carries fifteen entries including `alpha` at ancestor depth 1 and `Thing` at ancestor depth 1.	entry_ancestor_depth
src/v1/annotation_bind.dag	So `data` and `type` module items DO receive occurrence identities; they are absent only from the declaration-role projection, because parsed_occurrence_role_for_node classifies a node carrying both a body and a type annotation as unclassified. Rostering from `declarations` would therefore have made exactly the population this feature exists to serve — the `_note` rows that hold this repository's prose — permanently unattachable, and it would have looked like a narrow first cut rather than a defect. That role-projection gap is real and pre-existing; it is NOT repaired here, because widening a declaration role changes what binding and resolution see, which is a semantic change this lane has no business making.	entry_ancestor_depth
src/v1/annotation_bind.dag	Ancestor depth is the module-item test: depth 0 is the module itself, depth 1 its direct members, depth 2 and beyond nested structure.	entry_ancestor_depth
src/v1/annotation_bind.dag	A module item's occurrence span is a START MARKER, not an extent: `data alpha` spans the `data` keyword and `fn go` spans the `fn` keyword. Attachment needs an extent, because the rule that keeps body prose from being filed against the next unrelated declaration is containment.	subject_extent_derivation_note
src/v1/annotation_bind.dag	The extent is derived rather than invented. An item runs from its own start to the furthest end of any occurrence lying between it and the next module item — those occurrences are precisely its own nested structure, since a sibling item's structure cannot begin before that sibling does. Taking the next item's start as the end instead would be wrong in the ordinary case: the whitespace and the leading comment BELONGING TO the next item sit in that gap, so a comment written to describe `type Thing` would fall inside `data alpha` and be refused as body grain.	subject_extent_derivation_note
src/v1/annotation_bind.dag	The module itself is rostered with its raw span and no derived extent. Its true extent is the whole file, which would swallow every capture; a degenerate span makes the two cases that matter come out right, since a comment above `module` still finds the module as its nearest following subject and a comment on the line immediately after `module` also attaches to the module when no blank line separates them (std.source_annotation `module_header_gap_subject`).	subject_extent_derivation_note
src/v1/annotation_bind.dag	The single place `//` is removed, and it is here rather than in the tokenizer on purpose. A capture carries RAW syntax so the lexer reports what it saw; normalization into durable text is a language fact, and .dag's fact is that the delimiter is two slashes optionally followed by one space.	strip_line_comment_delimiter
src/v1/annotation_bind.dag	Exactly one space is consumed, never a run, because `//   indented` is authored indentation the writer chose and swallowing it would silently rewrite the prose. A lexeme not starting with the delimiter passes through unchanged rather than being truncated by a fixed offset — that arm is unreachable through the .dag annotation rule today, and the alternative is a normalizer that corrupts content when it is wrong about its input.	strip_line_comment_delimiter
src/v1/annotation_bind.dag	THE CANONICAL .dag SPELLING, and the inverse of strip_line_comment_delimiter directly above. It sits beside its inverse deliberately: the delimiter is stated once, and a change to what .dag strips is in the same edit as what .dag writes. Two functions in two modules agreeing about `//` by coincidence is the fork this lane removes.	render_line_comment_delimiter
src/v1/annotation_bind.dag	The form is a single `//` followed by one space, then the line's normalized text. The empty line renders as a bare `//` with NO trailing space, and that is a correctness case rather than tidiness: `// ` and `//` both normalize to the empty string, so either round-trips, but a trailing space is the character formatters and editors strip on save — so choosing the spelling that survives contact with a text editor is choosing the one that keeps the fixed point stable. An empty line inside a block is reachable: an author writing a bare `//` between two commented lines produces one block whose middle line is empty.	render_line_comment_delimiter
src/v1/annotation_bind.dag	Text is NOT re-indented, re-wrapped, or trimmed. `//   indented` normalizes to two spaces plus `indented` and renders back to `//   indented`, because the authored indentation is content — the strip step consumes exactly one space for exactly this reason, and a renderer that trimmed would delete authored structure on the first round trip.	render_line_comment_delimiter
src/v1/annotation_bind.dag	One row becomes one BLOCK: its embedded line breaks become adjacent annotation lines with nothing between them. Adjacency is what the grouping fold reads back as `one block`, so the joiner here and the blank-line rule below are the two halves of one law stated in std.source_annotation.	render_annotation_block
src/v1/annotation_bind.dag	SELECTION AND RENDERING ARE ONE OPERATION, and the fusion is the fix for a rung inflation this seam produced twice. The first cut took a bare List<SourceAnnotationDebt> named `rows for one subject` and ignored every row's subject field. The second cut minted a SubjectAnnotationBlocks { subject, texts } carrier and claimed mixed-subject content was structurally impossible — but the record's fields were public and the renderer read only texts, so a caller could still pair a subject with texts selected from anywhere. The invalid ASSOCIATION stayed writable; erasing the texts' own subjects had only made the contradiction unobservable, which is strictly worse than validated. A guarantee that names a subject while accepting caller-supplied content is validation wearing construction's name.	render_subject_annotation_blocks
src/v1/annotation_bind.dag	So no carrier exists. This function takes the admitted graph and a subject, selects exactly the rows whose subject matches, refuses an empty selection, and renders the result. The only path to `subject S's rendering` runs through the graph's own subject edges, so text that is not provenance-bound to S has no route into a value that claims S. render_annotation_block below it still renders arbitrary text — a text-level spelling that names no subject and therefore claims none.	render_subject_annotation_blocks
src/v1/annotation_bind.dag	Consecutive blocks on the one subject are separated by a blank line — the renderer half of the std grouping law. Joining with a single newline would emit two authored blocks as one, and the round trip would then certify a merge the author never wrote.	render_subject_annotation_blocks
src/v1/annotation_bind.dag	EMPTY SELECTION REFUSES: a subject carrying no prose yields Absent rather than an empty string, because rendering `no blocks` is the caller's decision to omit the region entirely, and an empty string would put a bare newline where nothing was authored.	render_subject_annotation_blocks
src/v1/annotation_bind.dag	Pins the element type of the accumulator's empty list. A bare `[]` in the fold's init leaves the element type unconstrained until the first push, and the emitter writes that unconstrained type into the closure signature it generates — so the seed compiles the accumulator as a list of unit and the fold no longer typechecks. Naming the empty list through a typed return is the one place the element type is stated.	empty_keyed_rows
src/v1/annotation_bind.dag	Projects a graph's rows onto the cross-parse-stable key std.source_annotation's agreement is defined over: the AUTHORED NAME, resolved against the SAME eligible-subject population attachment rosters — the module and its depth-1 items — never the whole occurrence index. An earlier cut scanned every occurrence, which admitted two failure directions the roster cannot produce: a nested binder sharing a top-level name could collide with the real subject, and nothing checked that the name identified ONE eligible subject at all.	keyed_annotation_rows
src/v1/annotation_bind.dag	It is FAIL-CLOSED twice, and both arms close the same false-AGREEMENT direction. A row whose subject is not in the eligible population yields Absent for the whole projection rather than a placeholder key — a placeholder would make two unresolvable subjects compare equal, turning a lookup failure into a false agreement. And a DUPLICATE authored name among the eligible subjects refuses the whole projection: with two module items named alpha, rows attached to DIFFERENT declarations would project onto ONE key, and agreement would certify prose as `the same subject's` across genuinely different subjects. The name key is sound exactly where eligible names are unique, so where they are not, there is no projection — the property is explicitly scoped to one module with unique eligible authored names, and everything outside that scope refuses rather than guessing.	keyed_annotation_rows
src/v1/annotation_bind.dag	Both lookups scan the eligible list per row — a join without a sorted index, bounded by one module's items, the same shape and dissolve-on as the roster scan in std.source_annotation: a name-indexed subject table on the parse artifact.	keyed_annotation_rows
src/v1/annotation_bind.dag	The .dag entry point: normalize, roster, attach. The attach step is the shared fold, so a change to the attachment rule reaches this realization and the v2 one together and cannot reach only one.	bind_annotations
src/v1/annotation_bind.dag	THE single seam both production frontend paths use. front_end_sources and parse_census_fill_sources call this one function rather than each repeating normalize-roster-attach-admit, so the two cannot drift into disagreeing about whether a given file's prose is admissible — a drift that would be invisible, because the census path and the compile path would simply produce different graphs for the same source.	admit_source_annotations
src/v1/annotation_bind.dag	Refusal is a BLOCKING diagnostic, not a note carried alongside a successful result. Unclassified prose stops the line: the graph is withheld entirely and every refusal is reported at its own span. Admitting the attachable rows while dropping the rest is the shape DESIGN forbids — the dropped prose would be uncounted, and a source that lost half its commentary would compile exactly like one that never had it.	admit_source_annotations
src/v1/annotation_bind.dag	refusals	0
src/v1/tests/claim/v1_annotation_round_trip_test.dag	D-C property 7: parse, render, parse — the graph survives. What agreement means is single-authored in std.source_annotation as keyed_annotation_rows_agree, so this file exercises the property rather than restating it.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	AGREEMENT IS KEYED ON THE AUTHORED NAME, NOT ON THE OCCURRENCE ID, and that is a correctness fix rather than a convenience. The two graphs come from two parses with two allocators, so equal id values would mean equal allocation ORDINALS — identical prose above `data alpha` in one source and above `data beta` in another of the same shape would compare equal. There is a discriminating arm below for exactly that, because a false-positive oracle is the failure this file would otherwise be unable to see in itself.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	EVERY GRAPH COMES THROUGH THE PRODUCTION ADMISSION SEAM. An earlier cut read the graph straight off the attachment result, which is the bypass the authority itself warns about: that accessor returns the attached rows even when others were refused, so a renderer that introduced an unattachable annotation could drop it and the surviving subset could still satisfy the comparison. admit_source_annotations is what both v1 frontend paths use, and requiring it on BOTH the original and the rendered source is what makes a dropped annotation fail the property instead of shrinking both sides of it.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE PROPERTY IS OVER THE ANNOTATION CHANNEL, with the declaration text held fixed to isolate it. Property 4 already proved that annotations do not perturb the semantic projection or the emitted bytes; what this proves is that the prose itself makes the trip. See the scope note in the module authority for why no production authored-source renderer is wired: there is none in the tree to wire.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	Two cuts of this file got that wrong in two different ways, both caught by review, and the second is the reason the helpers below take a AnnotationParsedSource instead of a String. The FIRST cut had separate admitted_of and transport_of helpers, so the keyed projection paired a graph from one parse with an occurrence index from another — the keying resolves a subject id through the index, so ids were being resolved against a foreign table. The SECOND cut fixed the static call sites and still parsed the same source three times DYNAMICALLY: once for the `before` rows, once inside the renderer, once for the rendered result. One static call site is not one parse per execution, and a witness that states a one-pass invariant while re-deriving its own inputs is asserting something it does not do.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The rule the shape now enforces: a AnnotationParsedSource is produced once per distinct SOURCE TEXT and passed down. The round trip parses exactly twice by construction — the authored source, and the rendered source — because those are two different texts, which is the property.	AnnotationParsedSource
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE SEMANTIC PARSE MUST SUCCEED FOR THE ANNOTATION RESULT TO MEAN ANYTHING, and an earlier cut of this file discarded the fact that would have said so — it kept only the occurrence transport and asked the annotation diagnostics whether the source was good. Annotations are captured on a lexical channel, so the parser can report an error and STILL have produced the occurrences the annotation attachment binds to.	annotation_parse_is_clean
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The false green that permits, in principle: a source whose valid prefix carries prose and a declaration and whose trailing syntax is malformed. Admission succeeds on the prefix, the renderer reproduces only the annotated subject and drops the malformed tail, the rendered source parses clean, and the rows agree — so `parse, render, parse` reports that the graph survived a trip whose second leg silently repaired the input. There is a control arm for exactly this below.	annotation_parse_is_clean
src/v1/tests/claim/v1_annotation_round_trip_test.dag	Evidence therefore requires the parse to be CLEAN — no error node AND a module produced — as well as admitted and nonempty. `error: Absent` alone is not the check: a result with neither error nor module is not a parse that succeeded.	annotation_parse_is_clean
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The keyed projection of a CLEANLY PARSED, ADMITTED graph, or Absent. Several distinct failures collapse to Absent here — the parse errored, the parse produced no module, the annotations were refused, or a subject would not resolve to a name — and every one of them must make a property arm fail rather than compare an empty list against an empty list, which is why the evidence predicate below requires nonemptiness explicitly.	keyed_rows_of
src/v1/tests/claim/v1_annotation_round_trip_test.dag	Rebuilds a source from the CANONICAL rendering of an admitted graph, through the production render_subject_annotation_blocks rather than assembling annotation lines locally, so a defect in it reaches these claims. The renderer selects the subject's rows from the graph itself — there is no carrier for a caller to fill with foreign text — so what this helper adds is only the module header and the declaration line beneath. A source whose graph is refused, empty, or unresolvable renders as a marker that parses to no annotations, so the failure surfaces as a disagreement rather than as an accidentally-matching empty pair.	rendered_source_for_parsed
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE AUTHORED-NAME KEY IS SOUND ONLY WHERE THE NAME IS UNIQUE, so a second match REFUSES rather than resolving. An earlier cut folded to the last match and returned it, which is the quiet half of the same false-agreement direction the keying exists to close: with two module items sharing a name, `the subject called alpha` is not a question with an answer, and answering it anyway picks one by traversal order and calls the other one's prose the same subject's.	subjects_named_in
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The scan is scoped to ancestor depth below two — the module and its direct items, the same eligible population attachment rosters — because a nested parameter or field sharing a top-level name is not an annotation subject, and counting it would refuse a name that is in fact unique among the subjects that matter.	subjects_named_in
src/v1/tests/claim/v1_annotation_round_trip_test.dag	Zero matches and two matches are both Absent here because both mean `this name does not identify a subject`. They are distinguishable at the call site if a future arm needs to tell them apart; what must never happen is a confident wrong id.	subjects_named_in
src/v1/tests/claim/v1_annotation_round_trip_test.dag	This helper protects the TEST'S OWN rendering selection. The production projection does not depend on it: keyed_annotation_rows carries its own eligible-roster resolution and duplicate refusal, and the arm below proves that at the projection itself rather than here.	subjects_named_in
src/v1/tests/claim/v1_annotation_round_trip_test.dag	ONE predicate for `this arm is evidence`, so no arm can pass on an empty comparison. It requires the authored source to parse CLEAN and be admitted with a NONEMPTY keyed projection, the rendered source to parse clean and be admitted too, and the two to agree. Agreement alone is satisfied by empty-against-empty, which is precisely how a round-trip witness greens while exercising nothing.	round_trip_is_evidence_for
src/v1/tests/claim/v1_annotation_round_trip_test.dag	It takes an already-parsed `before` and the declaration to render beneath, rather than a source string and a fixed declaration. The parsed argument is what carries the one-parse rule (see single_parse_note); the declaration argument is what keeps the two-declaration arm honest, because rendering beta's prose above `data alpha` proves only that beta's TEXT was selected, never that it stayed attached to beta.	round_trip_is_evidence_for
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE CASE THAT DECIDES THE RENDERER. Two blocks separated by a blank line must come back as two rows. The arm asserts the count on BOTH sides as well as agreement, because agreement between two identically-merged graphs would also pass and the count is what pins which shape survived.	w_two_blocks_survive_the_round_trip
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The RED control: a renderer joining blocks with a single newline instead of a blank line. It is reconstructed here because the production renderer does not offer the wrong behaviour, and it asserts the round trip would have MERGED two authored blocks into one. Without it, the arm above passes for a renderer that is right and for one that is wrong in a way nothing else here observes.	w_a_renderer_without_the_blank_line_merges_them
src/v1/tests/claim/v1_annotation_round_trip_test.dag	Vacuity is asserted to FAIL, not merely demonstrated. An earlier cut showed that empty agrees with empty and returned true, which proves the round trip is reflexive and says nothing about whether prose survives. The evidence predicate requires a nonempty admitted projection, so the bare fixture must NOT satisfy it — and it must still be admitted, so the failure is `no prose to carry` rather than `refused`.	w_a_source_with_no_prose_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE ARM THAT PROVES THE ADMISSION ROUTING, by contrasting the seam against the bypass on one source. This fixture carries admissible leading prose AND an unattachable trailing annotation, so the two paths disagree about it: the raw attachment accessor returns the one attached row while the trailing prose is silently dropped, and the admission seam withholds the graph entirely.	w_a_refused_source_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	That is the whole hazard in one fixture. Reading the raw graph, a round trip over this source compares one row against one row and PASSES while an annotation was lost. Reading the seam, there is nothing to compare and the property correctly refuses to call it evidence. The arm asserts both halves, because asserting only the second would leave `the bypass would have passed` an unverified claim in a note.	w_a_refused_source_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The MEASURED fact that makes the control below discriminating rather than merely true. If the two declarations happened to receive different occurrence ordinals, `they disagree` would hold for the wrong reason and would keep holding even under the unsound id-valued comparison the keying replaced. They do not: alpha in one source and beta in the other are allocated the SAME id value, so an id-valued oracle agreed on two annotations naming different declarations. This arm pins that, so the pair reads as `same ordinal, different key, must disagree`.	w_the_two_declarations_share_an_occurrence_ordinal
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE FALSE-POSITIVE CONTROL for the keying decision. Both sources carry the same annotation text in the same position above a single declaration; only the declaration's NAME differs. Their subjects therefore receive the same allocation ordinal in their own parses, so an id-valued comparison would report agreement. Keyed on the authored name they must disagree — and the arm also asserts each side is nonempty, so disagreement cannot come from one side being absent.	w_same_text_same_position_different_declaration_disagrees
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The control the single-declaration fixtures could not provide: two declarations each carrying their own prose. It asserts the blocks are selected PER SUBJECT — alpha's rendering contains alpha's text and not beta's — which is what the typed one-subject carrier exists to guarantee and what a bare row list silently got wrong.	w_each_declaration_keeps_its_own_prose
src/v1/tests/claim/v1_annotation_round_trip_test.dag	BETA IS RENDERED ABOVE `data beta`, NOT ABOVE THE SHARED `data alpha`, and the difference is the whole claim. An earlier cut reused declaration_text for both subjects, so beta's expected rendering was literally `// about beta` above `data alpha` — a string that shows beta's TEXT was selected while quietly REATTACHING it to a declaration named alpha. Text selection is the weaker claim; the arm's name promises the stronger one.	w_each_declaration_keeps_its_own_prose
src/v1/tests/claim/v1_annotation_round_trip_test.dag	So each rendering is reparsed and the prose is read back THROUGH ITS SUBJECT: beta's rendering must bind `// about beta` to the subject named beta. The old form is kept as the RED control, executed rather than described — rendering beta's blocks above `data alpha` and asserting that the reparse binds beta's prose to ALPHA. That is the misattachment the arm exists to exclude, so it is asserted to happen in the wrong form rather than merely avoided in the right one.	w_each_declaration_keeps_its_own_prose
src/v1/tests/claim/v1_annotation_round_trip_test.dag	WHY THIS ARM DOES NOT USE round_trip_is_evidence: the fixture carries two subjects and any single rendering reproduces one, so `before` has two rows and `after` has one and the round trip correctly disagrees. Per-subject reattachment is the claim here; whole-source survival is what the single-declaration arms above prove.	w_each_declaration_keeps_its_own_prose
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE ARM THAT PROVES THE PARSE GATE, and it is tested AT ITS OWN SEAM rather than end to end, because the end-to-end interleaving is not reachable with any fixture tried.	w_a_parse_error_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	MEASURED FIRST, then built accordingly. Five malformed shapes were probed — trailing garbage token, a declaration truncated after `=`, an unclosed `fn` parameter list, an unclosed `type` body, a stray `)` — and in every one the parse errors AND the annotation admission refuses, with an EMPTY graph. So the hypothesised false green (parse errors, annotations admit cleanly, renderer drops the malformed tail, rows agree) has no fixture in this tree today: admission refuses first, because a failed parse yields no module items for the capture to attach to. Reporting the end-to-end arm as proof of the parse gate would therefore have been the inert-check tier — an arm that greens whether or not the gate exists.	w_a_parse_error_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	So the gate is discriminated where it actually lives, by a forged value that differs from a working one in EXACTLY the parse health and nothing else. Getting that minimal is the whole difficulty, and the first attempt failed its own mutation test: swapping in the malformed source's entire ParseWithTableResult also swapped its OCCURRENCE TRANSPORT, so the good graph's subject ids no longer resolved and the projection refused for the wrong reason — the arm greened with the gate deleted. What is forged now is only `result`, the error-and-module pair; the transport, intern table, allocator and admitted graph all stay the clean source's. Delete the gate and the arm reds, because the only remaining reason to refuse is the one under test.	w_a_parse_error_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	That is a lesson about controls, not about this gate: a synthetic input that perturbs more than the property under test proves the wrong thing while looking like proof.	w_a_parse_error_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	WHY THE GATE STAYS despite having no reachable end-to-end case: `annotations admit only when the parse succeeded` is a coupling in today's attachment implementation, not a property this witness is entitled to assume. The gate is the property stating its own precondition instead of inheriting it from a neighbour.	w_a_parse_error_is_not_evidence
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The control for the authored-name uniqueness refusal. Two module items share the name `alpha`, so `the subject called alpha` has no answer — and the resolver must say so rather than folding to whichever match traversal reaches last. The arm pins that the SAME source resolves a uniquely named sibling normally, so the Absent is the duplicate refusing and not the lookup being broken.	w_a_duplicated_authored_name_refuses_as_subject
src/v1/tests/claim/v1_annotation_round_trip_test.dag	THE DISCRIMINATOR AT THE PROJECTION ITSELF, because testing the lookup helper above and calling the projection covered would be proving a neighbour. The fixture is the exact false positive the keyed projection could produce before it refused duplicates: one module, two declarations sharing the authored name `alpha`, the SAME annotation text attached to each — so both rows would project onto the identical (`alpha`, text) key, and two graphs whose prose is attached to DIFFERENT declarations would compare as agreeing. The arm demands the projection REFUSE the ambiguous graph outright; anything it returned would make that false agreement constructible one call later.	w_duplicate_names_refuse_the_keyed_projection
src/v1/tests/claim/v1_annotation_round_trip_test.dag	The positive control is the same shape with unique names, proving the Absent is the duplicate refusing and not the projection being broken. round_trip_is_evidence must also be false on the ambiguous source, since a projection that refuses cannot be evidence of anything.	w_duplicate_names_refuse_the_keyed_projection
src/v1/tests/claim/v1_annotation_round_trip_test.dag	WHAT THIS ARM CAN AND CANNOT SHOW, restated after the carrier it used to exercise was deleted for rung inflation. The earlier SubjectAnnotationBlocks record claimed mixed-subject content was unwritable while its public fields let a caller pair a subject with texts from anywhere — the association stayed writable and had merely lost the provenance that would expose it. Now there is no carrier at all: render_subject_annotation_blocks selects the subject's rows from the admitted graph itself, so no value exists through which foreign text can claim a subject, and unrepresentability of that VALUE is exactly what no executed arm can witness. What executes here is the selection semantics the fusion must get right: over a two-declaration graph, alpha's rendering is exactly alpha's prose and contains none of beta's; and a subject carrying no prose yields Absent rather than an empty rendering.	w_mixed_subject_rows_cannot_reach_the_renderer
src/v1/tests/claim/v1_annotation_round_trip_test.dag	refusals	0
dag/extdeps/auth/jwt.dag	THIS MODULE DESCRIBES CLAIMS; IT DOES NOT VERIFY THEM. Decoding a JWT payload is base64url and JSON, requiring no key and proving nothing — a token whose claims decode cleanly may be entirely forged. Verification means fetching the issuer's JWKS and checking the signature over the header and payload, which is a network operation with its own failure modes and cache semantics.	extdeps.auth.jwt
dag/extdeps/auth/jwt.dag	NumericDate is RFC 7519 section 2: a JSON numeric value counting seconds from the UNIX epoch,	NumericDate
dag/extdeps/auth/jwt.dag	The lifetime of two dates is only a whole number of seconds when BOTH are whole seconds, so the	NumericDateLifetime
dag/extdeps/auth/jwt.dag	RFC 7515 section 3.1: three base64url segments joined by dots — header, payload, signature. Branded	JwtCompactSerialization
dag/extdeps/auth/jwt.dag	RFC 7519 section 2 defines StringOrURI as any JSON string, with the added requirement that a value	JwtStringOrUri
dag/extdeps/auth/jwt.dag	This decoder is not an exception to the module's describe-not-verify boundary. It checks SYNTAX, which is decidable from the value alone; it does not check that the URI resolves, that the issuer is trusted, or that anyone signed anything.	jwt_string_or_uri_of_wire
dag/extdeps/auth/jwt.dag	aud IS A WIRE UNION AND THE FIRST CUT NORMALIZED IT AWAY. Section 4.1.3 states both forms: in the	JwtAudience
dag/extdeps/auth/jwt.dag	RFC 7519 REGISTERED CLAIMS BELONG TO THE IETF, NOT TO WHOEVER ISSUED THE TOKEN. Every provider that emits a JWT emits iss, sub, aud, exp and iat with the same meanings, so modeling them inside a vendor's module would coin the seventh copy of a concept the RFC already owns.	JwtRegisteredClaims
dag/extdeps/auth/jwt.dag	refusals	0
