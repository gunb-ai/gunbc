module extdeps.catalog_estimate

type Estimate<T>
  = Exact { value: T }
  | Interval { lo: T, hi: T }

fn estimate_floor<T>(estimate: Estimate<T>) -> T {
  match estimate {
    Exact { value: value } => value
    Interval { lo: lo, hi: _ } => lo
  }
}
