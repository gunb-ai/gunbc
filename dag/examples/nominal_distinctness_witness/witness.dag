module examples.nominal_distinctness_witness

type UserId {
  raw: Int
}

type AccountId {
  raw: Int
}

fn lookup(id: UserId) -> Int {
  id.raw
}

fn main() -> Int {
  let acct = AccountId { raw: 7 }
  lookup(id: acct)
}
