module tools.self_host_program_partition_behavioral_transport

import tools.self_host_curated_seed_linked_harness { cssl_seed_linked_behavioral_receipt }
import v2.std.collection { List }
import std.logic { Bool }

data spp_doc: String = "Wave 2 parallel flip (program_partition self-emit): curated seed-linked behavioral receipt via tools.self_host_curated_seed_linked_harness. Emit ONLY v2.compiler.program_partition via gunbc compile --entry; runtime gunbc closure (empty shim_lib + shim_writes) — no committed frozen dep snapshots. Seed oracle: v1_compiler::v2_compiler_program_partition in src/v1/stage0 (exported module-authority constants from program_partition.dag). Witness compares emitted vs seed on partition_structural_value_carriers_dissolution_trigger with inject-fault perturbing emitted side (03_resolve pattern). Full partition-tree behavioral oracle deferred until post-#6775 Rc/Optional emit surface greens. Frontier NOT flipped until rustc-green receipt."

data spp_emit_surface_note: String = "emit-surface rustc refusal probe (program_partition entry): gunbc compile --entry is clean (0 diagnostics); cargo is the honest gate. Runtime emit supplies dep closure."

data spp_source_rel: String = "src/v2/compiler/program_partition.dag"

data spp_shim_driver_rel: String = "dag/tools/self_host_program_partition_shims/witness_main.rs"

data spp_scaffold_dissolution_trigger: String = "dissolve-on: dag/tools/self_host_program_partition_shims/witness_main.rs + spp harness transport. DISSOLVES WHEN modeled self-host behavioral-equivalence harness emits comparison driver + manifest."

data spp_receipt_pass_marker: String = "SELF_HOST_PROGRAM_PARTITION_BEHAVIORAL_RECEIPT: PASS"

fn spp_behavioral_receipt_holds() -> Bool {
  cssl_seed_linked_behavioral_receipt(
    source_rel: spp_source_rel,
    shim_lib_rel: "",
    shim_writes: [],
    driver_rel: spp_shim_driver_rel,
    pass_marker: spp_receipt_pass_marker,
    claim_entry_rel: "",
    claim_function: ""
  )
}
