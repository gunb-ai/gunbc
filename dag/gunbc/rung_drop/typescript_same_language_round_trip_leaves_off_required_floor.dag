module gunbc.rung_drop.typescript_same_language_round_trip_leaves_off_required_floor

import std.types { NonEmptyStr }
import gunbc.rung_drop { RungDrop, Standing, TypedDeclaration, DeletedWithoutReplacement }
import gunbc.guarantee_rung { Mitigatable, MechanicallyPreventable }

// After the roster-referenced roll-up `emit_ingest_typescript_same_language_round_trip_holds` was
// removed (test fns may not call test fns), its TypeScript parse-bridge and same-language source-emit
// leaves became independently discovered new-witness identities. They were never on the frozen
// grandfathered roster, so the floor judged them against the 72300 eval-step new-witness budget
// (run 35179697709 / 351868... successor: parse-bridge 73516, source-emit 114390). They cannot be
// added to grandfathered (that roster only shrinks). source-emit also passed while still named on
// floor_expected_red, which the floor refuses as stale_quarantine. They are therefore off the
// required floor rather than kept as over-budget new witnesses or as a silently greened expected-red.
data typescript_same_language_round_trip_leaves_off_required_floor: RungDrop = RungDrop {
  identity: "typescript_same_language_round_trip_leaves_off_required_floor" as NonEmptyStr,

  subject: "TypeScript same-language parse-bridge, source-emit round-trip, and real-tree discrimination witnesses in v2.test.execution.emit_ingest_typescript_same_language_round_trip",

  declared: "2026-09-17",

  standing: Standing,

  declaration: TypedDeclaration {
    previous: MechanicallyPreventable,
    temporary: Mitigatable,
    reason: DeletedWithoutReplacement,
    population: [
      "v2.test.execution.emit_ingest_typescript_same_language_round_trip.typescript_same_language_parse_bridge_holds",
      "v2.test.execution.emit_ingest_typescript_same_language_round_trip.typescript_same_language_source_emit_round_trip_holds",
      "v2.test.execution.emit_ingest_typescript_same_language_round_trip.typescript_same_language_discriminates_on_real_tree",
    ],
    restoration_trigger: "THE CAPABILITY, not a re-add of the identities: a required-floor claim can inhabit TypeScript parse_tree_to_target_model_bridge and same-language cross_language_compile on the acceptance path UNDER the NewWitnessTier eval-step budget that v2.workflow.required_floor claim_ceiling_eval_step_budget derives, without grandfathering and without expected-red enrolment of a passing identity. Observed at retirement: those two identities (and the discriminator if it is a test fn) are planned test fns, reach a verdict on a required run, and measure at or under that derived budget. Re-adding them over budget, or adding them to floor_grandfathered_roster, does not retire this row.",
  },

  amendments: [],
}
