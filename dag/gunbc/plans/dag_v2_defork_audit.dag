module gunbc.plans.dag_v2_defork_audit


fn dag_v2_defork_audit_body() -> List<std.markdown.MarkdownBlock> {
  [
    gunbc.plans.md_helpers.p(text: "**Status:** carrier-grounded audit + sequencing · **DESIGN.md + the carriers remain the authority** — this doc is an audit/tracker, not a fact ledger (DESIGN §6 \"no parallel-ledger docs\"). Each collapse dissolves into a mark on its carrier (a deleted file, a re-pointed import) when it lands. A task's real state is its branch/PR, not this file. Linked from `ROADMAP.md` §5 *Self-host v2 → delete `src/v1`* (the de-fork sub-lane)."),
    gunbc.plans.md_helpers.p(text: "**Re-verified against the live tree on 2026-06-22 by execution** (decl-name set `comm` per concept + shared-type-body diff). This pass **corrected a load-bearing project assumption**: the dag↔v2 std fork is *not* \"mostly temporary v2 mirrors of dag.\" It is **2 true mirrors + 2 pure name-collisions + 7 divergent groundings** — the same concepts grounded on *different axes/realizations*. The hard part of the de-fork is therefore a grounding-unification **design** (operator-owned, downstream of the numeric tower #5428 and the model↔realization grounding), not a mechanical sweep. The prior version of this doc (\"11 mechanical PRs / 5 collapses / `Classical`↔`Bool` nickname\") was wrong by execution and is superseded below."),
    gunbc.plans.md_helpers.p(text: "**Re-confirmed by execution 2026-06-23 (post-#5640), for the\n[#2 resolver-wall](resolver-type-name-collision-wall.md) (PR #5652) Q2 ruling — see §2A.** Net distribution now: **2 true mirrors** (`reducible` DONE #5507, `measure` in-flight\n#5509) **+ 6 divergent groundings** (`algebra`/`logic`/`nat`/`integer`/`float`/`effects`) **+ 2\nv1-artifact name-collisions** (`node`, `coercion`) **+ 1 RESOLVED** (`verification`, #5640 — it was a\nname-collision resolved by rename-apart, *not* a grounding-merge, so it leaves the cluster). **Executed\nverdict: cant-unify-yet (needs a milestone later than the grounding de-fork) == \{node, coercion\}\nexactly — no third stuck mirror — so the signed `flag-ANY + \{node,coercion\}` wall policy HOLDS.**"),
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "2A. 2026-06-23 executed Q2 confirmation (post-#5640) — supersedes the `verification` census row"),
    gunbc.plans.md_helpers.p(text: "Read-only, by execution. Structural extraction of every top-level `type` decl in `dag/std/<b>.dag`\nvs `src/v2/std/<b>.dag`; shared **unqualified type names** compared with the #2 guard's own\n`structural_inequality` predicate (normalized body equality) — this run is also the §5 proof that the\npredicate classifies correctly. Reachability = BFS of all **351** `_test.dag` floor entries' whole-module\nimport closures testing whether `std.<b>` and `v2.std.<b>` co-occur in one closure (= the guard fires)."),
    std.markdown.TableBlock {
      header: gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "basename"), gunbc.plans.md_helpers.cell(text: "shared type-names"), gunbc.plans.md_helpers.cell(text: "structural verdict"), gunbc.plans.md_helpers.cell(text: "floor co-occurrence"), gunbc.plans.md_helpers.cell(text: "category"), gunbc.plans.md_helpers.cell(text: "unify status")]),
      alignments: [std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone],
      rows: [
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**algebra**"), gunbc.plans.md_helpers.cell(text: "16 (13 differ; 3 byte-identical `Lattice`/`Magma`/`Ordering`)"), gunbc.plans.md_helpers.cell(text: "same concept, divergent **encoding** (dag flat records vs v2 compositional/coproduct)"), gunbc.plans.md_helpers.cell(text: "**LIVE — 75 entries**"), gunbc.plans.md_helpers.cell(text: "grounding (Root-A: v2 coproduct authority RULED 2026-06-22)"), gunbc.plans.md_helpers.cell(text: "cant-unify-yet, **has path** (de-fork-resolved, not exempted)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**nat**"), gunbc.plans.md_helpers.cell(text: "1 (`Nat`: `= CommutativeSemiring<Magnitude>` vs `= Zero \\| Succ`)"), gunbc.plans.md_helpers.cell(text: "same concept, divergent **model** (semiring alias vs Peano coproduct — representational, not nominal)"), gunbc.plans.md_helpers.cell(text: "**LIVE — 138 floor closures** (946 scanned; was 4 @ 351) — **risk measure, not a task count** (see §2A note)"), gunbc.plans.md_helpers.cell(text: "grounding (#5428 numeric tower; census nat-grounding-unification-design.md)"), gunbc.plans.md_helpers.cell(text: "**DESIGN READY** · IMPLEMENTATION LARGE/ATOMIC · SEQUENCED WITH integer+float repoint (not last)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**effects**"), gunbc.plans.md_helpers.cell(text: "3 (`EffectShape`/`KeySource`/`CreateCause`; `EffectShape` body re-modeled on a different axis)"), gunbc.plans.md_helpers.cell(text: "divergent **axis** (operation-kind vs idempotency-class)"), gunbc.plans.md_helpers.cell(text: "latent (0)"), gunbc.plans.md_helpers.cell(text: "grounding"), gunbc.plans.md_helpers.cell(text: "cant-unify-yet, has path (axis-authority decision)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**float**"), gunbc.plans.md_helpers.cell(text: "3 (2 differ; 1 byte-identical `Float = Float64`)"), gunbc.plans.md_helpers.cell(text: "same concept, divergent encoding"), gunbc.plans.md_helpers.cell(text: "latent (0)"), gunbc.plans.md_helpers.cell(text: "grounding (#5428)"), gunbc.plans.md_helpers.cell(text: "cant-unify-yet, has path")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**integer**"), gunbc.plans.md_helpers.cell(text: "14 (11 differ only on `MachineWidth<8>` vs `<Word8>`; 3 byte-identical)"), gunbc.plans.md_helpers.cell(text: "same concept, near-identical (token diff)"), gunbc.plans.md_helpers.cell(text: "latent (0)"), gunbc.plans.md_helpers.cell(text: "grounding (#5428)"), gunbc.plans.md_helpers.cell(text: "cant-unify-yet, has path (mechanical once the tower lands)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**verification**"), gunbc.plans.md_helpers.cell(text: "**0** (was 1: `TestClaim`)"), gunbc.plans.md_helpers.cell(text: "**RESOLVED #5640** (dag record renamed `TestClaim → AssertionClaim`)"), gunbc.plans.md_helpers.cell(text: "none now"), gunbc.plans.md_helpers.cell(text: "**RESOLVED name-collision** (not a grounding)"), gunbc.plans.md_helpers.cell(text: "**DONE**")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**logic**"), gunbc.plans.md_helpers.cell(text: "**0** (dag `Classical` vs v2 `Bool`)"), gunbc.plans.md_helpers.cell(text: "divergent concept, **no name overlap**"), gunbc.plans.md_helpers.cell(text: "none (0 shared)"), gunbc.plans.md_helpers.cell(text: "grounding-or-rename (undecided)"), gunbc.plans.md_helpers.cell(text: "**no type-name collision → guard never fires**")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**coercion**"), gunbc.plans.md_helpers.cell(text: "**0** (dag cast vocab vs v2 `coercion_fold`)"), gunbc.plans.md_helpers.cell(text: "divergent concept, **no name overlap**"), gunbc.plans.md_helpers.cell(text: "none (0 shared)"), gunbc.plans.md_helpers.cell(text: "v1-artifact name-collision"), gunbc.plans.md_helpers.cell(text: "dissolve-on v1-delete; **0 type-name collisions → exemption vacuous for the guard**")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**node**"), gunbc.plans.md_helpers.cell(text: "**0** (dag v1-only `compiler_inductive_fields` vs v2 Node substrate)"), gunbc.plans.md_helpers.cell(text: "divergent concept, **no name overlap**"), gunbc.plans.md_helpers.cell(text: "none (0 shared)"), gunbc.plans.md_helpers.cell(text: "v1-artifact name-collision"), gunbc.plans.md_helpers.cell(text: "dissolve-on v1-delete; **0 type-name collisions → exemption vacuous for the guard**")]),
      ],
    },
    gunbc.plans.md_helpers.p(text: "**Co-occurrence closure counts (algebra 75, nat 138):** each integer counts floor `*_test.dag` **import closures** where both `std.<b>` and `v2.std.<b>` are reachable — a **risk and closure-population measure** for the LIVE fail-open pair, **not** a migration task count or work estimate. New floor entries and tests can grow these numbers legitimately; **do not** ratchet on the integer (gate the unsafe facts instead — nat census §7.6 in [nat-grounding-unification-design.md](nat-grounding-unification-design.md)). Until the fork dissolves, mixed closures silently shadow record-with-record (benign today for nat; coproduct-variant-drop for algebra)."),
    gunbc.plans.md_helpers.p(text: "**Confirms parent's derived distribution; cant-unify-yet == \{node, coercion\} exactly; flag-ANY +\n\{node,coercion\} HOLDS.** Two scope refinements (strengthen, do not refute the ruling):"),
    gunbc.plans.md_helpers.ol(items: [
      gunbc.plans.md_helpers.li(text: "**The \{node,coercion\} exemption is VACUOUS for the type-name guard.** node/coercion (and logic,\nand now verification) share **zero** type names — only the *basename*. The guard keys on a shared\nunqualified **type name** within one closure, so it never fires on them; their de-fork is a\n*module-basename* rename (Route-C / v1-delete), a different surface. The guard needs no\n\{node,coercion\} roster entry to land green — keeping it would be a dead (non-firing) exemption,\nitself a §5 smell. Recommend marking the roster entry explicitly \"Route-C basename, non-firing\"\nor dropping it from the guard model."),
      gunbc.plans.md_helpers.li(text: "**The guard's real lands-green gate is the grounding de-fork of the shared-type-name basenames**,\nfronted by the LIVE pair `\{algebra (75 floor entries), nat (138)\}` — they silently fail-open today\n(benign only because they shadow record-with-record, not the coproduct-variant-drop that broke\n`verification` under A1), and a flag-ANY wall reds them on landing. `\{effects, float, integer\}`\nare latent (no co-occurrence today) but re-arm risks (exactly how verification went latent→LIVE\nunder A1). So the wall lands-already-green only after `\{algebra, nat\}` (then the latent three)\nde-fork — that sequence, not the \{node,coercion\} exemption, gates activation."),
    ]),
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "0. Thesis — the only duplication is v2's bootstrap copies of dag"),
    gunbc.plans.md_helpers.p(text: "`dag/` is the single authority (the standard library + the grounded `extdeps/` domain models + the CI spec). `src/v2/` is the **compiler**, and a compiler needs a standard library to run. During bootstrap it made copies of pieces of `dag/std` inside `src/v2/std`. Those copies are the fork surface."),
    gunbc.plans.md_helpers.p(text: "\"De-fork\" is: **delete v2's duplicate copies and point v2 at the dag authority**, until no concept has two homes — and no genuinely historical fork survives (a name shared by two copies, or two names for one concept). Folder/module naming must reflect one authority, not the fork's history."),
    gunbc.plans.md_helpers.p(text: "**The correction (2026-06-22):** only a *minority* of the overlapping basenames are clean copies. Most are the **model↔realization fork** (DESIGN open thread) surfacing across `std`: the *same concept* modeled on a different axis in each tree (e.g. `EffectShape` by operation-kind in dag vs by idempotency-class in v2), or grounded into a realization in one tree and left thin in the other (the numeric tower). Those cannot be \"delete + repoint\" — repointing breaks consumers at missing-symbol level, and the *shared* type's body itself disagrees. They need a single-authority **design** first."),
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "1. Cross-tree import — ACTIVATED (the former blocker is dissolved)"),
    gunbc.plans.md_helpers.p(text: "The machinery to import `dag/` from `src/v2/` is **wired and on**, proven by execution:"),
    gunbc.plans.md_helpers.ul(items: [
      gunbc.plans.md_helpers.li(text: "Grounded `source_root` tagging landed (#5473 `source_root: SourceRootRef` on `DagSourceReadWitness`; #5486 grounded cross-tree admission — the QualifiedName-prefix fallback is **deleted**, and `tree_fundamentality_order(V2Tree, DagTree) = MoreFundamental` is derived, not guessed)."),
      gunbc.plans.md_helpers.li(text: "`src/v2/compiler/03_name_resolve.dag` no longer carries `FundamentalityUnknown`; `admit_import_entry` calls the grounded `cross_tree_edge_decision`."),
      gunbc.plans.md_helpers.li(text: "**Activation arbiter witness** (#5506, `src/v2/test/claim/cross_tree_real_ingest_activation_test.dag`) feeds a host-tagged `SourceRootIngest` through the *real* grounding machinery (`program_assembly_fold_ingest` builds the QN→`SourceRootRef` index; `source_root_set_from_ingest` builds the active-root set) and runs the real per-edge decision: v2→dag ⟹ `EdgeCrossAdmitted`, tags-reversed dag→v2 ⟹ `EdgeCrossDenied`. So cross-tree import is **live on main, carrier consumed, no operator flip pending**."),
      gunbc.plans.md_helpers.li(text: "The source-root admission abs-vs-rel host bug (#5473's `source_root_ref_token_for_path`) that blocked the rust gate is **fixed** (#5504): file paths and `--source-root` values are grounded through `repo_relative_dag_path` before matching, so admission is invocation-independent."),
    ]),
    gunbc.plans.md_helpers.p(text: "Remaining concrete blocker, scoped to the first real cross-tree **data** import only (NOT the std collapses — std collapses resolve cross-tree clean, no `std.*` collision, no `Option`/`Optional`): `src/v2/std/probe_selector.dag:52` — v2 cannot import `dag/product/compute_fabric` (`Option<T>` vs `Optional<T>`; `std.*` namespace collision under dual source-root). **RESOLVED (#5904):** `compute_fabric` is now the 130-line thin connector (imports only `std.types` + `std.measure`); the namespace collision is gone. Repro test `dag/test/claim/probe_selector_compute_fabric_import_repro_test.dag` deleted (blocker dissolved)."),
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "2. Fork census, re-verdicted by execution (the real shape)"),
    gunbc.plans.md_helpers.p(text: "Three tiers. **Mirror** = v2's used symbols ⊆ dag → mechanical delete + repoint. **Not-a-fork** = shared symbol count is *zero*, the basename is the only collision → rename to disambiguate — **but where the dag-side file is consumed by the `src/v1` seed (import + emitted Rust + a guard test), the rename cascades into the seed and is held** (see the v1-coupled subsection). **Grounding cluster** = the same concept grounded on a different axis/realization in each tree (shared symbols exist, often with a *diverging shared-type body*) → single-authority **design**, operator-owned, downstream of #5428 + the model↔realization grounding. *Not* a repoint and *not* a clean additive merge."),
    gunbc.plans.md_helpers.h3(text: "Mechanical lane (still-deer authority, bright-stag review) — finishable independently"),
    std.markdown.TableBlock {
      header: gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "concept"), gunbc.plans.md_helpers.cell(text: "shared"), gunbc.plans.md_helpers.cell(text: "each side"), gunbc.plans.md_helpers.cell(text: "verdict / state")]),
      alignments: [std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone],
      rows: [
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**reducible**"), gunbc.plans.md_helpers.cell(text: "8 (full set)"), gunbc.plans.md_helpers.cell(text: "v2 header self-declares \"ported from dag\""), gunbc.plans.md_helpers.cell(text: "**Mirror — DONE** (#5507)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**measure**"), gunbc.plans.md_helpers.cell(text: "dag 40-decl authority ⊇ all 10 importer symbols (only 1 importer, `timeseries_signal`, across all trees)"), gunbc.plans.md_helpers.cell(text: "v2 = 3-decl declared mirror"), gunbc.plans.md_helpers.cell(text: "**Mirror — in flight** (#5509, CI-green, ready)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**probe_selector** (step-4)"), gunbc.plans.md_helpers.cell(text: "—"), gunbc.plans.md_helpers.cell(text: "`Option<T>` vs `Optional<T>` + `std.*` collision on the `compute_fabric` data import"), gunbc.plans.md_helpers.cell(text: "**DONE** (#5904). Thin connector replaces `compute_fabric`; `std.*` collision gone; repro test deleted.")]),
      ],
    },
    gunbc.plans.md_helpers.h3(text: "Name-collision renames — V1-SEED-COUPLED (held for ruling)"),
    gunbc.plans.md_helpers.p(text: "`coercion` and `node` are genuine not-a-forks (shared symbol count = 0; the dag and v2 files denote different concepts), **but they are not clean-lane renames**: the dag-side file in each is consumed by the `src/v1` bootstrap seed, so renaming its module cascades into the seed. *Caught by execution — importer greps that omitted `src/v1` initially mis-scoped both as low/zero-importer.* **Ruled DEFER** (bright-stag, 2026-06-22): both renames touch `04_infer` (a DESIGN-named load-bearing inference stage), the emitted Rust seed, and a guard test — high cost + load-bearing **now**, for a pure naming-collision fix whose collision is largely a v1-existence artifact. The cost drops toward zero once v1's consumers are gone (DESIGN §7 / ROADMAP §5 shrink-to-zero), and **the deferred work shrinks rather than just moving** (see each row). Dissolution trigger: *when v1's consumers of `dag/std/\{node,coercion\}` are removed (v1 shrink).*"),
    std.markdown.TableBlock {
      header: gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "concept"), gunbc.plans.md_helpers.cell(text: "shared"), gunbc.plans.md_helpers.cell(text: "each side"), gunbc.plans.md_helpers.cell(text: "v1 coupling"), gunbc.plans.md_helpers.cell(text: "verdict / state")]),
      alignments: [std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone],
      rows: [
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**coercion**"), gunbc.plans.md_helpers.cell(text: "**0**"), gunbc.plans.md_helpers.cell(text: "dag = cast / type-representation vocab (`TypeCheckpoint`, `CastRule`, `CastSyntax`, `CallableRepr`, `dag_cast_rules`); v2 = coercion-as-homomorphism (`coercion_fold` via `find_witness`, `CoercionWitness/Result/Quality`)"), gunbc.plans.md_helpers.cell(text: "dag side imported by `src/v1/coercion.dag` + `src/v1/04_infer.dag`, emitted to `src/v1/stage0/src/std_coercion.rs` (+ 4 `dag/extdeps/languages/*/types.dag` that survive v1)"), gunbc.plans.md_helpers.cell(text: "**DEFERRED.** The 4 extdeps importers survive v1, so `dag/std/coercion.dag` persists — on v1-delete the rename **shrinks to a v1-free scope** (4 extdeps + v2, no seed / no `04_infer` / no guard test), a clean disambiguation. End-state (DESIGN §4): the *fold* **is** coercion → keep `coercion` for the v2 side, rename the dag cast vocab to `cast_rules`. (#5510 attempted it but broke v1 — missed the `src/v1` consumers — and over-scoped by renaming the v2 side; **closed**, not merged.)")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**node**"), gunbc.plans.md_helpers.cell(text: "**0**"), gunbc.plans.md_helpers.cell(text: "dag = `compiler_inductive_fields`/`compiler_recursive_types`/`is_compiler_recursive_type` — the complexity analyzer's inductive-structure *authority* (instance of `std/induction.dag`); v2 = 126-decl, 506-importer real Node substrate"), gunbc.plans.md_helpers.cell(text: "dag side imported by `src/v1/04_infer.dag`, emitted to `src/v1/stage0/src/std_node.rs`, guarded by `src/v1/tests/src/source_audit.rs`; `compiler_inductive_fields` is consumed **only by v1, never v2**"), gunbc.plans.md_helpers.cell(text: "**DEFERRED — self-dissolves.** Since `compiler_inductive_fields` is v1-only, on v1-delete `dag/std/node.dag` goes **dead → delete it, collision gone, no rename needed at all.** It is a live v1-only authority today (not a dead imposter); the collision with the v2 substrate is a v1-existence artifact.")]),
      ],
    },
    gunbc.plans.md_helpers.h3(text: "Grounding cluster (operator authority) — 7 concepts, a unification *design*"),
    gunbc.plans.md_helpers.p(text: "Each row is **operator decision-input**: what is shared (and whether the shared body itself diverges), what each side uniquely carries, and the specific grounding entanglement that makes it a design, not a repoint."),
    std.markdown.TableBlock {
      header: gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "concept"), gunbc.plans.md_helpers.cell(text: "shared"), gunbc.plans.md_helpers.cell(text: "shared body diverges?"), gunbc.plans.md_helpers.cell(text: "dag-only / v2-only"), gunbc.plans.md_helpers.cell(text: "grounding entanglement")]),
      alignments: [std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone, std.markdown.AlignNone],
      rows: [
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**algebra**"), gunbc.plans.md_helpers.cell(text: "16 abstract structures (`Magma`…`Field`, `FreeMonoid`, `Ordering`)"), gunbc.plans.md_helpers.cell(text: "no (the structures match)"), gunbc.plans.md_helpers.cell(text: "dag-only 19 = template/codegen machinery **+ `GroupCompletion`/`FieldOfFractions`**; v2-only 74 = **41 `_node`/`_type_node` substrate projections** + the list/fold framework (213 importers)"), gunbc.plans.md_helpers.cell(text: "numeric tower (`GroupCompletion` = `Int = GroupCompletion<Nat>`) **+** model↔realization substrate reflection. Each side owns a different grounding.")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**logic**"), gunbc.plans.md_helpers.cell(text: "**0**"), gunbc.plans.md_helpers.cell(text: "— (no overlap)"), gunbc.plans.md_helpers.cell(text: "dag-only = `Classical`/`classical_and/or/not`; v2-only = `Bool` + boolean-algebra instance + 6 node-projections + `BoolEncodingFact`/`BoolWidthFact`/`BoolPrimitiveFacts`"), gunbc.plans.md_helpers.cell(text: "Bool grounded into **bit-width** (model↔realization). Operator picks: genuinely different concepts (→ rename) **or** one boolean modeled twice (→ unify on v2's grounded).")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**nat**"), gunbc.plans.md_helpers.cell(text: "`Nat` (the name)"), gunbc.plans.md_helpers.cell(text: "yes (thin alias vs coproduct)"), gunbc.plans.md_helpers.cell(text: "dag = 4-decl `Nat = CommutativeSemiring<Magnitude>`; v2 = 12-decl coproduct `Zero/Succ` + `nat_cata`/`nat_add`/`nat_mul`/`is_zero`/`nat_lte`/`nat_gte`/`NatAlgebraLawObligation`"), gunbc.plans.md_helpers.cell(text: "**#5428** grounded numeric tower (`Zero → Int(0)`, `Succ → Int(k+1)`). Census complete (2026-08-01): **DESIGN READY** — [nat-grounding-unification-design.md](nat-grounding-unification-design.md); prerequisites discharged (#6341 merged; coproduct keystone green). **IMPLEMENTATION LARGE/ATOMIC**, sequenced with integer+float repoint (not last).")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**integer**"), gunbc.plans.md_helpers.cell(text: "14 = the **whole `Int`/`UInt` width tower** (`Int`, `Int8…128`, `UInt`, `UInt8…128`, `IntPlatform`, `UIntPlatform`)"), gunbc.plans.md_helpers.cell(text: "no"), gunbc.plans.md_helpers.cell(text: "v2-only +72 = the arithmetic ops grounded **on** the tower"), gunbc.plans.md_helpers.cell(text: "`Int = GroupCompletion<Nat>` numeric tower.")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**float**"), gunbc.plans.md_helpers.cell(text: "3 (`Float`, `Float32`, `Float64`)"), gunbc.plans.md_helpers.cell(text: "no"), gunbc.plans.md_helpers.cell(text: "v2-only +18 = algebraic-vs-bit-level ops"), gunbc.plans.md_helpers.cell(text: "the algebraic-vs-bit-level layer of the numeric tower + bit-width.")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**effects**"), gunbc.plans.md_helpers.cell(text: "6 core (`EffectShape`, `KeySource`, `CreateCause`, +3 fns)"), gunbc.plans.md_helpers.cell(text: "**YES — the shared `EffectShape` body is re-modeled on a different axis**"), gunbc.plans.md_helpers.cell(text: "dag = **operation axis** (`ReadEffect`/`UpsertEffect`/`DeleteEffect`/`CreateEffect`/`AppendEffect`) + derivation (`derive_effect_shape`/`OperationEffect`/`compose_effects`); v2 = **idempotency-class axis** (`IsIdempotent(IdempotentShape)`/`IsBreaking(BreakingShape)`) + idempotency machinery + node-projections"), gunbc.plans.md_helpers.cell(text: "DESIGN §4 \"idempotency dissolved from an `idempotent:Bool` flag into the `EffectShape` variant\" — v2 is that grounding. Unification question: *which axis is the single authority, or are they orthogonal dimensions of one `EffectShape`?*")]),
        gunbc.plans.md_helpers.row(cells: [gunbc.plans.md_helpers.cell(text: "**verification**"), gunbc.plans.md_helpers.cell(text: "1 = `TestClaim` (**one concept modeled twice**)"), gunbc.plans.md_helpers.cell(text: "**YES**"), gunbc.plans.md_helpers.cell(text: "dag = `TestClaim \{ kind, label \}` simple proposition (+ `TestCase`/`TestNodeRef`/`std.measure Nanosecond`/cost-dimension); v2 = closed assertion coproduct `CompilesClaim`/`DiagnosticClaim`/`EqualsClaim`/`StructuralEqualsClaim`/`RoundTripClaim` (each carrying `anchor`/`Node`/`classification`)"), gunbc.plans.md_helpers.cell(text: "v2's coproduct is entangled with **#5428 + the `Value::Null` straddle fencing + the testgen-oracle** (`GeneratedCrossRepresentationEquality`). Merge + grounding-adjacent.")]),
      ],
    },
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "3. Sequencing"),
    gunbc.plans.md_helpers.p(text: "**Mechanical lane — proceeds now (still-deer authority + bright-stag review, each PR atomic):**"),
    gunbc.plans.md_helpers.ol(items: [
      gunbc.plans.md_helpers.li(text: "**Mirrors:** `reducible` (DONE #5507), `measure` (#5509). Delete the v2 copy, repoint imports to `dag/std/*`; decrement the `fact_cardinality.dag` cross-tree baseline by *exactly* the deleted symbol count; stay green by execution (the existing witnesses are the oracle)."),
      gunbc.plans.md_helpers.li(text: "**Step-4 data import:** `probe_selector` `Option`/`Optional` + `std.*` collision, so `dag/product/compute_fabric` imports cleanly into v2. **DONE (#5904).**"),
    ]),
    gunbc.plans.md_helpers.p(text: "**Deferred to v1-shrink (the not-a-fork renames):** `coercion`, `node`. v1-seed-coupled; ruled DEFER with the dissolution trigger *\"when v1's consumers of `dag/std/\{node,coercion\}` are removed\"* — at which point `node` self-dissolves (delete the dead file) and `coercion` shrinks to a v1-free disambiguation. Not dispatched until then."),
    gunbc.plans.md_helpers.p(text: "**Grounding cluster — held for the operator** (a single-authority unification *design*, downstream of the numeric tower #5428 and the model↔realization grounding): `algebra`, `logic`, `nat`, `integer`, `float`, `effects`, `verification`. These are **not** dispatched as mechanical PRs. The grounded dag authority for each has to be *designed* before any fan-out can repoint to it (the same shape as the project's \"substrate migration precedes the ratchet, is not a path to it\"). **`nat` census complete (2026-08-01):** per-concept design in [nat-grounding-unification-design.md](nat-grounding-unification-design.md) — **DESIGN READY**; prerequisites discharged (`algebra` `FreeMonoid` shadow #6341 merged; generic-alias coproduct keystone green). **IMPLEMENTATION LARGE/ATOMIC**, **SEQUENCED WITH integer+float repoint** (not last). Operational gate before the wave: bounded P3b PR2 overflow on `std.integer` lands first (#7511 merged; both lanes touch the numeric authority — one writer at a time). **Atomic wave file ownership (one push):** `dag/std/nat.dag` ← coproduct + Peano ops; delete dag semiring alias; `v2.std.nat` ← thin reimport + node-bound law roster only; `integer`/`float` repoint `GroupCompletion<Nat>` to `std.nat.Nat`; all importer repoints in the same push (§5 auto-committer hazard)."),
    gunbc.plans.md_helpers.p(text: "**Hazard (atomicity):** the dashboard auto-committer can snapshot a multi-file rename/collapse mid-edit (a symbol deleted in file A while file B still calls it) → an internally-inconsistent intermediate commit caught by a *frozen* merge-sha CI run → phantom \"not found in scope\" red. `gh run rerun` cannot fix it (it re-merges the broken head); only a fresh consistent push. Stage every file of a rename/collapse together."),
    std.markdown.ThematicBreak,
    gunbc.plans.md_helpers.h2(text: "3b. OPERATOR RULING — grounding-cluster anchor (2026-06-22) — the brief"),
    gunbc.plans.md_helpers.p(text: "The operator ruled the FreeMonoid/algebra single authority that category (b) was parked on. **Both confirmed:**"),
    gunbc.plans.md_helpers.ol(items: [
      gunbc.plans.md_helpers.li(text: "**Structural authority = the coproduct** `type FreeMonoid<T> = Empty | Cons \{ head, tail \}` (the `src/v2/std/algebra.dag` form). The `dag/std/algebra.dag` record-of-methods form is **derived from inhabitance** (DESIGN §4 — \"ops from inhabitance, no per-type ops\"), **not** a second definition. The record surface is a projection over the coproduct, not an authority."),
      gunbc.plans.md_helpers.li(text: "**Grounded-realization wins.** Generalize the **#5428 `RustCorpusRepr` seam** (`HostNative` → `Nat/Int`=`i64`, `List`=`Vec`; `FaithfulFreeMonoid` → coproduct) from List/Nat to **all FreeMonoid carriers**, so `String = FreeMonoid<Char>`, `List<T> = FreeMonoid<T>`, `QualifiedName = FreeMonoid<Symbol>` are **aliases** grounding to native `Vec` in the seed and to the faithful coproduct in pure-v2. **SUPERSEDED PREMISE (eager-deer-389):** `RustCorpusRepr` is DELETED -- there is no `HostNative`/`FaithfulFreeMonoid` seam left to generalize, and `rust_corpus_repr` no longer exists. Realization is keyed on each type's declaring module. This item is quarry per DESIGN section 3 (existing designs are re-read as quarry, never authority over the replacement): its OBSERVATION about FreeMonoid carriers may still hold, but its proposed MECHANISM is gone and cannot be built."),
    ]),
    gunbc.plans.md_helpers.h3(text: "Two roots, two lanes (do not collide)"),
    gunbc.plans.md_helpers.ul(items: [
      gunbc.plans.md_helpers.li(text: "**Root A — grounding (jolly-cat owns, build-now).** The `src/v1/05_emit_rust.dag` emit-seam (`rust_seed_host_container_base`/`rust_named_type_base`/`rust_corpus_repr`) generalized to String/FreeMonoid → host `Vec`. **Emit-layer only — does NOT change `std/algebra.dag` definitions**, so it runs concurrently with Root B without conflict. Kills ~48% of the 16,071 self-host cargo errors (String+List). The carrier-independent tail (import-dedup E0252, closure-Debug) rides here too. **SUPERSEDED PREMISE (eager-deer-389):** `RustCorpusRepr` is DELETED -- there is no `HostNative`/`FaithfulFreeMonoid` seam left to generalize, and `rust_corpus_repr` no longer exists. Realization is keyed on each type's declaring module. This item is quarry per DESIGN section 3 (existing designs are re-read as quarry, never authority over the replacement): its OBSERVATION about FreeMonoid carriers may still hold, but its proposed MECHANISM is gone and cannot be built."),
      gunbc.plans.md_helpers.li(text: "**Root B — generic-inference keystone + de-fork (THIS lane).** Phased, in order:"),
    ]),
    gunbc.plans.md_helpers.ol(items: [
      gunbc.plans.md_helpers.li(text: "**Keystone (partially landed via #5552):** simple generic-alias instantiation resolves — `type X = FreeMonoid<Symbol>` defines and construct/match/use-site works (proven by `generic_alias_coproduct_instantiation_test.dag`); `qualified_name.dag` is fully dissolved (incl. the v1 seed host-builtins made generic — `free_monoid_symbol_*`). **Still open:** raw variant-matching a value bound from a recursive `Cons.tail` field still fails *\"variant not found in type FreeMonoid\"* (`04_infer` fixpoint gap) — consumers avoid it via the `list_head`/algebra idiom; `catalog.dag` `fold_list`/`ParseTable` still await it. This residue is load-bearing resolver/infer work (`03_resolve`/`04_infer`) — model-before-implement, escalate before touching under any pre-dating brief."),
      gunbc.plans.md_helpers.li(text: "**Definition unification:** make the coproduct the single authority in `std/algebra.dag`, delete the dag record-of-methods *definition* (re-express its surface as inhabitance), introduce the aliases. Only after the keystone lands (so the aliases compile). jolly-cat's Root A emit-seam should already be in by here — coordinate the `algebra.dag` touch."),
      gunbc.plans.md_helpers.li(text: "**Repoints** (mechanical once 1–2 land): `nat`/`integer`/`float` (numeric tower `Int = GroupCompletion<Nat>`), `logic`, `effects` (operation-axis ⊕ idempotency-axis grounding), `verification` (entangled with #5428 + `Value::Null` — may stay last). Per the #5511 per-concept census above."),
      gunbc.plans.md_helpers.li(text: "**Dissolve the 🟡 markers:** `qualified_name.dag` — **DONE** (`QnEmpty/QnCons` → `Empty/Cons`, `qualified_name_eq/for_all/singleton` → FreeMonoid ops + structural `==`, v1 seed bridge made generic); `catalog.dag` `fold_list` still awaits the recursive-tail resolver residue. The self_gen8 3 ignores fold into the def-unification PR — their premise flips from \"stays_unemitted\" to \"expects grounded `List<T>=Vec<T>` emission\" (`node://adhoc-9d2bb9c3-e7b`)."),
    ]),
    gunbc.plans.md_helpers.h3(text: "Fences (DESIGN §3/§6, and the #5516 respawn-mandate lesson)"),
    gunbc.plans.md_helpers.ul(items: [
      gunbc.plans.md_helpers.li(text: "**v1-coupled `coercion`/`node` renames stay DEFERRED to v1-delete** — category (c). Out of scope for this lane regardless of title."),
      gunbc.plans.md_helpers.li(text: "**The emit-seam (Root A) is jolly-cat's** — this lane does not touch `05_emit_rust.dag`."),
      gunbc.plans.md_helpers.li(text: "**Stage every file of a rename/collapse together** (the auto-committer atomicity hazard, §3 above)."),
    ]),
    gunbc.plans.md_helpers.h3(text: "Execution update (#6341, 2026-07-07) — Root B narrowed to shadow-removal; three walls re-map the follow-ons"),
    gunbc.plans.md_helpers.p(text: "`#6341` landed the **core of Root B**: the `FreeMonoid`/`Empty`/`Cons` variant-drop shadow is removed — the coproduct now lives once in `dag/std/algebra.dag:111`, `v2.std.algebra` imports it, 158 importers repointed. But implementing the *fuller* Root B (move the ops; unify the definition; promote QualifiedName) surfaced **three walls by-execution** that re-scope the remaining lanes. Full trace + census: **[algebra-grounding-unification-design.md §9](algebra-grounding-unification-design.md)**. Summary for the roadmap:"),
    gunbc.plans.md_helpers.ul(items: [
      gunbc.plans.md_helpers.li(text: "**A — reserved-name → builtin dissolution.** `dag/std/algebra.dag`'s templates *declare* the builtin collection-method names (`fold`/`map`/`filter`/`length`/`any`/`contains`/`skip`/…); a `std.*` free fn matching one breaks the whole method table (bare `fold(...)` fails corpus-wide). So the 8 ops `filter`/`length`/`any`/`contains`/`skip`/`is_empty` (+ `non_empty`/`bag_eq`) **cannot** move to `std.*` — they are free-fn duplicates of dag builtins and must *dissolve into* `.filter()`/`.length()` at v2 call sites (a lane, not a rename — a `list_filter` nickname is a §3 violation)."),
      gunbc.plans.md_helpers.li(text: "**B — Root A is UNOWNED and confirmed load-bearing for the ops.** `src/v1/05_emit_rust.dag` hardcodes `crate::v2_std_algebra::\{freemonoid_empty,fold_list,list_snoc_item\}` for the `String=FreeMonoid<Char>` host bridge — so those 3 ops can't move until Root A relocates them. **jolly-cat (the assigned Root-A owner above) is no longer in the tree** (operator, 2026-07-06); the `String`/`List` alias grounding (§3b.2 above) is blocked until Root A is re-dispatched."),
      gunbc.plans.md_helpers.li(text: "**C — QualifiedName is `node`-gated, not algebra-gated.** `src/v2/std/qualified_name.dag` imports `v2.std.node`/`v2.std.diagnostic`/`v2.std.compilers.lexing`, all v2-only (`dag→v2` denied). FreeMonoid single-authority was **necessary but not sufficient**; promoting QualifiedName to `std.qualified_name` (unparking `type-env-single-authority-design.md` §5.1) needs the deferred **`node` defork** too (category (c), v1-delete)."),
    ]),
    gunbc.plans.md_helpers.p(text: "Definition-unification (§3b.2: delete the dag record-of-methods *definition*) and the structure-record grounding (12 of 14 records diverge) remain deferred; the `flag-ANY` co-occurrence wall for the full `algebra` pair greens only after those. `#6341` removed the *dangerous* variant-drop shadow — the record shadow left is the benign record-with-record kind."),
  ]
}

data dag_v2_defork_audit_plan: gunbc.plan.Plan = gunbc.plan.Plan {
  slug: "dag-v2-defork-audit",
  title: "Plan — De-fork `dag/` ↔ `src/v2/` (collapse the duplicated standard library)",
  body: dag_v2_defork_audit_body(),
  retirement: gunbc.plan.PlanRetiresWhen { condition: std.dissolution.unbound_dissolution(description: "Delete this doc when the fork census reaches zero — when no `std` basename denotes two concepts and every concept has a single authority. The census now has **three categories**, dissolving on different triggers: **(a) True mirrors** (`reducible`, `measure`) — mechanical collapse; dissolve into their carriers (absent files, re-pointed imports) as each PR lands. Plus `probe_selector` (the step-4 data-import unblock), independent. **(b) Grounding divergences** (`algebra`, `logic`, `nat`, `integer`, `float`, `effects`, `verification`) — downstream of the operator's grounding-unification design; tracked here as decision-input until that design lands and the repoints become mechanical. **(c) v1-artifact collisions** (`node`, `coercion`) — deferred to v1-shrink; trigger \"when v1's consumers of `dag/std/\{node,coercion\}` are removed\" (`node` self-dissolves, `coercion` shrinks to a v1-free rename). At the point all three are resolved the carriers tell the whole story and this audit is redundant.")
  }
}
