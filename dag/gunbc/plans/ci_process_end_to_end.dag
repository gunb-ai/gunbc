module gunbc.plans.ci_process_end_to_end


fn ci_process_end_to_end_body() -> List<MarkdownBlock> {
  [
    quote(blocks: [
      p(text: "**The charter for ROADMAP §1.** Every other CI plan doc is a *slice*; this is the whole pipeline in one place — the causal chain from `git push` to test execution, each link marked on-`.dag` or not, with each slice linked where it applies (no dual representation — the carriers and linked docs stay the authority). Built from two independent audits that agree (neat-dove-397 Explore + quick-ant-298, the §1 CI lead), both verified on `origin/main`, 2026-06-22. DESIGN refs: §1 (time = cost/safety), §2 (Realization — the parallelization arm), §3 (single authority; the *measured-peripheral* split), §5 (fail-closed — **derive, don't hand-set**), §6 (price the lane in displaced cost, not elegance), §7 (the substrate analyzing itself)."),
      p(text: "Live scalars are marked **[confirm]** — quick-ant-298 holds the freshest numbers; the *shape* is the point."),
    ]),
    hr(),
    h2(text: "0. Why CI is a lane, not a chore"),
    p(text: "CI is **ROADMAP §1**, and it is *upstream of every §0 claim*: a flaky or green-but-broken floor means no gate protects anything, so every `[x]` in §0–§4 rests on it. But the deeper reason it deserves a lane: **CI is the one workload that flexes *every* substrate layer at once** — execution-as-a-DAG, scheduling, caching, secrets/effects, emission — so it is the **forcing function** that turns each *modeled-but-inert* abstraction load-bearing (the direct antidote to DESIGN §6's \"the machinery exists but nothing gates on it\")."),
    p(text: "**The deliverable is therefore not \"faster CI.\"** Faster CI is the displaced-cost *symptom* you feel (*\"move with confidence\"*). The deliverable is **shared abstractions, proven by CI consuming them**: one Materialization kernel (collapsing the caches), one Placement authority (collapsing jobs/threads/sessions), one secrets model. Speed falls *out* of that. Priced in elegance instead, \"flex every layer\" is unbounded (the §6 purity trap) — so the lane is on-dial only insofar as CI-timeliness is the *cheapest path to the pain*, and each shared abstraction is pulled in **as CI actually flexes it**, not by taxonomy."),
    hr(),
    h2(text: "1. The pipeline as it runs today"),
    p(text: "One PR push → one CI run: trigger (`ci.yml` on `pull_request`+`workflow_dispatch`) → GitHub starts the run → concurrency-group eval → GitHub places the **single** `jobs.ci` (no `matrix`) on the first idle `[self-hosted, linux, arm64]` runner → the runner builds `v1-compiler` and invokes `claim_executor … --plan-function gunbc_ci_floor_batches` → the executor derives batches from `ci_floor_plan.dag`, reads the live cgroup budget, shards by `spawn_width`, and runs the `.dag` witness corpus + gates → exit code → GitHub marks the check → dashboard gates review/merge."),
    p(text: "`ci.yml` itself is a byte-for-byte projection of `gunbc.ci_yaml_emit` (`ci_workflow.dag` → `ci_yaml_emit`, authored steps incl. the build command and the `claim_executor` invocation), drift-gated by `tools/ci_yaml_gate.dag`. So the **workflow description is on fabric.** The question §2 answers is whether the *fabric* is."),
    hr(),
    h2(text: "2. The causal chain — what's on `.dag` today vs not"),
    p(text: "**Legend:** 🟢 **DERIVED** (`.dag` is the authority; artifact generated + drift-gated) · 🔵 **EXECUTING `.dag`** (the `.dag` is literally interpreted at runtime — the strongest sense) · 🟡 **MODELED-INERT** (`.dag` *describes* it but doesn't operate/enforce it) · 🔴 **OFF-FABRIC** (GitHub-native or hand/host; no `.dag`)."),
    TableBlock {
      header: row(cells: [cell(text: "#"), cell(text: "Link (push → execute)"), cell(text: "Verdict"), cell(text: "Wall (#5427 run)"), cell(text: "Authority today / gap")]),
      alignments: [AlignNone, AlignNone, AlignNone, AlignNone, AlignNone],
      rows: [
        row(cells: [cell(text: "1"), cell(text: "push branch / open-sync PR"), cell(text: "🔴"), cell(text: "—"), cell(text: "git + GitHub native")]),
        row(cells: [cell(text: "2"), cell(text: "GitHub reads committed `ci.yml` triggers, starts run"), cell(text: "🟢 *file* / 🔴 *act*"), cell(text: "~0"), cell(text: "file derived+gated; dispatch engine native. **Bootstrap seam**: reads the *committed* file → stale-on-invocation-surface is uncaught")]),
        row(cells: [cell(text: "3"), cell(text: "concurrency group + cancel-in-progress"), cell(text: "🟢 *key* / 🔴 *eval*"), cell(text: "~0"), cell(text: "key modeled (`ci_workflow_expressions`), **carries the `run_id`-fallback dup bug**; evaluated by GitHub")]),
        row(cells: [cell(text: "3′"), cell(text: "dashboard *also* fires `workflow_dispatch` same SHA"), cell(text: "🔴"), cell(text: "—"), cell(text: "ctrl/dashboard — **the dup source**")]),
        row(cells: [cell(text: "4"), cell(text: "GitHub matches labels & **places the job on a host**"), cell(text: "🟢 *labels* / 🔴 *placement*"), cell(text: "~0 (queue)"), cell(text: "labels derived (`runner_spec_from_offer`); **placement is native, demand-blind, first-idle** ← **underutilization root (G1)**")]),
        row(cells: [cell(text: "5"), cell(text: "a runner daemon on srv1/srv2 picks it up"), cell(text: "🔴 (🟡 inventory)"), cell(text: "—"), cell(text: "runners/host, registration, on-box labels = **hand-run shell, no repo artifact (G2)**; `operator_fleet` only *describes* the hosts")]),
        row(cells: [cell(text: "6"), cell(text: "the runner's cgroup cap bounds it"), cell(text: "🔴"), cell(text: "—"), cell(text: "`TasksMax`/`MemoryMax` host-set by hand **(G3)**; `.dag` only *reads* it live — adaptive, not authoritative")]),
        row(cells: [cell(text: "7"), cell(text: "steps: isolate toolchain, checkout, setup-rust, cache"), cell(text: "🟢 *list* / 🔴 *tools*"), cell(text: "**~15s**"), cell(text: "step list derived; bodies shell out to git/rustup/cache")]),
        row(cells: [cell(text: "8"), cell(text: "`cargo build -p v1-compiler` + freshness/exists verify"), cell(text: "🟢"), cell(text: "**~1m45s**"), cell(text: "command + §5 guards derived; sccache-warm (847/859 hits) at `BUILD_JOBS=1`")]),
        row(cells: [cell(text: "9"), cell(text: "`claim_executor … gunbc_ci_floor_batches`"), cell(text: "🔵"), cell(text: "*(batches ↓)*"), cell(text: "**executor interprets `ci_floor_plan.dag`; batches from dependency edges** — strongest on-fabric link")]),
        row(cells: [cell(text: "·"), cell(text: "⤷ batch 1 — compile-clean gate"), cell(text: "🔵"), cell(text: "**~22s**"), cell(text: "`gunbc compile --target rust` over the whole tree + a RED control")]),
        row(cells: [cell(text: "·"), cell(text: "⤷ batch 2 — corpus + rust monolith + 5 gates (width 8)"), cell(text: "🔵/🔴"), cell(text: "**~43m20s**"), cell(text: "**87% of the run; one atomic rust node holds it open — see §6**")]),
        row(cells: [cell(text: "·"), cell(text: "⤷ batch 3 — source-root-ingest + self-host closure"), cell(text: "🔵"), cell(text: "**~4m29s**"), cell(text: "2 serial (width-1) sub-shells of heavy `gunbc run`s")]),
        row(cells: [cell(text: "10"), cell(text: "read live budget → `memory_aware_spawn_width` → shard"), cell(text: "🔵 *decision* / 🔴 *input*"), cell(text: "*(in 9)*"), cell(text: "width logic executes `.dag`, reading off-fabric host state; gave `width=8` from a 112 GB live budget")]),
        row(cells: [cell(text: "11"), cell(text: "affected-set skip over the git-diff frontier"), cell(text: "🔵"), cell(text: "*(in 9)*"), cell(text: "the `.dag` floor already shrinks to affected (0 skipped here)")]),
        row(cells: [cell(text: "12"), cell(text: "gates: ci.yml-drift, rust fmt/clippy+**run-all**, layering, corpus"), cell(text: "🔵 *corpus* / 🔴 *rust*"), cell(text: "*(in batch 2)*"), cell(text: "corpus **is `.dag` executing**; the rust gate shells to cargo, **no affected-set (G5)**, and is the tentpole")]),
        row(cells: [cell(text: "13"), cell(text: "exit code → GitHub marks the check"), cell(text: "🔴"), cell(text: "~0"), cell(text: "GitHub-native")]),
        row(cells: [cell(text: "14"), cell(text: "dashboard reads check + reviews → merge"), cell(text: "🔴"), cell(text: "—"), cell(text: "ctrl/dashboard; merge manual")]),
        row(cells: [cell(text: ""), cell(text: "**total step**"), cell(text: ""), cell(text: "**49m58s**"), cell(text: "one `gunbc ci` step")]),
      ],
    },
    hr(),
    h2(text: "3. The conflation, named: three layers of \"on fabric\""),
    p(text: "\"Largely on fabric\" hid that **two different links are both genuinely on `.dag`, at opposite ends**, with an off-fabric band between them:"),
    ul(items: [
      li(text: "**A — the workflow *description*** (links 2–8): `ci.yml`. **🟢 DERIVED**, drift-gated."),
      li(text: "**B — the fabric *operation*** (links 4–6): which host, runners-per-host, cgroup caps, the shell you SSH in and run. **🔴 OFF-FABRIC** — imperative, hand-run, unversioned, *no repo artifact*. `operator_fleet` *describes* the hosts (🟡 inert) but does not *operate* them; nothing generates host config, nothing reconciles host supply against `ci.yml`'s demand. A mis-set host doesn't red — CI silently runs narrow/slow or OOMs."),
      li(text: "**C — the A↔B relationship**: **not enforced.** Coupled only by a label string + a one-way live read of `memory.max` (adaptive, not authoritative)."),
    ]),
    p(text: "**The one-line gap:** the *description* (A) and the *work* (§4) are on `.dag`; the *operation of the fabric* (B — placement + runner deployment + caps) is entirely off it. Close B as one unit and \"on `.dag`\" becomes true of the whole chain except GitHub's irreducible engine."),
    hr(),
    h2(text: "4. Which substrate layers CI flexes — and do they work?"),
    TableBlock {
      header: row(cells: [cell(text: "Layer"), cell(text: "State"), cell(text: "Evidence")]),
      alignments: [AlignNone, AlignNone, AlignNone],
      rows: [
        row(cells: [cell(text: "**Execution as a dependency graph**"), cell(text: "**✅ WORKS — the realest layer**"), cell(text: "the floor *is* a bounded forward DAG walk; `claim_executor` interprets `ci_floor_plan.dag`, one fold (`fold_node`), batches from dependency edges. The core thesis runs in prod every push")]),
        row(cells: [cell(text: "**Scheduling**"), cell(text: "**◑ one axis live, rest inert**"), cell(text: "**Width** consumes the measured envelope (#5444), single-host. `Placement`/`Materialization`/`RealizationObjective` are modeled + witness-passing but have **no non-test consumer** — the same band as the host-ops gap (§3-B), substrate side")]),
        row(cells: [cell(text: "**Caching**"), cell(text: "**◑ five forks, converging**"), cell(text: "sccache live (build) · resolve-cache **dormant** (modeled, pure-proven 616/616, env var unset — biggest dormant lever) · ParseTable memo live (content-addressed, amortization witness) · RecordedFixture (record/replay) · BuildBuddy opt-in. The one-door `realize(subject)` kernel (§2 P2) is staged")]),
        row(cells: [cell(text: "**Secrets / effects**"), cell(text: "**○ ad hoc**"), cell(text: "live cgroup reads, sccache servers, BMC, tokens — no single model yet")]),
        row(cells: [cell(text: "**Emission**"), cell(text: "**✅ works**"), cell(text: "`ci.yml` + testgen are emitted + drift-gated; stage0 seed `--verify` is the one missing gate (§5 keystone)")]),
      ],
    },
    p(text: "So: execution and emission **work**; scheduling and caching **work in one slice each and are inert/forked otherwise**; secrets are unmodeled. CI is the workload that makes finishing each of these *pay*."),
    hr(),
    h2(text: "5. Why it runs poorly — one root, several faces"),
    p(text: "**Root: the model is on fabric, but the decisions that govern utilization — which host a run lands on, runners-per-host, per-runner caps — are NOT derived from it. They're demand-blind hand/host knobs.** The symptom is a fleet with **no operating point** — two 128-core hosts either oversubscribed/crashing or at ~1%, never the middle ([compute-envelope-model.md](compute-envelope-model.md) §1). Headline: a 128-core box runs CI at **width ~4 [confirm]** while the other host idles."),
    TableBlock {
      header: row(cells: [cell(text: "Face"), cell(text: "Mechanism"), cell(text: "Link")]),
      alignments: [AlignNone, AlignNone, AlignNone],
      rows: [
        row(cells: [cell(text: "placement demand-blind"), cell(text: "single job, no matrix → first-idle → heavy runs co-reside, other host idles"), cell(text: "G1 (link 4)")]),
        row(cells: [cell(text: "runner deployment hand-managed"), cell(text: "runners/host + caps not in repo → over-commit"), cell(text: "G2/G3 (links 5–6)")]),
        row(cells: [cell(text: "resolve cache dormant"), cell(text: "`GUNBC_RESOLVED_GRAPH_CACHE_DIR` unset → ~191s [confirm] cold resolve + high peak caps width low"), cell(text: "§4 caching")]),
        row(cells: [cell(text: "`workflow_dispatch` dup"), cell(text: "dispatch + PR → two same-SHA runs escape the `run_id` key → OOM"), cell(text: "G4 (links 3/3′)")]),
        row(cells: [cell(text: "rust gate all-or-nothing"), cell(text: "no affected-set on `.rs` PRs"), cell(text: "G5 (link 12)")]),
        row(cells: [cell(text: "stale-green merge"), cell(text: "PR validated before a new gate landed"), cell(text: "[ci-merge-freshness.md](ci-merge-freshness.md)")]),
      ],
    },
    hr(),
    h2(text: "6. Where the 50 minutes goes (measured — #5427 run 27924855136, srv2, width 8)"),
    p(text: "The whole 50 min is one step (`gunbc ci`, 49m58s). By phase, then one level deeper into what each runs:"),
    TableBlock {
      header: row(cells: [cell(text: "Phase"), cell(text: "Wall"), cell(text: "What it runs (one level down)")]),
      alignments: [AlignNone, AlignNone, AlignNone],
      rows: [
        row(cells: [cell(text: "Setup"), cell(text: "~15s"), cell(text: "checkout (fetch-depth 0, ~1s) · setup-rust (rustfmt already current, ~10s) · cargo-cache restore")]),
        row(cells: [cell(text: "Build `v1-compiler --release`"), cell(text: "~1m45s"), cell(text: "`cargo build … --bins` at **`CARGO_BUILD_JOBS=1`**; **sccache warm (847/859 hits)** so fast anyway. Produces the seed bins: `gunbc` · `claim_executor` · `discover_source_root_ingest`")]),
        row(cells: [cell(text: "**Batch 1** — compile-clean gate"), cell(text: "~22s"), cell(text: "`gunbc compile --target rust` over `dag`+`src/v2` (whole substrate must compile clean) **+ a RED control** (a deliberately-perturbed compile must fail)")]),
        row(cells: [cell(text: "**Batch 2** (width 8, 7 nodes)"), cell(text: "**~43m20s**"), cell(text: "one *atomic* node dominates — split below")]),
        row(cells: [cell(text: "**Batch 3** — ingest + self-host"), cell(text: "~4m29s"), cell(text: "2 **serial** (width-1) sub-shells: ① `discover_source_root_ingest` + 3 program-assembly ingest witnesses (~2m16s) · ② self-host closure + gap4-parse + closure module-count witnesses (~2m13s)")]),
        row(cells: [cell(text: "Teardown"), cell(text: "~19s"), cell(text: "save cargo cache (tar+zstd, 233 MB)")]),
      ],
    },
    p(text: "**Batch 2, one level down.** The 7 nodes run width-8 parallel but are wildly unbalanced — one *atomic* node holds the batch open for 43 min while the rest finish in the first ~7:"),
    TableBlock {
      header: row(cells: [cell(text: "Batch-2 node"), cell(text: "Wall"), cell(text: "What it runs")]),
      alignments: [AlignNone, AlignNone, AlignNone],
      rows: [
        row(cells: [cell(text: "discovery corpus (660 `.dag` witnesses)"), cell(text: "~6m (done 02:06)"), cell(text: "**resolve 840s + eval 910s = 1750s CPU** ÷ width 8. resolve = re-resolving each witness's import graph **cold (resolve cache OFF)**; eval = interpreting each witness **un-memoized**")]),
        row(cells: [cell(text: "5 lens/gate witnesses (emit-host · layering · resolved-imports · extdeps-authority · ci.yml-drift)"), cell(text: "sec–min"), cell(text: "each a `gunbc run --claim-run` over one `_test.dag`; finish early, masked")]),
        row(cells: [cell(text: "**`rust_monolith_gate`**"), cell(text: "**~43m**"), cell(text: "`cargo fmt` (~s) · `clippy --all-targets` (~35s, sccache-warm) · **`cargo test -p v1-compiler-tests` = 42m39s** ← the #5427 run-all")]),
      ],
    },
    p(text: "**The tentpole, one more level — `cargo test -p v1-compiler-tests` (42m39s):** *compile* the test crate at **`CARGO_BUILD_JOBS=1`** + *run* the ~792 tests. The compile-vs-run split is **not instrumented** — the one measurement worth taking before committing a fix (`cargo test --no-run` timing). The *mechanism*, though, is already clear (§7)."),
    hr(),
    h2(text: "7. Why batch 2 is slow — the coherent, whole-pipeline reading"),
    p(text: "Not \"the rust gate is inherently 43 min.\" It's the same mechanism §1 keeps hitting: **on a 128-core box, every heavy phase runs at single-digit parallelism or recomputes cold — because nothing derives the safe parallelism from the host.** Seen across the whole pipeline:"),
    ul(items: [
      li(text: "**The rust tentpole compiles at parallelism ~1.** `CARGO_BUILD_JOBS=1` is force-set whenever sccache is active — a **panic-clamp** to dodge the `sccache × codegen-units` pids-cap EAGAIN crash ([compute-envelope-model.md](compute-envelope-model.md) §1). To avoid the *crash* extreme the box is pinned to the *idle* extreme: it compiles the test crate single-threaded on 128 cores. That clamp **is** the crash-or-idle swing, landed on the rust gate."),
      li(text: "**It is also a single un-sharded node.** The width-8 scheduler shards the `.dag` corpus (1750s CPU → 6m wall) but treats `cargo test -p v1-compiler-tests` as **one opaque node** — no nextest partition, no per-test-group shard — so its cost is *serial wall*, not divided. The scheduler's parallelism never reaches inside it."),
      li(text: "**The `.dag` corpus is fast only because it's sharded — it still pays cold.** 1750s CPU for 660 witnesses is itself high: resolve runs **cold every run** (dormant resolve cache) and eval is **un-memoized**. Width 8 hides it as 6m wall; it is not cheap."),
      li(text: "**Batch 3 is serial** — two width-1 sub-shells of heavy `gunbc run`s for ~4.5m."),
    ]),
    p(text: "So the whole pipeline tells **one** story: the heaviest work is either clamped to low parallelism (rust `jobs=1`, batch-3 serial) or recomputed cold (resolve cache off) — a 128-core machine used at single-digit parallelism for its two most expensive phases. That is exactly the **scheduling-inert + caching-dormant** gap of §4, on the clock. The levers, none a tweak:"),
    ol(items: [
      li(text: "**Derive a safe `CARGO_BUILD_JOBS` (>1) from the envelope** (`jobs = f(cores, pids-cap, mem)`) instead of the panic-clamp to 1 — unclamps compile (G3 / compute-envelope)."),
      li(text: "**Shard the rust node** (nextest partition) so the width-8 scheduler reaches inside it (G5 / §9 one-Placement-authority — the rust gate is off the `.dag` scheduler today)."),
      li(text: "**Enable the resolve cache** — drops the 840s cold resolve under the corpus."),
      li(text: "**edge-(b) selection** — run only the affected rust tests, so the run-all isn't paid per-PR at all."),
    ]),
    p(text: "(1)+(2) attack the tentpole's two mechanisms; (3) the corpus; (4) removes the run-all from most PRs. Each closes a derive-from-the-host gap — none is \"trim a number.\""),
    hr(),
    h2(text: "8. What to do now — leverage-ranked, none a tweak"),
    p(text: "Ordering principle: **finishes/enables that make utilization fall out of the model come before any new scheduler.** Sharding/tiering knobs are excluded — they lower a number once and don't compound (§6)."),
    ol(items: [
      li(text: "**G2/G3 — derive runner *deployment* (count + caps) from `operator_fleet` + `ResourceEnvelope`, generate + drift-gate it** (the `ci.yml` pattern, for the host). Makes *\"N runners × peak < host RAM\"* a computed fact, gives the envelope its first real consumer, and converts \"I SSH in and run shell\" into \"regenerate + a gate reds on drift.\" The cheapest substrate-migration win; not a scheduler. → [compute-envelope-model.md](compute-envelope-model.md)"),
      li(text: "**Enable the resolve cache in CI** — cuts cold resolve *and* lowers per-shard peak → width rises → the host fills. Cheap, orthogonal, purity proven. → [realization-measurement-loop.md](realization-measurement-loop.md)"),
      li(text: "**G1 — emit a cross-host `matrix` from the fleet model** (shard across srv1+srv2). The placement predicate's real consumer. **The one step needing a decision** — build CI placement now vs fence the dead predicate."),
      li(text: "**G5 — edge-(b) rust affected-set.** → [ci-selection-vs-scheduling.md](ci-selection-vs-scheduling.md)"),
      li(text: "**G4 — collapse the `workflow_dispatch` dup** (key model + stop redundant dispatch). → [ci-merge-freshness.md](ci-merge-freshness.md)"),
      li(text: "**Reclassify contention OOMs before green-on-main can gate** — a cross-run clustering consumer that promotes a clustered `exit-137` back to `Infra` (two OOM-kill handlers as one fact + a clustering discriminator, fail-closed). Prerequisite (a) of the merge-freshness LIVE flip; model-only design today. → [ci-oom-reclassification.md](ci-oom-reclassification.md)"),
    ]),
    p(text: "Steps 1–2 use idle capacity and cut time with no new scheduler. Step 3 removes the contention; the matrix shards the rest. The 50 min is a *symptom of placement*."),
    hr(),
    h2(text: "9. The lane's real deliverable — shared abstractions"),
    p(text: "Beyond closing the gaps, the lane converges the forks CI exposes (pull each in **as CI flexes it**, §6):"),
    ul(items: [
      li(text: "**One Materialization kernel** — collapse sccache / resolve / ParseTable-memo / RecordedFixture / BuildBuddy onto `realize(subject)` (§2 P2)."),
      li(text: "**One Placement authority** — jobs (GitHub), threads (`spawn_width`), sessions (ctrl `plans.capacity`) are three forks of \"put work on a host.\""),
      li(text: "**One secrets/effects model** — BMC, tokens, sccache-auth modeled once."),
      li(text: "**gunbhub** — owning the Git/CI engine eventually closes the irreducible GitHub boundary (links 2, 13–14 / G6). *Not pressing*; the pain that denominates the lane is timeliness-for-confidence, not engine ownership."),
    ]),
    hr(),
    h2(text: "10. Open decisions (operator + bright-stag)"),
    ol(items: [
      li(text: "**Target ceiling** — \"~50 is the unacceptable status quo; get it to **~___**.\" Decides whether steps 1–2 suffice or step 3 is load-bearing this window."),
      li(text: "**Step 3 — build CI-run placement now, or fence it.** *Build* → the DEAD placement predicate (§4) gets its consumer. *Defer* → delete/fence it under one named runway. (Keep `operator_fleet` either way — it's the input to step 1.)"),
      li(text: "**Lane shape** — adopt §0's framing (CI as the substrate integration dogfood; deliverable = shared abstractions) as the §1 ROADMAP lane, with this doc as its charter."),
    ]),
    p(text: "Steps 1, 2, 4, 5 are sound under any answer; only step 3 waits on decision 2."),
  ]
}

data ci_process_end_to_end_plan: Plan = Plan {
  slug: "ci-process-end-to-end",
  title: "CI, end to end — what's on `.dag` today, what isn't, and the lane that closes the gap",
  body: ci_process_end_to_end_body(),
  dissolution: HasTrigger {
    text: "Delete this doc when §3-B (the fabric operation — placement, runner deployment, per-runner caps) is on `.dag` and drift-gated (steps 1 + 3 landed), the resolve cache is enabled in CI (step 2), and the rust tentpole no longer dominates a CI run — at which point the push→execute chain is on fabric except GitHub's irreducible engine, the §4 scheduling/caching gaps are closed by a real non-test consumer, and this charter's gap-map is a witnessed property rather than an audit."
  }
}
