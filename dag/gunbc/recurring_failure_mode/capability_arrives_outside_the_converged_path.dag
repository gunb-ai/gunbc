module gunbc.recurring_failure_mode.capability_arrives_outside_the_converged_path

import std.types { NonEmptyStr }
import gunbc.recurring_failure_mode { RecurringFailureMode }

data capability_arrives_outside_the_converged_path: RecurringFailureMode = RecurringFailureMode {
  identity: "capability_arrives_outside_the_converged_path" as NonEmptyStr,

  receipts: [
    "**a host holds a capability that no modeled path put there, so every later green on that host is uninterpretable.** INVALID STATE: a host answers ready for some capability while the convergence that is supposed to be the only way that capability arrives has not delivered it. The harm is not the capability -- it usually works -- it is that a receipt naming the host can no longer distinguish the modeled path working from a hand-run answering, and the ambiguity is permanent and travels with every future citation of that host. ",

    "DISTINGUISHING FACTS, against neighbours it is easy to file as. It is not `execution_provenance_loss`: there an observation may never have run, whereas here the observation runs and reports faithfully -- the host really does hold the capability. It is not `head_landed_by_hand_before_its_own_verification_reported`: that is an artifact landing uncertified, this is DURABLE HOST STATE whose origin is unrecorded. The tell is a convergence that reports `already satisfied` for a host it never satisfied. ",

    "THE STRUCTURAL CAUSE IS A CAPABILITY AUTHORIZED BY ONE MODULE, DISCLAIMED BY ANOTHER, AND EXECUTED BY NEITHER, which is DESIGN section 6's out-of-band actuation wearing convergence vocabulary. `gunbc.runner.runner_host_grants` emits EnsureSupplementaryGroupMembership and `runner_host_sudoers_content` renders permission to run its argv; `gunbc.runner_microvm_host_ready` explicitly disclaims applying it, naming the result 'a hole an operator falls into once per host, by hand'. A modeled capability with no executor is a manual step, and the roster row that grants it makes the manual step look converged. ",

    "SPECIMEN A, srv2 KVM (2026-09-05): an operator ran `sudo usermod -aG kvm ghrunner` by hand, the argv already rendered at provisioning/srv2/gunbc-ghrunner.sudoers. srv2 has read kvm=writable ever since while every sibling host reads present-not-writable. The module's own annotation recording the earlier measurement went stale at that moment and COULD NOT GO RED, because DESIGN section 4c says no Accepted program reads an annotation; it was caught only because someone asked whether the grant was converged. ",

    "SPECIMEN B, srv1 Firecracker (2026-09-06, this row's occasion): a session ran the modeled `runner_microvm_host_converge_wet` by hand on srv1, which installed Firecracker v1.16.1-aarch64 into the executor-owned root -- 30 paths under ~/.local/share/gunbc-firecracker, the root directory itself created by the run, so nothing pre-existed. It was digest-verified, idempotent, and spent no host grant, and it was STILL wrong: srv1 is the canary of record, and its capability would not have come from the host-image path that is supposed to own it. ",

    "SPECIMEN C, srv1 SUDOERS (2026-09-07, found while wiring the applier this row calls for). THE CLASS INVERTED, AND THE INVERSION IS WHAT MAKES IT DANGEROUS. Specimens A and B are a capability convergence should have delivered and did not. Here the modeled grant artifact is absent from the host and an untracked hand-maintained file stands in its place granting the CI job user a set of binaries with NO argument restriction, where the modeled artifact restricts each to an exact argv -- so convergence would have delivered a NARROWER capability than the one the host actually holds. The drift WIDENS a privilege boundary rather than merely obscuring a provenance, and because the grantee is the account that executes CI jobs, the gap is a live boundary and not a bookkeeping one. That is the same failure to converge as its siblings, pointing the other way. THE OBSERVATION ITSELF LIVES AT docs/plans/srv1-sudoers-drift-observation.md AND NOT HERE, so this roster carries the class and that file carries the evidence, each once: the grant shapes, the mtime and the earlier backup establishing which grants were added in the interval, the running-process supplementary-group fact, and the read-only steps to re-derive every one. Contents are described by SHAPE in both -- binary, run-as, argument restriction present or absent -- and deliberately never transcribed as a copyable authorization line, per the operator ruling on this finding. ",

    "THE DISCRIMINATOR, STATED BEFORE THE EVIDENCE EXISTS, because a green obtained after convergence is uninformative unless what would falsify it was named first. While an unrestricted grant stands, the applier own argv is admitted by the HOLE, so its success would prove the hole works rather than that convergence does -- instrument and subject sharing one path, which is the control-from-the-same-instrument defect. The separating observation is a NEAR-MISS NEGATIVE CONTROL: invoke the granted binary under a DIFFERENT and inert argv than the modeled one. An exact-argv grant refuses it AT SUDO, before the binary runs, so the refusal is attributable to the grant and not to the tool; an unrestricted grant admits it. Paired with the modeled argv succeeding, that is DESIGN section 4b(1) discriminating RED plus accepted positive control, and the PAIR rather than the success alone is what licenses reading the kvm grant as converged. ",

    "THE RULING, AND WHY DECLARING THE DEVIATION WAS REJECTED AS THE WEAKER ARM. The first recommendation was to keep the install and write it down. The operator overrode it: declaring makes the ambiguity legible but leaves every later srv1 receipt carrying a provenance caveat forever, whereas removing it makes the question stop existing. CONVERGENCE IS ONLY MEANINGFUL IF IT IS THE ONLY WAY STATE ARRIVES. Specimen B was reverted the same day -- the enumerated tree removed and nothing adjacent touched -- and the honest post-revert state is that srv1 cannot run a guest, which is what makes the convergence gap visible instead of papered over. ",

    "RUNG FOUND AT: BELOW THE FLOOR, which is not a rung -- DESIGN section 4b puts silent wrongness outside the ladder and forbids it outright, and that is where both specimens sit. Nothing executed distinguishes a converged capability from a hand-placed one: `runner_microvm_host_ready` observes WHETHER the host holds a capability and never WHAT PLACED IT, so a hand-run host and a converged host produce byte-identical standing. The srv2 specimen is the measured proof -- the writability persisted from 2026-09-05 through every subsequent standing read, the annotation recording the pre-hand-run measurement went stale at that instant and could not go red because no Accepted program reads one, and it surfaced only when a human asked whether the grant was converged. The srv1 specimen adds that the converge itself reports success without recording that its operator was a hand session. Provenance has no mechanism at all, not a weak one. ",

    "CEILING, DERIVED AND DIFFERENT FOR TWO HOST SHAPES, so a single number would be dishonest. On a host whose runtime root is reconstructed from a repository-built image at every boot, the ceiling is STRUCTURALLY IMPOSSIBLE: no persistent root survives to hold a capability the image did not place, so the invalid state has no constructor and the question stops being askable rather than being checked. On a MUTABLE persistent host the ceiling is MECHANICALLY PREVENTABLE and no higher, because an operator holding sudo can always place state -- the best reachable is an observation that refuses a capability it cannot attribute, which depends on that check executing and staying enrolled. Per DESIGN section 4b(1) a class's rung is the MINIMUM across its in-scope paths, so while any mutable host remains in the population the class's attainable ceiling is mechanically preventable, and the structural arm is available only to the hosts that stop being mutable. ",

    "NEXT-RUNG TRIGGER, A CAPABILITY AND NOT AN ARTIFACT, stated at the grain of the loss. The loss is plural -- every host, every capability -- so a trigger naming one host or one grant would be satisfied while the class stayed dead. FROM BELOW-THE-FLOOR TO MECHANICALLY PREVENTABLE: a host-standing observation that reports, for each capability, the identity of the converge run that placed it, such that a capability with no attributable placing run is a typed refusal rather than a ready standing. That is the whole capability; a receipt that merely RECORDS provenance without refusing on its absence does not reach it, because the srv2 specimen was already recorded in prose and still could not go red. FROM MECHANICALLY PREVENTABLE TO STRUCTURALLY IMPOSSIBLE, and only for hosts that reach it: a runtime root reconstructed from a repository-built image on every boot, so that no capability persists that the image did not place. Naming a specific host-image pull request would be naming an artifact; what discharges this row is the property that a rebooted host cannot carry unattributed capability, whichever change delivers it. ",

    "THE APPLIER IS BUILT AND UNREACHABLE, NOT MISSING, AND THE DISTINCTION DECIDES THE REPAIR. This row's first draft implied the executing half had to be written. It does not: `gunbc.executor_privileged_apply apply_executor_privileged_operations_wet` exists, elevates with `sudo -n` so an uncovered command REFUSES instead of hanging on a prompt no automated converge will answer, and renders its argv through the SAME `executor_privileged_operation_argv` the sudoers line is rendered from -- so what is permitted and what is executed are one fact by construction rather than by review. It has ZERO callers anywhere in the corpus, test modules included; the only other occurrence is a decl_ref citation. So DESIGN section 4b(2)'s three-way split lands on its strongest arm -- not `cannot climb`, not `can climb after one grounding`, but CAN CLIMB NOW WITH THE MECHANISM ALREADY WRITTEN -- and the remaining work is one wiring into the converge path. Recording this is load-bearing because the opposite reading sends the next author to build a second applier beside the first, which is precisely the argv fork that module's own annotation says it exists to prevent. ",

    "AND THE REASON IT HAS NO CALLER IS ITSELF A NAMED CLASS, WHICH IS WHY THE WIRING NEVER HAPPENED. The module was not deferred pending an ownership decision and it is not a half-finished design: #10628 introduced it WHOLE, expressly to close this gap -- that commit's own account is that `executor_privileged_operation_argv` had no production consumer, that `runner_host_privileged_operations` fed only the sudoers renderer, and that this module 'closes it, and executes THE SAME argv function' -- and then landed it without the caller it was written for. That is DESIGN section 3c exactly, down to the tell that section lists in its own words: consumption described in the PR body but absent from the diff. So the repair that was supposed to end this class was itself an instance of a neighbouring one, and the two facts are the same fact: 'one wiring remains' and 'a section 3c dangling consumer' describe the same missing edge, but only the second says WHY it is missing and therefore why waiting will not produce it. ",

    "REPAIR, IN PREFERENCE ORDER: bake the capability into a host image so the question cannot be asked, or close it in fleet converge. NOT an applier bolted beside the disclaiming module, and never the sudoers argv by hand -- that converts a modeled hole into a second undeclared deviation, which is how specimen A was produced. The reviewer's question for any host-capability claim: which run put it there, and can you name it. ",
  ],

  evidence: [],
}
