module gunbc.recurring_failure_mode.prose_asserts_an_enforcement_no_operation_can_produce

import std.types { NonEmptyStr }
import gunbc.recurring_failure_mode { RecurringFailureMode }

data prose_asserts_an_enforcement_no_operation_can_produce: RecurringFailureMode = RecurringFailureMode {
  identity: "prose_asserts_an_enforcement_no_operation_can_produce" as NonEmptyStr,

  receipts: [
    "**an annotation states a refusal the model has no operation capable of returning, over a field that is recorded and never read** (INVALID STATE: a carrier records a discriminating value, prose beside it describes what that value prevents, and no function anywhere ACCEPTS the value and can return a different answer because of it. The declaration typechecks, the witnesses pass, and the sentence is not a lie about intent -- it is a description of a wall that was never built. HARM: the prose is read as the guarantee, because a reader checking whether a class is handled finds a paragraph saying it is. The field's presence corroborates the paragraph and the paragraph explains the field, so each is the other's evidence and neither touches an executed path. ",

    "**THE RECOGNITION RULE IS ABOUT THE QUESTION, NOT THE ANSWER: name the operation that TAKES this value and can return two different results because of it. If no such operation exists, the field is inert and the sentence beside it is the only wall.** The tell is a paragraph in the indicative about what a consumer 'gets' or 'cannot' do, sitting above a record whose field appears exactly three times -- type declaration, constructor parameter, constructor body -- and nowhere else. That triple is mechanically greppable and is the cheapest instrument for the class. ",

    "**WHY IT SURVIVES REVIEW, WHICH IS THE PART THAT MAKES IT RECUR: THE DISCRIMINATING RED IS UNAUTHORABLE.** A reviewer looking for the missing negative case cannot write one, because there is no call to make -- the wrong input has no operation to be wrong to. So the class produces no red, and review of a diff that contains it correctly reports no failing check. Measured: four instances passed three independent review rounds and two side-chat rulings on gunbc#10264 and gunbc#10249 before any was found, and the fourth was found only after a peer session described the shape from an unrelated lane. ",

    "**BOUNDARY AGAINST check_subject_shape_cannot_represent_the_state_the_check_detects, which is the nearest neighbour and a DIFFERENT defect with a different remedy.** There a check EXISTS, executes, and is enrolled, and its red is unauthorable because the subject representation has no constructor for the state it matches -- the repair is to change the subject's shape. Here NO CHECK EXISTS AT ALL: the red is unauthorable because there is no operation to call, and the repair is to add the question so the recorded value is examined. Both are permanently green and both read as coverage, which is why they are easy to conflate; a scope edit fixes neither, and a subject-shape edit fixes only the first. ",

    "**BOUNDARY AGAINST specification-without-execution (DESIGN section 5), which is the general case rather than this class.** There a specification has no consumer. Here there IS a consumer, it executes on every run, and it is green -- what is missing is the interrogation of one field it already carries. The general rule 'find a real consumer green by execution' is satisfied by the defect, which is exactly why it does not catch it. ",

    "**FOUR RECEIPTS ON ONE LANE, gunbc#10264 and gunbc#10249, 2026-09-03.** (1) `tensor_parallel_rank` was recorded on every rank participant and examined by nothing, so a population claiming both workers were tensor-parallel rank 0 decided COHERENT under a law named after tensor parallelism. (2) `vllm_host_vantage(host:)` took a host name and returned a vantage, and the module annotation asserted this made acquired provenance cost 'a vantage per host rather than a keyword' -- it cost one wrapper call per host, and the annotation asserting the wall was itself the inflation. (3) `EvidenceRouteResolvedConfiguration { capture }` carried three arbitrary strings from a public constructor, so `\"garbage\"`, `\"0\"` and `\"anything\"` advanced a refusal stage because the ARM NAME said the configuration had been resolved. (4) `VllmHealthCoverageFact.backend` was written once and read by nothing, beneath an annotation promising that a consumer asking about another backend 'gets no answer here rather than a borrowed one' -- the only reachable path was the Ray row, so a multiprocessing deployment would have read Ray's answer off it. ",

    "**A FIFTH FROM AN INDEPENDENT LANE ON UNRELATED SUBJECT MATTER, which is what makes the class structural rather than one author's habit:** a peer session reported the same shape in gunbc#10254 as 'an annotation standing where a field should have been', alongside an `OobeBrowserSessionReady` success surface that whole-tree resolution showed had NO construction anywhere -- an advertised entry point that could not reach the state it advertised. That lane recorded the same reason for its persistence: an unproducible carrier reads as a finished model, so it never ranks for inspection, while a dead import reads as debris and gets deleted. ",

    "**THE CEILING IS STRUCTURAL AND THE TRIGGER IS A SWEEP, NOT ATTENTION.** Both lanes found instances only after independently repairing three or four smaller ones each, and both kept missing the next at the top of the type graph. The next rung is a lens over the Node tree asking, for every declared field of every record, whether any function in the closure destructures it -- the same read the namespace authority already performs, so the analysis costs no substrate edit. Until that lens exists the class is mitigatable by review diligence, which both lanes have now measured to be insufficient. ",
  ],

  evidence: [],
}
