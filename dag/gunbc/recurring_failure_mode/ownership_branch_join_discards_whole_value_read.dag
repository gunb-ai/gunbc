module gunbc.recurring_failure_mode.ownership_branch_join_discards_whole_value_read

import std.types { NonEmptyStr }
import gunbc.recurring_failure_mode { RecurringFailureMode }

// Consumer: gunbc.design_ledgers renders this class through directory-derived row membership.
data ownership_branch_join_discards_whole_value_read: RecurringFailureMode = RecurringFailureMode {
  identity: "ownership_branch_join_discards_whole_value_read" as NonEmptyStr,
  receipts: [
    "INVALID STATE: ownership.max_usage_by_fan_out selects a branch usage list with more projections and discards another branch's whole-value Read. build_movable_set then admits a move while another use still needs the value.",
    "HARM AND FOUND RUNG: emit_var_ref emits a move into a constructor before a subsequent projection; rustc refuses E0382. The defect is loudly mitigated by the target borrow check, not silently accepted.",
    "REPAIR: max_usage_by_fan_out retains missing Read evidence from either binding usage, deduplicating shared-prefix reads and keeping Projected distinct. The emitter carries no new rule. Corpus-grain prevention is mechanical through the required regen-fixed-point phase, which re-derives the committed stage0 mirrors and refuses divergence.",
    "FIXTURE EVIDENCE: ownership_branch_read Rust integration tests call the emitted ownership mirror with the same three BindingUsage fixtures and compile_sources on the authored move probe. cargo test -p v1-compiler --test ownership_branch_read executes them directly. Required clippy --all-targets compiles them, but no CI step runs them; gunbc.rung_drop.rust_unit_tests_off_the_merge_path declares that execution drop.",
    "NEXT-RUNG HARNESS TRIGGER: a required phase in an existing lane that executes seed-rooted claims and writes same-identity, candidate-bound terminals into the disposition receipt. The fixture controls return to .dag when that capability lands. The missing harness is the next-rung trigger, not a permanent ceiling. Work item node://adhoc-babdd2de-8db, owned by eager-raven-113, carries this model-first harness in an existing lane and dissolves with v1 deletion. Witness roots remain dag and src/v2; adding the compiler root for one fixture would widen the floor's subject.",
    "COST RESIDUE: Read is non-tail residue in the existing ownership model, so both constructor orders conservatively clone. This is bounded cost, not a correctness gap. Its separate next-rung capability is per-path last-use evidence at the ownership fold.",
  ],
  evidence: [],
}
