module gunbc.recurring_failure_mode.base_readability_gate_refuses_a_grammar_change

import std.types { NonEmptyStr }
import gunbc.recurring_failure_mode { RecurringFailureMode }
import std.decl_ref { DeclarationRef, WholeDeclaration }

data base_readability_gate_refuses_a_grammar_change: RecurringFailureMode = RecurringFailureMode {
  identity: "base_readability_gate_refuses_a_grammar_change" as NonEmptyStr,

  receipts: [
    "**a gate that reads the base revision with the HEAD compiler refuses every change to the GRAMMAR ITSELF.** INVALID STATE: an adjudicator compares base-side and head-side declarations, parses both with one compiler, and has no representable arm for `the base is well-formed under its OWN grammar and unreadable only under the head's`. Removing a keyword makes every file in the diff unreadable at base at once, so the gate refuses in proportion to how thoroughly the change succeeded.",

    "SEPARATE FROM ITS NEAREST NEIGHBOUR, and the distinction is the whole reason for a second row. fail_closed_gate_refuses_its_own_repair covers a base that is DEFECTIVE, where the head is the repair; the shared symptom is NotEvaluated and the shared disposition is an exceptional landing. Here the base is HEALTHY -- main parses clean under main's own compiler and every other lane sees nothing wrong -- and only the head's changed grammar makes it unreadable. Filing them as one row would state `restores base health` as the recognition rule, which is false of this instance and would send a reader looking for a defect on main that does not exist.",

    "MEASURED SPECIMEN (2026-09-08, gunbc#10850, the `func` keyword cut). namespace-wave-admission reported `NotEvaluated -- dag/extdeps/access/posix_effective_principal_read_op.dag does not parse at the base revision (5 diagnostic(s)), so its base-side declarations cannot be read`, BLOCKING, with every other required signal green: build lane PASS, floor planned=3550 executed=3550 claims_failed=0, .dag parse sweep 0 errors on the merged tree, regen first_generation_equal=true, and both arms of the new wall's enrolled witness passing. The 5 diagnostics are one refused declaration plus its orphaned annotations, and the same shape holds for each of the 140+ changed files.",

    "DISTINGUISHING FACTS: nothing widened, defaulted or fabricated -- the gate preserved ignorance as NotEvaluated exactly as fail-closed requires, so this is NOT absorbing_fallback. The defect is transition reachability across a grammar boundary, not verdict truth. Recognition rule: a change that edits the LANGUAGE the corpus is written in, where the gate's precondition is that base and head share that language. The precondition is unstated, which is why it is discovered by a red rather than by reading the gate.",

    "SELF-CLEARING, WHICH BOUNDS THE HARM AND IS ALSO THE TRAP: once the change lands, every later pull request has a post-cut base that parses under the head grammar and the phase evaluates normally. So the refusal is one-time per grammar change and cannot be waited out -- holding the change holds the refusal, and only landing clears it.",

    "THE TEMPTING REPAIR IS THE ONE DESIGN FORBIDS: keeping the removed spelling as a transitional parse-only alias satisfies the gate, and is exactly the staged migration section 3 delete-first rejects (a second authority preserved to buy a migration window that nothing consumes). A gate should not be able to make a language-design decision by attrition; that it currently can is the finding.",

    "RUNG FOUND AT: mitigatable by operator inspection and exceptional landing, same disposition as the neighbour row and reached for a different reason. CEILING: mechanically preventable -- the gate can read the base revision with the BASE revision's own compiler, which is the same seed-versus-head discipline the bootstrap already observes elsewhere in this repository, rather than assuming one grammar across the diff. NEXT-RUNG TRIGGER: base-side declaration reading bound to the compiler of the revision being read, so a grammar change is adjudicable instead of unevaluable.",

    "DISPOSITION (2026-09-09, operator ruling, REVERSING an earlier one recorded here and now corrected rather than softened): THE PHASE IS RESTORED, NOT DELETED. An earlier revision of this row recorded that the subject phase had been deleted whole and that the guarantee it supplied was declared as a rung drop. That disposition was withdrawn. DESIGN section 3 admits exactly this case and the first reading of it was wrong: the delete-first root is the obsolete `func` DECLARATION FORM, and this wall is not another spelling of that fact -- it asks an independent safety question, whether subject membership or occurrence binding changed without adjudication. Its refusal WAS the deletion census doing its job, and section 3 says to fix a surfaced dependent FORWARD from first principles, not to delete the question it was enforcing. Two clauses of that same passage decide it: a GAP-INTOLERANT BOUNDARY -- which a required merge wall is -- keeps the STAGED form, Y built in shadow and then one transition that switches the root and deletes X together; and the minimum Y must PRESERVE EVERY REQUIRED REFUSAL, or it has erased a correctness distinction rather than completed the replacement. Deleting with no Y at all did precisely that. The earlier argument recorded here -- that freezing preserves the refusal and so the carve-out cannot apply -- answered the FROZEN carve-out while the applicable one is the STAGED carve-out, where preserving the refusal is the REASON the wall holds the cut rather than an objection to it.",

    "SO THIS ROW IS NOW A PREREQUISITE RATHER THAN A POSTMORTEM, and it travels with the repair instead of being resolved by removing its only specimen. The class is unchanged and the NEXT-RUNG TRIGGER above is unchanged; what changed is that the trigger is now scheduled work rather than a condition nobody owns. After the repair lands this row STAYS ENROLLED as regression evidence -- DESIGN section 4b(4) keeps a class's discriminating RED after the climb, and an expecting-red probe that greens when its wall lands flips to a permanent regression control rather than retiring. The measured specimen above is what that control must reproduce: a grammar change whose every changed file is well-formed under its own revision and unreadable under the other's.",

    "CLIMB (2026-09-13, gunbc#10970): the trigger named above has fired. The wave phase now reads the base under the base revision's own ParseEnvironment -- v1_compiler.namespace_wave_admission run_wave_admission_between asks environment_agreement whether the declaring closure's blobs differ between base and head, loads the base's environment through load_parse_environment_at when they do, abandons the changed-file baseline reconstruction (an untouched FILE has an untouched PARSE only while the grammars agree) and re-reads every base-side file in sweep scope under the base's grammar. The enrolled evidence is the grammar-differs witness, src/v1/stage0/tests/wave_admission_grammar_differs.rs: a scratch base whose grammar admits an extra item-form keyword and a probe declared with it, a head that cuts the keyword and rewrites the probe -- the measured specimen in miniature. Its RED is the direct pair on the parse the partition performs: base_records of the base probe REFUSES under the head environment with this row's own text and READS under the base's. Its GREEN drives the production adjudication over the pair and requires Adjudicated. Both are executed, and the class is at rung 2 on this path: mechanically preventable, the invalid state still writable, safety depending on the witness staying enrolled. ",

    "WHAT THE RED FOUND BEFORE IT WAS GREEN, recorded because it is the class one level down and the reason the witness earns its place. The loader's first shape materialized the base's WHOLE dag tree and handed it to the module index -- which parses every file it is shown under THIS binary's grammar -- so a base file written in the base's grammar refused inside the loader on the way to learning the base's grammar. The witness's first run reported exactly that: `module index refused: 1 unparseable .dag source(s)` against the probe, from the loader's own materialization directory. The provenance probe alone could never have seen it, because its diverged file was the environment declaration (a different keyword STRING, still parseable), not a file USING the diverged grammar. The repair is bounded materialization: only the environment's declaring closure is shown to the index. ",

    "THE BOUNDARY THAT DRAWS, stated so it is not later read as a ceiling: the loader reads a base whose grammar differs from the head's so long as the base's ENVIRONMENT CLOSURE is itself readable under the head's grammar. A change that alters the grammar AND uses the altered grammar inside std.syntax's own closure is outside any head-built loader's reach -- the bootstrap boundary the compiler itself has -- and refuses as ClosureNotEvaluable rather than answering under the wrong rules. The measured specimen sits inside the boundary: none of the thirteen closure files at the base use the cut spelling, which is why gunbc#10850 is adjudicable by this repair. NEXT-RUNG TRIGGER for the residual: the wave phase itself in .dag, where a revision-relative read is a fold over a ParseEnvironment value and there is no head-built index in the path.",
  ],

  evidence: [
    DeclarationRef { module_path: "v1_compiler.namespace_wave_admission", decl_name: "run_wave_admission_between", field: WholeDeclaration },
    DeclarationRef { module_path: "v1_compiler.namespace_wave_admission", decl_name: "environment_agreement", field: WholeDeclaration },
    DeclarationRef { module_path: "v1_compiler.namespace_wave_admission", decl_name: "materialize_environment_closure_at", field: WholeDeclaration },
  ],
}
