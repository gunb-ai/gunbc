module test.fixture.data_reference_identity_shared

// The POSITIVE CONTROL's declaration. One data item, one module, referenced from
// somewhere else -- so a repair that narrowed data references to the referring
// module would break here rather than in the collision arms.
data dri_shared_endpoint: String = "dri-shared-endpoint"
