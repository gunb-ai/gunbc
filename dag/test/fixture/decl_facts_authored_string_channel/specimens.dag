module test.fixture.decl_facts_authored_string_channel.specimens

import std.types { String }

// Tiny pool for newly enrolled authored-string witnesses. The reflection
// support pool is too large to first-touch on a newly enrolled identity (run
// 34690964764: 520ms marginal vs 38ms for sibling contains-walks on a warm memo).
data planted_constructor_spelling_string_specimen: String = "SingleAuthority"

data planted_concat_string_specimen: String = concat("concat-left", "concat-right")
