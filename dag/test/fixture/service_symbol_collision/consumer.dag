module test.fixture.service_symbol_collision.consumer

import test.fixture.service_symbol_collision.probe
import std.types { String }

fn probe_both(tag: String) -> String {
  concat(
    collision.Probe.PingDotted(tag: tag).line,
    CollisionProbe.PingFlat(tag: tag).line
  )
}
