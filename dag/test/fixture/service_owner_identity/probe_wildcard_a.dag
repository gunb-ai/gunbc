module test.fixture.service_owner_identity.probe_wildcard_a

import std.types { String }
import test.fixture.service_owner_identity.service

// A REAL MODULE, not a synthetic census source: importing ONE of the two same-named
// OwnerIdentityProbe services -- by name or by wildcard -- compiles clean through the ordinary
// module path. Any import line closes the bare-reference closure for this file
// (cli_run source_declares_import_lines), so which declaration the name means is decided only by
// v1.compiler.infer over the typed candidate set. The refusing forms (both imported) are not
// writable as corpus files; they are pinned by test.claim.service_owner_identity_witness.
fn ping() -> String {
  let r = OwnerIdentityProbe.Ping()
  r.ok
}
