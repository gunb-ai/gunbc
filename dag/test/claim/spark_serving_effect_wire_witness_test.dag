module test.claim.spark_serving_effect_wire_witness


data live_tree_disposition: v2.std.live_tree.LiveTreeDisposition = v2.std.live_tree.SubstrateInputsOnly

// EVERY VARIANT REACHES ITS OWN ARM. Removing the wildcard is only half the fix; the other half is
// that the twelve bare names really are variant PATTERNS. A bare lowercase-or-not name in pattern
// position could bind instead of match, and a binder in the first arm would swallow every later
// effect -- reinstating the wildcard invisibly and making the exhaustiveness look real while one
// arm answered for all of them. This distinguishes those two worlds by execution: under a binder
// every call below returns the first non-linger arm's string.
test fn w_each_serving_effect_wires_to_its_own_name() -> Bool {
  gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.EnableLingerForOwnAccount) == "EnableLingerForOwnAccount"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.WriteSystemUnitFile) == "WriteSystemUnitFile"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.ReloadSystemManager) == "ReloadSystemManager"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.EnableSystemUnit) == "EnableSystemUnit"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.WriteUserUnitFile) == "WriteUserUnitFile"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.ReloadUserManager) == "ReloadUserManager"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.EnableUserUnit) == "EnableUserUnit"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.InstallRuntimePackage) == "InstallRuntimePackage"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.InstallOwnedRuntimeClosure) == "InstallOwnedRuntimeClosure"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.MaterializeModelArtifact) == "MaterializeModelArtifact"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.GrantContainerRuntimeAccess) == "GrantContainerRuntimeAccess"
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.RunContainerAsOwnUser) == "RunContainerAsOwnUser"
}

// AND THE NAMES ARE DISTINCT, so the assertion above cannot be satisfied by a fold that returns one
// constant. Twelve equalities against twelve different literals is the discriminating shape; this
// pins that no two arms collapsed onto the same wire label.
test fn w_no_two_effects_share_a_wire_label() -> Bool {
  gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.WriteSystemUnitFile)
    != gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.WriteUserUnitFile)
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.ReloadSystemManager)
      != gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.ReloadUserManager)
    && gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.EnableSystemUnit)
      != gunbc.fleet_converge_plan.fleet_converge_spark_serving_effect_wire(effect: gunbc.spark.serving_realization.EnableUserUnit)
}
