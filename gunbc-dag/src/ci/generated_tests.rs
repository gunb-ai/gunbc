// Generated tests for ci_generated_tests DAG.
// 
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 128 obligations (56 discharged, 72 testable: A=25, B=30, C=15, D=2)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: 0d15c241978c034d0667c116e5d90e1acb362ca2ce9fa0218ecd59098b3d8a9a


use gunbc_exec::{execute_with_mode, lower, BoundaryMocks, ExecutionMode};
use gunbc_ir::{detect_boundaries, Cardinality, Value};
use gunbc_test::{assert_boundary_mockable, assert_types_compatible, MockSpec};
use gunbc_test::{apply_window_inputs, assert_window_outputs, window_subdag, Window};
use gunbc_test::{ResourceAcquireResult, ResourceSimulation};

fn mock_spec() -> MockSpec {
    crate::ci::graph_mock::ci_mock_spec()
}

// =========================================================================
// Signature Validation
// =========================================================================

/// Declared signature matches the DAG inputs/outputs.
#[test]
fn test_signature_matches_dag() {
    let dag = crate::build_ci_graph().unwrap();
    let sig = crate::ci_signature();
    sig.validate(&dag).expect("signature should match DAG");
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
// 
// Determinism obligations: 17 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'prepare_deps_exists': same inputs → same outputs
// - 'parse_deps_exists': same inputs → same outputs
// - 'prepare_codegen_exists': same inputs → same outputs
// - 'parse_codegen_exists': same inputs → same outputs
// - 'prepare_codegen_cmd': same inputs → same outputs
// - 'parse_codegen_result': same inputs → same outputs
// - 'prepare_testgen': same inputs → same outputs
// - 'parse_testgen': same inputs → same outputs
// - 'prepare_build': same inputs → same outputs
// - 'parse_build': same inputs → same outputs
// - 'prepare_test': same inputs → same outputs
// - 'parse_test': same inputs → same outputs
// - 'prepare_clippy_lint': same inputs → same outputs
// - 'parse_clippy_lint': same inputs → same outputs
// - 'prepare_guardrail_check': same inputs → same outputs
// - 'parse_guardrail_check': same inputs → same outputs
// - 'report': same inputs → same outputs

/// DryRun execution completes without crash.
/// 
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    let dag = crate::build_ci_graph().unwrap();
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("DryRun execution should complete without crash");
    assert!(!log.entries.is_empty(), "execution should produce log entries");
}

/// All transport executors are intercepted in DryRun.
/// 
/// Proves: every transport executor is interceptable; DryRun won't
/// accidentally perform real I/O.
#[test]
fn test_transport_interception() {
    let dag = crate::build_ci_graph().unwrap();
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(result.is_ok(), "All transports should be interceptable: {:?}", result.error);
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_deps_exists"), "transport executor 'execute_deps_exists' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_codegen_exists"), "transport executor 'execute_codegen_exists' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_codegen"), "transport executor 'execute_codegen' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_testgen"), "transport executor 'execute_testgen' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_build"), "transport executor 'execute_build' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_test"), "transport executor 'execute_test' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_guardrail_check"), "transport executor 'execute_guardrail_check' should be in intercepted list");
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 23 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'execute_deps_exists': valid inputs → valid outputs
// - 'parse_deps_exists': valid inputs → valid outputs
// - 'execute_codegen_exists': valid inputs → valid outputs
// - 'parse_codegen_exists': valid inputs → valid outputs
// - 'prepare_codegen_cmd': valid inputs → valid outputs
// - 'execute_codegen': valid inputs → valid outputs
// - 'parse_codegen_result': valid inputs → valid outputs
// - 'prepare_testgen': valid inputs → valid outputs
// - 'execute_testgen': valid inputs → valid outputs
// - 'parse_testgen': valid inputs → valid outputs
// - 'prepare_build': valid inputs → valid outputs
// - 'execute_build': valid inputs → valid outputs
// - 'parse_build': valid inputs → valid outputs
// - 'prepare_test': valid inputs → valid outputs
// - 'execute_test': valid inputs → valid outputs
// - 'parse_test': valid inputs → valid outputs
// - 'prepare_clippy_lint': valid inputs → valid outputs
// - 'clippy_lint': valid inputs → valid outputs
// - 'parse_clippy_lint': valid inputs → valid outputs
// - 'prepare_guardrail_check': valid inputs → valid outputs
// - 'execute_guardrail_check': valid inputs → valid outputs
// - 'parse_guardrail_check': valid inputs → valid outputs
// - 'report': valid inputs → valid outputs

/// Cardinality coverage: parse_codegen_exists.prep_success with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port parse_codegen_exists.prep_success.
#[test]
fn test_cardinality_parse_codegen_exists_prep_success_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "prep_success", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: parse_codegen_exists.prep_success with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port parse_codegen_exists.prep_success.
#[test]
fn test_cardinality_parse_codegen_exists_prep_success_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "prep_success", Value::Bool(true));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: parse_codegen_exists.codegen_ran with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port parse_codegen_exists.codegen_ran.
#[test]
fn test_cardinality_parse_codegen_exists_codegen_ran_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "codegen_ran", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: parse_codegen_exists.codegen_ran with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port parse_codegen_exists.codegen_ran.
#[test]
fn test_cardinality_parse_codegen_exists_codegen_ran_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "codegen_ran", Value::Bool(true));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: parse_codegen_exists.prep_message with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port parse_codegen_exists.prep_message.
#[test]
fn test_cardinality_parse_codegen_exists_prep_message_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "prep_message", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: parse_codegen_exists.prep_message with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port parse_codegen_exists.prep_message.
#[test]
fn test_cardinality_parse_codegen_exists_prep_message_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("parse_codegen_exists", "prep_message", Value::Str("<MOCK>".to_string()));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: execute_testgen.skip_reason with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port execute_testgen.skip_reason.
#[test]
fn test_cardinality_execute_testgen_skip_reason_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_testgen", "skip_reason", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: execute_testgen.skip_reason with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port execute_testgen.skip_reason.
#[test]
fn test_cardinality_execute_testgen_skip_reason_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_testgen", "skip_reason", Value::Str("<MOCK>".to_string()));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: execute_build.skip_reason with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port execute_build.skip_reason.
#[test]
fn test_cardinality_execute_build_skip_reason_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_build", "skip_reason", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: execute_build.skip_reason with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port execute_build.skip_reason.
#[test]
fn test_cardinality_execute_build_skip_reason_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_build", "skip_reason", Value::Str("<MOCK>".to_string()));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: execute_test.skip_reason with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port execute_test.skip_reason.
#[test]
fn test_cardinality_execute_test_skip_reason_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_test", "skip_reason", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: execute_test.skip_reason with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port execute_test.skip_reason.
#[test]
fn test_cardinality_execute_test_skip_reason_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_test", "skip_reason", Value::Str("<MOCK>".to_string()));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: execute_guardrail_check.skip_reason with 0 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port execute_guardrail_check.skip_reason.
#[test]
fn test_cardinality_execute_guardrail_check_skip_reason_empty_0() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_guardrail_check", "skip_reason", Value::Unit);
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: execute_guardrail_check.skip_reason with 1 element(s) (cardinality: 0..1).
/// 
/// Proves: DAG handles count=1 (one) for boundary port execute_guardrail_check.skip_reason.
#[test]
fn test_cardinality_execute_guardrail_check_skip_reason_one_1() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_guardrail_check", "skip_reason", Value::Str("<MOCK>".to_string()));
    let _log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("cardinality count=1 (one) should not crash");
}

// =========================================================================
// Bucket C: Scenario Coverage
// =========================================================================

// N+1 scenarios: one success + one per-transport failure + guard toggles.
// 7 single-failure scenarios (one per transport executor).
// Full failure scenarios require per-transport failure mocks (Tier 0 infra).
// 

/// Happy path: all transports succeed.
/// 
/// Proves: workflow reaches terminal outputs with all transports mocked as success.
#[test]
fn test_scenario_all_succeed() {
    let dag = crate::build_ci_graph().unwrap();
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("all-succeed scenario should complete");
    let entry = log.get("execute_deps_exists").expect("'execute_deps_exists' should be in log");
    assert!(entry.was_intercepted, "'execute_deps_exists' should be intercepted in DryRun");
    let entry = log.get("execute_codegen_exists").expect("'execute_codegen_exists' should be in log");
    assert!(entry.was_intercepted, "'execute_codegen_exists' should be intercepted in DryRun");
    let entry = log.get("execute_codegen").expect("'execute_codegen' should be in log");
    assert!(entry.was_intercepted, "'execute_codegen' should be intercepted in DryRun");
    let entry = log.get("execute_testgen").expect("'execute_testgen' should be in log");
    assert!(entry.was_intercepted, "'execute_testgen' should be intercepted in DryRun");
    let entry = log.get("execute_build").expect("'execute_build' should be in log");
    assert!(entry.was_intercepted, "'execute_build' should be intercepted in DryRun");
    let entry = log.get("execute_test").expect("'execute_test' should be in log");
    assert!(entry.was_intercepted, "'execute_test' should be intercepted in DryRun");
    let entry = log.get("execute_guardrail_check").expect("'execute_guardrail_check' should be in log");
    assert!(entry.was_intercepted, "'execute_guardrail_check' should be intercepted in DryRun");
}

/// Single failure: 'execute_deps_exists' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_deps_exists_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_deps_exists'
    mocks.set_value("execute_deps_exists", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_codegen_exists' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_codegen_exists_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_codegen_exists'
    mocks.set_value("execute_codegen_exists", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_codegen' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_codegen_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_codegen'
    mocks.set_value("execute_codegen", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_testgen' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_testgen_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_testgen'
    mocks.set_value("execute_testgen", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_build' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_build_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_build'
    mocks.set_value("execute_build", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_test' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_test_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_test'
    mocks.set_value("execute_test", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Single failure: 'execute_guardrail_check' transport fails.
/// 
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_guardrail_check_fails() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_guardrail_check'
    mocks.set_value("execute_guardrail_check", "response", Value::Str("<TRANSPORT_FAILURE>".to_string()));
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_with_mode(&dag, ExecutionMode::DryRun(mocks));
}

/// Skip propagation: 'execute_deps_exists' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_deps_exists() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_deps_exists", "response", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_deps_exists").is_some(), "downstream 'parse_deps_exists' should still appear in log");
}

/// Skip propagation: 'execute_codegen_exists' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_codegen_exists() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_codegen_exists", "response", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_codegen_exists").is_some(), "downstream 'parse_codegen_exists' should still appear in log");
}

/// Skip propagation: 'execute_codegen' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_codegen() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_codegen", "response", Value::Skipped);
    mocks.set_value("execute_codegen", "skip", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_codegen_result").is_some(), "downstream 'parse_codegen_result' should still appear in log");
    assert!(log.get("parse_codegen_result").is_some(), "downstream 'parse_codegen_result' should still appear in log");
}

/// Skip propagation: 'execute_testgen' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_testgen", "response", Value::Skipped);
    mocks.set_value("execute_testgen", "skip", Value::Skipped);
    mocks.set_value("execute_testgen", "skip_reason", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_testgen").is_some(), "downstream 'parse_testgen' should still appear in log");
    assert!(log.get("parse_testgen").is_some(), "downstream 'parse_testgen' should still appear in log");
}

/// Skip propagation: 'execute_build' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_build() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_build", "response", Value::Skipped);
    mocks.set_value("execute_build", "skip", Value::Skipped);
    mocks.set_value("execute_build", "skip_reason", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_build").is_some(), "downstream 'parse_build' should still appear in log");
    assert!(log.get("parse_build").is_some(), "downstream 'parse_build' should still appear in log");
}

/// Skip propagation: 'execute_test' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_test() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_test", "response", Value::Skipped);
    mocks.set_value("execute_test", "skip", Value::Skipped);
    mocks.set_value("execute_test", "skip_reason", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_test").is_some(), "downstream 'parse_test' should still appear in log");
    assert!(log.get("parse_test").is_some(), "downstream 'parse_test' should still appear in log");
}

/// Skip propagation: 'execute_guardrail_check' returns Skipped → downstream handles it.
/// 
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_guardrail_check() {
    let dag = crate::build_ci_graph().unwrap();
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("execute_guardrail_check", "response", Value::Skipped);
    mocks.set_value("execute_guardrail_check", "skip", Value::Skipped);
    mocks.set_value("execute_guardrail_check", "skip_reason", Value::Skipped);
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("skip propagation should not crash or hang");
    assert!(log.get("parse_guardrail_check").is_some(), "downstream 'parse_guardrail_check' should still appear in log");
    assert!(log.get("parse_guardrail_check").is_some(), "downstream 'parse_guardrail_check' should still appear in log");
}

// =========================================================================
// Bucket D: Resource Hygiene
// =========================================================================

// Structural resource/tool wiring correctness + simulation tests.
// Resource simulation tests (MockSpec-based)

/// Test resource 'cargo:build' (Lock) acquisition.
#[test]
fn test_resource_cargo_build_acquire() {
    let spec = mock_spec();
    let resource = spec.get_resource("cargo:build").expect("resource should exist");
    let result = resource.acquire();
    assert!(matches!(result, ResourceAcquireResult::Acquired), "should acquire successfully");
}

/// Test resource 'cargo:test' (Lock) acquisition.
#[test]
fn test_resource_cargo_test_acquire() {
    let spec = mock_spec();
    let resource = spec.get_resource("cargo:test").expect("resource should exist");
    let result = resource.acquire();
    assert!(matches!(result, ResourceAcquireResult::Acquired), "should acquire successfully");
}

/// Test resource 'cargo:clippy' (Lock) acquisition.
#[test]
fn test_resource_cargo_clippy_acquire() {
    let spec = mock_spec();
    let resource = spec.get_resource("cargo:clippy").expect("resource should exist");
    let result = resource.acquire();
    assert!(matches!(result, ResourceAcquireResult::Acquired), "should acquire successfully");
}

// =========================================================================
// Flow Verification Tests
// =========================================================================

// These tests execute the full DAG in DryRun mode with mocked transport
// responses, verifying that pure node logic produces expected outputs.

/// Flow verification: ci scenario.
/// 
/// Builds the DAG, injects mocked transport responses via DryRun,
/// and verifies that the pure node chain produces expected terminal outputs.
#[test]
fn test_flow_ci() {
    let dag = crate::build_ci_graph().unwrap();
    let spec = mock_spec();
    let mocks = spec.to_boundary_mocks();
    let log = execute_with_mode(&dag, ExecutionMode::DryRun(mocks)).expect("DryRun execution should succeed");

    // Verify report.overall_success
    let entry = log.get("report").expect("node 'report' should be in execution log");
    assert_eq!(entry.outputs.get("overall_success").expect("port 'overall_success' should exist on 'report'"), &Value::Bool(true), "flow verification: report.overall_success mismatch");

}

// =========================================================================
// Windowed Segment Tests
// =========================================================================

// These tests execute contiguous windows of the DAG using baseline DryRun
// values as injected inputs, then verify window exit outputs match baseline.

/// Window: prepare_codegen_cmd -> execute_codegen
#[test]
fn test_window_prepare_codegen_cmd_through_execute_codegen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_codegen_cmd", "execute_codegen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_codegen -> parse_codegen_result
#[test]
fn test_window_execute_codegen_through_parse_codegen_result() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_codegen", "parse_codegen_result"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_codegen_result -> prepare_testgen
#[test]
fn test_window_parse_codegen_result_through_prepare_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_codegen_result", "prepare_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_testgen -> execute_testgen
#[test]
fn test_window_prepare_testgen_through_execute_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_testgen", "execute_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_testgen -> parse_testgen
#[test]
fn test_window_execute_testgen_through_parse_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_testgen", "parse_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_testgen -> prepare_build
#[test]
fn test_window_parse_testgen_through_prepare_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_testgen", "prepare_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_test -> report
#[test]
fn test_window_parse_test_through_report() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_test", "report"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_codegen_cmd -> parse_codegen_result
#[test]
fn test_window_prepare_codegen_cmd_through_parse_codegen_result() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_codegen_cmd", "execute_codegen", "parse_codegen_result"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_codegen -> prepare_testgen
#[test]
fn test_window_execute_codegen_through_prepare_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_codegen", "parse_codegen_result", "prepare_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_codegen_result -> execute_testgen
#[test]
fn test_window_parse_codegen_result_through_execute_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_codegen_result", "prepare_testgen", "execute_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_testgen -> parse_testgen
#[test]
fn test_window_prepare_testgen_through_parse_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_testgen", "execute_testgen", "parse_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_testgen -> prepare_build
#[test]
fn test_window_execute_testgen_through_prepare_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_testgen", "parse_testgen", "prepare_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_testgen -> prepare_guardrail_check
#[test]
fn test_window_parse_testgen_through_prepare_guardrail_check() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_testgen", "prepare_build", "prepare_guardrail_check"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_clippy_lint -> report
#[test]
fn test_window_parse_clippy_lint_through_report() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_clippy_lint", "parse_test", "report"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_codegen_cmd -> prepare_testgen
#[test]
fn test_window_prepare_codegen_cmd_through_prepare_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_codegen_cmd", "execute_codegen", "parse_codegen_result", "prepare_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_codegen -> execute_testgen
#[test]
fn test_window_execute_codegen_through_execute_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_codegen", "parse_codegen_result", "prepare_testgen", "execute_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_codegen_result -> parse_testgen
#[test]
fn test_window_parse_codegen_result_through_parse_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_codegen_result", "prepare_testgen", "execute_testgen", "parse_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_testgen -> prepare_build
#[test]
fn test_window_prepare_testgen_through_prepare_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_testgen", "execute_testgen", "parse_testgen", "prepare_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_testgen -> prepare_guardrail_check
#[test]
fn test_window_execute_testgen_through_prepare_guardrail_check() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_testgen", "parse_testgen", "prepare_build", "prepare_guardrail_check"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_testgen -> execute_build
#[test]
fn test_window_parse_testgen_through_execute_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_testgen", "prepare_build", "prepare_guardrail_check", "execute_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_test -> report
#[test]
fn test_window_execute_test_through_report() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_test", "parse_clippy_lint", "parse_test", "report"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_codegen_cmd -> execute_testgen
#[test]
fn test_window_prepare_codegen_cmd_through_execute_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_codegen_cmd", "execute_codegen", "parse_codegen_result", "prepare_testgen", "execute_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_codegen -> parse_testgen
#[test]
fn test_window_execute_codegen_through_parse_testgen() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_codegen", "parse_codegen_result", "prepare_testgen", "execute_testgen", "parse_testgen"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_codegen_result -> prepare_build
#[test]
fn test_window_parse_codegen_result_through_prepare_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_codegen_result", "prepare_testgen", "execute_testgen", "parse_testgen", "prepare_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: prepare_testgen -> prepare_guardrail_check
#[test]
fn test_window_prepare_testgen_through_prepare_guardrail_check() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("prepare_testgen", "execute_testgen", "parse_testgen", "prepare_build", "prepare_guardrail_check"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: execute_testgen -> execute_build
#[test]
fn test_window_execute_testgen_through_execute_build() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("execute_testgen", "parse_testgen", "prepare_build", "prepare_guardrail_check", "execute_build"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: parse_testgen -> execute_guardrail_check
#[test]
fn test_window_parse_testgen_through_execute_guardrail_check() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("parse_testgen", "prepare_build", "prepare_guardrail_check", "execute_build", "execute_guardrail_check"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: clippy_lint -> report
#[test]
fn test_window_clippy_lint_through_report() {
    let dag = crate::build_ci_graph().unwrap();
    let flat = lower(&dag).expect("lower should succeed");
    let baseline = execute_with_mode(&dag, ExecutionMode::DryRun(mock_spec().to_boundary_mocks())).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("clippy_lint", "execute_test", "parse_clippy_lint", "parse_test", "report"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_with_mode(&window_dag, ExecutionMode::DryRun(mocks)).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: report - All stages pass → overall success
/// 
/// Tests that node 'report' produces expected outputs for given inputs.
#[test]
fn test_example_report_all_stages_pass_overall_success() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("build_success".to_string(), Value::Bool(true));
    inputs.insert("guardrail_success".to_string(), Value::Bool(true));
    inputs.insert("lint_success".to_string(), Value::Bool(true));
    inputs.insert("test_success".to_string(), Value::Bool(true));
    inputs.insert("testgen_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "report", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'report' should execute successfully");

    // Check output port 'overall_success'
    let output_overall_success = outputs.get("overall_success").expect("output port 'overall_success' should exist");
    assert_eq!(*output_overall_success, Value::Bool(true), "expected exact value");
    // Check output port 'report'
    let output_report = outputs.get("report").expect("output port 'report' should exist");
    assert!(output_report.as_str().map(|s| s.contains("SUCCESS")).unwrap_or(false), "expected to contain 'SUCCESS', got: {:?}", output_report);
}

/// Node example: report - Build failure → overall failure
/// 
/// Tests that node 'report' produces expected outputs for given inputs.
#[test]
fn test_example_report_build_failure_overall_failure() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("build_stderr".to_string(), Value::Str("error: compilation failed".to_string()));
    inputs.insert("build_success".to_string(), Value::Bool(false));
    inputs.insert("guardrail_success".to_string(), Value::Bool(true));
    inputs.insert("lint_stderr".to_string(), Value::Str("".to_string()));
    inputs.insert("lint_stdout".to_string(), Value::Str("".to_string()));
    inputs.insert("lint_success".to_string(), Value::Bool(true));
    inputs.insert("test_stderr".to_string(), Value::Str("".to_string()));
    inputs.insert("test_stdout".to_string(), Value::Str("".to_string()));
    inputs.insert("test_success".to_string(), Value::Bool(true));
    inputs.insert("testgen_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "report", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'report' should execute successfully");

    // Check output port 'overall_success'
    let output_overall_success = outputs.get("overall_success").expect("output port 'overall_success' should exist");
    assert_eq!(*output_overall_success, Value::Bool(false), "expected exact value");
    // Check output port 'report'
    let output_report = outputs.get("report").expect("output port 'report' should exist");
    assert!(output_report.as_str().map(|s| s.contains("FAILURE")).unwrap_or(false), "expected to contain 'FAILURE', got: {:?}", output_report);
}

/// Node example: parse_deps_exists - File exists: deps.toml found → deps_exists true
/// 
/// Tests that node 'parse_deps_exists' produces expected outputs for given inputs.
#[test]
fn test_example_parse_deps_exists_file_exists_deps_toml_found_deps_exists_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::File(gunbc_ir::transport::FileResponse { path: "deps.toml".to_string(), operation: gunbc_ir::transport::FileOp::Exists, success: true, content: None, exists: Some(true), error: None })));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_deps_exists", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_deps_exists' should execute successfully");

    // Check output port 'deps_checked'
    let output_deps_checked = outputs.get("deps_checked").expect("output port 'deps_checked' should exist");
    assert_eq!(*output_deps_checked, Value::Bool(true), "expected exact value");
    // Check output port 'deps_exists'
    let output_deps_exists = outputs.get("deps_exists").expect("output port 'deps_exists' should exist");
    assert_eq!(*output_deps_exists, Value::Bool(true), "expected exact value");
    // Check output port 'deps_installed'
    let output_deps_installed = outputs.get("deps_installed").expect("output port 'deps_installed' should exist");
    assert_eq!(*output_deps_installed, Value::Int(0), "expected exact value");
    // Check output port 'message'
    let output_message = outputs.get("message").expect("output port 'message' should exist");
    assert!(output_message.as_str().map(|s| s.contains("deps.toml found")).unwrap_or(false), "expected to contain 'deps.toml found', got: {:?}", output_message);
}

/// Node example: parse_deps_exists - Handles skipped transport response gracefully
/// 
/// Tests that node 'parse_deps_exists' produces expected outputs for given inputs.
#[test]
fn test_example_parse_deps_exists_handles_skipped_transport_response_gracefully() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Skipped);
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_deps_exists", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_deps_exists' should execute successfully");

    // Check output port 'deps_checked'
    let _output_deps_checked = outputs.get("deps_checked").expect("output port 'deps_checked' should exist");
    // Any value accepted for output_deps_checked
}

/// Node example: parse_codegen_exists - File exists: codegen dir found → codegen not needed
/// 
/// Tests that node 'parse_codegen_exists' produces expected outputs for given inputs.
#[test]
fn test_example_parse_codegen_exists_file_exists_codegen_dir_found_codegen_not_needed() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("exec_mode".to_string(), Value::Str("ensure".to_string()));
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::File(gunbc_ir::transport::FileResponse { path: "Cargo.toml".to_string(), operation: gunbc_ir::transport::FileOp::Exists, success: true, content: None, exists: Some(true), error: None })));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_codegen_exists", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_codegen_exists' should execute successfully");

    // Check output port 'codegen_needed'
    let output_codegen_needed = outputs.get("codegen_needed").expect("output port 'codegen_needed' should exist");
    assert_eq!(*output_codegen_needed, Value::Bool(false), "expected exact value");
    // Check output port 'codegen_ran'
    let output_codegen_ran = outputs.get("codegen_ran").expect("output port 'codegen_ran' should exist");
    assert_eq!(*output_codegen_ran, Value::Bool(false), "expected exact value");
    // Check output port 'prep_success'
    let output_prep_success = outputs.get("prep_success").expect("output port 'prep_success' should exist");
    assert_eq!(*output_prep_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_codegen_exists - Handles skipped transport response gracefully
/// 
/// Tests that node 'parse_codegen_exists' produces expected outputs for given inputs.
#[test]
fn test_example_parse_codegen_exists_handles_skipped_transport_response_gracefully() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("exec_mode".to_string(), Value::Str("ensure".to_string()));
    inputs.insert("response".to_string(), Value::Skipped);
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_codegen_exists", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_codegen_exists' should execute successfully");

    // Check output port 'codegen_needed'
    let _output_codegen_needed = outputs.get("codegen_needed").expect("output port 'codegen_needed' should exist");
    // Any value accepted for output_codegen_needed
}

/// Node example: parse_codegen_result - Codegen shell success → prep_success true, codegen_ran true
/// 
/// Tests that node 'parse_codegen_result' produces expected outputs for given inputs.
#[test]
fn test_example_parse_codegen_result_codegen_shell_success_prep_success_true_codegen_ran_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::Shell(gunbc_ir::transport::ShellResponse { exit_code: 0, stdout: "Generated 3 files".to_string(), stderr: "".to_string() })));
    inputs.insert("skip".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_codegen_result", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_codegen_result' should execute successfully");

    // Check output port 'codegen_ran'
    let output_codegen_ran = outputs.get("codegen_ran").expect("output port 'codegen_ran' should exist");
    assert_eq!(*output_codegen_ran, Value::Bool(true), "expected exact value");
    // Check output port 'prep_message'
    let output_prep_message = outputs.get("prep_message").expect("output port 'prep_message' should exist");
    assert!(output_prep_message.as_str().map(|s| s.contains("successfully")).unwrap_or(false), "expected to contain 'successfully', got: {:?}", output_prep_message);
    // Check output port 'prep_success'
    let output_prep_success = outputs.get("prep_success").expect("output port 'prep_success' should exist");
    assert_eq!(*output_prep_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_codegen_result - Skip path: codegen exists → prep_success, not ran
/// 
/// Tests that node 'parse_codegen_result' produces expected outputs for given inputs.
#[test]
fn test_example_parse_codegen_result_skip_path_codegen_exists_prep_success_not_ran() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("skip".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_codegen_result", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_codegen_result' should execute successfully");

    // Check output port 'codegen_ran'
    let output_codegen_ran = outputs.get("codegen_ran").expect("output port 'codegen_ran' should exist");
    assert_eq!(*output_codegen_ran, Value::Bool(false), "expected exact value");
    // Check output port 'prep_success'
    let output_prep_success = outputs.get("prep_success").expect("output port 'prep_success' should exist");
    assert_eq!(*output_prep_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_testgen - Testgen shell success → testgen_success true
/// 
/// Tests that node 'parse_testgen' produces expected outputs for given inputs.
#[test]
fn test_example_parse_testgen_testgen_shell_success_testgen_success_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::Shell(gunbc_ir::transport::ShellResponse { exit_code: 0, stdout: "Generated tests".to_string(), stderr: "".to_string() })));
    inputs.insert("skip".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_testgen", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_testgen' should execute successfully");

    // Check output port 'testgen_success'
    let output_testgen_success = outputs.get("testgen_success").expect("output port 'testgen_success' should exist");
    assert_eq!(*output_testgen_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_testgen - Skip path: testgen skipped → success false
/// 
/// Tests that node 'parse_testgen' produces expected outputs for given inputs.
#[test]
fn test_example_parse_testgen_skip_path_testgen_skipped_success_false() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("skip".to_string(), Value::Bool(true));
    inputs.insert("skip_reason".to_string(), Value::Str("Skipped due to prep failure".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_testgen", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_testgen' should execute successfully");

    // Check output port 'testgen_success'
    let output_testgen_success = outputs.get("testgen_success").expect("output port 'testgen_success' should exist");
    assert_eq!(*output_testgen_success, Value::Bool(false), "expected exact value");
}

/// Node example: parse_build - Build shell success → build_success true
/// 
/// Tests that node 'parse_build' produces expected outputs for given inputs.
#[test]
fn test_example_parse_build_build_shell_success_build_success_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::Shell(gunbc_ir::transport::ShellResponse { exit_code: 0, stdout: "Compiling gunbc v0.1.0
    Finished dev".to_string(), stderr: "".to_string() })));
    inputs.insert("skip".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_build", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_build' should execute successfully");

    // Check output port 'build_skipped'
    let output_build_skipped = outputs.get("build_skipped").expect("output port 'build_skipped' should exist");
    assert_eq!(*output_build_skipped, Value::Bool(false), "expected exact value");
    // Check output port 'build_stdout'
    let output_build_stdout = outputs.get("build_stdout").expect("output port 'build_stdout' should exist");
    assert!(output_build_stdout.as_str().map(|s| s.contains("Compiling")).unwrap_or(false), "expected to contain 'Compiling', got: {:?}", output_build_stdout);
    // Check output port 'build_success'
    let output_build_success = outputs.get("build_success").expect("output port 'build_success' should exist");
    assert_eq!(*output_build_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_build - Skip path: build skipped → success false, skipped true
/// 
/// Tests that node 'parse_build' produces expected outputs for given inputs.
#[test]
fn test_example_parse_build_skip_path_build_skipped_success_false_skipped_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("skip".to_string(), Value::Bool(true));
    inputs.insert("skip_reason".to_string(), Value::Str("Skipped due to prep failure".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_build", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_build' should execute successfully");

    // Check output port 'build_skipped'
    let output_build_skipped = outputs.get("build_skipped").expect("output port 'build_skipped' should exist");
    assert_eq!(*output_build_skipped, Value::Bool(true), "expected exact value");
    // Check output port 'build_success'
    let output_build_success = outputs.get("build_success").expect("output port 'build_success' should exist");
    assert_eq!(*output_build_success, Value::Bool(false), "expected exact value");
}

/// Node example: parse_test - Test shell success → test_success true
/// 
/// Tests that node 'parse_test' produces expected outputs for given inputs.
#[test]
fn test_example_parse_test_test_shell_success_test_success_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::Shell(gunbc_ir::transport::ShellResponse { exit_code: 0, stdout: "running 42 tests
test result: ok. 42 passed".to_string(), stderr: "".to_string() })));
    inputs.insert("skip".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_test", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_test' should execute successfully");

    // Check output port 'test_skipped'
    let output_test_skipped = outputs.get("test_skipped").expect("output port 'test_skipped' should exist");
    assert_eq!(*output_test_skipped, Value::Bool(false), "expected exact value");
    // Check output port 'test_stdout'
    let output_test_stdout = outputs.get("test_stdout").expect("output port 'test_stdout' should exist");
    assert!(output_test_stdout.as_str().map(|s| s.contains("42 tests")).unwrap_or(false), "expected to contain '42 tests', got: {:?}", output_test_stdout);
    // Check output port 'test_success'
    let output_test_success = outputs.get("test_success").expect("output port 'test_success' should exist");
    assert_eq!(*output_test_success, Value::Bool(true), "expected exact value");
}

/// Node example: parse_test - Skip path: test skipped → success false, skipped true
/// 
/// Tests that node 'parse_test' produces expected outputs for given inputs.
#[test]
fn test_example_parse_test_skip_path_test_skipped_success_false_skipped_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("skip".to_string(), Value::Bool(true));
    inputs.insert("skip_reason".to_string(), Value::Str("Skipped due to build failure".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_test", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_test' should execute successfully");

    // Check output port 'test_skipped'
    let output_test_skipped = outputs.get("test_skipped").expect("output port 'test_skipped' should exist");
    assert_eq!(*output_test_skipped, Value::Bool(true), "expected exact value");
    // Check output port 'test_success'
    let output_test_success = outputs.get("test_success").expect("output port 'test_success' should exist");
    assert_eq!(*output_test_success, Value::Bool(false), "expected exact value");
}

/// Node example: prepare_codegen_exists - Prepares file-exists check for codegen dir
/// 
/// Tests that node 'prepare_codegen_exists' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_codegen_exists_prepares_file_exists_check_for_codegen_dir() {
    let dag = crate::build_ci_graph().unwrap();
    let inputs = std::collections::HashMap::new();
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_codegen_exists", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_codegen_exists' should execute successfully");

    // Check output port 'request'
    let output_request = outputs.get("request").expect("output port 'request' should exist");
    assert!(!output_request.is_empty(), "expected non-empty value");
}

/// Node example: prepare_codegen_cmd - Codegen command prepare emits skip flag
/// 
/// Tests that node 'prepare_codegen_cmd' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_codegen_cmd_codegen_command_prepare_emits_skip_flag() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("codegen_needed".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_codegen_cmd", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_codegen_cmd' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert!(output_skip.as_bool().is_some(), "expected Bool for output_skip");
}

/// Node example: prepare_testgen - Testgen prepare emits skip flag
/// 
/// Tests that node 'prepare_testgen' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_testgen_testgen_prepare_emits_skip_flag() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("prep_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_testgen", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_testgen' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert!(output_skip.as_bool().is_some(), "expected Bool for output_skip");
}

/// Node example: prepare_build - Build prepare emits skip flag
/// 
/// Tests that node 'prepare_build' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_build_build_prepare_emits_skip_flag() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("prep_success".to_string(), Value::Bool(true));
    inputs.insert("testgen_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_build", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_build' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert!(output_skip.as_bool().is_some(), "expected Bool for output_skip");
}

/// Node example: prepare_test - Test prepare emits skip flag
/// 
/// Tests that node 'prepare_test' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_test_test_prepare_emits_skip_flag() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("build_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_test", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_test' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert!(output_skip.as_bool().is_some(), "expected Bool for output_skip");
}

/// Node example: prepare_guardrail_check - Guardrail prepare emits skip flag
/// 
/// Tests that node 'prepare_guardrail_check' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_guardrail_check_guardrail_prepare_emits_skip_flag() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("testgen_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_guardrail_check", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_guardrail_check' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert!(output_skip.as_bool().is_some(), "expected Bool for output_skip");
}

/// Node example: prepare_clippy_lint - Build success → clippy not skipped
/// 
/// Tests that node 'prepare_clippy_lint' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_clippy_lint_build_success_clippy_not_skipped() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("build_success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_clippy_lint", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_clippy_lint' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert_eq!(*output_skip, Value::Bool(false), "expected exact value");
}

/// Node example: prepare_clippy_lint - Build failure → clippy skipped
/// 
/// Tests that node 'prepare_clippy_lint' produces expected outputs for given inputs.
#[test]
fn test_example_prepare_clippy_lint_build_failure_clippy_skipped() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("build_success".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "prepare_clippy_lint", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'prepare_clippy_lint' should execute successfully");

    // Check output port 'skip'
    let output_skip = outputs.get("skip").expect("output port 'skip' should exist");
    assert_eq!(*output_skip, Value::Bool(true), "expected exact value");
}

/// Node example: parse_clippy_lint - Clippy result parse produces success/skipped flags
/// 
/// Tests that node 'parse_clippy_lint' produces expected outputs for given inputs.
#[test]
fn test_example_parse_clippy_lint_clippy_result_parse_produces_success_skipped_flags() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("skip".to_string(), Value::Bool(false));
    inputs.insert("stderr".to_string(), Value::Str("".to_string()));
    inputs.insert("stdout".to_string(), Value::Str("Checking gunbc".to_string()));
    inputs.insert("success".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_clippy_lint", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_clippy_lint' should execute successfully");

    // Check output port 'lint_skipped'
    let output_lint_skipped = outputs.get("lint_skipped").expect("output port 'lint_skipped' should exist");
    assert!(output_lint_skipped.as_bool().is_some(), "expected Bool for output_lint_skipped");
    // Check output port 'lint_success'
    let output_lint_success = outputs.get("lint_success").expect("output port 'lint_success' should exist");
    assert!(output_lint_success.as_bool().is_some(), "expected Bool for output_lint_success");
}

/// Node example: parse_guardrail_check - Guardrail check success → guardrail_success true
/// 
/// Tests that node 'parse_guardrail_check' produces expected outputs for given inputs.
#[test]
fn test_example_parse_guardrail_check_guardrail_check_success_guardrail_success_true() {
    let dag = crate::build_ci_graph().unwrap();
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("response".to_string(), Value::Response(gunbc_ir::transport::TransportResponse::Shell(gunbc_ir::transport::ShellResponse { exit_code: 0, stdout: "OK".to_string(), stderr: "".to_string() })));
    inputs.insert("skip".to_string(), Value::Bool(false));
    let outputs = gunbc_exec::execute_single_node(&dag, "parse_guardrail_check", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'parse_guardrail_check' should execute successfully");

    // Check output port 'guardrail_success'
    let output_guardrail_success = outputs.get("guardrail_success").expect("output port 'guardrail_success' should exist");
    assert_eq!(*output_guardrail_success, Value::Bool(true), "expected exact value");
}

