[package]
name = "gunbc-exec"
version = "0.1.0"
edition = "2021"

[dependencies]
gunbc-ir = { path = "../gunbc-ir" }
gunbc-validate = { path = "../gunbc-validate", optional = true }

[features]
default = ["validate"]
validate = ["dep:gunbc-validate"]
