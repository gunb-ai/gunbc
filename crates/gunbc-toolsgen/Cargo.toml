[package]
name = "gunbc-toolsgen"
version = "0.1.0"
edition = "2021"

[dependencies]
gunbc-ir = { path = "../gunbc-ir" }
gunbc-exec = { path = "../gunbc-exec" }
clap = { version = "4", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
