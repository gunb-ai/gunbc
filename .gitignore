# Generated by gunbc-bootstrap
# DO NOT EDIT - regenerate with: cargo run -p gunbc-dag --bin gunbc-bootstrap

# Rule: Ignore anything that is (a) reproducible from committed source-of-truth
# files, or (b) environment-specific and not shared across developers.

# --- Cargo build artifacts (from cargo) ---
# Reproducible from source via cargo build
/target/
/target-codex/

# --- Cargo cache (local CARGO_HOME) (from tool/rust) ---
# Local Cargo cache may include git checkouts; not project state
/.cargo-home/

# --- Codegen bin symlink (from codegen) ---
# Symlink to target/release created by codegen
/bin/

# --- Coverage reports (from cargo-tarpaulin) ---
# Generated, often large, reproducible
tarpaulin-report.html
tarpaulin-report.json
cobertura.xml
lcov.info
coverage/

# --- Editor/IDE state (from editor) ---
# Per-developer configuration, not project state
.idea/
.vscode/
*.swp
*.swo
*~

# --- OS metadata (from os) ---
# OS-generated, not project state
.DS_Store
Thumbs.db

# --- Secrets and local config (from secrets) ---
# Environment-specific, may contain secrets
.env
.env.local
.env.*.local

# --- Generator stamp files (from generators) ---
# Producer-centric model stamps; regenerated by make targets
.*-stamp

# --- Bootstrap outputs (from bootstrap) ---
# Regenerated by gunbc-bootstrap/makegen; not source-of-truth
/Makefile
/output

# --- Pragma outputs (from pragma) ---
# Regenerated by gunbc-pragma; not source-of-truth
/clippy.toml
/tools/disallowed-methods-allowlist.txt
/tools/pragma-lint-policy.txt

# --- Generated test files (from testgen) ---
# Regenerated by make testgen; staleness checked by make test
**/generated_tests*.rs

# --- Workflow runtime state (from workflow) ---
# Local execution ledger and config; not source-of-truth
/.gunbc/

