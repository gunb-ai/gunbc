# Regroup — 2026-05-22

Working planning doc for the pause-and-replan. Operator paused new dispatches mid-day after a high-velocity burst that started outpacing planning. This doc grounds where we are vs the thesis, what landed, what's queued, and gives a collaborative space to re-plan.

**Status:** drain phase. ~9-10 in-flight PRs landing under normal cadence. No new dispatches anywhere.

Source-of-truth files this defers to: `THESIS.md`, `ROADMAP.md`, `INVARIANTS.md`, `src/v4/TASKS.md`, `src/v4/SELF_HOSTING.md`. This doc is operational, not a parallel ledger — once we converge on the re-plan, update those, not this.

---

## 1. Thesis state — claim-by-claim, post-drain

| Thesis claim | State pre-burst | State post-drain (in-flight included) | Distance to claimed-complete |
|---|---|---|---|
| Self-host facet 1 — `.dag` authors the compiler | Substrate scaffold + key passes in `.dag` | Pipeline stages parse/lower/infer/ground/translate/eval/emit all wired; MVP-1 + MVP-2 host-eval CI gates GREEN | Closer — full-tree emit still pending |
| Self-host facet 2 — bit-identical self-emit | v3 `regen_bootstrap` mechanism exists | v2 `regen_stage0 --verify` GREEN + CI-enforced (PR #3471); v3 layer still has 0 stage0 files (no rust yet at v3 layer) | v2 fixed-point essentially closed; v3 unstarted |
| Self-host facet 3 — tests are data | 204 hand-Rust paths / 1240 functions ratchetted | Audit doc PR #3568 classified 384 DELETE / 537 REPLACE / 319 KEEP; v2_oracle KEEP-corrected; T-19 sixth category (LanguageBehaviorEquivalence) live (PR #3530) | Substrate ready; categorical retirement campaign queued (Batch 1 first) |
| Self-host facet 4 — recursive-flex (lens on own CI) | Lens-CI MechanicalReverification runtime (PR #3511) | T-21 affected_set lens landed (PR #3528); Lens-CI active (#3529 parked-on-RC) | Mechanism in place; ratchets accumulating |
| Tier-1 grounding completeness | Target-side primitives modeled | T-4 language fact-bundles landed for python/cpp/java/kotlin/lean/typescript/WASM+spice/sql; rust as pattern anchor | 8 languages + 2 formats covered; long-tail extdeps remain |
| Substrate shape — six connectives / five behaviors | Stable | Parser/lowering substrate upgrade for generic type apps in variant records (PR #3488) — enabled variant-parameterization sweep | Substrate-axis healthy; recent dissolution-finding stream caught more parallel-payload-vs-typed-reference cases |
| Substrate algebra — termination + cardinality | Carriers declared (TerminationProof / Multiplicity / DescentEvidence) | SPI-1+2 cardinality.dag Tier-A Multiplicity declarations + fold_node→Witness<TerminationProof> propagation (PR #3559) | Substrate slot filled; per-fn population is next |
| Lens-as-data composition | Lens scaffolds across coverage / complexity / cost / parallelism / fact_density / synthesis / testgen / affected_set | Lens TestClaim pairs authored (PR #3515); §11 DissolutionSubsumption substrate (PR #3496); MechanicalReverification runtime (#3511) | Authorship discipline locked; per-lens fill long-tail |
| Tests-as-data — TestClaim integration | TestClaim coproduct hardened (PR #3503); HostModel/TestClaim alignment (PR #3541) | T-19 sixth category live; english_ingest_fail_closed boundary TestClaims (PR #3543) | Substrate done; generator-per-category long-tail |
| Cost-of-change = 1 file | Held | Held — most PRs touch 1-3 `.dag` files; the substrate vs consumer race conditions caused multi-file churn three times today (free_monoid_fold rename, PrimitiveFactAxisBinding removal, T-4 parallel merges) — DISCIPLINE GAP, not invariant violation |

**Net thesis movement (last 3 days):** real substantive forward motion on facets 1/3/4 + Tier-1 grounding + substrate algebra. Facet 2 (self-emit fixed point) made one concrete step (v2 verify GREEN) but the v3 layer hasn't started.

---

## 2. What landed — thematic grouping (3-day window)

8 commits today, 59 yesterday (2026-05-21), 23 day before. Thematically:

**Language fact-bundles (T-4 wave)** — 8 PRs:
`#3533 python` · `#3535 cpp` · `#3536 java` · `#3537 kotlin` · `#3538 typescript` · `#3546 WASM+spice` · `#3547 lean` · `#3545 sql` (format)

**Substrate evolution (Modeling lane)** — 5 numbered PRs:
`#3549 ModelCore List→Map uniqueness (3/4)` · `#3554 FiniteSet + TotalMap + L1.13.c (5/5)` · `#3559 cardinality.dag SPI-1+2 (Multiplicity/TerminationProof)` · `#3496 DissolutionSubsumption Set<DiffId>` · `#3488 parser/lowering generic type apps in variant records`

**Self-host / pipeline stages**:
`#3527 MVP-2 host-eval CI receipt` · `#3542 MVP-1 end-to-end CI gate` · `#3521 T-11 emit per-target rust-first` · `#3528 T-21 affected_set lens` · `#3511 Lens-CI MechanicalReverification runtime` · `#3530 T-19 sixth testgen category (LanguageBehaviorEquivalence)` · `#3531 T-25-tail predicate prover real semantics`

**Substrate-name discipline / no-quirked**:
`#3505 SubValueRelation lawful BoundedLattice` · `#3499 L1.4.b variant-tag-recoverable-from-payload` · `#3525 L1.4.b Locus anchor in v4 diagnostics` · `#3503 substrate hardening bundle (node well-formedness + P9 single-owner)` · `#3522 HostModel decomposition (drop umbrella)` · `#3534 Eval_node fail-closed` · `#3534/#3541 HostModel/TestClaim alignment`

**File-as-data / separate-concerns / honest-naming sweep** (operator-direct push 05-19→21):
`#3517 errors.dag per-provider catalog → per-provider homes` · `#3520 cloud.dag → GitHubActions/LocalDev/Metadata discrete homes` · `#3516 POSIX-specific facts → posix.dag` · `#3518 coreutils → gnu_coreutils + cloudevents rename` · `#3543 english_ingest_fail_closed.dag boundary TestClaims`

**Process / naming hygiene**:
`#3564 free_monoid_fold → fold_list (Practice 11 prefix-drop)` · `#3514 v2 dag-serializer recursive-by-value fix` · `#3540 stale extdeps refs aligned to posix.dag`

**Doc-by-doc maintained-ledger NUKE class** (memory): completed via PR #3389 8c25654e6 (DECISIONS / STRUCTURE / dissolution-inventory deleted; INVARIANTS reconciled).

**Lens TestClaim pairs**: `#3515` authored TestClaim pairs across complexity/cost/parallelism/affected_set/coverage/fact_density/synthesis/testgen.

**Volume signal:** ~50 substantive PRs in 3 days. Velocity was real. Operator is correct that quality dipped — three substrate-vs-consumer race conditions, four verify-don't-trust paraphrase gaps from sharp-crab, one rename-coordination grep-zero miss from smart-boar.

---

## 3. v4 goal state when in-flight drains

In-flight PRs that will land in the next 2-4h (per peer-manager snapshots):

| PR | Lane | Adds |
|---|---|---|
| #3519 | sharp-crab | LLM Anthropic-shape split |
| #3551 | sharp-crab | A-tight follow-up to #3505 + v2_compiler_tests E0004 fix |
| #3568 | sharp-crab | Hand-Rust retirement audit doc (DELETE/REPLACE/KEEP classification) |
| #3529 | jolly-eagle | Lens-CI activation |
| #3553 | jolly-eagle | Substrate-evolution 2/4 overflow_disposition |
| #3550 | jolly-eagle | Substrate-evolution 4/4 snoc-fold (needs hotfix-unblock) |
| #3552 | jolly-eagle | Substrate-evolution 1/4 range carriers |
| #3567 | jolly-eagle | Phase 1 fold_list_right substrate add |
| (hotfix) | jolly-eagle | typescript fold_list rename + PrimitiveFactAxisBinding orphan cleanup |
| snappy-bear subtree | jolly-eagle | T-4 go.dag (existing scope only) |

Post-drain state: `regen_stage0 --verify` GREEN + CI-enforced; cardinality substrate slotted; lens-CI runtime ratchets growing; T-4 language coverage at 8+ languages + 2 formats; substrate-evolution numbered wave complete (1/4 through 5/5); hand-Rust retirement classified and ready for batch dispatch; (b) `fold_list_right` substrate primitive landed (sweep deferred).

**Closest-to-claim-complete goal:** facet 2 self-emit at the v2 layer (essentially done). **Furthest:** Tier-2 runtime-safety proofs (gated post-R1 per ROADMAP).

---

## 4. Queued future dispatches — rolling forward to re-plan

These were teed up but paused. Operator's call which to revive, defer, or drop in the re-plan:

| Queued item | Lane (would-be) | Status at pause |
|---|---|---|
| Merge PR #3551 | sharp-crab → operator | ready-to-merge; operator merge call |
| Batch 1 retirement (~211 deletes from #3568 audit) | sharp-crab | classified ready; PR not opened |
| Phase 2 `fold_list_right` sweep (sql/rust/python/lean/java extdeps) | jolly-eagle | scope clear; gates on Phase 1 #3567 landing |
| Full-tree v4 cargo-build cleanup | smart-boar | named as next rust→0 axis; not scoped in detail |
| Tier-A Multiplicity per-fn population (post-#3559) | jolly-eagle / smart-boar | substrate ready; declarations not authored |
| Cluster-wide rename-coordination discipline doc | (cross-lane) | smart-boar self-flagged; not authored |
| ModuleServiceParseSurface T-19 generator | smart-boar | 1-of-4 missing category; design deferred |
| Affected-set lens activation in CI runner | (cross-lane) | T-21 landed (#3528); activation pending |
| REPLACE-via-TestClaim batches (537 hand-Rust tests) | sharp-crab + smart-boar | gated on per-category T-19 generators |
| Rust → 0 ratchet retirement campaign (204 → smaller "keep" set) | (cross-lane) | sharp-crab audit done; per-category landings not started |

---

## 5. Quality-dip themes (placeholder for peer audits)

What operator surfaced as the diagnosis: velocity outran planning; quality dipped. Peer managers were invited to audit their PRs for roll-forward — replies pending. Initial themes from the day's surface stream:

- **Substrate-vs-consumer race conditions.** Three instances: free_monoid_fold rename vs in-flight language imports (java/lean/typescript); PrimitiveFactAxisBinding removal vs orphaned extdep references; T-4 lean.dag merging with stale imports. Pattern: substrate change PR + consumer PR merge in parallel without one rebasing on the other. **Discipline gap:** pre-merge "rebase against rename-or-removal-substrate-change-PRs" check for any extdep-touching PR. Could be a CI check.
- **Paraphrase-without-verification (sharp-crab self-flagged 4 instances).** Routing summaries of worker reports without `git show` against the authoritative PR/file. Discipline correction: PR-content claim → verify against doc before routing.
- **Grep-zero race (smart-boar self-flagged).** Pre-dispatch verification that catches state at PR-author time but not in-flight call-site additions. Discipline correction: rename sweeps need cross-lane coordination, not just one-shot grep.
- **Closeout-leaf premature completion / orphan-PR detection.** Quick-hawk-443 misread as "archived" when "+N quiet descendants." Discipline correction: subtree-digest "quiet" ≠ "archived"; verify worker status before orphan-PR escalation.
- **PM coordination gap.** I dispatched (b) `fold_list_right` to jolly-eagle but didn't fan to smart-boar; smart-boar surfaced (b) as "operator-pending" not knowing it had been ruled. Discipline correction: cross-lane rulings get fanned to ALL affected peer-managers, not just the dispatch-owner.

Peers may surface more when they audit. Fold their adds here.

---

## 6. Re-plan space

Open to your direction. Some sequencing questions to organize around when you're ready:

- **Closure priority.** Of facet 2 (v3 self-emit / full-tree v4 cargo-build), facet 3 (hand-Rust retirement campaign), facet 1 (rust→0 substrate completion), which is the nearest milestone you want to push first in the re-plan?
- **Discipline gates first?** Before re-firing dispatches, do you want to land the discipline corrections (rename-coordination, paraphrase-verify) as cross-cutting protocol? Or fold them in as we dispatch and self-correct?
- **R1 vs v4-XL framing.** ROADMAP still carries the R1 nine-lane structure with v4-supersession-notes layered on. Worth a top-level update to make v4-XL the primary structure? Or wait until more lanes close?
- **MVP-N progression.** MVP-1 (substrate reduction) and MVP-2 (host-eval) both have CI gates green. Is there a defined MVP-3 / MVP-4 / etc., or do we shape that here?

— smart-lynx-142 (PM) · 2026-05-22
