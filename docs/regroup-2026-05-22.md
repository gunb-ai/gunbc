# Regroup — 2026-05-22

Working planning doc for the pause-and-replan. Operator paused new dispatches mid-day after a high-velocity burst that started outpacing planning. This doc grounds where we are vs the thesis, what landed, what's queued, and gives a collaborative space to re-plan.

**Status:** drain phase. ~9-10 in-flight PRs landing under normal cadence (cool-cat-24 subtree); broader open-PR count higher when smart-lynx-142 peer-PM subtree is included. No new dispatches anywhere.

**PM-thread consolidation (operator-ruled 2026-05-22):** smart-lynx-142 peer-PM session (the predecessor PM I forked from to separate v4 from v2 work) is converging. Operator forwarded forward-movement authority from smart-lynx-142 to cool-cat-24 for re-planning. smart-lynx-142's active in-flight workers (silent-deer-669 / cool-hawk-221 / royal-eagle-196 / jolly-stag-521 / gentle-otter-329 / calm-pike-762 / fierce-ant-16 / quiet-moth-603) finish under their session; new dispatches and re-planning go through cool-cat-24. Unified manager pool emerges post-drain.

Source-of-truth files this defers to: `THESIS.md`, `ROADMAP.md`, `INVARIANTS.md`, `src/v4/TASKS.md`, `src/v4/compiler/self_host.dag` (active T-15 scaffold-against-contract), `src/v4/workflow/bootstrap.dag`, `docs/design-pure-bootstrap-zero.md` (LIVE 2026-04-25 0-floor target). `src/v3/SELF_HOSTING.md` is the v2→v3 transition reference that v4 supersedes (ROADMAP `v4 supersession (2026-05-15)`). This doc is operational, not a parallel ledger — once we converge on the re-plan, update those, not this.

---

## 1. Thesis state — claim-by-claim, post-drain

| Thesis claim | State pre-burst | State post-drain (in-flight included) | Distance to claimed-complete |
|---|---|---|---|
| Self-host facet 1 — `.dag` authors the compiler | Substrate scaffold + key passes in `.dag` | Pipeline stages parse/lower/infer/ground/translate/eval/emit all wired; MVP-1 + MVP-2 host-eval CI gates GREEN | Closer — full-tree emit still pending |
| Self-host facet 2 — bit-identical self-emit | v2 regen mechanism extant | **v2:** `regen_stage0 --verify` GREEN + CI-enforced (PR #3471); ~97% per THESIS. **v3:** frozen pending v4 ship (ROADMAP `v4 supersession (2026-05-15)`); CI gated to `src/v3/`-affected only; **NOT** an active zero-floor target — v3→0 retracted per operator 2026-05-22. **v4 (active program):** substrate itself is already 0 hand-Rust (183 `.dag` files, 0 `.rs`); `src/v4/compiler/self_host.dag` scaffold-against-contract (T-15); MVP-1 end-to-end CI gate GREEN (PR #3542) + MVP-2 host-eval CI receipt GREEN (PR #3527); fixed-point validation runner pending substrate completion | v2 closed; v3 frozen comparison-only baseline; v4 substrate at 0; v4 fixed-point runner pending |
| Self-host facet 3 — tests are data | 204 hand-Rust paths / 1240 functions ratchetted under v3 | Audit doc PR #3568 classified 384 DELETE / 537 REPLACE / 319 KEEP (v3-surface lens). **Operator reframe 2026-05-22:** v3 tests existed because v4 testing wasn't there yet; goal is **delete most + harvest into v4 TestClaim specs for what's actually wanted** — not 1:1 v3→v4 test migration. KEEP-AS-RUST (319) likely over-conservative; re-audit pending. T-19 sixth category (LanguageBehaviorEquivalence) live (PR #3530) | v4 TestClaim substrate ready; harvest-vs-delete pass needed per the reframe |
| Self-host facet 4 — recursive-flex (lens on own CI) | Lens-CI MechanicalReverification runtime (PR #3511) | T-21 affected_set lens landed (PR #3528); Lens-CI active (#3529 parked-on-RC) | Mechanism in place; ratchets accumulating |
| Tier-1 grounding completeness | Target-side primitives modeled | T-4 language fact-bundles landed for python/cpp/java/kotlin/lean/typescript/WASM+spice/sql; rust as pattern anchor | 8 languages + 2 formats covered; long-tail extdeps remain |
| Substrate shape — six connectives / five behaviors | Stable | Parser/lowering substrate upgrade for generic type apps in variant records (PR #3488) — enabled variant-parameterization sweep | Substrate-axis healthy; recent dissolution-finding stream caught more parallel-payload-vs-typed-reference cases |
| Substrate algebra — termination + cardinality | Carriers declared (TerminationProof / Multiplicity / DescentEvidence) | SPI-1+2 cardinality.dag Tier-A Multiplicity declarations + fold_node→Witness<TerminationProof> propagation (PR #3559) | Substrate slot filled; per-fn population is next |
| Lens-as-data composition | Lens scaffolds across coverage / complexity / cost / parallelism / fact_density / synthesis / testgen / affected_set | Lens TestClaim pairs authored (PR #3515); §11 DissolutionSubsumption substrate (PR #3496); MechanicalReverification runtime (#3511) | Authorship discipline locked; per-lens fill long-tail |
| Tests-as-data — TestClaim integration | TestClaim coproduct hardened (PR #3503); HostModel/TestClaim alignment (PR #3541) | T-19 sixth category live; english_ingest_fail_closed boundary TestClaims (PR #3543) | Substrate done; generator-per-category long-tail |
| Cost-of-change = 1 file | Held | Held — most PRs touch 1-3 `.dag` files; the substrate vs consumer race conditions caused multi-file churn three times today (free_monoid_fold rename, PrimitiveFactAxisBinding removal, T-4 parallel merges) — DISCIPLINE GAP, not invariant violation |

**Net thesis movement (last 3 days):** real substantive forward motion on facets 1/3/4 + Tier-1 grounding + substrate algebra. Facet 2 (self-emit fixed point) made one concrete step (v2 verify GREEN) but the v3 layer hasn't started.

---

## 2. What landed — thematic grouping (3-day window)

8 commits today, 59 yesterday (2026-05-21), 23 day before. Thematically:

**Language fact-bundles (T-4 wave)** — 8 PRs:
`#3533 python` · `#3535 cpp` · `#3536 java` · `#3537 kotlin` · `#3538 typescript` · `#3546 WASM+spice` · `#3547 lean` · `#3545 sql` (format)

**Substrate evolution (Modeling lane)** — 5 numbered PRs:
`#3549 ModelCore List→Map uniqueness (3/4)` · `#3554 FiniteSet + TotalMap + L1.13.c (5/5)` · `#3559 cardinality.dag SPI-1+2 (Multiplicity/TerminationProof)` · `#3496 DissolutionSubsumption Set<DiffId>` · `#3488 parser/lowering generic type apps in variant records`

**Self-host / pipeline stages**:
`#3527 MVP-2 host-eval CI receipt` · `#3542 MVP-1 end-to-end CI gate` · `#3521 T-11 emit per-target rust-first` · `#3528 T-21 affected_set lens` · `#3511 Lens-CI MechanicalReverification runtime` · `#3530 T-19 sixth testgen category (LanguageBehaviorEquivalence)` · `#3531 T-25-tail predicate prover real semantics`

**Substrate-name discipline / no-quirked**:
`#3505 SubValueRelation lawful BoundedLattice` · `#3499 L1.4.b variant-tag-recoverable-from-payload` · `#3525 L1.4.b Locus anchor in v4 diagnostics` · `#3503 substrate hardening bundle (node well-formedness + P9 single-owner)` · `#3522 HostModel decomposition (drop umbrella)` · `#3534 Eval_node fail-closed` · `#3534/#3541 HostModel/TestClaim alignment`

**File-as-data / separate-concerns / honest-naming sweep** (operator-direct push 05-19→21):
`#3517 errors.dag per-provider catalog → per-provider homes` · `#3520 cloud.dag → GitHubActions/LocalDev/Metadata discrete homes` · `#3516 POSIX-specific facts → posix.dag` · `#3518 coreutils → gnu_coreutils + cloudevents rename` · `#3543 english_ingest_fail_closed.dag boundary TestClaims`

**Process / naming hygiene**:
`#3564 free_monoid_fold → fold_list (Practice 11 prefix-drop)` · `#3514 v2 dag-serializer recursive-by-value fix` · `#3540 stale extdeps refs aligned to posix.dag`

**Doc-by-doc maintained-ledger NUKE class** (memory): completed via PR #3389 8c25654e6 (DECISIONS / STRUCTURE / dissolution-inventory deleted; INVARIANTS reconciled).

**Lens TestClaim pairs**: `#3515` authored TestClaim pairs across complexity/cost/parallelism/affected_set/coverage/fact_density/synthesis/testgen.

**Volume signal:** ~50 substantive PRs in 3 days. Velocity was real. Operator is correct that quality dipped — three substrate-vs-consumer race conditions, four verify-don't-trust paraphrase gaps from sharp-crab, one rename-coordination grep-zero miss from smart-boar.

---

## 3. v4 goal state when in-flight drains

In-flight PRs that will land in the next 2-4h (per peer-manager snapshots):

| PR | Lane | Adds |
|---|---|---|
| #3519 | sharp-crab | LLM Anthropic-shape split |
| #3551 | sharp-crab | A-tight follow-up to #3505 + v2_compiler_tests E0004 fix |
| #3568 | sharp-crab | Hand-Rust retirement audit doc (DELETE/REPLACE/KEEP classification) |
| #3529 | jolly-eagle | Lens-CI activation |
| #3553 | jolly-eagle | Substrate-evolution 2/4 overflow_disposition |
| #3550 | jolly-eagle | Substrate-evolution 4/4 snoc-fold (needs hotfix-unblock) |
| #3552 | jolly-eagle | Substrate-evolution 1/4 range carriers |
| #3567 | jolly-eagle | Phase 1 fold_list_right substrate add |
| (hotfix) | jolly-eagle | typescript fold_list rename + PrimitiveFactAxisBinding orphan cleanup |
| snappy-bear subtree | jolly-eagle | T-4 go.dag (existing scope only) |

Post-drain state: `regen_stage0 --verify` GREEN + CI-enforced; cardinality substrate slotted; lens-CI runtime ratchets growing; T-4 language coverage at 8+ languages + 2 formats; substrate-evolution numbered wave complete (1/4 through 5/5); hand-Rust retirement classified and ready for batch dispatch; (b) `fold_list_right` substrate primitive landed (sweep deferred).

**Closest-to-claim-complete goal:** facet 2 self-emit at the v2 layer (essentially done). **Furthest:** Tier-2 runtime-safety proofs (gated post-R1 per ROADMAP).

---

## 4. Queued future dispatches — rolling forward to re-plan

These were teed up but paused. Operator's call which to revive, defer, or drop in the re-plan:

| Queued item | Lane (would-be) | Status at pause |
|---|---|---|
| Merge PR #3551 | sharp-crab → operator | ready-to-merge; operator merge call |
| Batch 1 retirement (~211 deletes from #3568 audit) | sharp-crab | classified ready; PR not opened |
| Phase 2 `fold_list_right` sweep (sql/rust/python/lean/java extdeps) | jolly-eagle | scope clear; gates on Phase 1 #3567 landing |
| Full-tree v4 cargo-build cleanup | smart-boar | named as next rust→0 axis; not scoped in detail |
| Tier-A Multiplicity per-fn population (post-#3559) | jolly-eagle / smart-boar | substrate ready; declarations not authored |
| Cluster-wide rename-coordination discipline doc | (cross-lane) | smart-boar self-flagged; not authored |
| ModuleServiceParseSurface T-19 generator | smart-boar | 1-of-4 missing category; design deferred |
| Affected-set lens activation in CI runner | (cross-lane) | T-21 landed (#3528); activation pending |
| REPLACE-via-TestClaim batches (537 hand-Rust tests) | sharp-crab + smart-boar | gated on per-category T-19 generators |
| v3 hand-Rust deletion + harvest-to-v4-TestClaim campaign (re-framed per operator 2026-05-22) | (cross-lane) | sharp-crab audit done as DELETE/REPLACE/KEEP under old lens; re-audit under harvest-vs-delete framing pending; KEEP bucket likely shrinks substantially |
| **Eval interpretation-algebra substrate (strategic lane; new 2026-05-22)** | (TBD; substrate work) | Per-primitive `Interpretation` declarations (mirror of cardinality-propagation's per-primitive `Multiplicity` declarations); composition fold over Node tree → `Outcome<EvalResult>` per Practice 10 homomorphism; replaces `v4.compiler.05_eval::eval_node` fail-closed scaffold. Bounded scope ~20-50 declarations + one structural fold. Closes T-19 Deferred witness-validity rows + F1 eval_node fail-closed scaffolding mechanically; could shift MVP-1/MVP-2 CI gates from subset-only to full-tree. **Routed from smart-lynx-142 peer-PM session (operator-ruled (ii) on disambiguation 2026-05-22) — queued under cool-cat-24 authority for post-drain re-plan.** Worked template: cardinality-propagation lane (PR #3559 + #3558) |

---

## 5. Quality-dip themes

Velocity outran planning; quality dipped. Two of three peer managers audited (jolly-eagle 6 themes, sharp-crab 7 themes); smart-boar invited, no audit yet (lane already drained, no-pressure invite). Themes consolidated cross-cutting first, then per-source detail.

### 5a. Cross-cutting themes (≥2 sources converge)

**T1 — Coordination is the catching layer, and the catching layer is failing.** PM (me) + sharp-crab + smart-boar all surfaced manager-tier verify-don't-trust failures today. Examples: my (b) `fold_list_right` ruling fanned only to dispatch-owner; sharp-crab paraphrase-without-verify ×4; smart-boar grep-zero race; sharp-crab squash-merge-commit-msg-vs-tree-state misread. Pattern: PR-level work was largely sound; the coordination-paraphrase across worker→manager→PM→operator was where signal degraded. **Re-plan ask:** verify-against-authoritative-artifact as explicit pre-routing manager-tier protocol — `git show origin/main:path` / `git ls-tree` / direct PR-content grep over relayed summary.

**T2 — Cross-lane merge-ordering blind spots (substrate vs consumer race).** Three race-condition instances today (jolly-eagle #1; PM-observed): `free_monoid_fold→fold_list` rename vs in-flight java/lean/typescript imports; `PrimitiveFactAxisBinding` removal vs orphaned extdep references; T-4 cascade with stale imports. Sharp-crab #2 converges: substrate-variant additions interact across hand-Rust sites, not just regen-mirror lockstep (#3505 SubValueRelation E0004 was this class). **Re-plan ask (operator-directed 2026-05-22):** *minimize* this class through planning that sorts dependencies properly — not a protocol/CI/INVARIANTS rule. When B depends on A, A is planned-and-landed before B (or co-bundled); independently-parallel work is genuinely independent. The race-conditions disappear as a consequence of dependency-aware sequencing, not as an enforced gate. Holistic structural fixes (e.g., lens/review practices that catch sub-classes) are a separate development effort and develop on their own track.

**T3 — CI-receipt integrity / vacuous passing signals.** Jolly-eagle #4: eval_node false-pass (TestClaim CI signals throughout most of today vacuously passed before #3534 fix); workers honestly reported "cargo test passed" but receipt was hollow. Sharp-crab #7: F1 eval_node catch was exactly the kind of CI-signal-integrity finding that justified the operator-direct pivot to runnability. **Re-plan ask:** verify TestClaim eval-execution actually exercises substrate before treating cargo-test as receipt. Convert into a meta-test: "would this CI gate pass if the substrate were absent?" — if yes, the gate is vacuous.

### 5b. jolly-eagle audit (6 themes)

1. Cross-lane merge-ordering — folded into **T2**.
2. **Over-dispatch from "routed under your lane" ambiguity.** Parallel-dispatched 11 language workers alongside zesty-owl-396's auto-orchestrator-spawn for the same work-item. System-level pattern: dashboard auto-orchestrator-spawn isn't visible to routing manager unless explicitly checked. **Re-plan ask:** auto-orchestrator-spawn visibility surface in dashboard-ops (or wait-30-60s-then-check-assignee protocol enforced in brief).
3. **Review-infra 5-class cluster.** codex hallucination (tree-snapshot-staleness, 8+ instances), verdict-parser-mismatch on approving codex bodies, scheduler-not-retriggering on rapid HEAD moves, openai-pro browser-subprocess-gone, +1. Multiple PRs parked-on-stale-RC waiting for re-review. **Re-plan ask:** review-infra investigation pre-empts next dispatch wave; ideally codex retrigger fixed before resuming.
4. eval_node false-pass — folded into **T3**.
5. **Parallel-payload recurrence as CI lens, not doc.** Practice 11 sub-rule landed in modeling-discipline.md but parallel-payload defects kept surfacing. **Re-plan ask:** Practice 11 sub-rule should be a CI lens (when Lens-CI activation lands) rather than doc-discipline. Converts a human-must-remember rule into a structural CI check — gunbc eating its own dogfood.
6. **Scope-fence briefs accumulating debt.** PR #3500 spawned 4 substrate-evolution debt items; recurrence pattern shows briefs scoped TOO narrow. **Re-plan ask:** when reviewer-concurrence on N≥3 distinct scope-expansion findings hits a PR, the brief was under-scoped — operator-tier surface earlier.

### 5c. sharp-crab audit (7 themes)

1. **Verify-don't-trust at the manager tier (4 instances).** Folded into **T1**. Manager-tier verify-don't-trust matters as much as worker-tier.
2. **Cross-stage coverage gaps in briefs.** Folded into **T2**. Substrate-variant additions need exhaustive-pattern-match search across hand-Rust sites; #3505 SubValueRelation E0004 was this class.
3. **Ratification under-specification → multi-round follow-ups.** PR #3505 ratified PATH-2-FULL→(A) Inc-split; codex re-flagged same projection-monotonicity class on a different inhabitant (MixedTop) → A-tight follow-up needed. Structural ratifications based on a worker's first-cut proof may UNDERSPECIFY the property being protected. **Re-plan ask:** when ratifying substrate-extension shape, include pre-flight projection-monotonicity / law-coverage check across ALL inhabitants, not just the counterexample-driving one.
4. **Heartbeat-traffic discipline as systemic prompt issue.** 7 workers cc'd parent reflexively on dashboard idle-checks before learning etiquette — not 7 worker defects; a prompt-design issue. **Re-plan ask:** bake "dashboard idle-checks do NOT cc parent unless material-change" into worker spawn-context or every initial brief. System-fix, not per-worker.
5. **Squash-merge WIP-Revert masking content reads.** Read PR #3516 commit-message body as "reverted" when the tree state was the rename-as-intended. Worker iteration history in squash bodies isn't reliable for content state. **Re-plan ask:** verify tree state (`git show origin/main:path`, `git ls-tree`), never commit message body.
6. **Velocity-vs-care non-uniform across work types.** Substrate dissolution (lattices/FidelityDisposition/dedup/http_path) + concern-separation (cloud/llm/errors) were high-quality. Quality dipped in (a) semantic-shape decisions requiring iterative ratification (SubValueRelation/Inc-split), (b) coordination-paraphrase across lane-PM-operator chain, (c) cross-stage coverage assumptions in briefs.
7. **Priority-ladder needed for next batch.** Operator-direct shifts during the day were right calls (substrate-hardening → CI-runnability pivot via F1 catch was substantive). **Re-plan ask:** explicit prioritization ladder — substrate / runnability / correctness / coordination — so workers know which axis they're optimizing on, instead of every dispatch implicitly competing for the same priority slot.

### 5d. PM-observed (self-flag)

**Cross-lane fan-out gap.** I dispatched (b) `fold_list_right` ruling to jolly-eagle but didn't fan to smart-boar; smart-boar surfaced (b) as "operator-pending" in their snapshot, not knowing it had been ruled. Same family as T1. **Re-plan ask:** cross-lane rulings get fanned to ALL affected peer-managers, not just the dispatch-owner.

**Multiplicity overestimate (50-200 vs actual ~22-24).** When operator asked "what are the 50-200 primitives we had? i thought we had like 5-6?" my number was wrong — royal-eagle-196 corrected to ~22-24 declarations (Tier-A kernel). Operator's "5-6" intuition was closer. Pattern: pre-conversation estimates without grep-verification can drift an order of magnitude. **Re-plan ask:** numeric claims to operator get grep-backed unless explicitly framed as ballpark.

**Misframed substrate-fill scope.** Surfaced "611 placeholders" to operator without categorizing; smart-boar corrected to "5 real + 606 normal TCO temps," then on investigation to "0 real, milestone already met + CI-enforced." Two consecutive misframings; verify-don't-trust caught both. Same family as T1.

### 5e. Closure-leaf premature / orphan-detection (cross-source)

PM-observed + sharp-crab #5 (subtree-digest "+N quiet" ≠ "archived"): orphan-PR escalations need PR-merge-state check (`gh pr view --json state`) before drafting, not just session-graph absence. Existing memory file captures this; pattern recurred today.

---

## 6. Re-plan space

### Operator standing direction (2026-05-22)

**Dependency-sorted planning, not coordination protocol.** Plan minimizes cross-merge collisions by sequencing dependencies properly, not by enforcing protocols at CI / INVARIANTS / brief-template level. When B depends on A, plan A first or co-bundle. Holistic fixes for sub-classes (lens/review practices) develop on their own track and don't gate the re-plan.

### Sequencing questions

- **Closure priority.** Of facet 2 (v3 self-emit / full-tree v4 cargo-build), facet 3 (hand-Rust retirement campaign), facet 1 (rust→0 substrate completion), which is the nearest milestone you want to push first in the re-plan?
- **R1 vs v4-XL framing.** ROADMAP still carries the R1 nine-lane structure with v4-supersession-notes layered on. Worth a top-level update to make v4-XL the primary structure? Or wait until more lanes close?
- **MVP-N progression.** MVP-1 (substrate reduction) and MVP-2 (host-eval) both have CI gates green. Is there a defined MVP-3 / MVP-4 / etc., or do we shape that here?
- **Priority-ladder shape (per sharp-crab #7).** "Substrate / runnability / correctness / coordination" as the optimization axes? Or different framing?

— smart-lynx-142 (PM) · 2026-05-22
