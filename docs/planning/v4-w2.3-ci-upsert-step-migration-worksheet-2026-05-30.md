# W2.3 Worksheet — Phase 1.5: every CI step becomes `CiUpsertStep<T>`

> **Status:** **READY-FOR-WORKER-DISPATCH** — Modeling DFS Manager §8 sign-off 2026-05-30 (proud-pike-680).  
> **Wave:** W2.3 per `docs/planning/v4-merge-wave-and-next-waves-2026-05-30.md` §5.  
> **Owners:** Modeling DFS (worksheet + §8) + **Compiler Spine** (implementation).  
> **Prerequisites on `origin/main` (verified 2026-05-30):** `#3981` `Upsert<T>` in `dsl/std/patterns.dag`; `#3989` `CiUpsertStep<T>` / `UpsertInputRef` / receipt carriers in `src/v4/workflow/ci.dag`; W1.5 `CiSelectionReceipt` shadow (`ci_selection_receipt_shadow_heartbeat`, `ci_selection_receipt_well_formed`) @ `907936852`.

---

## Mechanical dispatch rule

> **No W2.3 implementation worker may land until this worksheet is Modeling DFS Manager–approved AND Compiler Spine agrees receipt + migration sequencing.**

Implementation remains **joint Compiler Spine** dispatch; Modeling DFS owns worksheet authority only.

**Compiler Spine sequencing (ratified in thread):** implementation workers **held** until `#4014` (SG-7) merges (MW-D8 #2) and W1.5 claims PR lands. Worksheet authoring is **not** blocked on those merges.

---

## §10.0-adapted worksheet (schema-migration class)

```text
Migration class:        W2.3-CI-UPSERT-STEP (every pipeline step as CiUpsertStep<T>)
Representative failure:  CI steps still modeled only as CiJob/CiCommand/CiGate rows;
                         cache/receipt logic cannot treat steps as Upsert<T> specializations;
                         shadow receipt enumerates step ids but step bodies lack UpsertInputRef.
Immediate local patch:   Hand-edit .github/workflows/ci.yml if: conditions per step;
                         or add parallel Rust selection in scripts/ without CiUpsertStep rows.
Why forbidden:           Re-creates hand-YAML authority (Phase 2 forbidden); bypasses
                         content_hash(step projection) cache discipline (Practice 10 / P2).
DFS path:
  dsl/std:               Upsert<T> canon (#3981)
  v4.workflow.ci:        CiUpsertStep<T>, UpsertInputRef, CiSelectionReceipt (#3989, W1.5)
  compiler spine:        T-24 emit of workflow; T-22 eval; shadow → active skip (Phase 2.5, out of scope)
  existing 🟡:           CiJob/CiGate/CiGateRunPolicy live-projection interim (gates #98/#100)
Deepest unsound boundary:
  Pipeline steps are not yet authored as CiUpsertStep<T> rows with typed UpsertInputRef inputs;
  receipt shadow proves partition/well-formed only over step *ids*, not upsert bodies.
Systemic fix:
  For every `CiStepId` in `ci_pipeline`, add a `CiUpsertStep<T>` row (per-step `T`) whose:
    - `inputs` uses only approved UpsertInputRef arms (v4-ci-schema-worksheet §1.3)
    - `verify` / `create` / `resolve` project current command semantics (no new parallel command vocab)
    - `cache_digest` = `content_hash(ci_upsert_step_projection_node(step))` — **Hash on main** (not planning-doc `Outcome<ContentHash>` amend)
  Keep `CiJob`/`CiGate` during staged migration (dual authority) with 🟡 dissolve-on when receipt + eval consume CiUpsertStep only.
Non-goals:
  - Phase 2.5 active skip / GHA if: dissolution (W2.3 is substrate migration only)
  - Phase 2 / generated ci.yml (W3.1)
  - Deleting CiJob/CiGate in the same PR as first upsert rows (staged dissolution)
  - Wall-clock CI speed claims as acceptance metric
Falsification probe:
  (1) `ci_pipeline_step_ids_shadow` bijection: every step id has exactly one CiUpsertStep row.
  (2) `ci_selection_receipt_well_formed` + shadow heartbeat still pass with upsert bodies present.
  (3) Two steps with identical `inputs` + different `create` → different `ci_upsert_step_cache_digest`.
  (4) Forbidden-pattern grep clean (v4-ci-schema-worksheet §4).
  (5) No new UpsertInputRef variant without Modeling DFS escalation.
Metric allowed only as secondary: skipped-step ratio, wall-clock.
```

---

## §1 Staging caveats (known, accepted for dispatch)

| Caveat | Disposition |
|--------|-------------|
| `cache_digest` on receipt rows uses **`Hash`** on main | Worksheet cites **main as-is**; `Outcome<ContentHash>` is interface-tighten follow-on, **not** a W2.3 gate |
| `CiJob` / `CiGate` remain alongside new upsert rows | **Staged dual authority** until shadow parity proven; 🟡 dissolve-on points at upsert-only consumption |
| `ci_upsert_step_upsert` bridge dropped on main (#3989) | Field-aligned `CiUpsertStep<T>` + `v4.std.patterns` mirror; migration uses projection fns only |
| Unit claims on fixture `ChangeSet` | Compiler Spine W1.5 close PR (separate); **not** blocking W2.3 worker start after #4014 |

**No unresolved design blockers** — RepoRoot, NodeQuery, CiStepId, UpsertInputRef coproduct were closed in `v4-ci-schema-worksheet-2026-05-30.md` §8 + #3989.

---

## §2 Per-step migration inventory (initial scope)

Migrate **`ci_pipeline`** steps only (data at `ci.dag` ~L360). Representative rows:

| CiStepId kind | Current carrier | UpsertInputRef arm (initial) | Notes |
|---------------|-----------------|------------------------------|-------|
| Job steps (fmt, affected, ci, v2/v3/v4, …) | `CiCommand` variants | `FileSet` / `SubstrateNodeSet` / `LensOutputRef` / `TestClaimRef` per command | Map each command to verify/create/resolve projection |
| Gate steps | `CiGate` + `CiGateRunPolicy` | `UpstreamUpsert` + carveout when policy = Always | Always → `ci_always_run_carveouts`, not `UpsertInputRef::Always` |

Worker PR must include a **step-id → CiUpsertStep** table in the PR body (variant-shape discipline per `v4-substrate-pr-review-gate-2026-05-30.md` §3 where coproducts change).

---

## §3 Downstream worker brief (post-§8)

```text
Land CiUpsertStep<T> rows for every ci_pipeline step id on main @ post-#4014.

MUST:
  - One CiUpsertStep row per CiStepId in ci_pipeline_step_ids_shadow
  - UpsertInputRef only (approved coproduct); carveouts via ci_always_run_carveouts
  - cache_digest via content_hash(ci_upsert_step_projection_node(...)) — Hash
  - Shadow receipt tests still pass; add collision-rejection probe (§10.0 #3)

MUST NOT:
  - Phase 2.5 active skip wiring
  - New UpsertInputRef variants without escalation
  - Hand ci.yml as authority
  - Delete CiJob/CiGate in W2.3 PR

Escalate to Modeling DFS:
  - Any step not expressible as UpsertInputRef without new variant
  - Any request to drop dual CiJob/CiGate before falsification passes

Pair with Compiler Spine manager on PR review.
```

---

## §4 Non-goals

- W2.4 atom-by-atom / `scripts/check-*` deletion  
- W2.6 python.dag leaf-model (separate worksheet)  
- SG-class emit work  

---

## §8 Manager approval — CLOSED 2026-05-30

- [x] §10.0 systemic fix + falsification probes approved  
- [x] Staging caveats (Hash, dual CiJob/CiGate) accepted  
- [x] No `blocked-on-decision` remainders  
- [x] **READY-FOR-WORKER-DISPATCH** (implementation gated by Compiler Spine: #4014 + W1.5 claims)  

---

## Related artifacts

- `docs/planning/v4-ci-schema-worksheet-2026-05-30.md` — types + UpsertInputRef authority  
- `docs/planning/v4-substrate-pr-review-gate-2026-05-30.md` — PR review discipline  
- `docs/planning/v4-upsert-t-substrate-worksheet-2026-05-30.md` — Phase 1.4  
- `src/v4/workflow/ci.dag` — `ci_pipeline`, shadow receipt fns  
