---
title: v4 Close Interrogation Validation
date: 2026-05-30
source: docs/v4-close-interrogation.md
validator: silent-raven-384
---

# v4 Close Interrogation Validation — 2026-05-30

This redo replaces the initial blanket-GAP pass and adopts the two-axis disposition vocabulary ratified in PR #3938 §10.0. Each probe below was evaluated from a codebase search against current `main` lineage and the PR #3941 branch. The close-ready headline is `0/346 PROVEN`; engineering evidence remains split as 233 substrate-present, 68 no-artifact-found, and 45 census/audit-not-run.

## Global close blocker

No probe is marked PROVEN. The common blocker is executable close evidence, not substrate absence: T-38/T-15 still say the claim corpus compiles but does not yet run through modeled `TestClaimRun` verdicts (`src/v4/TASKS.md:194`, `src/v4/TASKS.md:274`, `src/v4/TASKS.md:815`), `RoundTripClaim` eval returns `Deferred` (`src/v4/compiler/05_eval.dag:1732`), and the corpus runner is explicitly gated on T-38 (`src/v4/test/claim/workflow/testclaim_corpus_runner.dag:2`). Closure invariant for this artifact: a probe moves to `ship_disposition: PROVEN` only via executable receipt plus falsification transcript, never via substrate declaration alone.

## Summary

| Section | Probes | ship_PROVEN | ship_GAP | SUBSTRATE_PRESENT | NO_ARTIFACT_FOUND | CENSUS_NOT_RUN |
|---|---:|---:|---:|---:|---:|---:|
| §1.1 Complexity | 9 | 0 | 9 | 9 | 0 | 0 |
| §1.2 Cost | 15 | 0 | 15 | 6 | 9 | 0 |
| §1.3 Parallelism | 4 | 0 | 4 | 4 | 0 | 0 |
| §1.4 Effect enumeration | 4 | 0 | 4 | 3 | 1 | 0 |
| §1.5 User-defined dimensions | 7 | 0 | 7 | 3 | 4 | 0 |
| §1.6 Tier 1 mechanics (coercion = emission / ownership / grounding completeness) | 13 | 0 | 13 | 13 | 0 | 0 |
| §1.7 Tier 2 runtime safety (proven safe or total) | 8 | 0 | 8 | 6 | 0 | 2 |
| §2.1 Pure Bootstrap (zero hand-Rust) | 5 | 0 | 5 | 1 | 3 | 1 |
| §2.2 Closed system / no escape hatches | 5 | 0 | 5 | 0 | 0 | 5 |
| §2.3 Single authority / cost-of-change = 1 | 5 | 0 | 5 | 0 | 0 | 5 |
| §2.4 Fail-closed discipline | 4 | 0 | 4 | 2 | 0 | 2 |
| §2.5 Impossible bugs by construction (THE META-PROMISE) | 45 | 0 | 45 | 45 | 0 | 0 |
| §2.6 Substrate-shape specifics (6 connectives + 5 behaviors + C1 stop-signal) | 12 | 0 | 12 | 12 | 0 | 0 |
| §2.7 Modeling discipline | 16 | 0 | 16 | 11 | 0 | 5 |
| §3.1 Omni-emission (R3 = 3 Shape-A targets: Rust / Python / Go) | 4 | 0 | 4 | 4 | 0 | 0 |
| §3.2 Workflow-as-data | 5 | 0 | 5 | 5 | 0 | 0 |
| §3.3 Tests-as-data | 4 | 0 | 4 | 4 | 0 | 0 |
| §3.4 Full-stack-from-one-`.dag` — visceral 4-layer omni-emission + R4 framework substrate (FORWARD POINTER) | 4 | 0 | 4 | 4 | 0 | 0 |
| §3.5 L6 — every structural form compiles to every target | 5 | 0 | 5 | 5 | 0 | 0 |
| §3.6 L7 — operations obey declared algebraic laws | 5 | 0 | 5 | 5 | 0 | 0 |
| §3.7 The verification-machinery promises (testgen / integration / mocks / dry-run) | 27 | 0 | 27 | 15 | 12 | 0 |
| §3.8 Multi-program / network-coordinated emission from one `.dag` (FORWARD POINTER) | 7 | 0 | 7 | 7 | 0 | 0 |
| §4.1 Lens self-application | 4 | 0 | 4 | 4 | 0 | 0 |
| §4.2 Self-host fixed point | 4 | 0 | 4 | 4 | 0 | 0 |
| §4.3 Concept unifications | 10 | 0 | 10 | 10 | 0 | 0 |
| §5.1 5 substrate-gap classes closed | 6 | 0 | 6 | 1 | 4 | 1 |
| §5.2 v2 fully retired | 3 | 0 | 3 | 0 | 3 | 0 |
| §5.3 BridgeLedgerZero | 3 | 0 | 3 | 0 | 3 | 0 |
| §5.4 Compiler-as-data residual — "is the compiler pure data yet?" | 19 | 0 | 19 | 0 | 9 | 10 |
| §5.5 Free consequences (when Tiers 1-2 close) | 11 | 0 | 11 | 5 | 6 | 0 |
| §6.1 "Show the correct code" | 4 | 0 | 4 | 4 | 0 | 0 |
| §6.2 Audience duality / opt-in depth | 6 | 0 | 6 | 6 | 0 | 0 |
| §6.3 Adoption model — economics, not enforcement | 10 | 0 | 10 | 3 | 2 | 5 |
| §7 Cross-doc ledger coherence (structural — keep from v0) | 9 | 0 | 9 | 0 | 0 | 9 |
| §8 Per-gate predicate execution at close | 4 | 0 | 4 | 4 | 0 | 0 |
| §10 Close ceremony | 8 | 0 | 8 | 0 | 8 | 0 |
| §13 Arbitrary ingestion — bidirectional substrate (NEW for v4, was R4) | 9 | 0 | 9 | 9 | 0 | 0 |
| §14 Additional Shape A languages — C, C++, LLVM IR, TypeScript (NEW for v4, was R4.A/R4.C) | 5 | 0 | 5 | 5 | 0 | 0 |
| §15 Framework substrates — React, future UI/server frameworks (NEW for v4, was R4 canvas) | 4 | 0 | 4 | 3 | 1 | 0 |
| §16 Multi-program / network coordination (NEW for v4, was §3.8 forward-pointer) | 7 | 0 | 7 | 5 | 2 | 0 |
| §17.1 C4 — Additional MachineConstraint axes | 3 | 0 | 3 | 3 | 0 | 0 |
| §17.2 C5 — Rounding-mode product-shape extension | 2 | 0 | 2 | 2 | 0 | 0 |
| §17.3 C6 — Aspect-axis (PointKind) for instant/duration/rate | 2 | 0 | 2 | 1 | 1 | 0 |
| **TOTAL** | **346** | **0** | **346** | **233** | **68** | **45** |

## Ledger

### §1.1 Complexity

Section total: 9 probes — ship_disposition: 0 PROVEN, 9 GAP; engineering_state: 9 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:75` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` program that violates a complexity contract. Quote the code. | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:76` | `GAP` | `SUBSTRATE_PRESENT` | Compile it. What is the verbatim error message? (`Diagnostic` with `reason` + `at`, not `panic!` or silent skip.) | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:77` | `GAP` | `SUBSTRATE_PRESENT` | Show me a SECOND example with a different complexity class (linear/quadratic/exponential). | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:78` | `GAP` | `SUBSTRATE_PRESENT` | Where is the test that pins the error message? Cite `<file>:<line>`. | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:79` | `GAP` | `SUBSTRATE_PRESENT` | There must be NO complexity annotation/tag anywhere (feedback_no_annotations). Show that the bound is a STRUCTURAL requirement — propagated from the consuming position / algebra inhabitance — not a `@complexity`-style tag. If a tag exists, that is the violation. | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:80` | `GAP` | `SUBSTRATE_PRESENT` | Can I write a structure whose lens-DERIVED complexity contradicts the bound its structural context requires? Show the diagnostic. (There is no annotation to "lie" — the bug is structural inconsistency between derived cost and the position that demands the bound.) | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:81` | `GAP` | `SUBSTRATE_PRESENT` | Is there a demonstration `.dag` program that exercises Complexity end-to-end via the v3 evaluator? Cite path. | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:82` | `GAP` | `SUBSTRATE_PRESENT` | Run the demonstration on clean checkout. What does stdout/stderr say? | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:83` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a program whose complexity is wrong in a way the lens hasn't been tested against. Does the lens catch it? Or did we only test the cases we already knew worked? | Complexity substrate exists (`src/v4/lens/complexity.dag:15`, `src/v4/lens/complexity.dag:70`) and worked claims call `cost_lens`/`complexity_lens` (`src/v4/test/claim/lens_complexity/map_id.dag:1`, `src/v4/test/claim/lens_complexity/nested.dag:102`). Missing for PROVEN: runner-verdict output and adversarial complexity falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §1.2 Cost

Section total: 15 probes — ship_disposition: 0 PROVEN, 15 GAP; engineering_state: 6 SUBSTRATE_PRESENT, 9 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:91` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` program with a cost budget. Quote it. | Cost substrate and worked claims exist: `SymbolicCost` covers Constant/Linear/Log/Polynomial/PolyLog/Exponential/Factorial/Sum/Product/Unknown (`src/v4/lens/cost.dag:100`) and claims exercise nested composition (`src/v4/test/claim/lens_cost/nested_product.dag:43`). Missing for PROVEN: executed budget diagnostic and falsification run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:92` | `GAP` | `SUBSTRATE_PRESENT` | Show me the symbolic-cost output the lens produces. Verbatim. | Cost substrate and worked claims exist: `SymbolicCost` covers Constant/Linear/Log/Polynomial/PolyLog/Exponential/Factorial/Sum/Product/Unknown (`src/v4/lens/cost.dag:100`) and claims exercise nested composition (`src/v4/test/claim/lens_cost/nested_product.dag:43`). Missing for PROVEN: executed budget diagnostic and falsification run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:93` | `GAP` | `SUBSTRATE_PRESENT` | Show me a program that EXCEEDS its cost budget. What error fires? | Cost substrate and worked claims exist: `SymbolicCost` covers Constant/Linear/Log/Polynomial/PolyLog/Exponential/Factorial/Sum/Product/Unknown (`src/v4/lens/cost.dag:100`) and claims exercise nested composition (`src/v4/test/claim/lens_cost/nested_product.dag:43`). Missing for PROVEN: executed budget diagnostic and falsification run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:94` | `GAP` | `SUBSTRATE_PRESENT` | Where is the test? Cite path. | Cost substrate and worked claims exist: `SymbolicCost` covers Constant/Linear/Log/Polynomial/PolyLog/Exponential/Factorial/Sum/Product/Unknown (`src/v4/lens/cost.dag:100`) and claims exercise nested composition (`src/v4/test/claim/lens_cost/nested_product.dag:43`). Missing for PROVEN: executed budget diagnostic and falsification run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:95` | `GAP` | `SUBSTRATE_PRESENT` | Is the cost arithmetic actually composing? Show me a multi-level program (call within call) and the symbolic-cost result. | Cost substrate and worked claims exist: `SymbolicCost` covers Constant/Linear/Log/Polynomial/PolyLog/Exponential/Factorial/Sum/Product/Unknown (`src/v4/lens/cost.dag:100`) and claims exercise nested composition (`src/v4/test/claim/lens_cost/nested_product.dag:43`). Missing for PROVEN: executed budget diagnostic and falsification run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:99` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a √n algorithm (e.g., trial division primality). Cost-lens output should be `PolynomialCost { degree: 1/2 }`, NOT `UnknownCost`. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:100` | `GAP` | `NO_ARTIFACT_FOUND` | Show me an exponential algorithm (subset enumeration). Cost-lens output should be `ExponentialCost { base: 2 }`, NOT `UnknownCost`. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:101` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a factorial algorithm (permutation enumeration). Cost-lens output should be `FactorialCost`, NOT `UnknownCost`. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:102` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a polylog algorithm (e.g., repeated binary search log² n). Cost-lens output should be `PolyLogCost { exponent: 2 }`, NOT `UnknownCost`. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:103` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a matrix-multiplication cost program (n^2.373 Coppersmith-Winograd). Cost-lens output should be `PolynomialCost { degree: 2.373 }`, NOT `UnknownCost`. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:107` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a union-find program (α(n) inverse Ackermann). Currently expected: `UnknownCost("Tier 2 — pending R4 named-variant canvas")` OR `ConstantCost-with-named-reason` per Director rationale; NOT a Tier-1-coverable bound being squelched. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:108` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a vEB-trees program (log log n). Same expectation; nested-log structure may compose via PolyLogCost — canvas surfaces if yes. | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:112` | `GAP` | `NO_ARTIFACT_FOUND` | Program with recursive call whose cost is bounded by a Tier-3 fact. Does the lens compute the bound or punt? | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:113` | `GAP` | `SUBSTRATE_PRESENT` | Program with cost in a class deliberately NOT in Tier 1 (e.g., O(n^n) hyperexponential). Is `UnknownCost("reason")` actionable for the user? Does the diagnostic name the gap class? | `SymbolicCost` has an `UnknownCost` floor and lattice composition (`src/v4/lens/cost.dag:100`, `src/v4/lens/cost.dag:646`), but no Tier-3 recursive program receipt was found. Missing for PROVEN: a concrete Tier-3 input with actionable diagnostic output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:114` | `GAP` | `NO_ARTIFACT_FOUND` | **STOP-SIGNAL trigger**: program with a Tier-1-coverable bound (e.g., √n) — verify the cost-lens does NOT collapse to UnknownCost post-gate-#105-landing. Pre-gate-#105: expected current behavior is UnknownCost (regression test). | Searched `src/v4` for trial/sqrt/subset/factorial/polylog/matrix/union-find/vEB/Tier-3/hyperexponential examples; no concrete `.dag` target program or lens output was found, only questionnaire/planning text. | Delivery artifact or executable demo receipt for the requested probe. |
### §1.3 Parallelism

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:122` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` program where the parallelism lens identifies independent subgraphs. Quote the program. | Parallelism lens exists and classifies dependency kinds (`src/v4/lens/parallelism.dag:32`, `src/v4/lens/parallelism.dag:43`) with unresolved witness diagnostics (`src/v4/lens/parallelism.dag:75`). Missing for PROVEN: an executed independent-vs-fake-dependency demo. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:123` | `GAP` | `SUBSTRATE_PRESENT` | Show me the lens's output — which nodes are flagged parallel, which serial. | Parallelism lens exists and classifies dependency kinds (`src/v4/lens/parallelism.dag:32`, `src/v4/lens/parallelism.dag:43`) with unresolved witness diagnostics (`src/v4/lens/parallelism.dag:75`). Missing for PROVEN: an executed independent-vs-fake-dependency demo. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:124` | `GAP` | `SUBSTRATE_PRESENT` | If I add a fake dependency, does the lens correctly serialize? Demo. | Parallelism lens exists and classifies dependency kinds (`src/v4/lens/parallelism.dag:32`, `src/v4/lens/parallelism.dag:43`) with unresolved witness diagnostics (`src/v4/lens/parallelism.dag:75`). Missing for PROVEN: an executed independent-vs-fake-dependency demo. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:125` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: program with hidden state coupling that LOOKS independent. Does the lens report independent (wrong) or report state coupling (right)? | Parallelism lens exists and classifies dependency kinds (`src/v4/lens/parallelism.dag:32`, `src/v4/lens/parallelism.dag:43`) with unresolved witness diagnostics (`src/v4/lens/parallelism.dag:75`). Missing for PROVEN: an executed independent-vs-fake-dependency demo. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §1.4 Effect enumeration

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 3 SUBSTRATE_PRESENT, 1 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:133` | `GAP` | `SUBSTRATE_PRESENT` | Show me a program with mixed pure + I/O effects. What does the lens classify per location? | Effect lens substrate exists but is signature-deferred: effect kind is read from facts (`src/v4/lens/effect.dag:139`, `src/v4/lens/effect.dag:175`) and emits unresolved diagnostics (`src/v4/lens/effect.dag:226`). Missing for PROVEN: mixed pure/I-O executed fixture and contradiction diagnostic output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:134` | `GAP` | `SUBSTRATE_PRESENT` | Show me a "pure" claim on a function that secretly does I/O. Does the lens catch the contradiction? | Effect lens substrate exists but is signature-deferred: effect kind is read from facts (`src/v4/lens/effect.dag:139`, `src/v4/lens/effect.dag:175`) and emits unresolved diagnostics (`src/v4/lens/effect.dag:226`). Missing for PROVEN: mixed pure/I-O executed fixture and contradiction diagnostic output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:135` | `GAP` | `SUBSTRATE_PRESENT` | Where is the test fixture? | Effect lens substrate exists but is signature-deferred: effect kind is read from facts (`src/v4/lens/effect.dag:139`, `src/v4/lens/effect.dag:175`) and emits unresolved diagnostics (`src/v4/lens/effect.dag:226`). Missing for PROVEN: mixed pure/I-O executed fixture and contradiction diagnostic output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:136` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: effect-leaks through a callback or higher-order construct. Does the lens trace it? | Searched `src/v4/lens/effect.dag` and `src/v4/test/claim` for callback/higher-order effect-leak fixtures; no such falsification fixture was found. | Delivery artifact or executable demo receipt for the requested probe. |
### §1.5 User-defined dimensions

Section total: 7 probes — ship_disposition: 0 PROVEN, 7 GAP; engineering_state: 3 SUBSTRATE_PRESENT, 4 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:144` | `GAP` | `SUBSTRATE_PRESENT` | Show me ONE user-defined lens that lives in `.dag` (not Rust). Cite the file. | The lens-application surface is modeled in `.dag` with `Lens`, `EnforceableLens`, `LensApplicationConfig`, and enforce/advisory modes (`src/v4/lens/application.dag:1`, `src/v4/lens/application.dag:80`, `src/v4/lens/application.dag:97`). Missing for PROVEN: a user-authored lens compiled and run through the common runner. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:145` | `GAP` | `SUBSTRATE_PRESENT` | Show me a program it validates. Show the output. | The lens-application surface is modeled in `.dag` with `Lens`, `EnforceableLens`, `LensApplicationConfig`, and enforce/advisory modes (`src/v4/lens/application.dag:1`, `src/v4/lens/application.dag:80`, `src/v4/lens/application.dag:97`). Missing for PROVEN: a user-authored lens compiled and run through the common runner. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:146` | `GAP` | `SUBSTRATE_PRESENT` | Did the user-defined lens use the same mechanism as built-in? Or a parallel infrastructure? | The lens-application surface is modeled in `.dag` with `Lens`, `EnforceableLens`, `LensApplicationConfig`, and enforce/advisory modes (`src/v4/lens/application.dag:1`, `src/v4/lens/application.dag:80`, `src/v4/lens/application.dag:97`). Missing for PROVEN: a user-authored lens compiled and run through the common runner. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:147` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: would a NEW user-defined lens (one we haven't written yet) actually work end-to-end? Author a simple one. Does it compile and run? | `lens/application.dag` exposes the application surface, but no newly-authored lens was created and run for this validation; searched `src/v4/test/claim/lens_*` for an end-to-end new-lens run. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:151` | `GAP` | `NO_ARTIFACT_FOUND` | **If a user needs O(α(n)) or O(log log n)** — bounds NOT in Tier 1 of gate #105: can they author a user-defined cost-variant lens that integrates with `SymbolicCost`'s dominance lattice + Sum/Product algebra, OR does the user-defined-dim mechanism stop at producing a separate Dimension carrier with no algebra-integration? | Searched `src/v4/lens` and `src/v4/test/claim` for user-authored Tier-2 cost-variant integration with `SymbolicCost` dominance/Sum/Product; no inverse-Ackermann/log-log extension was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:152` | `GAP` | `NO_ARTIFACT_FOUND` | **Compositional vs named-variant**: if the user-defined-dim infrastructure supports compositional cost-bound extension (e.g., authoring a `LogLogCost` lens that participates in dominance), R3 may NOT need named-variants for Tier 2 (per Director caveat: "if Substrate Mgr canvas surfaces a compositional mechanism for Tier 2 that satisfies feedback_groundedness_gates_lenses + composes with Sum/Product algebra + carries consumer-evidence justification, accept that mechanism IN-R3 instead of named variants"). Test this end-to-end. | Searched `src/v4/lens` and `src/v4/test/claim` for user-authored Tier-2 cost-variant integration with `SymbolicCost` dominance/Sum/Product; no inverse-Ackermann/log-log extension was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:153` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe (Tier 2 escape hatch)**: write a user-defined cost lens for a textbook-known bound that's R4-deferred (e.g., inverse Ackermann). Does it integrate? If yes — Tier 2 R4-deferral is structurally bounded. If no — user-defined-dim promise has a load-bearing gap; surface as Director-tier scope question. | Searched `src/v4/lens` and `src/v4/test/claim` for user-authored Tier-2 cost-variant integration with `SymbolicCost` dominance/Sum/Product; no inverse-Ackermann/log-log extension was found. | Delivery artifact or executable demo receipt for the requested probe. |
### §1.6 Tier 1 mechanics (coercion = emission / ownership / grounding completeness)

Section total: 13 probes — ship_disposition: 0 PROVEN, 13 GAP; engineering_state: 13 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:165` | `GAP` | `SUBSTRATE_PRESENT` | Find the "coercion engine." Show me the file or argument-flow. Is it a separate phase, or does it dissolve into emission? | Coercion is modeled through `std/coercion.dag`, `find_witness`, and translate-stage `coerce_grounded_node` (`src/v4/std/coercion.dag:1`, `src/v4/std/find_witness.dag:222`, `src/v4/compiler/06_translate.dag:956`). Missing for PROVEN: a fail-closed no-inhabitance run with emitted diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:166` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` value being coerced to a target representation. Cite the call site. Is the coercion logic in emission, or in a separate `coerce.rs`? | Coercion is modeled through `std/coercion.dag`, `find_witness`, and translate-stage `coerce_grounded_node` (`src/v4/std/coercion.dag:1`, `src/v4/std/find_witness.dag:222`, `src/v4/compiler/06_translate.dag:956`). Missing for PROVEN: a fail-closed no-inhabitance run with emitted diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:167` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a `.dag` type with no target inhabitance. Does the compiler emit it via some default-coercion path, or fail-closed? | Coercion is modeled through `std/coercion.dag`, `find_witness`, and translate-stage `coerce_grounded_node` (`src/v4/std/coercion.dag:1`, `src/v4/std/find_witness.dag:222`, `src/v4/compiler/06_translate.dag:956`). Missing for PROVEN: a fail-closed no-inhabitance run with emitted diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:171` | `GAP` | `SUBSTRATE_PRESENT` | Show me where the compiler proves "no aliased mutation" in emitted code. Cite the proof site (lens, predicate, or test). | Ownership lens projects dependency classifications and fails closed for `RequiresAccessWitness`/`UnknownOwnership` (`src/v4/lens/ownership.dag:1`, `src/v4/lens/ownership.dag:32`, `src/v4/lens/ownership.dag:102`). Missing for PROVEN: an executed aliased-mutation program and diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:172` | `GAP` | `SUBSTRATE_PRESENT` | Show me an `.dag` program that would COMPILE if aliased mutation were allowed but FAILS now. Demonstrate the diagnostic. | Ownership lens projects dependency classifications and fails closed for `RequiresAccessWitness`/`UnknownOwnership` (`src/v4/lens/ownership.dag:1`, `src/v4/lens/ownership.dag:32`, `src/v4/lens/ownership.dag:102`). Missing for PROVEN: an executed aliased-mutation program and diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:173` | `GAP` | `SUBSTRATE_PRESENT` | Where is "aliasing" modeled in the substrate? Is it a Behavior shape, a Cardinality property, or implicit-via-purity? | Ownership lens projects dependency classifications and fails closed for `RequiresAccessWitness`/`UnknownOwnership` (`src/v4/lens/ownership.dag:1`, `src/v4/lens/ownership.dag:32`, `src/v4/lens/ownership.dag:102`). Missing for PROVEN: an executed aliased-mutation program and diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:174` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a `.dag` program that mutates the same logical resource from two call sites. Does the compiler reject it, or emit potentially-aliasing target code? | Ownership lens projects dependency classifications and fails closed for `RequiresAccessWitness`/`UnknownOwnership` (`src/v4/lens/ownership.dag:1`, `src/v4/lens/ownership.dag:32`, `src/v4/lens/ownership.dag:102`). Missing for PROVEN: an executed aliased-mutation program and diagnostic. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:178` | `GAP` | `SUBSTRATE_PRESENT` | Show me the structural model of Rust primitives. Cite `dsl/extdeps/languages/rust/primitives.dag` (or wherever). Does each Rust primitive have algebra-inhabitance declared (e.g., `i32 inhabits OrderedRing`)? | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:179` | `GAP` | `SUBSTRATE_PRESENT` | Same for Python data model. Same for Go specification. Cite both. | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:180` | `GAP` | `SUBSTRATE_PRESENT` | Show me the algebra-homomorphism-search code path. Cite the function. Is it a structural search, or a name-keyed table lookup? | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:181` | `GAP` | `SUBSTRATE_PRESENT` | Pick a target primitive at random. Trace the grounding chain: `.dag` type → algebra inhabitance → target primitive. Is every step structural? | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:182` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a `.dag` type carrying algebra X. No target language inhabits X. Does the compiler fail-closed with a named diagnostic? Or does it pick a "closest" target primitive (failure mode — name-keyed shortcut)? | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:183` | `GAP` | `SUBSTRATE_PRESENT` | **Per-target falsification**: for each of Rust / Python / Go, find a `.dag` algebra with no target inhabitance. Verify fail-closed on each. | Target primitive substrate exists: Rust/Python/Go language models declare scalar carriers and algebra-inhabitance axes (`src/v4/extdeps/languages/rust.dag:1`, `src/v4/extdeps/languages/rust.dag:52`, `src/v4/extdeps/languages/python.dag:1`, `src/v4/extdeps/languages/go.dag:1`); grounding search uses canonical grounding and witness paths (`src/v4/compiler/04_infer.dag:321`, `src/v4/compiler/06_translate.dag:962`). Missing for PROVEN: per-target no-inhabitance falsification and emitted fail-closed diagnostics. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §1.7 Tier 2 runtime safety (proven safe or total)

Section total: 8 probes — ship_disposition: 0 PROVEN, 8 GAP; engineering_state: 6 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 2 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:193` | `GAP` | `SUBSTRATE_PRESENT` | **Division by zero**: show me a `.dag` program with a division. Show the divisor's proven-non-zero predicate (or the total-form making it safe). Cite the lens / check. | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:194` | `GAP` | `SUBSTRATE_PRESENT` | **Integer overflow**: show me a `.dag` arithmetic expression whose target-side overflow is bound by structural analysis. Cite the cost-lens-or-machine-constraint composition. | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:195` | `GAP` | `SUBSTRATE_PRESENT` | **Out-of-bounds**: show me a `.dag` indexed access. Show the index's proven-in-range predicate (or the total-form making OOB unrepresentable). | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:196` | `GAP` | `SUBSTRATE_PRESENT` | **Force-unwrap**: search for force-unwrap patterns in the `.dag` substrate. Should be zero in the language surface. If present, justify as user-input-boundary. | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:197` | `GAP` | `CENSUS_NOT_RUN` | **Partial functions generally**: enumerate every partial primitive operation in `dsl/std/`. For each, show the totalization (Option-return / Diagnostic / refinement-precondition). | Requires a complete partial-primitive census across `src/v4/std`; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:201` | `GAP` | `SUBSTRATE_PRESENT` | Author a `.dag` program with division where the divisor's non-zero predicate is unprovable from structure. Does the compiler reject (Tier 1) or insert a runtime check (Tier 2 total)? | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:202` | `GAP` | `SUBSTRATE_PRESENT` | Author an integer-arithmetic program where overflow CANNOT be proven safe. Does the compiler reject or insert a Diagnostic-returning total form? | Runtime-safety substrate uses total carriers, diagnostics, and refinements across `std` plus canonical grounding checks (`src/v4/std/collection.dag`, `src/v4/std/diagnostic.dag`, `src/v4/compiler/04_infer.dag:180`). Missing for PROVEN: class-by-class executed division/overflow/OOB/unwrap falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:203` | `GAP` | `CENSUS_NOT_RUN` | If a partial primitive lands in `dsl/std/` post-R3, what catches it? (Should be a §1.8 ratchet or anti-pattern; cite.) | Requires a complete partial-primitive census across `src/v4/std`; not completed in this redo window. | Complete targeted audit/census run for this probe. |
### §2.1 Pure Bootstrap (zero hand-Rust)

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 1 SUBSTRATE_PRESENT, 3 NO_ARTIFACT_FOUND, 1 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:217` | `GAP` | `NO_ARTIFACT_FOUND` | Run the PB-0 census predicate. What's the count? Should be 0. | Local PB-0 census proxy found 242 apparent hand-authored `.rs` files under `src/v3` (`find src/v3 -name '*.rs' ! -name '*_generated.rs' | wc -l`), so the zero target is not met. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:218` | `GAP` | `NO_ARTIFACT_FOUND` | If non-zero: enumerate each survivor. Justify each as named-retirement-schedule. | The survivor set is non-zero (242); first examples include `src/v3/grounding_cross_target_meta/src/closure_ledger_gate.rs`, `src/v3/grounding_cross_target_meta/src/cells.rs`, and `src/v3/grounding_tests/src/lib.rs`. This audit did not find a complete per-survivor retirement schedule. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:219` | `GAP` | `CENSUS_NOT_RUN` | If zero: when was census last run? Cite SHA. | The zero condition is false under the local census (242 hand-authored `.rs` files); no zero-census SHA was applicable in this pass. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:220` | `GAP` | `NO_ARTIFACT_FOUND` | Compile gunbc with itself end-to-end. Does the output match a reference build? Bit-for-bit, or just behavioral? | Searched workflow and self-host claims: `self_host.dag` has witness-body scaffold (`src/v4/compiler/self_host.dag:51`) but no bit-for-bit self-host run artifact at current HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:221` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a new compiler feature in pure `.dag` (no Rust). Does it self-host? | PB/self-host substrate and task tracking exist (`src/v4/compiler/self_host.dag:1`, `src/v4/TASKS.md:815`), but this audit did not author a new pure-`.dag` compiler feature or produce a self-host result for it. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §2.2 Closed system / no escape hatches

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 5 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:229` | `GAP` | `CENSUS_NOT_RUN` | grep for `unsafe` in `src/v3/`. Count: 0 expected. | This probe requires live census over generated/bootstrap Rust survivors; not rerun to completion in this redo. Existing project blocker cites partial T-15/T-38 close status (`src/v4/TASKS.md:815`). | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:230` | `GAP` | `CENSUS_NOT_RUN` | grep for `panic!()` outside narrow boundaries. Where are the survivors? Justify each. | This probe requires live census over generated/bootstrap Rust survivors; not rerun to completion in this redo. Existing project blocker cites partial T-15/T-38 close status (`src/v4/TASKS.md:815`). | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:231` | `GAP` | `CENSUS_NOT_RUN` | grep for annotations (`#[...]` on user-facing constructs). Should be zero per `feedback_no_annotations`. | This probe requires live census over generated/bootstrap Rust survivors; not rerun to completion in this redo. Existing project blocker cites partial T-15/T-38 close status (`src/v4/TASKS.md:815`). | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:232` | `GAP` | `CENSUS_NOT_RUN` | grep for metadata markers (`__is_X` strings). Should be zero per `feedback_no_metadata_markers`. | This probe requires live census over generated/bootstrap Rust survivors; not rerun to completion in this redo. Existing project blocker cites partial T-15/T-38 close status (`src/v4/TASKS.md:815`). | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:233` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: write a program that tries to "leave the stack" via an opaque-string back-channel. Does the compiler accept it? | This probe requires live census over generated/bootstrap Rust survivors; not rerun to completion in this redo. Existing project blocker cites partial T-15/T-38 close status (`src/v4/TASKS.md:815`). | Complete targeted audit/census run for this probe. |
### §2.3 Single authority / cost-of-change = 1

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 5 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:241` | `GAP` | `CENSUS_NOT_RUN` | Add a new built-in type to the language. How many files change? Show the diff line count per file. | Requires PR-history or edit-distance experiment; no bounded local code search can validate the claimed O(1)/cost-of-change metric. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:242` | `GAP` | `CENSUS_NOT_RUN` | Add a new expression form. Same question. | Requires PR-history or edit-distance experiment; no bounded local code search can validate the claimed O(1)/cost-of-change metric. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:243` | `GAP` | `CENSUS_NOT_RUN` | Add a new emission target (or extend an existing one). Same question. | Requires PR-history or edit-distance experiment; no bounded local code search can validate the claimed O(1)/cost-of-change metric. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:244` | `GAP` | `CENSUS_NOT_RUN` | If the count is >1 for any case: name the files, explain WHY they need editing. | Requires PR-history or edit-distance experiment; no bounded local code search can validate the claimed O(1)/cost-of-change metric. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:245` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: pick a recent PR that added a feature. Re-do the change. Was it ACTUALLY 1 file, or were there hidden auxiliary edits? | Requires PR-history or edit-distance experiment; no bounded local code search can validate the claimed O(1)/cost-of-change metric. | Complete targeted audit/census run for this probe. |
### §2.4 Fail-closed discipline

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 2 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 2 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:253` | `GAP` | `CENSUS_NOT_RUN` | grep for `Option::None` returns in lens read channels. Should be zero (replaced with `Witness<C>::Violates`). | Requires a targeted grep/count for `Option::None` in lens read channels; not completed. The diagnostic substrate exists, but this probe asks for census evidence. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:254` | `GAP` | `CENSUS_NOT_RUN` | grep for `unreachable!()` outside truly-impossible-by-construction paths. Justify each survivor. | Requires a targeted grep/count and survivor justification for `unreachable!()`; not completed. The diagnostic substrate alone does not answer this census probe. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:255` | `GAP` | `SUBSTRATE_PRESENT` | Trigger an error condition at runtime. Is the result a `Diagnostic` with `reason: <named>` + `at: <port_or_node>`, or something less structured? | Fail-closed diagnostic substrate is modeled via `Outcome`, `Diagnostic`, and witness violations (`src/v4/std/diagnostic.dag`; examples in `src/v4/lens/ownership.dag:94`, `src/v4/std/find_witness.dag:62`). Missing for PROVEN: executed demo showing diagnostic instead of panic/skip for each asked case. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:256` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a malformed program. Does the compiler emit a clear diagnostic that points to the structurally correct alternative (per THESIS.md:103-105 "Show the correct code")? | Fail-closed diagnostic substrate is modeled via `Outcome`, `Diagnostic`, and witness violations (`src/v4/std/diagnostic.dag`; examples in `src/v4/lens/ownership.dag:94`, `src/v4/std/find_witness.dag:62`). Missing for PROVEN: executed demo showing diagnostic instead of panic/skip for each asked case. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §2.5 Impossible bugs by construction (THE META-PROMISE)

Section total: 45 probes — ship_disposition: 0 PROVEN, 45 GAP; engineering_state: 45 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:266` | `GAP` | `SUBSTRATE_PRESENT` | Enumerate the bug classes the architecture claims are impossible **against canonical authority** (THESIS.md:370-413 "Enumerable impossible-bug classes" + ROADMAP.md:93 T-Demo R1/R2+ split — NOT a PM-fabricated count). At HEAD, THESIS commits to: **[R1]** Suboptimal-complexity contract violation + Idempotency-contract violation + Transport/type drift; **[R2+]** Nested-optional flatten + Unenumerated effects + Unhandled diagnostic paths. R3 close audit MUST verify against the canonical THESIS enumeration, NOT a session-author guess. For each [R1] class: cite the substrate fact that prevents it + the demo fixture (per ROADMAP T-Demo). For each [R2+] class: confirm V4-IN-SCOPE per operator ratification 2026-05-15 ("Please frontload R2 into v4 — and any other impossible-bug classes"); each R2+ class has a TestClaim scaffold in `src/v4/test/claim/impossible_bug/`. **Anti-pattern**: hard-coding a candidate list at audit-author time rather than deferring to THESIS authority — `feedback_thesis_gate_state_drift`-class miss; classes may have been added/removed in THESIS since the audit was written. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:267` | `GAP` | `SUBSTRATE_PRESENT` | For each: write a `.dag` program that ATTEMPTS the bug-class. Does the compiler refuse to compile? Show the diagnostic. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:268` | `GAP` | `SUBSTRATE_PRESENT` | **Discrimination probe**: is "impossible" actually "(a) impossible to express in surface vocabulary" OR "(b) caught at compile time by lens"? They differ. (a) means the bug-shape has no syntactic form. (b) means the bug compiles syntactically but fails a check. Both prevent the bug at user-visible level; only (a) prevents the bug-shape from existing in the substrate at all. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:269` | `GAP` | `SUBSTRATE_PRESENT` | **Compiler-correctness gating probe**: if the prevention is "caught at compile" (b-shape), what happens if the compiler itself has a bug? Is the prevention then defective? How is the compiler's correctness gated? (R3 close pointer: PB-self-compile fixed point + lens self-application.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:270` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: name ONE bug class the architecture historically claimed was "impossible" but turned out to have an instance. What was the cause — structural gap, lens gap, modeling error, or implementation bug? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:276` | `GAP` | `SUBSTRATE_PRESENT` | **Glue #1 — substrate (`.dag`) → emit target (Rust/Python/Go)**: How does the system PROVE that emitted target output faithfully realizes substrate semantics? Cite the proof. Show a concrete example. (R3 anchor: §3.1 L4 runtime equivalence + §3.1 L6 EmissionPathProjection data-coverage.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:277` | `GAP` | `SUBSTRATE_PRESENT` | **Glue #2 — emitter ↔ runtime**: even if substrate is correct + emission faithful, runtime behavior (memory layout, scheduler, OS, language-runtime) may diverge. Where does the architecture handle these vs. assume them? Where does "impossible by construction" become "trust the runtime"? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:278` | `GAP` | `SUBSTRATE_PRESENT` | **Glue #3 — lens composition** (lens A output → lens B input): if lens A produces `WitnessA<C>` and lens B reads it as input, what enforces structural compatibility? (R3 anchor: `Witness<C>` shape lock + `Lens<C>` per-Behavior typed channel per design-lens-framework.md.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:279` | `GAP` | `SUBSTRATE_PRESENT` | **Glue #4 — bootstrap** (v3 substrate ↔ stage0 Rust): if stage0 has a bug that affects v3 substrate generation, is the bug caught by `pb_self_compile_fixed_point`? If stage0's generated code is wrong but compiles cleanly, what catches it? (R3 anchor: gate #16 self-compile fixed point + gate #18 numeric_width_refinements.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:280` | `GAP` | `SUBSTRATE_PRESENT` | **Glue #5 — `ExecuteCommand` PB-Runtime boundary**: external-toolchain boundary tests run via `ExecuteCommand`. The boundary itself is opaque to the lens framework. Is THAT a glue bug class, or is "calling external thing is by-definition outside scope"? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:281` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: identify a glue boundary in the current architecture with NO structural enforcement of correctness — just convention, comment, or runtime assertion. List 3 concrete examples at HEAD. Is each an "impossible bug" class, or a class that's still possible just by being at an interface? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:287` | `GAP` | `SUBSTRATE_PRESENT` | **Intent vs. spec**: write a `.dag` program that compiles cleanly + satisfies ALL lens contracts + does something the user obviously didn't intend (e.g., `sort_descending` named but body is `sort_ascending`; both type-correct). How many "impossible bug" classes does this hit? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:288` | `GAP` | `SUBSTRATE_PRESENT` | **Wrong contract**: if the user's CONTRACT (Lens enforcement budget, structural cost bound, algebra/effect inhabitance — all compositional, no annotations) is wrong, the compiler accepts compliance with the wrong contract. How is the CONTRACT'S correctness checked? Or is that meta-level out-of-scope by design? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:289` | `GAP` | `SUBSTRATE_PRESENT` | **Empty program**: an always-correct but useless program (`fn main = unit`). Does the system distinguish "no work" from "intended no work"? Or is "user wrote what they meant" axiomatic? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:290` | `GAP` | `SUBSTRATE_PRESENT` | **Spec-as-program collapse**: when spec and implementation are the same artifact (.dag), is the user error "wrote wrong spec" identical to "wrote wrong program"? Does the architecture's "no parallel authority" discipline mean user-error is single-point-of-failure rather than divergence-detectable? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:291` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: enumerate 3 concrete user-error classes the architecture can NEVER catch by construction. Confirm that "impossible bugs by construction" is shorthand for "structurally-defined bug classes are impossible", not "all user errors are impossible". | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:297` | `GAP` | `SUBSTRATE_PRESENT` | **Lens-composition emergent**: lens A + lens B individually correct + composed in unexpected order. Show 2 lenses where composition order MATTERS; demonstrate ordering effect on output. (R3 anchor: `Lens<C>` typed per-Behavior channels per design-lens-framework.md — is ordering structural or behavioral?) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:298` | `GAP` | `SUBSTRATE_PRESENT` | **Scale emergent**: 1000-program corpus where each is individually correct but together produce a performance / cost / behavior surprise (e.g., aggregate cost dominates per-program cost; cross-program coupling). What's the architecture's tooling for catching scale-only bugs? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:299` | `GAP` | `SUBSTRATE_PRESENT` | **Time-evolving emergent**: substrate at time T compiles cleanly + at time T+1 (with new dependencies / extdeps revision) compiles differently or with different cost. Is the divergence caught? (R3 anchor: `feedback_thesis_gate_state_drift` — gate state drifts over time.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:300` | `GAP` | `SUBSTRATE_PRESENT` | **Lens-set silent gap**: a 2-lens composition where the composition reports "all lenses green" but the actual program behavior is wrong because a third dimension was needed (and not modeled). Does the architecture's "lenses are folds over physics" claim catch THIS, or does "all modeled lenses green" silently mean "the modeled dimensions are individually green; jointly silent on unmodeled dimensions"? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:301` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: construct a `.dag` program where 4 individual lens claims (complexity, cost, parallelism, effect) all pass cleanly + the program is observably wrong (e.g., wrong arithmetic, wrong I/O sequence). How many such constructions exist? Is the architecture's response "model the missing dimension" or "user error is out-of-scope"? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:311` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module effect-leak through "pure" boundary**: Module A declares `compute_stats(x)` as pure. Module B's helper it calls writes to disk. Traditional compilers catch only if Module A is RE-ANNOTATED. gunbc's effect lens folds over the full Dag — Module B's I/O propagates to A's call sites structurally without annotation maintenance. (R3 anchor: gate #82 `effect_enumeration_lens_behaviorally_complete`.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:312` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module cost-composition emergence**: Module A wraps `each(items, \|x\| Module_B.process(x))` thinking it's O(n). Module B's `process` is O(n²) internally. Traditional: zero visibility into composed cost. gunbc: cost lens reads algebra + realization across modules — `Cost(A.each) = Cost(B.process) · n` falls out structurally. (R3 anchor: gates #70 `cost_lens_demonstration` + #105 `symbolic_cost_textbook_coverage_landed`.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:313` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module dimensional drift / unit confusion**: Module A's `Time` is "ms since epoch"; Module B's `Time` is "ns since epoch". Both ground to `i64`. Traditional: type system doesn't carry the unit. gunbc: dimensional fact lives in the algebra/witness — `Time<MS>` and `Time<NS>` are structurally different even when both ground to the same primitive. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:314` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module ordering / sequencing assumption**: Module A: `validate(x); save(x)`. Module B refactors to `save(x); validate(x)`. Traditional: zero structural way to express ordering invariant. gunbc: the Dag IS a structural ordering; "save must follow validate" is a relation on Node identities. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:315` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module callback effect-set drift**: Module A's `with_lock(callback)` assumes callback is pure. Module B's callback later acquires another lock → deadlock class. Traditional: callback contracts are convention. gunbc: callback's effect-set is structurally readable by lens framework. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:316` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module aliasing / shadow definition**: Module A: `const MAX_RETRIES = 3`. Module B re-declares `MAX_RETRIES = 5`. Traditional: name collision detected only at link time. gunbc: P1 single-authority refuses parallel definitions structurally — second declaration is a compile-time INVARIANTS-P1 violation. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:317` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module data-flow capability leak**: Module A's `read_secrets()` returns `Secret<String>`. Module B's caller strips into plain `String` via implicit conversion. Traditional: capability typing if you have it (most don't). gunbc: Secret is a structural carrier in the algebra DAG; downcasting requires explicit lens application, structurally trackable. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:323` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target serialization round-trip**: Module A (emitted Rust) sends `User { id, name, email }` over wire to Module B (emitted Go). Both emissions derive from the same `.dag` substrate. Field rename in `.dag` → both emissions update structurally; no schema-drift class possible. **Falsification**: rename a field in `.dag`; verify both Rust + Go (+ Python) emissions update; observe any cross-target consumer that didn't rebuild. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:324` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target numeric width**: `.dag` declares `Counter: Nat<32>`. Rust emits `u32`; Go emits `uint32`; Python emits arbitrary-precision `int`. At `Counter > 2^32`, Rust wraps/panics, Go wraps, Python silently widens. Traditional: zero cross-language analysis. gunbc: cost-lens + dimensional carrier reads ALL THREE emissions' realization cost — Python's arbitrary-precision `int` carries different overflow semantics than Rust's `u32` or Go's `uint32`, structurally expressible per dim-substrate (R3 anchor: gate #18 `numeric_width_refinements_landed` + Q-MachineConstraint-Carrier). | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:325` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target effect divergence**: `.dag` declares an `async` operation. Rust emits via `tokio` futures; Go emits via `goroutines + channels`; Python emits via `asyncio`. Cancellation/coroutine semantics differ across all three. Traditional: each emission target writes its own async-handling; cross-target tests catch only late. gunbc: substrate models async as structural fact; per-target LanguageSpec encodes realization; cost-lens reads composition. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:326` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target boundary trust**: Rust service calls Go service via gRPC / HTTP / FFI / shared-memory. Type marshaling at the boundary. Traditional: protobuf-like schema at best (post-hoc). gunbc: BOTH ends derive from same `.dag` declaration; marshaling is structural emission per target. (Rust ↔ Python via PyO3 / Rust ↔ Go via cgo are alternate boundary realizations of the same probe.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:327` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target test-claim transferability**: `.dag` TestClaim asserts behavior X. Rust emission runs it via `cargo test`; Python via `pytest`; Go via `go test`. All three should pass-or-fail identically for the SAME claim. Traditional: tests are language-specific; cross-language test-claims don't exist. gunbc: per gate #15 `l5_cross_target_consistency` — for every `.dag` program, emitted Rust/Python/Go produce equivalent runtime behavior on the certification corpus. (R3 close anchor — see §3.1 for L5 status.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:331` | `GAP` | `SUBSTRATE_PRESENT` | **Pick a `.dag` program** that compiles + lens-passes cleanly + emit to all 3 R3 targets (Rust/Python/Go). Run the same fixture through each. Do outputs agree on every (input, output) pair? Where they don't agree, is the divergence: (a) a target-realization fidelity gap caught by L5 lens, (b) a substrate ambiguity (modeling-level error), or (c) a target-LanguageSpec drift (extdeps issue)? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:332` | `GAP` | `SUBSTRATE_PRESENT` | **Construct a cross-language wire scenario**: emit Rust client + Go server (or Python server) from the same `.dag` substrate. Have client send `User { ... }` to server; verify field-rename propagation, type-marshaling, async-cancellation semantics. Does the structural emission preserve all the facts the lens framework reads? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:333` | `GAP` | `SUBSTRATE_PRESENT` | **Modeling-level cross-target gap**: identify a target-language-specific bug class (e.g., Go's nil-interface vs nil-concrete-type distinction, Rust's lifetime-pin-projection, Python's GIL-aware threading) that does NOT have substrate representation. Confirm the architecture's response: "model the missing dimension" or "target-specific gap is in extdeps lane". | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:392` | `GAP` | `SUBSTRATE_PRESENT` | Where does affected-set live in CI/build? Cite the workflow file + the invocation site. Is it `gunbc query affected-set --since=main`? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:393` | `GAP` | `SUBSTRATE_PRESENT` | What's the SHA-diff input shape? `(Dag_before, Dag_after)` per design doc, or a different surface? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:394` | `GAP` | `SUBSTRATE_PRESENT` | Show me a concrete subtle dep that **ONLY** the dimension-parameterized variant catches — a case where value-only-diff would miss the affected-set but cost-or-complexity-diff catches it. Cite the test fixture. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:395` | `GAP` | `SUBSTRATE_PRESENT` | Run affected-set on the last 5 merged PRs. What's the cardinality of the affected-set per PR? (Should be much smaller than transitive-downstream — that's the structural progress claim.) | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:396` | `GAP` | `SUBSTRATE_PRESENT` | Show me a PR where the affected-set predicted a test would run, AND the test caught a regression that value-only-diff would have skipped. | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:400` | `GAP` | `SUBSTRATE_PRESENT` | **Cost-regression-in-uncovered-helper**: introduce a complexity regression in a helper (e.g., O(n) → O(n²)) that no test directly covers. Does affected-set flag every downstream consumer reading the cost dimension? Or does it miss them because no test directly asserts cost on the helper? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:401` | `GAP` | `SUBSTRATE_PRESENT` | **Effect-leak-across-module-boundary**: add a side effect (I/O) to a previously-pure function. Does affected-set flag the consumers asserting purity? Or only the consumers calling the function directly? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:402` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-module dimension narrowing**: change function F's cost from `O(n)` to `O(n log n)` in a way the lens classifies correctly. Does the affected-set surface every transitive consumer reading the cost dimension, AT LEAST as far as the dimension flow propagates? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:403` | `GAP` | `SUBSTRATE_PRESENT` | **PB-Runtime opacity**: introduce a change inside PB-Runtime (the bounded kernel) that affects a structural-dimension claim. Does the affected-set lens correctly identify the dimension-flow boundary at PB-Runtime, or does it under/over-propagate? | Impossible-bug and lens claim corpus exists under `src/v4/test/claim/impossible_bug/*` and generated corpus files; T-14 says all six classes landed with execution deferred (`src/v4/TASKS.md:734`). Missing for PROVEN: runner verdicts for each bug class and adversarial negative fixtures. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §2.6 Substrate-shape specifics (6 connectives + 5 behaviors + C1 stop-signal)

Section total: 12 probes — ship_disposition: 0 PROVEN, 12 GAP; engineering_state: 12 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:487` | `GAP` | `SUBSTRATE_PRESENT` | Enumerate every type at HEAD that is structurally NOT one of the 6 connectives (or composed of them). Should be ZERO. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:488` | `GAP` | `SUBSTRATE_PRESENT` | For each of the 6 connectives, cite ONE concrete use case at HEAD (e.g., `Conj` for product types, `Arrow` for function types, `Cardinality` for refinement, `Instantiation` for template-instantiation). | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:489` | `GAP` | `SUBSTRATE_PRESENT` | Where is "Instantiation matches C++ template-instantiation vocabulary and is ONLY used for type parameterization" enforced? Show me a value-construction site — does it use plain `Conj` with optional inhabits tag (per THESIS.md:199), not `Instantiation`? | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:490` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: try to author a type at HEAD that resembles a 7th connective (e.g., a "metadata" or "reflection" variant outside the 6). Does the substrate reject it, or absorb it via one of the 4 dissolution patterns? | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:494` | `GAP` | `SUBSTRATE_PRESENT` | Enumerate every Behavior at HEAD that is structurally NOT one of the 5 (Value / Transform / Branch / Loop / Bind). Should be ZERO. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:495` | `GAP` | `SUBSTRATE_PRESENT` | For each of the 5, cite ONE concrete use case at HEAD. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:496` | `GAP` | `SUBSTRATE_PRESENT` | Where is `Transform → FunctionRef → Arrow` composition enforced (per THESIS.md:201)? Cite the substrate connection. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:497` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: try to author a behavior at HEAD that doesn't fold into the 5 (e.g., a "guard" or "interrupt" variant). Does the substrate reject it, or absorb it? | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:501` | `GAP` | `SUBSTRATE_PRESENT` | Has anyone proposed a 7th connective or 6th behavior in PRs / canvases since R1? Cite each. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:502` | `GAP` | `SUBSTRATE_PRESENT` | For each proposal, was the C1 stop-signal protocol followed (all 4 dissolution patterns attempted before extension)? Cite the canvas / rationale. | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:503` | `GAP` | `SUBSTRATE_PRESENT` | What's the current count of dissolution patterns documented in §"Structural decompression"? (Should be 4 per THESIS.md.) | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:504` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: an R4 proposal arrives proposing a 7th connective. What's the structural gate that ensures it goes through the 4-dissolution-attempt protocol? Is this a §1.8 ratchet or an unwritten norm? | Core substrate shape exists in `src/v4/std/node.dag`, `src/v4/std/connective.dag`, `src/v4/std/behavior.dag`, and generated corpus claims; evaluation still defers structured runner closure (`src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: one complete C1-to-verdict replay and falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §2.7 Modeling discipline

Section total: 16 probes — ship_disposition: 0 PROVEN, 16 GAP; engineering_state: 11 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 5 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:521` | `GAP` | `CENSUS_NOT_RUN` | Run the unused-types lens (or grep for un-referenced `type` declarations). What's the count? Should be 0. | Quantitative ratchet/census probe not completed for this redo; requires full-tree counting policy rather than spot substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:522` | `GAP` | `SUBSTRATE_PRESENT` | Pick 5 types at random from `dsl/std/`. For each, cite the consumer site. | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:523` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: declare a new type with no consumer. Does the compiler / lens flag it pre-merge? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:527` | `GAP` | `CENSUS_NOT_RUN` | Grep for `String` or `Bool` used as discriminators at service boundaries. Enumerate. Each is a candidate for `feedback_opaque_strings_attract_heuristics`-class violation. | Quantitative ratchet/census probe not completed for this redo; requires full-tree counting policy rather than spot substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:528` | `GAP` | `SUBSTRATE_PRESENT` | Pick 3 service boundaries (e.g., extdeps providers, RFC carriers). For each, are discriminators typed enums or `String`/`Bool`? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:529` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a String-typed discriminator at a service boundary. Does code review / lens / ratchet catch it? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:533` | `GAP` | `CENSUS_NOT_RUN` | Grep for `__BUG_`, `__EMIT_BUG_`, `__TODO_`, `__PENDING_` etc. across `dsl/` + `src/v3/`. Count. | Quantitative ratchet/census probe not completed for this redo; requires full-tree counting policy rather than spot substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:534` | `GAP` | `SUBSTRATE_PRESENT` | For any survivors: cite the named-dissolution path or operator-ratified justification. | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:535` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: try to add a `__SENTINEL_X__` string-marker as a code path. Does ratchet / lens reject it? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:539` | `GAP` | `CENSUS_NOT_RUN` | Run the duplicate-record-shape lens (or grep for structurally-identical types). What's the count? | Quantitative ratchet/census probe not completed for this redo; requires full-tree counting policy rather than spot substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:540` | `GAP` | `SUBSTRATE_PRESENT` | Pick 5 record shapes from `dsl/std/`. For each, verify structurally-distinct from every other. | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:541` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: declare two record types with identical fields/types but different names. Does the substrate flag the duplication, or accept it as namespacing? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:545` | `GAP` | `CENSUS_NOT_RUN` | Count hand-authored `.rs` test files (per SG-0 census `EXPECTED_HAND_AUTHORED_TEST`). What's the number at HEAD? | Quantitative ratchet/census probe not completed for this redo; requires full-tree counting policy rather than spot substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:546` | `GAP` | `SUBSTRATE_PRESENT` | For each hand-authored Rust test: what predicate / effect-model / mock surface does it flag as "language-doesn't-yet-express"? Enumerate. | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:547` | `GAP` | `SUBSTRATE_PRESENT` | What's the named-retirement path per test? (Cite `pb_rust_tests_outside_residual_zero` ROADMAP gate.) | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:548` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: try to add a new hand-authored `.rs` test for a behavior that COULD be expressed in `.dag` `TestClaim`. Does the SG-0 census reject it? | Modeling-discipline substrate and audits exist, including fact-density lenses and deferral audit (`src/v4/lens/fact_density.dag`, `docs/audit/v4-deferral-audit-2026-05-29.md:69`). Missing for PROVEN: fresh ratchet counts and enforcement evidence. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.1 Omni-emission (R3 = 3 Shape-A targets: Rust / Python / Go)

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:560` | `GAP` | `SUBSTRATE_PRESENT` | Show me a single `.dag` program. Emit it to all 3 Shape-A targets (Rust / Python / Go). Are the outputs runnable? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:561` | `GAP` | `SUBSTRATE_PRESENT` | Behavioral parity: same input through each output target. Do they produce equivalent results? (THESIS.md:180 L5 claim.) | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:562` | `GAP` | `SUBSTRATE_PRESENT` | Pick the most complex Shape-A target (Go's goroutine substrate? Python's dynamic dispatch?). Is there a non-trivial example that compiles and runs? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:563` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: program with feature X. Does target Y handle X correctly, or punt to a stub? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.2 Workflow-as-data

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:594` | `GAP` | `SUBSTRATE_PRESENT` | Show me the `.dag` source for the CI workflow. Cite path. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:595` | `GAP` | `SUBSTRATE_PRESENT` | Where does the YAML get generated from the `.dag`? Show the emitter. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:596` | `GAP` | `SUBSTRATE_PRESENT` | When the CI runs, is it reading from a generated YAML, OR is `.dag` directly executed by some runner? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:597` | `GAP` | `SUBSTRATE_PRESENT` | If the `.dag` source is changed, does the YAML auto-regenerate? Show me the regen path. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:598` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: change the `.dag` source, leave the YAML stale. Does CI fail? Or silently pass? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.3 Tests-as-data

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:606` | `GAP` | `SUBSTRATE_PRESENT` | How many tests are ported to `.dag` TestClaim form? Count. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:607` | `GAP` | `SUBSTRATE_PRESENT` | Pick one. Show the TestClaim source. Show it executing. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:608` | `GAP` | `SUBSTRATE_PRESENT` | Where's the runner? Is it itself `.dag` or hand-Rust? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:609` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: add a new TestClaim entirely in `.dag`, no Rust. Does it run? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.4 Full-stack-from-one-`.dag` — visceral 4-layer omni-emission + R4 framework substrate (FORWARD POINTER)

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:628` | `GAP` | `SUBSTRATE_PRESENT` | Demo: one `.dag` → 4 artifacts (Rust + SQL DDL + OpenAPI + Markdown). Show them. Show coherence test passing. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:629` | `GAP` | `SUBSTRATE_PRESENT` | Canvas: 5 Director Q-dispositions ratified. Cite anchor. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:630` | `GAP` | `SUBSTRATE_PRESENT` | Falsification probe: introduce a divergence between any two of the 4 emitted artifacts at the substrate level. Does gate #28 catch it? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:631` | `GAP` | `SUBSTRATE_PRESENT` | R4 thesis-pitch: extend story to TS client + React frontend. What's the smallest structural extension? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.5 L6 — every structural form compiles to every target

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:641` | `GAP` | `SUBSTRATE_PRESENT` | Enumerate the structural forms in `.dag` (the 6 connectives × 5 behaviors product space, minus disallowed combinations). For each, is it emittable to Rust? Python? Go? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:642` | `GAP` | `SUBSTRATE_PRESENT` | Show me the L6 matrix at HEAD: (structural-form × target) → emit-status. Is it dense (all green), or sparse (per-target gaps)? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:643` | `GAP` | `SUBSTRATE_PRESENT` | Pick 5 structural forms. For each, trace the emit code path for Rust + Python + Go. Are they parallel, or does one target have special-case branching the others lack? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:644` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: pick a structural form whose emit is implemented for one target but stubbed for another. Does the compiler fail-closed when targeting the stubbed lang, or silently emit broken code? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:645` | `GAP` | `SUBSTRATE_PRESENT` | **L6 vs L5 distinction probe**: a form that emits to all 3 targets BUT produces semantically-divergent output is an L5 failure. A form that fails to emit at all on one target is an L6 failure. R3-close: zero of either? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.6 L7 — operations obey declared algebraic laws

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:657` | `GAP` | `SUBSTRATE_PRESENT` | Pick an algebra carrier at HEAD (e.g., `Monoid<X>` for some X). Find the emitted operation. Show me the proof / test that the emitted op honors the algebra's law. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:658` | `GAP` | `SUBSTRATE_PRESENT` | Where do the algebra-law tests live? `dsl/std/algebra_axioms.dag` (or equivalent)? Cite path. Are these `.dag` `TestClaim` declarations (per §3.3) or hand-Rust? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:659` | `GAP` | `SUBSTRATE_PRESENT` | **Coverage probe**: for the 4-5 most-used algebra carriers (Monoid, Group, AbelianGroup, Ring, Field), is there a per-axiom test demonstrating laws hold post-emit? Tabulate. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:660` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-target consistency**: do algebra-law tests pass on Rust + Python + Go independently? Or only Rust? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:661` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a `.dag` Monoid declaration whose emit deliberately violates associativity (e.g., a free-monoid with a non-associative concat). Does the L7 test surface catch it? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §3.7 The verification-machinery promises (testgen / integration / mocks / dry-run)

Section total: 27 probes — ship_disposition: 0 PROVEN, 27 GAP; engineering_state: 15 SUBSTRATE_PRESENT, 12 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:677` | `GAP` | `SUBSTRATE_PRESENT` | Where does testgen live? Cite the file or pipeline-stage. Is it `.dag` or hand-Rust? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:678` | `GAP` | `SUBSTRATE_PRESENT` | Pick a type from `dsl/std/` at random. Does testgen produce inhabitant + coercion tests for it? Cite the generated test count. | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:679` | `GAP` | `SUBSTRATE_PRESENT` | What's the testgen→TestClaim flow? Generated TestClaim declarations land where (in-tree, in-memory)? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:680` | `GAP` | `SUBSTRATE_PRESENT` | **Coverage probe**: count types in `dsl/std/` vs count generated tests per type. Is coverage monotonic with declared structure? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:681` | `GAP` | `SUBSTRATE_PRESENT` | **Reshape probe per TESTING.md:343**: `m1_5_testgen_test.rs` should be spot-check not exhaustive-compile-every-claim. Is this discipline applied at HEAD? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:682` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: declare a new type in `dsl/std/` with no consumer. Does testgen generate inhabitant tests for it, or does it skip? (Per §2.7 "every type has a structural consumer" — testgen IS a consumer if it's downstream of structure.) | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:692` | `GAP` | `SUBSTRATE_PRESENT` | Enumerate integration tests at HEAD. How many? In what file-locations? (`src/v3/compiler/tests/` etc.) | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:693` | `GAP` | `SUBSTRATE_PRESENT` | For each: is it `.dag` `TestClaim`-shaped or hand-authored `.rs`? Tabulate. | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:694` | `GAP` | `SUBSTRATE_PRESENT` | Pick 3 integration tests. For each, what fixture does it compile? What's the end-to-end coverage (which substrate carriers, which emission targets, which lenses)? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:695` | `GAP` | `NO_ARTIFACT_FOUND` | **Cross-target coverage**: are integration tests run for Rust + Python + Go targets independently? Or only one? | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:696` | `GAP` | `SUBSTRATE_PRESENT` | **Mock-over-compile anti-pattern probe (per TESTING.md:84)**: does any integration test mock its compile-result rather than actually compiling? Should be zero. | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:697` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a regression that ONLY surfaces end-to-end (passes unit-level tests). Does any integration test catch it pre-merge? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:709` | `GAP` | `SUBSTRATE_PRESENT` | Pick a `.dag` program that performs I/O (e.g., HTTP call, file read). Show the effect-source as a typed parameter. Cite the carrier (e.g., `HttpClient`, `FileSystem`). | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:710` | `GAP` | `SUBSTRATE_PRESENT` | Write a test for that program substituting a fake effect-source. How much new code did you write — a fake `HttpClient` implementation, or a mock-framework invocation? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:711` | `GAP` | `NO_ARTIFACT_FOUND` | Run the test. Are there any hidden-state interactions (global state, ambient capabilities, environment dependencies)? | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:712` | `GAP` | `SUBSTRATE_PRESENT` | **"No flaky tests" probe**: enumerate test-flakiness incidents in CI history. For each, what was the root cause? Hidden state, time-dependency, race? Are any caused by mocking infrastructure? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:713` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: try to write a `.dag` program that uses an ambient capability (hidden state not in parameters). Does the substrate reject it, or admit it? | T-16/T-24/T-38 task substrate names the full-stack demo and runner path (`src/v4/TASKS.md:958`, `src/v4/workflow/ci.dag:117`, `src/v4/compiler/05_eval.dag:1826`). Missing for PROVEN: actual full-stack run and falsification transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:714` | `GAP` | `NO_ARTIFACT_FOUND` | **Cross-test pollution probe**: run integration tests in randomized order. Does any test depend on order, or on prior test leftover state? Should be zero. | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:724` | `GAP` | `NO_ARTIFACT_FOUND` | What's the `dag run --dry-run` (or equivalent) invocation? Does it exist as a CLI flag, an `IntrospectApplication` lens, or implicit-via-purity? | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:725` | `GAP` | `NO_ARTIFACT_FOUND` | Run dry-run on an example `.dag` workflow. What does it output? (Expected: execution-trace lens reading + effect-summary + cost-estimate.) | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:726` | `GAP` | `NO_ARTIFACT_FOUND` | **Effect-shape preview probe**: pick a workflow with HTTP / DB / file effects. Can dry-run enumerate the effects it WOULD perform without performing them? Cite the lens / output. | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:727` | `GAP` | `NO_ARTIFACT_FOUND` | **Cost-preview probe**: can dry-run report the symbolic cost (§1.2) of executing the workflow without executing it? (Should compose with cost-lens output.) | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:728` | `GAP` | `NO_ARTIFACT_FOUND` | **Affected-set composition** (per §2.5.F): does dry-run compose with affected-set lens — i.e., "given this diff, what would actually re-execute"? Or are they separate query surfaces? | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:729` | `GAP` | `NO_ARTIFACT_FOUND` | **Simulated-inputs probe**: can dry-run accept simulated input values + report what the program would compute? Distinguish from "running with fake values" (which is actual execution). | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:730` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: a program performs an HTTP POST. Run dry-run. Does the actual HTTP request happen (failure) or is it captured as an effect-trace entry (success)? | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:738` | `GAP` | `NO_ARTIFACT_FOUND` | Run a single program through testgen + integration + mocks + dry-run. Do they share one substrate-read pass, or are they four separate pipelines? (Per `feedback_holistic_over_patches` + `feedback_compositional_not_templating`: should be one substrate-read with four lens-reads.) | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:739` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: a bug surfaces in production. Could ANY of the four (testgen / integration / mocks / dry-run) have caught it pre-merge? Tabulate per bug class. If one surface ALWAYS misses, that's a gap class. | Searched `src/v4/test/claim` and workflow files: claims compile, but no runnable full-stack demo transcript or stdout/stderr receipt exists at HEAD. | Delivery artifact or executable demo receipt for the requested probe. |
### §3.8 Multi-program / network-coordinated emission from one `.dag` (FORWARD POINTER)

Section total: 7 probes — ship_disposition: 0 PROVEN, 7 GAP; engineering_state: 7 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:760` | `GAP` | `SUBSTRATE_PRESENT` | **Multi-program shape**: how does `.dag` express "this fragment runs on machine A, this on machine B"? Is it a Cluster F lens reading a "deployment-target" dimension, OR substrate-level partitioning (carriers for `DeploymentUnit` / `Endpoint`)? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:761` | `GAP` | `SUBSTRATE_PRESENT` | **Wire derivation extension**: does extending gate #28 `omni_layers_share_one_node_tree` to "share one Dag across deployment units" hold, OR does cross-deployment need a new invariant gate? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:762` | `GAP` | `SUBSTRATE_PRESENT` | **Coordination semantics modeling**: are sync / async / stream / pub-sub first-class behaviors (a 6th L1 behavior or beyond? — would trigger C1 stop-signal per §2.6) OR compositions over existing 5 behaviors (Bind composition + effect-typed parameters)? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:763` | `GAP` | `SUBSTRATE_PRESENT` | **Failure-at-boundary**: is "partial failure" a lens read or a substrate variant? (NOT an "effect annotation" — effects are type-intrinsic, no annotation layer.) How does it compose with effect-enumeration lens (§1.4)? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:764` | `GAP` | `SUBSTRATE_PRESENT` | **Idempotency at endpoint**: composes with existing idempotency lens (per THESIS:188 "idempotency + cancellation + redundancy = algebraic simplification" + R1 demo class per THESIS:378-380)? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:765` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-endpoint dimension propagation**: does the affected-set lens (§2.5.F) extend across deployment-unit boundaries? When endpoint A's cost dimension changes, are endpoint B's consumers reading A's wire-contract dimension flagged? | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:766` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: design a 2-endpoint distributed program in `.dag`. Demonstrate end-to-end emission: each endpoint emits its own backend (per Shape-A) + the wire contract between them (per Shape-B) + coordination behavior captured structurally. Or: identify the gap class. | Compiler/workflow substrate exists as `.dag` stages and CI-as-data (`src/v4/compiler/02_parse.dag`, `src/v4/compiler/04_infer.dag`, `src/v4/compiler/05_eval.dag`, `src/v4/workflow/ci.dag:1`, `src/v4/workflow/ci.dag:242`). Missing for PROVEN: an executed clean-checkout transcript for the exact probe. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §4.1 Lens self-application

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:786` | `GAP` | `SUBSTRATE_PRESENT` | Run the complexity lens on the compiler's own source. What does it report? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:787` | `GAP` | `SUBSTRATE_PRESENT` | Run the cost lens on a self-host pipeline. Show the symbolic cost. | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:788` | `GAP` | `SUBSTRATE_PRESENT` | If the compiler has a complexity contract violation against itself, does it fail to compile? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:789` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a known complexity-violation into the compiler. Does the self-application lens catch it before tests do? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §4.2 Self-host fixed point

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:797` | `GAP` | `SUBSTRATE_PRESENT` | Run the fixed-point pipeline. Does iteration N == iteration N+1? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:798` | `GAP` | `SUBSTRATE_PRESENT` | How many iterations to converge? (Should be ≤2.) | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:799` | `GAP` | `SUBSTRATE_PRESENT` | If divergent: what's the byte-diff or behavior-diff? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:800` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: introduce a non-deterministic compiler step. Does the fixed-point predicate catch it? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §4.3 Concept unifications

Section total: 10 probes — ship_disposition: 0 PROVEN, 10 GAP; engineering_state: 10 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:815` | `GAP` | `SUBSTRATE_PRESENT` | Find the "coercion cost" data. Cite the substrate carrier. Is it `Complexity` (the same one used by §1.1), or a separate `CoercionCost` carrier? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:816` | `GAP` | `SUBSTRATE_PRESENT` | If separate: that's a parallel-authority finding. Surface as P1 violation candidate. | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:817` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: a coercion between two representations has cost C. Is C readable via the complexity lens, or via a separate read? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:823` | `GAP` | `SUBSTRATE_PRESENT` | Find the Rust language spec at `dsl/extdeps/languages/rust/`. Find the Rust transport spec. Are they ONE file / ONE substrate carrier, or distinct authorities? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:824` | `GAP` | `SUBSTRATE_PRESENT` | Same for an interpreter runtime (if v3 has one at HEAD; or the runtime-emission target). Is it the same substrate, or parallel? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:825` | `GAP` | `SUBSTRATE_PRESENT` | If distinct: each pair is a parallel-authority finding. Enumerate. | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:826` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: edit the Rust language spec. Does the transport spec auto-update (i.e., it IS the same fact) or require a parallel edit (P1 violation)? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:830` | `GAP` | `SUBSTRATE_PRESENT` | Find the idempotency lens / mechanism. Find the cancellation lens / mechanism. Find the redundancy lens / mechanism. Are they three separate mechanisms or ONE algebraic-simplification engine with three lenses? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:831` | `GAP` | `SUBSTRATE_PRESENT` | If three: where's the unification path? (Should be in algebra.dag's compositional-fold of `Behavior::Bind` per `feedback_closed_system_effects`.) | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:832` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a `.dag` program with an obviously-redundant operation (e.g., reading the same key twice with no intervening write). Does the idempotency lens, cancellation lens, AND redundancy lens all catch it? Same diagnostic, or three different paths? | Bootstrap/self-host workflow data exists (`src/v4/workflow/bootstrap.dag:1`, `src/v4/compiler/self_host.dag:1`) with runner closure still tracked open. Missing for PROVEN: executed self-application/fixed-point proof. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §5.1 5 substrate-gap classes closed

Section total: 6 probes — ship_disposition: 0 PROVEN, 6 GAP; engineering_state: 1 SUBSTRATE_PRESENT, 4 NO_ARTIFACT_FOUND, 1 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:846` | `GAP` | `NO_ARTIFACT_FOUND` | Class 1 (parser/grammar #60): show concept-faithful `Int<64>`, `Real<64>`, `Nat<8>` lower without v2-fallback. Where? Demo. | Searched `src/v4`/`docs` for `Int<64>`, `Real<64>`, `Nat<8>`, and `v2-fallback`; found design/history mentions such as `docs/briefs/r3-substrate-s8-approximate-field-float-migration-worker.md:72`, but no current v4 demo proving concept-faithful lowering without fallback. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:847` | `GAP` | `NO_ARTIFACT_FOUND` | Class 2 (function-valued #61): show a function-valued data flow program working end-to-end. Demo. | Searched `src/v4`/`docs` for `function-valued`/`FunctionValue`; no current end-to-end v4 function-valued data-flow demo was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:848` | `GAP` | `NO_ARTIFACT_FOUND` | Class 3 (file-ingestion #62): show `.dag` program reading an external file via `FileAttachment` carrier (per Director ratification 2026-05-13). Demo. | Searched `src/v4`/`docs` for `FileAttachment`, `file attachment`, and `Attachment`; no v4 `FileAttachment` carrier demo or external-file ingestion receipt was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:849` | `GAP` | `SUBSTRATE_PRESENT` | Class 4 (workflow/scheduling #63): show CI workflow scheduling executing via `.dag`. Demo. | Workflow-as-data substrate exists (`src/v4/workflow/ci.dag:1`) and T-38 tracks modeled workflow/claim-runner closure (`src/v4/TASKS.md:274`), but this pass found no clean execution transcript for CI workflow scheduling via `.dag`. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:850` | `GAP` | `NO_ARTIFACT_FOUND` | Class 5 (reflection-closure #64): show `lens_apply.rs` reflection via PB-Runtime, end-to-end. Demo. | Searched for `lens_apply`/`reflection`; docs describe staged or in-flight reflection (`docs/r2-closure-ledger.md:264`, `docs/lens-library-design.md:141`), but no current v4 PB-Runtime `lens_apply.rs` end-to-end receipt was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:851` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: write a new substrate-gap (a 6th class). Could the framework absorb it without changes to the closure criteria? Or would §1.4 need re-authoring? | Requires authoring and evaluating a new substrate-gap class; not attempted in this audit pass. | Complete targeted audit/census run for this probe. |
### §5.2 v2 fully retired

Section total: 3 probes — ship_disposition: 0 PROVEN, 3 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 3 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:859` | `GAP` | `NO_ARTIFACT_FOUND` | grep for `src/v2/` imports in `src/v3/`. Should be zero. | Targeted grep found live `src/v2/` path references in `src/v3`, including `src/v3/compiler/tests/integration/v4_workflow_release_dag_smoke_test.rs:25` and `src/v3/compiler/tests/integration/v4_workflow_ci_runner_dag_smoke_test.rs:53`; the zero target is not met. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:860` | `GAP` | `NO_ARTIFACT_FOUND` | grep for v2-oracle fixtures in v3 tests. Survivors must be frozen-snapshot consumption only, not live. | Targeted grep found v2-oracle/v2-compiler references in v3 tests and fixtures (`src/v3/compiler/tests/integration/v2_oracle_no_remaining_test_consumers_test.rs:9`, `src/v3/compiler/tests/integration/v4_lens_registry_dag_smoke_test.rs:9`, `src/v3/compiler/tests/integration/cementing/cost_lens_symbolic_consumer_test.rs:17`); this audit did not prove all survivors are frozen-snapshot-only. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:861` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: delete `src/v2/`. Does the test suite still pass? | Delete-suite falsification was not run, and existing include/path references to `src/v2/stage0/Cargo.toml` (`src/v3/compiler/tests/integration/v4_workflow_release_dag_smoke_test.rs:24`, `src/v3/compiler/tests/integration/v4_workflow_release_dag_smoke_test.rs:25`) indicate the suite is not proven independent of `src/v2`. | Delivery artifact or executable demo receipt for the requested probe. |
### §5.3 BridgeLedgerZero

Section total: 3 probes — ship_disposition: 0 PROVEN, 3 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 3 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:869` | `GAP` | `NO_ARTIFACT_FOUND` | What's the current bridge ledger? Should be 0. | `src/v3/std/bridge_ledger.dag` is the live ledger authority (`src/v3/std/bridge_ledger.dag:6`, `src/v3/std/bridge_ledger.dag:132`) and currently contains Open rows (`src/v3/std/bridge_ledger.dag:134`, `src/v3/std/bridge_ledger.dag:170`); `rg bridge_ledger src/v4` found no v4 ledger authority. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:870` | `GAP` | `NO_ARTIFACT_FOUND` | If non-zero: enumerate each bridge. Has each a named dissolution trigger and target date? | The v3 ledger enumerates concrete rows and retirement authority (`src/v3/std/bridge_ledger.dag:117`, `src/v3/std/bridge_ledger.dag:132`), including Open rows with authority strings (`src/v3/std/bridge_ledger.dag:134`, `src/v3/std/bridge_ledger.dag:170`). A zero-current ledger receipt was not found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:871` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: would a code reviewer notice if a new bridge type was introduced without ledger entry? | Bridge status is a closed coproduct and new states require substrate amendment (`src/v3/std/bridge_ledger.dag:23`, `src/v3/std/bridge_ledger.dag:84`), but this validation found no v4-side ratchet proving reviewers would catch an undeclared bridge (`rg bridge_ledger src/v4` -> no matches). | Delivery artifact or executable demo receipt for the requested probe. |
### §5.4 Compiler-as-data residual — "is the compiler pure data yet?"

Section total: 19 probes — ship_disposition: 0 PROVEN, 19 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 9 NO_ARTIFACT_FOUND, 10 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:887` | `GAP` | `CENSUS_NOT_RUN` | Walk through R3-close-anchored changes: how many required edits to `src/v3/compiler/src/*.rs` (hand-Rust) vs. `.dag` substrate? | Requires last-PR/history tabulation across R3-close anchored changes; not completed in this redo. No `.rs`/`.dag` edit distribution was inferred from substrate search. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:888` | `GAP` | `CENSUS_NOT_RUN` | List the last 20 merged R3 PRs. For each, was the load-bearing change in `.rs` files or `.dag` files? Tabulate. | Requires listing and classifying the last 20 merged R3 PRs; not completed in this redo. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:889` | `GAP` | `CENSUS_NOT_RUN` | Is there a SINGLE merged R3 PR where the load-bearing change touched ONLY `.dag` files (no `_generated.rs` regen, no hand-Rust)? Cite SHA. | Requires merged-PR history search for a dag-only load-bearing PR; not completed in this redo. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:890` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: pick a random `.dag` file. Edit it (e.g., add a field). Run the compiler. Does the compiler produce a correct emission without requiring any `_generated.rs` regen step that itself needs hand-Rust orchestration? | Requires editing a random `.dag` file and running regeneration; not attempted in this docs-only validation. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:896` | `GAP` | `NO_ARTIFACT_FOUND` | Count `.rs` files in `src/v3/` total. Cite the number at HEAD. | Local census found `find src/v3 -name '*.rs' | wc -l` -> 270. This is a count receipt, not a pure-data close proof. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:897` | `GAP` | `NO_ARTIFACT_FOUND` | Count `_generated.rs` (machine-emitted from `.dag`). Cite the number. | Local census found `find src/v3 -name '*_generated.rs' | wc -l` -> 28; the generated subset is far smaller than total Rust files. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:898` | `GAP` | `NO_ARTIFACT_FOUND` | **Hand-Rust = total − generated**. Cite the count. Is it 0 per operator's 2026-05-09 framing? | Local census found 270 total `.rs` minus 28 `_generated.rs` = 242 apparent hand-authored `.rs` files under `src/v3`, so the PB-0 hand-Rust target is not shown closed. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:899` | `GAP` | `NO_ARTIFACT_FOUND` | If non-zero: enumerate each hand-Rust survivor. For each, cite the named-retirement-schedule (PR # / gate # / target SHA). | The hand-authored survivor count is non-zero (242 by local census); this audit did not enumerate all survivors with named retirement schedules. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:900` | `GAP` | `CENSUS_NOT_RUN` | Cross-check against `src/v3/SELF_HOSTING.md` §1 "bootstrap seed" framing: are the survivors *bootstrap-seed-only* (i.e., regenerable from `.dag` via the compiler itself), or do any encode authoritative behavior absent from `.dag`? | Requires line-by-line cross-check against `src/v3/SELF_HOSTING.md` bootstrap-seed framing; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:901` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: delete one hand-Rust file. Can the `.dag` substrate regenerate it via the compiler running on itself? Cite the regen invocation. | A delete-and-regenerate falsification run was not performed; no invocation proving `.dag` regeneration of a hand-Rust survivor was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:907` | `GAP` | `CENSUS_NOT_RUN` | Enumerate hand-maintained files by extension class. `.yml` (CI/build). `.toml` (Cargo / Rust). `.md` (docs). `.sh` (scripts). Others. | Requires a full hand-maintained file census by extension class; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:908` | `GAP` | `CENSUS_NOT_RUN` | For each class, is the count R3-close-acceptable, or is there a named-dissolution-schedule? | Requires per-extension acceptability/dissolution schedule review; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:913` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: pick a hand-maintained non-Rust file (e.g., a build script). Is the load-bearing fact it encodes derivable from `.dag` substrate? If yes, why isn't it derived? If no, what's the named carrier for the fact? | Requires selecting a hand-maintained non-Rust file and tracing its load-bearing fact to `.dag`; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:927` | `GAP` | `NO_ARTIFACT_FOUND` | Run the PB-0 census at HEAD. What's the count of hand-authored `.rs` files in `src/v3/`? The committed target is **0** per design-pure-bootstrap-zero.md. | PB-0 census proxy found 242 apparent hand-authored `.rs` files under `src/v3` (`find src/v3 -name '*.rs' ! -name '*_generated.rs' | wc -l`), not zero. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:928` | `GAP` | `NO_ARTIFACT_FOUND` | For each hand-authored survivor: cite the named-retirement-schedule (PR # / gate # / target SHA). Survivors without named retirement are 0-floor debt. | Because the hand-authored survivor set is non-zero, this probe requires per-survivor retirement schedules; this audit did not enumerate all 242 survivors. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:929` | `GAP` | `NO_ARTIFACT_FOUND` | For each hand-authored survivor flagged as "bootstrap-seed": verify it is **machine-emitted from `.dag`** (replayable via `cargo run --bin regen-*` or equivalent). Hand-authored bootstrap-seed is NOT acceptable per design-pure-bootstrap-zero.md:210. | No replayable proof was found that each bootstrap-seed survivor is machine-emitted from `.dag`; the local count still shows 242 non-`_generated.rs` Rust files. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:930` | `GAP` | `CENSUS_NOT_RUN` | Cross-reference SELF_HOSTING.md §1 "bootstrap seed" framing against design-pure-bootstrap-zero.md:210 generated-trampoline framing: is every claimed bootstrap-seed survivor actually generated, not hand-authored? | Requires cross-reference of `SELF_HOSTING.md` and `design-pure-bootstrap-zero.md:210` for every claimed bootstrap-seed survivor; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:931` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: produce the `.dag` source for the LARGEST hand-Rust survivor at R3 close. Compile the `.dag`. Diff the emitted `.rs` against the survivor. If the survivor doesn't match the emission, the survivor is authoring facts not present in `.dag` — that's R3-close debt against the 0-floor target. | Largest-survivor `.dag` source/emitted-Rust diff falsification was not performed; no matching emission receipt was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:932` | `GAP` | `CENSUS_NOT_RUN` | **Bootstrap-resolution boundary probe** (per design-pure-bootstrap-zero.md:191 STOP-condition): if first-time bootstrap (N=0) resolution requires hand-Rust in v3's source tree, the 0-floor target is unreachable and the framing needs revision back toward an explicit alternative. Verify the N=0 resolution lives OUTSIDE `src/v3/` (install script, gunbc-runtime crate, rustc macro). | Requires N=0 bootstrap-resolution boundary audit outside `src/v3`; not completed. | Complete targeted audit/census run for this probe. |
### §5.5 Free consequences (when Tiers 1-2 close)

Section total: 11 probes — ship_disposition: 0 PROVEN, 11 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 6 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:960` | `GAP` | `NO_ARTIFACT_FOUND` | Find the memoization mechanism. Cite the lens / substrate carrier. | Searched `src/v4` and docs for memoization; only design/planning references were found (`docs/design-free-consequences.md:88`, `docs/thesis/what-else-falls-out.md:394`), not a v4 memoization carrier or emitter mechanism. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:961` | `GAP` | `NO_ARTIFACT_FOUND` | Memoization MUST be by-construction (compiler derives purity + cost and applies it automatically). Confirm there is NO opt-in/annotation form — if memoization requires a `@memoize`-style tag, that is the violation (feedback_no_annotations). | No v4 by-construction memoization mechanism was found, so no opt-in-free/annotation-free memoization receipt exists. Search terms: `memo`, `memoization`, `cache-hit` across `src/v4` and docs. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:962` | `GAP` | `NO_ARTIFACT_FOUND` | Show me a `.dag` program that should benefit from memoization. Compile + run. Was memoization applied? How is "was applied" verifiable (cost-lens output? execution-trace lens? cache-hit metric)? | No `.dag` program/run receipt demonstrating automatic memoization was found; design docs name intended claims (`docs/design-free-consequences.md:102`) but no current v4 execution artifact. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:963` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: a pure function with high cost is called twice with the same args. Does the compiler emit code that memoizes, or naive double-execution? | No falsification run for repeated pure calls was found; no v4 emitter/runtime memoization receipt was found by `rg memo src/v4`. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:967` | `GAP` | `SUBSTRATE_PRESENT` | Find the incremental-execution mechanism. Cite the lens / data carrier. | Incremental-execution substrate exists in `src/v4/lens/affected_set.dag` (`src/v4/lens/affected_set.dag:1`, `src/v4/lens/affected_set.dag:102`) and T-21 names re-exec/cache requirements (`src/v4/TASKS.md:1102`, `src/v4/TASKS.md:1116`). Missing for PROVEN: executed workflow trace. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:968` | `GAP` | `SUBSTRATE_PRESENT` | Run an example `.dag` workflow twice with no input changes. Does the second run skip pure subgraphs that haven't changed? Verify via execution-trace lens. | `affected_set` models re-exec frontier and fail-closed frontier decisions (`src/v4/lens/affected_set.dag:93`, `src/v4/lens/affected_set.dag:102`), but no two-run unchanged-input execution trace was found. Missing for PROVEN: execution-trace lens output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:969` | `GAP` | `SUBSTRATE_PRESENT` | Now change ONE input. Verify that only the affected-set subgraph re-executes (composes with §2.5.F affected-set lens). | T-21 explicitly composes affected-set with incremental re-test and cache reuse (`src/v4/TASKS.md:1116`, `src/v4/TASKS.md:1120`), but no one-input-change run transcript was found. Missing for PROVEN: concrete affected-frontier run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:970` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: a non-deterministic step is in the graph. Does the incremental-execution mechanism correctly avoid skipping its re-evaluation? | Affected-set substrate has fail-closed and boundary receipt carriers (`src/v4/lens/affected_set.dag:87`, `src/v4/lens/affected_set.dag:117`), but no nondeterministic-step falsification run was found. Missing for PROVEN: executed negative case. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:974` | `GAP` | `SUBSTRATE_PRESENT` | Find the "shared cost algebra" that enables cross-language optimization. Cite the substrate carrier. | Shared cost algebra substrate exists via `SymbolicCost` and dominance/lattice composition (`src/v4/lens/cost.dag:100`, `src/v4/lens/cost.dag:646`), but no cross-language optimizer consuming it was found. Missing for PROVEN: optimizer application receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:975` | `GAP` | `NO_ARTIFACT_FOUND` | Pick an optimization (e.g., loop-fusion / tail-recursion / cse). Is it applied uniformly across Rust + Python + Go targets, or per-target? | Searched `src/v4` for loop-fusion/tail-recursion/CSE/cross-language optimizer implementation; no executable optimization pass or uniform target application receipt was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:976` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: write a `.dag` program where one target language's optimizer would catch a cost reduction but another wouldn't. Does the shared-cost-algebra propagate the optimization to ALL targets, or only the one whose host optimizer applies? | No falsification `.dag` program or shared-cost-algebra optimization propagation run was found; search found design/planning claims but no v4 optimizer artifact. | Delivery artifact or executable demo receipt for the requested probe. |
### §6.1 "Show the correct code"

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:990` | `GAP` | `SUBSTRATE_PRESENT` | Find a recent compile error. Does the diagnostic point to the correct alternative, or just say "this is wrong"? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:991` | `GAP` | `SUBSTRATE_PRESENT` | Pick 5 diagnostic message types. For each, does it satisfy the "show the correct code" criterion? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:992` | `GAP` | `SUBSTRATE_PRESENT` | If diagnostic just says "X is wrong" without "Y would be right": GAP per operator-ratified 2026-05-15 — discipline rule requires every Diagnostic emit site to populate `suggested_correction`. None only when correction is genuinely undeterminable (e.g., user-input-boundary diagnostics) — and that None must carry a named reason. Working demos: complexity-violation diagnostic shows correct alternative; synthesis Report shows alternative algorithm. Demo TestClaims land in `src/v4/test/claim/diagnostic_correction/`. | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:993` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write a program with a known structural error. Read the diagnostic. Could a user act on it without reading source code? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §6.2 Audience duality / opt-in depth

Section total: 6 probes — ship_disposition: 0 PROVEN, 6 GAP; engineering_state: 6 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1005` | `GAP` | `SUBSTRATE_PRESENT` | Show me the SIMPLEST `.dag` program — types + function + maybe one workflow. How many concepts must the author know? Is the proof / lens surface invisible? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1006` | `GAP` | `SUBSTRATE_PRESENT` | Compile the simple program for Rust + Python + Go. Does it emit working code in all 3 without the user touching lens/proof surface? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1007` | `GAP` | `SUBSTRATE_PRESENT` | ROADMAP cites `fixture_integration_canonical` for the glue-generation audience. Cite the fixture path. Compile + run. Does it land in <100 LOC of `.dag` surface for the user? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1011` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` program that opens the advanced surface — author a user-defined lens. How much new surface does the user touch? Is the base-language surface untouched (per "opening doesn't change the base")? | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1012` | `GAP` | `SUBSTRATE_PRESENT` | ROADMAP cites `fixture_compiler_nerd_canonical` for the structural-proof audience. Cite the fixture path. Verify it exercises lens + proof surfaces. | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1013` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: a base-language change (e.g., a new type connective) shouldn't require the advanced-surface user to relearn. Conversely, an advanced-surface change shouldn't break base-language programs. Demonstrate non-coupling at each direction. | Diagnostic correction and audience surfaces are modeled through `std/diagnostic.dag`, `lens/application.dag`, and T-14/T-16 fixture scopes (`src/v4/lens/application.dag:121`, `src/v4/TASKS.md:734`, `src/v4/TASKS.md:958`). Missing for PROVEN: executed user-facing fixture output and PM-vs-compiler-nerd audience comparison. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §6.3 Adoption model — economics, not enforcement

Section total: 10 probes — ship_disposition: 0 PROVEN, 10 GAP; engineering_state: 3 SUBSTRATE_PRESENT, 2 NO_ARTIFACT_FOUND, 5 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1028` | `GAP` | `SUBSTRATE_PRESENT` | Pick an arbitrary `.dag` program at HEAD. Apply the complexity lens. Does it produce output? (It should — no opt-in flag should be required.) | Complexity lens substrate and worked claims exist (`src/v4/lens/complexity.dag:1`, `src/v4/test/claim/lens_complexity/map_id.dag:1`), but this audit did not run it on an arbitrary HEAD program. Missing for PROVEN: arbitrary-program lens output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1029` | `GAP` | `SUBSTRATE_PRESENT` | Same for effect / cost / parallelism lenses. Each should produce output for ANY `.dag` program at HEAD. | Cost/effect/parallelism lens substrates exist (`src/v4/lens/cost.dag:1`, `src/v4/lens/effect.dag:139`, `src/v4/lens/parallelism.dag:1`), but no universal all-program lens run was found. Missing for PROVEN: arbitrary-program outputs for each lens. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1030` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: try to author a `.dag` program that opts OUT of complexity/cost/effect lens reads. Is there ANY in-language syntax that disables lens reads? (Should be zero per `feedback_groundedness_gates_lenses`.) | Requires syntax/grammar audit for any in-language opt-out of lens reads; not completed. No result was inferred from diagnostic/audience substrate. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1034` | `GAP` | `CENSUS_NOT_RUN` | Author a `.dag` namespace pattern. Run all 4 lenses on it. Do they all read through the namespacing, or does one of them stop at the named boundary? (Should read through per "compiler sees through" claim.) | Requires authoring/running a namespace-pattern program through four lenses; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1035` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: try to "hide" a complexity violation behind a named pattern (e.g., wrap an O(n²) function in a typedef'd "FastLookup<T>"). Does the complexity lens still flag it? | No namespace-hidden complexity-violation falsification fixture or run transcript was found in `src/v4/test/claim` or lens files. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1039` | `GAP` | `SUBSTRATE_PRESENT` | Show me a `.dag` program that calls out to an externally-implemented operation (e.g., `ExecuteCommand` / `extdeps` provider). Where does lens-coverage end? Cite the explicit boundary marker (per `feedback_groundedness_gates_lenses`). | External boundary substrate exists in extdeps and diagnostics (`src/v4/extdeps/coordination.dag:1`, `src/v4/lens/effect.dag:175`), but no explicit ExecuteCommand/extdeps boundary lens-coverage demo was found. Missing for PROVEN: boundary-marker fixture output. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1040` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: try to author a program that EFFECTIVELY leaves the stack inside the language (e.g., via heavy use of `extdeps` opaque calls). Is the lens-coverage boundary explicit + auditable? | No falsification fixture showing heavy extdeps/opaque calls with explicit auditable lens boundary was found; searched `src/v4` for opaque/extdeps/lens-boundary receipts. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1044` | `GAP` | `CENSUS_NOT_RUN` | What's the LOC overhead of a `.dag` program vs. an equivalent program in Rust / Python / Go? Tabulate for a canonical small + medium + large example. | Requires LOC comparison for canonical small/medium/large examples across `.dag`, Rust, Python, and Go; not completed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1045` | `GAP` | `CENSUS_NOT_RUN` | What's the percentage of programs where ALL lenses produce green output (no violations)? If the percentage is high, "high free value" is structurally true. | Requires corpus-wide percentage of programs where all lenses produce green output; not computed. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1046` | `GAP` | `CENSUS_NOT_RUN` | **Falsification probe**: is there a class of programs gunbc IS more verbose / more constraining than alternatives, in a way that fails the "economics" test? Enumerate (acceptable to defer as R4 if narrow). | Requires economics/falsification class enumeration for verbosity/constraint overhead; not completed. | Complete targeted audit/census run for this probe. |
### §7 Cross-doc ledger coherence (structural — keep from v0)

Section total: 9 probes — ship_disposition: 0 PROVEN, 9 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 9 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1056` | `GAP` | `CENSUS_NOT_RUN` | §1.5 lane counts sum to §1.8 enumerated total | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1057` | `GAP` | `CENSUS_NOT_RUN` | §1.8 enumerated count matches r3-structure.md §"Acceptance" | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1058` | `GAP` | `CENSUS_NOT_RUN` | Q1 row in §10 matches §1.5 + §1.8 totals | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1059` | `GAP` | `CENSUS_NOT_RUN` | No gate ID gaps or duplicates in §1.8 | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1060` | `GAP` | `CENSUS_NOT_RUN` | All `docs/audit/*.md` ratifications represented in §1.8 OR §10 (per `feedback_grep_audit_docs_before_answering_close_questions`) | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1061` | `GAP` | `CENSUS_NOT_RUN` | `docs/r3-remaining-work-dependency-graph.md` consistent with §1.8 PASSING/DECLARED state | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1062` | `GAP` | `CENSUS_NOT_RUN` | `r3_debt_paydown_zero_remaining` at close: 0 | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1063` | `GAP` | `CENSUS_NOT_RUN` | ROADMAP `Post-merge debt` rows: 0 | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
| `docs/v4-close-interrogation.md:1064` | `GAP` | `CENSUS_NOT_RUN` | §10 RED escalations: ALL CLOSED with owner sign-off on-ledger | Cross-doc ledger coherence requires a document-to-document audit beyond code search; not completed in this redo window. | Complete targeted audit/census run for this probe. |
### §8 Per-gate predicate execution at close

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 4 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1072` | `GAP` | `SUBSTRATE_PRESENT` | All 104 §1.8 predicates executed at HEAD within 24h of close ceremony | Close gates and CI-as-data are represented in `src/v4/TASKS.md` and `src/v4/workflow/ci.dag`, but T-38 says runner closure remains pending (`src/v4/TASKS.md:274`, `src/v4/workflow/ci.dag:117`). Missing for PROVEN: operator-ready green close transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1073` | `GAP` | `SUBSTRATE_PRESENT` | Execution log preserved as audit artifact | Close gates and CI-as-data are represented in `src/v4/TASKS.md` and `src/v4/workflow/ci.dag`, but T-38 says runner closure remains pending (`src/v4/TASKS.md:274`, `src/v4/workflow/ci.dag:117`). Missing for PROVEN: operator-ready green close transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1074` | `GAP` | `SUBSTRATE_PRESENT` | No predicate trivially passes via empty match-target or narrowed scope without justification | Close gates and CI-as-data are represented in `src/v4/TASKS.md` and `src/v4/workflow/ci.dag`, but T-38 says runner closure remains pending (`src/v4/TASKS.md:274`, `src/v4/workflow/ci.dag:117`). Missing for PROVEN: operator-ready green close transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1075` | `GAP` | `SUBSTRATE_PRESENT` | PASSING gates whose predicate produces output: output preserved in close audit doc | Close gates and CI-as-data are represented in `src/v4/TASKS.md` and `src/v4/workflow/ci.dag`, but T-38 says runner closure remains pending (`src/v4/TASKS.md:274`, `src/v4/workflow/ci.dag:117`). Missing for PROVEN: operator-ready green close transcript. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §10 Close ceremony

Section total: 8 probes — ship_disposition: 0 PROVEN, 8 GAP; engineering_state: 0 SUBSTRATE_PRESENT, 8 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1096` | `GAP` | `NO_ARTIFACT_FOUND` | Every §1-§8 item PROVEN or R4-DEFERRED with operator acceptance | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1097` | `GAP` | `NO_ARTIFACT_FOUND` | Zero GAP, zero WEAK-EVIDENCE | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1098` | `GAP` | `NO_ARTIFACT_FOUND` | Director sign-off recorded on-ledger | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1099` | `GAP` | `NO_ARTIFACT_FOUND` | Operator sign-off recorded on-ledger | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1100` | `GAP` | `NO_ARTIFACT_FOUND` | `ROADMAP.md` R3 → R3-closed milestone update merged | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1101` | `GAP` | `NO_ARTIFACT_FOUND` | §1.8 status sweep frozen | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1102` | `GAP` | `NO_ARTIFACT_FOUND` | R4 work-item creation greenlit | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1103` | `GAP` | `NO_ARTIFACT_FOUND` | R3 close audit doc preserved: `docs/audit/r3-close-YYYY-MM-DD.md` capturing per-gate predicate execution output + every probe's disposition with evidence link | Close ceremony requires every probe ship-eligible; current task evidence explicitly leaves T-15/T-38 runner closure open (`src/v4/TASKS.md:815`, `src/v4/TASKS.md:2277`). | Delivery artifact or executable demo receipt for the requested probe. |
### §13 Arbitrary ingestion — bidirectional substrate (NEW for v4, was R4)

Section total: 9 probes — ship_disposition: 0 PROVEN, 9 GAP; engineering_state: 9 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1140` | `GAP` | `SUBSTRATE_PRESENT` | Can a user write `.dag` code that reads an arbitrary JSON file and produces a typed `.dag` value? Show the program. Is the ingestion logic in `.dag` (per closed-system discipline) or in hand-Rust? | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1141` | `GAP` | `SUBSTRATE_PRESENT` | Same for YAML, CSV, TOML. | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1142` | `GAP` | `SUBSTRATE_PRESENT` | Can the compiler ingest a Rust source file (a subset of Rust grammar) and produce typed `.dag` representation? Same for Python, Go. | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1143` | `GAP` | `SUBSTRATE_PRESENT` | Schema-driven ingestion: given an OpenAPI spec, can the compiler produce corresponding `.dag` types AND validate incoming JSON against them? | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1144` | `GAP` | `SUBSTRATE_PRESENT` | **Bidirectionality**: for each Shape A target (Rust/Python/Go), is the language spec in `extdeps/languages/<lang>.dag` used for BOTH emission and ingestion, OR are these separate substrates? Per concept-unification claim (THESIS:185-188), they should be ONE substrate. | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1145` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: write an ingestion target for a format never tested (e.g., XML). Does the substrate accommodate it without new connectives or behaviors? Or does ingestion require a dedicated 6th L1 behavior (would trigger C1 stop-signal per §2.6)? | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1149` | `GAP` | `SUBSTRATE_PRESENT` | Roundtrip: ingest a Rust source file → produce typed `.dag` → emit Rust. Is the emitted Rust byte-equivalent to the input (modulo whitespace/comments)? If not, the ingestion lost information — gap. | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1150` | `GAP` | `SUBSTRATE_PRESENT` | Coverage: enumerate the 6 connectives × 5 behaviors structural form space. For each, does the v4 ingestion path support it for all anchored formats? L6-style completeness for ingestion. | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1151` | `GAP` | `SUBSTRATE_PRESENT` | Effect-model preservation: ingest a Python source file with side effects. Are the effects classifiable via the effect lens (§1.4) post-ingestion? Or does ingestion produce opaque blobs the lens can't read? | Language/format extdeps and parse claims exist (`src/v4/extdeps/languages/*.dag`, `src/v4/extdeps/formats/openapi.dag`, `src/v4/test/claim/parse/*`). Missing for PROVEN: cross-format executable round-trip receipt. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §14 Additional Shape A languages — C, C++, LLVM IR, TypeScript (NEW for v4, was R4.A/R4.C)

Section total: 5 probes — ship_disposition: 0 PROVEN, 5 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1191` | `GAP` | `SUBSTRATE_PRESENT` | Per language: can v4 emit a non-trivial program to it? (Same probes as §3.1 for Rust/Python/Go, replicated.) | Additional target models exist for the §14 set: C++ (`src/v4/extdeps/languages/cpp.dag:1`), LLVM IR (`src/v4/extdeps/languages/llvm_ir.dag:1`), and TypeScript (`src/v4/extdeps/languages/typescript.dag:1`); C is treated as subsumed by the C++/LLVM-carveout in the questionnaire. Missing for PROVEN: non-trivial emitted code per target. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1192` | `GAP` | `SUBSTRATE_PRESENT` | Per language: can v4 ingest source files written in it? (Per §13 ingestion claim.) | The §14 language files include grammar/model substrate for C++/LLVM IR/TypeScript (`src/v4/extdeps/languages/cpp.dag:2`, `src/v4/extdeps/languages/llvm_ir.dag:2`, `src/v4/extdeps/languages/typescript.dag:2`), but no source-ingestion run transcript was found. Missing for PROVEN: per-language ingest receipts. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1193` | `GAP` | `SUBSTRATE_PRESENT` | **L5 cross-target consistency**: same `.dag` program emitted to ALL targets (Rust/Python/Go/C/C++/LLVM IR/TypeScript) produces equivalent behavior. Test surface scales by target count — is the test framework substrate (`src/v4/test/claim/`) per-target-parameterized or per-target-duplicated? | Language behavior claims are generated (`src/v4/test/claim/generated/language_behavior_equivalence.dag`) and §14 target models exist (`src/v4/extdeps/languages/cpp.dag:1`, `src/v4/extdeps/languages/llvm_ir.dag:1`, `src/v4/extdeps/languages/typescript.dag:1`), but no all-target equivalence run was found. Missing for PROVEN: Rust/Python/Go/C++/LLVM IR/TypeScript equivalence verdicts. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1194` | `GAP` | `SUBSTRATE_PRESENT` | **L6 form completeness**: do all 6 connectives × 5 behaviors emit to every target? Per §3.5 L6 matrix. | C++/LLVM IR/TypeScript model substrates exist (`src/v4/extdeps/languages/cpp.dag:47`, `src/v4/extdeps/languages/llvm_ir.dag:49`, `src/v4/extdeps/languages/typescript.dag:57`), but no L6 6x5 matrix run across these targets was found. Missing for PROVEN: per-target L6 matrix results. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1195` | `GAP` | `SUBSTRATE_PRESENT` | **Falsification probe**: pick a `.dag` form that emits cleanly to Rust but cannot emit to LLVM IR (e.g., something Rust's borrow checker accepts but LLVM IR's SSA form rejects). Does v4 fail-closed when LLVM IR is targeted, or silently emit broken IR? | LLVM IR structural vocabulary is modeled (`src/v4/extdeps/languages/llvm_ir.dag:66`, `src/v4/extdeps/languages/llvm_ir.dag:78`) and C++/TypeScript models exist, but no fail-closed LLVM-target falsification run was found. Missing for PROVEN: negative emission diagnostic for a form that cannot target LLVM IR. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §15 Framework substrates — React, future UI/server frameworks (NEW for v4, was R4 canvas)

Section total: 4 probes — ship_disposition: 0 PROVEN, 4 GAP; engineering_state: 3 SUBSTRATE_PRESENT, 1 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1219` | `GAP` | `SUBSTRATE_PRESENT` | Per framework: are its carriers structurally modeled (Component, Hook, etc.) or just stringly-named? | React framework substrate exists for components, hooks, context, refs, keys, effect cleanup, and dependency arrays (`src/v4/extdeps/frameworks/react.dag:1`, `src/v4/extdeps/frameworks/react.dag:47`, `src/v4/extdeps/frameworks/react.dag:117`). Missing for PROVEN: actual React ingest/emit/typecheck/demo receipts. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1220` | `GAP` | `SUBSTRATE_PRESENT` | **Cross-language consistency**: React substrate emits to TypeScript (typed React) and to JavaScript (untyped). Does the substrate distinguish meta-typing (the framework's structural rules) from target-language-typing? | React framework substrate exists for components, hooks, context, refs, keys, effect cleanup, and dependency arrays (`src/v4/extdeps/frameworks/react.dag:1`, `src/v4/extdeps/frameworks/react.dag:47`, `src/v4/extdeps/frameworks/react.dag:117`). Missing for PROVEN: actual React ingest/emit/typecheck/demo receipts. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1221` | `GAP` | `SUBSTRATE_PRESENT` | **Bidirectional**: can v4 ingest existing React components (per design-r4-canvas §Q3-b "TS class → Component") AND emit them? | React framework substrate exists for components, hooks, context, refs, keys, effect cleanup, and dependency arrays (`src/v4/extdeps/frameworks/react.dag:1`, `src/v4/extdeps/frameworks/react.dag:47`, `src/v4/extdeps/frameworks/react.dag:117`). Missing for PROVEN: actual React ingest/emit/typecheck/demo receipts. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1222` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: introduce a framework-specific anti-pattern (e.g., a React Hook called inside a conditional). Does the React-substrate lens catch it at compile-time? | React framework carriers exist, but searched `src/v4` found no emitted React demo app, anti-pattern compile-time rejection transcript, or run output. | Delivery artifact or executable demo receipt for the requested probe. |
### §16 Multi-program / network coordination (NEW for v4, was §3.8 forward-pointer)

Section total: 7 probes — ship_disposition: 0 PROVEN, 7 GAP; engineering_state: 5 SUBSTRATE_PRESENT, 2 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1257` | `GAP` | `NO_ARTIFACT_FOUND` | Multi-program shape: how does `.dag` express "this fragment runs on machine A, this on machine B"? | Coordination carriers exist, but searched `src/v4` found no multi-service demo execution or emitted deployment transcript. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1258` | `GAP` | `SUBSTRATE_PRESENT` | Wire derivation extension: does extending gate #28 `omni_layers_share_one_node_tree` to "share one Dag across deployment units" hold? | Coordination substrate models endpoints, exchange patterns, wire contracts, and deployment units (`src/v4/extdeps/coordination.dag:1`, `src/v4/extdeps/coordination.dag:48`, `src/v4/extdeps/coordination.dag:128`, `src/v4/extdeps/coordination.dag:143`). Missing for PROVEN: end-to-end multi-program coordination run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1259` | `GAP` | `SUBSTRATE_PRESENT` | Coordination semantics: are sync/async/stream/pub-sub first-class behaviors OR compositions over existing 5 behaviors? | Coordination substrate models endpoints, exchange patterns, wire contracts, and deployment units (`src/v4/extdeps/coordination.dag:1`, `src/v4/extdeps/coordination.dag:48`, `src/v4/extdeps/coordination.dag:128`, `src/v4/extdeps/coordination.dag:143`). Missing for PROVEN: end-to-end multi-program coordination run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1260` | `GAP` | `SUBSTRATE_PRESENT` | Failure-at-boundary: how does "partial failure" compose with effect-enumeration (§1.4)? | Coordination substrate models endpoints, exchange patterns, wire contracts, and deployment units (`src/v4/extdeps/coordination.dag:1`, `src/v4/extdeps/coordination.dag:48`, `src/v4/extdeps/coordination.dag:128`, `src/v4/extdeps/coordination.dag:143`). Missing for PROVEN: end-to-end multi-program coordination run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1261` | `GAP` | `SUBSTRATE_PRESENT` | Idempotency at endpoint: composes with idempotency lens (THESIS:188 + §3.6)? | Coordination substrate models endpoints, exchange patterns, wire contracts, and deployment units (`src/v4/extdeps/coordination.dag:1`, `src/v4/extdeps/coordination.dag:48`, `src/v4/extdeps/coordination.dag:128`, `src/v4/extdeps/coordination.dag:143`). Missing for PROVEN: end-to-end multi-program coordination run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1262` | `GAP` | `SUBSTRATE_PRESENT` | Cross-endpoint dimension propagation: does affected-set lens extend across deployment-unit boundaries? | Coordination substrate models endpoints, exchange patterns, wire contracts, and deployment units (`src/v4/extdeps/coordination.dag:1`, `src/v4/extdeps/coordination.dag:48`, `src/v4/extdeps/coordination.dag:128`, `src/v4/extdeps/coordination.dag:143`). Missing for PROVEN: end-to-end multi-program coordination run. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1263` | `GAP` | `NO_ARTIFACT_FOUND` | **Falsification probe**: design a 2-endpoint distributed program. Demonstrate end-to-end emission per Shape-A + the wire contract per Shape-B + coordination behavior captured structurally. | Coordination carriers exist, but searched `src/v4` found no multi-service demo execution or emitted deployment transcript. | Delivery artifact or executable demo receipt for the requested probe. |
### §17.1 C4 — Additional MachineConstraint axes

Section total: 3 probes — ship_disposition: 0 PROVEN, 3 GAP; engineering_state: 3 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1287` | `GAP` | `SUBSTRATE_PRESENT` | Per target: are RegisterClass / EndianMode / Alignment declared structurally (typed carriers) or string-keyed? | Machine-code substrate includes endian and register classes (`src/v4/extdeps/languages/machine_code.dag:53`, `src/v4/extdeps/languages/machine_code.dag:137`), but no emitted assembly/bytecode run receipt was found. Missing for PROVEN: target execution/falsification across register/endian cases. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1288` | `GAP` | `SUBSTRATE_PRESENT` | Cross-target consistency: emit same `.dag` program to two targets with different EndianMode. Do the emitted serializations agree (modulo endian)? | Machine-code substrate includes endian and register classes (`src/v4/extdeps/languages/machine_code.dag:53`, `src/v4/extdeps/languages/machine_code.dag:137`), but no emitted assembly/bytecode run receipt was found. Missing for PROVEN: target execution/falsification across register/endian cases. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1289` | `GAP` | `SUBSTRATE_PRESENT` | Falsification: write a `.dag` program that depends on EndianMode being Big. Emit to a Little-endian target. Does the compiler fail-closed or silently emit broken serialization? | Machine-code substrate includes endian and register classes (`src/v4/extdeps/languages/machine_code.dag:53`, `src/v4/extdeps/languages/machine_code.dag:137`), but no emitted assembly/bytecode run receipt was found. Missing for PROVEN: target execution/falsification across register/endian cases. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §17.2 C5 — Rounding-mode product-shape extension

Section total: 2 probes — ship_disposition: 0 PROVEN, 2 GAP; engineering_state: 2 SUBSTRATE_PRESENT, 0 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1298` | `GAP` | `SUBSTRATE_PRESENT` | Float arithmetic: does v4 model rounding-mode as a structural axis or assume IEEE-754 default? | Floating-point rounding laws are modeled in target language files (e.g. `src/v4/extdeps/languages/rust.dag:482`, `src/v4/extdeps/languages/wasm.dag:1981`). Missing for PROVEN: executed numeric-equivalence/falsification cases. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
| `docs/v4-close-interrogation.md:1299` | `GAP` | `SUBSTRATE_PRESENT` | Cross-language: Rust's `f64::round` vs Python's `round()` vs Go's `math.Round` — do their semantics agree at the v4 substrate level? | Floating-point rounding laws are modeled in target language files (e.g. `src/v4/extdeps/languages/rust.dag:482`, `src/v4/extdeps/languages/wasm.dag:1981`). Missing for PROVEN: executed numeric-equivalence/falsification cases. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
### §17.3 C6 — Aspect-axis (PointKind) for instant/duration/rate

Section total: 2 probes — ship_disposition: 0 PROVEN, 2 GAP; engineering_state: 1 SUBSTRATE_PRESENT, 1 NO_ARTIFACT_FOUND, 0 CENSUS_NOT_RUN.

| Source | ship_disposition | engineering_state | Probe | Evidence | blocking_receipt |
|---|---|---|---|---|---|
| `docs/v4-close-interrogation.md:1308` | `GAP` | `NO_ARTIFACT_FOUND` | Time arithmetic: subtracting two `Instant` values produces `Duration` (Magnitude). Is this enforced structurally? | Searched `src/v4/std/datetime.dag` and target extdeps: `Instant` exists, but no `Duration`/`PointKind` enforcement receipt was found. | Delivery artifact or executable demo receipt for the requested probe. |
| `docs/v4-close-interrogation.md:1309` | `GAP` | `SUBSTRATE_PRESENT` | Type errors: adding two `Instant` values (semantically nonsensical) should fail at compile time. Demo. | Time substrate includes `Instant` and constructors (`src/v4/std/datetime.dag:147`, `src/v4/std/datetime.dag:774`). Missing for PROVEN: executed time/duration/point-kind falsification. | Executable `TestClaimRun` verdict plus adversarial falsification transcript for this probe. |
