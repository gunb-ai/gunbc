---
session: silent-raven-384
node: adhoc-7020540d-622
date: 2026-05-30
parent: nimble-dove-733 (PM May 29)
artifact: validation audit for `docs/v4-close-interrogation.md`
base: origin/main @ 18ddb8a203b0d801ca2e2257c5c6c37c5f7f2d3b
---

# v4-close interrogation - validation audit (2026-05-30)

## Verdict

**INTERROGATION NOT ANSWERED.** Against current `main` HEAD, all 346 probes in `docs/v4-close-interrogation.md` disposition as **GAP** for ship validation.

This is deliberately strict. Many owner files, substrate rows, and TestClaim scaffolds exist. They do not satisfy the questionnaire's §0 four-receipt bar unless the audit can cite the promise, delivery, reproducible concrete output, and falsification attempt. The live tree still records the key execution and close-artifact blockers: TestClaim corpus execution is not wired as structured CI verdicts, `RoundTripClaim` is still deferred, T-15 is partial, and no v4 close predicate/close audit artifact exists.

## Summary Table

| Section | Area | PROVEN | WEAK-EVIDENCE | GAP | NOT-CHECKED | ODR | NOT-IN-V4 | NOT-PROMISED |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| §1.1 | §1.1 Complexity | 0 | 0 | 9 | 0 | 0 | 0 | 0 |
| §1.2 | §1.2 Cost | 0 | 0 | 15 | 0 | 0 | 0 | 0 |
| §1.3 | §1.3 Parallelism | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §1.4 | §1.4 Effect enumeration | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §1.5 | §1.5 User-defined dimensions | 0 | 0 | 7 | 0 | 0 | 0 | 0 |
| §1.6 | §1.6 Tier 1 mechanics (coercion = emission / ownership / grounding completeness) | 0 | 0 | 13 | 0 | 0 | 0 | 0 |
| §1.7 | §1.7 Tier 2 runtime safety (proven safe or total) | 0 | 0 | 8 | 0 | 0 | 0 | 0 |
| §2.1 | §2.1 Pure Bootstrap (zero hand-Rust) | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §2.2 | §2.2 Closed system / no escape hatches | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §2.3 | §2.3 Single authority / cost-of-change = 1 | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §2.4 | §2.4 Fail-closed discipline | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §2.5 | §2.5 Impossible bugs by construction (THE META-PROMISE) | 0 | 0 | 45 | 0 | 0 | 0 | 0 |
| §2.6 | §2.6 Substrate-shape specifics (6 connectives + 5 behaviors + C1 stop-signal) | 0 | 0 | 12 | 0 | 0 | 0 | 0 |
| §2.7 | §2.7 Modeling discipline | 0 | 0 | 16 | 0 | 0 | 0 | 0 |
| §3.1 | §3.1 Omni-emission (R3 = 3 Shape-A targets: Rust / Python / Go) | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §3.2 | §3.2 Workflow-as-data | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §3.3 | §3.3 Tests-as-data | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §3.4 | §3.4 Full-stack-from-one-`.dag` — visceral 4-layer omni-emission + R4 framework substrate (FORWARD POINTER) | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §3.5 | §3.5 L6 — every structural form compiles to every target | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §3.6 | §3.6 L7 — operations obey declared algebraic laws | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §3.7 | §3.7 The verification-machinery promises (testgen / integration / mocks / dry-run) | 0 | 0 | 27 | 0 | 0 | 0 | 0 |
| §3.8 | §3.8 Multi-program / network-coordinated emission from one `.dag` (FORWARD POINTER) | 0 | 0 | 7 | 0 | 0 | 0 | 0 |
| §4.1 | §4.1 Lens self-application | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §4.2 | §4.2 Self-host fixed point | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §4.3 | §4.3 Concept unifications | 0 | 0 | 10 | 0 | 0 | 0 | 0 |
| §5.1 | §5.1 5 substrate-gap classes closed | 0 | 0 | 6 | 0 | 0 | 0 | 0 |
| §5.2 | §5.2 v2 fully retired | 0 | 0 | 3 | 0 | 0 | 0 | 0 |
| §5.3 | §5.3 BridgeLedgerZero | 0 | 0 | 3 | 0 | 0 | 0 | 0 |
| §5.4 | §5.4 Compiler-as-data residual — "is the compiler pure data yet?" | 0 | 0 | 19 | 0 | 0 | 0 | 0 |
| §5.5 | §5.5 Free consequences (when Tiers 1-2 close) | 0 | 0 | 11 | 0 | 0 | 0 | 0 |
| §6.1 | §6.1 "Show the correct code" | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §6.2 | §6.2 Audience duality / opt-in depth | 0 | 0 | 6 | 0 | 0 | 0 | 0 |
| §6.3 | §6.3 Adoption model — economics, not enforcement | 0 | 0 | 10 | 0 | 0 | 0 | 0 |
| §7 | §7. Cross-doc ledger coherence (structural — keep from v0) | 0 | 0 | 9 | 0 | 0 | 0 | 0 |
| §8 | §8. Per-gate predicate execution at close | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §10 | §10. Close ceremony | 0 | 0 | 8 | 0 | 0 | 0 | 0 |
| §13 | §13. Arbitrary ingestion — bidirectional substrate (NEW for v4, was R4) | 0 | 0 | 9 | 0 | 0 | 0 | 0 |
| §14 | §14. Additional Shape A languages — C, C++, LLVM IR, TypeScript (NEW for v4, was R4.A/R4.C) | 0 | 0 | 5 | 0 | 0 | 0 | 0 |
| §15 | §15. Framework substrates — React, future UI/server frameworks (NEW for v4, was R4 canvas) | 0 | 0 | 4 | 0 | 0 | 0 | 0 |
| §16 | §16. Multi-program / network coordination (NEW for v4, was §3.8 forward-pointer) | 0 | 0 | 7 | 0 | 0 | 0 | 0 |
| §17.1 | §17.1 C4 — Additional MachineConstraint axes | 0 | 0 | 3 | 0 | 0 | 0 | 0 |
| §17.2 | §17.2 C5 — Rounding-mode product-shape extension | 0 | 0 | 2 | 0 | 0 | 0 | 0 |
| §17.3 | §17.3 C6 — Aspect-axis (PointKind) for instant/duration/rate | 0 | 0 | 2 | 0 | 0 | 0 | 0 |
| **Total** |  | **0** | **0** | **346** | **0** | **0** | **0** | **0** |

## Why Every Probe Is GAP In This Pass

The questionnaire's own vocabulary says `GAP` means promise exists and delivery is absent, the demo does not run, or the example does not reproduce (`docs/v4-close-interrogation.md:31-40`). At HEAD, no probe has a recorded per-probe receipt bundle, and multiple live blockers prevent treating scaffolds as reproducible evidence:

- `src/v4/TASKS.md:194-195` says T-38 gates T-15's claim suite and "claims compile only today; no CI step invokes T-22 eval on the corpus."
- `src/v4/TASKS.md:274-283` says the T-38 structural bridge still does not close the runner bar; modeled runner execution with structured `TestClaimRun` verdicts remains the close condition.
- `src/v4/TASKS.md:815` records T-15 as partial/open across P5 bridge removal, T22 eval/cache work, and modeled T-22 eval + structured verdicts.
- `src/v4/TASKS.md:1182-1204` says T-24 is not done until YAML authority is derived and hand-authored workflow YAML is deleted.
- `src/v4/TASKS.md:2242` and `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3` say the T-36 round-trip claim is wired but its eval path is deferred pending T-38.
- `src/v4/compiler/05_eval.dag:1732-1736` and `src/v4/compiler/05_eval.dag:1793-1797` return `Deferred` for `RoundTripClaim`.
- `src/v4/test/claim/workflow/testclaim_corpus_runner.dag:2-4` marks the corpus runner as a wedge gated on `t38-testclaim-corpus-eval`.
- `docs/audit/v4-deferral-audit-2026-05-29.md:69-73` records 141 distinct gate names and 280 total gated annotation rows at its snapshot.
- No v4 close predicate artifact exists under `docs/audit/`; only `docs/audit/r3-close-predicate-execution-2026-05-13.md` exists for the older R3 pass.
- `docs/v4-close-interrogation.md:1096-1103` still has R3-shaped close ceremony rows, including the R3 close artifact path, rather than a completed v4 close audit artifact.

## Inventory Checks

- `grep -c '^- \[ \]' docs/v4-close-interrogation.md` -> 346
- `grep -c '^- \[x\]' docs/v4-close-interrogation.md` -> 0
- `find src/v4/test/claim -type f -name '*.dag' | wc -l` -> 172
- `find docs/audit -maxdepth 1 -type f \( -name 'v4-close*' -o -name '*v4*predicate*' -o -name '*close-predicate*' \) -print | sort` -> only the R3 predicate artifact is present

## Probe Ledger

Each row below is a source probe from `docs/v4-close-interrogation.md`, identified by source line. The status is `GAP` because the current tree lacks the close-executable receipt bundle needed to prove that probe. Section-specific scaffolds should be reused by the eventual execution sweep, but this pass does not promote scaffolds to `PROVEN`.

| Probe ID | Source line | Status | Probe | Evidence |
|---|---:|---|---|---|
| §1.1.01 | 75 | GAP | Show me a `.dag` program that violates a complexity contract. Quote the code. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.02 | 76 | GAP | Compile it. What is the verbatim error message? (`Diagnostic` with `reason` + `at`, not `panic!` or silent skip.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.03 | 77 | GAP | Show me a SECOND example with a different complexity class (linear/quadratic/exponential). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.04 | 78 | GAP | Where is the test that pins the error message? Cite `<file>:<line>`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.05 | 79 | GAP | There must be NO complexity annotation/tag anywhere (feedback_no_annotations). Show that the bound is a STRUCTURAL requirement — propagated from the consuming position / algebra inhabitance — not a `@complexity`-style tag. If a tag exists, that is the violation. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.06 | 80 | GAP | Can I write a structure whose lens-DERIVED complexity contradicts the bound its structural context requires? Show the diagnostic. (There is no annotation to "lie" — the bug is structural inconsistency between derived cost and the position that demands the bound.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.07 | 81 | GAP | Is there a demonstration `.dag` program that exercises Complexity end-to-end via the v3 evaluator? Cite path. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.08 | 82 | GAP | Run the demonstration on clean checkout. What does stdout/stderr say? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.1.09 | 83 | GAP | **Falsification probe**: write a program whose complexity is wrong in a way the lens hasn't been tested against. Does the lens catch it? Or did we only test the cases we already knew worked? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.01 | 91 | GAP | Show me a `.dag` program with a cost budget. Quote it. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.02 | 92 | GAP | Show me the symbolic-cost output the lens produces. Verbatim. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.03 | 93 | GAP | Show me a program that EXCEEDS its cost budget. What error fires? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.04 | 94 | GAP | Where is the test? Cite path. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.05 | 95 | GAP | Is the cost arithmetic actually composing? Show me a multi-level program (call within call) and the symbolic-cost result. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.06 | 99 | GAP | Show me a √n algorithm (e.g., trial division primality). Cost-lens output should be `PolynomialCost { degree: 1/2 }`, NOT `UnknownCost`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.07 | 100 | GAP | Show me an exponential algorithm (subset enumeration). Cost-lens output should be `ExponentialCost { base: 2 }`, NOT `UnknownCost`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.08 | 101 | GAP | Show me a factorial algorithm (permutation enumeration). Cost-lens output should be `FactorialCost`, NOT `UnknownCost`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.09 | 102 | GAP | Show me a polylog algorithm (e.g., repeated binary search log² n). Cost-lens output should be `PolyLogCost { exponent: 2 }`, NOT `UnknownCost`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.10 | 103 | GAP | Show me a matrix-multiplication cost program (n^2.373 Coppersmith-Winograd). Cost-lens output should be `PolynomialCost { degree: 2.373 }`, NOT `UnknownCost`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.11 | 107 | GAP | Show me a union-find program (α(n) inverse Ackermann). Currently expected: `UnknownCost("Tier 2 — pending R4 named-variant canvas")` OR `ConstantCost-with-named-reason` per Director rationale; NOT a Tier-1-coverable bound being squelched. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.12 | 108 | GAP | Show me a vEB-trees program (log log n). Same expectation; nested-log structure may compose via PolyLogCost — canvas surfaces if yes. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.13 | 112 | GAP | Program with recursive call whose cost is bounded by a Tier-3 fact. Does the lens compute the bound or punt? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.14 | 113 | GAP | Program with cost in a class deliberately NOT in Tier 1 (e.g., O(n^n) hyperexponential). Is `UnknownCost("reason")` actionable for the user? Does the diagnostic name the gap class? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.2.15 | 114 | GAP | **STOP-SIGNAL trigger**: program with a Tier-1-coverable bound (e.g., √n) — verify the cost-lens does NOT collapse to UnknownCost post-gate-#105-landing. Pre-gate-#105: expected current behavior is UnknownCost (regression test). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.3.01 | 122 | GAP | Show me a `.dag` program where the parallelism lens identifies independent subgraphs. Quote the program. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.3.02 | 123 | GAP | Show me the lens's output — which nodes are flagged parallel, which serial. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.3.03 | 124 | GAP | If I add a fake dependency, does the lens correctly serialize? Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.3.04 | 125 | GAP | **Falsification probe**: program with hidden state coupling that LOOKS independent. Does the lens report independent (wrong) or report state coupling (right)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.4.01 | 133 | GAP | Show me a program with mixed pure + I/O effects. What does the lens classify per location? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.4.02 | 134 | GAP | Show me a "pure" claim on a function that secretly does I/O. Does the lens catch the contradiction? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.4.03 | 135 | GAP | Where is the test fixture? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.4.04 | 136 | GAP | **Falsification probe**: effect-leaks through a callback or higher-order construct. Does the lens trace it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.01 | 144 | GAP | Show me ONE user-defined lens that lives in `.dag` (not Rust). Cite the file. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.02 | 145 | GAP | Show me a program it validates. Show the output. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.03 | 146 | GAP | Did the user-defined lens use the same mechanism as built-in? Or a parallel infrastructure? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.04 | 147 | GAP | **Falsification probe**: would a NEW user-defined lens (one we haven't written yet) actually work end-to-end? Author a simple one. Does it compile and run? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.05 | 151 | GAP | **If a user needs O(α(n)) or O(log log n)** — bounds NOT in Tier 1 of gate #105: can they author a user-defined cost-variant lens that integrates with `SymbolicCost`'s dominance lattice + Sum/Product algebra, OR does the user-defined-dim mechanism stop at producing a separate Dimension carrier with no algebra-integration? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.06 | 152 | GAP | **Compositional vs named-variant**: if the user-defined-dim infrastructure supports compositional cost-bound extension (e.g., authoring a `LogLogCost` lens that participates in dominance), R3 may NOT need named-variants for Tier 2 (per Director caveat: "if Substrate Mgr canvas surfaces a compositional mechanism for Tier 2 that satisfies feedback_groundedness_gates_lenses + composes with Sum/Product algebra + carries consumer-evidence justification, accept that mechanism IN-R3 instead of named variants"). Test this end-to-end. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.5.07 | 153 | GAP | **Falsification probe (Tier 2 escape hatch)**: write a user-defined cost lens for a textbook-known bound that's R4-deferred (e.g., inverse Ackermann). Does it integrate? If yes — Tier 2 R4-deferral is structurally bounded. If no — user-defined-dim promise has a load-bearing gap; surface as Director-tier scope question. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.01 | 165 | GAP | Find the "coercion engine." Show me the file or argument-flow. Is it a separate phase, or does it dissolve into emission? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.02 | 166 | GAP | Show me a `.dag` value being coerced to a target representation. Cite the call site. Is the coercion logic in emission, or in a separate `coerce.rs`? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.03 | 167 | GAP | **Falsification probe**: introduce a `.dag` type with no target inhabitance. Does the compiler emit it via some default-coercion path, or fail-closed? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.04 | 171 | GAP | Show me where the compiler proves "no aliased mutation" in emitted code. Cite the proof site (lens, predicate, or test). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.05 | 172 | GAP | Show me an `.dag` program that would COMPILE if aliased mutation were allowed but FAILS now. Demonstrate the diagnostic. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.06 | 173 | GAP | Where is "aliasing" modeled in the substrate? Is it a Behavior shape, a Cardinality property, or implicit-via-purity? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.07 | 174 | GAP | **Falsification probe**: write a `.dag` program that mutates the same logical resource from two call sites. Does the compiler reject it, or emit potentially-aliasing target code? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.08 | 178 | GAP | Show me the structural model of Rust primitives. Cite `dsl/extdeps/languages/rust/primitives.dag` (or wherever). Does each Rust primitive have algebra-inhabitance declared (e.g., `i32 inhabits OrderedRing`)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.09 | 179 | GAP | Same for Python data model. Same for Go specification. Cite both. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.10 | 180 | GAP | Show me the algebra-homomorphism-search code path. Cite the function. Is it a structural search, or a name-keyed table lookup? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.11 | 181 | GAP | Pick a target primitive at random. Trace the grounding chain: `.dag` type → algebra inhabitance → target primitive. Is every step structural? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.12 | 182 | GAP | **Falsification probe**: introduce a `.dag` type carrying algebra X. No target language inhabits X. Does the compiler fail-closed with a named diagnostic? Or does it pick a "closest" target primitive (failure mode — name-keyed shortcut)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.6.13 | 183 | GAP | **Per-target falsification**: for each of Rust / Python / Go, find a `.dag` algebra with no target inhabitance. Verify fail-closed on each. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.01 | 193 | GAP | **Division by zero**: show me a `.dag` program with a division. Show the divisor's proven-non-zero predicate (or the total-form making it safe). Cite the lens / check. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.02 | 194 | GAP | **Integer overflow**: show me a `.dag` arithmetic expression whose target-side overflow is bound by structural analysis. Cite the cost-lens-or-machine-constraint composition. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.03 | 195 | GAP | **Out-of-bounds**: show me a `.dag` indexed access. Show the index's proven-in-range predicate (or the total-form making OOB unrepresentable). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.04 | 196 | GAP | **Force-unwrap**: search for force-unwrap patterns in the `.dag` substrate. Should be zero in the language surface. If present, justify as user-input-boundary. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.05 | 197 | GAP | **Partial functions generally**: enumerate every partial primitive operation in `dsl/std/`. For each, show the totalization (Option-return / Diagnostic / refinement-precondition). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.06 | 201 | GAP | Author a `.dag` program with division where the divisor's non-zero predicate is unprovable from structure. Does the compiler reject (Tier 1) or insert a runtime check (Tier 2 total)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.07 | 202 | GAP | Author an integer-arithmetic program where overflow CANNOT be proven safe. Does the compiler reject or insert a Diagnostic-returning total form? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §1.7.08 | 203 | GAP | If a partial primitive lands in `dsl/std/` post-R3, what catches it? (Should be a §1.8 ratchet or anti-pattern; cite.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.1.01 | 217 | GAP | Run the PB-0 census predicate. What's the count? Should be 0. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.1.02 | 218 | GAP | If non-zero: enumerate each survivor. Justify each as named-retirement-schedule. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.1.03 | 219 | GAP | If zero: when was census last run? Cite SHA. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.1.04 | 220 | GAP | Compile gunbc with itself end-to-end. Does the output match a reference build? Bit-for-bit, or just behavioral? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.1.05 | 221 | GAP | **Falsification probe**: write a new compiler feature in pure `.dag` (no Rust). Does it self-host? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.2.01 | 229 | GAP | grep for `unsafe` in `src/v3/`. Count: 0 expected. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.2.02 | 230 | GAP | grep for `panic!()` outside narrow boundaries. Where are the survivors? Justify each. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.2.03 | 231 | GAP | grep for annotations (`#[...]` on user-facing constructs). Should be zero per `feedback_no_annotations`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.2.04 | 232 | GAP | grep for metadata markers (`__is_X` strings). Should be zero per `feedback_no_metadata_markers`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.2.05 | 233 | GAP | **Falsification probe**: write a program that tries to "leave the stack" via an opaque-string back-channel. Does the compiler accept it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.3.01 | 241 | GAP | Add a new built-in type to the language. How many files change? Show the diff line count per file. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.3.02 | 242 | GAP | Add a new expression form. Same question. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.3.03 | 243 | GAP | Add a new emission target (or extend an existing one). Same question. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.3.04 | 244 | GAP | If the count is >1 for any case: name the files, explain WHY they need editing. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.3.05 | 245 | GAP | **Falsification probe**: pick a recent PR that added a feature. Re-do the change. Was it ACTUALLY 1 file, or were there hidden auxiliary edits? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.4.01 | 253 | GAP | grep for `Option::None` returns in lens read channels. Should be zero (replaced with `Witness<C>::Violates`). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.4.02 | 254 | GAP | grep for `unreachable!()` outside truly-impossible-by-construction paths. Justify each survivor. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.4.03 | 255 | GAP | Trigger an error condition at runtime. Is the result a `Diagnostic` with `reason: <named>` + `at: <port_or_node>`, or something less structured? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.4.04 | 256 | GAP | **Falsification probe**: write a malformed program. Does the compiler emit a clear diagnostic that points to the structurally correct alternative (per THESIS.md:103-105 "Show the correct code")? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.01 | 266 | GAP | Enumerate the bug classes the architecture claims are impossible **against canonical authority** (THESIS.md:370-413 "Enumerable impossible-bug classes" + ROADMAP.md:93 T-Demo R1/R2+ split — NOT a PM-fabricated count). At HEAD, THESIS commits to: **[R1]** Suboptimal-complexity contract violation + Idempotency-contract violation + Transport/type drift; **[R2+]** Nested-optional flatten + Unenumerated effects + Unhandled diagnostic paths. R3 close audit MUST verify against the canonical THESIS enumeration, NOT a session-author guess. For each [R1] class: cite the substrate fact that prevents it + the demo fixture (per ROADMAP T-Demo). For each [R2+] class: confirm V4-IN-SCOPE per operator ratification 2026-05-15 ("Please frontload R2 into v4 — and any other impossible-bug classes"); each R2+ class has a TestClaim scaffold in `src/v4/test/claim/impossible_bug/`. **Anti-pattern**: hard-coding a candidate list at audit-author time rather than deferring to THESIS authority — `feedback_thesis_gate_state_drift`-class miss; classes may have been added/removed in THESIS since the audit was written. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.02 | 267 | GAP | For each: write a `.dag` program that ATTEMPTS the bug-class. Does the compiler refuse to compile? Show the diagnostic. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.03 | 268 | GAP | **Discrimination probe**: is "impossible" actually "(a) impossible to express in surface vocabulary" OR "(b) caught at compile time by lens"? They differ. (a) means the bug-shape has no syntactic form. (b) means the bug compiles syntactically but fails a check. Both prevent the bug at user-visible level; only (a) prevents the bug-shape from existing in the substrate at all. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.04 | 269 | GAP | **Compiler-correctness gating probe**: if the prevention is "caught at compile" (b-shape), what happens if the compiler itself has a bug? Is the prevention then defective? How is the compiler's correctness gated? (R3 close pointer: PB-self-compile fixed point + lens self-application.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.05 | 270 | GAP | **Falsification probe**: name ONE bug class the architecture historically claimed was "impossible" but turned out to have an instance. What was the cause — structural gap, lens gap, modeling error, or implementation bug? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.06 | 276 | GAP | **Glue #1 — substrate (`.dag`) → emit target (Rust/Python/Go)**: How does the system PROVE that emitted target output faithfully realizes substrate semantics? Cite the proof. Show a concrete example. (R3 anchor: §3.1 L4 runtime equivalence + §3.1 L6 EmissionPathProjection data-coverage.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.07 | 277 | GAP | **Glue #2 — emitter ↔ runtime**: even if substrate is correct + emission faithful, runtime behavior (memory layout, scheduler, OS, language-runtime) may diverge. Where does the architecture handle these vs. assume them? Where does "impossible by construction" become "trust the runtime"? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.08 | 278 | GAP | **Glue #3 — lens composition** (lens A output → lens B input): if lens A produces `WitnessA<C>` and lens B reads it as input, what enforces structural compatibility? (R3 anchor: `Witness<C>` shape lock + `Lens<C>` per-Behavior typed channel per design-lens-framework.md.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.09 | 279 | GAP | **Glue #4 — bootstrap** (v3 substrate ↔ stage0 Rust): if stage0 has a bug that affects v3 substrate generation, is the bug caught by `pb_self_compile_fixed_point`? If stage0's generated code is wrong but compiles cleanly, what catches it? (R3 anchor: gate #16 self-compile fixed point + gate #18 numeric_width_refinements.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.10 | 280 | GAP | **Glue #5 — `ExecuteCommand` PB-Runtime boundary**: external-toolchain boundary tests run via `ExecuteCommand`. The boundary itself is opaque to the lens framework. Is THAT a glue bug class, or is "calling external thing is by-definition outside scope"? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.11 | 281 | GAP | **Falsification probe**: identify a glue boundary in the current architecture with NO structural enforcement of correctness — just convention, comment, or runtime assertion. List 3 concrete examples at HEAD. Is each an "impossible bug" class, or a class that's still possible just by being at an interface? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.12 | 287 | GAP | **Intent vs. spec**: write a `.dag` program that compiles cleanly + satisfies ALL lens contracts + does something the user obviously didn't intend (e.g., `sort_descending` named but body is `sort_ascending`; both type-correct). How many "impossible bug" classes does this hit? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.13 | 288 | GAP | **Wrong contract**: if the user's CONTRACT (Lens enforcement budget, structural cost bound, algebra/effect inhabitance — all compositional, no annotations) is wrong, the compiler accepts compliance with the wrong contract. How is the CONTRACT'S correctness checked? Or is that meta-level out-of-scope by design? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.14 | 289 | GAP | **Empty program**: an always-correct but useless program (`fn main = unit`). Does the system distinguish "no work" from "intended no work"? Or is "user wrote what they meant" axiomatic? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.15 | 290 | GAP | **Spec-as-program collapse**: when spec and implementation are the same artifact (.dag), is the user error "wrote wrong spec" identical to "wrote wrong program"? Does the architecture's "no parallel authority" discipline mean user-error is single-point-of-failure rather than divergence-detectable? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.16 | 291 | GAP | **Falsification probe**: enumerate 3 concrete user-error classes the architecture can NEVER catch by construction. Confirm that "impossible bugs by construction" is shorthand for "structurally-defined bug classes are impossible", not "all user errors are impossible". | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.17 | 297 | GAP | **Lens-composition emergent**: lens A + lens B individually correct + composed in unexpected order. Show 2 lenses where composition order MATTERS; demonstrate ordering effect on output. (R3 anchor: `Lens<C>` typed per-Behavior channels per design-lens-framework.md — is ordering structural or behavioral?) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.18 | 298 | GAP | **Scale emergent**: 1000-program corpus where each is individually correct but together produce a performance / cost / behavior surprise (e.g., aggregate cost dominates per-program cost; cross-program coupling). What's the architecture's tooling for catching scale-only bugs? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.19 | 299 | GAP | **Time-evolving emergent**: substrate at time T compiles cleanly + at time T+1 (with new dependencies / extdeps revision) compiles differently or with different cost. Is the divergence caught? (R3 anchor: `feedback_thesis_gate_state_drift` — gate state drifts over time.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.20 | 300 | GAP | **Lens-set silent gap**: a 2-lens composition where the composition reports "all lenses green" but the actual program behavior is wrong because a third dimension was needed (and not modeled). Does the architecture's "lenses are folds over physics" claim catch THIS, or does "all modeled lenses green" silently mean "the modeled dimensions are individually green; jointly silent on unmodeled dimensions"? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.21 | 301 | GAP | **Falsification probe**: construct a `.dag` program where 4 individual lens claims (complexity, cost, parallelism, effect) all pass cleanly + the program is observably wrong (e.g., wrong arithmetic, wrong I/O sequence). How many such constructions exist? Is the architecture's response "model the missing dimension" or "user error is out-of-scope"? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.22 | 311 | GAP | **Cross-module effect-leak through "pure" boundary**: Module A declares `compute_stats(x)` as pure. Module B's helper it calls writes to disk. Traditional compilers catch only if Module A is RE-ANNOTATED. gunbc's effect lens folds over the full Dag — Module B's I/O propagates to A's call sites structurally without annotation maintenance. (R3 anchor: gate #82 `effect_enumeration_lens_behaviorally_complete`.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.23 | 312 | GAP | **Cross-module cost-composition emergence**: Module A wraps `each(items, \|x\| Module_B.process(x))` thinking it's O(n). Module B's `process` is O(n²) internally. Traditional: zero visibility into composed cost. gunbc: cost lens reads algebra + realization across modules — `Cost(A.each) = Cost(B.process) · n` falls out structurally. (R3 anchor: gates #70 `cost_lens_demonstration` + #105 `symbolic_cost_textbook_coverage_landed`.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.24 | 313 | GAP | **Cross-module dimensional drift / unit confusion**: Module A's `Time` is "ms since epoch"; Module B's `Time` is "ns since epoch". Both ground to `i64`. Traditional: type system doesn't carry the unit. gunbc: dimensional fact lives in the algebra/witness — `Time<MS>` and `Time<NS>` are structurally different even when both ground to the same primitive. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.25 | 314 | GAP | **Cross-module ordering / sequencing assumption**: Module A: `validate(x); save(x)`. Module B refactors to `save(x); validate(x)`. Traditional: zero structural way to express ordering invariant. gunbc: the Dag IS a structural ordering; "save must follow validate" is a relation on Node identities. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.26 | 315 | GAP | **Cross-module callback effect-set drift**: Module A's `with_lock(callback)` assumes callback is pure. Module B's callback later acquires another lock → deadlock class. Traditional: callback contracts are convention. gunbc: callback's effect-set is structurally readable by lens framework. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.27 | 316 | GAP | **Cross-module aliasing / shadow definition**: Module A: `const MAX_RETRIES = 3`. Module B re-declares `MAX_RETRIES = 5`. Traditional: name collision detected only at link time. gunbc: P1 single-authority refuses parallel definitions structurally — second declaration is a compile-time INVARIANTS-P1 violation. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.28 | 317 | GAP | **Cross-module data-flow capability leak**: Module A's `read_secrets()` returns `Secret<String>`. Module B's caller strips into plain `String` via implicit conversion. Traditional: capability typing if you have it (most don't). gunbc: Secret is a structural carrier in the algebra DAG; downcasting requires explicit lens application, structurally trackable. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.29 | 323 | GAP | **Cross-target serialization round-trip**: Module A (emitted Rust) sends `User { id, name, email }` over wire to Module B (emitted Go). Both emissions derive from the same `.dag` substrate. Field rename in `.dag` → both emissions update structurally; no schema-drift class possible. **Falsification**: rename a field in `.dag`; verify both Rust + Go (+ Python) emissions update; observe any cross-target consumer that didn't rebuild. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.30 | 324 | GAP | **Cross-target numeric width**: `.dag` declares `Counter: Nat<32>`. Rust emits `u32`; Go emits `uint32`; Python emits arbitrary-precision `int`. At `Counter > 2^32`, Rust wraps/panics, Go wraps, Python silently widens. Traditional: zero cross-language analysis. gunbc: cost-lens + dimensional carrier reads ALL THREE emissions' realization cost — Python's arbitrary-precision `int` carries different overflow semantics than Rust's `u32` or Go's `uint32`, structurally expressible per dim-substrate (R3 anchor: gate #18 `numeric_width_refinements_landed` + Q-MachineConstraint-Carrier). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.31 | 325 | GAP | **Cross-target effect divergence**: `.dag` declares an `async` operation. Rust emits via `tokio` futures; Go emits via `goroutines + channels`; Python emits via `asyncio`. Cancellation/coroutine semantics differ across all three. Traditional: each emission target writes its own async-handling; cross-target tests catch only late. gunbc: substrate models async as structural fact; per-target LanguageSpec encodes realization; cost-lens reads composition. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.32 | 326 | GAP | **Cross-target boundary trust**: Rust service calls Go service via gRPC / HTTP / FFI / shared-memory. Type marshaling at the boundary. Traditional: protobuf-like schema at best (post-hoc). gunbc: BOTH ends derive from same `.dag` declaration; marshaling is structural emission per target. (Rust ↔ Python via PyO3 / Rust ↔ Go via cgo are alternate boundary realizations of the same probe.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.33 | 327 | GAP | **Cross-target test-claim transferability**: `.dag` TestClaim asserts behavior X. Rust emission runs it via `cargo test`; Python via `pytest`; Go via `go test`. All three should pass-or-fail identically for the SAME claim. Traditional: tests are language-specific; cross-language test-claims don't exist. gunbc: per gate #15 `l5_cross_target_consistency` — for every `.dag` program, emitted Rust/Python/Go produce equivalent runtime behavior on the certification corpus. (R3 close anchor — see §3.1 for L5 status.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.34 | 331 | GAP | **Pick a `.dag` program** that compiles + lens-passes cleanly + emit to all 3 R3 targets (Rust/Python/Go). Run the same fixture through each. Do outputs agree on every (input, output) pair? Where they don't agree, is the divergence: (a) a target-realization fidelity gap caught by L5 lens, (b) a substrate ambiguity (modeling-level error), or (c) a target-LanguageSpec drift (extdeps issue)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.35 | 332 | GAP | **Construct a cross-language wire scenario**: emit Rust client + Go server (or Python server) from the same `.dag` substrate. Have client send `User { ... }` to server; verify field-rename propagation, type-marshaling, async-cancellation semantics. Does the structural emission preserve all the facts the lens framework reads? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.36 | 333 | GAP | **Modeling-level cross-target gap**: identify a target-language-specific bug class (e.g., Go's nil-interface vs nil-concrete-type distinction, Rust's lifetime-pin-projection, Python's GIL-aware threading) that does NOT have substrate representation. Confirm the architecture's response: "model the missing dimension" or "target-specific gap is in extdeps lane". | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.37 | 392 | GAP | Where does affected-set live in CI/build? Cite the workflow file + the invocation site. Is it `gunbc query affected-set --since=main`? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.38 | 393 | GAP | What's the SHA-diff input shape? `(Dag_before, Dag_after)` per design doc, or a different surface? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.39 | 394 | GAP | Show me a concrete subtle dep that **ONLY** the dimension-parameterized variant catches — a case where value-only-diff would miss the affected-set but cost-or-complexity-diff catches it. Cite the test fixture. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.40 | 395 | GAP | Run affected-set on the last 5 merged PRs. What's the cardinality of the affected-set per PR? (Should be much smaller than transitive-downstream — that's the structural progress claim.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.41 | 396 | GAP | Show me a PR where the affected-set predicted a test would run, AND the test caught a regression that value-only-diff would have skipped. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.42 | 400 | GAP | **Cost-regression-in-uncovered-helper**: introduce a complexity regression in a helper (e.g., O(n) → O(n²)) that no test directly covers. Does affected-set flag every downstream consumer reading the cost dimension? Or does it miss them because no test directly asserts cost on the helper? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.43 | 401 | GAP | **Effect-leak-across-module-boundary**: add a side effect (I/O) to a previously-pure function. Does affected-set flag the consumers asserting purity? Or only the consumers calling the function directly? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.44 | 402 | GAP | **Cross-module dimension narrowing**: change function F's cost from `O(n)` to `O(n log n)` in a way the lens classifies correctly. Does the affected-set surface every transitive consumer reading the cost dimension, AT LEAST as far as the dimension flow propagates? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.5.45 | 403 | GAP | **PB-Runtime opacity**: introduce a change inside PB-Runtime (the bounded kernel) that affects a structural-dimension claim. Does the affected-set lens correctly identify the dimension-flow boundary at PB-Runtime, or does it under/over-propagate? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.01 | 487 | GAP | Enumerate every type at HEAD that is structurally NOT one of the 6 connectives (or composed of them). Should be ZERO. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.02 | 488 | GAP | For each of the 6 connectives, cite ONE concrete use case at HEAD (e.g., `Conj` for product types, `Arrow` for function types, `Cardinality` for refinement, `Instantiation` for template-instantiation). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.03 | 489 | GAP | Where is "Instantiation matches C++ template-instantiation vocabulary and is ONLY used for type parameterization" enforced? Show me a value-construction site — does it use plain `Conj` with optional inhabits tag (per THESIS.md:199), not `Instantiation`? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.04 | 490 | GAP | **Falsification probe**: try to author a type at HEAD that resembles a 7th connective (e.g., a "metadata" or "reflection" variant outside the 6). Does the substrate reject it, or absorb it via one of the 4 dissolution patterns? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.05 | 494 | GAP | Enumerate every Behavior at HEAD that is structurally NOT one of the 5 (Value / Transform / Branch / Loop / Bind). Should be ZERO. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.06 | 495 | GAP | For each of the 5, cite ONE concrete use case at HEAD. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.07 | 496 | GAP | Where is `Transform → FunctionRef → Arrow` composition enforced (per THESIS.md:201)? Cite the substrate connection. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.08 | 497 | GAP | **Falsification probe**: try to author a behavior at HEAD that doesn't fold into the 5 (e.g., a "guard" or "interrupt" variant). Does the substrate reject it, or absorb it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.09 | 501 | GAP | Has anyone proposed a 7th connective or 6th behavior in PRs / canvases since R1? Cite each. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.10 | 502 | GAP | For each proposal, was the C1 stop-signal protocol followed (all 4 dissolution patterns attempted before extension)? Cite the canvas / rationale. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.11 | 503 | GAP | What's the current count of dissolution patterns documented in §"Structural decompression"? (Should be 4 per THESIS.md.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.6.12 | 504 | GAP | **Falsification probe**: an R4 proposal arrives proposing a 7th connective. What's the structural gate that ensures it goes through the 4-dissolution-attempt protocol? Is this a §1.8 ratchet or an unwritten norm? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.01 | 521 | GAP | Run the unused-types lens (or grep for un-referenced `type` declarations). What's the count? Should be 0. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.02 | 522 | GAP | Pick 5 types at random from `dsl/std/`. For each, cite the consumer site. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.03 | 523 | GAP | **Falsification probe**: declare a new type with no consumer. Does the compiler / lens flag it pre-merge? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.04 | 527 | GAP | Grep for `String` or `Bool` used as discriminators at service boundaries. Enumerate. Each is a candidate for `feedback_opaque_strings_attract_heuristics`-class violation. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.05 | 528 | GAP | Pick 3 service boundaries (e.g., extdeps providers, RFC carriers). For each, are discriminators typed enums or `String`/`Bool`? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.06 | 529 | GAP | **Falsification probe**: introduce a String-typed discriminator at a service boundary. Does code review / lens / ratchet catch it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.07 | 533 | GAP | Grep for `__BUG_`, `__EMIT_BUG_`, `__TODO_`, `__PENDING_` etc. across `dsl/` + `src/v3/`. Count. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.08 | 534 | GAP | For any survivors: cite the named-dissolution path or operator-ratified justification. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.09 | 535 | GAP | **Falsification probe**: try to add a `__SENTINEL_X__` string-marker as a code path. Does ratchet / lens reject it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.10 | 539 | GAP | Run the duplicate-record-shape lens (or grep for structurally-identical types). What's the count? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.11 | 540 | GAP | Pick 5 record shapes from `dsl/std/`. For each, verify structurally-distinct from every other. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.12 | 541 | GAP | **Falsification probe**: declare two record types with identical fields/types but different names. Does the substrate flag the duplication, or accept it as namespacing? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.13 | 545 | GAP | Count hand-authored `.rs` test files (per SG-0 census `EXPECTED_HAND_AUTHORED_TEST`). What's the number at HEAD? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.14 | 546 | GAP | For each hand-authored Rust test: what predicate / effect-model / mock surface does it flag as "language-doesn't-yet-express"? Enumerate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.15 | 547 | GAP | What's the named-retirement path per test? (Cite `pb_rust_tests_outside_residual_zero` ROADMAP gate.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §2.7.16 | 548 | GAP | **Falsification probe**: try to add a new hand-authored `.rs` test for a behavior that COULD be expressed in `.dag` `TestClaim`. Does the SG-0 census reject it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.1.01 | 560 | GAP | Show me a single `.dag` program. Emit it to all 3 Shape-A targets (Rust / Python / Go). Are the outputs runnable? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.1.02 | 561 | GAP | Behavioral parity: same input through each output target. Do they produce equivalent results? (THESIS.md:180 L5 claim.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.1.03 | 562 | GAP | Pick the most complex Shape-A target (Go's goroutine substrate? Python's dynamic dispatch?). Is there a non-trivial example that compiles and runs? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.1.04 | 563 | GAP | **Falsification probe**: program with feature X. Does target Y handle X correctly, or punt to a stub? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.2.01 | 594 | GAP | Show me the `.dag` source for the CI workflow. Cite path. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.2.02 | 595 | GAP | Where does the YAML get generated from the `.dag`? Show the emitter. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.2.03 | 596 | GAP | When the CI runs, is it reading from a generated YAML, OR is `.dag` directly executed by some runner? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.2.04 | 597 | GAP | If the `.dag` source is changed, does the YAML auto-regenerate? Show me the regen path. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.2.05 | 598 | GAP | **Falsification probe**: change the `.dag` source, leave the YAML stale. Does CI fail? Or silently pass? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.3.01 | 606 | GAP | How many tests are ported to `.dag` TestClaim form? Count. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.3.02 | 607 | GAP | Pick one. Show the TestClaim source. Show it executing. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.3.03 | 608 | GAP | Where's the runner? Is it itself `.dag` or hand-Rust? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.3.04 | 609 | GAP | **Falsification probe**: add a new TestClaim entirely in `.dag`, no Rust. Does it run? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.4.01 | 628 | GAP | Demo: one `.dag` → 4 artifacts (Rust + SQL DDL + OpenAPI + Markdown). Show them. Show coherence test passing. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.4.02 | 629 | GAP | Canvas: 5 Director Q-dispositions ratified. Cite anchor. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.4.03 | 630 | GAP | Falsification probe: introduce a divergence between any two of the 4 emitted artifacts at the substrate level. Does gate #28 catch it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.4.04 | 631 | GAP | R4 thesis-pitch: extend story to TS client + React frontend. What's the smallest structural extension? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.5.01 | 641 | GAP | Enumerate the structural forms in `.dag` (the 6 connectives × 5 behaviors product space, minus disallowed combinations). For each, is it emittable to Rust? Python? Go? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.5.02 | 642 | GAP | Show me the L6 matrix at HEAD: (structural-form × target) → emit-status. Is it dense (all green), or sparse (per-target gaps)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.5.03 | 643 | GAP | Pick 5 structural forms. For each, trace the emit code path for Rust + Python + Go. Are they parallel, or does one target have special-case branching the others lack? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.5.04 | 644 | GAP | **Falsification probe**: pick a structural form whose emit is implemented for one target but stubbed for another. Does the compiler fail-closed when targeting the stubbed lang, or silently emit broken code? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.5.05 | 645 | GAP | **L6 vs L5 distinction probe**: a form that emits to all 3 targets BUT produces semantically-divergent output is an L5 failure. A form that fails to emit at all on one target is an L6 failure. R3-close: zero of either? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.6.01 | 657 | GAP | Pick an algebra carrier at HEAD (e.g., `Monoid<X>` for some X). Find the emitted operation. Show me the proof / test that the emitted op honors the algebra's law. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.6.02 | 658 | GAP | Where do the algebra-law tests live? `dsl/std/algebra_axioms.dag` (or equivalent)? Cite path. Are these `.dag` `TestClaim` declarations (per §3.3) or hand-Rust? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.6.03 | 659 | GAP | **Coverage probe**: for the 4-5 most-used algebra carriers (Monoid, Group, AbelianGroup, Ring, Field), is there a per-axiom test demonstrating laws hold post-emit? Tabulate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.6.04 | 660 | GAP | **Cross-target consistency**: do algebra-law tests pass on Rust + Python + Go independently? Or only Rust? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.6.05 | 661 | GAP | **Falsification probe**: introduce a `.dag` Monoid declaration whose emit deliberately violates associativity (e.g., a free-monoid with a non-associative concat). Does the L7 test surface catch it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.01 | 677 | GAP | Where does testgen live? Cite the file or pipeline-stage. Is it `.dag` or hand-Rust? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.02 | 678 | GAP | Pick a type from `dsl/std/` at random. Does testgen produce inhabitant + coercion tests for it? Cite the generated test count. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.03 | 679 | GAP | What's the testgen→TestClaim flow? Generated TestClaim declarations land where (in-tree, in-memory)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.04 | 680 | GAP | **Coverage probe**: count types in `dsl/std/` vs count generated tests per type. Is coverage monotonic with declared structure? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.05 | 681 | GAP | **Reshape probe per TESTING.md:343**: `m1_5_testgen_test.rs` should be spot-check not exhaustive-compile-every-claim. Is this discipline applied at HEAD? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.06 | 682 | GAP | **Falsification probe**: declare a new type in `dsl/std/` with no consumer. Does testgen generate inhabitant tests for it, or does it skip? (Per §2.7 "every type has a structural consumer" — testgen IS a consumer if it's downstream of structure.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.07 | 692 | GAP | Enumerate integration tests at HEAD. How many? In what file-locations? (`src/v3/compiler/tests/` etc.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.08 | 693 | GAP | For each: is it `.dag` `TestClaim`-shaped or hand-authored `.rs`? Tabulate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.09 | 694 | GAP | Pick 3 integration tests. For each, what fixture does it compile? What's the end-to-end coverage (which substrate carriers, which emission targets, which lenses)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.10 | 695 | GAP | **Cross-target coverage**: are integration tests run for Rust + Python + Go targets independently? Or only one? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.11 | 696 | GAP | **Mock-over-compile anti-pattern probe (per TESTING.md:84)**: does any integration test mock its compile-result rather than actually compiling? Should be zero. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.12 | 697 | GAP | **Falsification probe**: introduce a regression that ONLY surfaces end-to-end (passes unit-level tests). Does any integration test catch it pre-merge? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.13 | 709 | GAP | Pick a `.dag` program that performs I/O (e.g., HTTP call, file read). Show the effect-source as a typed parameter. Cite the carrier (e.g., `HttpClient`, `FileSystem`). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.14 | 710 | GAP | Write a test for that program substituting a fake effect-source. How much new code did you write — a fake `HttpClient` implementation, or a mock-framework invocation? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.15 | 711 | GAP | Run the test. Are there any hidden-state interactions (global state, ambient capabilities, environment dependencies)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.16 | 712 | GAP | **"No flaky tests" probe**: enumerate test-flakiness incidents in CI history. For each, what was the root cause? Hidden state, time-dependency, race? Are any caused by mocking infrastructure? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.17 | 713 | GAP | **Falsification probe**: try to write a `.dag` program that uses an ambient capability (hidden state not in parameters). Does the substrate reject it, or admit it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.18 | 714 | GAP | **Cross-test pollution probe**: run integration tests in randomized order. Does any test depend on order, or on prior test leftover state? Should be zero. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.19 | 724 | GAP | What's the `dag run --dry-run` (or equivalent) invocation? Does it exist as a CLI flag, an `IntrospectApplication` lens, or implicit-via-purity? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.20 | 725 | GAP | Run dry-run on an example `.dag` workflow. What does it output? (Expected: execution-trace lens reading + effect-summary + cost-estimate.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.21 | 726 | GAP | **Effect-shape preview probe**: pick a workflow with HTTP / DB / file effects. Can dry-run enumerate the effects it WOULD perform without performing them? Cite the lens / output. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.22 | 727 | GAP | **Cost-preview probe**: can dry-run report the symbolic cost (§1.2) of executing the workflow without executing it? (Should compose with cost-lens output.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.23 | 728 | GAP | **Affected-set composition** (per §2.5.F): does dry-run compose with affected-set lens — i.e., "given this diff, what would actually re-execute"? Or are they separate query surfaces? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.24 | 729 | GAP | **Simulated-inputs probe**: can dry-run accept simulated input values + report what the program would compute? Distinguish from "running with fake values" (which is actual execution). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.25 | 730 | GAP | **Falsification probe**: a program performs an HTTP POST. Run dry-run. Does the actual HTTP request happen (failure) or is it captured as an effect-trace entry (success)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.26 | 738 | GAP | Run a single program through testgen + integration + mocks + dry-run. Do they share one substrate-read pass, or are they four separate pipelines? (Per `feedback_holistic_over_patches` + `feedback_compositional_not_templating`: should be one substrate-read with four lens-reads.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.7.27 | 739 | GAP | **Falsification probe**: a bug surfaces in production. Could ANY of the four (testgen / integration / mocks / dry-run) have caught it pre-merge? Tabulate per bug class. If one surface ALWAYS misses, that's a gap class. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.01 | 760 | GAP | **Multi-program shape**: how does `.dag` express "this fragment runs on machine A, this on machine B"? Is it a Cluster F lens reading a "deployment-target" dimension, OR substrate-level partitioning (carriers for `DeploymentUnit` / `Endpoint`)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.02 | 761 | GAP | **Wire derivation extension**: does extending gate #28 `omni_layers_share_one_node_tree` to "share one Dag across deployment units" hold, OR does cross-deployment need a new invariant gate? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.03 | 762 | GAP | **Coordination semantics modeling**: are sync / async / stream / pub-sub first-class behaviors (a 6th L1 behavior or beyond? — would trigger C1 stop-signal per §2.6) OR compositions over existing 5 behaviors (Bind composition + effect-typed parameters)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.04 | 763 | GAP | **Failure-at-boundary**: is "partial failure" a lens read or a substrate variant? (NOT an "effect annotation" — effects are type-intrinsic, no annotation layer.) How does it compose with effect-enumeration lens (§1.4)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.05 | 764 | GAP | **Idempotency at endpoint**: composes with existing idempotency lens (per THESIS:188 "idempotency + cancellation + redundancy = algebraic simplification" + R1 demo class per THESIS:378-380)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.06 | 765 | GAP | **Cross-endpoint dimension propagation**: does the affected-set lens (§2.5.F) extend across deployment-unit boundaries? When endpoint A's cost dimension changes, are endpoint B's consumers reading A's wire-contract dimension flagged? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §3.8.07 | 766 | GAP | **Falsification probe**: design a 2-endpoint distributed program in `.dag`. Demonstrate end-to-end emission: each endpoint emits its own backend (per Shape-A) + the wire contract between them (per Shape-B) + coordination behavior captured structurally. Or: identify the gap class. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.1.01 | 786 | GAP | Run the complexity lens on the compiler's own source. What does it report? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.1.02 | 787 | GAP | Run the cost lens on a self-host pipeline. Show the symbolic cost. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.1.03 | 788 | GAP | If the compiler has a complexity contract violation against itself, does it fail to compile? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.1.04 | 789 | GAP | **Falsification probe**: introduce a known complexity-violation into the compiler. Does the self-application lens catch it before tests do? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.2.01 | 797 | GAP | Run the fixed-point pipeline. Does iteration N == iteration N+1? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.2.02 | 798 | GAP | How many iterations to converge? (Should be ≤2.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.2.03 | 799 | GAP | If divergent: what's the byte-diff or behavior-diff? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.2.04 | 800 | GAP | **Falsification probe**: introduce a non-deterministic compiler step. Does the fixed-point predicate catch it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.01 | 815 | GAP | Find the "coercion cost" data. Cite the substrate carrier. Is it `Complexity` (the same one used by §1.1), or a separate `CoercionCost` carrier? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.02 | 816 | GAP | If separate: that's a parallel-authority finding. Surface as P1 violation candidate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.03 | 817 | GAP | **Falsification probe**: a coercion between two representations has cost C. Is C readable via the complexity lens, or via a separate read? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.04 | 823 | GAP | Find the Rust language spec at `dsl/extdeps/languages/rust/`. Find the Rust transport spec. Are they ONE file / ONE substrate carrier, or distinct authorities? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.05 | 824 | GAP | Same for an interpreter runtime (if v3 has one at HEAD; or the runtime-emission target). Is it the same substrate, or parallel? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.06 | 825 | GAP | If distinct: each pair is a parallel-authority finding. Enumerate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.07 | 826 | GAP | **Falsification probe**: edit the Rust language spec. Does the transport spec auto-update (i.e., it IS the same fact) or require a parallel edit (P1 violation)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.08 | 830 | GAP | Find the idempotency lens / mechanism. Find the cancellation lens / mechanism. Find the redundancy lens / mechanism. Are they three separate mechanisms or ONE algebraic-simplification engine with three lenses? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.09 | 831 | GAP | If three: where's the unification path? (Should be in algebra.dag's compositional-fold of `Behavior::Bind` per `feedback_closed_system_effects`.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §4.3.10 | 832 | GAP | **Falsification probe**: write a `.dag` program with an obviously-redundant operation (e.g., reading the same key twice with no intervening write). Does the idempotency lens, cancellation lens, AND redundancy lens all catch it? Same diagnostic, or three different paths? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.01 | 846 | GAP | Class 1 (parser/grammar #60): show concept-faithful `Int<64>`, `Real<64>`, `Nat<8>` lower without v2-fallback. Where? Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.02 | 847 | GAP | Class 2 (function-valued #61): show a function-valued data flow program working end-to-end. Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.03 | 848 | GAP | Class 3 (file-ingestion #62): show `.dag` program reading an external file via `FileAttachment` carrier (per Director ratification 2026-05-13). Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.04 | 849 | GAP | Class 4 (workflow/scheduling #63): show CI workflow scheduling executing via `.dag`. Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.05 | 850 | GAP | Class 5 (reflection-closure #64): show `lens_apply.rs` reflection via PB-Runtime, end-to-end. Demo. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.1.06 | 851 | GAP | **Falsification probe**: write a new substrate-gap (a 6th class). Could the framework absorb it without changes to the closure criteria? Or would §1.4 need re-authoring? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.2.01 | 859 | GAP | grep for `src/v2/` imports in `src/v3/`. Should be zero. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.2.02 | 860 | GAP | grep for v2-oracle fixtures in v3 tests. Survivors must be frozen-snapshot consumption only, not live. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.2.03 | 861 | GAP | **Falsification probe**: delete `src/v2/`. Does the test suite still pass? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.3.01 | 869 | GAP | What's the current bridge ledger? Should be 0. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.3.02 | 870 | GAP | If non-zero: enumerate each bridge. Has each a named dissolution trigger and target date? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.3.03 | 871 | GAP | **Falsification probe**: would a code reviewer notice if a new bridge type was introduced without ledger entry? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.01 | 887 | GAP | Walk through R3-close-anchored changes: how many required edits to `src/v3/compiler/src/*.rs` (hand-Rust) vs. `.dag` substrate? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.02 | 888 | GAP | List the last 20 merged R3 PRs. For each, was the load-bearing change in `.rs` files or `.dag` files? Tabulate. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.03 | 889 | GAP | Is there a SINGLE merged R3 PR where the load-bearing change touched ONLY `.dag` files (no `_generated.rs` regen, no hand-Rust)? Cite SHA. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.04 | 890 | GAP | **Falsification probe**: pick a random `.dag` file. Edit it (e.g., add a field). Run the compiler. Does the compiler produce a correct emission without requiring any `_generated.rs` regen step that itself needs hand-Rust orchestration? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.05 | 896 | GAP | Count `.rs` files in `src/v3/` total. Cite the number at HEAD. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.06 | 897 | GAP | Count `_generated.rs` (machine-emitted from `.dag`). Cite the number. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.07 | 898 | GAP | **Hand-Rust = total − generated**. Cite the count. Is it 0 per operator's 2026-05-09 framing? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.08 | 899 | GAP | If non-zero: enumerate each hand-Rust survivor. For each, cite the named-retirement-schedule (PR # / gate # / target SHA). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.09 | 900 | GAP | Cross-check against `src/v3/SELF_HOSTING.md` §1 "bootstrap seed" framing: are the survivors *bootstrap-seed-only* (i.e., regenerable from `.dag` via the compiler itself), or do any encode authoritative behavior absent from `.dag`? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.10 | 901 | GAP | **Falsification probe**: delete one hand-Rust file. Can the `.dag` substrate regenerate it via the compiler running on itself? Cite the regen invocation. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.11 | 907 | GAP | Enumerate hand-maintained files by extension class. `.yml` (CI/build). `.toml` (Cargo / Rust). `.md` (docs). `.sh` (scripts). Others. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.12 | 908 | GAP | For each class, is the count R3-close-acceptable, or is there a named-dissolution-schedule? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.13 | 913 | GAP | **Falsification probe**: pick a hand-maintained non-Rust file (e.g., a build script). Is the load-bearing fact it encodes derivable from `.dag` substrate? If yes, why isn't it derived? If no, what's the named carrier for the fact? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.14 | 927 | GAP | Run the PB-0 census at HEAD. What's the count of hand-authored `.rs` files in `src/v3/`? The committed target is **0** per design-pure-bootstrap-zero.md. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.15 | 928 | GAP | For each hand-authored survivor: cite the named-retirement-schedule (PR # / gate # / target SHA). Survivors without named retirement are 0-floor debt. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.16 | 929 | GAP | For each hand-authored survivor flagged as "bootstrap-seed": verify it is **machine-emitted from `.dag`** (replayable via `cargo run --bin regen-*` or equivalent). Hand-authored bootstrap-seed is NOT acceptable per design-pure-bootstrap-zero.md:210. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.17 | 930 | GAP | Cross-reference SELF_HOSTING.md §1 "bootstrap seed" framing against design-pure-bootstrap-zero.md:210 generated-trampoline framing: is every claimed bootstrap-seed survivor actually generated, not hand-authored? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.18 | 931 | GAP | **Falsification probe**: produce the `.dag` source for the LARGEST hand-Rust survivor at R3 close. Compile the `.dag`. Diff the emitted `.rs` against the survivor. If the survivor doesn't match the emission, the survivor is authoring facts not present in `.dag` — that's R3-close debt against the 0-floor target. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.4.19 | 932 | GAP | **Bootstrap-resolution boundary probe** (per design-pure-bootstrap-zero.md:191 STOP-condition): if first-time bootstrap (N=0) resolution requires hand-Rust in v3's source tree, the 0-floor target is unreachable and the framing needs revision back toward an explicit alternative. Verify the N=0 resolution lives OUTSIDE `src/v3/` (install script, gunbc-runtime crate, rustc macro). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.01 | 960 | GAP | Find the memoization mechanism. Cite the lens / substrate carrier. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.02 | 961 | GAP | Memoization MUST be by-construction (compiler derives purity + cost and applies it automatically). Confirm there is NO opt-in/annotation form — if memoization requires a `@memoize`-style tag, that is the violation (feedback_no_annotations). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.03 | 962 | GAP | Show me a `.dag` program that should benefit from memoization. Compile + run. Was memoization applied? How is "was applied" verifiable (cost-lens output? execution-trace lens? cache-hit metric)? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.04 | 963 | GAP | **Falsification probe**: a pure function with high cost is called twice with the same args. Does the compiler emit code that memoizes, or naive double-execution? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.05 | 967 | GAP | Find the incremental-execution mechanism. Cite the lens / data carrier. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.06 | 968 | GAP | Run an example `.dag` workflow twice with no input changes. Does the second run skip pure subgraphs that haven't changed? Verify via execution-trace lens. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.07 | 969 | GAP | Now change ONE input. Verify that only the affected-set subgraph re-executes (composes with §2.5.F affected-set lens). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.08 | 970 | GAP | **Falsification probe**: a non-deterministic step is in the graph. Does the incremental-execution mechanism correctly avoid skipping its re-evaluation? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.09 | 974 | GAP | Find the "shared cost algebra" that enables cross-language optimization. Cite the substrate carrier. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.10 | 975 | GAP | Pick an optimization (e.g., loop-fusion / tail-recursion / cse). Is it applied uniformly across Rust + Python + Go targets, or per-target? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §5.5.11 | 976 | GAP | **Falsification probe**: write a `.dag` program where one target language's optimizer would catch a cost reduction but another wouldn't. Does the shared-cost-algebra propagate the optimization to ALL targets, or only the one whose host optimizer applies? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.1.01 | 990 | GAP | Find a recent compile error. Does the diagnostic point to the correct alternative, or just say "this is wrong"? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.1.02 | 991 | GAP | Pick 5 diagnostic message types. For each, does it satisfy the "show the correct code" criterion? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.1.03 | 992 | GAP | If diagnostic just says "X is wrong" without "Y would be right": GAP per operator-ratified 2026-05-15 — discipline rule requires every Diagnostic emit site to populate `suggested_correction`. None only when correction is genuinely undeterminable (e.g., user-input-boundary diagnostics) — and that None must carry a named reason. Working demos: complexity-violation diagnostic shows correct alternative; synthesis Report shows alternative algorithm. Demo TestClaims land in `src/v4/test/claim/diagnostic_correction/`. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.1.04 | 993 | GAP | **Falsification probe**: write a program with a known structural error. Read the diagnostic. Could a user act on it without reading source code? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.01 | 1005 | GAP | Show me the SIMPLEST `.dag` program — types + function + maybe one workflow. How many concepts must the author know? Is the proof / lens surface invisible? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.02 | 1006 | GAP | Compile the simple program for Rust + Python + Go. Does it emit working code in all 3 without the user touching lens/proof surface? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.03 | 1007 | GAP | ROADMAP cites `fixture_integration_canonical` for the glue-generation audience. Cite the fixture path. Compile + run. Does it land in <100 LOC of `.dag` surface for the user? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.04 | 1011 | GAP | Show me a `.dag` program that opens the advanced surface — author a user-defined lens. How much new surface does the user touch? Is the base-language surface untouched (per "opening doesn't change the base")? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.05 | 1012 | GAP | ROADMAP cites `fixture_compiler_nerd_canonical` for the structural-proof audience. Cite the fixture path. Verify it exercises lens + proof surfaces. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.2.06 | 1013 | GAP | **Falsification probe**: a base-language change (e.g., a new type connective) shouldn't require the advanced-surface user to relearn. Conversely, an advanced-surface change shouldn't break base-language programs. Demonstrate non-coupling at each direction. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.01 | 1028 | GAP | Pick an arbitrary `.dag` program at HEAD. Apply the complexity lens. Does it produce output? (It should — no opt-in flag should be required.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.02 | 1029 | GAP | Same for effect / cost / parallelism lenses. Each should produce output for ANY `.dag` program at HEAD. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.03 | 1030 | GAP | **Falsification probe**: try to author a `.dag` program that opts OUT of complexity/cost/effect lens reads. Is there ANY in-language syntax that disables lens reads? (Should be zero per `feedback_groundedness_gates_lenses`.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.04 | 1034 | GAP | Author a `.dag` namespace pattern. Run all 4 lenses on it. Do they all read through the namespacing, or does one of them stop at the named boundary? (Should read through per "compiler sees through" claim.) | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.05 | 1035 | GAP | **Falsification probe**: try to "hide" a complexity violation behind a named pattern (e.g., wrap an O(n²) function in a typedef'd "FastLookup<T>"). Does the complexity lens still flag it? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.06 | 1039 | GAP | Show me a `.dag` program that calls out to an externally-implemented operation (e.g., `ExecuteCommand` / `extdeps` provider). Where does lens-coverage end? Cite the explicit boundary marker (per `feedback_groundedness_gates_lenses`). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.07 | 1040 | GAP | **Falsification probe**: try to author a program that EFFECTIVELY leaves the stack inside the language (e.g., via heavy use of `extdeps` opaque calls). Is the lens-coverage boundary explicit + auditable? | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.08 | 1044 | GAP | What's the LOC overhead of a `.dag` program vs. an equivalent program in Rust / Python / Go? Tabulate for a canonical small + medium + large example. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.09 | 1045 | GAP | What's the percentage of programs where ALL lenses produce green output (no violations)? If the percentage is high, "high free value" is structurally true. | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §6.3.10 | 1046 | GAP | **Falsification probe**: is there a class of programs gunbc IS more verbose / more constraining than alternatives, in a way that fails the "economics" test? Enumerate (acceptable to defer as R4 if narrow). | No per-probe receipt; executable close evidence blocked by T-38 runner/T-15 close gaps (`src/v4/TASKS.md:194-195`, `src/v4/TASKS.md:815`). |
| §7.01 | 1056 | GAP | §1.5 lane counts sum to §1.8 enumerated total | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.02 | 1057 | GAP | §1.8 enumerated count matches r3-structure.md §"Acceptance" | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.03 | 1058 | GAP | Q1 row in §10 matches §1.5 + §1.8 totals | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.04 | 1059 | GAP | No gate ID gaps or duplicates in §1.8 | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.05 | 1060 | GAP | All `docs/audit/*.md` ratifications represented in §1.8 OR §10 (per `feedback_grep_audit_docs_before_answering_close_questions`) | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.06 | 1061 | GAP | `docs/r3-remaining-work-dependency-graph.md` consistent with §1.8 PASSING/DECLARED state | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.07 | 1062 | GAP | `r3_debt_paydown_zero_remaining` at close: 0 | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.08 | 1063 | GAP | ROADMAP `Post-merge debt` rows: 0 | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §7.09 | 1064 | GAP | §10 RED escalations: ALL CLOSED with owner sign-off on-ledger | Cross-doc ledger probe not executed; no v4 close audit/predicate artifact exists and deferral audit shows live gate rows (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §8.01 | 1072 | GAP | All 104 §1.8 predicates executed at HEAD within 24h of close ceremony | No v4 predicate execution artifact exists; only R3 predicate artifact is present (`docs/audit/r3-close-predicate-execution-2026-05-13.md`). |
| §8.02 | 1073 | GAP | Execution log preserved as audit artifact | No v4 predicate execution artifact exists; only R3 predicate artifact is present (`docs/audit/r3-close-predicate-execution-2026-05-13.md`). |
| §8.03 | 1074 | GAP | No predicate trivially passes via empty match-target or narrowed scope without justification | No v4 predicate execution artifact exists; only R3 predicate artifact is present (`docs/audit/r3-close-predicate-execution-2026-05-13.md`). |
| §8.04 | 1075 | GAP | PASSING gates whose predicate produces output: output preserved in close audit doc | No v4 predicate execution artifact exists; only R3 predicate artifact is present (`docs/audit/r3-close-predicate-execution-2026-05-13.md`). |
| §10.01 | 1096 | GAP | Every §1-§8 item PROVEN or R4-DEFERRED with operator acceptance | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.02 | 1097 | GAP | Zero GAP, zero WEAK-EVIDENCE | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.03 | 1098 | GAP | Director sign-off recorded on-ledger | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.04 | 1099 | GAP | Operator sign-off recorded on-ledger | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.05 | 1100 | GAP | `ROADMAP.md` R3 → R3-closed milestone update merged | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.06 | 1101 | GAP | §1.8 status sweep frozen | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.07 | 1102 | GAP | R4 work-item creation greenlit | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §10.08 | 1103 | GAP | R3 close audit doc preserved: `docs/audit/r3-close-YYYY-MM-DD.md` capturing per-gate predicate execution output + every probe's disposition with evidence link | Close ceremony remains unperformed and R3-shaped in the source questionnaire (`docs/v4-close-interrogation.md:1096-1103`). |
| §13.01 | 1140 | GAP | Can a user write `.dag` code that reads an arbitrary JSON file and produces a typed `.dag` value? Show the program. Is the ingestion logic in `.dag` (per closed-system discipline) or in hand-Rust? | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.02 | 1141 | GAP | Same for YAML, CSV, TOML. | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.03 | 1142 | GAP | Can the compiler ingest a Rust source file (a subset of Rust grammar) and produce typed `.dag` representation? Same for Python, Go. | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.04 | 1143 | GAP | Schema-driven ingestion: given an OpenAPI spec, can the compiler produce corresponding `.dag` types AND validate incoming JSON against them? | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.05 | 1144 | GAP | **Bidirectionality**: for each Shape A target (Rust/Python/Go), is the language spec in `extdeps/languages/<lang>.dag` used for BOTH emission and ingestion, OR are these separate substrates? Per concept-unification claim (THESIS:185-188), they should be ONE substrate. | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.06 | 1145 | GAP | **Falsification probe**: write an ingestion target for a format never tested (e.g., XML). Does the substrate accommodate it without new connectives or behaviors? Or does ingestion require a dedicated 6th L1 behavior (would trigger C1 stop-signal per §2.6)? | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.07 | 1149 | GAP | Roundtrip: ingest a Rust source file → produce typed `.dag` → emit Rust. Is the emitted Rust byte-equivalent to the input (modulo whitespace/comments)? If not, the ingestion lost information — gap. | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.08 | 1150 | GAP | Coverage: enumerate the 6 connectives × 5 behaviors structural form space. For each, does the v4 ingestion path support it for all anchored formats? L6-style completeness for ingestion. | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §13.09 | 1151 | GAP | Effect-model preservation: ingest a Python source file with side effects. Are the effects classifiable via the effect lens (§1.4) post-ingestion? Or does ingestion produce opaque blobs the lens can't read? | Ingestion/roundtrip is in scope, but T-36 is in progress and RoundTripClaim eval is deferred pending T-38 (`src/v4/TASKS.md:2220-2242`, `src/v4/test/claim/round_trip/dag_ingest_round_trip.dag:3`). |
| §14.01 | 1191 | GAP | Per language: can v4 emit a non-trivial program to it? (Same probes as §3.1 for Rust/Python/Go, replicated.) | Additional language substrate is present, but multi-target ingest/emit/equivalence/L6 execution receipts are not recorded; T-36/T-38 remain blockers (`src/v4/TASKS.md:2220-2242`, `src/v4/TASKS.md:2277-2301`). |
| §14.02 | 1192 | GAP | Per language: can v4 ingest source files written in it? (Per §13 ingestion claim.) | Additional language substrate is present, but multi-target ingest/emit/equivalence/L6 execution receipts are not recorded; T-36/T-38 remain blockers (`src/v4/TASKS.md:2220-2242`, `src/v4/TASKS.md:2277-2301`). |
| §14.03 | 1193 | GAP | **L5 cross-target consistency**: same `.dag` program emitted to ALL targets (Rust/Python/Go/C/C++/LLVM IR/TypeScript) produces equivalent behavior. Test surface scales by target count — is the test framework substrate (`src/v4/test/claim/`) per-target-parameterized or per-target-duplicated? | Additional language substrate is present, but multi-target ingest/emit/equivalence/L6 execution receipts are not recorded; T-36/T-38 remain blockers (`src/v4/TASKS.md:2220-2242`, `src/v4/TASKS.md:2277-2301`). |
| §14.04 | 1194 | GAP | **L6 form completeness**: do all 6 connectives × 5 behaviors emit to every target? Per §3.5 L6 matrix. | Additional language substrate is present, but multi-target ingest/emit/equivalence/L6 execution receipts are not recorded; T-36/T-38 remain blockers (`src/v4/TASKS.md:2220-2242`, `src/v4/TASKS.md:2277-2301`). |
| §14.05 | 1195 | GAP | **Falsification probe**: pick a `.dag` form that emits cleanly to Rust but cannot emit to LLVM IR (e.g., something Rust's borrow checker accepts but LLVM IR's SSA form rejects). Does v4 fail-closed when LLVM IR is targeted, or silently emit broken IR? | Additional language substrate is present, but multi-target ingest/emit/equivalence/L6 execution receipts are not recorded; T-36/T-38 remain blockers (`src/v4/TASKS.md:2220-2242`, `src/v4/TASKS.md:2277-2301`). |
| §15.01 | 1219 | GAP | Per framework: are its carriers structurally modeled (Component, Hook, etc.) or just stringly-named? | React/framework scope is in v4, but flagship full-stack demo is task scope, not executed close evidence (`src/v4/TASKS.md:958-986`). |
| §15.02 | 1220 | GAP | **Cross-language consistency**: React substrate emits to TypeScript (typed React) and to JavaScript (untyped). Does the substrate distinguish meta-typing (the framework's structural rules) from target-language-typing?  | React/framework scope is in v4, but flagship full-stack demo is task scope, not executed close evidence (`src/v4/TASKS.md:958-986`). |
| §15.03 | 1221 | GAP | **Bidirectional**: can v4 ingest existing React components (per design-r4-canvas §Q3-b "TS class → Component") AND emit them? | React/framework scope is in v4, but flagship full-stack demo is task scope, not executed close evidence (`src/v4/TASKS.md:958-986`). |
| §15.04 | 1222 | GAP | **Falsification probe**: introduce a framework-specific anti-pattern (e.g., a React Hook called inside a conditional). Does the React-substrate lens catch it at compile-time? | React/framework scope is in v4, but flagship full-stack demo is task scope, not executed close evidence (`src/v4/TASKS.md:958-986`). |
| §16.01 | 1257 | GAP | Multi-program shape: how does `.dag` express "this fragment runs on machine A, this on machine B"? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.02 | 1258 | GAP | Wire derivation extension: does extending gate #28 `omni_layers_share_one_node_tree` to "share one Dag across deployment units" hold? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.03 | 1259 | GAP | Coordination semantics: are sync/async/stream/pub-sub first-class behaviors OR compositions over existing 5 behaviors? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.04 | 1260 | GAP | Failure-at-boundary: how does "partial failure" compose with effect-enumeration (§1.4)? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.05 | 1261 | GAP | Idempotency at endpoint: composes with idempotency lens (THESIS:188 + §3.6)? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.06 | 1262 | GAP | Cross-endpoint dimension propagation: does affected-set lens extend across deployment-unit boundaries? | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §16.07 | 1263 | GAP | **Falsification probe**: design a 2-endpoint distributed program. Demonstrate end-to-end emission per Shape-A + the wire contract per Shape-B + coordination behavior captured structurally. | Coordination scope is in v4, but the two-endpoint end-to-end demo receipt is not recorded; T-16 is still the flagship task surface (`src/v4/TASKS.md:958-986`). |
| §17.1.01 | 1287 | GAP | Per target: are RegisterClass / EndianMode / Alignment declared structurally (typed carriers) or string-keyed? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.1.02 | 1288 | GAP | Cross-target consistency: emit same `.dag` program to two targets with different EndianMode. Do the emitted serializations agree (modulo endian)? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.1.03 | 1289 | GAP | Falsification: write a `.dag` program that depends on EndianMode being Big. Emit to a Little-endian target. Does the compiler fail-closed or silently emit broken serialization? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.2.01 | 1298 | GAP | Float arithmetic: does v4 model rounding-mode as a structural axis or assume IEEE-754 default? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.2.02 | 1299 | GAP | Cross-language: Rust's `f64::round` vs Python's `round()` vs Go's `math.Round` — do their semantics agree at the v4 substrate level? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.3.01 | 1308 | GAP | Time arithmetic: subtracting two `Instant` values produces `Duration` (Magnitude). Is this enforced structurally? | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |
| §17.3.02 | 1309 | GAP | Type errors: adding two `Instant` values (semantically nonsensical) should fail at compile time. Demo. | Axis extension probe lacks per-target executable evidence; gated corpus still has live unresolved markers (`docs/audit/v4-deferral-audit-2026-05-29.md:69-73`). |

## Closeout Discipline

This audit does not mark any probe answered in `docs/v4-close-interrogation.md`. It records the Phase 0 state for PR #3938 review: v4 has substantial substrate and scaffold progress, but the questionnaire remains receipt-unvalidated. The next pass should replace these GAP rows section-by-section only when each probe has a cited promise, delivery, reproducible output, and falsification result.
