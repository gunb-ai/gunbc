# SG-0 deferral brief fixture (CI self-test only)

This file exists so `scripts/check-pr-sg0-net-shrink-discipline.sh --self-test`
can exercise `(c)` pairing brief-path existence checks in the **public snapshot**
(`docs/briefs/` is stripped there). It is not a dispatch brief.

**Do not cite this path in real PR bodies** — use `docs/briefs/*.md` or a tracker issue.
