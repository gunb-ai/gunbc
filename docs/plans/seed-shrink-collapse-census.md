# Plan — Rust seed-shrink census toward `5-collapse-v1`

**Status:** carrier-grounded audit + per-chunk sign-off queue · **DESIGN.md + the carriers remain the authority** — this doc is an audit/tracker, not a fact ledger (DESIGN §6). A chunk's real state is its branch/PR + operator sign, not this file. Linked from `ROADMAP.md` §5 *Self-host v2 → delete `src/v1`* and [v2-self-hosting.md](v2-self-hosting.md) Track Z.

**Re-verified against the live tree on 2026-06-29 by execution** (session bright-lark-472, receipt `main @ 5acbff0f8b`). Re-run the receipt block in §0 before acting on LOC figures — the seed drifts with every merge.

> **SCOPE (operator-ruling).** READ-ONLY inventory for `5-collapse-v1`. **No deletions or seed authoring** until per-chunk operator sign. Collapse is incremental replacement by a pinned v2-emitted bootstrap binary, not a big-bang `rm -rf src/v1` ([v2-self-hosting.md](v2-self-hosting.md) Track Z).

---

## 0. Receipt — scale snapshot (`main @ 5acbff0f8b`)

| Surface | Files | LOC | Notes |
| --- | ---: | ---: | --- |
| `src/v1` (all Rust) | 215 | **170,330** | Entire bootstrap seed |
| `src/v1/stage0/src` | 113 | ~135,000 | Compiler + runtime + lens projections |
| `GENERATED_STAGE0_FILES` | 89 | **102,450** | v2-emitted; regen cutover bulk |
| `HAND_MAINTAINED_STAGE0_FILES` | 24 | **32,528** | Dissolution queue (per-chunk sign) |
| CLI bins (`stage0/src/bin`) | 10 | 4,843 | `claim_executor`, `regen_stage0`, … |
| Integration tests (`src/v1/tests`) | 88 | 29,934 | `pipeline.rs` alone 11,842 |
| `src/v2` | 795 `.dag` | **0 `.rs`** | Authority — compiler logic |
| `dsl/` | 632 `.dag` | — | Std + extdeps + CI spec |

The roadmap's ~154k figure is the **compiler-logic** subset; the live tree includes tests, bins, and runtime (~170k total).

**Registry authority:** `src/v1/stage0/src/bin/regen_stage0.rs` — `GENERATED_STAGE0_FILES`, `HAND_MAINTAINED_STAGE0_FILES`, and three active `patch_*` hooks.

---

## 1. Roadmap gates — what blocks `5-collapse-v1`

Per `dsl/gunbc/roadmap_authority.dag` (§5 self-host):

| Gate | Status | Relation to collapse |
| --- | --- | --- |
| `5-cargo-green` | **done** (#5777/#5873) | Parent of `5-collapse-v1` in roadmap DAG |
| `5-regen-verify` | **done** | Enforces GENERATED byte-identical; HAND excluded by design |
| `5-real-fixpoint` | open | `content_hash` stage1==stage2 |
| `5-dissolve-patches` | open | Empty HAND list + no `patch_*` |
| `5-defork` | open | Grounding cluster ([dsl-v2-defork-audit.md](dsl-v2-defork-audit.md)) |
| `5-seed-honesty` | open | Diverse Double-Compiling |
| `5-collapse-v1` | open | Terminal |

**Stricter sequencing** ([v2-self-hosting.md](v2-self-hosting.md) Track Z forced order): cargo-green → real fixpoint → regen-verify green on full seed → dissolve HAND_MAINTAINED → seed-honesty → **incremental** collapse.

---

## 2. Two deletion classes

### 2A. GENERATED (~102k LOC) — bulk cutover, not piecemeal delete

These files are v2-emitted compiler pipeline + std mirrors + extdeps language rows. At collapse they are **replaced wholesale** by `regen_stage0` from a green whole-tree emit — not deleted one file at a time.

**Largest generated modules (LOC):**

| File | LOC | `.dag` authority |
| --- | ---: | --- |
| `v1_compiler_emit_rust.rs` | 24,825 | `05_emit` / `06_translate` |
| `v1_compiler_infer.rs` | 14,264 | `04_infer` |
| `v1_compiler_complexity.rs` | 9,895 | infer complexity |
| `v1_compiler_emit.rs` | 6,214 | `05_emit` |
| `v1_test_non_ascii_perf_fixture.rs` | 6,029 | perf fixture |
| `std_*` mirrors (28 files) | ~10,401 | `dsl/std/*` (de-fork target) |

**Pipeline stage LOC (seed compiler modules):**

| Stage | `.dag` | LOC |
| --- | --- | ---: |
| `04_infer` | `04_infer.dag` | 36,463 |
| `05_emit` | `05_emit.dag` + orchestration | 38,558 |
| `02_parse` | `02_parse.dag` | 13,911* |
| std mirrors | `dsl/std/*` | 10,401 |
| `00_compile` | `00_compile.dag` | 2,500 |
| `01_tokenize` | `01_tokenize.dag` | 1,031 |
| `03_resolve` | `03_resolve.dag` | 982 |

\*parse is HAND_MAINTAINED until Chunk A dissolves (#5864 cursor).

### 2B. HAND_MAINTAINED (~33k LOC) — per-chunk operator sign queue

Copied through regen, excluded from `regen --verify`. Each row below is a **sign-off unit** — must dissolve (move to GENERATED or be v2-emitted) before trustworthy collapse.

| Chunk | File(s) | LOC | Dissolution trigger | Owner lane |
| --- | --- | ---: | --- | --- |
| **A** | `v1_compiler_parse.rs` | 13,911 | Ground #5864 `TokenStream` cursor in `02_parse.dag` → GENERATED | jolly-cat emitter |
| **B** | `main.rs` | 513 | Emit dep-pool from `gunbc.compile_source_model` ([seed-debt-bundle-item-2.md](seed-debt-bundle-item-2.md); model #5894 ✓) | jolly-cat emitter |
| **C** | `v1_compiler_dag_collect*.rs` | 521 | Emitter emits delegation; delete `patch_bootstrap_dag_collect` | emitter |
| **D** | Lens projections (10 files) | ~3,908 | Interpreter consumes `.dag` lens tables at runtime | lens + interpreter |
| **E** | `v1_interpreter.rs` | 6,373 | v2-emitted evaluator + host-effects | runtime |
| **F** | `cli_run.rs` | 4,165 | CI floor → workflow host-effect `apply()` | workflow |
| **G** | `coproduct_reflection.rs` | 1,297 | Model↔realization fork grounded (#5428 pattern) | de-fork |
| **H** | Runtime support | ~1,500 | `recorded_fixture`, `resolved_graph_cache`, `wire_value_serialize`, `rest_transport_facts`, `cache_purity_oracle` | runtime |

**Active `patch_*` hooks (3):** `patch_bootstrap_dag_collect`, `patch_languages_consumer_census_mod`, `patch_cargo_toml_for_generated_crate` — dissolve with their chunk.

**Recommended sign-off order:** A → B → C → D (parallel per projection) → G → E → F → H → GENERATED bulk cutover.

---

## 3. Survivors — roles that cannot go at collapse

Track Z decomposition ([v2-self-hosting.md](v2-self-hosting.md)): `src/v1` is not only compiler logic. These must be **v2-emitted or pinned** before the role can shrink:

| Role | LOC (approx) | Replacement |
| --- | ---: | --- |
| `.dag` evaluator | 6,373 (`v1_interpreter.rs`) | v2-emitted eval + host-effect transports |
| CI floor runner | 4,165 (`cli_run.rs`) | workflow host-effect `apply()` |
| CLI bins | 4,843 | `claim_executor`, `regen_stage0` → pinned bootstrap artifacts |
| Host transports | ~400 | shell/file/REST dispatch in interpreter |
| Caches / fixtures | ~1,000 | `resolved_graph_cache`, `recorded_fixture` |
| Integration tests | 29,934 | Migrate witnesses to floor `*_test.dag`; retire with covered modules |

**CLI bins (detail):**

| Bin | LOC | Notes |
| --- | ---: | --- |
| `claim_executor.rs` | 1,711 | Floor witness runner |
| `regen_stage0.rs` | 1,681 | Bootstrap tool → pinned artifact |
| `claim_batch.rs` | 654 | Batch witness execution |
| `yaml_check.rs` | 24 | SCAFFOLD — dissolve when yaml ingest lands |
| Others | ~773 | Discovery / measurement utilities |

---

## 4. Shrink ratchets (seed growing, not shrinking)

| Ratchet | State | Resolution |
| --- | --- | --- |
| **Clone census** | RED — 21,540 `.clone()` vs 20,200 budget (+1,138); `#[ignore]` on `pipeline.rs` | Substrate migration / clone reduction; **do not bump cap** |
| **Inert carrier roster** | 12 staged-ahead carriers (`inert_carrier_project.rs`) | Shrink as realization loop wires consumers |
| **Languages consumer census** | 71 data decls, 64 per-language rows | Dissolve when `extdeps/languages/*` repointing lands |
| **Fact cardinality** | Cross-tree coexistence ratchet | Dissolve with de-fork consolidation |

---

## 5. Verdict

- **Authority side done:** `src/v2` is 100% `.dag` (795 modules, 0 Rust).
- **`5-collapse-v1` not yet actionable** despite cargo-green: HAND queue (~33k LOC + 3 patches), open fixpoint, and de-fork grounding cluster remain.
- **Honest terminal state:** pinned, content-addressed, v2-emitted bootstrap binary — not zero bytes.
- **First operator sign candidates:** Chunk A (parse cursor) and Chunk B (dep-pool emit-wiring) — scoped in active emitter lanes.

---

## Dissolution trigger (DESIGN §6)

Delete this doc when `5-collapse-v1` lands: `src/v1` compiler logic is gone, the pinned bootstrap survives, and the per-chunk queue in §2B is empty. Until then, re-run §0 receipt on each review.
