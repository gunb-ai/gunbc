# Cross-platform instrument motion: capability bindings, a synthetic impulse, and a bob that settles


---

*Provenance: this lane was carried as an Open Threads row in `DESIGN.md` until 2026-08-18, when that section was deleted on operator ruling — `DESIGN.md` is a first-principles document and per-lane status in it was a dual representation. The text below is preserved verbatim from that row; its authority is the carriers it names, not this file.*

**cross-platform instrument motion — capability bindings, a synthetic impulse, and a bob that settles (operator-directed 2026-08-04; verdict withdrawn 2026-08-05).** The lane exists because of the mistake that opened it: a browser probe found `DeviceMotionEvent` constructible, never called `start()`, and a verdict of *sensor absent* was asserted from it — refuted three ways, because the specification's hollow-event allowance is a **SHOULD**, constructor presence is not sensor presence, and the probe ran inside an artifact whose Permissions Policy default allowlist is `'self'`, so the decisive question was never asked. Receipt kept, verdict withdrawn, observation reclassified `MotionReachabilityUnresolved`. **Four layers stay separate** because collapsing them is how a browser became the architecture: product semantics, application container, renderer realization and platform capability bindings are independent axes on `gunbc.host_realization` `HostRealization`, a browser is ONE realization, and **OS support is not one Boolean** — each provider is its own extdeps authority (`extdeps.w3c.device_orientation_and_motion`, `extdeps.w3c.generic_sensor`, `extdeps.w3c.accelerometer`, `extdeps.browser.google_chrome` as a distribution distinct from the Chromium engine), so a platform is a row and never a wider product enum. **The support wall took two reviews to become real:** naming `qualify_tap` the sole minter of `TapQualified` did not make it one, because that arm is a public constructor and the stream handed the verdict an already-qualified value — so a caller could author it around a failing receipt and reach `CapabilitySupported`. The stream now carries the RAW requirement and receipt and `derive_capability_verdict` qualifies them itself; the negative control lost the `Passed`/`Failed` arms whose NAME qualification trusted and derives its verdict from the observed idle peak; a measurement clearing thresholds the model calls unsigned lands on `CapabilityQualifiedAgainstProvisionalRequirement`, which is not the key the durable-claim gate reads; and an unmeasured detection latency is `LatencyUnobserved` rather than zero, zero being the BEST possible latency and therefore the worst available answer. **Web admission moved onto the decision path:** `derive_reachability` and `project_binding_stream` matched the stream arm alone, so an insecure context with a blocked policy, a denied permission and arriving readings still derived reachable — a witness proved the checker noticed, nothing proved a contradicted observation had no route to a verdict. Both consumers run the check first, deliberately rather than routing an admitted-observation wrapper, since a substrate without opaque construction would make that wrapper the same forgery one level up; coherence reads the gyroscope policy DeviceMotion requires, treats an unanswered prompt as contradicting readings, and derives required axes per provider rather than demanding a `requestPermission` exposure from sensors that have none. **The bob did not settle, and the reason is worth keeping:** the envelope decayed by each STEP's duration, so at the production sixteen-millisecond frame against a one-hundred-and-eighty-millisecond half-life it computed zero half-lives and fell by nothing, the budget truncated the sequence to an empty tail, and the client set `translateY(0)` on its own — between them a model that never settled rendered as one that had. Amplitude is now a function of CUMULATIVE elapsed time with linear interpolation inside each half-life; descent evidence is MEASURED by comparing two amplitudes and returns `DescentUnknown` on growth; the two-valued oscillation became a triangle over four quarter-period legs because a square wave satisfies 'appears on both sides of rest' while never occupying a position between them; budget exhaustion is `BobSequenceRefused`; the client's manufactured final zero is deleted. **And the keystone was not conjoined** — five frame claims sat in a discarded expression above the returned one, which is how all of this reported green, and the same shape was then found in the web witness: a claim that is called but not joined is indistinguishable from one that is enforced. **A third review found five more seams, and four of them were one shape — a name standing where a fact was owed.** ONE REQUIREMENT: the observation carried two independently writable requirements, one on the binding and one on the tap outcome, so a row could name signed thresholds while measuring against provisional ones; they are now `BoundCapabilityRequirement { identity, snapshot }` and the snapshot is what qualification reads, asserted in both directions by a control that swaps the label and the numbers. NAMES: `PhysicalImpulse.source_binding` pointed at a producer function and is `source_provider`; the coverage state fused *is it written* with *has anything run*, and its `receipt` field carried that same producer reference — it splits into `BindingImplementationState` and `BindingProbeState`, and the one row that claims a probe now cites an executing witness. ONE WINDOW: three web stream arms and two binding stream arms each carried a `window_ms` beside the observation's own `observation_window_ms`, so a record whose stream reported four seconds inside a window of zero was constructible; the arms keep only what is theirs. THE THIRD PATH: `project_sensor_availability` — the projection feeding the sandbox's admission input — was still reading the stream arm alone after the other two consumers were moved onto the coherence check, so the exact bypass that repair closed stayed open where a consumer acts on the answer. TWO ABSORPTIONS, at the page: `frames_for`/`frames_wire` answered a refused sequence with an empty list and the empty string, which made every `string_contains(html, wire)` control VACUOUSLY TRUE — the serializer now takes frames a caller obtained by matching the settled arm, and every wire claim asserts non-emptiness first; and `BobSequenceRefused` reported the REMAINING budget, always zero, under a name a reader takes for the budget allowed, so the field is `budget_allowed` and the control pins three rather than the zero it used to enshrine. AND THE PROBES MISCLASSIFIED READINGS: the event path tested `acceleration.x` alone and the sensor path counted reading EVENTS while testing nothing, so a knock landing on y or z read as hollow and a nullable-axis route reported readings — both now emit the disjunction `acceleration_carries_any_value` models, from axis names each specification owns, and the sensor reports reading events and value-bearing readings as two numbers. The `Try` arm gained its own byte-exact serialization fixtures, because a substring check proves an arm was reached and nothing about what it produced. **PARKED HERE, with the unfinished work carried as roadmap rows rather than prose (2026-08-07).** Two release receipts were executed before parking: the complete emitted client passes `node --check` — the whole assembled program, not the three isolated `Try` fixtures — and a playback run drove the emitted page and the emitted client together, crossing both sides of rest, ending at the model-supplied zero, restarting coherently on a second tap, staying still under reduced motion, and replacing both *not yet run* lines with exact observations. That playback ran in a DOM implementation, NOT a rendering engine: the build host had no way to install one, and calling the substitute a browser receipt would be the fabricated-plausible-output failure aimed at the one claim a reader cannot check. It is owed as part of `instrument-motion-physical-qualification`. A preview of the emitted bytes was published for a human ruling, and the probes run there returned `DeviceMotionEvent · 0 events, 0 carrying values, over 2002ms` and `LinearAccelerationSensor · construction refused · SecurityError` — which is the Permissions Policy path the lane opened on, observed live rather than reasoned about, and says NOTHING about the host's hardware; it is a fact about the harness. No feel verdict was given, so the tuning constants stay `AuditionProvisional` and no constants commit was made. The three named successors are `instrument-motion-physical-qualification` (executed browser receipts, the idle/typing negative control, calibration, and the rendering-engine playback), `instrument-rendered-control-coverage` (the false-green denominator that omits the theme, clear and activity controls), and `discarded-bool-result-wall` (census then refuse a Boolean computed in non-terminal position — the class two keystones in this lane were caught by). **Rung, honestly:** a support claim without a derived qualification is unreachable at the `TargetCapabilityVerdict` seam, scoped to that seam because the carriers stay publicly constructible and the guarantee is that no CONSUMER accepts a pre-judged one; hardware reachability on any real host is UNOBSERVED, the roster carries obligations rather than support claims, and `bob_impulse_requirement_candidate` stands at `AuditionProvisional`. **NOT closed**, stated because the first attempt claimed more than it had: an executed Chrome receipt, an executed Safari receipt, a qualified physical tap, a physical sensor reaching `PhysicalImpulse`, native macOS access, Windows/mobile/Linux bindings, the false-green dead-hover law, node-level syntax validation of the emitted client, and a DOM playback receipt — the client counts events and prints prose, so this is the synthetic bob plus RAW capability probes. The superseded `PhysicalImpulseSource`/`SensorAvailability` path in `gunbc.design.instrument_physical` carries its dissolution trigger rather than being deleted while the old page still renders it.
