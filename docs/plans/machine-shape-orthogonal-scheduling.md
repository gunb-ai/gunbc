# Machine-shape orthogonal scheduling — one realization kernel over faithfully modeled machine facts, with locality

Status: DRAFT for operator review (2026-07-15, session sunny-deer-248). No code lands from this doc; every phase below is model-before-implement and names its trigger. Receipts cite the live tree as of 9011600a92.

Dissolution trigger (DESIGN §6): this document's authority transfers to the carriers it names — when Phase 1 lands, §2/§3 live as std/extdeps declarations with in-carrier marks, this file is registered as a modeled `gunbc.plan.Plan` row (`HasTrigger`, slug `machine-shape-orthogonal-scheduling`) and the hand-authored md dissolves into that carrier. Until then this md is the staging carrier (review finding 2026-07-15: an unregistered plan md is an orphan — registration is part of Phase 1's definition of done).

## 0. Displaced cost (§6 — what pain this removes)

- **Fusion is modeled but unpriced.** The accelerator demo's `RunnableKernelWorkload { fused_op_count }` is the on-chip form of cache shaping (operand stays in registers across ops), but its cost is literally `cost_account_predicted_zero()` (`dag/gunbc/accelerator_demo_plan.dag:87-89`): fused and unfused have identical (zero) cost, so nothing can *justify* fusion. A decision the model makes but cannot price is a decision made by convention (§1).
- **The reduce spine on parallel hardware.** `docs/plans/execution-spine-design.md:67` measures 1/128 serialism from non-monoidal accumulator-threading; its enforcement item "every combine inhabits Monoid" is design-level only — no carrier a scheduler could dispatch on exists (`Monoid`/`CommutativeMonoid` are field-identical records, `dag/std/algebra.dag:14-23`; no law is machine-carried).
- **Placement is modeled but inert.** `dag/gunbc/roadmap_authority.dag:285`: "`Placement`/`Materialization` are modeled but inert — CI consuming them is the lane's real deliverable." The demo stamps `LocalAccelerator` as inspectable data; nothing routes execution by it. The WGSL handler is `DormantAwaitingOperator`.
- **Locality handled reactively, with a declared dissolve-on.** Runtime AIMD memory governor note (`src/v2/workflow/ci_floor_plan.dag:225`): "Dissolve-on: graph-derived per-node demand (CostAccount.space measured) replaces the reactive estimator." `CostAccount.space` is first-class but constructor-zeroed (`dag/std/realization_schedule.dag:39-46`); populating it is already declared P1 (`docs/plans/bounded-input-cost-envelope-scheduling.md` §7.2).

## 1. The two design axioms

**A. Machine shape is data — and there is no machine *kind*.** One scheduling kernel; machine shapes are rows — never two schedulers (the §4 N×M-adapter trap). Stronger (operator directive, 2026-07-15): "CPU vs GPU" is not a modeled distinction, the same way "batch vs streaming" is not — a nickname pair for points on shared axes (§3). No `MachineKind = Cpu | Gpu` discriminant may exist, and no `match` over one can, because the types carry no such field — the fork is unwritable by construction (§5), not merely discouraged. Every scheduling difference must *emerge* from faithful facts:

- **"serial folds are bad on GPUs"** derives as idle-lane cost — a loop-carried dependency utilizes 1 of `lane_count` lanes; at `lane_count = 1` the *same formula* says the serial fold wastes nothing. A scalar core, a SIMD core (8–16 lanes), and a GPU SM cluster (thousands) are three fills of one axis, and the schedule's response is monotone in it — no threshold, no kind test.
- **"transfer cost to the device"** derives as interconnect-crossing in the machine graph — and vanishes *by topology* on unified-memory shapes (the cited `apple_m5` row shares the DRAM level between execution domains), never by a special case. A discrete card and an integrated one are two topologies of one model.
- **"batch vs streaming"** is already dissolved by the substrate's bounded-and-forward execution: everything is a fold over a bounded prefix; residency windows (`transfer_grain`) express streaming without a second mode.

The orthogonality claim: computation graph × machine facts × algebraic evidence → schedule, each axis independently variable, no cross-term hand-authored.

**B. Every quantity carries evidence; the abstract shape is a first-class citizen, not a degraded one.** We cannot always know the concrete hierarchy (cloud VM, unknown SKU, future device). The intersubjective grounding (§3/§4 — cite the accepted framework, don't re-coin) is the **external-memory / ideal-cache model** (Aggarwal–Vitter 1988; Frigo–Leiserson–Prokop–Ramachandran 1999): a memory level is characterized by capacity M and transfer grain B, and the cache-oblivious result proves shape-level decisions (recursive blocking, fusion, affinity) are correct for *all* (M, B) simultaneously — only *pricing* needs concrete fills. So:

- **abstract shape** = the level structure with `UnknownQuantity`/`Interval` magnitudes — sufficient to *derive legality and preference order* of locality decisions;
- **concrete shape** = the same structure with `Exact` magnitudes carrying evidence (`Cited`/`MeasuredBy`/`DerivedFrom`) — sufficient to *price* them (roofline).

Unknown magnitudes fail closed to "no claim" (no affinity asserted, no accelerator placement chosen), never to a fabricated default (§5). Precedent for exactly this posture: operator ruling 2026-07-12 (`dag/std/realization_schedule.dag:82`) — "Memory class is a structural marker, not a quantity... Quantified per-runnable demand returns when it is derivable from the graph, not as authored literals."

## 2. The end shape (terminal model — operator agreement requested, then held fixed)

Operator working ruling (2026-07-15): define the end shape immediately, agree on the target, then move toward it in stages — no axis deferred into vagueness. The carrier types live in §3's block (they land in Phase 1); this section fixes the *semantics* — the laws every later phase moves toward. The movement is staged; the target is not.

- **Locality and latency are two axes, not one lattice (revised 2026-07-15 per review — the fused `LocalityTier` total order was wrong twice: `cache_interface` deliberately carries locality and latency as *separate* fields, cache_interface.dag:124-140, and `DeviceLocal` vs `InProcess` has no universal order — a level's nearness is relative to the consuming execution domain).** End state: (a) **latency** is a per-level magnitude — `MemoryLevel.latency: Quantified<Time>` — with ONE std latency-class *projection* converging the existing two-way class fork (`ReadLatencyClass = InProcessNs|LocalDiskUs|LanMs|WanTensMs`, cache_interface.dag:41; `LatencyClass = UltraLow|Low|Medium|High`, product/network_topology.dag:21) — the lift-out converges both class enums, never becomes a third; (b) **locality** is *graph-derived and domain-relative* — a level's position relative to a consuming domain is computed from `MachineShape` structure (tower order within a domain; edges crossed between domains), exactly as `sharing` already derives from `SharedLevelEdge` membership — never a stored universal enum. `cache_interface`'s monotone law (`cache_layer_ids_respect_locality`) survives as a law over each domain's *derived* ordering. Register file to WAN, one concept — but one *derived axis*, not one stored order (§2 horizontal).
- **Lockstep is structure; divergence is a derived price.** `LaneGrouping = IndependentLanes | Lockstep { group_width: Quantified<Int> }`. The divergence law: within a `Lockstep` group, branch arms *serialize* (cost = sum of arms, lanes masked); across `IndependentLanes`, concurrent arms cost max. SIMD masking and SIMT warp divergence are this one law at two fills — a `gpu_divergence` special case cannot exist. The already-modeled PTX rows (`ThreadHierarchyShape`, `PtxCost` — `src/v2/extdeps/languages/ptx.dag`, today zero consumers) become concrete fills, gaining their consumer.
- **Idle-lane law.** A loop-carried dependency occupies 1 of `lane_count` lanes; idle cost is derived and monotone in `lane_count`. "Serial folds are bad on wide machines" is this law's high-fill reading; at `lane_count = 1` the same law prices the waste at zero.
- **Crossing law.** Transfer cost is a property of the `InterconnectEdge` crossed — `LinkEdge` prices by link bandwidth/latency; `SharedLevelEdge` crossings price as ordinary tower traffic on the shared level (zero link cost). Unified vs discrete is topology, never a mode.
- **Placement's terminal state.** `Placement` dissolves into a `DomainId` — a *reference to an `ExecutionDomain` in the machine graph*, a position, never a kind (representable because domains carry stable identities — §3 type block, revised per review). `LocalAccelerator` is acknowledged as a smuggled kind (operator ruling 2026-07-15: added recently, "needs to keep being scrutinized"); retained during migration under the coarse-tier reading, with this dissolution as its named trigger (§6 discipline: scaffold + trigger, never a silent survivor).
- **Kind-erasure at the derivation boundary.** Cited catalog layers *keep* their upstream kinds — `ProcessorKind = CpuProcessor|GpuProcessor|AcceleratorProcessor` (fleet_intent.dag) and `MemoryKind = Dram|Hbm|UnifiedShared` (extdeps/memory/types.dag:18) are §3-faithful where vendors publish those families — but the `ComputeHost → MachineShape` derivation **erases** them: the scheduler's view carries facts only. One catalog row flagged for the same scrutiny as `LocalAccelerator`: `MemoryKind.UnifiedShared` fuses a *topology* fact (who can address it) into a *technology* enum (`Dram`/`Hbm` are silicon; an M5's "unified" memory is technologically LPDDR that happens to be shared) — the end shape derives unified-ness from `SharedLevelEdge` and the enum should shed that variant when the graph carrier lands.
- **The one-derivation law.** `schedule = derive(computation_graph × machine_facts × algebraic_evidence)`. Any difference between two machines' schedules must be traceable to a fact difference. This wall is judgment-guarded for now (§9, ruling 4) — held in operator review until a mechanical guardian exists.

## 3. Phase 1 — model the machine shape (std + extdeps; no scheduler change)

New std authority (name open: `std.machine_shape`):

```
type Precision<T> = Exact { value: T } | Interval { lo: T, hi: T }
type Evidence     = Cited       { source: DeclarationRef }
                  | MeasuredBy  { receipt: ExecutionReceiptDigest }   -- cache_identity.dag: ExecutionReceiptDigest = ContentHash
                  | DerivedFrom { derivation: DeclarationRef }
type Quantified<T> = Known { precision: Precision<T>, evidence: Evidence } | UnknownQuantity
```

(Revised 2026-07-15 per review: the original flat coproduct conflated provenance with precision AND was forgeable — an authored literal could simply be relabeled `Measured{...}` with nothing to check. Precision and evidence are now orthogonal fields, and every `Known` carries evidence *by construction*: a `Cited`/`DerivedFrom` ref must resolve, a `MeasuredBy` receipt must exist — an authored literal then has nowhere to hide. **PRECONDITION (#6663 review amendment 1, refuted-by-execution class-wide): today's typechecker checks record-literal field *names* only — field *presence* and field *types* are unchecked (`Known` omitting `evidence` compiles at 0 diagnostics), so an authored literal hides by omitting the field. The construction claim is unenforceable until the record-field wall (witness-realization P0 / #6663 audit F1) lands — P0 is a named Phase-1 acceptance precondition. The asymmetry cuts the other way for §1's no-kind wall: bogus field names DO hard-error, so no-kind holds today while evidence-required does not.** Mirrors the `DescentEvidence` fail-closed-bottom shape; `CostBasis = Predicted | Measured` convergence is **ruled** (§9 ruling 6): staged, with the forced #6663-P1 join as the migration trigger.)

```
type DomainId = NonEmptyStr where brand("DomainId")   -- stable identity (brand convention per placement_supply.HostIdentity)
type LevelId  = NonEmptyStr where brand("LevelId")    -- levels are referenceable graph nodes, not anonymous values
type MemoryLevel {
  id:             LevelId
  capacity:       Quantified<ByteSize>
  transfer_grain: Quantified<ByteSize>      -- the ideal-cache B: cache line / page / DMA burst
  bandwidth:      Quantified<Bandwidth>
  latency:        Quantified<Time>           -- a magnitude; latency CLASSES are projections (§2 locality/latency bullet)
}
    -- NO sharing field (resolved 2026-07-15, §9 open-5): who shares a level is GRAPH structure —
    -- a level referenced by a SharedLevelEdge is cross-domain by that fact alone; a stored
    -- sharing enum would be the same fact in two places, desyncable (§3/§5)
type MemoryShape = List<MemoryLevel>         -- ordered outward; the recursive tower
type ExecutionShape { lane_count: Quantified<HardwareThreadCount>, grouping: LaneGrouping }
    -- scalar core / SIMD core / GPU SM: one axis, three fills — no kind tag
    -- HardwareThreadCount (measure.dag:207) reused, not minted: the tree already stretched this
    -- one brand across CPU threads (placement_supply, compute_fabric) and GPU lanes
    -- (GpuModelCatalogRow.execution_lane_count) — prior proof this is one axis
type ExecutionDomain { id: DomainId, execution: ExecutionShape, memory: MemoryShape }
    -- a domain owns its tower; every level it declares is that level's single authority
type InterconnectLink = PcieAttach { link: PcieLink } | NetworkAttach { interface: NetworkInterface }
    -- open, cited link-family coproduct (revised per review: LinkEdge previously accepted only
    -- PcieLink while the convergence map claimed NetworkInterface as a fill the type could not hold)
type InterconnectEdge
  = LinkEdge        { from: DomainId, to: DomainId, link: InterconnectLink }
  | SharedLevelEdge { owner: DomainId, sharer: DomainId, level: LevelId }
    -- explicit endpoints (revised per review: edges without endpoints are not a graph).
    -- SharedLevelEdge references the owner's level BY ID — embedding a MemoryLevel value here
    -- would duplicate the level, the same §3 desync the dropped `sharing` field had
type MachineShape { domains: List<ExecutionDomain>, edges: List<InterconnectEdge> }
    -- well-formedness, fail-closed: edge endpoints must resolve to declared DomainIds;
    -- SharedLevelEdge.level must resolve within the owner's tower; each LevelId declared
    -- exactly once; any dangling reference refuses at construction
```

A host is a *graph* of execution domains whose memory towers may share levels — and it is now literally representable as one: domains and levels carry stable identities, edges carry endpoints, and §2's terminal `Placement` is a `DomainId` reference. "Where a computation runs" is a domain in this graph; "what a move costs" is the edge it crosses (zero link-edges crossed = zero transfer, emerging on unified-memory shapes with no special case). There is deliberately no field that names what a domain *is* — only what it *has*.

**§3 de-forks this phase performs rather than creating:**

1. **Latency classes unify with `cache_interface`'s, not beside them** (revised per review — see §2's two-axis bullet: latency is a magnitude with one std class *projection*; locality is graph-derived and domain-relative, never a stored total order). The projection lifts out to the std authority and converges `ReadLatencyClass` (cache_interface.dag:41) with `network_topology.LatencyClass` (:21) — never a third enum. `cache_interface`'s deliberate locality/latency field separation (:124-140) is *kept*, and its monotone law (`cache_layer_ids_respect_locality`, :382-403) survives as a law over each domain's derived ordering. The nanosecond memoization and the CAS artifact store remain one concept at two breadths — §2 horizontal — expressed as derived position, not shared enum rungs. Keying semantics (`CacheKeying`, `ProviderTier`) stay `cache_interface`'s own.
2. **GPU interconnect de-forks `PcieLink` from storage.** `NvmeDeviceCatalogRow.pcie_link: PcieLink` with derived per-lane bandwidth already exists (`dag/extdeps/storage/types.dag:23-38,56`); `GpuModelCatalogRow` has no bus. Move/share `PcieLink` at the layer both can import and add `interconnect: PcieLink` to GPU rows — cited, like the NVMe rows.
3. **CPU catalog rows gain cited cache geometry** (`dag/extdeps/cpu/*`): L1d/L2/L3 capacity + line size per SKU, cited to vendor datasheets — same discipline as the DRAM organization axes. The Ampere row is the natural first fill (fleet hardware).
4. **Concrete GPU fill**: RTX 5090 / Apple M5 rows already carry `execution_lane_count`, `memory_bandwidth`, `boost_clock` (`dag/extdeps/gpu/nvidia.dag:18-38`) — enough for a roofline once shaped. Add SM shared-memory/register-file levels as cited rows. (The demo cites an "A100" with no catalog row — either add the row or repoint the demo at a cited device.)

**Doc-divergence repair (small, honest — ruled, see §9 ruling 7):** `CostAccount` is `{ time, space, power: Watt }` while DESIGN.md §2 says `Cost = Time|Space|Energy`; `measure.dag` has `Power` but no `Energy` quantity. Operator-ruled 2026-07-15: `Energy = Power × Time` lands as a *derived* quantity in `measure.dag` (never stored) in Phase 1; `CostAccount.power: Watt` unchanged; DESIGN §2 becomes true as written.

Acceptance (green-by-execution): witness that one `MachineShape` value with all-`UnknownQuantity` magnitudes is constructible and consumable (the abstract shape is a citizen); witness that the concrete RTX 5090 fill derives a roofline bound; REDs: a `Known` whose evidence ref does not resolve — or whose `MeasuredBy` receipt does not exist — refuses at construction (enforceable only once witness-realization P0 — record-literal field presence/type checking — lands; P0 is a Phase-1 precondition, #6663 review amendment 1); a `MachineShape` with a dangling `DomainId`/`LevelId` reference refuses.

### Convergence map — every end-shape element to its existing carrier (§2/§3 discipline; DFS'd before minting)

| End-shape element | Existing carrier (live tree) | Relationship |
|---|---|---|
| `Quantified<T>` | `CostBasis = Predicted\|Measured` (realization_schedule.dag:19); evidence family: `DescentEvidence`, `DecodeFidelity`, `Disposition` (std/disposition.dag), `Witness<V>` | new value-level member of an existing family; convergence **ruled** (§9 ruling 6): staged — `Quantified<T>` greenfield for machine-shape quantities, `Evidence.MeasuredBy` reused by P1 receipts from day one, `CostBasis` migrates per-field at the forced #6663-P1 join (the marked interim two-carrier period's trigger) |
| latency classes + locality (two axes — revised per review) | `PersistenceLocality` + `ReadLatencyClass` kept as *separate fields* (cache_interface.dag:24-28,41,124-140) **and** `LatencyClass` (network_topology.dag:21) | one std latency-class *projection* converges the two class enums; locality stays a distinct, graph-derived, domain-relative axis — the earlier fused total order was wrong |
| `MemoryLevel` magnitudes | `ByteSize` (measure.dag:171), `Bandwidth` (measure.dag:293) | reuse |
| `MemoryLevel` concrete fills | `MemoryFacts { capacity, memory_kind }` (extdeps/memory/types.dag:20) — coarse ancestor; RAM level: `DramModuleCatalogRow`/`HostMemoryPopulation` (extdeps/memory); device level: `GpuModelCatalogRow.memory` + `memory_bandwidth` (extdeps/gpu); on-chip levels: **new cited rows** (the one genuinely absent piece) | fills / extends |
| `MemoryKind.UnifiedShared` | extdeps/memory/types.dag:18 | flagged (§2 kind-erasure bullet): topology fused into technology enum; sheds when `SharedLevelEdge` lands |
| `ExecutionShape.lane_count` | `HardwareThreadCount` (measure.dag:207) — already spans CPU threads and GPU lanes | reuse the brand |
| `LaneGrouping` fills | `ThreadHierarchyShape`, `PtxCost` (src/v2/extdeps/languages/ptx.dag — zero consumers today) | gains its first consumer |
| `InterconnectEdge.LinkEdge` | `PcieLink` + derived per-lane bandwidth (extdeps/storage/types.dag:23-38) | de-fork onto GPU rows (which have no bus today) |
| `InterconnectEdge` fleet-grain fill | `NetworkInterface { bandwidth, latency_class, locality }` + reachability zones (product/network_topology.dag:23-28,64-98) | now held by the type: `InterconnectLink.NetworkAttach { interface: NetworkInterface }` (revised per review — previously claimed as a fill the `PcieLink`-only type could not hold) |
| `MachineShape` (degenerate) | `PlacementSupplyRow { identity, hardware_threads, clock_hz, ram_bytes }` + `cpu_capacity_hz_row` (product/placement_supply.dag) — **corrected per review: one constructor (`placement_supply_row_from_host`, fleet_intent.dag:98), zero production callers; `cpu_capacity_hz_row` unused; the earlier "~10 fleet consumers" was a grep -l overcount** | `PlacementSupplyRow` IS the single-domain, one-level-tower `MachineShape` and `cpu_capacity_hz_row` (threads × clock) is the compute roof — a zero-consumer carrier is itself a §6 flag, and supersession is correspondingly cheap: converge it into the first `MachineShape` fill; there are no consumers to migrate |
| `MachineShape` derivation source | `ComputeHost { processors, memory, storage, network_interfaces }` (fleet_intent.dag) | derives-from, with kind-erasure at the boundary (§2) |
| `MachineShape` market consumer | `compute_fabric.Shape { hard: HardRequirements { threads } }`, `Opportunity.offers`, `shape_covers` (product/compute_fabric.dag:6-14,30-34) | **`Shape` IS the degenerate one-axis MachineShape** (operator-ruled 2026-07-15): MachineShape grounds it; the thin market (`Program`/`Opportunity`/`connect`) stays product-layer and is NOT rebuilt (the #5904 dissolution stands); `shape_covers` generalizes to the derived legality relation over the full shape; a `Fabric` is the outermost rung of the same recursive tower — hosts as domains, `network_topology` links as edges |
| pricing/comparison fold | `cache_reach_comparisons` pareto fold (cache_interface.dag:289-307); `time_measure_par`/`seq` (realization_measurement.dag:74-96) | roofline is the on-chip instance of the same comparison shape |
| demand side (unchanged) | `ResourceEnvelope`/`MemoryRequirement` (fleet_container.dag:35-45); `CostAccount.space` | `MachineShape` is supply; demand carriers stay put |
| `Evidence.MeasuredBy` receipt | `ExecutionReceiptDigest = ContentHash` (cache_identity.dag) | reuse |
| one hash authority (Phase 2 precondition) | `v2.std.node.Hash` (materialize's key, materialize.dag:3) vs std `ContentHash` (`RealizationPlan.target`) — the fnv1a64 dual-surface §3 thread DESIGN.md tracks | converge before `OperandFlow` lands; two hash carriers would cement the fork |
| name collisions (explicitly NOT converged) | `std.resources` (effect capabilities), `std.width` (text truncation), `machine_constraints.MachineWidth` (word width ≠ lane count) | disjoint concepts; noted to prevent accidental unification |

## 4. Phase 2 — operand facts on edges (the crux the survey located)

Today `DependencyView` edges carry no operand: the scheduler collapses `DataDependsOn` to a Bool (`src/v2/workflow/scheduler.dag:55-73`), so shared-operand structure — N consumers of one value — is invisible to batch formation. Locality is a property of *edges* (data flowing), not nodes; this phase gives it a carrier.

- **Total derivation, spelled out (revised per review: the original row assumed identities that do not exist yet — `DependencyView` carries no operand, dependency.dag:36-41).** Three defined pieces, each fail-closed:
  1. *Operand identity* = the producer's output value-root: `operand_root(edge: DependencyView) -> OperandRoot { root: Node } | OperandRootUnresolved { edge }` — defined for `DataDependsOn` as the source node's materialized output binding; every other `DependencyKind` carries no operand by construction; an unresolvable producer output refuses, never guesses.
  2. *One hash authority, as a precondition*: `materialize` keys on `v2.std.node.Hash` (materialize.dag:3,13) while the plan layer carries `ContentHash` (`RealizationPlan.target`) — the same fnv1a64 dual-surface §3 thread DESIGN.md already tracks. `OperandFlow` is **gated on grounding these as one authority**; landing it across two hash carriers would cement the fork this lane exists to prevent.
  3. *Footprint* binds to `node_keyed_graph_transitive_bytes` (`realization_runner.dag:60`: "transitive footprint is DERIVED, never stored... no parallel size walk may exist anywhere") **via the node-keyed artifact/root mapping that fn requires (realization_runner.dag:174-182)** — the edge→root mapping of (1) is exactly that missing input; a second size walk is a hard reject; `NodeKeyedGraphTransitiveBytesUnsettled` surfaces as a refused row.

  Result rows: `OperandFlow { edge, operand, footprint: Quantified<ByteSize> } | OperandFlowRefused { edge, cause }` — refused rows are typed and counted, and produce no affinity claims downstream.
- The operand's `ContentHash` is the *same* identity `materialize` keys `Share` on — one identity, two schedule consequences: dedup (don't compute twice) and affinity (consumers run adjacent). This is the §3 unification that makes cache shaping fall out of machinery we already trust.
- Populate `CostAccount.space`'s **`DerivedFrom` fill only** (static operand footprint) from the same authority — discharging the declared P1 fill, **not** the AIMD governor's dissolve-on (#6663 review amendment 2): the governor packs against *measured per-runnable peak*, a different space fact — receipt: the whole-corpus run peaked at 6.25 GB VmSize while operand data was kilobytes; the mass was retention, not operands (the v1 run-stability throughline's exact axis). The dissolve-on additionally requires `MeasuredBy` peaks (witness-realization P1, #6663). The Evidence split expresses the distinction — `DerivedFrom` footprint vs `MeasuredBy` peak — never conflate them.

Acceptance: for a fixture graph with one Substantial shared operand and two consumers, the derived `OperandFlow` rows name the same `ContentHash` with equal derived footprints; RED: a graph whose footprint is unsettled yields typed `UnknownQuantity` rows and *no* affinity claim downstream.

## 5. Phase 3 — locality: affinity as derived topology (every level of the tower, any machine)

The repel primitive exists (`runnable_excludes_corpus_co_residence` → `ResourceDependsOn` edges, `ci_floor_plan.dag:322-352`); the attract dual does not.

**Refinement, operator-ruled 2026-07-15 — dissolves the apparent conflict with the signed scheduling authority rather than superseding it.** The signed invariant's wording (`bounded-input-cost-envelope-scheduling.md:10-18`: topology hardware-free; `schedule_eq` across hosts) **conflated determinism with input-invariance** (operator: "if an A100 has more resources it's probably expected that the output is different"). Restated as three separate properties: (1) **determinism** — `topology = derive(computation_graph × declared MachineShape)` is a pure function: same inputs → same schedule, always, everywhere; (2) **the surviving wall: no UNSTABLE inputs to topology** — `MeasuredBy` quantities are banned because measured values vary run-to-run on *identical* inputs, which is what actually breaks reproducibility; a *declared* shape (`Cited`/`DerivedFrom`, content-addressed) is as stable an input as the source code itself; measured values stay peripheral (width, admission, the AIMD governor); (3) **cross-host `schedule_eq` is demoted from axiom to corollary** — it holds exactly when declared shapes are equal; a schedule that differs when its inputs differ is behaving, not flapping. The signed doc's invariant-4 wording receives a marked amendment to this restatement when this phase lands. (Composes cleanly with witness-realization P4, #6663: this plan prices *topology* from declared shape; P4 packs *width* from measured peaks — the same stable/unstable-input split, verified compatible by that review.)

- Rule (under the refinement): consumers of one `Share`d operand whose footprint is `Substantial` relative to a level's capacity prefer co-batch placement within that operand's residency window; where the readiness frontier permits multiple legal compositions, affinity selects deterministically among them.
- Pricing: a `TrafficAccount` per level boundary — bytes crossing each `MemoryLevel`, derived from schedule × OperandFlow × MachineShape; roofline `predicted_time = max(compute_time, traffic_i / bandwidth_i)`. Fused-vs-unfused kernels now price differently — the Phase-0 gap closes. **The pricer refuses, never degrades (revised per review — the earlier "term-incomplete `Predicted` account" was a §5 fallback answer: pricing that continues despite ignorance):** it returns `Priced { account } | PricingRefused { missing: List<QuantityRef> }`; a missing bandwidth or footprint yields `PricingRefused`, counted, and any comparison consuming a refused price is itself refused. No account with silent holes exists.
- Anti-goal (§4): no tunable knobs — no prefetch distances, no blocking factors, no thresholds. Every decision derives from (footprint vs capacity) comparisons over evidence-carrying quantities; absent evidence = a typed refusal, not a default.

Acceptance: a two-consumer shared-operand fixture schedules the consumers co-batch when capacity evidence admits both, with a `TrafficAccount` showing the operand loaded once; RED (discriminating): force the consumers into separate batches and the account shows the double load — the defect is *visible in the account*, which is the point.

## 6. Phase 4 — within-fold parallelization: licensed by algebraic evidence, priced by lane facts

A serial fold is optimal at `lane_count = 1` and leaves `lane_count − 1` lanes idle otherwise — one derived cost, no machine kinds. What licenses parallelizing *within* a fold (tree-reduce/scan) is associativity of the combine. MapReduce demands it by assertion; this substrate can demand it **by executed witness**. Note the first consumer is not a GPU: the demo's contiguous-loop/SIMD path is a CPU execution domain with `lane_count > 1`, and it is where the seam already lives ("Build the seam on CPU... same plan, one target row → GPU", accelerator-demo-roundtrip.md:110-111) — the GPU is the same recognition with a larger fill and an interconnect edge to price, not a second path:

- **Law carrier — subject-bound, not a bare ref (revised per review: a lone `DeclarationRef` is forgeable — an unrelated law could license an unsound reduction).** The mechanism exists in two halves: law obligations consumed by testgen (`NatAlgebraLawObligation { algebra, inhabitant, law }` — note it already carries its *subject* fields, `src/v2/std/nat.dag:83-105`) and green-by-execution monoid law witnesses (`keyed_delta_fold_witness_test.dag:116-146` — cited as the *mechanism precedent only*: it proves `KeyedPatch` associativity and licenses nothing about any other combine). Phase 4's carrier keeps the obligation's subject shape and adds the execution receipt: `AssociativityEvidence = WitnessedAssociative { combine: DeclarationRef, inhabitant: DeclarationRef, law: DeclarationRef, receipt: ExecutionReceiptDigest } | NotWitnessed` — and the license check requires `evidence.combine` to be structurally identical to the scheduled combine (fail-closed on mismatch; an unrelated law cannot attach). Commutativity is **never** required: structural order is preserved by the DependencyView/tree-reduce (Feinberg's commutative-monoid demand exists only because hash-shuffle destroys order; we never destroy order). Scope note resolving this phase's half of the §5-phase invariant question: within-fold expansion (serial vs tree-reduce) is realized *inside* the `RunnableKernelWorkload`'s kernel plan — like `fused_op_count`, a workload-internal fact — it never edits central `Schedule` topology.
- **Recognition fold grows one class.** `ElementwiseFoldClass` gains `AssociativeFoldChain { combine, evidence }` beside `PureElementwiseChain`; recognized → a tree-reduce/scan kernel plan whose depth/width derive from the target domain's `lane_count`; `NotWitnessed` → typed refusal (the fold stays serial — a correct schedule at any lane count, merely priced with its idle-lane cost so the displaced value of *witnessing* the law becomes visible). `DataDependentGather` stays `Refused` — the honest wall, unchanged.
- **Float reassociation is a declared contract, reusing the FMA machinery verbatim.** Tree-reducing a float fold reassociates — not bit-exact against the serial oracle. The demo already models exactly this class: FMA-permitted is declared `Lossy` and proven non-bit-exact; FMA-refused + insist → `FmaContractionViolatesContract` (`accelerator_demo_realize.dag:393-435`). Reduction-order relaxation is one more `NumericalContract` axis: permitted → `Lossy` with differential bound; refused → the fold is not accelerator-schedulable, typed refusal. Int/Nat folds tree-reduce `BitExact`.
- **Placement is priced, not preferred — and the decision refuses rather than answers (revised per review: "stay at the current domain" was a fallback answer, placement continuing despite ignorance).** A step's domain is chosen by comparing, per candidate domain, `edge_crossing_cost + roofline_time(domain)` over evidence-carrying quantities — the operand's current domain is one candidate whose crossing cost is zero. Any `PricingRefused` among candidates → the decision returns `PlacementRefused { missing }`, typed and counted; **no placement claim is made**, and the step remains under the pre-optimization baseline semantics — as the *absence* of the optimizer's output, never as its fabricated answer (the same posture as `NotWitnessed → serial`: a correct plan, visibly unoptimized, the refusal's frequency observable so the missing evidence ranks for fixing). No "prefer GPU" flag exists; no domain-kind is consulted (none exists to consult).
- **Realization handlers are the existing seams**: simd contiguous-loop (live), WGSL kernel (dormant, `feature:dag-gpu-realization-handler` — waking it is this phase's optional closer, operator-gated). Differential witness against the scalar oracle stays the acceptance instrument.

## 7. The orthogonality proof (the DESIGN §7 deliverable)

One derivation, **three fills on one axis**, by execution: run the same computation graph (the demo's fused elementwise chain + one witnessed-associative reduction) through the one scheduling kernel against three `MachineShape` fills that differ only in facts — scalar domain (`lane_count: 1`, Ampere core), SIMD domain (`lane_count: ~8-16`, same tower), many-lane domain behind a `LinkEdge` (RTX 5090 fill, `lane_count: 21760`):

- the derived schedules are, respectively: serial fold / shallow tree-reduce / deep tree-reduce with priced edge-crossing — a **monotone response to `lane_count`**, with graph-independence parallelism and affinity batching identical across all three (those derive from the computation graph and memory tower, not the lanes);
- a fourth fill — same many-lane domain joined by `SharedLevelEdge` instead (the `apple_m5` topology) — prices crossing at zero and the placement flips accordingly, *with no code path knowing it is "integrated"*;
- all fills produce differential-equivalent results under their declared contracts;
- **discriminating REDs**: (i) strip the associativity witness → every fill degrades to the serial fold (never fabricates a parallel plan), and the idle-lane cost term makes the loss visible at high lane counts; (ii) an all-`UnknownQuantity` shape → still a *legal* schedule, no affinity/placement claims (the abstract shape suffices for correctness, not for pricing — by construction, visibly); (iii) the kind-fork RED is discharged by construction — there is no discriminant to match on, so the forked-scheduler state is unwritable rather than tested-for.

That set of REDs *is* the orthogonality claim made executable: the schedule is a function of machine facts — monotone where the facts are ordered — and every cross-term is either derived or a typed refusal.

## 8. Non-goals and standing walls

- No interpreter parallelization (Rc→Arc gate removed, operator-signed — parallelism stays at the plan/Runnable grain).
- No hash-partition-by-key primitive; sharding remains "an independent region of the DependencyView" (execution-spine-design §5).
- No heuristic knobs, thresholds, or authored magnitude literals anywhere (2026-07-12 ruling, generalized).
- Scheduling determinism preserved as refined (§9 ruling 8): topology is a pure function of the computation graph × the *declared* shape; `MeasuredBy` quantities stay banned from topology; cross-host `schedule_eq` holds as a corollary under equal declared shapes (not unconditionally). Affinity is derived topology, never a per-host measured tweak.
- No new size walk: all footprints derive through `node_keyed_graph_transitive_bytes`.

## 9. Operator rulings (2026-07-15) and remaining open questions

Ruled:

1. **Locality/latency authority**: lifted out of `cache_interface` into std; `cache_interface` imports it. Refined per review (2026-07-15) into two axes: one std latency-class projection converging `ReadLatencyClass` + `network_topology.LatencyClass` (a pre-existing §3 fork this lane closes rather than widens), with locality graph-derived and domain-relative — the originally-ruled fused total order was revised, not the lift-out itself.
2. **`Placement::LocalAccelerator`**: acknowledged a probable smuggled kind (operator: added recently; "needs to keep being scrutinized/guided"); retained under active scrutiny with §2's domain-reference dissolution as its named trigger.
3. **Faithfulness staging**: the end shape is defined immediately and agreed up front (§2), including the divergence law; *implementation* moves toward it in stages — the target is fixed, the movement is staged. Consequences: the `LaneGrouping` axis lands with the end shape (semantics priced when a divergent workload exists), and the multi-domain graph types land in Phase 1 with single-domain fills only (the multi-domain consumer is Phase 4 — no machinery without a consumer, but no type churn either).
4. **No-kind wall guardianship**: judgment-guarded for now — no mechanical enforcement exists yet; the operator is working on that separately (the enforcement-intent lane is the natural home). Until then, §2's laws are the review checklist for every PR touching this lane; dissolution trigger for the judgment scaffold = a `StandingIntent` row once that machinery lands.
5. **`MachineShape` ⊂ `compute_fabric` (2026-07-15)**: `std.machine_shape` name accepted; `compute_fabric.Shape` is the degenerate one-axis instance MachineShape grounds; the market stays thin (no #5904 rebuild); a `Fabric` is the outermost rung of the recursive supply tower. Convergence-map row added.
6. **Evidence-carrier convergence (2026-07-15)**: staged — `Quantified<T>` for new machine-shape quantities (greenfield, zero migration); witness-realization P1 receipts reuse `Evidence.MeasuredBy` from day one; `CostBasis` untouched until the P1 join forces per-field migration; single owner = the #6663 lane; the interim two-carrier period is marked, with the forced join as its convergence trigger.
7. **`Energy` (2026-07-15)**: derived quantity in `measure.dag` — `Energy = Power × Time`, derived never stored; `CostAccount.power: Watt` unchanged; DESIGN §2 becomes true as written. Lands in Phase 1; this assignment is the #6663-F3 dedupe.
8. **Scheduling-invariant refinement (2026-07-15)**: the §5 restatement — determinism ≠ input-invariance; declared shape is a stable input; MeasuredBy-banned-from-topology is the surviving wall; cross-host `schedule_eq` demoted to corollary under equal declared shapes. **Phase 3 unblocked**; the signed doc's wording amended at Phase-3 landing.

Open:

1. Whether Phase 4's law carrier waits for the enforcement-intent lane (`StandingIntent` ⇄ `LensContract`) since "every combine inhabits Monoid" is precisely a standing intent — or lands locally and enrolls later.
2. Sequencing vs the namespace/SymbolIndex lane: OperandFlow derivation wants the containment SymbolIndex for cheap whole-tree walks; Phase 2 may be gated the same way the general body producer is.
3. **Hash-authority convergence (BLOCKS Phase 2)**: ground `v2.std.node.Hash` and std `ContentHash` as one authority — the pre-existing fnv1a64 dual-surface thread, now with a consumer waiting. Dispatched as a work item under sunny-deer-248 (2026-07-15).
4. **Witness-realization P0 field wall (#6663)**: record-literal field presence/type checking — Phase-1 acceptance-RED precondition; owned by the #6663 lane.

(Earlier open items 1, 2, 5, 6 resolved as rulings 5–8 and the sharing resolution above.)
