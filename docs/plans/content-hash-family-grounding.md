# ContentHash family grounding: Phase A landed, Phase B is the structural wall


---

*Provenance: this lane was carried as an Open Threads row in `DESIGN.md` until 2026-08-18, when that section was deleted on operator ruling — `DESIGN.md` is a first-principles document and per-lane status in it was a dual representation. The text below is preserved verbatim from that row; its authority is the carriers it names, not this file.*

**§3 residue — ContentHash family grounding — Phase A LANDED (#7480).** `std.content_hash` grounds `ContentHash` as a family coproduct (`Fnv1a64 | Sha256Hash | Sha1Hash`) with where-refined digest carriers; union-level `content_hash_eq`/`combine` deleted; `compare_content_hash` returns `ContentHashCrossFamilyIncomparable` (third state, not Bool collapse). Carrier walls: `OciDescriptor.digest` → `OciContentDigest`; `GateRosterHash` / `v2.std.node.Hash` → `Fnv1a64Structural`; Git tree hashes → `GitObjectId` (Sha1, cited via `extdeps.git.object_store`). `extdeps.crypto.hash` `sha256_digest_content_hash` is the VALIDATING PARTIAL bridge `Digest`→`ContentHash?` (invalid hex refuses via `Absent`, never absorbs a fallback), and `std.content_hash` `as_content_hash_cryptographic` is the TOTAL injection from an already-validated `Sha256Digest` — the distinction matters because only the second is unconditional (corrected 2026-08-01: this row previously called the partial bridge total, and its positive-path witness was named `..._is_total` while a sibling test proved the same function refuses invalid hex); `sha256:` prefix is wire serialization (`serialize_content_hash`), not a discriminant. `Pin.expected_identity` retains the union (subject-generic); `admit_pin_integrity` routes cross-family to `CrossFamilyIdentityIncomparable`. R0 backstop refuses bare union `==`/`!=` on family mismatch (`content_hash_cross_family_eq_v1_seed_bridge_note`; witness `cross_representation_equality_test`, DELETED 2026-08-11 with the gunbc#8146 suite cutover — the backstop is live and UNWITNESSED, dissolve-on recorded on that note). **Remaining Phase B (dissolve-on — this row deletes when landed):** substrate typecheck refuses union-level `ContentHash == ContentHash` so cross-family comparison is structurally unwritable; Phase A closes carrier construction walls + R0 eval only. Authority: `std.content_hash` `content_hash_family_grounding_note`. Witness: `dag/test/claim/content_hash_family_grounded_witness_test.dag`, `where_refinement_enforcement_witness_test.dag` (hex refinement at literal sites).

## fnv1a64 dual surface (moved from DESIGN.md §3, 2026-08-18)

**§3 convergence thread — fnv1a64 dual surface:** `v1.compiler.dag_collect_support.dag_node_surface_fingerprint_rec` calls the raw v1 intrinsics `atom_identity_hash`/`hash_combine` (fnv1a64 primitives registered in `04_method.dag` builtins); `std.content_hash` is the modeled surface over the same underlying authority. **LANDED (v2 carrier):** `v2.std.node` `Hash` is `std.content_hash` `Fnv1a64Structural` — the structural family member, NOT the subject-generic union; `content_hash`/`combine_hash`/`symbol_identity_digest` route through `std.content_hash`; `v2.std.materialize` keys on `ContentHash` (same identity as `RealizationPlan.target`). *(Corrected 2026-08-01, review on gunbc#7480: this clause read `v2.std.node.Hash = std.types.ContentHash`, which named the wrong home AND the wrong type once the family grounding row below landed — `ContentHash` no longer lives in `std.types` at all, and pinning the node carrier to the union would have re-opened exactly the cross-family comparison that row closes. The mechanism was always correct; the sentence describing it in this document was not, and it contradicted the Phase A row a few entries down — the §3 stale-citation class, caught by review rather than by the corpus.)* Remaining: ground `v1.compiler.dag_collect_support` fingerprint calls through `std.content_hash` when v1-stage v2 adoption clears.
