YOUR BRIEF. The work item's TITLE IS STALE — ignore it and read this. Two sessions spent an evening on a premise that was wrong twice; what survived is a real §7 repair, and it is much smaller than "79 files."

=== ITEM 0 — LEAD WITH THIS. AN ARCHITECTURAL QUESTION THE CARRIER CANNOT EXPRESS ===

`src/v1/stage0/src/v1_interpreter.rs` is 16,982 lines and IS declared. Its row says, in full:

    SeedRetainedIntrinsicRegistration { basename: "v1_interpreter", has_pub_mod: true, is_dir: false }

Nothing anywhere answers the question that actually matters about it:

    KERNEL    -- the interpreter IS the reference semantics, permanently seed-resident
    MIGRATION -- the interpreter is a TEMPORARY v1 realization, to be replaced by an emitted
                 v2 evaluator

That is not a row someone forgot to fill. THERE IS NO FIELD IT COULD GO IN. The decision is not undecided in the carrier — it is UNREPRESENTABLE in it.

YOUR DELIVERABLE HERE IS TO MAKE THE QUESTION ASKABLE, NOT TO ANSWER IT. Land a disposition arm that says the decision is OPEN and names both alternatives, so the tree carries the question. An operator-level architectural call is not a worker's to make, and the entire value is that afterwards someone can answer a question that today cannot even be posed. If you find yourself deciding it, you have exceeded the lane.

This is small — one arm, one row — and it is the only part of this lane that touches self-host architecture rather than its bookkeeping. Do it first.

=== THE GAP, STATED CORRECTLY (two wrong versions preceded it; do not re-derive them) ===

  NOT "79 files are undeclared"   -- false. 13, and mostly structural.
  NOT "the axis has no carrier"   -- false. It exists, is derived, is enrolled, and refuses.
  BUT  36 DECLARED ROWS CARRY WHY THE FILE IS IN THE CRATE AND NOT WHY IT IS SEED-RETAINED.

The three families:

    type SeedRetainedIntrinsicRegistration  { basename: String, has_pub_mod: Bool, is_dir: Bool }
    type SeedRetainedScaffoldRegistration   { basename: String }
    type SeedRetainedSupportDirRegistration { basename: String }

Three tree facts and nothing else. §7 requires "every seed-retained module is a DECLARED ROW WITH A REASON AND A MIGRATION TRIGGER, never a silent escape hatch." These satisfy the census half. Not one satisfies the reason half.

AND THE FIX IS ALREADY IN THE SAME FILE. `v2.compiler.self_host.stage0_crate_layout` `EmitterProducedDivergentRegistration`:

    type EmitterProducedDivergentRegistration { basename: String, reason: String, restoration_trigger: String }

with a note carrying declared rung (mitigatable), ceiling (structural impossibility), and a consumer that enforces "in three directions rather than reading it as permission: an undeclared divergence FAILS, a declared row that no longer diverges FAILS, and a declared row the emitter no longer produces FAILS (a row cannot outlive its producer)." The repo did this once, for `main.rs`, and never generalised it.

=== ITEM 1 — THE STALE CITATION ===

`emit_coverage_frontier_note` in `src/v2/compiler/self_host/emit_coverage_frontier.dag` describes, PRESENT TENSE, "a DIFFERENT authority from ... v2.compiler.self_host.frontier (that frontier tracks COMPILER MODULE self-emission)" as one of a live three-way sibling set. That module was deleted at the root by 39859f15b72 (#8344). The note is wrong twice: the module is gone, AND its surviving carve-out lives at `stage0_crate_layout`, which the note never mentions.

DELETE THE CLAIM, do not reword it — the note should not describe a third axis if nothing carries one. This cost two sessions an hour: I read it, found no frontier.dag, and concluded the axis had never been carried. Same absence, opposite history, opposite conclusion.

=== ITEM 2 — std_lens_verdict.rs, THE UNSWEPT HALF OF main.rs's REPAIR ===

    on disk:     "// Generated by v1 compiler -- do not edit." / "// Source module: std.lens_verdict"
    authority:   seed_retained_intrinsic_registrations carries
                 SeedRetainedIntrinsicRegistration { basename: "std_lens_verdict", has_pub_mod: false, is_dir: false }
    wiring:      no `mod std_lens_verdict` anywhere -> outside the crate module tree

The authority says seed-retained; the file's own generated header says emitter-produced. `src/v2/std/lens_verdict.dag` EXISTS (confirmed), so the repair is REMOVING THE ROW, not deleting the file.

This is item 3's problem in miniature: the row sits in a family with NO reason field, so there was nowhere to record that the file is emitter-produced. `main.rs` was moved OUT of SeedRetained into its own typed family for exactly that reason — the note says so — and this is the row that move did not sweep.

NOT A GATE HOLE, and I checked, because the alarming reading was available: `verify_hand_maintained` iterates the same list, finds this file IS in the emitted population, and compares both sides, routing a mismatch to `undeclared_divergent`. The code's comment names the count: "For 35 of the 36 entries this is the ordinary case." This is the one exception it already singles out. Behaviour correct; the NAME lies.

=== ITEM 3 — role / reason / trigger ON THE THREE SeedRetained FAMILIES ===

Model on `EmitterProducedDivergentRegistration`. **COPY ITS THREE-DIRECTION CONSUMER, NOT JUST ITS FIELDS** — fields without that consumer is 36 rows of prose nobody joins, which is #8344's death exactly. The fields are the easy half.

SCOPING, RULED, and BOTH OBVIOUS OPTIONS ARE WRONG:

  enforcement on, 36 rows refuse   -> reds main on 36 PRE-EXISTING rows. Not a line-stop: the factory
                                      model stops the line on a deficit SOMEONE JUST CAUSED. Applying
                                      a new rung retroactively to a legacy population is a different
                                      act, and it stops work nobody can currently do.
  enforcement off                  -> the inert lens, verbatim. Machinery exists, nothing gates, and
                                      it gets cited as coverage. DESIGN calls an inert lens a lie.

THE ANSWER IS §5's MONOTONE DEBT CONTRACT, and it is sanctioned explicitly because all four of its conditions hold here: the subject universe is independently discovered and closed (the roster is DERIVED from the tree); membership is checked at IDENTITY grain, never by count (per-row basename); the roadmap justifies the direction (§7, the seed shrinks toward zero); and every removal or exception carries a typed disposition (each row carries its own).

  ENFORCEMENT ON FROM DAY ONE, WITH THE WALL AIMED AT THE NEXT ROW, NOT THE EXISTING 36.
  A NEW seed-retained row without role/reason/trigger REFUSES immediately.
  The existing 36 carry an explicit typed disposition — name it so it CANNOT be misread as a
  decision (`ReasonNotYetDeclared` or similar) — counted, located, and MAY ONLY DECREASE.

§7 forbids SILENCE, not debt. The 36 stop being invisible and start ranking, which was the entire point, and main stays green.

WHY THIS IS NOT THE ABSORBING FALLBACK, since that is the objection to check: the 36 are typed, located and counted; the count is one-sided; and the arm does not WIDEN — a new row still refuses. The fabrication it would be is a disposition that made undeclared rows PASS. This makes them PASS-WHILE-COUNTED under a monotone bound, which is the contract §5 permits, not the default it forbids.

=== ITEM 4 — THE 13, AS A PREDICATE AND NOT A LIST ===

    lib.rs · v1_rt.rs · compiler_tests.rs · emitted_population.rs ·
    module_path_index/{mod,index,parsed_dag_file}.rs · six cli_run/ submodules

Each gets either a row with a reason, or a DERIVED STRUCTURAL EXEMPTION expressed as a predicate. Several are obvious — lib.rs is the crate root; the six cli_run/ files are `#[path]`-wired inside cli_run.rs, WHICH IS ITSELF DECLARED — so the honest count after that reasoning is smaller than 13 again. The 31 `bin/` files are outside the crate mod tree by construction and outside the comparison's denominator by design, not omission; they are not a gap.
If you write thirteen prose rows you have rebuilt the 27 at half scale.

=== WHAT IS ALREADY DONE — DO NOT REDO IT ===

gunbc#9096 (deep-ant-102, +60/-0 on `required_regen_host.rs`) closes ROW-NAMES-NOTHING: `copy_hand_maintained_support` iterated both rosters with a bare if-exists and no else, so a declared row naming nothing on disk was silently skipped. Both loops now refuse, typed and located, discriminating red proven by two independent controls, main verified green first.

YOUR ITEM 3 CLOSES ROW-EXPLAINS-NOTHING. Different walls on one authority. NEITHER SUBSUMES THE OTHER — do not read #9096 as having covered this.

=== THE GOVERNING RULE FOR ANY FIELD YOU ADD ===

    NO AUTHORED FIELD MAY BE SPELLED AS EVIDENCE.

Operator ruling 39859f15b72 (#8344) deleted a 27-row roster because "measurement fields were ORDINARY PARAMETERS, so a row that CLAIMED execution measurement was indistinguishable from one that ASSERTED it." The defect was not authorship — it was a field purporting to be a measurement being writable by hand. Partition by truth-maker: truth in the TREE must be derived; truth in a JUDGEMENT is authored, is POLICY, and its type must say Decision.

Note the split falls cleanly along the EXISTING field boundary here: basename/has_pub_mod/is_dir are tree facts and are already correctly derived. Role and reason are judgements. Nothing derived becomes authored; nothing authored is masquerading as derived. That is why this version is buildable and "classify 79 files" was not.

=== SCOPE FENCES ===

NOT this lane: migrating anything; making anything self-emit; a NEW authority beside stage0_crate_layout (three families exist — add a field, do not mint a fourth); ANSWERING the v1_interpreter question; the 03_ingest rustc board; the two live sibling frontiers.
An external ruling prohibits this becoming a migration lane and warns that LINE COUNT MUST NOT DRIVE CLASSIFICATION: "a 44k-line CLI driver may be a legitimate host boundary; a 200-line compiler authority file may be the real self-host blocker." The receipt: cli_run.rs and v1_interpreter.rs, the two largest, are both already declared.

=== TOOLING, MEASURED TONIGHT ===

`dashboard-ops reviews <n> <repo>` returns a confident all-zero with head_sha=None. Use bare `<n>` or the full PR URL. In the response read `merge_criteria`, not `readiness` — and even merge_criteria can publish an approval bound to a superseded head in the minutes after a push, so JOIN `reviews[].sha` AGAINST `merge_criteria.head_sha` YOURSELF. The row key is `sha`, not `head_sha`; asking for the wrong one returns None on every row and renders as "nothing matches" — the exact condition the check exists to detect.

Builds remote by default; anything that RUNS what it builds stays in ONE dispatch (runners amd64, container arm64). No whole-tree builds locally.

=== HOW TO REPORT ===

Items 0, 1 and 2 are small and near-certain. Land them and say so before starting item 3. If item 3's consumer is not reachable, say that early and say what blocks it — that is a real result. Do not improvise past a modeling doubt.
