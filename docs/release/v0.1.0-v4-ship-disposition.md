# v0.1.0 v4 Ship-Disposition Supplement — 2026-05-30

**Manager session:** `sharp-otter-407` (Close/Receipt lane).
**Coordination:** authored under PM dispatch with `snappy-bee-513` (Release lane, PR #3991 author).
**Operator policy (flavor (iv), 2026-05-30):** *"we can release an alpha v4 assuming its compilable — and just note the errors/wherever we are — its probably better to release it as a WIP instead of arbitrarily gating it (same with v3)"*.
**Authority:** PR #3949 §1 (two-axis vocabulary), PR #3938 §11.1 (Close/Receipt lane scope), PR #3973 §1 (TASKS.md predicate anchors), PR #4012 + #4017 (MW-D8 Wave 1 exit ledger), PR #4013 (v4-done six-predicate burn-down).
**Path choice:** `docs/release/` — survives `scripts/publish-snapshot.sh` strip-list per snappy-bee's framing.
**Supersedes inside D-REL-1:** PR #3991's `Strip src/v4 wholesale` posture is replaced by the alpha/WIP two-tier matrix below. PR #3991 D-REL-1 should link this doc.

---

## §1. Policy frame — flavor (iv) "alpha/WIP, no gates"

Operator decision 2026-05-30 (PM relay): v0.1.0 ships under **flavor (iv)**. Honest labeling, not Jun-1 gating.

Two public-support tiers:

| Tier | Surfaces | Public contract |
| ---- | -------- | --------------- |
| **`supported`** | The Rust + Python + Go subset per operator D-REL-3b 2026-05-30. | Bug reports accepted; advertised compile guarantees per `SUPPORTED.md`; user-facing regression discipline. |
| **`alpha/WIP — not on support contract`** | Everything in `src/v3/` and `src/v4/` not in the supported subset. | Ships in-tree, advertised as alpha; **no compile-clean guarantee, no support/regression contract, no advertised pre-tag close-gate**. Users running it accept current-state errors. The project's internal fail-closed invariant (every diagnostic path either succeeds fully or fails with a typed diagnostic, no fabricated plausible output) **remains in effect** for any code that runs — alpha-WIP is a release-tier waiver of support guarantees, not a license to violate compiler correctness. README + SUPPORTED.md state this verbatim. |

**What flavor (iv) replaced.** Earlier same-day PM messages floated flavor (iii) ("conditional on Wave 1+2 closing by Jun 1") which would have made the v4 advertisement a per-Wave receipt gate. Operator iterated within ~2 min to flavor (iv): no gating, alpha label. This supplement was rewritten before first commit to reflect flavor (iv); no flavor (iii) artifact reaches main.

**Two-axis vocabulary mapping** (this lane, per PR #3949 §1):

- All v4 surfaces ship under `ship_disposition: ALPHA_WIP`. This is a new value added to the Close/Receipt vocabulary specifically for flavor (iv); it does NOT replace `PROVEN` / `GAP` / `NOT_IN_V4` / etc. for internal close-readiness adjudication. `ALPHA_WIP` is a public-tier label, distinct from the internal closure-axis.
- `engineering_state` continues to track maturity per surface (the same six values from PR #3949 §1). The public reader sees: alpha surface + honest engineering_state.
- The MW-D8 Wave 1 ledger and v4-done burn-down remain the **internal maturation trackers**; they no longer carry release-gate weight. PR #4017 (Wave 1) and PR #4013 (burn-down) stay live as the v0.1.1+ roadmap source.

---

## §2. Per-surface table — every v4 surface, public tier × engineering_state

Reference HEAD: `origin/main` post-#4017 merge.

### §2.1 v4 substrate subtrees (all `ALPHA_WIP`)

| Surface | Public tier | `engineering_state` | Honest one-liner for public README/SUPPORTED.md |
| ------- | ----------- | ------------------- | ---------------------------------------------- |
| `src/v4/std/` — substrate type library | `alpha/WIP` | `SUBSTRATE_PRESENT` | "Authoritative `.dag` substrate; substrate-as-data is the public surface; downstream projections are work-in-progress." |
| `src/v4/lens/` — lens definitions (complexity, cost, parallelism, effect, leaf_model_verification, ...) | `alpha/WIP` | `SUBSTRATE_PRESENT` + `PARTIAL_GATE_PRESENT` for `leaf_model_verification.dag` R1/R2a/R2b/R3-external (PR #3972 + #4000) | "Lens models in-tree; R1/R2a/R2b/R3-external runners executable; everything else is alpha." |
| `src/v4/extdeps/languages/rust.dag` — Rust LanguageModel with R1/R2a/R2b/R3-external/R3-internal claims | `alpha/WIP` | `PARTIAL_GATE_PRESENT` for R1/R2a/R2b/R3-external; `SUBSTRATE_PRESENT` for R3-internal (blocker named: `"exercisable post-SG-1"`) | "Rust language model with executable per-claim runners for R1/R2a/R2b/R3-external; R3-internal authored with named blocker." |
| `src/v4/extdeps/languages/{python,go,typescript,ecmascript,swift,java,…}.dag` — other LanguageModels | `alpha/WIP` | `SUBSTRATE_PRESENT` | "Language model substrate landed; per-language claim corpora work-in-progress." |
| `src/v4/extdeps/formats/` — format models (json, yaml, csv, toml, sql, …) | `alpha/WIP` | `SUBSTRATE_PRESENT` | "Format model substrate; parse/emit operations declared; end-to-end runners work-in-progress." |
| `src/v4/install/install.dag` — installer with ShellStatic projection deferred | `alpha/WIP` | `SUBSTRATE_PRESENT` (🟡 deferred-projection markers in-tree) | Canonical exemplar — substrate ships, projected `install.sh` does not. |
| `src/v4/compiler/` — pipeline stages (01_parse / 03_normalize / 03_resolve / 04_infer / 05_emit / 05_eval / 06_lower) | `alpha/WIP` | `PARTIAL_GATE_PRESENT`; **~7,951 rustc errors on full-tree v4 Rust emit per `RELEASE_v0.1.0.md` §6.D-REL-1 source**; `INVARIANTS A3/P5` resolve-posture bridge (`.github/workflows/ci.yml:377-384` — `v2 → v4 bootstrap resolve-posture gate (CI emit-wall bridge)` step + `scripts/v4-bootstrap-resolve-posture-gate.sh` invocation) still masks compile failure | "v4 compiler pipeline alpha; substantial error count today (~7.9k); resolve-posture bridge in CI masks compile failure. Runs only via the bridge; not bootstrap-ready." |
| `src/v4/workflow/bootstrap.dag`, `src/v4/workflow/ci.dag` | `alpha/WIP` | `SCAFFOLD_PRESENT`; SG-7 `ci.dag` recursion in flight (#4014; MW-D8 C2 GAP per PR #4017) | "Workflow substrate alpha; bootstrap is the resolve-posture seed gated behind the same bridge; ci.dag SG-7 dissolution in flight." |
| `src/v4/bin/main.dag` | `alpha/WIP` | `SCAFFOLD_PRESENT` (T-15 structural harness only) | "Binary substrate alpha; T-15 structural harness landed; full bin/main execution work-in-progress." |
| `src/v4/test/claim/language_model/rust*.dag` — R1/R2a/R2b/R3-external claim + runner corpus | `alpha/WIP` | `PARTIAL_GATE_PRESENT` | "Per-claim runners executable on the four R-tier rows shipped." |
| `src/v4/test/claim/round_trip/`, `src/v4/test/fixture/` | `alpha/WIP` | `PARTIAL_GATE_PRESENT` — `RoundTripClaim` eval is **live** at `src/v4/compiler/05_eval.dag:1895-1907` (via `run_test_claim_round_trip_verdict_runtime` + `eval_round_trip_claim_input_for_verdict`); residual maturation is T-38 corpus-runner wiring and widening fixture coverage. | "Round-trip eval live in 05_eval.dag; full-corpus run wiring is work-in-progress per T-38." |

### §2.2 v3 subtrees (also `alpha/WIP` per operator quote)

| Surface | Public tier | `engineering_state` | Honest one-liner |
| ------- | ----------- | ------------------- | ---------------- |
| `src/v3/compiler/` and the hand-Rust bootstrap seed | `alpha/WIP` | `EXECUTION_NOT_WIRED` for v4-emit replacement (P6 burn-down GRAY); `PARTIAL_GATE_PRESENT` for v3-of-record compile + tests | "v3 hand-Rust bootstrap is the current compile authority; reproduction-proof gate (P6 v4-done) is alpha-pending." |
| `src/v3/dsl/std/` and downstream v3 substrate | `alpha/WIP` | `SUBSTRATE_PRESENT` | "v3 substrate ships in-tree; v4 is the substrate-of-record successor." |

### §2.3 Surfaces explicitly OUT of v0.1.0 advertisement (still ship in-tree as `alpha/WIP`)

Per operator D-REL-3b 2026-05-30: the **`supported` tier** for v0.1.0 is **Rust + Python + Go** subset. Everything else is `alpha/WIP` at most, even when its LanguageModel substrate exists in v4.

- `src/v4/extdeps/languages/typescript.dag`, `ecmascript.dag`: substrate ships as `alpha/WIP`; TypeScript moves to v4 early-support per D-REL-3b. **Not** on the `supported` tier for v0.1.0.
- `src/v4/extdeps/languages/swift.dag`, `java.dag`, …: substrate ships as `alpha/WIP`; not on the `supported` tier for v0.1.0.
- C / C++ / LLVM IR LanguageModels (if added): same — `alpha/WIP` if in-tree, never `supported` for v0.1.0.

### §2.4 What `supported` tier covers for v0.1.0 (cross-link out)

The `supported` tier itself is **snappy-bee-513's authority** via D-REL-3a (the `.dag` subset) and D-REL-3b (the target/artifact matrix — Rust + Python + Go per operator 2026-05-30). This supplement does NOT re-litigate which Rust/Python/Go surfaces are `supported`; that table lives in `docs/RELEASE_v0.1.0.md` D-REL-3a/3b and the downstream `docs/SUPPORTED.md`. This supplement only adjudicates the `alpha/WIP` complement.

---

## §3. What this supplement does NOT do

- **Does NOT gate the Jun 1 tag.** Per flavor (iv) the tag ships regardless of Wave 1 / Wave 2 / v4-done burn-down state. PR #4017 (MW-D8 ledger) and PR #4013 (v4-done burn-down) become the **post-tag maturation trackers**, not pre-tag gates.
- **Does NOT amend `RELEASE_v0.1.0.md`.** snappy-bee-513 owns PR #3991. The link from D-REL-1 to this supplement is at snappy-bee's discretion.
- **Does NOT amend `TASKS.md`.** v4-done predicates remain at `src/v4/TASKS.md:806-817`; this supplement maps surfaces to public tiers, not to predicate redefinitions.
- **Does NOT dispatch worker briefs.** Wave 1 / Wave 2 / SG-class work remains with the per-lane managers; this lane only adjudicates the public-tier disposition.
- **Does NOT introduce a pre-tag close-gate.** Flavor (iv) explicitly opts out of Jun-1 gating; the public surface advertises alpha, no compile-clean guarantee. This is a release-tier support waiver, NOT a waiver of the project's internal fail-closed invariant — implemented diagnostic boundaries must still either succeed fully or emit typed diagnostics without fabricating plausible output, in alpha code as in supported code.
- **Not a parallel ledger.** Per PR #3938 §11.1 standing principle, this supplement consumes inline marks and worksheets as authority and adds only the public-tier × engineering_state row per subtree.

---

## §4. Post-tag maturation cadence (informational, not a gate)

Even though flavor (iv) un-gates the tag, anti-shelfware deadlines from PR #3949 §4 continue to apply to the live in-tree work. Specifically:

- **PR #4017 MW-D8 ledger** stays live; this lane updates it as Wave 1 PRs land (next update on #4014 merge or its `revisit-by` 2026-05-31 12:00Z).
- **PR #4013 burn-down** stays live (Self-host/Release lane respawn pending per PM 2026-05-30 20:18Z; work-item `adhoc-d7d695d5-c7d` open against the Self-host/Release role-node).
- **Per-surface `engineering_state` transitions** drive future SUPPORTED.md amendments: if a `SUBSTRATE_PRESENT` surface flips to `PARTIAL_GATE_PRESENT` and operator decides to advertise it, it moves from this `alpha/WIP` table to the `supported` tier via a SUPPORTED.md amendment — a follow-on docs PR, not an automatic flip.

This cadence is informational. It does NOT block the v0.1.0 tag and it does NOT impose a v0.1.1 deadline; if the operator wants a v0.1.1 named-commitment-date on a particular surface's promotion, that's a separate decision authored against SUPPORTED.md.

---

## §5. Related artifacts

- PR #3991 (`docs/RELEASE_v0.1.0.md`) — release-readiness snapshot; D-REL-1 link target.
- PR #3949 (`docs/planning/v4-close-receipt-manager-pass-2026-05-30.md`) — two-axis vocabulary + close grades; `ALPHA_WIP` extends the vocabulary as a public-tier label.
- PR #3938 (`docs/planning/v4-correctness-ladder-2026-05-30.md`) — lane architecture; Close/Receipt scope (§11.1).
- PR #3973 (`docs/planning/v4-done-predicate-tasks-mapping-2026-05-30.md`) — line-anchor mapping for v4-done predicates this supplement references.
- PR #3983 (`docs/planning/v4-merge-wave-and-next-waves-2026-05-30.md`) — §7 MW-D8 (Wave 1 exit); under flavor (iv) this is maturation, not gating.
- PR #4012 + #4017 (`docs/planning/v4-mw-d8-wave1-exit-ledger-2026-05-30.md`) — live Wave 1 exit-condition tracker (maturation use only).
- PR #4013 (`docs/planning/v4-done-predicate-burn-down-2026-05-30.md`) — v4-done six-predicate burn-down (maturation use only).
- `src/v4/install/install.dag` — canonical substrate-shipped + projection-deferred exemplar.
- `src/v4/TASKS.md:806-817` — v4-done predicate definitions (operational authority, unchanged by this supplement).
