diff --git a/src/v1/stage0/src/lib.rs b/src/v1/stage0/src/lib.rs
index fdddcf7e82..d855195083 100644
--- a/src/v1/stage0/src/lib.rs
+++ b/src/v1/stage0/src/lib.rs
@@ -155,6 +155,7 @@ pub mod memory_governor;
 pub mod module_path_index;
 pub mod recorded_fixture;
 pub mod resolved_graph_cache;
+pub mod shadow_arg_conformance;
 pub mod shared_typecheck_store;
 pub mod usv_pilot_v2_std_algebra;
 pub mod usv_pilot_v2_std_collection;
diff --git a/src/v1/stage0/src/main.rs b/src/v1/stage0/src/main.rs
index 554bf524a7..ea15597cc6 100644
--- a/src/v1/stage0/src/main.rs
+++ b/src/v1/stage0/src/main.rs
@@ -696,6 +696,7 @@ fn main() {
                     v1_compiler_compile::stage0_self_compile_refusal_message(result.clone())
                 {
                     eprintln!("{message}");
+                    v1_compiler::shadow_arg_conformance::flush();
                     std::process::exit(1);
                 }
                 write_output_files(&output_dir, &result);
@@ -705,6 +706,7 @@ fn main() {
                     result.diagnostics.len()
                 );
                 render_diagnostics(&result);
+                v1_compiler::shadow_arg_conformance::flush();
                 if has_blocking_diagnostics(result.diagnostics.as_ref()) {
                     std::process::exit(1);
                 }
@@ -724,6 +726,7 @@ fn main() {
                         v1_compiler_compile::stage0_self_compile_refusal_message(result.clone())
                     {
                         eprintln!("{message}");
+                        v1_compiler::shadow_arg_conformance::flush();
                         std::process::exit(1);
                     }
                     let target_output_dir = format!("{}/{}", output_dir, name);
@@ -735,6 +738,7 @@ fn main() {
                         result.diagnostics.len()
                     );
                     render_diagnostics(&result);
+                    v1_compiler::shadow_arg_conformance::flush();
                     total_files += result.files.len();
                     total_diagnostics += result.diagnostics.len();
                     if has_blocking_diagnostics(result.diagnostics.as_ref()) {
diff --git a/src/v1/stage0/src/v1_compiler_infer.rs b/src/v1/stage0/src/v1_compiler_infer.rs
index 8040898507..f84a5c1659 100644
--- a/src/v1/stage0/src/v1_compiler_infer.rs
+++ b/src/v1/stage0/src/v1_compiler_infer.rs
@@ -6557,9 +6557,19 @@ pub fn infer_expr_body(
                                         scope.type_env.clone(),
                                         scope.module_name.clone(),
                                     );
-                                    let arg_compat_diags = if module_skips_direct_call_arg_check(
+                                    let __shadow_exempt = module_skips_direct_call_arg_check(
                                         scope.module_name.clone(),
-                                    ) {
+                                    );
+                                    crate::shadow_arg_conformance::shadow_direct_call(
+                                        call_application_plan.clone(),
+                                        scope.type_env.clone(),
+                                        scope.module_name.clone(),
+                                        scope.caller_decl_name.clone(),
+                                        func_name.clone(),
+                                        span.clone(),
+                                        __shadow_exempt,
+                                    );
+                                    let arg_compat_diags = if __shadow_exempt {
                                         Rc::new(vec![])
                                     } else {
                                         direct_call_arg_mismatch_diags(
