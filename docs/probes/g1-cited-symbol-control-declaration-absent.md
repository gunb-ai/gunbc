# G1 cited-symbol resolution — declaration-absent planted control

Permanent discriminating probe for `feature:cited-symbol-resolution`. The enrolled `HandAuthoredDocBind` cites `v2.std.node` / `G1_planted_declaration_absent_control_RED`, which has no `decl_facts` row. The production gate excludes this bind; witnesses prove the resolver refuses it and that including controls in the enrolled population yields a positive refusal count.
