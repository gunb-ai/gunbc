# G1 cited-symbol resolution — module-absent planted control

Permanent discriminating probe for `feature:cited-symbol-resolution`. The enrolled `HandAuthoredDocBind` cites `synthetic.g1_planted_module_absent_control_RED`, which is absent from `module_declaration_facts`. Distinct refusal arm from declaration-absent; never retires when production citations resolve.
