# G1 cited-symbol resolution — field-absent planted control

Permanent discriminating probe for `feature:cited-symbol-resolution`. The enrolled `HandAuthoredDocBind` cites `v2.std.node` / `NodeKind` with `NamedField { field_name: G1_planted_field_absent_control_RED }` — the declaration exists but the named field is absent from its node. Distinct refusal arm from declaration-absent and module-absent.
