**#1139 dispatch — L1 `Value` → `ValueBehavior` (PB-Runtime flat namespace).** Landed on `session/smart-boar-25` (includes regen + parse manifest).

**What shipped**
- `src/v3/spec/v3_l1.dag`: marker renamed to `ValueBehavior`; header + debt note (other L1 markers stay short until a concrete collision; full `*Behavior` set not done per stop+ping on blast radius).
- `Dag::populate_primitive_cache`: resolves `"ValueBehavior"`; `value_marker()` API unchanged (still returns that `DeclarationId`).
- `lower.rs`: behavior-marker validation + error text use `ValueBehavior`.
- Regenerated `bootstrap_generated.rs` and `bootstrap_generated_without_parse_surface.rs`.
- Tests/docs: `m1_5_testgen_test` declaration refs, `m1_substrate_test` expect string, `design-reference-resolution-provenance.md`, `emit/rust_target.rs` path comments (`src/v3/spec/v3_l1.dag`).
- Regression: `value_behavior_marker_tests::runtime_value_decl_coexists_with_value_behavior_marker` in `src/v3/compiler/src/lib.rs` — bootstrap has no bare `Value` marker; lowering a user `type Value {}` module onto `Dag::new()` stays diagnostic-clean; `value_marker()` still resolves `ValueBehavior`; user `Value` decl id ≠ marker id.

**Constraints:** no `RuntimeValue`; no module-qualified declaration identity; full marker-set rename deferred with in-file debt note.

**Verification:** `cargo test -p v3-compiler` (638 passed).

Reply here per relay — not posting to #1139.
