## T-Modeling `Secret<T>` — pre-flight + Shape A/B coordination (jolly-crane-283)

**Scope:** Pre-flight only per dispatch; no `Secret<T>` implementation PR opened.

### Authority audit (current tree)

- **`Declaration` (Rust):** `src/v3/compiler/src/dag.rs` — fields are `id`, `name`, `connective`, `type_params`, `phantom_params`, `meta_tag`, `specialization_parent`, `inhabits`, `value_body`, `refinement`, `span`. **No nominal-opacity / sealed-accessor / opacity carrier** today.
- **`Secret` in std types:** `dsl/std/types.dag` — `type Secret = String` (type alias; compiler sees through to `String`). Matches v2 `std_types` (`Secret` → `String`). **Not** thesis-grade nominal opacity.
- **Substrate language:** `src/v3/std/substrate.dag` documents `AtomPayload::ResolvedByStructure(DeclarationId)` as “resolved by structural walk” — that is **name-resolution / structure wiring**, not a secrecy opacity boundary.
- **Lenses / structural walks:** Multiple consumers descend `TypeConnective` and `AtomPayload` (e.g. `src/v3/compiler/src/lens_apply.rs` — `declaration_is_callable_type` peels `Instantiation` and follows `ResolvedByStructure` / `ResolvedByName`; cost / complexity lenses and generated `lens_*_generated.rs` follow the same substrate graph). **No opacity hook** exists yet; adding one is cross-cutting for Substrate + lens discipline (per producer brief §“Generic-walk discipline”).

### Shape A vs Shape B (Modeling view)

| | Assessment |
|---|------------|
| **Shape A (preferred)** | **Not feasible on this tree alone** — nominal-opacity carrier is absent, so Modeling cannot land `Secret<T>` + accessors + opacity diagnostic without Substrate’s PR. **Feasible as a joint same-PR plan** once #856 defines and implements the carrier + walk rule: the Modeling slice (declare `Secret<T>` consuming carrier, gated accessors, typed diagnostic for non-gated structural access, positive/negative/spoofing tests, DB-8) is bounded *if* lens/walk updates are in scope for that same PR. |
| **Same-PR consumer proof (Shape A)** | Needs: (1) substrate fact on the `Secret`-shaped declaration, (2) lowering/parser attachment per producer contract, (3) **one** generic-walk discipline implemented consistently (compiler + at least every lens path that today descends through `ResolvedByStructure` into “interior” types — likely coordinated list from Substrate), (4) accessor allowlist or equivalent static gate, (5) new diagnostic kind + tests, (6) DB-8 / determinism matrix unchanged bit-for-bit. |
| **Shape B (fallback)** | **Minimal consumer** in Substrate PR: e.g. a dedicated small lens or compile-time structural pass that **detects nominal-opaque declarations and fails closed** when a walk would cross the boundary outside an allowed accessor context — satisfies P2 “real consumer” without full `redact` / `compare_in_constant_time` / spoofing suite. T-Modeling follow-up PR then authors full `Secret<T>` + accessors + diagnostics + tests on the landed carrier. |

**Recommendation:** Treat **Shape A as the default target** when #856 and Modeling can share one merge without blowing past review/CI risk on lens refactors. **Choose Shape B** if Substrate needs to land carrier + walk skeleton first and lens blast radius must be staged.

**Ambiguity:** Shape A vs B **remains manager-owned until #856 posts an explicit readiness signal** (carrier shape + walk discipline + whether Modeling is in-scope same PR). Modeling pre-flight does **not** resolve that alone — not a STOP-AND-ESCALATE blocker yet; escalation only if #856 stays silent after substrate lands.

### STOP-AND-ESCALATE (pre-flight)

- **Producer vs gated accessors:** Cannot validate “carrier admits accessors cleanly” until #856 picks among bool / sealed `DeclarationRef` list / `inhabits`-shape (producer brief). No red flag from read-only audit beyond “not implemented.”
- **Lens fixtures:** Expect **touches** to generated + handwritten walk code when opacity is enforced; risk is real but not quantified without a prototype — flag for Substrate + Modeling joint design review, not automatic STOP from this pass.
- **Runtime dispatch for gating:** No evidence in current code that accessor gating *must* be runtime; static `DeclarationRef` allowlist is aligned with brief options.
- **DB-8:** Local `cargo` unavailable in this worker environment — **no determinism / fixed-point probe run here.** No drift observed; request CI or dev machine confirmation on any branch that adds `Declaration` fields or emit paths.

### Questions for R2 Substrate (#856) — please route

1. **Carrier:** Which option will land first — `Declaration` boolean, sealed accessor `DeclarationRef` list, or `inhabits`-mediated shape — and what is the Rust mirror location (`dag.rs` + `substrate.dag` parity)?
2. **Walk discipline:** Hard-stop diagnostic vs opaque-atom vs per-lens policy — which is canonical for v3, and will #856 include a **checklist of lens modules** that must be updated in the same PR as the carrier?
3. **Shape:** Does Substrate intend to **bundle full `Secret<T>` Modeling** (Shape A) or only a **minimal fail-closed structural consumer** (Shape B) in the landing PR? Please post the explicit one-line readiness signal Modeling is gated on.

---

*Refs: `docs/briefs/r2-modeling-secret-graduation-worker.md`, `docs/briefs/r2-substrate-nominal-opaque-for-secret-subset.md`.*
