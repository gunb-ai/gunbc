module t.crec

type Acc { n: Int }

type Fold<R> {
  init: fn(Int) -> R
  step: fn(R, Int) -> R
}

fn run<R>(f: Fold<R>, x: Int) -> R {
  f.step(f.init(x), x)
}

fn go(x: Int) -> Acc {
  run(f: Fold {
    init: fn(i) { Acc { n: i } },
    step: fn(acc, i) { Acc { n: acc.n + i } },
  }, x: x)
}
