[package]
name = "gunbc-prep"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Repository preparation tool - unwinds all DAGs and runs code generation"

[dependencies]
gunbc-ir = { path = "../../../core/ir" }
gunbc-exec = { path = "../../../core/exec" }
gunbc-primitives = { path = "../../primitives" }
gunbc-test = { path = "../../../core/test" }

[dev-dependencies]
gunbc-test = { path = "../../../core/test" }

[[bin]]
name = "gunbc-prep"
path = "../../../buck-out/gen/bin/prep/main.rs"
