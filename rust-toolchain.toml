# Canonical Rust channel + components for local rustup and GitHub Actions
# (`actions-rust-lang/setup-rust-toolchain` reads this file after checkout).
# Dependency notes: `dsl/extdeps/rustup.dag` (no second pin constant — this file is sole authority).
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
