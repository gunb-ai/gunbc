# Channel pin for local rustup and GitHub Actions (`setup-rust-toolchain` reads
# this file after checkout when no explicit `toolchain:` input is passed).
# Keep `channel` aligned with `dsl/extdeps/rustup.dag` `ci_pinned_toolchain`.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
