# Rustup install authority for this repo. Hand-maintained (no regen step
# overwrites this file). Keep [toolchain].channel aligned with
# dsl/extdeps/rustup.dag `ci_pinned_toolchain`.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
