# Channel pin for local rustup and GitHub Actions (`actions-rust-lang/setup-rust-toolchain`).
# Keep aligned with `dsl/extdeps/rustup.dag` `ci_pinned_toolchain`.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
