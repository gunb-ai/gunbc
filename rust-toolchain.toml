# Channel and components for local rustup and GitHub Actions.
# `actions-rust-lang/setup-rust-toolchain` reads this file after checkout.
# Sole hand-maintained toolchain pin for this repo (see `dsl/extdeps/rustup.dag`).
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
