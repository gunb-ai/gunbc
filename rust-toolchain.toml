# Cargo/rustup read this file on every build. CI uses the same pin via
# `actions-rust-lang/setup-rust-toolchain` (no per-job override); keep
# `dsl/extdeps/rustup.dag` `ci_pinned_toolchain` in sync when bumping.
[toolchain]
channel = "1.93.0"
components = ["clippy", "rustfmt"]
