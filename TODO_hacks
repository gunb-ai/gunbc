# TODO_hacks
#
# Known consolidation debt and implicit dependencies that should be
# made structural. Items here are "works but fragile" — they won't
# break today but will bite when something gets renamed/moved.
#
# Note: resolved items have been moved to `TODO/TODONE/TODO_hacks.md`
# and `TODO/TODONE/TODO_hacks_general.md`.

## "List" used as type_id AND cardinality shape (dual encoding) — PARTIAL FIXES

The design doc says cardinality is the canonical shape layer, but "List" is
deeply embedded as a type_id across 28 files — CLI arg parsing, mock
generation, loop patterns, makegen repeatable detection, etc.

Concrete bug (FIXED 2026-02-05): `tool_names` now uses `"String"` as the
element type in hand-written graphs (makegen/deps + workspace subdags).

Progress so far:
- CLI generation now derives list-ness from cardinality (no type_id == "List").
- makegen registry repeatable flags derive from cardinality.
- loop pattern defaults use element types + cardinality.
- deps graphs now use `"String"` element types for dep/tool name lists.
- bootstrap graphs now use list("crate_names", "String") instead of type_id "List".
- language subdags/patterns now use list(..., "String") for list ports.
- registry entrypoints now use type_id "String" + cardinality for extensions.
- CliEntrypoint::new no longer infers cardinality from "List"/"Set".

Remaining work: finish removing type_id-encoded cardinality
(e.g., StringList/OptionalString) once the type registry refactor lands.
Mock generation now hard-fails on unknown type_ids and on `List`/`Set`.

## Cardinality test-case cap is a bandaid (TODO)

To avoid huge generated vectors when a bounded max is large, testgen now
uses a capped boundary set (`Cardinality::test_cases_for_tests`, cap=64).
This prevents OOM/giant files but hides the deeper design issue: the
contract/testgen layer doesn't distinguish "boundary correctness" from
"large-N stress." We need a principled sampling strategy or explicit
test-budget policy (e.g., boundary-only by default, optional stress tests
per port/type) rather than a hardcoded cap.

Files:
  - core/ir/src/types.rs               (test_cases_for_tests / cap)
  - core/ir/src/contract.rs            (witnesses uses capped cases)
  - core/codegen/src/testgen/analyze.rs
  - core/codegen/src/testgen/obligation.rs

## `Map` type_id is under-specified for proofs/testgen

Value::Map(BTreeMap<String, Value>) lost type parameter info when
MapStrStr was replaced with generic Map. Codegen can't serialize
Value::Map in general, and there's no way to express "map of string
to string" vs "map of string to json" at the port type level.

For proofs/testgen, need one of:
- Parametric type IDs: Map<String,String>, Map<String,Json>
- Type DAG / type expression structure instead of flat String type_id

Without this, "Map" is effectively "untyped JSON object."

Files:
  - core/ir/src/value.rs                (Value::Map definition)
  - core/ir/src/types.rs                (type_id is a flat String)
  - core/codegen/src/testgen/codegen.rs (can't serialize Map faithfully)


## Cardinality constants are flat — no compositional modeling

Named cardinality constants (ZERO, ONE, ZERO_OR_ONE, etc.) are syntactic
sugar for interval structs. If "everything is a DAG" then cardinality
constraints could be modeled as composable DAG nodes — making multiplicity
runtime-evaluable and composable with other constraint nodes rather than
a static port property. Currently cardinality is compile-time only (used
for test generation and port validation), not a first-class runtime concept.

Files:
  - core/ir/src/types.rs (Cardinality struct and associated constants)

---

## DryRun mocks use silent defaults (may mask missing resource wiring)

Mock values for resources default to deterministic values:
  - Platform::Linux
  - Timestamp(0)
  - EnvVars empty map

This is good for determinism but acts as a fallback — a DAG can appear
to work in DryRun even if it never properly acquired/wired those resources.

**Suggested fix:** Consider a "strict DryRun" mode where env-node outputs
default to poison/UNSET unless explicitly mocked. Missing resource wiring
would then fail loudly instead of silently working with defaults.

Files:
  - core/exec/src/execute.rs (DryRun mock injection)
  - TODO/TODONE/design-resource-acquisition.md (resource modeling, DONE)

---

## Coercion coverage tests don't verify actual coercion (only "doesn't crash") — PARTIAL FIX (2026-02-12)

`build_coercion_coverage_tests()` generates tests that:
1. Build the DAG
2. Execute in DryRun
3. Assert via `.expect()` that it didn't crash
4. **NEW**: Assert the target node has a log entry (proving it actually executed)

This is better than just "didn't crash" but still won't catch shape bugs
(e.g., nested lists vs flat lists) if downstream ops are loosely typed.

The deeper fix (value-shape assertions) is blocked on one of:
- Recording node inputs in the execution log (refactor `LogEntry`)
- Injecting shape-assert nodes into test graphs (testgen complexity)

For now, **definitive coercion behavior is verified by the 4 explicit
coercion-kind-mapped tests in `execute.rs`** — these directly assert
value shapes for each `CoercionKind`. The generated tests provide
coverage that real DAGs don't crash at coercion edges.

Files:
  - core/codegen/src/testgen/codegen.rs (build_coercion_coverage_tests)
  - core/exec/src/execute.rs (test_coercion_* — definitive shape tests)

---

## ~~Coercion analysis may be ahead of runtime implementation~~ AUDITED (2026-02-12)

All three `classify_coercion()` kinds are correctly implemented by the
executor's fan-in logic. No gap between analysis and runtime.

| CoercionKind    | Classification                           | Runtime behavior                              |
|-----------------|------------------------------------------|-----------------------------------------------|
| `WrapScalar`    | `from.is_scalar() && to.is_list()`       | Scalar pushed to fan-in bucket → `List([v])`  |
| `OptionalToList`| `from == ZERO_OR_ONE && to.is_list()`    | Unit skipped → `List([])`; present → `List([v])` |
| `Widen`         | `from.is_bounded() && !to.is_bounded()`  | List items flattened via fan-in → re-wrapped  |

Explicit coercion-kind-mapped tests added in `execute.rs` (4 tests):
  - `test_coercion_wrap_scalar_single_edge`
  - `test_coercion_optional_to_list_absent`
  - `test_coercion_optional_to_list_present`
  - `test_coercion_widen_bounded_to_unbounded`

Generated coercion coverage tests (`build_coercion_coverage_tests`) now
assert the target node actually executed (log entry exists), not just that
the DAG didn't crash. No current DAGs trigger coercion edges (all builders
use matching cardinalities), so the improvement is forward-looking.

Files:
  - core/ir/src/coerce.rs (classify_coercion — unchanged, correct)
  - core/exec/src/execute.rs (fan-in logic — added tests)
  - core/codegen/src/testgen/codegen.rs (improved generated test assertions)

---

## Resource capabilities potentially forgeable via TryFrom<Value>

The resource acquisition design proposes:
```rust
pub trait Resource: Into<Value> + TryFrom<Value>
```

If `TryFrom<Value>` accepts any token-like value, capabilities become forgeable.
A malicious/buggy node could construct a fake handle from data.

**Suggested fix:** Runtime guard ensuring only env nodes can mint capability handles
(e.g., internal IDs stored in executor-side handle table, not in Value itself).

Also: `EnvVars` as observation resource risks spilling secrets unless it's a
filtered projection (the `CredentialOp` approach is safer).

Files:
  - TODO/TODONE/design-resource-acquisition.md (Resource trait proposal, DONE)

---

## Compound shell commands should be replaced with native Rust

Two places in the codebase build compound `sh -c` scripts instead of using
native Rust. These work but bypass the type system — the compound behavior
is invisible to the DAG and can't be individually tested or reasoned about.

### Batch file read (gist): `sh -c` with `;`-joined `cat` commands

`execute_prepare_read_files` builds a shell script that concatenates N
`echo marker; cat file` commands joined with `;`. This exists because we
need the contents of N files and chose to batch them into one shell call.

**Why shell isn't necessary:** Rust can read files directly via `std::fs::read`.
The Prepare node already knows all the paths — it could return a structured
request (e.g., `TransportRequest::BatchRead(Vec<PathBuf>)`) and the transport
executor could read them in Rust, returning structured results instead of
parsing marker-delimited stdout.

Alternatively, a LoopBuilder-based approach (already noted in the code) would
give per-file observability at the cost of N transport calls instead of 1.

Files:
  - lib/tools/gist/src/graph.rs (`execute_prepare_read_files`, ~line 150)
  - lib/tools/gist/src/graph.rs (`execute_parse_read_files`, ~line 207)

### Codegen exists check: `sh -c` with `&&`-joined `test -f` commands

`execute_prepare_codegen_exists` builds `test -f path1 && test -f path2 && ...`
to check whether generated CLI files exist. The answer is a single bool.

**Why shell isn't necessary:** Rust can check file existence directly via
`std::path::Path::exists()`. This could be a pure Rust check in the transport
executor (e.g., `TransportRequest::FilesExist(Vec<PathBuf>)`) or even a pure
node that checks the filesystem through a resource handle.

Files:
  - gunbc-dag/src/codegen/ops.rs (`execute_prepare_codegen_exists`, ~line 41)

### TransportOps::Execute skip is explicit (resolved)

`TransportOps::Execute` now requires an explicit `skip` input, and transport
graphs wire `skip` through from their prepare nodes. This removes the implicit
default and makes skippability visible in the DAG.

If wiring churn becomes a problem, add a small DAG-builder helper to connect
`prepare.* -> execute.skip` by convention.

Files:
  - lib/transport/src/ops.rs (`TransportOps::Execute`, ~line 25)

### Common theme

Both cases use shell as a "poor man's executor" for operations that Rust
handles natively. The transport layer could support structured file operations
(read, exists) directly, making the shell indirection unnecessary and keeping
all behavior visible to the type system.

---

## Cross-references (tracked elsewhere)

The following items are tracked in other docs:

- **`buck-out/gen` hardcoded in 16 locations** → TODO/consolidation.md §6
- **CODEGEN_SOURCES path list is static** → TODO/consolidation.md §6
- **Environment/resource modeling (inline handle construction)** →
  TODO/TODONE/design-resource-acquisition.md (comprehensive design, DONE)
- **IR completeness for generated code** → TODO/design-codegen-quality.md
