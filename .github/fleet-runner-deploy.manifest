runner-deploy UNSOUND: runner-slot conserving cap not enforced live: the runner-slice MemoryMax drop-in is inert (effective MemoryMax=INFINITY); refuse to derive a governing runner count that assumes a cap the host does not enforce (fail closed until ctrl runner_host_reconcile makes disk==live and the design authority flips it)
session-slice host=srv1 slice_max_bytes=21474836480 per_session_max_bytes=10737418240 oom_pressure_kill=true oom_pressure_limit_pct=60 cpu_weight=1000
session-slice host=srv2 slice_max_bytes=21474836480 per_session_max_bytes=10737418240 oom_pressure_kill=true oom_pressure_limit_pct=60 cpu_weight=1000
session-reservation UNSOUND: REGIME-2 keystone unmet: it over-commits the per-session caps, so it is unsound until per-cgroup memory.max + systemd-oomd eviction is verified live on the hosts (gunbc_fleet_oomd_evidence = OomdUnverified). Fail closed; do NOT reserve the smaller aggregate without the runtime eviction floor.
operating-curve (SESSION-side coverage only: operator selects one per-session operating point grounded in measured per-session peaks min 0.28 / median ~2.06 / max 8.27 GiB; sessions_slice = per_session_cap*concurrency. The per-host runner_count is budget-derived from runner_slice_cap and reported on the runner-deploy lines above, independent of this curve):
  per_session_cap_bytes=2211908157 concurrency=16 sessions_slice_bytes=35390530512 peak_coverage=6/13
  per_session_cap_bytes=4294967296 concurrency=16 sessions_slice_bytes=68719476736 peak_coverage=10/13
  per_session_cap_bytes=8879844884 concurrency=6 sessions_slice_bytes=53279069304 peak_coverage=13/13
  per_session_cap_bytes=10737418240 concurrency=6 sessions_slice_bytes=64424509440 peak_coverage=13/13
  per_session_cap_bytes=8879844884 concurrency=4 sessions_slice_bytes=35519379536 peak_coverage=13/13
