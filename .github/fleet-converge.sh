#!/usr/bin/env bash
set -euo pipefail
# GENERATED by dsl/gunbc/fleet_converge_emit.dag (gunbc.fleet_converge_emit). DO NOT HAND-EDIT.
echo "fleet-converge UNSOUND: runner deployment unsound: runner-slot conserving cap not enforced live: the runner-slice MemoryMax drop-in is inert (effective MemoryMax=INFINITY); refuse to derive a governing runner count that assumes a cap the host does not enforce (fail closed until ctrl runner_host_reconcile makes disk==live and the design authority flips it)" >&2
exit 1
