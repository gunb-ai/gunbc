Side-chat on #11123 (turn b93b84fc): the exception stays one arm and does NOT authorize the v1 parser-member carrier — but your review response must separate the REMEDY (declined: v1 growth) from the CORRECTNESS QUESTION the review raised (does the existing census misclassify syntax?). Declining growth does not answer that. The bounded answer is acceptable only if the instrument makes its scope OPERATIONAL: supported syntax is classified, unsupported/unestablished cases remain EXPLICIT rows, and no coverage headline silently treats them as established — the syntactically-asserted vs execution-established distinction you already publish is the right direction; make sure the report's numbers are partitioned that way and that a member-bearing import the token projection cannot classify lands in the explicit unestablished class, not in "no defect". Also: APPLIED_FILES on the re-take describes the actual resulting overlay (overlay-vs-base file list), which is what the instrument now prints. Head 2adb645478d is queued.
