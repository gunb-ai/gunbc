# Generated by gunbc-clippy
# DO NOT EDIT - regenerate with: cargo run -p gunbc-dag --bin gunbc-pragma

# Clippy configuration for gunbc
#
# BuilderError is intentionally large (144 bytes) to contain diagnostic info.
# Increase the threshold to allow it in Result types.
large-error-threshold = 256

# This configuration enforces system invariants:
#
# I6. NO ESCAPE HATCHES
#     The system cannot be bypassed. If I/O must go through transport,
#     there's no function to call to skip it.
#
# I7. NO FALLBACKS
#     Operations either succeed or fail. No silent degradation.
#     Don't use .unwrap_or_default() to hide errors.
#
# I8. NO WARNINGS
#     Run with: cargo clippy --all-targets -- -D warnings
#     Warnings are errors. If something is wrong, the build fails.
#
# APPROVED EXCEPTIONS (must have documented reason):
#   - gunbc-lib-transport (IS the I/O boundary - the designated place for I/O)
#   - gunbc-codegen (Bootstrap code - can't use transport (chicken/egg))
#   - gunbc-infra (Infra hub (fs helpers, test utilities))
#
# To add an exception: #[allow(clippy::disallowed_methods)] with comment.

disallowed-methods = [
    # Filesystem operations - use PrepareFileReadOp/PrepareFileWriteOp instead
    { path = "std::fs::read", reason = "I6: No escape hatches. Use PrepareFileReadOp + TransportOps::Execute" },
    { path = "std::fs::read_to_string", reason = "I6: No escape hatches. Use PrepareFileReadOp + TransportOps::Execute" },
    { path = "std::fs::write", reason = "I6: No escape hatches. Use PrepareFileWriteOp + TransportOps::Execute" },
    { path = "std::fs::read_dir", reason = "I6: No escape hatches. Use PrepareDirectoryListOp + TransportOps::Execute" },
    { path = "std::fs::remove_file", reason = "I6: No escape hatches. Direct filesystem ops must be in transport layer" },
    { path = "std::fs::remove_dir_all", reason = "I6: No escape hatches. Direct filesystem ops must be in transport layer" },
    { path = "std::fs::create_dir_all", reason = "I6: No escape hatches. Direct filesystem ops must be in transport layer" },
    
    # Process execution - use node.requires(&cli::TOOL) for tool dependencies
    # Direct Command::new should only be in transport executor and cli.rs
    { path = "std::process::Command::new", reason = "I6: No escape hatches. Use env nodes + tool handles. Command::new only in transport executor/cli." },
]
