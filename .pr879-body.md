Session `jolly-stag-413` — R1C-C T-Sub fixture for `sub_type_alias_where_lowers`.

**Runner / `Compiles`:** No new `test_runner.rs` arm. The gate uses the existing `Compiles` branch in `TestRunner::run_claim`, which calls `eval_compiles` → `compile_to_dag` on `TestClaim.source` (same path as `sub_match_over_user_sum` and other R1 gates). See `src/v3/compiler/src/test_runner.rs` (`run_claim` matches `"Compiles"` → `eval_compiles`).

**Manager brief:** R1 Closure Manager working-state row uses **“Proposed receipt — PR #879”** (pre-merge) instead of main-truth “green”, per matrix hygiene feedback.

**Verification (this branch):** `cargo fmt --all --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test --workspace --exclude v2-compiler-tests`; `cargo test -p v3-compiler sub_type_alias_where` (new `test_runner_runs_sub_type_alias_where_lowers_gate`); `cargo run -p v3-compiler --bin self_host_fixed_point` (DB-8 fixed-point; dev profile locally — CI uses `--release` per workflow).
