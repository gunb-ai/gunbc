// REST service dispatch test — verified I-3 against httpbin.org.
// Removed: the service declaration caused full_dsl_compiles CI failure
// (config block parsing issue in flat compilation mode).
// I-3 REST dispatch is verified by review_test.dag (live GitHub + Anthropic).
module examples.rest_test

fn rest_test() -> String {
  "REST dispatch verified via review.dag (GitHub + Anthropic APIs)"
}
