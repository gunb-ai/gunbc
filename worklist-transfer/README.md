# AmbiguousBare worklist — Wave-0 (c) homonym qualification

Transfer branch (NOT for merge). Generated by fierce-ram-489 via a direct
`reference_resolution_facts` call on a full `dag` + `src/v2` strip (main @ b3b05f01).

- `qualify_now_per_site.tsv` (1115 sites) — Capitalized domain type/variant homonyms =
  the genuine (c) qualification scope. Columns: tag(witness|compile), file, name, candidate_modules.
- `ambiguous_bare_full.tsv` (18588 sites) — everything, INCLUDING ~15675 producer over-collection
  rows (locals/params/field-names coinciding with top-level decls). IGNORE the lowercase names.

Bucket routing (coordinator-confirmed): numeric-tower (Int/Nat/Unsigned) + algebra fork
(Equal/Greater/Less: std.algebra vs v2.std.algebra) → §3 consolidation, NOT per-site qualify.
std-universal (String/List/Unit/Bool/Named/Empty/Absent/Present) → proximity usually resolves.
