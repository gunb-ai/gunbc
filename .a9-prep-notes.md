# A9 prep notes (wise-bat-550, unpushed)

Per clever-cat-115 directive 2026-05-29: hold pending #3886 ratification + #3853 witness-only merge.
DO NOT push, DO NOT open PR, DO NOT commit to session/clever-cat-115-ci-dag-m1-node.

## Planned edits to src/v4/workflow/ci.dag (line numbers vs main @ 2d2a8fc75)

### 1. Cache-digest fold — line 429
Replace:
    M1RustEmitProbeCommand => ci_symbol_digest(sym: ci_cache_cmd_m1_probe_tag)
With a content_hash over the M1 probe's real inputs. Candidate inputs per manager
brief ("content_hash over src/v4/**.dag + v2 binary"):
  - m1_probe_cargo_check_jobs (Int, line 739 area on #3853 branch; currently on main? grep)
  - digest of v2 emit binary path (symbol)
  - digest of src/v4 .dag source frontier (deferred until probe-input declaration lands)

Likely interim shape (mirrors LensCiCommand fold-of-inputs pattern at 416–423):
    M1RustEmitProbeCommand =>
      combine_hash(
        a: ci_symbol_digest(sym: ci_cache_cmd_m1_probe_tag),  // keep as discriminator
        b: scalar_int_cache_digest(n: m1_probe_cargo_check_jobs)
      )
Then drop ci_cache_cmd_m1_probe_tag entirely once content_hash(Node projection of
CiCommand) lands per T-22 dissolve-on (line 467 mark).

### 2. Delete scripts/v4-m1-rust-emit-probe.sh
Dissolve-on-arrival mark at line 108: deletable once ci_pipeline projection
emits the M1 probe step directly. #3853 lands that emission; after merge,
the .sh file has no callers in ci.yml.

Verify before deletion:
  grep -rn "v4-m1-rust-emit-probe.sh" --include="*.yml" --include="*.dag" \
    --include="*.sh" --include="*.rs" .
Expect zero hits in emitted ci.yml after #3853.

### 3. Re-emit ci.yml
    cargo run -p v4-emit -- workflow ci > .github/workflows/ci.yml
(or whatever the post-#3853 emit invocation is; confirm from workflow.dag README)

### 4. PR
Open against main once both gates clear. Title:
    ci.dag: A9 M1RustEmitProbeCommand merkle digest + drop v4-m1-rust-emit-probe.sh

## Open questions for manager when unblocking
- CONFIRMED 2026-05-29: neither `m1_probe_cargo_check_jobs` nor
  `scalar_int_cache_digest` exist on main; both arrive in #3853. A9 is
  strictly sequenced after #3853 — no useful local compile possible until then.
- Confirm whether A9 owns the deletion of ci_cache_cmd_m1_probe_tag symbol
  itself, or only the swap of its consumer in the fold.
