## Summary

Substrate-declared **diagnostic-only** enumeration order for fixed-width signed and unsigned integers (T-Ground-Diagnostic **slice 2**, Modeling problem 4 corrected). Ordering is **not** consulted for emission selection.

## Substrate

- **`src/v3/std/integer_diagnostic_order.dag`**: `signed_int_diagnostic_order` and `unsigned_int_diagnostic_order` as `List<DeclarationRef>` (narrow→wide).
- **`dsl/std/integer.dag`**: Documents parent-child modeling link + **dissolution trigger** (typed width metadata on word carriers → derive order; delete lists).

**Why `src/v3/std/` not inline in `dsl/std/integer.dag`:** `DeclarationRef` lives in `v3.spec.v3_l1`, outside the dsl-only std bootstrap seed.

## Ratchet / tests

- `v3-grounding-tests`: `integer_diagnostic_orders_are_declref_chains` asserts bootstrap lowers both lists to expected declaration-ref chains.

## Bootstrap

`regen_bootstrap`; committed `bootstrap_*_generated.rs` snapshots. Verify with `cargo run -p v3-compiler --features bootstrap-regen-fresh --bin regen_bootstrap -- --verify`.

## Bundled cleanup (not scope creep)

Removes unused `Cell` import in `grounding_cross_target_meta/src/closure_ledger_gate.rs` — same change as noop **#1434** (calm-tern); bundled so **`cargo clippy --all-targets -- -D warnings`** stays green for this PR.

## P1 receipts (`INVARIANTS.md` §94–129)

1. **DAG-ancestor:** Ordering attaches to the integer family in `dsl/std/integer.dag`; v3 file holds `DeclarationRef` lists pointing at those declarations (required because `DeclarationRef` is not in the dsl seed).
2. **Coproduct-vs-coordinate:** Signed vs unsigned lists are coordinates of one diagnostic-ordering concern (two `data` rows), not unrelated coproduct alternatives.
3. **Primitive-vs-lens-extensible:** Closed substrate primitive enumeration for fixed-width diagnostics; not lens-extensible vocabulary.

## Dissolution trigger

When Substrate Manager lands queryable width on `dsl/std/bit.dag` word carriers (e.g. `width_bits`), **derive** enumeration from carriers and **remove** these authored lists.

## Named deferrals (locked scope)

- **String-ownership diagnostic enumeration order:** deferred — no substrate home yet; cross-coord with Substrate (**#1130**).
- **`EmissionDiagnostic::UnderRefined` payload extension:** deferred until first Coercion-Fold consumer needs richer typed candidates.

## Discipline

- No `src/v3/compiler/` hand-authored logic edits (SG-0); only generated bootstrap snapshots from regen.
