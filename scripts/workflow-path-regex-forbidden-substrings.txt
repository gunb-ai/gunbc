# Gate #103 (`ci_uses_affected_set_selection`) — substrings that must not appear in
# `.github/workflows/*.{yml,yaml}` after YAML path-regex / docs-only bridges were removed.
#
# Single authority: consumed by `scripts/check-workflow-path-regex-inventory.sh` and by
# `workflow_no_path_regex_policy_ci_yml` in `t_ci_workflow_as_data_demo_test.rs`.
# Extend here only; keep the ratchet and the integration test aligned.

git diff --name-only
needs.changes.outputs
grep -vE '^(docs/.*|[^/]+\.md)$'
