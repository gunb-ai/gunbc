SG-0 hand-path delta: +1

SG-0 pairing: (c) Net add is one `EXPECTED_HAND_AUTHORED_TEST` row for the v4 coverage lens ratchet (`v4_lens_coverage_dag_smoke_test.rs`) plus the matching `INVARIANTS.md` §P5(b) receipt row. Dispatch posture: retire this host check when the v4 coverage lens runs as `.dag` / generated coverage per ROADMAP T-PB-B (`pb_rust_tests_outside_residual_zero`); dispatch evidence: gunbc#3318 (this PR).
