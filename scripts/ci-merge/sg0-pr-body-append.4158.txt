SG-0 hand-path delta: +1

SG-0 pairing: (c) Net +1 on `EXPECTED_HAND_AUTHORED_TEST` for `v4_extdeps_typecheckers_dag_smoke_test.rs` (INVARIANTS §P5(b) PY-L1-STATIC-STRUCTURAL `compile_to_dag` smoke for the per-tool static-analysis profiles `src/v4/extdeps/typecheckers/pyright.dag` + `mypy.dag` and the shared `TargetStaticAnalysisInvocation` / `TargetStaticAnalysisVerdict` carriers in `src/v4/std/leaf_model_verification.dag`; Modeling DFS Arbiter ruling proud-fox-405 msg_41813c03; pairs with the on-demand F1 host receipt `scripts/v4-leaf-model-python-l1-static-verify.sh`). Follow-up dispatch: retire when the typechecker profiles + static-analysis carriers are exercised only by `.dag` `TestClaim` rows / a generated harness without this hand-Rust `compile_to_dag` probe — gunb-ai/gunbc#4158 / T-PB-B `pb_rust_tests_outside_residual_zero` in `_internal/ROADMAP_OPS.md`.
