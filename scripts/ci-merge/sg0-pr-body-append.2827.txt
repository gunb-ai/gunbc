SG-0 hand-path delta: +1
SG-0 pairing: (c) follow-up dispatch via gunbc#2827

P5 / Dispatch-Discipline Mechanisms **(b)** — **one** checkable receipt for this PR’s `src/v3/` hand-Rust surface (Codex #10761 / INVARIANTS.md §P5):

1. **`src/v3/compiler/tests/integration/t_gate_58_apply_lens_self_application_test.rs`** (+1 `EXPECTED_HAND_AUTHORED_TEST` row; paired above with **(c)** + `gunbc#2827`). **ROADMAP:** `ROADMAP.md` → `### Nine lanes` → **T-PB-B** / `pb_rust_tests_outside_residual_zero`. **Plan:** `docs/r3-program-plan.md` §1.8 gate **#58** `apply_lens_self_application_demonstrated` — lane **T-Lens-Self-Application**. **Dissolution:** port the bootstrap diagnostic receipt to a `.dag` `TestClaim` / generated harness once PB-Runtime/TestClaim coverage subsumes `generated_full_bootstrap_dag()` empty-diagnostics checks for this witness.

2. **`src/v3/compiler/src/enforced_lens_application.rs`** (expanded timing `EnforcedApplication` consumer; reads lowered `TimingMeasurement` from `gate_58_modeled_ci_timing_measurement` / `gate_58_timing_enforcement_section` in `t_ci_workflow_as_data_demo.dag`, aligned with `timing_enforcement_project` / `timing_lens.dag`). **Plan:** same gate **#58** / **T-Lens-Self-Application** row as (1) in `docs/r3-program-plan.md` §1.8. **Dissolution:** fold enforcement into the same generated lens-fold path as **T-Lens-Application-Surface** gate **#92** `complexity_violation_compile_error_demonstrated` (`complexity_lens_generated` parity) so infer does not own a parallel timing projection.

3. **`src/v3/compiler/build.rs`** (staged `STAGED_FILES` sort tie-break: `timing_lens.dag` before `t_ci_workflow_as_data_demo.dag` so PB-1 lowering resolves `timing_enforceable` before the gate #58 row). **ROADMAP:** `ROADMAP.md` → `### Goals (the six non-negotiables)` → **6. Self-hosting (Pure Bootstrap)** / staged-bootstrap ordering (same authority class as `regen_bootstrap --verify`). **Dissolution:** replace the filename-pair comparator with declared topo order in the producer that emits `STAGED_FILES` once structural dependency order is modeled.
