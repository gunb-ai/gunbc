SG-0 pairing: (b) https://github.com/gunb-ai/gunbc/issues/1956
P5 receipt: Net hand-path add pairs `src/v3/compiler/tests/integration/t_ci_workflow_as_data_demo_test.rs` (T-PB-B census) with Director budget URL on the pairing line; dissolution trigger is `.dag` `TestClaim` migration — checkable hooks: integration module `include_str!(docs/briefs/r3-substrate-t-workflow-as-data-slice-1-worker.md)` + `sg0_census_test.rs` R1C-E dissolution notes. Lane / program anchor: `ROADMAP.md` subsection **Forward-Tracked Lane: T-Workflow-As-Data** (workflow-as-data carriers + timing-lens direction).
Dispatch-Discipline / §0 alignment (scaffold debt): PB-1 evaluator bridge `try_dispatch_std_list_is_empty` in `src/v3/compiler/src/lib.rs` (`list.dag` span suffix + `is_empty` name) remains an identity scaffold until UserDefined lowering for list helpers lands on embedded bootstrap; demo spine gate no longer uses a name-keyed `_ci_wad_nonlinear_timing_stub` escape hatch — `t_ci_workflow_as_data_demo.dag` closes through declared `is_empty(behavior_spine(d))` only.
