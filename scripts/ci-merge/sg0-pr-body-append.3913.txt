SG-0 hand-path delta: +1

SG-0 pairing: (c) Net +1 on `EXPECTED_HAND_AUTHORED_TEST` for `emit_verification_gates_test.rs` (INVARIANTS §P5(b) receipt in `_internal/INVARIANTS_OPS.md`; multi-target E-5 `post_emit_verifier` on `PROGRAM_FIXTURES` + non-ignored R1C-E `ExecuteCommand` claim `program_fixtures_post_emit_clean_all_targets`). Dissolution: `.dag` TestClaim rows own the obligation without hand-Rust — gunb-ai/gunbc#3913.
