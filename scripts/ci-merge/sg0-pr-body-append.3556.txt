SG-0 hand-path delta: +1

SG-0 pairing: (c) T-21/T-24 Wave-0 — net +1 on `EXPECTED_HAND_AUTHORED_TEST` for `v4_workflow_ci_runner_dag_smoke_test.rs` (INVARIANTS §P5(b) parse smoke on `src/v4/workflow/ci_runner.dag` + `affected_set_ci_runner.dag` structural receipts). Dissolves when Node-level TestClaim CI selection runs via `.dag` harness / host runner without this probe per ROADMAP T-PB-B / `pb_rust_tests_outside_residual_zero`; PR #3556.
