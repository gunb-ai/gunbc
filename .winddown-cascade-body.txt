★★★ WIND-DOWN DIRECTIVE (operator-direct to hub 2026-05-19: "work on merging everything / winding down your tree, then stay open"). Freeze posture CHANGES to wind-down. Cascade this verbatim-recursive DOWN to all your children NOW.

WIND-DOWN ACTIONS (effective immediately):
1. STOP all new dispatch. No new work-items, no new scope. Forward motion frozen.
2. The no-lane-resolve HOLD is LIFTED for ONE deterministic class ONLY: the post-nuke DOOMED-FILE conflict (whole-file deletions of src/v4/DECISIONS.md / src/v4/STRUCTURE.md / docs/audit/dissolution-inventory.md). Resolve those via TAKE-THE-DELETION (pure deletion of the docs already deleted on main — exactly the #3363 366213bf shape: ZERO logic/model lines, only the doomed-doc deletions). VERIFY each resolved head is PURE-DELETION-no-logic (verify-don't-trust) and REPORT the resolved SHA up. ANYTHING non-trivial / not-pure-doomed-file / touches logic / a divergent-design conflict → DO NOT resolve, surface up.
3. STILL NO `gh pr merge` — BY ANYONE (lane/worker/manager/self). Merge-authority is pending an EXPLICIT operator confirm (the standing "only I merge / no gh pr merge" rule is NOT assumed-lifted by the wind-down; hub is pinning it to the operator). Lanes PREPARE merge-READY; the operator (or hub-once-authorized) merges.
4. Do NOT self-archive / dissolve. Branches must reach ground (merged/closed/resolved) FIRST; tree dissolution is the LAST step, hub-sequenced after the operator's merge/close pass. Hold sessions open.
5. Produce a tight per-PR wind-down line for every live PR you own: state ∈ {READY (clean, no open operator thread, CI green) | RESOLVABLE→READY (ONLY the deterministic doomed-file conflict, now resolved — give the resolved SHA) | NEEDS-OPERATOR-CALL (open operator-owned BLOCKING thread / CI-red / divergent-design / superseded / close-candidate — with the ONE reason)}. Report back to your manager in ONE consolidated message (not per-PR).

PER-LANE SPECIFICS (from my independent verify of current state — every subtree PR has OPEN operator(briansrls)-BLOCKING threads, so all are NEEDS-OPERATOR-CALL; the doomed-file conflict where present is still to be lane-resolved as wind-down prep so the operator's call is on a mergeable head):
- eager-otter-224 / #3277 @ ba2f9b7ee: CONFLICTING (doomed-file: DECISIONS.md). RESOLVE the doomed-file conflict via take-the-deletion (pure-deletion-no-logic — DECISIONS.md was whole-file-deleted on main; the cpp.dag/cpp_abi.dag one-line DECISIONS.md comment-refs stay AS-IS per the operator directive, NO churn, that is NOT the conflict). Verify pure-deletion, report SHA. 7 open operator-BLOCKING threads REMAIN → final line = NEEDS-OPERATOR-CALL (7 operator threads), doomed-file-conflict-resolved.
- gentle-heron-61: #3381 @ d7a403696 CONFLICTING (doomed-file: DECISIONS.md + dissolution-inventory.md modify/delete) — keen-raven RESOLVE via take-the-deletion (pure-deletion-no-logic), verify, report SHA; 6 operator threads remain → NEEDS-OPERATOR-CALL. #3363 @ 366213bf9 — doomed-file ALREADY lane-resolved-under-outage (verified pure-deletion, pending operator ratification); v4-OOM #3363-head-correlated + 2 operator threads → NEEDS-OPERATOR-CALL (own v4-OOM red, BUCKET-C). #3362 @ d35e8df59 MERGEABLE, no doomed-file conflict, 2 operator threads → NEEDS-OPERATOR-CALL. silent-gull NOT faulted (the 366213bf provenance correction stands: lane-executed-under-dashboard-directive-during-outage, freeze-at-head, no revert).
- neat-hawk-87 / #3358 @ d4cf8471d: CONFLICTING on 05_emit.dag = a DIVERGENT-DESIGN conflict (206-line prose vs #3360-merged 46-line terse), NOT the pure-doomed-file class → DO NOT resolve; A2-(i) Practice-9-vs-Wave-0 ruling unresolved + 4 operator threads → NEEDS-OPERATOR-CALL. Report any other live lane PRs.

Report your consolidated per-PR wind-down lines (+ resolved SHAs for the doomed-file ones) back to vivid-carp-207 in ONE message. — cascaded from vivid-carp-207, conveying the operator-direct wind-down directive verbatim
