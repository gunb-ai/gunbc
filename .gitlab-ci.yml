# Generated by gunbc-codegen
# DO NOT EDIT - regenerate with: cargo run -p gunbc-codegen -- cigen

image: rust:latest

variables:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

stages:
  - ci

cache:
  key: cargo-${CI_COMMIT_REF_SLUG}
  paths:
    - .cargo/
    - target/

ci:
  stage: ci
  script:
    - cargo run -p gunbc-dag --bin gunbc-ci --release
