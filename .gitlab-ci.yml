# Generated by gunbc-codegen cigen. Do not edit manually.
# Regenerate with: cargo run -p gunbc-codegen -- cigen

image: rust:latest

variables:
  CARGO_TERM_COLOR: always

stages:
  - build
  - run

cache:
  key: cargo-${CI_COMMIT_REF_SLUG}
  paths:
    - .cargo/
    - target/

build:
  stage: build
  script:
    - cargo build --release -p gunbc-ci
  artifacts:
    paths:
      - target/release/gunbc_ci
    expire_in: 1 hour

ci:
  stage: run
  needs:
    - build
  script:
    - ./target/release/gunbc_ci
