// §4.B — AFTER state (paired with case_b_before.dag via shared attribution label).

module user.affected_set_case_b_signature

fn caller_alpha() -> Int =
  add(1, 2, 3)

fn add(x: Int, y: Int, z: Int) -> Int =
  x + y + z
