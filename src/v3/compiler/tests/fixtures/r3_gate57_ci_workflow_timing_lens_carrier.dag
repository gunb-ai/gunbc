// R3 gate #57 — merges `dsl/gunbc/ci.dag` with `v3.std.t_ci_workflow_as_data_demo` so
// `evaluate_body(demo_ci_modeled_timing_dimension_report, …)` shares one lowered artifact with
// the gunbc CI workflow carrier (`ci_workflow_dag` anchor — same authority as `compile_to_dag`
// on `dsl/gunbc/ci.dag` alone).
module user.r3_gate57_ci_workflow_timing_lens_carrier

import gunbc.ci { CIWorkflowDag, ci_workflow_dag }
import v3.std.t_ci_workflow_as_data_demo { demo_ci_modeled_timing_dimension_report }

data r3_gate57_ci_timing_carrier_anchor: CIWorkflowDag = ci_workflow_dag
