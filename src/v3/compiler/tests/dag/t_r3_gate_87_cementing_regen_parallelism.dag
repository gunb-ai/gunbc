// R3 gate #87 — `LensRegistryEntry` `parallelism` partial-surface compile receipt.
// The capability register marks `parallelism.dag` PARTIAL: it emits the generated Stage 2e
// consumer, but `PairwiseNonCommute` still lacks typed left/right operation evidence in the public
// report payload. This narrow `Compiles` claim is therefore a registry cementing receipt, not a
// behavioral-completion claim.
//
// Dissolution trigger: when `WorkflowParallelismReport` / `ParallelismUnsupportedDetail` carries
// typed pairwise evidence as authorable `.dag` data, replace this compile receipt with a
// `LensOutputEquals` claim over that public report carrier and promote the register row in the same
// PR.

module v3.compiler.tests.t_r3.gate_87_cementing_regen_parallelism

import std.verification { Compiles, TestClaim, TestSuite }

data cementing_regen_parallelism_claim: TestClaim = {
  name: "cementing_regen_parallelism",
  source: "let lit: Int = 7",
  file_name: "r3_gate_87_parallelism_smoke.v3",
  predicate: Compiles,
  requires: []
}

data r3_gate_87_cementing_regen_parallelism_suite: TestSuite = {
  name: "r3-gate-87/cementing/regen/parallelism",
  claims: [Enumerated(cementing_regen_parallelism_claim)]
}
