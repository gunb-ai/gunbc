[package]
name = "v3-grounding-cross-target-meta"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# T-Ground-CrossTarget-Meta — L6 cross-product walker scaffold. Sibling crate
# to grounding_inhabitance_mirror / grounding_engine / grounding_lifetime / grounding_coercion_fold;
# does not touch `src/v3/compiler/` SG-0. Per the lane brief at
# `docs/briefs/t-ground-cross-target-meta.md` (#1224 + L6 form-axis anchor #1229)
# and manager dispatch (#1133 inbox 4353412409): walks
# `(TypeConnective × Behavior × Shape A target)` cells and reports
# fail-closed `MissingEmissionPath` diagnostics for cells with no
# LanguageSpec coverage. Runtime check; NOT a `Lens<C>` instance.

[dependencies]
v3-compiler = { path = "../compiler" }
