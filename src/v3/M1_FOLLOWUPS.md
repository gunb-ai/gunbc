# M1(2.5) Follow-ups — moved to ROADMAP

This document's contents have been consolidated into
[`src/v3/ROADMAP.md`](./ROADMAP.md) as the single source of truth for
v3 status, active work, and deferred items.

- **M1(2.6) work in progress** (parser extensions, real `dsl/std/*.dag`
  bootstrap, SubstStack + §8.9 inhabitance walks, delete
  `inject_primitive_operators`) → ROADMAP §"M1(2.6) — FACTS FLOW
  FORWARD + SINGLE AUTHORITY"
- **Deferred M1(3)+** (cost lens, ownership lens, emitter, §8.11
  ratchet, `ArrowBody::Pending` removal) → ROADMAP §"M1(3)+ — deferred
  work"
- **M2+ deferred** (generics in user code, match/pipe/lambda, `data`
  semantics, `where` refinement, `TypeShape` → `DeclarationId`) →
  ROADMAP §"M2 — Feature parity with v2 subset"

See also:
- `src/v3/M1_DESIGN.md` — historical M1(2.5)–M1(3) design oracle
- `src/v3/M0_RETROSPECTIVE.md` — M0 retrospective + M1(2.5) addendum
