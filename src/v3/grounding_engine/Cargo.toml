[package]
name = "v3-grounding-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# T-Ground-Engine: Phase 1 type-structure validation + Phase 2 enumeration of
# `rust_grounding_primitives` list bodies via v3-compiler's Dag accessor.

[dependencies]
v3-compiler = { path = "../compiler" }
v3-grounding-inhabitance-mirror = { path = "../grounding_inhabitance_mirror" }
