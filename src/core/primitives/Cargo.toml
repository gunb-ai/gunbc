[package]
name = "gunbc-primitives"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Fundamental DAG primitives - the leaf operations for all compositions"

[dependencies]
gunbc-infra = { path = "../infra" }
gunbc-ir = { path = "../ir" }
gunbc-exec = { path = "../exec" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
