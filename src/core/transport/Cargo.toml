[package]
name = "gunbc-lib-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Transport operations and executors for gunbc I/O"

[dependencies]
gunbc-ir = { path = "../ir" }
gunbc-exec = { path = "../exec" }
serde_json = { workspace = true }
glob = { workspace = true }
ureq = { workspace = true }

[dev-dependencies]
gunbc-test = { path = "../test" }
