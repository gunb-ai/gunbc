// src/v4/compiler/03_normalize.dag
//
// Scope: pipeline stage 3a — ParseTree to NormalizedTree. Surface sugar dissolution.
// Anchor: https://en.wikipedia.org/wiki/Abstract_syntax_tree
//
// C3 (operator-ratified 2026-05-15): normalize dissolves EXACTLY the 4
// bounded surface-sugar forms THESIS:198-203 names — `service`, `fn`,
// `type`, `operation` — into Node trees. It is NOT identity and NOT
// open-ended. There is no 5th sugar form; a new one is a substrate
// extension = STOP, not an inline add. (This closes the "what does
// normalize do in a no-sugar language" tension — there IS bounded sugar,
// exactly these 4; cascade-closure of A1.)
//
// Owns:
//   - NormalizedTree type
//   - normalize: ParseTree -> Result<NormalizedTree, Diagnostic>
//   - dissolves the 4 sugar forms { service, fn, type, operation } -> Node
//     tree (per MODELING.md composition layer); exactly 4, closed
//
// Consumes:
//   - std/node.dag, std/diagnostic.dag
//   - compiler/02_parse.dag: ParseTree
//
// Status: scaffold — fill per TASKS.md T-8
// Brief: see BRIEF_TEMPLATE.md; this file's I/O contract is the immutable
// contract for the worker. Splitting this file requires explicit operator
// ratification (substrate extension = stop signal).


module v4.compiler.normalize


import v4.compiler.parse { ParseTree }
import v4.std.diagnostic { Outcome }
import v4.std.node { Node, Symbol }


// The exact four surface forms ratified by C3. A fifth form is a
// substrate-extension stop signal, not an inline parser addition.
type SurfaceForm
  = ServiceForm
  | FunctionForm
  | TypeForm
  | OperationForm


// A normalization rule maps one bounded surface form to its kernel Node
// construction pattern. The rule is data; normalize is the fold over it.
type NormalizationRule {
  form: SurfaceForm
  pattern: Node
}


type NormalizedTree {
  root: Node
  applied_rules: List<Symbol>
}


type Normalize = fn(ParseTree, List<NormalizationRule>) -> Outcome<NormalizedTree>
