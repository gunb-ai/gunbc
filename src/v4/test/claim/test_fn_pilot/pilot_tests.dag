// src/v4/test/claim/test_fn_pilot/pilot_tests.dag
// Pilot for single-representation tests: the function IS the test. Declared with
// the `test fn` keyword — no `unified_claim_*` registration and no hardcoded entry
// path. The CI claim gate discovers it by the `test fn` source marker and runs it.

module v4.test.claim.test_fn_pilot.pilot_tests

import v4.std.logic { Bool }

test fn test_fn_pilot_holds() -> Bool {
  true
}
