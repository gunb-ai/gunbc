// src/v4/test/v2_run_preflight/probes.dag
//
// Scope: reserved module for future v2 `dag run` probes against src/v4.
//        Move 1 (2026-05-16) showed the frozen v2 *interpreter* is not yet a
//        reliable execution engine for the merged v4 substrate graph (see
//        MOVE1_COVERAGE.txt). Probes that called cross-module `fn` values
//        failed at runtime (`nat_add` undefined); `node_well_formed` + host
//        `ProcessExit` paths hung or hit `int_add` undefined — so no runnable
//        entry points are committed here until T-22 (v4 eval) dissolves the gap.
// Anchor: parent brief Move 1 + DECISIONS.md T-20

module v4.test.v2_run_preflight.probes


data v2_run_preflight_probe_marker: Int = 0
