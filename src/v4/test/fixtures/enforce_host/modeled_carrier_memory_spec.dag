module v4.test.fixtures.enforce_host.modeled_carrier_memory_spec

// Self-contained modeled carrier (no dsl/std.measure import): compiles under
// ci_floor_emit's src/v4-only closure while still discriminating ByteSize vs Int.
type ByteSize

type MemorySpec {
  ram_bytes: ByteSize
}
