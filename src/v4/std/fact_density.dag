// src/v4/std/fact_density.dag
//
// Scope: T-30 — structural hollow-alias gate substrate (TASKS.md T-30;
//        DECISIONS.md D2-REV / fact-bundle modeling).
// Anchor: docs/modeling-discipline.md **Practice 8** — *Fact-bundle modeling*
//        + *Interim floor: the hollow-alias discriminator* (authoritative
//        text on PR **#3226** until merged; **sync** if Practice 8 shifts
//        during review). Companion: docs/modeling/grounding-worked-examples.md
//        (carrier + meaning as `Node`). INVARIANTS P1 / P2.
//
// Owns:
//   - SourceSpecReadFact — nominal witness that a declaration **anchors** a
//     spec read (opaque `Symbol` read-site identity per `std/node.dag` K-1 —
//     not arithmetic, not an unconstrained product of independent fields).
//     **Intended richer witness:** `Node` subtree (carrier + meaning on the
//     same object). Standalone `compile_to_dag` still rejects `body: Node` in
//     this module’s record fields (`unresolved type identifier Node`); migrate
//     when the resolver accepts `Node` in this position.
//
// Consumes:
//   - std/node.dag: Symbol (K-1 opaque identity — same import pattern as
//     `std/integer.dag` angle-parameter carriers).
//
// Implementation split (M1(2.8)):
//   The mechanical **declaration-tree → Outcome** check is mirrored in Rust
//   as `v3_compiler::v4_hollow_alias_gate` (hermetic unit tests there),
//   projecting Practice 8’s three-part discriminator until the gate ships in
//   generated `.dag` form (T-30 acceptance).
//
// Status: T-30 modeled (warm-dove-632).


module v4.std.fact_density


import v4.std.node { Symbol }


type SourceSpecReadFact {
  read_site: Symbol
}
