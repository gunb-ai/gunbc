// src/v4/std/fact_density.dag
//
// Scope: T-30 — structural fact-density / hollow-alias gate (operator 2026-05-17,
//        DECISIONS.md D2 reversal, TASKS.md T-30).
// Anchor: docs/modeling/grounding-worked-examples.md + docs/modeling-discipline.md
//        + INVARIANTS P1.
//
// Owns:
//   - SourceSpecReadFact — nominal witness carrier (see implementation split).
//
// Consumes:
//   - std/nat.dag: Nat (nominal staging carrier — see NOTE below).
//
// Implementation split (M1(2.8) / standalone `compile_to_dag`):
//   The mechanical **Node → Outcome** gate is mirrored in Rust as
//   `v3_compiler::v4_hollow_alias_gate` (hermetic unit tests there).
//
// NOTE — nominal staging (resolver edge, not semantic content):
//   The intended witness is `SourceSpecRead { body: Node }` (grounding-worked-
//   examples: carrier + **meaning** as `Node`). Importing `v4.std.node` and
//   naming `Node` in this module's top-level type position currently surfaces
//   `unresolved type identifier Node` under standalone `compile_to_dag` (same
//   staging class as other v4 sum-payload edges). Until that path lands, this
//   file carries a **distinct nominal alias** to `Nat` so the module stays a
//   first-class `v4.std.*` citizen in STRUCTURE.md + CI smoke. **Do not** read
//   arithmetic meaning into `Nat` here — it is only a nominal marker; migrate
//   to the `Node` payload sum when lowering accepts it.
//
// Status: T-30 modeled (warm-dove-632).


module v4.std.fact_density


import v4.std.nat { Nat }


type SourceSpecReadFact = Nat
