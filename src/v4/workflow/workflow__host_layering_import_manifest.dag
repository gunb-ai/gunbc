// COMPILE STUB — empty facts for M1 / full-v4 typecheck only.
// Runtime by-execution authority: target/v4-layering-imports-manifest.dag
// (generated by layering_imports_scan; never commit the target artifact).
// Consumers MUST pass --source-root "$(dirname "$manifest")" AFTER --source-root src/v4
// so build_module_index overlays this stub with the generated module (same module name).

module v4.test.workflow.host_layering_import_manifest

import v4.std.collection { List }
import v4.lens.layering_imports { LayerImportFact }

data host_layer_import_facts: List<LayerImportFact> = []
