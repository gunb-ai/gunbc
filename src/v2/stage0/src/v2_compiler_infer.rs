// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.infer

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}
pub use crate::std_types::{SourceSpan};
pub use crate::v2_std_core::{Node, module_node, module_imports, module_items, is_import_node, import_is_all, import_specific_names, make_param_node, param_node_name, param_node_type_expr, field_node_type_expr, Connective, ExprData, make_expr_node, make_named_expr_node, make_expr_error_node, expr_var_name, field_access_base, field_access_field, expr_call_func, expr_method_name, let_binding_name, foreach_variable, lambda_param_names, record_lit_type_name, make_arg_node, make_arm_node, arm_pattern, arm_guard, arm_body, make_field_init_node, field_init_node_name, field_init_node_value, make_text_part_node, make_interp_part_node, map_children, arg_value, arg_name, rt_node, has_inferred, is_compiler_error, InferredNode, NodeType, Cardinality, ErrorNode, make_error_node, is_error_diagnostic, resource_use_name, resource_use_resource, kernel_type_set, is_kernel_type, expr_has_self_call, expr_has_non_tail_self_call, DeclaredFuncSig, DeclaredFuncEnv, LiteralValue, FieldAccessStyle, FieldValueShape, FieldSummary, VarBindingKind, CallSemantics, MethodSemantics, LambdaSemantics, ExprErrorKind, BinOp, UnaryOpKind, unaryop_operand, MatchPattern, StringPart, make_field_binding_node, field_binding_name, field_binding_pattern, let_value, let_body, lambda_body, foreach_collection, foreach_body, method_receiver, method_arg_nodes, match_scrutinee, match_arm_nodes, if_condition, if_then_branch, if_else_branch, index_base, index_expr, slice_base, slice_start, slice_end, cast_target, cast_expr, return_value, binop_left, binop_right, make_transport_node, local_transport_node, with_optional_cardinality, with_required_cardinality, no_span, make_span, NewlineIndex, unit_type, bool_type, string_type, int_type, float_type, none_type, error_type, is_container_type, container_expected_arity, CompilerDiagnostic};
use crate::v2_std_core::Connective::{Conj, Disj, NoConnective};
use crate::v2_std_core::ExprData::{NoExprData, ExprLiteral, ExprError, ExprVar, ExprFieldAccess, ExprCall, ExprMethodCall, ExprMatch, ExprIf, ExprLet, ExprRecordLit, ExprListLit, ExprBinOp, ExprUnaryOp, ExprLambda, ExprStringInterp, ExprBlock, ExprCast, ExprForEach, ExprIndex, ExprSlice, ExprReturn};
use crate::v2_std_core::InferredNode::{Resolved, CompilerError, TypeVariable};
use crate::v2_std_core::NodeType::{Typed, InferError, InferVariable, Untyped};
use crate::v2_std_core::Cardinality::{Required, CardOptional};
use crate::v2_std_core::CompilerDiagnostic::{InternalError, VariantCollision};
use crate::v2_std_core::LiteralValue::{LitStr, LitInt, LitFloat, LitBool, LitNull};
use crate::v2_std_core::FieldAccessStyle::{StoredField, EnumAccessor, OptionalUnwrap, TupleFirst, TupleSecond};
use crate::v2_std_core::FieldValueShape::{PlainValue, OptionalValue};
use crate::v2_std_core::VarBindingKind::{LocalValueBinding, FunctionValueBinding, VariantValueBinding, MatchBoundBinding};
use crate::v2_std_core::CallSemantics::{PlainCallSemantics, LookupCallSemantics};
use crate::v2_std_core::MethodSemantics::{PlainMethodSemantics, AlgebraMethodSemantics, ServiceMethodSemantics};
use crate::v2_std_core::ExprErrorKind::{ParseRecoveryError, SemanticExprError, InternalExprError};
use crate::v2_std_core::BinOp::{Add, Sub, Mul, Div, Mod, Eq, Ne, Lt, Gt, Le, Ge, And, Or, NullCoalesce};
use crate::v2_std_core::UnaryOpKind::{Not, Neg};
use crate::v2_std_core::MatchPattern::{Bind, VariantPattern, Wildcard};
use crate::v2_std_core::StringPart::{Text, Interpolation};
pub use crate::v2_compiler_resolve::{ModuleGraph, ResolvedModule, ResolvedImport};
pub use crate::v2_compiler_infer_types::{child_inferred_or_name, nominal_type_ref, container_node, callable_node, node_is_keyed_collection, node_is_element_collection, node_is_collection, is_fully_resolved, map_node, bare_map_node, callable_inferred, normalize_access_type_node, bridge_placeholder_type_names, is_bridge_placeholder, node_type_shape, node_type_compatible, node_type_equals, prefer_specific_type, node_type_deps, method_receiver_element_node, infer_literal_node, infer_binop_type_node, extract_optional_inner_node, for_each_element_type_node, rt_type, emit_map_has, enrich_kernel_type};
pub use crate::v2_compiler_infer_method::{infer_builtin_call_type, resolve_builtin_call_type, list_of_element};
pub use crate::v2_compiler_infer_cycle::{detect_type_cycles_kahn};
pub use crate::v2_compiler_infer_env::{TypeEnv, TypeBinding, is_recursive_type, lookup_type, lookup_type_for, merge_envs, RecursiveVariantFieldWitness, put_recursive_variant_field_witness, merge_recursive_variant_fields};
pub use crate::v2_compiler_infer_resolve::{resolve_node, resolve_item_types, NodeResolveResult, ItemResult};
pub use crate::v2_compiler_infer_sigs::{ResolvedFuncSig, ResolvedFuncEnv, ResolveFuncSigsResult, resolve_func_sigs};
pub use crate::v2_compiler_infer_emit_info::{TypeRepr, TypeSummary, EmitGraphInfo, EmitInfoBuildState, empty_emit_graph_info, lookup_emit_type_summary, build_struct_field_summaries, build_enum_field_summaries, add_emit_item_summary, derive_variant_to_enum};
use crate::v2_compiler_infer_emit_info::TypeRepr::{StructRepr, EnumRepr};
pub use crate::v2_compiler_infer_items::{ItemKind, ItemInfo, TypedModule, TypedGraph, ResolvedGraph, inferred_to_outputs, item_kind, variant_locals_from_items};
use crate::v2_compiler_infer_items::ItemKind::{FnItem, FuncItem, TypeItem, DataItem, ServiceItem, OtherItem};
pub use crate::v2_compiler_infer_service::{UniqueAccum, OpEntry, ServiceMethodResult, is_typed_service_call_receiver, extract_typed_service_name, collect_typed_service_calls, collect_called_func_names, expand_transitive_services, check_service_field_access_node, check_service_method_call_node, service_op_entry};
pub use crate::v2_compiler_infer_patterns::{NodeLookupResult, PatternSubject, lookup_variant_in_type, lookup_field_in_variant, pattern_subject_from_node_type, pattern_subject_from_node, lookup_result_subject, pattern_binding_type, check_match_exhaustiveness};
use crate::v2_compiler_infer_patterns::PatternSubject::*;
pub use crate::v2_compiler_infer_lookup::{KnownMethodResolution, resolve_known_method_node, resolve_scrutinee_type_node, field_summary_for_type, lookup_field_type_node, lookup_coproduct_common_field_node, map_value_type_in_env, map_key_type_in_env, lookup_in_scope, lookup_func_sig};
pub use crate::v2_compiler_infer_access::{AccessCheckResultNode, check_index_access_node, check_slice_access_node};

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ItemContribution {
    pub resolved_item: Rc<Node>,
    pub resolve_diagnostics: Rc<Vec<Rc<ErrorNode>>>,
    pub func_sig: Option<Rc<DeclaredFuncSig>>,
    pub svc_entries: Rc<Vec<Rc<OpEntry>>>,
    pub svc_local: Option<Rc<TypeBinding>>,
    pub item_info: Rc<ItemInfo>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ModuleContext {
    pub resolved_items: Rc<Vec<Rc<Node>>>,
    pub func_env: Rc<ResolvedFuncEnv>,
    pub svc_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>,
    pub locals: Rc<HashMap<String, Rc<TypeBinding>>>,
    pub item_registry: Rc<HashMap<String, Rc<ItemInfo>>>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VariantFoldState {
    pub locals: Rc<HashMap<String, Rc<TypeBinding>>>,
    pub collision_errors: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct InferScope {
    pub type_env: Rc<TypeEnv>,
    pub func_env: Rc<ResolvedFuncEnv>,
    pub locals: Rc<HashMap<String, Rc<TypeBinding>>>,
    pub match_bound_names: Rc<HashMap<String, bool>>,
    pub module_name: String,
    pub service_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>,
    pub item_registry: Rc<HashMap<String, Rc<ItemInfo>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct InferResult {
    pub typed: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BlockInferState {
    pub scope: Rc<InferScope>,
    pub diag_chunks: Rc<Vec<Rc<Vec<Rc<ErrorNode>>>>>,
    pub last_type: Rc<Node>,
    pub typed_stmts: Rc<Vec<Rc<Node>>>,
}

pub fn infer_block_stmts(mut remaining: Rc<Vec<Rc<Node>>>, mut remaining_count: i64, mut scope: Rc<InferScope>, mut typed_stmts: Rc<Vec<Rc<Node>>>, mut diag_chunks: Rc<Vec<Rc<Vec<Rc<ErrorNode>>>>>, mut last_type: Rc<Node>, mut expected: Option<Rc<Node>>) -> Rc<BlockInferState> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(BlockInferState {
    scope: scope.clone(),
    diag_chunks: diag_chunks.clone(),
    last_type: last_type.clone(),
    typed_stmts: typed_stmts.clone(),
}); },
    Some(stmt) => { let is_last = (remaining_count.clone() == 1);
let stmt_expected = if is_last.clone() {
            expected.clone()
} else {
            None
};
let stmt_result = infer_expr(stmt.clone(), scope.clone(), stmt_expected.clone());
let stmt_typed = stmt_result.typed.clone();
let stmt_diags = stmt_result.diagnostics.clone();
let stmt_rt = rt_type(stmt_typed.clone());
let next_scope = scope_after_stmt_node(stmt.clone(), stmt_rt.clone(), scope.clone());
{
            let __tco_0 = Rc::new(remaining.iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let __tco_1 = (remaining_count - 1);
let __tco_2 = next_scope.clone();
let __tco_3 = v2_rt::rc_list_push(typed_stmts, stmt_typed.clone());
let __tco_4 = v2_rt::rc_list_push(diag_chunks, stmt_diags.clone());
let __tco_5 = stmt_rt.clone();
let __tco_6 = expected;
remaining = __tco_0;
remaining_count = __tco_1;
scope = __tco_2;
typed_stmts = __tco_3;
diag_chunks = __tco_4;
last_type = __tco_5;
expected = __tco_6;
continue;
} },
}
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypedItemResult {
    pub item: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArmInferResult {
    pub typed_arm: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
    pub body_type: Rc<Node>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PatternScopeResult {
    pub scope: Rc<InferScope>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StringPartInferResult {
    pub typed_part: Rc<StringPart>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArgInferResult {
    pub typed_arg: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldInferResult {
    pub typed_field: Rc<Node>,
    pub infer_result: Rc<InferResult>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BuildTypeEnvResult {
    pub env: Rc<TypeEnv>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParentModulesResult {
    pub modules: Rc<Vec<Rc<TypedModule>>>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VariantResult {
    pub variant: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CycleDetectState {
    pub recursive_names: Rc<HashMap<String, bool>>,
    pub global_visited: Rc<HashMap<String, bool>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct InferScopeComponents {
    pub func_sigs: Rc<HashMap<String, Rc<DeclaredFuncSig>>>,
    pub svc_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>,
    pub svc_locals: Rc<HashMap<String, Rc<TypeBinding>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct LocalContributionState {
    pub resolved_items: Rc<Vec<Rc<Node>>>,
    pub func_sigs: Rc<HashMap<String, Rc<DeclaredFuncSig>>>,
    pub svc_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>,
    pub svc_locals: Rc<HashMap<String, Rc<TypeBinding>>>,
    pub item_registry: Rc<HashMap<String, Rc<ItemInfo>>>,
    pub diag_chunks: Rc<Vec<Rc<Vec<Rc<ErrorNode>>>>>,
}

pub fn merge_scope_from_imports(mut remaining: Rc<Vec<Rc<ResolvedImport>>>, mut parent_index: Rc<HashMap<String, Rc<TypedModule>>>, mut env: Rc<TypeEnv>, mut func_sigs: Rc<HashMap<String, Rc<DeclaredFuncSig>>>, mut svc_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>, mut svc_locals: Rc<HashMap<String, Rc<TypeBinding>>>) -> Rc<InferScopeComponents> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(InferScopeComponents {
    func_sigs: func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
}); },
    Some(imp) => { match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => { let next_func_sigs = Rc::new(v2_rt::map_values(&typed_parent.func_env.clone().signatures.clone())).iter().cloned().fold(func_sigs.clone(), |acc: Rc<HashMap<String, Rc<DeclaredFuncSig>>>, rsig: Rc<ResolvedFuncSig>| {
            let sig_key = rsig.name.clone();
let sig_params = rsig.params.clone();
let sig_rt = rsig.inferred.clone();
let sig_async = rsig.is_async.clone();
v2_rt::rc_map_insert(acc.clone(), sig_key.clone(), Rc::new(DeclaredFuncSig {
    name: sig_key.clone(),
    params: sig_params.clone(),
    inferred: Some(sig_rt.clone()),
    is_async: sig_async.clone(),
}))
});
let parent_result = typed_parent.items.clone().iter().cloned().fold(Rc::new(InferScopeComponents {
    func_sigs: next_func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
}), |acc: Rc<InferScopeComponents>, titem: Rc<Node>| if ((titem.transport.clone() != None) && ((titem.children.clone().len() as i64) > 0)) {
            {
                let entries = Rc::new({ let mut __result = Vec::new(); for c in titem.children.clone().iter().cloned() { __result.push(Rc::new(OpEntry {
    name: c.name.clone(),
    outputs: inferred_to_outputs(c.inferred.clone(), c.span.clone()),
    params: c.params.clone(),
})); } __result });
let root = namespace_root_from_properties(titem.properties.clone(), titem.name.clone());
Rc::new(InferScopeComponents {
    func_sigs: acc.func_sigs.clone(),
    svc_registry: v2_rt::rc_map_insert(acc.svc_registry.clone(), titem.name.clone(), entries.clone()),
    svc_locals: v2_rt::rc_map_insert(acc.svc_locals.clone(), root.clone(), nominal_type_binding(root.clone())),
})
}
} else {
            if (((((titem.body.clone() != None) && ((titem.params.clone().len() as i64) == 0)) && (titem.transport.clone() == None)) && (titem.connective.clone() == Connective::NoConnective)) && (titem.inferred.clone() != None)) {
                Rc::new(InferScopeComponents {
    func_sigs: acc.func_sigs.clone(),
    svc_registry: acc.svc_registry.clone(),
    svc_locals: v2_rt::rc_map_insert(acc.svc_locals.clone(), titem.name.clone(), Rc::new(TypeBinding {
    name: titem.name.clone(),
    resolved: rt_type(titem.clone()),
})),
})
} else {
                acc.clone()
}
});
{
            let __tco_0 = Rc::new(remaining.iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let __tco_1 = parent_index;
let __tco_2 = env;
let __tco_3 = parent_result.func_sigs.clone();
let __tco_4 = parent_result.svc_registry.clone();
let __tco_5 = parent_result.svc_locals.clone();
remaining = __tco_0;
parent_index = __tco_1;
env = __tco_2;
func_sigs = __tco_3;
svc_registry = __tco_4;
svc_locals = __tco_5;
continue;
} },
    None => { {
            let __tco_0 = Rc::new(remaining.iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let __tco_1 = parent_index;
let __tco_2 = env;
let __tco_3 = func_sigs;
let __tco_4 = svc_registry;
let __tco_5 = svc_locals;
remaining = __tco_0;
parent_index = __tco_1;
env = __tco_2;
func_sigs = __tco_3;
svc_registry = __tco_4;
svc_locals = __tco_5;
continue;
} },
} },
}
}
}

pub fn nominal_leaf_type(name: String) -> Rc<Node> {
    nominal_type_ref(name)
}

pub fn nominal_type_binding(name: String) -> Rc<TypeBinding> {
    Rc::new(TypeBinding {
    name: name.clone(),
    resolved: nominal_leaf_type(name.clone()),
})
}

pub fn collect_item_recursive_variant_fields(item: Rc<Node>, recursive_type_set: Rc<HashMap<String, bool>>) -> Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>> {
    match item.connective.clone() {
    Connective::Disj => if (v2_rt::map_get(&recursive_type_set, item.name.clone()) == None) {
        v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>()
} else {
        item.children.clone().iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(), |variant_acc: Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>>, variant_node: Rc<Node>| variant_node.children.clone().iter().cloned().fold(variant_acc.clone(), |field_acc: Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>>, field_node: Rc<Node>| {
            let field_type_name = field_node_type_expr(field_node.clone()).name.clone();
if (field_type_name.clone().as_str() == item.name.clone().as_str()) {
                put_recursive_variant_field_witness(field_acc.clone(), item.name.clone(), variant_node.name.clone(), field_node.name.clone())
} else {
                field_acc.clone()
}
}))
},
    _ => v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(),
}
}

pub fn build_item_recursive_variant_fields(items: Rc<Vec<Rc<Node>>>, recursive_type_set: Rc<HashMap<String, bool>>) -> Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>> {
    items.iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(), |acc: Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>>, item: Rc<Node>| merge_recursive_variant_fields(acc.clone(), collect_item_recursive_variant_fields(item.clone(), recursive_type_set.clone())))
}

pub fn nominal_ref_node(name: String, span: Rc<SourceSpan>, ident_span: Option<Rc<SourceSpan>>) -> Rc<Node> {
    Rc::new(Node {
    name: name.clone(),
    span: span,
    ident_span: ident_span,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: nominal_leaf_type(name.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn named_collection_type(kind_name: String, element: Rc<Node>) -> Rc<Node> {
    container_node(kind_name, element)
}

pub fn resolved_callable_type(func_params: Rc<Vec<Rc<Node>>>, ret: Rc<Node>) -> Rc<Node> {
    callable_node(func_params, ret)
}

pub fn namespace_root_from_properties(properties: Rc<Vec<Rc<Node>>>, name: String) -> String {
    match Rc::new({ let mut __result = Vec::new(); for p in properties.iter().cloned() { if (field_init_node_name(p.clone()).as_str() == "namespace_root".to_string().as_str()) { __result.push(p); } } __result }).first().cloned() {
    Some(ns_prop) => match (*field_init_node_value(ns_prop.clone()).expr_data.clone()).clone() {
    ExprData::ExprLiteral { ref value, .. } => { let LiteralValue::LitStr { value: root, .. } = value.as_ref() else { unreachable!() }; root.clone() },
    _ => name,
},
    None => name,
}
}

pub fn expr_span(texpr: Rc<Node>) -> Rc<SourceSpan> {
    texpr.span.clone()
}

pub fn ok_infer(texpr: Rc<Node>) -> Rc<InferResult> {
    Rc::new(InferResult {
    typed: texpr,
    diagnostics: Rc::new(vec![]),
})
}

pub fn semantic_expr_error_node(message: String, span: Rc<SourceSpan>) -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::SemanticExprError, message, span)
}

pub fn internal_expr_error_node(message: String, span: Rc<SourceSpan>) -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::InternalExprError, message, span)
}

pub fn lookup_variant_parent_enum(scope: Rc<InferScope>, name: String) -> Option<String> {
    match v2_rt::map_get(&scope.locals.clone(), name.clone()) {
    Some(binding) => match lookup_type_for(scope.type_env.clone(), binding.resolved.clone()) {
    Some(parent) => {
        let is_coproduct = (parent.connective.clone() == Connective::Disj);
if is_coproduct {
            if { let mut __found = false; for child in parent.children.clone().iter().cloned() { if (child.name.clone().as_str() == name.clone().as_str()) { __found = true; break; } } __found } {
                Some(binding.resolved.clone().name.clone())
} else {
                None
}
} else {
            None
}
},
    None => None,
},
    None => None,
}
}

pub fn infer_var_binding_kind(scope: Rc<InferScope>, name: String) -> Rc<VarBindingKind> {
    match lookup_variant_parent_enum(scope.clone(), name.clone()) {
    Some(parent_enum) => Rc::new(VarBindingKind::VariantValueBinding {
    parent_enum: parent_enum.clone(),
}),
    None => match v2_rt::map_get(&scope.locals.clone(), name.clone()) {
    Some(_) => if v2_rt::map_has(&scope.match_bound_names.clone(), name.clone()) {
        Rc::new(VarBindingKind::MatchBoundBinding)
} else {
        Rc::new(VarBindingKind::LocalValueBinding)
},
    None => Rc::new(VarBindingKind::FunctionValueBinding),
},
}
}

pub fn inference_error(message: String, span: Rc<SourceSpan>, module_name: String) -> Rc<ErrorNode> {
    make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: message,
    span: span,
}), module_name)
}

pub fn categorized_error(message: String, span: Rc<SourceSpan>, module_name: String, category: String) -> Rc<ErrorNode> {
    make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: message,
    span: span,
}), module_name)
}

pub fn type_variable_node(id: String) -> Rc<Node> {
    Rc::new(Node {
    name: "".to_string(),
    span: make_span(0, 0),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::TypeVariable {
    id: id,
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn is_type_variable(inferred: Rc<InferredNode>) -> bool {
    match (*inferred).clone() {
    InferredNode::TypeVariable { .. } => true,
    _ => false,
}
}

pub fn inferred_from_node_type(result: Rc<NodeType>, fallback_message: String, fallback_span: Rc<SourceSpan>) -> Rc<InferredNode> {
    match (*result).clone() {
    NodeType::Typed { node: resolved, .. } => Rc::new(InferredNode::Resolved {
    node: resolved.clone(),
}),
    NodeType::InferError { message, span, .. } => Rc::new(InferredNode::CompilerError {
    message: message.clone(),
    span: span.clone(),
}),
    NodeType::InferVariable { id, .. } => Rc::new(InferredNode::TypeVariable {
    id: id.clone(),
}),
    NodeType::Untyped => Rc::new(InferredNode::CompilerError {
    message: fallback_message,
    span: fallback_span,
}),
}
}

pub fn lambda_semantics_from_param_types(param_types: Rc<Vec<Rc<Node>>>) -> Rc<LambdaSemantics> {
    Rc::new(LambdaSemantics {
    param_types: param_types,
})
}

pub fn lambda_param_types_from_scope(scope: Rc<InferScope>, params: Rc<Vec<String>>) -> Rc<Vec<Rc<Node>>> {
    Rc::new({ let mut __result = Vec::new(); for param_name in params.iter().cloned() { __result.push(match v2_rt::map_get(&scope.locals.clone(), param_name.clone()) {
    Some(binding) => binding.resolved.clone(),
    None => error_type(),
}); } __result })
}

pub fn resolve_pattern_subject(scope: Rc<InferScope>, scrutinee_subject: Rc<PatternSubject>) -> Rc<PatternSubject> {
    match (*scrutinee_subject).clone() {
    PatternSubject::PatternResolved { node: scrutinee_type, .. } => pattern_subject_from_node(resolve_scrutinee_type_node(scope.type_env.clone(), scrutinee_type.clone())),
    PatternSubject::PatternDynamic { span: dynamic_span, .. } => Rc::new(PatternSubject::PatternDynamic {
    span: dynamic_span.clone(),
}),
    PatternSubject::PatternLookupBlocked => Rc::new(PatternSubject::PatternLookupBlocked),
}
}

pub fn annotate_pattern_parent_enums(pattern: Rc<MatchPattern>, scrutinee_subject: Rc<PatternSubject>, scope: Rc<InferScope>) -> Rc<MatchPattern> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*pattern.clone()).clone() {
    MatchPattern::VariantPattern { name: variant_name, field_bindings: bindings, .. } => {
            let resolved_scrut = resolve_pattern_subject(scope.clone(), scrutinee_subject);
let inferred_parent = match (*resolved_scrut.clone()).clone() {
    PatternSubject::PatternResolved { node: resolved_scrut_node, .. } => if ((resolved_scrut_node.return_cardinality.clone() == Cardinality::CardOptional) && ((variant_name.clone().as_str() == "Some".to_string().as_str()) || (variant_name.clone().as_str() == "None".to_string().as_str()))) {
                Some("Optional".to_string())
} else {
                {
                    let is_coproduct = (resolved_scrut_node.connective.clone() == Connective::Disj);
if is_coproduct {
                        Some(resolved_scrut_node.name.clone())
} else {
                        None
}
}
},
    PatternSubject::PatternDynamic { .. } => None,
    PatternSubject::PatternLookupBlocked => None,
};
let variant_lookup = lookup_variant_in_type(resolved_scrut.clone(), variant_name.clone(), scope.module_name.clone());
let variant_subject = lookup_result_subject(variant_lookup);
let annotated_bindings = Rc::new({ let mut __result = Vec::new(); for binding in bindings.clone().iter().cloned() { __result.push({
                let field_lookup = lookup_field_in_variant(variant_subject.clone(), field_binding_name(binding.clone()), scope.module_name.clone());
let field_subject = lookup_result_subject(field_lookup.clone());
make_field_binding_node(field_binding_name(binding.clone()), annotate_pattern_parent_enums(field_binding_pattern(binding.clone()), field_subject.clone(), scope.clone()), binding.span.clone())
}); } __result });
match inferred_parent {
    Some(parent_name) => Rc::new(MatchPattern::VariantPattern {
    name: variant_name.clone(),
    parent_enum: Some(parent_name.clone()),
    field_bindings: annotated_bindings,
}),
    None => Rc::new(MatchPattern::VariantPattern {
    name: variant_name.clone(),
    parent_enum: None,
    field_bindings: annotated_bindings,
}),
}
},
    _ => pattern.clone(),
}
    })
}

pub fn build_params_scope(scope: Rc<InferScope>, params: Rc<Vec<Rc<Node>>>) -> Rc<InferScope> {
    {
        let new_locals = params.iter().cloned().fold(scope.locals.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, p: Rc<Node>| v2_rt::rc_map_insert(acc.clone(), param_node_name(p.clone()), Rc::new(TypeBinding {
    name: param_node_name(p.clone()),
    resolved: param_node_type_expr(p.clone()),
})));
Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: new_locals,
    match_bound_names: scope.match_bound_names.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}
}

pub fn extend_scope(scope: Rc<InferScope>, name: String, resolved: Rc<Node>) -> Rc<InferScope> {
    Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: v2_rt::rc_map_insert(scope.locals.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: resolved,
})),
    match_bound_names: scope.match_bound_names.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}

pub fn extend_scope_match_bound(scope: Rc<InferScope>, name: String, resolved: Rc<Node>) -> Rc<InferScope> {
    Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: v2_rt::rc_map_insert(scope.locals.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: resolved,
})),
    match_bound_names: v2_rt::rc_map_insert(scope.match_bound_names.clone(), name.clone(), true),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}

pub fn extend_scope_with_params(scope: Rc<InferScope>, params: Rc<Vec<String>>) -> Rc<InferScope> {
    {
        let new_locals = params.iter().cloned().fold(scope.locals.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, p: String| v2_rt::rc_map_insert(acc.clone(), p.clone(), Rc::new(TypeBinding {
    name: p.clone(),
    resolved: type_variable_node("lambda_param".to_string()),
})));
Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: new_locals,
    match_bound_names: scope.match_bound_names.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}
}

pub fn scope_after_stmt_node(stmt: Rc<Node>, stmt_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<InferScope> {
    match (*stmt.expr_data.clone()).clone() {
    ExprData::ExprLet => if ((stmt.children.clone().len() as i64) <= 1) {
        extend_scope(scope, let_binding_name(stmt.clone()), stmt_type)
} else {
        scope
},
    _ => scope,
}
}

pub fn extend_scope_with_pattern_node(scope: Rc<InferScope>, pattern: Rc<MatchPattern>, scrutinee_subject: Rc<PatternSubject>) -> Rc<PatternScopeResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*pattern).clone() {
    MatchPattern::Bind { name: n, .. } => Rc::new(PatternScopeResult {
    scope: extend_scope_match_bound(scope.clone(), n.clone(), pattern_binding_type(scrutinee_subject)),
    diagnostics: Rc::new(vec![]),
}),
    MatchPattern::Wildcard => Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: Rc::new(vec![]),
}),
    MatchPattern::LitPattern { .. } => Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: Rc::new(vec![]),
}),
    MatchPattern::VariantPattern { name: vname, field_bindings: bindings, .. } => {
            let resolved_scrut = resolve_pattern_subject(scope.clone(), scrutinee_subject);
let variant_lookup = lookup_variant_in_type(resolved_scrut, vname.clone(), scope.module_name.clone());
let variant_subject = lookup_result_subject(variant_lookup.clone());
let variant_diags = variant_lookup.diagnostics.clone();
bindings.clone().iter().cloned().fold(Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: variant_diags,
}), |acc: Rc<PatternScopeResult>, fb: Rc<Node>| {
                let field_lookup = lookup_field_in_variant(variant_subject.clone(), field_binding_name(fb.clone()), scope.module_name.clone());
let field_subject = lookup_result_subject(field_lookup.clone());
let field_type = pattern_binding_type(field_subject.clone());
let fb_pattern = field_binding_pattern(fb.clone());
match (*fb_pattern.clone()).clone() {
    MatchPattern::Bind { name: n, .. } => Rc::new(PatternScopeResult {
    scope: extend_scope_match_bound(acc.scope.clone(), n.clone(), field_type.clone()),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), field_lookup.diagnostics.clone()),
}),
    _ => {
                    let nested = extend_scope_with_pattern_node(acc.scope.clone(), fb_pattern.clone(), field_subject.clone());
Rc::new(PatternScopeResult {
    scope: nested.scope.clone(),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), v2_rt::concat(field_lookup.diagnostics.clone(), nested.diagnostics.clone())),
})
},
}
})
},
}
    })
}

pub fn method_name_is(opt: Option<String>, expected: String) -> bool {
    if (opt.clone() == None) {
        false
} else {
        (opt.clone().unwrap().as_str() == expected.as_str())
}
}

pub fn arg_has_name(arg: Rc<Node>, name: String) -> bool {
    {
        let n = arg_name(arg);
if (n.clone() == None) {
            false
} else {
            (n.clone().unwrap().as_str() == name.as_str())
}
}
}

pub fn extract_fold_init_info(method_name: Option<String>, method_args: Rc<Vec<Rc<Node>>>, min_args: i64, scope: Rc<InferScope>, expected: Option<Rc<Node>>) -> Option<Rc<ArgInferResult>> {
    {
        let is_fold = method_name_is(method_name, "fold".to_string());
if (is_fold && ((method_args.clone().len() as i64) >= min_args)) {
            {
                let init_arg = match Rc::new({ let mut __result = Vec::new(); for a in method_args.clone().iter().cloned() { if arg_has_name(a.clone(), "init".to_string()) { __result.push(a); } } __result }).first().cloned() {
    Some(a) => Some(a.clone()),
    None => method_args.clone().first().cloned(),
};
match init_arg {
    Some(ia) => {
                    let init_result = infer_expr(arg_value(ia.clone()), scope, expected);
Some(Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(ia.clone()), init_result.typed.clone(), ia.span.clone()),
    diagnostics: init_result.diagnostics.clone(),
}))
},
    None => None,
}
}
} else {
            None
}
}
}

pub fn infer_method_args_with_fold(method_name: Option<String>, method_args: Rc<Vec<Rc<Node>>>, fold_info: Option<Rc<ArgInferResult>>, fold_acc_type: Rc<Node>, element_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<Vec<Rc<ArgInferResult>>> {
    {
        let is_fold = method_name_is(method_name, "fold".to_string());
Rc::new({ let mut __result = Vec::new(); for idx_pair in Rc::new(method_args.iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.push({
            let a = idx_pair.1.clone();
let idx = idx_pair.0.clone();
let is_init_arg = (arg_has_name(a.clone(), "init".to_string()) || ((arg_name(a.clone()) == None) && (idx.clone() == 0)));
if ((is_fold.clone() && (fold_info.clone() != None)) && is_init_arg.clone()) {
                match fold_info.clone() {
    Some(fi) => fi.clone(),
    None => infer_arg_with_element_type(a.clone(), element_type.clone(), scope.clone()),
}
} else {
                if (is_fold.clone() && (fold_info.clone() != None)) {
                    {
                        let fold_params = Rc::new(vec![make_param_node("acc".to_string(), fold_acc_type.clone(), None, no_span()), make_param_node("elem".to_string(), element_type.clone(), None, no_span())]);
let fold_callable = Rc::new(Node {
    name: "".to_string(),
    span: no_span(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: fold_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let ar = infer_expr(arg_value(a.clone()), scope.clone(), Some(fold_callable.clone()));
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(a.clone()), ar.typed.clone(), a.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
}
} else {
                    infer_arg_with_element_type(a.clone(), element_type.clone(), scope.clone())
}
}
}); } __result })
}
}

pub fn is_lambda_expr(e: Rc<Node>) -> bool {
    match (*e.expr_data.clone()).clone() {
    ExprData::ExprLambda { .. } => true,
    _ => false,
}
}

pub fn refine_collection_result_type(semantics: Option<Rc<MethodSemantics>>, typed_args: Rc<Vec<Rc<Node>>>, receiver_type: Rc<Node>, fallback: Rc<Node>) -> Rc<Node> {
    {
        let receiver_type_name = receiver_type.name.clone();
let receiver_is_map = node_is_keyed_collection(receiver_type.clone());
let method_name = if (semantics.clone() == None) {
            None
} else {
            match (*semantics.clone().unwrap()).clone() {
    MethodSemantics::AlgebraMethodSemantics { method_def: md, .. } => Some(md.name.clone()),
    _ => None,
}
};
if method_name_is(method_name.clone(), "map".to_string()) {
            match Rc::new({ let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }).first().cloned() {
    Some(lambda_arg) => {
                let lambda_ret = rt_type(arg_value(lambda_arg.clone()));
named_collection_type(receiver_type_name, lambda_ret)
},
    None => fallback.clone(),
}
} else {
            if method_name_is(method_name.clone(), "flat_map".to_string()) {
                match Rc::new({ let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }).first().cloned() {
    Some(lambda_arg) => match (*rt_node(arg_value(lambda_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::InferVariable { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
}
} else {
                if (((method_name_is(method_name.clone(), "filter".to_string()) || method_name_is(method_name.clone(), "sort_by".to_string())) || method_name_is(method_name.clone(), "unique".to_string())) || method_name_is(method_name.clone(), "reverse".to_string())) {
                    receiver_type.clone()
} else {
                    if method_name_is(method_name.clone(), "fold".to_string()) {
                        {
                            let fold_acc = if (semantics.clone() == None) {
                                None
} else {
                                match (*semantics.clone().unwrap()).clone() {
    MethodSemantics::AlgebraMethodSemantics { fold_accumulator_type: fat, .. } => fat.clone(),
    _ => None,
}
};
let acc_under_resolved = match fold_acc.clone() {
    Some(acc_type) => !is_fully_resolved(acc_type.clone()),
    None => true,
};
if acc_under_resolved {
                                match Rc::new({ let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }).first().cloned() {
    Some(lambda_arg) => rt_type(arg_value(lambda_arg.clone())),
    None => match fold_acc.clone() {
    Some(at) => at.clone(),
    None => fallback.clone(),
},
}
} else {
                                match fold_acc.clone() {
    Some(at) => at.clone(),
    None => fallback.clone(),
}
}
}
} else {
                        if ((method_name_is(method_name.clone(), "list_push".to_string()) && ((typed_args.clone().len() as i64) >= 1)) && ((receiver_type.children.clone().len() as i64) == 0)) {
                            match typed_args.clone().first().cloned() {
    Some(item_arg) => match (*rt_node(arg_value(item_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => named_collection_type("List".to_string(), rt.clone()),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::InferVariable { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
}
} else {
                            if ((((method_name_is(method_name.clone(), "map_insert".to_string()) && ((receiver_type.children.clone().len() as i64) == 0)) && is_container_type(receiver_type.name.clone())) && (container_expected_arity(receiver_type.name.clone()) == Some(2))) && ((typed_args.clone().len() as i64) >= 2)) {
                                {
                                    let key_type = match typed_args.clone().first().cloned() {
    Some(key_arg) => match (*rt_node(arg_value(key_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::InferVariable { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
};
match typed_args.clone().get(1 as usize).cloned() {
    Some(val_arg) => map_node(key_type, match (*rt_node(arg_value(val_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::InferVariable { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
}),
    None => fallback.clone(),
}
}
} else {
                                if ((method_name_is(method_name.clone(), "map_merge".to_string()) && (receiver_is_map || (is_container_type(receiver_type.name.clone()) && (container_expected_arity(receiver_type.name.clone()) == Some(2))))) && ((typed_args.clone().len() as i64) >= 1)) {
                                    {
                                        let overlay_type = match typed_args.clone().first().cloned() {
    Some(overlay_arg) => match (*rt_node(arg_value(overlay_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => receiver_type.clone(),
    NodeType::InferVariable { .. } => receiver_type.clone(),
    NodeType::Untyped => receiver_type.clone(),
},
    None => receiver_type.clone(),
};
if (((receiver_type.children.clone().len() as i64) == 0) && ((overlay_type.children.clone().len() as i64) > 0)) {
                                            overlay_type.clone()
} else {
                                            if ((receiver_type.children.clone().len() as i64) > 0) {
                                                receiver_type.clone()
} else {
                                                fallback.clone()
}
}
}
} else {
                                    fallback.clone()
}
}
}
}
}
}
}
}
}

pub fn infer_arg_with_element_type(arg: Rc<Node>, element_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<ArgInferResult> {
    {
        let ar = infer_expr(arg_value(arg.clone()), scope, Some(element_type));
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(arg.clone()), ar.typed.clone(), arg.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
}
}

pub fn infer_expr(texpr: Rc<Node>, scope: Rc<InferScope>, expected: Option<Rc<Node>>) -> Rc<InferResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*texpr.expr_data.clone()).clone() {
    ExprData::ExprLiteral { value: lit, .. } => {
            let span = texpr.span.clone();
ok_infer(make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: lit.clone(),
}), Rc::new(vec![]), Some(Rc::new(InferredNode::Resolved {
    node: infer_literal_node(lit.clone()),
})), span.clone()))
},
    ExprData::ExprError { kind, message, .. } => {
            let span = texpr.span.clone();
Rc::new(InferResult {
    typed: make_expr_error_node(kind.clone(), message.clone(), span.clone()),
    diagnostics: Rc::new(vec![]),
})
},
    ExprData::ExprVar { .. } => {
            let name = expr_var_name(texpr.clone());
let span = texpr.span.clone();
match v2_rt::map_get(&scope.locals.clone(), name.clone()) {
    Some(binding) => {
                let binding_kind = infer_var_binding_kind(scope.clone(), name.clone());
ok_infer(make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: Some(binding_kind),
}), Rc::new(vec![]), Some(Rc::new(InferredNode::Resolved {
    node: binding.resolved.clone(),
})), span.clone()))
},
    None => match lookup_func_sig(scope.func_env.clone(), name.clone()) {
    Some(fsig) => ok_infer(make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: Some(Rc::new(VarBindingKind::FunctionValueBinding)),
}), Rc::new(vec![]), Some(Rc::new(InferredNode::Resolved {
    node: resolved_callable_type(fsig.params.clone(), fsig.inferred.clone()),
})), span.clone())),
    None => {
                let err_texpr = make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: None,
}), Rc::new(vec![]), Some(Rc::new(InferredNode::Resolved {
    node: error_type(),
})), span.clone());
Rc::new(InferResult {
    typed: err_texpr,
    diagnostics: Rc::new(vec![inference_error(v2_rt::concat(v2_rt::concat("undefined variable '".to_string(), name.clone()), "'".to_string()), span.clone(), scope.module_name.clone())]),
})
},
},
}
},
    ExprData::ExprFieldAccess { .. } => {
            let field_name = field_access_field(texpr.clone());
let span = texpr.span.clone();
let base_expr = field_access_base(texpr.clone());
let base_result = infer_expr(base_expr, scope.clone(), None);
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let base_rt = match (*rt_node(base_typed.clone())).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type(),
    NodeType::InferVariable { .. } => error_type(),
    NodeType::Untyped => unit_type(),
};
let resolved_base = resolve_scrutinee_type_node(scope.type_env.clone(), base_rt.clone());
let resolved_base_is_error = if (resolved_base.inferred.clone() != None) {
                is_compiler_error(resolved_base.inferred.clone().clone().unwrap())
} else {
                false
};
if resolved_base_is_error {
                Rc::new(InferResult {
    typed: make_expr_error_node(ExprErrorKind::SemanticExprError, "error type cascade".to_string(), span.clone()),
    diagnostics: base_diags,
})
} else {
                match lookup_field_type_node(resolved_base.clone(), field_name.clone()) {
    Some(field_type) => {
                    let field_summary = field_summary_for_type(base_rt.clone(), scope.type_env.clone(), field_name.clone());
let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
    summary: field_summary,
}), Rc::new(vec![base_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: field_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr,
    diagnostics: base_diags,
})
},
    None => {
                    let base_is_type_var = if (resolved_base.inferred.clone() != None) {
                        is_type_variable(resolved_base.inferred.clone().clone().unwrap())
} else {
                        false
};
if base_is_type_var {
                        {
                            let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
    summary: Some(Rc::new(FieldSummary {
    access_style: FieldAccessStyle::StoredField,
    value_shape: FieldValueShape::PlainValue,
})),
}), Rc::new(vec![base_typed.clone()]), Some(Rc::new(InferredNode::TypeVariable {
    id: "field_of_type_var".to_string(),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr,
    diagnostics: base_diags,
})
}
} else {
                        match check_service_field_access_node(base_rt.clone(), field_name.clone(), scope.service_registry.clone()) {
    Some(svc_type) => {
                            let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
    summary: Some(Rc::new(FieldSummary {
    access_style: FieldAccessStyle::StoredField,
    value_shape: FieldValueShape::PlainValue,
})),
}), Rc::new(vec![base_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: svc_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr,
    diagnostics: base_diags,
})
},
    None => {
                            let error_message = v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("no field '".to_string(), field_name.clone()), "' on type '".to_string()), resolved_base.name.clone()), "'".to_string());
let fa_texpr = make_expr_error_node(ExprErrorKind::SemanticExprError, error_message.clone(), span.clone());
Rc::new(InferResult {
    typed: fa_texpr,
    diagnostics: v2_rt::concat(base_diags, Rc::new(vec![inference_error(error_message.clone(), span.clone(), scope.module_name.clone())])),
})
},
}
}
},
}
}
},
    ExprData::ExprCall { .. } => {
            let func_name = expr_call_func(texpr.clone());
let span = texpr.span.clone();
let call_args = texpr.children.clone();
let sig = lookup_func_sig(scope.func_env.clone(), func_name.clone());
let sig_params = match sig.clone() {
    Some(s) => s.params.clone(),
    None => Rc::new(vec![]),
};
let has_lambda = { let mut __found = false; for a in call_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __found = true; break; } } __found };
let call_method_args = Rc::new(call_args.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let call_method_name = Some(func_name.clone());
let call_fold_info = extract_fold_init_info(call_method_name.clone(), call_method_args.clone(), 2, scope.clone(), expected.clone());
let call_fold_acc_type = match call_fold_info.clone() {
    Some(fi) => match (*rt_node(arg_value(fi.typed_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type(),
    NodeType::InferVariable { .. } => error_type(),
    NodeType::Untyped => error_type(),
},
    None => error_type(),
};
let arg_infer_results = if ((has_lambda && ((call_args.clone().len() as i64) >= 2)) && (sig.clone() == None)) {
                match call_args.clone().first().cloned() {
    Some(first_arg) => {
                    let first_result = infer_expr(arg_value(first_arg.clone()), scope.clone(), None);
let first_type = rt_type(first_result.typed.clone());
let elem_type = for_each_element_type_node(first_type);
let remaining_results = infer_method_args_with_fold(call_method_name.clone(), call_method_args.clone(), call_fold_info.clone(), call_fold_acc_type.clone(), elem_type, scope.clone());
v2_rt::concat(Rc::new(vec![Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(first_arg.clone()), first_result.typed.clone(), first_arg.span.clone()),
    diagnostics: first_result.diagnostics.clone(),
})]), remaining_results)
},
    None => Rc::new(vec![]),
}
} else {
                Rc::new({ let mut __result = Vec::new(); for pair in Rc::new(call_args.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.push({
                    let a = pair.1.clone();
let formal_param_type = match sig_params.clone().get(pair.0.clone() as usize).cloned() {
    Some(p) => param_node_type_expr(p.clone()),
    None => type_variable_node("callable_param".to_string()),
};
let has_formal = match sig_params.clone().get(pair.0.clone() as usize).cloned() {
    Some(_) => true,
    None => false,
};
let expected = if has_formal.clone() {
                        Some(formal_param_type.clone())
} else {
                        None
};
let ar = infer_expr(arg_value(a.clone()), scope.clone(), expected.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(a.clone()), ar.typed.clone(), a.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
}); } __result })
};
let typed_args = Rc::new({ let mut __result = Vec::new(); for air in arg_infer_results.clone().iter().cloned() { __result.push(air.typed_arg.clone()); } __result });
let arg_diags = Rc::new({ let mut __result = Vec::new(); for air in arg_infer_results.clone().iter().cloned() { __result.extend((*air.diagnostics.clone()).iter().cloned()); } __result });
let typed_arg_nodes = typed_args.clone();
if (sig.clone() != None) {
                {
                    let resolved_type = match sig.clone() {
    Some(s) => s.inferred.clone(),
    None => error_type(),
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
    call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes, Some(Rc::new(InferredNode::Resolved {
    node: resolved_type,
})), span.clone()),
    diagnostics: arg_diags,
})
}
} else {
                {
                    let first_arg_type = match typed_args.clone().first().cloned() {
    Some(ta) => rt_type(arg_value(ta.clone())),
    None => unit_type(),
};
let method_receiver = match typed_args.clone().first().cloned() {
    Some(ta) => arg_value(ta.clone()),
    None => internal_expr_error_node("method bridge missing receiver".to_string(), span.clone()),
};
let call_acc_is_under_resolved = !is_fully_resolved(call_fold_acc_type.clone());
let refined_call_fold_acc_type = if ((call_fold_info.clone() != None) && call_acc_is_under_resolved) {
                        match Rc::new({ let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }).first().cloned() {
    Some(lambda_arg) => rt_type(arg_value(lambda_arg.clone())),
    None => call_fold_acc_type.clone(),
}
} else {
                        call_fold_acc_type.clone()
};
let method_resolution = resolve_known_method_node(method_receiver.clone(), first_arg_type.clone(), func_name.clone(), if (call_fold_info.clone() != None) {
                        Some(refined_call_fold_acc_type)
} else {
                        None
}, scope.service_registry.clone());
let is_known_method = (method_resolution.result_type.clone() != None);
if (is_known_method && ((typed_args.clone().len() as i64) > 0)) {
                        {
                            let receiver = method_receiver.clone();
let remaining = Rc::new(typed_args.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let base_result_type = match method_resolution.result_type.clone() {
    Some(mt) => mt.clone(),
    None => error_type(),
};
let bridge_result_type = refine_collection_result_type(method_resolution.semantics.clone(), remaining.clone(), first_arg_type.clone(), base_result_type);
let remaining_arg_nodes = Rc::new({ let mut __result = Vec::new(); for ta in remaining.clone().iter().cloned() { __result.push(make_arg_node(arg_name(ta.clone()), arg_value(ta.clone()), span.clone())); } __result });
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprMethodCall {
    method_semantics: method_resolution.semantics.clone(),
}), v2_rt::concat(Rc::new(vec![receiver]), remaining_arg_nodes), Some(Rc::new(InferredNode::Resolved {
    node: bridge_result_type,
})), span.clone()),
    diagnostics: arg_diags,
})
}
} else {
                        if (func_name.clone().as_str() == "empty_map".to_string().as_str()) {
                            {
                                let empty_map_type = match expected.clone() {
    Some(exp) => if node_is_keyed_collection(exp.clone()) {
                                    exp.clone()
} else {
                                    bare_map_node()
},
    None => bare_map_node(),
};
let empty_map_diags = match expected.clone() {
    Some(exp) => if node_is_keyed_collection(exp.clone()) {
                                    Rc::new(vec![])
} else {
                                    Rc::new(vec![inference_error("empty_map(): expected type is not a keyed collection".to_string(), span.clone(), scope.module_name.clone())])
},
    None => Rc::new(vec![]),
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
    call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes, Some(Rc::new(InferredNode::Resolved {
    node: empty_map_type,
})), span.clone()),
    diagnostics: v2_rt::concat(arg_diags, empty_map_diags),
})
}
} else {
                            if (infer_builtin_call_type(func_name.clone()) != None) {
                                {
                                    let bt = if ((func_name.clone().as_str() == "lookup".to_string().as_str()) || (func_name.clone().as_str() == "map_get".to_string().as_str())) {
                                        match typed_args.clone().first().cloned() {
    Some(receiver_arg) => match (*rt_node(arg_value(receiver_arg.clone()))).clone() {
    NodeType::Typed { node: receiver_type, .. } => match map_value_type_in_env(receiver_type.clone(), scope.type_env.clone()) {
    Some(value_type) => with_optional_cardinality(value_type.clone()),
    None => resolve_builtin_call_type(func_name.clone()),
},
    _ => resolve_builtin_call_type(func_name.clone()),
},
    None => resolve_builtin_call_type(func_name.clone()),
}
} else {
                                        if (func_name.clone().as_str() == "map_keys".to_string().as_str()) {
                                            match typed_args.clone().first().cloned() {
    Some(receiver_arg) => match (*rt_node(arg_value(receiver_arg.clone()))).clone() {
    NodeType::Typed { node: receiver_type, .. } => match map_key_type_in_env(receiver_type.clone(), scope.type_env.clone()) {
    Some(key_type) => list_of_element(key_type.clone()),
    None => resolve_builtin_call_type(func_name.clone()),
},
    _ => resolve_builtin_call_type(func_name.clone()),
},
    None => resolve_builtin_call_type(func_name.clone()),
}
} else {
                                            if (func_name.clone().as_str() == "map_values".to_string().as_str()) {
                                                match typed_args.clone().first().cloned() {
    Some(receiver_arg) => match (*rt_node(arg_value(receiver_arg.clone()))).clone() {
    NodeType::Typed { node: receiver_type, .. } => match map_value_type_in_env(receiver_type.clone(), scope.type_env.clone()) {
    Some(value_type) => list_of_element(value_type.clone()),
    None => resolve_builtin_call_type(func_name.clone()),
},
    _ => resolve_builtin_call_type(func_name.clone()),
},
    None => resolve_builtin_call_type(func_name.clone()),
}
} else {
                                                resolve_builtin_call_type(func_name.clone())
}
}
};
let call_semantics = if (func_name.clone().as_str() == "lookup".to_string().as_str()) {
                                        Some(CallSemantics::LookupCallSemantics)
} else {
                                        Some(CallSemantics::PlainCallSemantics)
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
    call_semantics: call_semantics,
}), typed_arg_nodes, Some(Rc::new(InferredNode::Resolved {
    node: bt,
})), span.clone()),
    diagnostics: arg_diags,
})
}
} else {
                                {
                                    let callable_local = match v2_rt::map_get(&scope.locals.clone(), func_name.clone()) {
    Some(binding) => if ((binding.resolved.clone().params.clone().len() as i64) > 0) {
                                        Some(binding.resolved.clone())
} else {
                                        None
},
    None => None,
};
if (callable_local.clone() != None) {
                                        {
                                            let callable_type = match callable_local.clone() {
    Some(ct) => ct.clone(),
    None => error_type(),
};
let resolved_type = callable_inferred(callable_type);
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
    call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes, Some(Rc::new(InferredNode::Resolved {
    node: resolved_type,
})), span.clone()),
    diagnostics: arg_diags,
})
}
} else {
                                        {
                                            let type_match = lookup_type(scope.type_env.clone(), func_name.clone());
let resolved_type = match type_match.clone() {
    Some(tn) => tn.clone(),
    None => error_type(),
};
let call_diags = match type_match.clone() {
    Some(_) => Rc::new(vec![]),
    None => Rc::new(vec![inference_error(v2_rt::concat(v2_rt::concat("function '".to_string(), func_name.clone()), "' not found in scope".to_string()), span.clone(), scope.module_name.clone())]),
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
    call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes, Some(Rc::new(InferredNode::Resolved {
    node: resolved_type,
})), span.clone()),
    diagnostics: v2_rt::concat(arg_diags, call_diags),
})
}
}
}
}
}
}
}
}
},
    ExprData::ExprMethodCall { .. } => {
            let method_name = expr_method_name(texpr.clone());
let span = texpr.span.clone();
let recv = method_receiver(texpr.clone());
let mc_args = method_arg_nodes(texpr.clone());
let recv_result = infer_expr(recv, scope.clone(), None);
let recv_typed = recv_result.typed.clone();
let recv_diags = recv_result.diagnostics.clone();
let recv_rt = rt_type(recv_typed.clone());
let recv_elem_type = for_each_element_type_node(recv_rt.clone());
let mc_method_name = Some(method_name.clone());
let fold_info = extract_fold_init_info(mc_method_name.clone(), mc_args.clone(), 2, scope.clone(), expected.clone());
let fold_acc_type = match fold_info.clone() {
    Some(fi) => match (*rt_node(arg_value(fi.typed_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type(),
    NodeType::InferVariable { .. } => error_type(),
    NodeType::Untyped => error_type(),
},
    None => error_type(),
};
let mc_arg_infer_results = infer_method_args_with_fold(mc_method_name.clone(), mc_args.clone(), fold_info.clone(), fold_acc_type.clone(), recv_elem_type, scope.clone());
let typed_mc_args = Rc::new({ let mut __result = Vec::new(); for air in mc_arg_infer_results.clone().iter().cloned() { __result.push(air.typed_arg.clone()); } __result });
let mc_arg_diags = Rc::new({ let mut __result = Vec::new(); for air in mc_arg_infer_results.clone().iter().cloned() { __result.extend((*air.diagnostics.clone()).iter().cloned()); } __result });
let mc_acc_is_under_resolved = !is_fully_resolved(fold_acc_type.clone());
let refined_fold_acc_type = if ((fold_info.clone() != None) && mc_acc_is_under_resolved) {
                match Rc::new({ let mut __result = Vec::new(); for a in typed_mc_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }).first().cloned() {
    Some(lambda_arg) => rt_type(arg_value(lambda_arg.clone())),
    None => fold_acc_type.clone(),
}
} else {
                fold_acc_type.clone()
};
let method_resolution = resolve_known_method_node(recv_typed.clone(), recv_rt.clone(), method_name.clone(), if (fold_info.clone() != None) {
                Some(refined_fold_acc_type)
} else {
                None
}, scope.service_registry.clone());
let base_result_type = match method_resolution.result_type.clone() {
    Some(rt) => rt.clone(),
    None => if (method_name.clone().as_str() == "map_keys".to_string().as_str()) {
                match map_key_type_in_env(recv_rt.clone(), scope.type_env.clone()) {
    Some(key_type) => list_of_element(key_type.clone()),
    None => recv_rt.clone(),
}
} else {
                if (method_name.clone().as_str() == "map_values".to_string().as_str()) {
                    match map_value_type_in_env(recv_rt.clone(), scope.type_env.clone()) {
    Some(value_type) => list_of_element(value_type.clone()),
    None => recv_rt.clone(),
}
} else {
                    recv_rt.clone()
}
},
};
let result_type = refine_collection_result_type(method_resolution.semantics.clone(), typed_mc_args.clone(), recv_rt.clone(), base_result_type);
let method_semantics = if (method_resolution.semantics.clone() != None) {
                method_resolution.semantics.clone()
} else {
                Some(Rc::new(MethodSemantics::PlainMethodSemantics))
};
let typed_mc_arg_nodes = typed_mc_args.clone();
let mc_texpr = make_named_expr_node(method_name.clone(), Rc::new(ExprData::ExprMethodCall {
    method_semantics: method_semantics,
}), v2_rt::concat(Rc::new(vec![recv_typed.clone()]), typed_mc_arg_nodes), Some(Rc::new(InferredNode::Resolved {
    node: result_type,
})), span.clone());
Rc::new(InferResult {
    typed: mc_texpr,
    diagnostics: v2_rt::concat(recv_diags, mc_arg_diags),
})
},
    ExprData::ExprMatch => {
            let span = texpr.span.clone();
let scrut = match_scrutinee(texpr.clone());
let arm_nodes = match_arm_nodes(texpr.clone());
let scrut_result = infer_expr(scrut, scope.clone(), None);
let scrut_typed = scrut_result.typed.clone();
let scrut_diags = scrut_result.diagnostics.clone();
let scrut_rt = rt_type(scrut_typed.clone());
let scrut_subject = pattern_subject_from_node_type(rt_node(scrut_typed.clone()));
let arm_infer_results = Rc::new({ let mut __result = Vec::new(); for arm_node in arm_nodes.iter().cloned() { __result.push({
                let arm_pat = arm_pattern(arm_node.clone());
let arm_g = arm_guard(arm_node.clone());
let arm_b = arm_body(arm_node.clone());
let typed_pattern = annotate_pattern_parent_enums(arm_pat.clone(), scrut_subject.clone(), scope.clone());
let pattern_result = extend_scope_with_pattern_node(scope.clone(), typed_pattern.clone(), scrut_subject.clone());
let arm_scope = pattern_result.scope.clone();
let pattern_diags = pattern_result.diagnostics.clone();
let guard_result = if (arm_g.clone() != None) {
                    Some(infer_expr(arm_g.clone().unwrap(), arm_scope.clone(), None))
} else {
                    None
};
let body_result = infer_expr(arm_b.clone(), arm_scope.clone(), expected.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let guard_unwrapped = match guard_result.clone() {
    Some(gr) => gr.clone(),
    None => Rc::new(InferResult {
    typed: arm_b.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let guard_diags = if (guard_result.clone() != None) {
                    guard_unwrapped.diagnostics.clone()
} else {
                    Rc::new(vec![])
};
Rc::new(ArmInferResult {
    typed_arm: make_arm_node(typed_pattern.clone(), if (guard_result.clone() != None) {
                    Some(guard_unwrapped.typed.clone())
} else {
                    None
}, body_typed.clone(), span.clone()),
    diagnostics: v2_rt::concat(pattern_diags.clone(), v2_rt::concat(guard_diags.clone(), body_diags.clone())),
    body_type: rt_type(body_typed.clone()),
})
}); } __result });
let typed_arms = Rc::new({ let mut __result = Vec::new(); for ar in arm_infer_results.clone().iter().cloned() { __result.push(ar.typed_arm.clone()); } __result });
let arm_diags = Rc::new({ let mut __result = Vec::new(); for ar in arm_infer_results.clone().iter().cloned() { __result.extend((*ar.diagnostics.clone()).iter().cloned()); } __result });
let result_type = match arm_infer_results.clone().first().cloned() {
    Some(ar) => ar.body_type.clone(),
    None => scrut_rt,
};
let empty_arms_diags = if ((arm_infer_results.clone().len() as i64) == 0) {
                Rc::new(vec![inference_error("match expression has no arms".to_string(), span.clone(), scope.module_name.clone())])
} else {
                Rc::new(vec![])
};
let exhaustiveness_diags = match (*resolve_pattern_subject(scope.clone(), scrut_subject.clone())).clone() {
    PatternSubject::PatternResolved { node: resolved_scrutinee, .. } => check_match_exhaustiveness(resolved_scrutinee.clone(), typed_arms.clone(), scope.type_env.clone(), span.clone(), scope.module_name.clone()),
    PatternSubject::PatternDynamic { .. } => Rc::new(vec![]),
    PatternSubject::PatternLookupBlocked => Rc::new(vec![]),
};
let match_texpr = make_expr_node(Rc::new(ExprData::ExprMatch), v2_rt::concat(Rc::new(vec![scrut_typed.clone()]), typed_arms.clone()), Some(Rc::new(InferredNode::Resolved {
    node: result_type,
})), span.clone());
Rc::new(InferResult {
    typed: match_texpr,
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(scrut_diags, arm_diags), empty_arms_diags), exhaustiveness_diags),
})
},
    ExprData::ExprIf => {
            let span = texpr.span.clone();
let cond = if_condition(texpr.clone());
let then_expr = if_then_branch(texpr.clone());
let else_expr = if_else_branch(texpr.clone());
let cond_result = infer_expr(cond, scope.clone(), None);
let cond_typed = cond_result.typed.clone();
let cond_diags = cond_result.diagnostics.clone();
let then_result = infer_expr(then_expr, scope.clone(), expected.clone());
let then_typed = then_result.typed.clone();
let then_diags = then_result.diagnostics.clone();
match else_expr {
    Some(else_branch) => {
                let else_result = infer_expr(else_branch.clone(), scope.clone(), expected.clone());
let else_typed = else_result.typed.clone();
let else_diags = else_result.diagnostics.clone();
let then_rt = rt_type(then_typed.clone());
let else_rt = rt_type(else_typed.clone());
let branch_diags = if node_type_compatible(then_rt.clone(), else_rt.clone()) {
                    Rc::new(vec![])
} else {
                    Rc::new(vec![inference_error(v2_rt::concat(v2_rt::concat(v2_rt::concat("if branches resolve to incompatible types: ".to_string(), node_type_shape(then_rt.clone())), " vs ".to_string()), node_type_shape(else_rt.clone())), span.clone(), scope.module_name.clone())])
};
let resolved_type = prefer_specific_type(then_rt.clone(), else_rt.clone());
let if_texpr = make_expr_node(Rc::new(ExprData::ExprIf), Rc::new(vec![cond_typed, then_typed.clone(), else_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: resolved_type,
})), span.clone());
Rc::new(InferResult {
    typed: if_texpr,
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(cond_diags, then_diags), else_diags), branch_diags),
})
},
    None => {
                let if_texpr2 = make_expr_node(Rc::new(ExprData::ExprIf), Rc::new(vec![cond_typed, then_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: unit_type(),
})), span.clone());
Rc::new(InferResult {
    typed: if_texpr2,
    diagnostics: v2_rt::concat(cond_diags, then_diags),
})
},
}
},
    ExprData::ExprLet => {
            let let_name = let_binding_name(texpr.clone());
let span = texpr.span.clone();
let val_expr = let_value(texpr.clone());
let body_expr = let_body(texpr.clone());
let val_result = infer_expr(val_expr, scope.clone(), None);
let val_typed = val_result.typed.clone();
let val_diags = val_result.diagnostics.clone();
let val_type = rt_type(val_typed.clone());
if (body_expr.clone() == None) {
                {
                    let let_texpr = make_named_expr_node(let_name.clone(), Rc::new(ExprData::ExprLet), Rc::new(vec![val_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: val_type,
})), span.clone());
Rc::new(InferResult {
    typed: let_texpr,
    diagnostics: val_diags,
})
}
} else {
                {
                    let extended = extend_scope(scope.clone(), let_name.clone(), val_type);
let body_result = infer_expr(body_expr.clone().unwrap(), extended, expected.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let let_texpr2 = make_named_expr_node(let_name.clone(), Rc::new(ExprData::ExprLet), Rc::new(vec![val_typed.clone(), body_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: let_texpr2,
    diagnostics: v2_rt::concat(val_diags, body_diags.clone()),
})
}
}
},
    ExprData::ExprRecordLit { .. } => {
            let span = texpr.span.clone();
let field_inits = texpr.children.clone();
infer_record_lit(record_lit_type_name(texpr.clone()), field_inits, span.clone(), scope.clone())
},
    ExprData::ExprListLit => {
            let span = texpr.span.clone();
let elements = texpr.children.clone();
let elem_results = Rc::new({ let mut __result = Vec::new(); for e in elements.iter().cloned() { __result.push(infer_expr(e.clone(), scope.clone(), None)); } __result });
let typed_elements = Rc::new({ let mut __result = Vec::new(); for r in elem_results.clone().iter().cloned() { __result.push(r.typed.clone()); } __result });
let elem_diags = Rc::new({ let mut __result = Vec::new(); for r in elem_results.clone().iter().cloned() { __result.extend((*r.diagnostics.clone()).iter().cloned()); } __result });
let elem_type_node = if ((elem_results.clone().len() as i64) > 0) {
                match elem_results.clone().first().cloned() {
    Some(r) => rt_type(r.typed.clone()),
    None => unit_type(),
}
} else {
                match expected.clone() {
    Some(exp) => if node_is_element_collection(exp.clone()) {
                    match exp.children.clone().first().cloned() {
    Some(elem) => elem.clone(),
    None => unit_type(),
}
} else {
                    unit_type()
},
    None => unit_type(),
}
};
let empty_list_diags = if ((elem_results.clone().len() as i64) == 0) {
                match expected.clone() {
    Some(exp) => if node_is_element_collection(exp.clone()) {
                    match exp.children.clone().first().cloned() {
    Some(_) => Rc::new(vec![]),
    None => Rc::new(vec![inference_error("empty list literal: expected type has no element type".to_string(), span.clone(), scope.module_name.clone())]),
}
} else {
                    Rc::new(vec![inference_error("empty list literal: expected type is not a collection".to_string(), span.clone(), scope.module_name.clone())])
},
    None => Rc::new(vec![]),
}
} else {
                Rc::new(vec![])
};
let ll_texpr = make_expr_node(Rc::new(ExprData::ExprListLit), typed_elements, Some(Rc::new(InferredNode::Resolved {
    node: named_collection_type("List".to_string(), elem_type_node),
})), span.clone());
Rc::new(InferResult {
    typed: ll_texpr,
    diagnostics: v2_rt::concat(elem_diags, empty_list_diags),
})
},
    ExprData::ExprBinOp { op, .. } => {
            let span = texpr.span.clone();
let left_expr = binop_left(texpr.clone());
let right_expr = binop_right(texpr.clone());
let left_result = infer_expr(left_expr, scope.clone(), None);
let left_typed = left_result.typed.clone();
let left_diags = left_result.diagnostics.clone();
let right_result = infer_expr(right_expr, scope.clone(), None);
let right_typed = right_result.typed.clone();
let right_diags = right_result.diagnostics.clone();
let result_type = infer_binop_type_node(op.clone(), rt_type(left_typed.clone()));
let bo_texpr = make_expr_node(Rc::new(ExprData::ExprBinOp {
    op: op.clone(),
}), Rc::new(vec![left_typed.clone(), right_typed]), Some(Rc::new(InferredNode::Resolved {
    node: result_type,
})), span.clone());
Rc::new(InferResult {
    typed: bo_texpr,
    diagnostics: v2_rt::concat(left_diags, right_diags),
})
},
    ExprData::ExprUnaryOp { op, .. } => {
            let span = texpr.span.clone();
let operand_expr = unaryop_operand(texpr.clone());
let operand_result = infer_expr(operand_expr, scope.clone(), None);
let operand_typed = operand_result.typed.clone();
let operand_diags = operand_result.diagnostics.clone();
let result_type = match op.clone() {
    UnaryOpKind::Not => bool_type(),
    UnaryOpKind::Neg => rt_type(operand_typed.clone()),
};
let uo_texpr = make_expr_node(Rc::new(ExprData::ExprUnaryOp {
    op: op.clone(),
}), Rc::new(vec![operand_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: result_type,
})), span.clone());
Rc::new(InferResult {
    typed: uo_texpr,
    diagnostics: operand_diags,
})
},
    ExprData::ExprLambda { .. } => {
            let span = texpr.span.clone();
let lam_body = lambda_body(texpr.clone());
let lam_params = lambda_param_names(texpr.clone());
let lam_param_nodes = Rc::new(texpr.children.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let lam_scope = if (expected.clone() != None) {
                {
                    let exp = expected.clone().unwrap();
if ((exp.params.clone().len() as i64) > 0) {
                        Rc::new(lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned().fold(scope.clone(), |acc: Rc<InferScope>, pair: (i64, String)| match exp.params.clone().get(pair.0.clone() as usize).cloned() {
    Some(cp) => extend_scope(acc.clone(), pair.1.clone(), param_node_type_expr(cp.clone())),
    None => extend_scope(acc.clone(), pair.1.clone(), type_variable_node("callable_param".to_string())),
})
} else {
                        if ((lam_params.clone().len() as i64) == 1) {
                            match lam_params.clone().first().cloned() {
    Some(p) => extend_scope(scope.clone(), p.clone(), exp.clone()),
    None => scope.clone(),
}
} else {
                            Rc::new(lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned().fold(scope.clone(), |acc: Rc<InferScope>, pair: (i64, String)| if (pair.0.clone() == ((lam_params.clone().len() as i64) - 1)) {
                                extend_scope(acc.clone(), pair.1.clone(), exp.clone())
} else {
                                extend_scope(acc.clone(), pair.1.clone(), type_variable_node("lambda_param".to_string()))
})
}
}
}
} else {
                extend_scope_with_params(scope.clone(), lam_params.clone())
};
let body_result = infer_expr(lam_body, lam_scope.clone(), None);
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let lam_texpr = make_expr_node(Rc::new(ExprData::ExprLambda {
    semantics: Some(lambda_semantics_from_param_types(lambda_param_types_from_scope(lam_scope.clone(), lam_params.clone()))),
}), v2_rt::concat(Rc::new(vec![body_typed.clone()]), lam_param_nodes), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: lam_texpr,
    diagnostics: body_diags.clone(),
})
},
    ExprData::ExprStringInterp => {
            let span = texpr.span.clone();
let typed_part_nodes = Rc::new({ let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.push(match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => part_node.clone(),
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = infer_expr(inner.clone(), scope.clone(), None);
make_interp_part_node(r.typed.clone(), part_node.span.clone())
},
    None => part_node.clone(),
},
}); } __result });
let interp_diags = Rc::new({ let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.extend((*match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => Rc::new(vec![]),
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = infer_expr(inner.clone(), scope.clone(), None);
r.diagnostics.clone()
},
    None => Rc::new(vec![]),
},
}).iter().cloned()); } __result });
let si_texpr = make_expr_node(Rc::new(ExprData::ExprStringInterp), typed_part_nodes, Some(Rc::new(InferredNode::Resolved {
    node: string_type(),
})), span.clone());
Rc::new(InferResult {
    typed: si_texpr,
    diagnostics: interp_diags,
})
},
    ExprData::ExprBlock => {
            let span = texpr.span.clone();
let stmts = texpr.children.clone();
if ((stmts.clone().len() as i64) > 0) {
                {
                    let state = infer_block_stmts(stmts.clone(), (stmts.clone().len() as i64), scope.clone(), Rc::new(vec![]), Rc::new(vec![]), unit_type(), expected.clone());
let blk_texpr = make_expr_node(Rc::new(ExprData::ExprBlock), state.typed_stmts.clone(), Some(Rc::new(InferredNode::Resolved {
    node: state.last_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: blk_texpr,
    diagnostics: Rc::new({ let mut __result = Vec::new(); for c in state.diag_chunks.clone().iter().cloned() { __result.extend((*c.clone()).iter().cloned()); } __result }),
})
}
} else {
                ok_infer(make_expr_node(Rc::new(ExprData::ExprBlock), Rc::new(vec![]), Some(Rc::new(InferredNode::Resolved {
    node: unit_type(),
})), span.clone()))
}
},
    ExprData::ExprCast => {
            let span = texpr.span.clone();
let cast_inner = cast_expr(texpr.clone());
let target_type = cast_target(texpr.clone());
let inner_result = infer_expr(cast_inner, scope.clone(), None);
let inner_typed = inner_result.typed.clone();
let inner_diags = inner_result.diagnostics.clone();
let cast_texpr = make_expr_node(Rc::new(ExprData::ExprCast), Rc::new(vec![inner_typed, target_type.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: target_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: cast_texpr,
    diagnostics: inner_diags,
})
},
    ExprData::ExprForEach => {
            let variable = foreach_variable(texpr.clone());
let span = texpr.span.clone();
let coll = foreach_collection(texpr.clone());
let body_expr = foreach_body(texpr.clone());
let coll_result = infer_expr(coll, scope.clone(), None);
let coll_typed = coll_result.typed.clone();
let coll_diags = coll_result.diagnostics.clone();
let elem_type_node = for_each_element_type_node(rt_type(coll_typed.clone()));
let body_scope = extend_scope(scope.clone(), variable.clone(), elem_type_node);
let body_result = infer_expr(body_expr.clone(), body_scope, None);
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let fe_texpr = make_named_expr_node(variable.clone(), Rc::new(ExprData::ExprForEach), Rc::new(vec![coll_typed.clone(), body_typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: fe_texpr,
    diagnostics: v2_rt::concat(coll_diags, body_diags.clone()),
})
},
    ExprData::ExprIndex => {
            let span = texpr.span.clone();
let base_expr = index_base(texpr.clone());
let idx_key = index_expr(texpr.clone());
let base_result = infer_expr(base_expr, scope.clone(), None);
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let index_result = infer_expr(idx_key, scope.clone(), None);
let index_typed = index_result.typed.clone();
let index_diags = index_result.diagnostics.clone();
let base_type_result = rt_node(base_typed.clone());
let index_type_result = rt_node(index_typed.clone());
let index_check = match (*base_type_result.clone()).clone() {
    NodeType::Typed { node: base_type, .. } => match (*index_type_result.clone()).clone() {
    NodeType::Typed { node: index_type, .. } => Some(check_index_access_node(base_type.clone(), index_type.clone(), span.clone(), scope.module_name.clone())),
    NodeType::InferError { .. } => None,
    NodeType::InferVariable { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::InferVariable { .. } => None,
    NodeType::Untyped => None,
};
let access_failure_source = match (*base_type_result.clone()).clone() {
    NodeType::Typed { .. } => index_type_result.clone(),
    NodeType::InferError { .. } => base_type_result.clone(),
    NodeType::InferVariable { .. } => base_type_result.clone(),
    NodeType::Untyped => base_type_result.clone(),
};
let idx_inferred = match index_check.clone() {
    Some(checked) => checked.inferred.clone(),
    None => Some(inferred_from_node_type(access_failure_source, "invalid index access".to_string(), span.clone())),
};
let index_access_diags = match index_check.clone() {
    Some(checked) => checked.diagnostics.clone(),
    None => Rc::new(vec![]),
};
let idx_texpr = make_expr_node(Rc::new(ExprData::ExprIndex), Rc::new(vec![base_typed.clone(), index_typed.clone()]), idx_inferred, span.clone());
Rc::new(InferResult {
    typed: idx_texpr,
    diagnostics: v2_rt::concat(v2_rt::concat(base_diags, index_diags), index_access_diags),
})
},
    ExprData::ExprSlice => {
            let span = texpr.span.clone();
let base_expr = slice_base(texpr.clone());
let start_expr = slice_start(texpr.clone());
let end_expr = slice_end(texpr.clone());
let base_result = infer_expr(base_expr, scope.clone(), None);
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let start_result = infer_expr(start_expr, scope.clone(), None);
let start_typed = start_result.typed.clone();
let start_diags = start_result.diagnostics.clone();
let end_result = infer_expr(end_expr, scope.clone(), None);
let end_typed = end_result.typed.clone();
let end_diags = end_result.diagnostics.clone();
let base_type_result = rt_node(base_typed.clone());
let start_type_result = rt_node(start_typed.clone());
let end_type_result = rt_node(end_typed.clone());
let slice_check = match (*base_type_result.clone()).clone() {
    NodeType::Typed { node: base_type, .. } => match (*start_type_result.clone()).clone() {
    NodeType::Typed { node: start_type, .. } => match (*end_type_result.clone()).clone() {
    NodeType::Typed { node: end_type, .. } => Some(check_slice_access_node(base_type.clone(), start_type.clone(), end_type.clone(), span.clone(), scope.module_name.clone())),
    NodeType::InferError { .. } => None,
    NodeType::InferVariable { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::InferVariable { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::InferVariable { .. } => None,
    NodeType::Untyped => None,
};
let access_failure_source = match (*base_type_result.clone()).clone() {
    NodeType::Typed { .. } => match (*start_type_result.clone()).clone() {
    NodeType::Typed { .. } => end_type_result.clone(),
    NodeType::InferError { .. } => start_type_result.clone(),
    NodeType::InferVariable { .. } => start_type_result.clone(),
    NodeType::Untyped => start_type_result.clone(),
},
    NodeType::InferError { .. } => base_type_result.clone(),
    NodeType::InferVariable { .. } => base_type_result.clone(),
    NodeType::Untyped => base_type_result.clone(),
};
let slc_inferred = match slice_check.clone() {
    Some(checked) => checked.inferred.clone(),
    None => Some(inferred_from_node_type(access_failure_source, "invalid slice access".to_string(), span.clone())),
};
let slice_access_diags = match slice_check.clone() {
    Some(checked) => checked.diagnostics.clone(),
    None => Rc::new(vec![]),
};
let slc_texpr = make_expr_node(Rc::new(ExprData::ExprSlice), Rc::new(vec![base_typed.clone(), start_typed.clone(), end_typed.clone()]), slc_inferred, span.clone());
Rc::new(InferResult {
    typed: slc_texpr,
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(base_diags, start_diags), end_diags), slice_access_diags),
})
},
    ExprData::ExprReturn => {
            let span = texpr.span.clone();
let inner_expr = return_value(texpr.clone());
let inner_result = infer_expr(inner_expr, scope.clone(), None);
let ret_texpr = make_expr_node(Rc::new(ExprData::ExprReturn), Rc::new(vec![inner_result.typed.clone()]), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(inner_result.typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: ret_texpr,
    diagnostics: inner_result.diagnostics.clone(),
})
},
    _ => {
            let span = texpr.span.clone();
Rc::new(InferResult {
    typed: make_expr_error_node(ExprErrorKind::InternalExprError, "unhandled expression variant in infer_expr".to_string(), span.clone()),
    diagnostics: Rc::new(vec![inference_error("unhandled expression variant in infer_expr".to_string(), span.clone(), scope.module_name.clone())]),
})
},
}
    })
}

pub fn infer_record_lit(type_name: Option<String>, field_inits: Rc<Vec<Rc<Node>>>, span: Rc<SourceSpan>, scope: Rc<InferScope>) -> Rc<InferResult> {
    {
        let struct_def = match type_name.clone() {
    Some(tn) => lookup_type(scope.type_env.clone(), tn.clone()),
    None => None,
};
let struct_fields = match struct_def {
    Some(sd) => sd.children.clone(),
    None => Rc::new(vec![]),
};
let fi_infer_results = Rc::new({ let mut __result = Vec::new(); for fi in field_inits.iter().cloned() { __result.push({
            let fi_name = field_init_node_name(fi.clone());
let field_expected = match Rc::new({ let mut __result = Vec::new(); for sf in struct_fields.clone().iter().cloned() { if (sf.name.clone().as_str() == fi_name.clone().as_str()) { __result.push(sf); } } __result }).first().cloned() {
    Some(sf) => {
                let ft = rt_type(sf.clone());
if (ft.name.clone().as_str() != "".to_string().as_str()) {
                    Some(ft.clone())
} else {
                    None
}
},
    None => None,
};
let ar = infer_expr(field_init_node_value(fi.clone()), scope.clone(), field_expected.clone());
let ar_typed = ar.typed.clone();
let ar_diags = ar.diagnostics.clone();
Rc::new(FieldInferResult {
    typed_field: make_field_init_node(fi_name.clone(), ar_typed.clone(), fi.span.clone()),
    infer_result: ar.clone(),
    diagnostics: ar_diags.clone(),
})
}); } __result });
let typed_fields = Rc::new({ let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.push(fir.typed_field.clone()); } __result });
let fi_diags = Rc::new({ let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.extend((*fir.diagnostics.clone()).iter().cloned()); } __result });
if (type_name.clone() == None) {
            {
                let child_nodes = Rc::new({ let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.push(Rc::new(Node {
    name: field_init_node_name(fir.typed_field.clone()),
    span: no_span(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: rt_type(fir.infer_result.clone().typed.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})); } __result });
let anon_node = Rc::new(Node {
    name: "".to_string(),
    span: no_span(),
    ident_span: None,
    children: child_nodes,
    connective: Connective::Conj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let typed_field_nodes = typed_fields;
let texpr = make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), typed_field_nodes, Some(Rc::new(InferredNode::Resolved {
    node: anon_node,
})), span.clone());
Rc::new(InferResult {
    typed: texpr,
    diagnostics: fi_diags,
})
}
} else {
            {
                let type_lookup = lookup_type(scope.type_env.clone(), type_name.clone().unwrap());
let local_variant_parent = lookup_variant_parent_enum(scope.clone(), type_name.clone().unwrap());
let effective_lookup = match type_lookup.clone() {
    Some(_) => type_lookup.clone(),
    None => {
                    let local_lookup = lookup_in_scope(scope.locals.clone(), type_name.clone().unwrap());
match local_lookup {
    Some(local_node) => lookup_type_for(scope.type_env.clone(), local_node.clone()),
    None => None,
}
},
};
let raw_resolved = match effective_lookup.clone() {
    Some(tn) => tn.clone(),
    None => error_type(),
};
let expected_optional_parent = Some("Optional".to_string());
let is_some_ctor = ((type_name.clone().unwrap().as_str() == "Some".to_string().as_str()) && (local_variant_parent.clone().as_deref() == expected_optional_parent.as_deref()));
let resolved_node = if is_some_ctor {
                    {
                        let val_field = Rc::new({ let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { if (field_init_node_name(fir.typed_field.clone()).as_str() == "value".to_string().as_str()) { __result.push(fir); } } __result }).first().cloned();
match val_field {
    Some(val_fir) => with_optional_cardinality(rt_type(val_fir.infer_result.clone().typed.clone())),
    None => raw_resolved,
}
}
} else {
                    raw_resolved
};
let type_diags = match effective_lookup.clone() {
    Some(_) => Rc::new(vec![]),
    None => Rc::new(vec![inference_error(v2_rt::concat(v2_rt::concat("type '".to_string(), type_name.clone().unwrap()), "' not found in scope".to_string()), span.clone(), scope.module_name.clone())]),
};
let typed_field_nodes2 = typed_fields;
let rl_name = match type_name.clone() {
    Some(tn_val) => tn_val.clone(),
    None => "".to_string(),
};
let texpr = make_named_expr_node(rl_name, Rc::new(ExprData::ExprRecordLit {
    parent_enum: local_variant_parent.clone(),
}), typed_field_nodes2, Some(Rc::new(InferredNode::Resolved {
    node: resolved_node,
})), span.clone());
Rc::new(InferResult {
    typed: texpr,
    diagnostics: v2_rt::concat(fi_diags, type_diags),
})
}
}
}
}

pub fn infer_item(item: Rc<Node>, scope: Rc<InferScope>) -> Rc<TypedItemResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let typed_anno = item.type_annotation.clone();
if ((item.connective.clone() != Connective::NoConnective) && (item.transport.clone() == None)) {
                Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new({ let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result }),
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: unit_type(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: item.connective.clone(),
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: Rc::new(vec![]),
})
} else {
                if ((item.body.clone() != None) && ((item.params.clone().len() as i64) > 0)) {
                    {
                        let fn_scope = build_params_scope(scope.clone(), item.params.clone());
let fn_scope = item.uses.clone().iter().cloned().fold(fn_scope, |s: Rc<InferScope>, u: Rc<Node>| extend_scope(s.clone(), resource_use_name(u.clone()), resource_use_resource(u.clone())));
let fn_return_expected = if (item.inferred.clone() != None) {
                            Some(rt_type(item.clone()))
} else {
                            None
};
let body_result = infer_expr(item.body.clone().clone().unwrap(), fn_scope, fn_return_expected);
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let resolved_ret = if (item.inferred.clone() == None) {
                            rt_type(body_typed.clone())
} else {
                            rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new({ let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result }),
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret,
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: Some(body_typed.clone()),
    connective: Connective::NoConnective,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: expr_has_self_call(body_typed.clone(), item.name.clone()),
    has_non_tail_self_call: expr_has_non_tail_self_call(body_typed.clone(), item.name.clone(), true),
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: body_diags,
})
}
} else {
                    if (((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.inferred.clone() != None)) {
                        {
                            let fn_scope = scope.clone();
let body_result = infer_expr(item.body.clone().clone().unwrap(), fn_scope, Some(rt_type(item.clone())));
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let resolved_ret = rt_type(item.clone());
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret,
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: Some(body_typed.clone()),
    connective: Connective::NoConnective,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: body_diags,
})
}
} else {
                        if ((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) {
                            {
                                let data_expected = if (item.type_annotation.clone() != None) {
                                    Some(item.type_annotation.clone().clone().unwrap())
} else {
                                    None
};
let val_result = infer_expr(item.body.clone().clone().unwrap(), scope.clone(), data_expected);
let val_typed = val_result.typed.clone();
let val_diags = val_result.diagnostics.clone();
let resolved_ret = if (item.type_annotation.clone() == None) {
                                    rt_type(val_typed.clone())
} else {
                                    item.type_annotation.clone().clone().unwrap()
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret,
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: Some(val_typed.clone()),
    connective: Connective::NoConnective,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: val_diags,
})
}
} else {
                            if (((item.params.clone().len() as i64) > 0) && (item.body.clone() == None)) {
                                {
                                    let resolved_ret = if (item.inferred.clone() == None) {
                                        unit_type()
} else {
                                        rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new({ let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result }),
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret,
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: Connective::NoConnective,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: Rc::new(vec![]),
})
}
} else {
                                {
                                    let resolved_ret = if (item.inferred.clone() == None) {
                                        unit_type()
} else {
                                        rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new({ let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result }),
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret,
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: item.connective.clone(),
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: Rc::new(vec![]),
})
}
}
}
}
}
}
}
    })
}

pub fn infer_items(items: Rc<Vec<Rc<Node>>>, scope: Rc<InferScope>) -> Rc<Vec<Rc<TypedItemResult>>> {
    Rc::new({ let mut __result = Vec::new(); for item in items.iter().cloned() { __result.push(infer_item(item.clone(), scope.clone())); } __result })
}

pub fn collect_filtered_bindings(envs: Rc<Vec<Rc<TypeEnv>>>, placeholder_names: Rc<HashMap<String, bool>>) -> Rc<HashMap<String, Rc<TypeBinding>>> {
    envs.iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, env: Rc<TypeEnv>| Rc::new(v2_rt::map_keys(&env.bindings.clone())).iter().cloned().fold(acc.clone(), |bacc: Rc<HashMap<String, Rc<TypeBinding>>>, name: String| if is_bridge_placeholder(placeholder_names.clone(), name.clone()) {
        bacc.clone()
} else {
        match v2_rt::map_get(&env.bindings.clone(), name.clone()) {
    Some(binding) => v2_rt::rc_map_insert(bacc.clone(), name.clone(), binding.clone()),
    None => bacc.clone(),
}
}))
}

pub fn build_type_env(module: Rc<ResolvedModule>, parent_index: Rc<HashMap<String, Rc<TypedModule>>>, source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<BuildTypeEnvResult> {
    {
        let placeholder_names = bridge_placeholder_type_names();
let zero_span = make_span(0, 0);
let source_index = v2_rt::map_get(&source_indices, module.module.clone().span.clone().file.clone());
let kernel_bindings_base = Rc::new(v2_rt::map_keys(&kernel_type_set())).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: Rc::new(Node {
    name: name.clone(),
    span: no_span(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
})));
let kernel_bindings = v2_rt::rc_map_insert(kernel_bindings_base, "Unit".to_string(), Rc::new(TypeBinding {
    name: "Unit".to_string(),
    resolved: Rc::new(Node {
    name: "Unit".to_string(),
    span: no_span(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::Conj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
}));
let some_value_field = Rc::new(Node {
    name: "value".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::TypeVariable {
    id: "some_value".to_string(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let some_variant = Rc::new(Node {
    name: "Some".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![some_value_field]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_optional = Rc::new(Node {
    name: "Optional".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![some_variant, none_type()]),
    connective: Connective::Disj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_bindings = v2_rt::rc_map_insert(kernel_bindings.clone(), "Optional".to_string(), Rc::new(TypeBinding {
    name: "Optional".to_string(),
    resolved: kernel_optional,
}));
let kernel = Rc::new(TypeEnv {
    bindings: kernel_bindings.clone(),
    recursive_types: Rc::new(vec![]),
    recursive_type_set: v2_rt::rc_empty_map::<bool>(),
    recursive_variant_fields: v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(),
    source_index: source_index.clone(),
});
let imports_std_types = { let mut __found = false; for imp in module.resolved_imports.clone().iter().cloned() { if (imp.module_path.clone().as_str() == "std.types".to_string().as_str()) { __found = true; break; } } __found };
let std_types_parent_env = if ((module.module.clone().name.clone().as_str() != "std.types".to_string().as_str()) && (imports_std_types == false)) {
            match v2_rt::map_get(&parent_index, "std.types".to_string()) {
    Some(typed_std) => Rc::new(vec![typed_std.type_env.clone()]),
    None => Rc::new(vec![]),
}
} else {
            Rc::new(vec![])
};
let imported_parent_envs = Rc::new({ let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend((*match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => Rc::new(vec![typed_parent.type_env.clone()]),
    None => Rc::new(vec![]),
}).iter().cloned()); } __result });
let parent_envs = v2_rt::concat(std_types_parent_env.clone(), imported_parent_envs);
let std_import_bindings = collect_filtered_bindings(std_types_parent_env.clone(), placeholder_names.clone());
let import_bindings = module.resolved_imports.clone().iter().cloned().fold(std_import_bindings.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, imp: Rc<ResolvedImport>| match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => if (imp.module_path.clone().as_str() == "std.types".to_string().as_str()) {
            Rc::new(v2_rt::map_keys(&typed_parent.type_env.clone().bindings.clone())).iter().cloned().fold(acc.clone(), |bacc: Rc<HashMap<String, Rc<TypeBinding>>>, name: String| if is_bridge_placeholder(placeholder_names.clone(), name.clone()) {
                bacc.clone()
} else {
                match v2_rt::map_get(&typed_parent.type_env.clone().bindings.clone(), name.clone()) {
    Some(binding) => v2_rt::rc_map_insert(bacc.clone(), name.clone(), binding.clone()),
    None => bacc.clone(),
}
})
} else {
            v2_rt::rc_map_merge(acc.clone(), typed_parent.type_env.clone().bindings.clone())
},
    None => acc.clone(),
});
let import_recursive = parent_envs.clone().iter().cloned().fold(Rc::new(vec![]), |acc: _, env: Rc<TypeEnv>| v2_rt::concat(acc.clone(), env.recursive_types.clone()));
let import_recursive_set = parent_envs.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, env: Rc<TypeEnv>| v2_rt::rc_map_merge(acc.clone(), env.recursive_type_set.clone()));
let import_recursive_variant_fields = parent_envs.clone().iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(), |acc: Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>>, env: Rc<TypeEnv>| merge_recursive_variant_fields(acc.clone(), env.recursive_variant_fields.clone()));
let import_env = Rc::new(TypeEnv {
    bindings: import_bindings,
    recursive_types: import_recursive,
    recursive_type_set: import_recursive_set,
    recursive_variant_fields: import_recursive_variant_fields,
    source_index: source_index.clone(),
});
let import_diags = Rc::new({ let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend((*match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(_) => Rc::new(vec![]),
    None => Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("missing parent environment for imported module '".to_string(), imp.module_path.clone()), "' while typechecking '".to_string()), module.module.clone().name.clone()), "'".to_string()),
    span: imp.target_module.clone().clone().unwrap().span.clone(),
}), module.module.clone().name.clone())]),
}).iter().cloned()); } __result });
let local_bindings = module_items(module.module.clone()).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, item: Rc<Node>| {
            let has_structure = (item.connective.clone() != Connective::NoConnective);
if has_structure.clone() {
                {
                    let type_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: item.children.clone(),
    connective: item.connective.clone(),
    params: item.params.clone(),
    inferred: None,
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: type_node.clone(),
}))
}
} else {
                if (((item.inferred.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.body.clone() == None)) {
                    {
                        let alias_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: item.inferred.clone(),
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: alias_node.clone(),
}))
}
} else {
                    if ((item.transport.clone() == None) && ((item.children.clone().len() as i64) > 0)) {
                        {
                            let ref_node = nominal_ref_node(item.name.clone(), item.span.clone(), item.ident_span.clone());
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: ref_node.clone(),
}))
}
} else {
                        if (((((item.params.clone().len() as i64) > 0) && (item.connective.clone() == Connective::NoConnective)) && (item.body.clone() == None)) && (item.transport.clone() == None)) {
                            {
                                let bare_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: item.params.clone(),
    inferred: item.inferred.clone(),
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: bare_node.clone(),
}))
}
} else {
                            if ((((((item.properties.clone().len() as i64) > 0) && (item.connective.clone() == Connective::NoConnective)) && (item.transport.clone() == None)) && (item.inferred.clone() == None)) && ((item.params.clone().len() as i64) == 0)) {
                                v2_rt::rc_map_insert(acc.clone(), item.name.clone(), nominal_type_binding(item.name.clone()))
} else {
                                acc.clone()
}
}
}
}
}
});
let param_bindings = module_items(module.module.clone()).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, item: Rc<Node>| {
            let is_type_decl = match v2_rt::map_get(&local_bindings, item.name.clone()) {
    Some(_) => true,
    None => false,
};
if (((item.params.clone().len() as i64) > 0) && is_type_decl.clone()) {
                {
                    let result = item.params.clone().iter().cloned().fold(acc.clone(), |pacc: Rc<HashMap<String, Rc<TypeBinding>>>, p: Rc<Node>| v2_rt::rc_map_insert(pacc.clone(), p.name.clone(), nominal_type_binding(p.name.clone())));
result.clone()
}
} else {
                acc.clone()
}
});
let local_name_set = Rc::new(v2_rt::map_keys(&local_bindings)).iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), true));
let all_local_bindings = v2_rt::rc_map_merge(local_bindings.clone(), param_bindings);
let pre_local_env = Rc::new(TypeEnv {
    bindings: all_local_bindings,
    recursive_types: Rc::new(vec![]),
    recursive_type_set: v2_rt::rc_empty_map::<bool>(),
    recursive_variant_fields: v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(),
    source_index: source_index.clone(),
});
let merged = merge_envs(Rc::new(vec![kernel, import_env, pre_local_env]));
let all_deps_map = Rc::new(v2_rt::map_values(&merged.bindings.clone())).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<String>>>(), |acc: Rc<HashMap<String, Rc<Vec<String>>>>, b: Rc<TypeBinding>| v2_rt::rc_map_insert(acc.clone(), b.name.clone(), node_type_deps(b.resolved.clone())));
let cycle_set = detect_type_cycles_kahn(all_deps_map.clone(), merged.bindings.clone());
let cycle_map = cycle_set.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), true));
let local_recursive_variant_fields = build_item_recursive_variant_fields(module_items(module.module.clone()), cycle_map.clone());
let merged_recursive_variant_fields = merge_recursive_variant_fields(merged.recursive_variant_fields.clone(), local_recursive_variant_fields);
let unresolved_env = Rc::new(TypeEnv {
    bindings: merged.bindings.clone(),
    recursive_types: cycle_set.clone(),
    recursive_type_set: cycle_map.clone(),
    recursive_variant_fields: merged_recursive_variant_fields,
    source_index: source_index.clone(),
});
let resolved = resolve_env_bindings(unresolved_env, module.module.clone().name.clone(), local_name_set, all_deps_map.clone());
let resolved_env_out = resolved.env.clone();
let resolved_diags = resolved.diagnostics.clone();
let final_env = Rc::new(TypeEnv {
    bindings: resolved_env_out.bindings.clone(),
    recursive_types: resolved_env_out.recursive_types.clone(),
    recursive_type_set: resolved_env_out.recursive_type_set.clone(),
    recursive_variant_fields: resolved_env_out.recursive_variant_fields.clone(),
    source_index: resolved_env_out.source_index.clone(),
});
Rc::new(BuildTypeEnvResult {
    env: final_env,
    diagnostics: v2_rt::concat(import_diags, resolved_diags),
})
}
}

pub fn build_type_env_unresolved(module: Rc<ResolvedModule>, parent_index: Rc<HashMap<String, Rc<TypedModule>>>, source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<BuildTypeEnvResult> {
    {
        let placeholder_names = bridge_placeholder_type_names();
let zero_span = make_span(0, 0);
let source_index = v2_rt::map_get(&source_indices, module.module.clone().span.clone().file.clone());
let kernel_bindings = Rc::new(v2_rt::map_keys(&kernel_type_set())).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: Rc::new(Node {
    name: name.clone(),
    span: no_span(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
})));
let some_value_field = Rc::new(Node {
    name: "value".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::TypeVariable {
    id: "some_value".to_string(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let some_variant = Rc::new(Node {
    name: "Some".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![some_value_field]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_optional = Rc::new(Node {
    name: "Optional".to_string(),
    span: zero_span.clone(),
    ident_span: None,
    children: Rc::new(vec![some_variant, none_type()]),
    connective: Connective::Disj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_bindings = v2_rt::rc_map_insert(kernel_bindings.clone(), "Optional".to_string(), Rc::new(TypeBinding {
    name: "Optional".to_string(),
    resolved: kernel_optional,
}));
let kernel = Rc::new(TypeEnv {
    bindings: kernel_bindings.clone(),
    recursive_types: Rc::new(vec![]),
    recursive_type_set: v2_rt::rc_empty_map::<bool>(),
    recursive_variant_fields: v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(),
    source_index: source_index.clone(),
});
let imports_std_types = { let mut __found = false; for imp in module.resolved_imports.clone().iter().cloned() { if (imp.module_path.clone().as_str() == "std.types".to_string().as_str()) { __found = true; break; } } __found };
let std_types_parent_env = if ((module.module.clone().name.clone().as_str() != "std.types".to_string().as_str()) && (imports_std_types == false)) {
            match v2_rt::map_get(&parent_index, "std.types".to_string()) {
    Some(typed_std) => Rc::new(vec![typed_std.type_env.clone()]),
    None => Rc::new(vec![]),
}
} else {
            Rc::new(vec![])
};
let imported_parent_envs = Rc::new({ let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend((*match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => Rc::new(vec![typed_parent.type_env.clone()]),
    None => Rc::new(vec![]),
}).iter().cloned()); } __result });
let parent_envs = v2_rt::concat(std_types_parent_env.clone(), imported_parent_envs);
let std_import_bindings = collect_filtered_bindings(std_types_parent_env.clone(), placeholder_names.clone());
let import_bindings = module.resolved_imports.clone().iter().cloned().fold(std_import_bindings.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, imp: Rc<ResolvedImport>| match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => if (imp.module_path.clone().as_str() == "std.types".to_string().as_str()) {
            Rc::new(v2_rt::map_keys(&typed_parent.type_env.clone().bindings.clone())).iter().cloned().fold(acc.clone(), |bacc: Rc<HashMap<String, Rc<TypeBinding>>>, name: String| if is_bridge_placeholder(placeholder_names.clone(), name.clone()) {
                bacc.clone()
} else {
                match v2_rt::map_get(&typed_parent.type_env.clone().bindings.clone(), name.clone()) {
    Some(binding) => v2_rt::rc_map_insert(bacc.clone(), name.clone(), binding.clone()),
    None => bacc.clone(),
}
})
} else {
            v2_rt::rc_map_merge(acc.clone(), typed_parent.type_env.clone().bindings.clone())
},
    None => acc.clone(),
});
let import_recursive = parent_envs.clone().iter().cloned().fold(Rc::new(vec![]), |acc: _, env: Rc<TypeEnv>| v2_rt::concat(acc.clone(), env.recursive_types.clone()));
let import_recursive_set = parent_envs.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, env: Rc<TypeEnv>| v2_rt::rc_map_merge(acc.clone(), env.recursive_type_set.clone()));
let import_recursive_variant_fields = parent_envs.clone().iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(), |acc: Rc<HashMap<String, Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>>, env: Rc<TypeEnv>| merge_recursive_variant_fields(acc.clone(), env.recursive_variant_fields.clone()));
let import_env = Rc::new(TypeEnv {
    bindings: import_bindings,
    recursive_types: import_recursive,
    recursive_type_set: import_recursive_set,
    recursive_variant_fields: import_recursive_variant_fields,
    source_index: source_index.clone(),
});
let local_bindings = module_items(module.module.clone()).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<TypeBinding>>(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, item: Rc<Node>| {
            let has_structure = (item.connective.clone() != Connective::NoConnective);
if has_structure.clone() {
                {
                    let type_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: item.children.clone(),
    connective: item.connective.clone(),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: type_node.clone(),
}))
}
} else {
                if (((item.inferred.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.body.clone() == None)) {
                    {
                        let alias_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: item.inferred.clone(),
    return_cardinality: item.return_cardinality.clone(),
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: alias_node.clone(),
}))
}
} else {
                    if ((item.transport.clone() == None) && ((item.children.clone().len() as i64) > 0)) {
                        {
                            let ref_node = nominal_ref_node(item.name.clone(), item.span.clone(), item.ident_span.clone());
v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: ref_node.clone(),
}))
}
} else {
                        if ((((((item.properties.clone().len() as i64) > 0) && (item.connective.clone() == Connective::NoConnective)) && (item.transport.clone() == None)) && (item.inferred.clone() == None)) && ((item.params.clone().len() as i64) == 0)) {
                            v2_rt::rc_map_insert(acc.clone(), item.name.clone(), nominal_type_binding(item.name.clone()))
} else {
                            acc.clone()
}
}
}
}
});
let pre_local_env = Rc::new(TypeEnv {
    bindings: local_bindings,
    recursive_types: Rc::new(vec![]),
    recursive_type_set: v2_rt::rc_empty_map::<bool>(),
    recursive_variant_fields: v2_rt::rc_empty_map::<Rc<Vec<Rc<RecursiveVariantFieldWitness>>>>(),
    source_index: source_index.clone(),
});
let merged = merge_envs(Rc::new(vec![kernel, import_env, pre_local_env]));
let all_deps_map = Rc::new(v2_rt::map_values(&merged.bindings.clone())).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Vec<String>>>(), |acc: Rc<HashMap<String, Rc<Vec<String>>>>, b: Rc<TypeBinding>| v2_rt::rc_map_insert(acc.clone(), b.name.clone(), node_type_deps(b.resolved.clone())));
let cycle_set = detect_type_cycles_kahn(all_deps_map, merged.bindings.clone());
let cycle_map = cycle_set.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), true));
let local_recursive_variant_fields = build_item_recursive_variant_fields(module_items(module.module.clone()), cycle_map.clone());
let merged_recursive_variant_fields = merge_recursive_variant_fields(merged.recursive_variant_fields.clone(), local_recursive_variant_fields);
let unresolved_env = Rc::new(TypeEnv {
    bindings: merged.bindings.clone(),
    recursive_types: cycle_set.clone(),
    recursive_type_set: cycle_map.clone(),
    recursive_variant_fields: merged_recursive_variant_fields,
    source_index: source_index.clone(),
});
Rc::new(BuildTypeEnvResult {
    env: unresolved_env,
    diagnostics: Rc::new(vec![]),
})
}
}

pub fn build_item_info(item: Rc<Node>) -> Rc<ItemInfo> {
    {
        let kind = item_kind(item.clone());
let res_names = Rc::new({ let mut __result = Vec::new(); for u in item.uses.clone().iter().cloned() { __result.push(resource_use_name(u.clone())); } __result });
match kind.clone() {
    ItemKind::FuncItem => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: if (item.body.clone() == None) {
            Rc::new(vec![])
} else {
            collect_typed_service_calls(item.body.clone().clone().unwrap())
},
    resource_names: res_names,
    params: item.params.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
}),
    ItemKind::FnItem => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: Rc::new(vec![]),
    resource_names: res_names,
    params: item.params.clone(),
    is_self_recursive: if (item.body.clone() == None) {
            false
} else {
            expr_has_self_call(item.body.clone().clone().unwrap(), item.name.clone())
},
    has_non_tail_self_call: if (item.body.clone() == None) {
            false
} else {
            expr_has_non_tail_self_call(item.body.clone().clone().unwrap(), item.name.clone(), true)
},
}),
    _ => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: Rc::new(vec![]),
    resource_names: res_names,
    params: item.params.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
}),
}
}
}

pub fn analyze_item(item: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ItemContribution> {
    {
        let resolved = resolve_item_types(item, env, module_name);
let ritem = resolved.item.clone();
let is_func = ((ritem.params.clone().len() as i64) > 0);
let is_zero_arg_func = ((((ritem.params.clone().len() as i64) == 0) && (ritem.inferred.clone() != None)) && (ritem.body.clone() != None));
let func_sig = if (is_func || is_zero_arg_func) {
            {
                let declared_rt = if (ritem.inferred.clone() != None) {
                    Some(rt_type(ritem.clone()))
} else {
                    None
};
Some(Rc::new(DeclaredFuncSig {
    name: ritem.name.clone(),
    params: ritem.params.clone(),
    inferred: declared_rt,
    is_async: ((ritem.uses.clone().len() as i64) > 0),
}))
}
} else {
            None
};
let svc_entries = if ((ritem.transport.clone() != None) && ((ritem.children.clone().len() as i64) > 0)) {
            Rc::new({ let mut __result = Vec::new(); for c in ritem.children.clone().iter().cloned() { __result.push(service_op_entry(c.clone())); } __result })
} else {
            Rc::new(vec![])
};
let svc_local = if ((ritem.transport.clone() != None) && ((ritem.children.clone().len() as i64) > 0)) {
            {
                let root = namespace_root_from_properties(ritem.properties.clone(), ritem.name.clone());
Some(nominal_type_binding(root))
}
} else {
            None
};
Rc::new(ItemContribution {
    resolved_item: ritem.clone(),
    resolve_diagnostics: resolved.diagnostics.clone(),
    func_sig: func_sig,
    svc_entries: svc_entries,
    svc_local: svc_local,
    item_info: build_item_info(ritem.clone()),
})
}
}

pub fn fold_module_contributions(mut remaining: Rc<Vec<Rc<ItemContribution>>>, mut resolved_items: Rc<Vec<Rc<Node>>>, mut func_sigs: Rc<HashMap<String, Rc<DeclaredFuncSig>>>, mut svc_registry: Rc<HashMap<String, Rc<Vec<Rc<OpEntry>>>>>, mut svc_locals: Rc<HashMap<String, Rc<TypeBinding>>>, mut item_registry: Rc<HashMap<String, Rc<ItemInfo>>>, mut diag_chunks: Rc<Vec<Rc<Vec<Rc<ErrorNode>>>>>) -> Rc<LocalContributionState> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(LocalContributionState {
    resolved_items: resolved_items.clone(),
    func_sigs: func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
    item_registry: item_registry.clone(),
    diag_chunks: diag_chunks.clone(),
}); },
    Some(contribution) => { let next_func_sigs = match contribution.func_sig.clone() {
    Some(sig) => v2_rt::rc_map_insert(func_sigs.clone(), sig.name.clone(), sig.clone()),
    None => func_sigs.clone(),
};
let next_svc_registry = if ((contribution.svc_entries.clone().len() as i64) > 0) {
            v2_rt::rc_map_insert(svc_registry.clone(), contribution.resolved_item.clone().name.clone(), contribution.svc_entries.clone())
} else {
            svc_registry.clone()
};
let next_svc_locals = match contribution.svc_local.clone() {
    Some(binding) => v2_rt::rc_map_insert(svc_locals.clone(), binding.name.clone(), binding.clone()),
    None => svc_locals.clone(),
};
{
            let __tco_0 = Rc::new(remaining.iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let __tco_1 = v2_rt::rc_list_push(resolved_items, contribution.resolved_item.clone());
let __tco_2 = next_func_sigs.clone();
let __tco_3 = next_svc_registry.clone();
let __tco_4 = next_svc_locals.clone();
let __tco_5 = v2_rt::rc_map_insert(item_registry, contribution.item_info.clone().name.clone(), contribution.item_info.clone());
let __tco_6 = v2_rt::rc_list_push(diag_chunks, contribution.resolve_diagnostics.clone());
remaining = __tco_0;
resolved_items = __tco_1;
func_sigs = __tco_2;
svc_registry = __tco_3;
svc_locals = __tco_4;
item_registry = __tco_5;
diag_chunks = __tco_6;
continue;
} },
}
}
}

pub fn build_module_context(contributions: Rc<Vec<Rc<ItemContribution>>>, parent_index: Rc<HashMap<String, Rc<TypedModule>>>, resolved_imports: Rc<Vec<Rc<ResolvedImport>>>, env: Rc<TypeEnv>, module_name: String) -> Rc<ModuleContext> {
    {
        let local = fold_module_contributions(contributions, Rc::new(vec![]), v2_rt::rc_empty_map::<Rc<DeclaredFuncSig>>(), v2_rt::rc_empty_map::<Rc<Vec<Rc<OpEntry>>>>(), v2_rt::rc_empty_map::<Rc<TypeBinding>>(), v2_rt::rc_empty_map::<Rc<ItemInfo>>(), Rc::new(vec![]));
let imported_enum_names = resolved_imports.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, imp: Rc<ResolvedImport>| imp.specific_names.clone().iter().cloned().fold(acc.clone(), |inner: Rc<HashMap<String, bool>>, n: String| v2_rt::rc_map_insert(inner.clone(), n.clone(), true)));
let variant_fold = Rc::new(v2_rt::map_values(&env.bindings.clone())).iter().cloned().fold(Rc::new(VariantFoldState {
    locals: v2_rt::rc_empty_map::<Rc<TypeBinding>>(),
    collision_errors: Rc::new(vec![]),
}), |acc: Rc<VariantFoldState>, binding: Rc<TypeBinding>| {
            let is_coproduct = (binding.resolved.clone().connective.clone() == Connective::Disj);
if is_coproduct.clone() {
                {
                    let curr_is_imported = (v2_rt::map_get(&imported_enum_names, binding.resolved.clone().name.clone()) != None);
binding.resolved.clone().children.clone().iter().cloned().fold(acc.clone(), |vacc: Rc<VariantFoldState>, child: Rc<Node>| match v2_rt::map_get(&vacc.locals.clone(), child.name.clone()) {
    Some(prev) => {
                        let prev_is_imported = (v2_rt::map_get(&imported_enum_names, prev.resolved.clone().name.clone()) != None);
if (curr_is_imported.clone() && prev_is_imported.clone()) {
                            Rc::new(VariantFoldState {
    locals: vacc.locals.clone(),
    collision_errors: v2_rt::rc_list_push(vacc.collision_errors.clone(), make_error_node(Rc::new(CompilerDiagnostic::VariantCollision {
    variant: child.name.clone(),
    enum1: prev.resolved.clone().name.clone(),
    enum2: binding.resolved.clone().name.clone(),
    span: no_span(),
}), module_name.clone())),
})
} else {
                            if curr_is_imported.clone() {
                                Rc::new(VariantFoldState {
    locals: v2_rt::rc_map_insert(vacc.locals.clone(), child.name.clone(), Rc::new(TypeBinding {
    name: child.name.clone(),
    resolved: binding.resolved.clone(),
})),
    collision_errors: vacc.collision_errors.clone(),
})
} else {
                                vacc.clone()
}
}
},
    None => Rc::new(VariantFoldState {
    locals: v2_rt::rc_map_insert(vacc.locals.clone(), child.name.clone(), Rc::new(TypeBinding {
    name: child.name.clone(),
    resolved: binding.resolved.clone(),
})),
    collision_errors: vacc.collision_errors.clone(),
}),
})
}
} else {
                acc.clone()
}
});
let imported_variant_locals = variant_fold.locals.clone();
let variant_collision_errors = variant_fold.collision_errors.clone();
let env_variant_locals = variant_locals_from_items(local.resolved_items.clone(), imported_variant_locals);
let merged_scope = merge_scope_from_imports(resolved_imports.clone(), parent_index, env.clone(), v2_rt::rc_empty_map::<Rc<DeclaredFuncSig>>(), local.svc_registry.clone(), local.svc_locals.clone());
let all_declared_sigs = v2_rt::rc_map_merge(merged_scope.func_sigs.clone(), local.func_sigs.clone());
let resolve_result = resolve_func_sigs(all_declared_sigs, local.resolved_items.clone(), module_name.clone());
let all_locals = Rc::new(v2_rt::map_values(&merged_scope.svc_locals.clone())).iter().cloned().fold(env_variant_locals.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, binding: Rc<TypeBinding>| v2_rt::rc_map_insert(acc.clone(), binding.name.clone(), binding.clone()));
Rc::new(ModuleContext {
    resolved_items: local.resolved_items.clone(),
    func_env: resolve_result.func_env.clone(),
    svc_registry: merged_scope.svc_registry.clone(),
    locals: all_locals,
    item_registry: local.item_registry.clone(),
    diagnostics: v2_rt::concat(v2_rt::concat(Rc::new({ let mut __result = Vec::new(); for c in local.diag_chunks.clone().iter().cloned() { __result.extend((*c.clone()).iter().cloned()); } __result }), resolve_result.diagnostics.clone()), variant_collision_errors),
})
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypecheckModuleResult {
    pub typed: Rc<TypedModule>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

pub fn typecheck_module(resolved: Rc<ResolvedModule>, parent_index: Rc<HashMap<String, Rc<TypedModule>>>, source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<TypecheckModuleResult> {
    {
        let env_result = build_type_env(resolved.clone(), parent_index.clone(), source_indices);
let env = env_result.env.clone();
let env_diags = env_result.diagnostics.clone();
let env_errors = Rc::new({ let mut __result = Vec::new(); for d in env_diags.clone().iter().cloned() { if is_error_diagnostic(d.diagnostic.clone()) { __result.push(d); } } __result });
if ((env_errors.len() as i64) > 0) {
            return Rc::new(TypecheckModuleResult {
    typed: Rc::new(TypedModule {
    module: resolved.module.clone(),
    items: Rc::new(vec![]),
    type_env: env.clone(),
    func_env: Rc::new(ResolvedFuncEnv {
    signatures: v2_rt::rc_empty_map::<Rc<ResolvedFuncSig>>(),
}),
    item_registry: v2_rt::rc_empty_map::<Rc<ItemInfo>>(),
}),
    diagnostics: env_diags.clone(),
})
}
let contributions = Rc::new({ let mut __result = Vec::new(); for item in module_items(resolved.module.clone()).iter().cloned() { __result.push(analyze_item(item.clone(), env.clone(), resolved.module.clone().name.clone())); } __result });
let ctx = build_module_context(contributions, parent_index.clone(), resolved.resolved_imports.clone(), env.clone(), resolved.module.clone().name.clone());
let data_locals = ctx.resolved_items.clone().iter().cloned().fold(ctx.locals.clone(), |acc: Rc<HashMap<String, Rc<TypeBinding>>>, item: Rc<Node>| if ((((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.inferred.clone() == None)) && (item.type_annotation.clone() != None)) {
            v2_rt::rc_map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: item.type_annotation.clone().clone().unwrap(),
}))
} else {
            acc.clone()
});
let infer_scope = Rc::new(InferScope {
    type_env: env.clone(),
    func_env: ctx.func_env.clone(),
    locals: data_locals,
    match_bound_names: v2_rt::rc_empty_map::<bool>(),
    module_name: resolved.module.clone().name.clone(),
    service_registry: ctx.svc_registry.clone(),
    item_registry: ctx.item_registry.clone(),
});
let typed_item_results = infer_items(ctx.resolved_items.clone(), infer_scope);
let typed_items = Rc::new({ let mut __result = Vec::new(); for tir in typed_item_results.clone().iter().cloned() { __result.push(tir.item.clone()); } __result });
let infer_diags = Rc::new({ let mut __result = Vec::new(); for tir in typed_item_results.clone().iter().cloned() { __result.extend((*tir.diagnostics.clone()).iter().cloned()); } __result });
let typed_module = module_node(resolved.module.clone().name.clone(), module_imports(resolved.module.clone()), ctx.resolved_items.clone(), resolved.module.clone().span.clone());
Rc::new(TypecheckModuleResult {
    typed: Rc::new(TypedModule {
    module: typed_module,
    items: typed_items,
    type_env: env.clone(),
    func_env: ctx.func_env.clone(),
    item_registry: ctx.item_registry.clone(),
}),
    diagnostics: v2_rt::concat(v2_rt::concat(env_diags.clone(), ctx.diagnostics.clone()), infer_diags),
})
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct EnvResolveResult {
    pub env: Rc<TypeEnv>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BindingResult {
    pub binding: Rc<TypeBinding>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BindingsAccum {
    pub bindings: Rc<HashMap<String, Rc<TypeBinding>>>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

pub fn resolve_env_bindings(env: Rc<TypeEnv>, module_name: String, local_names: Rc<HashMap<String, bool>>, deps_map: Rc<HashMap<String, Rc<Vec<String>>>>) -> Rc<EnvResolveResult> {
    {
        let remaining = Rc::new({ let mut __result = Vec::new(); for b in Rc::new({ let mut __result = Vec::new(); for b in Rc::new(v2_rt::map_values(&env.bindings.clone())).iter().cloned() { if emit_map_has(local_names.clone(), b.name.clone()) { __result.push(b); } } __result }).iter().cloned() { __result.push(b.name.clone()); } __result });
topo_resolve_types(remaining.clone(), env.clone(), module_name, Rc::new(vec![]), local_names.clone(), deps_map, (remaining.clone().len() as i64))
}
}

pub fn topo_resolve_types(mut remaining: Rc<Vec<String>>, mut env: Rc<TypeEnv>, mut module_name: String, mut diagnostics: Rc<Vec<Rc<ErrorNode>>>, mut local_names: Rc<HashMap<String, bool>>, mut deps_map: Rc<HashMap<String, Rc<Vec<String>>>>, mut fuel: i64) -> Rc<EnvResolveResult> {
    loop {
        if ((remaining.clone().len() as i64) == 0) {
            return Rc::new(EnvResolveResult {
    env: env.clone(),
    diagnostics: diagnostics.clone(),
})
}
let remaining_set = remaining.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), true));
let ready = Rc::new({ let mut __result = Vec::new(); for name in remaining.clone().iter().cloned() { if match v2_rt::map_get(&deps_map, name.clone()) {
    Some(deps) => { let mut __all = true; for dep in deps.clone().iter().cloned() { if !(((((is_kernel_type(dep.clone()) || (dep.clone().as_str() == "None".to_string().as_str())) || (dep.clone().as_str() == "".to_string().as_str())) || is_recursive_type(env.clone(), dep.clone())) || (emit_map_has(remaining_set.clone(), dep.clone()) == false))) { __all = false; break; } } __all },
    None => true,
} { __result.push(name); } } __result });
if ((ready.clone().len() as i64) == 0) {
            {
                let stuck_accum = remaining.clone().iter().cloned().fold(Rc::new(BindingsAccum {
    bindings: env.bindings.clone(),
    diagnostics: Rc::new(vec![]),
}), |acc: Rc<BindingsAccum>, name: String| match v2_rt::map_get(&env.bindings.clone(), name.clone()) {
    Some(binding) => {
                    let result = resolve_node(binding.resolved.clone(), env.clone(), module_name.clone());
Rc::new(BindingsAccum {
    bindings: v2_rt::rc_map_insert(acc.bindings.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: result.resolved.clone(),
})),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), result.diagnostics.clone()),
})
},
    None => acc.clone(),
});
return Rc::new(EnvResolveResult {
    env: Rc::new(TypeEnv {
    bindings: stuck_accum.bindings.clone(),
    recursive_types: env.recursive_types.clone(),
    recursive_type_set: env.recursive_type_set.clone(),
    recursive_variant_fields: env.recursive_variant_fields.clone(),
    source_index: env.source_index.clone(),
}),
    diagnostics: v2_rt::concat(diagnostics.clone(), stuck_accum.diagnostics.clone()),
})
}
}
let ready_accum = ready.clone().iter().cloned().fold(Rc::new(BindingsAccum {
    bindings: env.bindings.clone(),
    diagnostics: Rc::new(vec![]),
}), |acc: Rc<BindingsAccum>, name: String| match v2_rt::map_get(&env.bindings.clone(), name.clone()) {
    Some(binding) => {
            let result = resolve_node(binding.resolved.clone(), env.clone(), module_name.clone());
Rc::new(BindingsAccum {
    bindings: v2_rt::rc_map_insert(acc.bindings.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: result.resolved.clone(),
})),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), result.diagnostics.clone()),
})
},
    None => acc.clone(),
});
let ready_set = ready.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, name: String| v2_rt::rc_map_insert(acc.clone(), name.clone(), true));
let next_remaining = Rc::new({ let mut __result = Vec::new(); for name in remaining.clone().iter().cloned() { if (emit_map_has(ready_set.clone(), name.clone()) == false) { __result.push(name); } } __result });
{
            let __tco_0 = next_remaining.clone();
let __tco_1 = Rc::new(TypeEnv {
    bindings: ready_accum.bindings.clone(),
    recursive_types: env.recursive_types.clone(),
    recursive_type_set: env.recursive_type_set.clone(),
    recursive_variant_fields: env.recursive_variant_fields.clone(),
    source_index: env.source_index.clone(),
});
let __tco_2 = module_name;
let __tco_3 = v2_rt::concat(diagnostics, ready_accum.diagnostics.clone());
let __tco_4 = local_names;
let __tco_5 = deps_map;
let __tco_6 = (fuel - 1);
remaining = __tco_0;
env = __tco_1;
module_name = __tco_2;
diagnostics = __tco_3;
local_names = __tco_4;
deps_map = __tco_5;
fuel = __tco_6;
continue;
}
}
}

pub fn collect_parent_envs(resolved: Rc<ResolvedModule>, module_index: Rc<HashMap<String, Rc<TypedModule>>>) -> Rc<ParentModulesResult> {
    {
        let modules = Rc::new({ let mut __result = Vec::new(); for imp in resolved.resolved_imports.clone().iter().cloned() { __result.extend((*match v2_rt::map_get(&module_index, imp.module_path.clone()) {
    Some(typed) => Rc::new(vec![typed.clone()]),
    None => Rc::new(vec![]),
}).iter().cloned()); } __result });
let diagnostics = Rc::new({ let mut __result = Vec::new(); for imp in resolved.resolved_imports.clone().iter().cloned() { __result.extend((*match v2_rt::map_get(&module_index, imp.module_path.clone()) {
    Some(_) => Rc::new(vec![]),
    None => Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("missing parent environment for imported module '".to_string(), imp.module_path.clone()), "' while ordering '".to_string()), resolved.module.clone().name.clone()), "'".to_string()),
    span: imp.target_module.clone().clone().unwrap().span.clone(),
}), resolved.module.clone().name.clone())]),
}).iter().cloned()); } __result });
Rc::new(ParentModulesResult {
    modules: modules,
    diagnostics: diagnostics,
})
}
}

pub fn build_fielded_variants(modules: Rc<Vec<Rc<TypedModule>>>, type_summaries: Rc<HashMap<String, Rc<TypeSummary>>>) -> Rc<HashMap<String, bool>> {
    {
        let result = modules.iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, m: Rc<TypedModule>| {
            let items = m.items.clone();
items.clone().iter().cloned().fold(acc.clone(), |inner: Rc<HashMap<String, bool>>, item: Rc<Node>| {
                let is_enum = match v2_rt::map_get(&type_summaries, item.name.clone()) {
    Some(summary) => match (*summary.repr.clone()).clone() {
    TypeRepr::EnumRepr { .. } => true,
    _ => false,
},
    None => false,
};
if is_enum.clone() {
                    {
                        let enum_name = item.name.clone();
let variants = item.children.clone();
variants.clone().iter().cloned().fold(inner.clone(), |vacc: Rc<HashMap<String, bool>>, variant: Rc<Node>| {
                            let has_fields = ((variant.children.clone().len() as i64) > 0);
if has_fields.clone() {
                                v2_rt::rc_map_insert(vacc.clone(), v2_rt::concat(v2_rt::concat(enum_name.clone(), "::".to_string()), variant.name.clone()), true)
} else {
                                vacc.clone()
}
})
}
} else {
                    inner.clone()
}
})
});
result
}
}

pub fn build_emit_graph_info(modules: Rc<Vec<Rc<TypedModule>>>) -> Rc<EmitGraphInfo> {
    {
        let init = Rc::new(EmitInfoBuildState {
    type_summaries: v2_rt::rc_empty_map::<Rc<TypeSummary>>(),
});
let built = modules.clone().iter().cloned().fold(init.clone(), |state: Rc<EmitInfoBuildState>, typed_module: Rc<TypedModule>| typed_module.items.clone().iter().cloned().fold(state.clone(), |inner_state: Rc<EmitInfoBuildState>, item: Rc<Node>| add_emit_item_summary(inner_state.clone(), item.clone())));
let all_recursive = modules.clone().iter().cloned().fold(v2_rt::rc_empty_map::<bool>(), |acc: Rc<HashMap<String, bool>>, m: Rc<TypedModule>| v2_rt::rc_map_merge(acc.clone(), m.type_env.clone().recursive_type_set.clone()));
let fielded = build_fielded_variants(modules.clone(), built.type_summaries.clone());
let vtoe = derive_variant_to_enum(built.type_summaries.clone());
Rc::new(EmitGraphInfo {
    type_summaries: built.type_summaries.clone(),
    recursive_type_set: all_recursive,
    fielded_variants: fielded,
    shared_types: v2_rt::rc_empty_map::<bool>(),
    ownership_index: v2_rt::rc_empty_map::<Rc<HashMap<String, bool>>>(),
    movable: v2_rt::rc_empty_map::<bool>(),
    variant_to_enum: vtoe,
    owned_bindings: v2_rt::rc_empty_map::<bool>(),
    fold_eligible_index: v2_rt::rc_empty_map::<Rc<HashMap<String, bool>>>(),
    fold_eligible: v2_rt::rc_empty_map::<bool>(),
})
}
}

pub fn typecheck(graph: Rc<ModuleGraph>, source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<TypedGraph> {
    typecheck_modules(graph.modules.clone(), Rc::new(vec![]), v2_rt::rc_empty_map::<Rc<TypedModule>>(), v2_rt::rc_empty_map::<Rc<ItemInfo>>(), Rc::new(vec![]), source_indices)
}

pub fn typecheck_modules(mut remaining: Rc<Vec<Rc<ResolvedModule>>>, mut modules: Rc<Vec<Rc<TypedModule>>>, mut module_index: Rc<HashMap<String, Rc<TypedModule>>>, mut item_registry: Rc<HashMap<String, Rc<ItemInfo>>>, mut diag_chunks: Rc<Vec<Rc<Vec<Rc<ErrorNode>>>>>, mut source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<TypedGraph> {
    loop {
        match remaining.clone().first().cloned() {
    None => { let expanded_registry = expand_transitive_services(modules.clone(), item_registry.clone(), 5);
break Rc::new(TypedGraph {
    modules: modules.clone(),
    item_registry: expanded_registry.clone(),
    diagnostics: Rc::new({ let mut __result = Vec::new(); for c in diag_chunks.clone().iter().cloned() { __result.extend((*c.clone()).iter().cloned()); } __result }),
}); },
    Some(resolved) => { let parent_result = collect_parent_envs(resolved.clone(), module_index.clone());
let tc_result = typecheck_module(resolved.clone(), module_index.clone(), source_indices.clone());
let typed = tc_result.typed.clone();
let tc_diags = tc_result.diagnostics.clone();
{
            let __tco_0 = Rc::new(remaining.iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let __tco_1 = v2_rt::rc_list_push(modules, typed.clone());
let __tco_2 = v2_rt::rc_map_insert(module_index, typed.module.clone().name.clone(), typed.clone());
let __tco_3 = v2_rt::rc_map_merge(item_registry, typed.item_registry.clone());
let __tco_4 = v2_rt::rc_list_push(v2_rt::rc_list_push(diag_chunks, parent_result.diagnostics.clone()), tc_diags.clone());
let __tco_5 = source_indices;
remaining = __tco_0;
modules = __tco_1;
module_index = __tco_2;
item_registry = __tco_3;
diag_chunks = __tco_4;
source_indices = __tco_5;
continue;
} },
}
}
}

pub fn reconcile(graph: Rc<ModuleGraph>, source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>) -> Rc<ResolvedGraph> {
    {
        let typed = typecheck(graph, source_indices);
let emit_info = build_emit_graph_info(typed.modules.clone());
Rc::new(ResolvedGraph {
    modules: typed.modules.clone(),
    item_registry: typed.item_registry.clone(),
    diagnostics: typed.diagnostics.clone(),
    emit_graph_info: emit_info,
})
}
}
