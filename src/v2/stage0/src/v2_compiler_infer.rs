// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.infer

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}


#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}

pub use crate::std_types::{SourceSpan};
pub use crate::v2_std_core::{param_node_name, param_node_type_expr, param_node_default_value, Node, module_node, module_imports, module_items, is_import_node, import_is_all, import_specific_names, Connective, ExprData, make_expr_node, make_expr_error_node, make_arg_node, make_arm_node, make_field_init_node, make_text_part_node, make_interp_part_node, field_init_node_name, field_init_node_value, resource_use_name, map_children, arg_value, arg_name, arm_body, arm_pattern, arm_guard, if_condition, if_then_branch, if_else_branch, match_scrutinee, match_arm_nodes, let_value, let_body, field_access_base, lambda_body, method_receiver, method_arg_nodes, binop_left, binop_right, foreach_collection, foreach_body, index_base, index_expr, slice_base, slice_start, slice_end, cast_target, cast_expr, return_value, unaryop_operand, is_error_diagnostic, rt_node, has_inferred, InferredNode, NodeType, Cardinality, CompilerDiagnostic, ErrorNode, make_error_node, KERNEL_TYPES, is_kernel_type, expr_has_self_call, expr_has_non_tail_self_call, DeclaredFuncSig, DeclaredFuncEnv, LiteralValue, FieldAccessStyle, FieldValueShape, FieldSummary, IntrinsicMethod, VarBindingKind, CallSemantics, MethodSemantics, RuntimeBridgeMethod, LambdaSemantics, ExprErrorKind, TransportKind, is_transport_kind, BinOpKind, UnaryOpKind, MatchPattern, make_field_binding_node, field_binding_name, field_binding_pattern, StringPart, make_transport_node, local_transport_node, leaf_node, with_optional_cardinality, with_required_cardinality, node_is_product, node_is_coproduct, node_has_structure, no_span, expr_var_name, field_access_field, expr_call_func, expr_method_name, let_binding_name, foreach_variable, lambda_param_names, record_lit_type_name, make_named_expr_node};
use crate::v2_std_core::Connective::{Conj, Disj};
use crate::v2_std_core::ExprData::{NoExprData, ExprLiteral, ExprError, ExprVar, ExprFieldAccess, ExprCall, ExprMethodCall, ExprMatch, ExprIf, ExprLet, ExprRecordLit, ExprListLit, ExprBinOp, ExprUnaryOp, ExprLambda, ExprStringInterp, ExprBlock, ExprCast, ExprForEach, ExprIndex, ExprSlice, ExprReturn};
use crate::v2_std_core::InferredNode::{Resolved, CompilerError};
use crate::v2_std_core::NodeType::{Typed, InferError, Untyped};
use crate::v2_std_core::Cardinality::{Required, CardOptional};
use crate::v2_std_core::LiteralValue::{LitStr, LitInt, LitFloat, LitBool, LitNull};
use crate::v2_std_core::FieldAccessStyle::{StoredField, EnumAccessor, OptionalUnwrap, TupleFirst, TupleSecond};
use crate::v2_std_core::FieldValueShape::{PlainValue, OptionalValue};
use crate::v2_std_core::IntrinsicMethod::{MethodCount, MethodJoin, MethodSplit, MethodLast, MethodFirst, MethodEnumerate, MethodChars, MethodStringContains, MethodConcat, MethodMap, MethodFilter, MethodAny, MethodAll, MethodFlatMap, MethodSkip, MethodTake, MethodFold, MethodSortBy, MethodAppend};
use crate::v2_std_core::VarBindingKind::{LocalValueBinding, FunctionValueBinding, VariantValueBinding};
use crate::v2_std_core::CallSemantics::{PlainCallSemantics, LookupCallSemantics};
use crate::v2_std_core::MethodSemantics::{PlainMethodSemantics, AlgebraMethodSemantics, ServiceMethodSemantics};
use crate::v2_std_core::RuntimeBridgeMethod::{BridgeGet, BridgeWith, BridgeListPush, BridgeMapInsert, BridgeMapMerge, BridgeMapGet, BridgeMapHas, BridgeEmitMapHas, BridgeMapValues, BridgeMapKeys, BridgeMapContainsKey, BridgeCharAt, BridgeStringAt, BridgeStringLength, BridgeLength, BridgeStartsWith, BridgeEndsWith, BridgeToString, BridgeTrim, BridgeToLower, BridgeToUpper, BridgeReplace, BridgeSubstring, BridgeToInt, BridgeEmptyMap, BridgeContains, BridgeReverse, BridgeLookup};
use crate::v2_std_core::ExprErrorKind::{ParseRecoveryError, SemanticExprError, InternalExprError};
use crate::v2_std_core::TransportKind::{LocalTransport};
use crate::v2_std_core::BinOpKind::{Add, Sub, Mul, Div, Mod, BinEq, BinNe, BinLt, BinGt, BinLe, BinGe, BinAnd, BinOr, NullCoalesce};
use crate::v2_std_core::UnaryOpKind::{Not, Neg};
use crate::v2_std_core::MatchPattern::{Wildcard};
use crate::v2_std_core::StringPart::{Text, Interpolation};

pub use crate::v2_compiler_resolve::{ModuleGraph, ResolvedModule, ResolvedImport};
pub use crate::v2_compiler_infer_types::{child_inferred_or_name, container_node, map_node, bare_map_node, tuple_node, callable_node, callable_inferred, error_type_node, node_is_container, node_is_optional, node_is_map, node_is_leaf, node_is_named_ref, normalize_access_type_node, node_type_shape, node_type_compatible, node_type_equals, prefer_specific_type, node_type_deps, is_int_type_node, is_string_type_node, is_bool_type_node, is_float_type_node, method_receiver_element_node, infer_literal_node, infer_binop_type_node, extract_optional_inner_node, for_each_element_type_node, rt_type, emit_map_has, enrich_kernel_type};
pub use crate::v2_compiler_infer_method::{classify_reconciled_intrinsic_method, classify_runtime_bridge_method, infer_intrinsic_method_type_node, infer_runtime_bridge_method_type_node, infer_builtin_call_type, resolve_builtin_call_type, intrinsic_method_index, runtime_bridge_method_index};
pub use crate::v2_compiler_infer_cycle::{detect_type_cycles_kahn};
pub use crate::v2_compiler_infer_env::{TypeEnv, TypeBinding, is_recursive_type, lookup_type, merge_envs};
pub use crate::v2_compiler_infer_resolve::{resolve_node, resolve_item_types, NodeResolveResult, ItemResult};
pub use crate::v2_compiler_infer_sigs::{ResolvedFuncSig, ResolvedFuncEnv, ResolveFuncSigsResult, resolve_func_sigs};
pub use crate::v2_compiler_infer_emit_info::{TypeRepr, TypeSummary, EmitGraphInfo, EmitInfoBuildState, empty_emit_graph_info, lookup_emit_type_summary, build_struct_field_summaries, build_enum_field_summaries, add_emit_item_summary};
use crate::v2_compiler_infer_emit_info::TypeRepr::{StructRepr, EnumRepr};
pub use crate::v2_compiler_infer_items::{ItemKind, ItemInfo, TypedModule, TypedGraph, ResolvedGraph, inferred_to_outputs, item_kind, variant_locals_from_items};
use crate::v2_compiler_infer_items::ItemKind::{FnItem, FuncItem, TypeItem, DataItem, ServiceItem, OtherItem};
pub use crate::v2_compiler_infer_service::{UniqueAccum, OpEntry, ServiceMethodResult, is_typed_service_call_receiver, extract_typed_service_name, collect_typed_service_calls, collect_called_func_names, expand_transitive_services, check_service_field_access_node, check_service_method_call_node, service_op_entry};
pub use crate::v2_compiler_infer_patterns::{NodeLookupResult, PatternSubject, lookup_variant_in_type, lookup_field_in_variant, pattern_subject_from_node_type, pattern_subject_from_node, lookup_result_subject, pattern_binding_type, check_match_exhaustiveness};
use crate::v2_compiler_infer_patterns::PatternSubject::*;
pub use crate::v2_compiler_infer_lookup::{KnownMethodResolution, resolve_known_method_node, resolve_scrutinee_type_node, field_summary_for_type, lookup_field_type_node, lookup_coproduct_common_field_node, map_value_type_in_env, lookup_in_scope, lookup_func_sig};
pub use crate::v2_compiler_infer_access::{AccessCheckResultNode, check_index_access_node, check_slice_access_node};

#[derive(Debug, Clone, PartialEq)]
pub struct ItemContribution {
    pub resolved_item: Rc<Node>,
    pub resolve_diagnostics: Vec<Rc<ErrorNode>>,
    pub func_sig: Option<Rc<DeclaredFuncSig>>,
    pub svc_entries: Vec<Rc<OpEntry>>,
    pub svc_local: Option<Rc<TypeBinding>>,
    pub item_info: Rc<ItemInfo>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct ModuleContext {
    pub resolved_items: Vec<Rc<Node>>,
    pub func_env: Rc<ResolvedFuncEnv>,
    pub svc_registry: HashMap<String, Vec<Rc<OpEntry>>>,
    pub locals: HashMap<String, Rc<TypeBinding>>,
    pub item_registry: HashMap<String, Rc<ItemInfo>>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct InferScope {
    pub type_env: Rc<TypeEnv>,
    pub func_env: Rc<ResolvedFuncEnv>,
    pub locals: HashMap<String, Rc<TypeBinding>>,
    pub module_name: String,
    pub service_registry: HashMap<String, Vec<Rc<OpEntry>>>,
    pub item_registry: HashMap<String, Rc<ItemInfo>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct InferResult {
    pub typed: Rc<Node>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct BlockInferState {
    pub scope: Rc<InferScope>,
    pub diag_chunks: Vec<Vec<Rc<ErrorNode>>>,
    pub last_type: Rc<Node>,
    pub typed_stmts: Vec<Rc<Node>>,
}

pub fn infer_block_stmts(mut remaining: Vec<Rc<Node>>, mut scope: Rc<InferScope>, mut typed_stmts: Vec<Rc<Node>>, mut diag_chunks: Vec<Vec<Rc<ErrorNode>>>, mut last_type: Rc<Node>) -> Rc<BlockInferState> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(BlockInferState {
    scope: scope.clone(),
    diag_chunks: diag_chunks.clone(),
    last_type: last_type.clone(),
    typed_stmts: typed_stmts.clone(),
}); },
    Some(stmt) => { let stmt_result = infer_expr(stmt.clone(), scope.clone());
let stmt_typed = stmt_result.typed.clone();
let stmt_diags = stmt_result.diagnostics.clone();
let stmt_rt = rt_type(stmt_typed.clone());
let next_scope = scope_after_stmt_node(stmt.clone(), stmt_rt.clone(), scope.clone());
{
            let __tco_0 = remaining.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let __tco_1 = next_scope.clone();
let __tco_2 = v2_rt::list_push(typed_stmts.clone(), stmt_typed.clone());
let __tco_3 = v2_rt::list_push(diag_chunks.clone(), stmt_diags.clone());
let __tco_4 = stmt_rt.clone();
remaining = __tco_0;
scope = __tco_1;
typed_stmts = __tco_2;
diag_chunks = __tco_3;
last_type = __tco_4;
continue;
} },
}
}
}

#[derive(Debug, Clone, PartialEq)]
pub struct TypedItemResult {
    pub item: Rc<Node>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct ArmInferResult {
    pub typed_arm: Rc<Node>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
    pub body_type: Rc<Node>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct PatternScopeResult {
    pub scope: Rc<InferScope>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct StringPartInferResult {
    pub typed_part: Rc<StringPart>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct ArgInferResult {
    pub typed_arg: Rc<Node>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct FieldInferResult {
    pub typed_field: Rc<Node>,
    pub infer_result: Rc<InferResult>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct BuildTypeEnvResult {
    pub env: Rc<TypeEnv>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct ParentModulesResult {
    pub modules: Vec<Rc<TypedModule>>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct VariantResult {
    pub variant: Rc<Node>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct CycleDetectState {
    pub recursive_names: HashMap<String, bool>,
    pub global_visited: HashMap<String, bool>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct InferScopeComponents {
    pub func_sigs: HashMap<String, Rc<DeclaredFuncSig>>,
    pub svc_registry: HashMap<String, Vec<Rc<OpEntry>>>,
    pub svc_locals: HashMap<String, Rc<TypeBinding>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct LocalContributionState {
    pub resolved_items: Vec<Rc<Node>>,
    pub func_sigs: HashMap<String, Rc<DeclaredFuncSig>>,
    pub svc_registry: HashMap<String, Vec<Rc<OpEntry>>>,
    pub svc_locals: HashMap<String, Rc<TypeBinding>>,
    pub item_registry: HashMap<String, Rc<ItemInfo>>,
    pub diag_chunks: Vec<Vec<Rc<ErrorNode>>>,
}

pub fn merge_scope_from_imports(mut remaining: Vec<Rc<ResolvedImport>>, mut parent_index: HashMap<String, Rc<TypedModule>>, mut env: Rc<TypeEnv>, mut func_sigs: HashMap<String, Rc<DeclaredFuncSig>>, mut svc_registry: HashMap<String, Vec<Rc<OpEntry>>>, mut svc_locals: HashMap<String, Rc<TypeBinding>>) -> Rc<InferScopeComponents> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(InferScopeComponents {
    func_sigs: func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
}); },
    Some(imp) => { match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => { let next_func_sigs = v2_rt::map_values(&typed_parent.func_env.clone().signatures.clone()).iter().cloned().fold(func_sigs.clone(), |acc: _, rsig: Rc<ResolvedFuncSig>| {
            let sig_key = rsig.name.clone();
let sig_params = rsig.params.clone();
let sig_rt = rsig.inferred.clone();
let sig_async = rsig.is_async.clone();
v2_rt::map_insert(acc.clone(), sig_key.clone(), Rc::new(DeclaredFuncSig {
    name: sig_key.clone(),
    params: sig_params.clone(),
    inferred: Some(sig_rt.clone()),
    is_async: sig_async.clone(),
}))
});
let parent_result = typed_parent.items.clone().iter().cloned().fold(Rc::new(InferScopeComponents {
    func_sigs: next_func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
}), |acc: _, titem: Rc<Node>| if ((titem.transport.clone() != None) && ((titem.children.clone().len() as i64) > 0)) {
            {
                let entries = { let mut __result = Vec::new(); for c in titem.children.clone().iter().cloned() { __result.push(Rc::new(OpEntry {
    name: c.name.clone(),
    outputs: inferred_to_outputs(c.inferred.clone(), c.span.clone()),
    params: c.params.clone(),
})); } __result };
let root = namespace_root_from_properties(titem.properties.clone(), titem.name.clone());
Rc::new(InferScopeComponents {
    func_sigs: acc.func_sigs.clone(),
    svc_registry: v2_rt::map_insert(acc.svc_registry.clone(), titem.name.clone(), entries.clone()),
    svc_locals: v2_rt::map_insert(acc.svc_locals.clone(), root.clone(), Rc::new(TypeBinding {
    name: root.clone(),
    resolved: leaf_node(root.clone()),
})),
})
}
} else {
            if (((((titem.body.clone() != None) && ((titem.params.clone().len() as i64) == 0)) && (titem.transport.clone() == None)) && (node_has_structure(titem.clone()) == false)) && (titem.inferred.clone() != None)) {
                Rc::new(InferScopeComponents {
    func_sigs: acc.func_sigs.clone(),
    svc_registry: acc.svc_registry.clone(),
    svc_locals: v2_rt::map_insert(acc.svc_locals.clone(), titem.name.clone(), Rc::new(TypeBinding {
    name: titem.name.clone(),
    resolved: rt_type(titem.clone()),
})),
})
} else {
                acc.clone()
}
});
{
            let __tco_0 = remaining.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let __tco_1 = parent_index.clone();
let __tco_2 = env.clone();
let __tco_3 = parent_result.func_sigs.clone();
let __tco_4 = parent_result.svc_registry.clone();
let __tco_5 = parent_result.svc_locals.clone();
remaining = __tco_0;
parent_index = __tco_1;
env = __tco_2;
func_sigs = __tco_3;
svc_registry = __tco_4;
svc_locals = __tco_5;
continue;
} },
    None => { {
            let __tco_0 = remaining.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let __tco_1 = parent_index.clone();
let __tco_2 = env.clone();
let __tco_3 = func_sigs.clone();
let __tco_4 = svc_registry.clone();
let __tco_5 = svc_locals.clone();
remaining = __tco_0;
parent_index = __tco_1;
env = __tco_2;
func_sigs = __tco_3;
svc_registry = __tco_4;
svc_locals = __tco_5;
continue;
} },
} },
}
}
}

pub fn namespace_root_from_properties(properties: Vec<Rc<Node>>, name: String) -> String {
    match { let mut __result = Vec::new(); for p in properties.clone().iter().cloned() { if (field_init_node_name(p.clone()) == "namespace_root".to_string()) { __result.push(p); } } __result }.first().cloned() {
    Some(ns_prop) => match (*field_init_node_value(ns_prop.clone()).expr_data.clone()).clone() {
    ExprData::ExprLiteral { ref value, .. } => { let LiteralValue::LitStr { value: root, .. } = value.as_ref() else { unreachable!() }; root.clone() },
    _ => name.clone(),
},
    None => name.clone(),
}
}

pub fn expr_span(texpr: Rc<Node>) -> Rc<SourceSpan> {
    texpr.span.clone()
}

pub fn ok_infer(texpr: Rc<Node>) -> Rc<InferResult> {
    Rc::new(InferResult {
    typed: texpr.clone(),
    diagnostics: vec![],
})
}

pub fn semantic_expr_error_node(message: String, span: Rc<SourceSpan>) -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::SemanticExprError, message.clone(), span.clone())
}

pub fn internal_expr_error_node(message: String, span: Rc<SourceSpan>) -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::InternalExprError, message.clone(), span.clone())
}

pub fn lookup_variant_parent_enum(scope: Rc<InferScope>, name: String) -> Option<String> {
    match v2_rt::map_get(&scope.locals.clone(), name.clone()) {
    Some(binding) => match lookup_type(scope.type_env.clone(), binding.resolved.clone().name.clone()) {
    Some(parent) => if node_is_coproduct(parent.clone()) {
        if { let mut __found = false; for child in parent.children.clone().iter().cloned() { if (child.name.clone() == name.clone()) { __found = true; break; } } __found } {
            Some(binding.resolved.clone().name.clone())
} else {
            None
}
} else {
        None
},
    None => None,
},
    None => None,
}
}

pub fn infer_var_binding_kind(scope: Rc<InferScope>, name: String) -> Rc<VarBindingKind> {
    match lookup_variant_parent_enum(scope.clone(), name.clone()) {
    Some(parent_enum) => Rc::new(VarBindingKind::VariantValueBinding {
    parent_enum: parent_enum.clone(),
}),
    None => match v2_rt::map_get(&scope.locals.clone(), name.clone()) {
    Some(_) => Rc::new(VarBindingKind::LocalValueBinding),
    None => Rc::new(VarBindingKind::FunctionValueBinding),
},
}
}

pub fn inference_error(message: String, span: Rc<SourceSpan>, module_name: String) -> Rc<ErrorNode> {
    make_error_node(Rc::new(CompilerDiagnostic::InternalError { message: message.clone(), span: span.clone() }), module_name.clone())
}

pub fn categorized_error(message: String, span: Rc<SourceSpan>, module_name: String, category: String) -> Rc<ErrorNode> {
    make_error_node(Rc::new(CompilerDiagnostic::InternalError { message: message.clone(), span: span.clone() }), module_name.clone())
}

pub fn inferred_from_node_type(result: Rc<NodeType>, fallback_message: String, fallback_span: Rc<SourceSpan>) -> Rc<InferredNode> {
    match (*result.clone()).clone() {
    NodeType::Typed { node: resolved, .. } => Rc::new(InferredNode::Resolved {
    node: resolved.clone(),
}),
    NodeType::InferError { message: message, span: span, .. } => Rc::new(InferredNode::CompilerError {
    message: message.clone(),
    span: span.clone(),
}),
    NodeType::Untyped => Rc::new(InferredNode::CompilerError {
    message: fallback_message.clone(),
    span: fallback_span.clone(),
}),
}
}

pub fn lambda_semantics_from_param_types(param_types: Vec<Rc<Node>>) -> Rc<LambdaSemantics> {
    Rc::new(LambdaSemantics {
    param_types: param_types.clone(),
})
}

pub fn lambda_param_types_from_scope(scope: Rc<InferScope>, params: Vec<String>) -> Vec<Rc<Node>> {
    { let mut __result = Vec::new(); for param_name in params.clone().iter().cloned() { __result.push(match v2_rt::map_get(&scope.locals.clone(), param_name.clone()) {
    Some(binding) => binding.resolved.clone(),
    None => error_type_node(),
}); } __result }
}

pub fn resolve_pattern_subject(scope: Rc<InferScope>, scrutinee_subject: Rc<PatternSubject>) -> Rc<PatternSubject> {
    match (*scrutinee_subject.clone()).clone() {
    PatternSubject::PatternResolved { node: scrutinee_type, .. } => pattern_subject_from_node(resolve_scrutinee_type_node(scope.type_env.clone(), scrutinee_type.clone())),
    PatternSubject::PatternDynamic { span: dynamic_span, .. } => Rc::new(PatternSubject::PatternDynamic {
    span: dynamic_span.clone(),
}),
    PatternSubject::PatternLookupBlocked => Rc::new(PatternSubject::PatternLookupBlocked),
}
}

pub fn annotate_pattern_parent_enums(pattern: Rc<MatchPattern>, scrutinee_subject: Rc<PatternSubject>, scope: Rc<InferScope>) -> Rc<MatchPattern> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*pattern.clone()).clone() {
    MatchPattern::VariantPattern { name: variant_name, field_bindings: bindings, .. } => {
            let resolved_scrut = resolve_pattern_subject(scope.clone(), scrutinee_subject.clone());
let inferred_parent = match (*resolved_scrut.clone()).clone() {
    PatternSubject::PatternResolved { node: resolved_scrut_node, .. } => if (node_is_optional(resolved_scrut_node.clone()) && ((variant_name.clone() == "Some".to_string()) || (variant_name.clone() == "None".to_string()))) {
                Some("Optional".to_string())
} else {
                if node_is_coproduct(resolved_scrut_node.clone()) {
                    Some(resolved_scrut_node.name.clone())
} else {
                    None
}
},
    PatternSubject::PatternDynamic { .. } => None,
    PatternSubject::PatternLookupBlocked => None,
};
let variant_lookup = lookup_variant_in_type(resolved_scrut.clone(), variant_name.clone(), scope.module_name.clone());
let variant_subject = lookup_result_subject(variant_lookup.clone());
let annotated_bindings = { let mut __result = Vec::new(); for binding in bindings.clone().iter().cloned() { __result.push({
                let field_lookup = lookup_field_in_variant(variant_subject.clone(), field_binding_name(binding.clone()), scope.module_name.clone());
let field_subject = lookup_result_subject(field_lookup.clone());
make_field_binding_node(field_binding_name(binding.clone()), annotate_pattern_parent_enums(field_binding_pattern(binding.clone()), field_subject.clone(), scope.clone()), binding.span.clone())
}); } __result };
match inferred_parent.clone() {
    Some(parent_name) => Rc::new(MatchPattern::VariantPattern {
    name: variant_name.clone(),
    parent_enum: Some(parent_name.clone()),
    field_bindings: annotated_bindings.clone(),
}),
    None => Rc::new(MatchPattern::VariantPattern {
    name: variant_name.clone(),
    parent_enum: None,
    field_bindings: annotated_bindings.clone(),
}),
}
},
    _ => pattern.clone(),
}
    })
}

pub fn build_params_scope(scope: Rc<InferScope>, params: Vec<Rc<Node>>) -> Rc<InferScope> {
    {
        let new_locals = params.clone().iter().cloned().fold(scope.locals.clone(), |acc: _, p: Rc<Node>| v2_rt::map_insert(acc.clone(), p.name.clone(), Rc::new(TypeBinding {
    name: p.name.clone(),
    resolved: param_node_type_expr(p.clone()),
})));
Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: new_locals.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}
}

pub fn extend_scope(scope: Rc<InferScope>, name: String, resolved: Rc<Node>) -> Rc<InferScope> {
    Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: v2_rt::map_insert(scope.locals.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: resolved.clone(),
})),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}

pub fn extend_scope_with_params(scope: Rc<InferScope>, params: Vec<String>) -> Rc<InferScope> {
    {
        let new_locals = params.clone().iter().cloned().fold(scope.locals.clone(), |acc: _, p: String| v2_rt::map_insert(acc.clone(), p.clone(), Rc::new(TypeBinding {
    name: p.clone(),
    resolved: leaf_node("Dynamic".to_string()),
})));
Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: new_locals.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
})
}
}

pub fn scope_after_stmt_node(stmt: Rc<Node>, stmt_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<InferScope> {
    match (*stmt.expr_data.clone()).clone() {
    ExprData::ExprLet => if ((stmt.children.clone().len() as i64) <= 1) {
    let name = let_binding_name(stmt.clone());
        extend_scope(scope.clone(), name.clone(), stmt_type.clone())
} else {
        scope.clone()
},
    _ => scope.clone(),
}
}

pub fn extend_scope_with_pattern_node(scope: Rc<InferScope>, pattern: Rc<MatchPattern>, scrutinee_subject: Rc<PatternSubject>) -> Rc<PatternScopeResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*pattern.clone()).clone() {
    MatchPattern::Bind { name: n, .. } => Rc::new(PatternScopeResult {
    scope: extend_scope(scope.clone(), n.clone(), pattern_binding_type(scrutinee_subject.clone())),
    diagnostics: vec![],
}),
    MatchPattern::Wildcard => Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: vec![],
}),
    MatchPattern::LitPattern { .. } => Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: vec![],
}),
    MatchPattern::VariantPattern { name: vname, field_bindings: bindings, .. } => {
            let resolved_scrut = resolve_pattern_subject(scope.clone(), scrutinee_subject.clone());
let variant_lookup = lookup_variant_in_type(resolved_scrut.clone(), vname.clone(), scope.module_name.clone());
let variant_subject = lookup_result_subject(variant_lookup.clone());
let variant_diags = variant_lookup.diagnostics.clone();
bindings.clone().iter().cloned().fold(Rc::new(PatternScopeResult {
    scope: scope.clone(),
    diagnostics: variant_diags.clone(),
}), |acc: _, fb: Rc<Node>| {
                let field_lookup = lookup_field_in_variant(variant_subject.clone(), field_binding_name(fb.clone()), scope.module_name.clone());
let field_subject = lookup_result_subject(field_lookup.clone());
let field_type = pattern_binding_type(field_subject.clone());
let fb_pattern = field_binding_pattern(fb.clone());
match (*fb_pattern.clone()).clone() {
    MatchPattern::Bind { name: n, .. } => Rc::new(PatternScopeResult {
    scope: extend_scope(acc.scope.clone(), n.clone(), field_type.clone()),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), field_lookup.diagnostics.clone()),
}),
    _ => {
                    let nested = extend_scope_with_pattern_node(acc.scope.clone(), fb_pattern.clone(), field_subject.clone());
Rc::new(PatternScopeResult {
    scope: nested.scope.clone(),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), v2_rt::concat(field_lookup.diagnostics.clone(), nested.diagnostics.clone())),
})
},
}
})
},
}
    })
}

pub fn intrinsic_is(opt: Option<IntrinsicMethod>, expected: IntrinsicMethod) -> bool {
    if (opt.clone() == None) {
        false
} else {
        (opt.clone().unwrap() == expected.clone())
}
}

pub fn bridge_is(opt: Option<RuntimeBridgeMethod>, expected: RuntimeBridgeMethod) -> bool {
    if (opt.clone() == None) {
        false
} else {
        (opt.clone().unwrap() == expected.clone())
}
}

pub fn arg_has_name(arg: Rc<Node>, name: String) -> bool {
    let n = arg_name(arg.clone());
    if (n.clone() == None) {
        false
} else {
        (n.clone().unwrap() == name.clone())
}
}

pub fn extract_fold_init_info(intrinsic: Option<IntrinsicMethod>, method_args: Vec<Rc<Node>>, min_args: i64, scope: Rc<InferScope>) -> Option<Rc<ArgInferResult>> {
    let is_fold = intrinsic_is(intrinsic.clone(), MethodFold);
    if (is_fold && ((method_args.clone().len() as i64) >= min_args.clone())) {
        {
            let init_arg = match { let mut __result = Vec::new(); for a in method_args.clone().iter().cloned() { if arg_has_name(a.clone(), "init".to_string()) { __result.push(a); } } __result }.first().cloned() {
    Some(a) => Some(a.clone()),
    None => method_args.clone().first().cloned(),
};
match init_arg.clone() {
    Some(ia) => {
                let init_result = infer_expr(arg_value(ia.clone()), scope.clone());
Some(Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(ia.clone()), init_result.typed.clone(), ia.span.clone()),
    diagnostics: init_result.diagnostics.clone(),
}))
},
    None => None,
}
}
} else {
        None
}
}

pub fn infer_method_args_with_fold(intrinsic: Option<IntrinsicMethod>, method_args: Vec<Rc<Node>>, fold_info: Option<Rc<ArgInferResult>>, fold_acc_type: Rc<Node>, element_type: Rc<Node>, scope: Rc<InferScope>) -> Vec<Rc<ArgInferResult>> {
    let is_fold = intrinsic_is(intrinsic.clone(), MethodFold);
    { let mut __result = Vec::new(); for idx_pair in method_args.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { __result.push({
        let a = idx_pair.1.clone();
let idx = idx_pair.0.clone();
let is_init_arg = (arg_has_name(a.clone(), "init".to_string()) || ((arg_name(a.clone()) == None) && (idx.clone() == 0)));
if ((is_fold && (fold_info.clone() != None)) && is_init_arg.clone()) {
            match fold_info.clone() {
    Some(fi) => fi.clone(),
    None => infer_arg_with_element_type(a.clone(), element_type.clone(), scope.clone()),
}
} else {
            if (is_fold && (fold_info.clone() != None)) {
                infer_fold_lambda_arg(a.clone(), scope.clone(), fold_acc_type.clone(), element_type.clone())
} else {
                infer_arg_with_element_type(a.clone(), element_type.clone(), scope.clone())
}
}
}); } __result }
}

pub fn infer_fold_lambda_arg(arg: Rc<Node>, scope: Rc<InferScope>, acc_type: Rc<Node>, elem_type: Rc<Node>) -> Rc<ArgInferResult> {
    let arg_val = arg_value(arg.clone());
    match (*arg_val.clone().expr_data.clone()).clone() {
    ExprData::ExprLambda { .. } => {
        let lam_params = lambda_param_names(arg_val.clone());
        let lam_body = lambda_body(arg_val.clone());
        let lam_span = arg_val.clone().span.clone();
let param_count = (lam_params.clone().len() as i64);
let param_types = { let mut __result = Vec::new(); for pair in lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { __result.push(if (pair.0.clone() == 0) {
            acc_type.clone()
} else {
            if (pair.0.clone() == (param_count.clone() - 1)) {
                elem_type.clone()
} else {
                leaf_node("Dynamic".to_string())
}
}); } __result };
let lam_locals = lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned().fold(scope.locals.clone(), |acc_locals: _, pair: (i64, String)| if (pair.0.clone() == 0) {
            v2_rt::map_insert(acc_locals.clone(), pair.1.clone(), Rc::new(TypeBinding {
    name: pair.1.clone(),
    resolved: acc_type.clone(),
}))
} else {
            if (pair.0.clone() == (param_count.clone() - 1)) {
                v2_rt::map_insert(acc_locals.clone(), pair.1.clone(), Rc::new(TypeBinding {
    name: pair.1.clone(),
    resolved: elem_type.clone(),
}))
} else {
                v2_rt::map_insert(acc_locals.clone(), pair.1.clone(), Rc::new(TypeBinding {
    name: pair.1.clone(),
    resolved: leaf_node("Dynamic".to_string()),
}))
}
});
let typed_lam_scope = Rc::new(InferScope {
    type_env: scope.type_env.clone(),
    func_env: scope.func_env.clone(),
    locals: lam_locals.clone(),
    module_name: scope.module_name.clone(),
    service_registry: scope.service_registry.clone(),
    item_registry: scope.item_registry.clone(),
});
let body_result = infer_expr(lam_body.clone(), typed_lam_scope.clone());
let typed_lam = make_expr_node(Rc::new(ExprData::ExprLambda {
semantics: Some(lambda_semantics_from_param_types(param_types.clone())),
}), v2_rt::concat(vec![body_result.typed.clone()], { let mut __result = Vec::new(); for p in lam_params.clone().iter().cloned() { __result.push(leaf_node(p.clone())); } __result }), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_result.typed.clone()),
})), lam_span.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(arg.clone()), typed_lam.clone(), arg.span.clone()),
    diagnostics: body_result.diagnostics.clone(),
})
},
    _ => {
        let ar = infer_expr(arg_val.clone(), scope.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(arg.clone()), ar.typed.clone(), arg.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
},
}
}

pub fn infer_lambda_with_element_type(lambda_expr: Rc<Node>, element_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<InferResult> {
    match (*lambda_expr.expr_data.clone()).clone() {
    ExprData::ExprLambda { .. } => {
    let lam_params = lambda_param_names(lambda_expr.clone());
        let lam_body = lambda_body(lambda_expr.clone());
        let span = lambda_expr.span.clone();
let param_types = if ((lam_params.clone().len() as i64) == 1) {
            vec![element_type.clone()]
} else {
            { let mut __result = Vec::new(); for pair in lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { __result.push(if (pair.0.clone() == ((lam_params.clone().len() as i64) - 1)) {
                element_type.clone()
} else {
                leaf_node("Dynamic".to_string())
}); } __result }
};
let typed_scope = if ((lam_params.clone().len() as i64) == 1) {
            match lam_params.clone().first().cloned() {
    Some(p) => extend_scope(scope.clone(), p.clone(), element_type.clone()),
    None => scope.clone(),
}
} else {
            lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned().fold(scope.clone(), |acc: _, pair: (i64, String)| if (pair.0.clone() == ((lam_params.clone().len() as i64) - 1)) {
                extend_scope(acc.clone(), pair.1.clone(), element_type.clone())
} else {
                extend_scope(acc.clone(), pair.1.clone(), leaf_node("Dynamic".to_string()))
})
};
let body_result = infer_expr(lam_body.clone(), typed_scope.clone());
let body_typed = body_result.typed.clone();
Rc::new(InferResult {
    typed: make_expr_node(Rc::new(ExprData::ExprLambda {
semantics: Some(lambda_semantics_from_param_types(param_types.clone())),
}), v2_rt::concat(vec![body_typed.clone()], { let mut __result = Vec::new(); for p in lam_params.clone().iter().cloned() { __result.push(leaf_node(p.clone())); } __result }), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone()),
    diagnostics: body_result.diagnostics.clone(),
})
},
    _ => infer_expr(lambda_expr.clone(), scope.clone()),
}
}

pub fn is_lambda_expr(e: Rc<Node>) -> bool {
    match (*e.expr_data.clone()).clone() {
    ExprData::ExprLambda { .. } => true,
    _ => false,
}
}

pub fn infer_lambda_with_callable_type(lambda_expr: Rc<Node>, callable_type: Rc<Node>, arg_name: Option<String>, scope: Rc<InferScope>) -> Rc<ArgInferResult> {
    match (*lambda_expr.expr_data.clone()).clone() {
    ExprData::ExprLambda { .. } => {
    let lam_params = lambda_param_names(lambda_expr.clone());
        let lam_body = lambda_body(lambda_expr.clone());
        let span = lambda_expr.span.clone();
let callable_params = callable_type.params.clone();
let param_types = { let mut __result = Vec::new(); for pair in lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { __result.push(match callable_params.clone().iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(cp) => param_node_type_expr(cp.clone()),
    None => leaf_node("Dynamic".to_string()),
}); } __result };
let typed_scope = lam_params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned().fold(scope.clone(), |acc: _, pair: (i64, String)| {
            let pt = match callable_params.clone().iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(cp) => param_node_type_expr(cp.clone()),
    None => leaf_node("Dynamic".to_string()),
};
extend_scope(acc.clone(), pair.1.clone(), pt.clone())
});
let body_result = infer_expr(lam_body.clone(), typed_scope.clone());
let body_typed = body_result.typed.clone();
let typed_lam = make_expr_node(Rc::new(ExprData::ExprLambda {
semantics: Some(lambda_semantics_from_param_types(param_types.clone())),
}), v2_rt::concat(vec![body_typed.clone()], { let mut __result = Vec::new(); for p in lam_params.clone().iter().cloned() { __result.push(leaf_node(p.clone())); } __result }), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name.clone(), typed_lam.clone(), no_span()),
    diagnostics: body_result.diagnostics.clone(),
})
},
    _ => {
        let ar = infer_expr(lambda_expr.clone(), scope.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name.clone(), ar.typed.clone(), no_span()),
    diagnostics: ar.diagnostics.clone(),
})
},
}
}

pub fn refine_collection_result_type(semantics: Option<Rc<MethodSemantics>>, typed_args: Vec<Rc<Node>>, receiver_type: Rc<Node>, fallback: Rc<Node>) -> Rc<Node> {
    {
        let receiver_type_name = receiver_type.name.clone();
let receiver_is_map = node_is_map(receiver_type.clone());
let method_name = if (semantics.clone() == None) {
            None
} else {
            match (*semantics.clone().unwrap()).clone() {
    MethodSemantics::AlgebraMethodSemantics { method_name: mn, .. } => Some(mn),
    _ => None,
}
};
let intrinsic = if (method_name.clone() == None) {
            None
} else {
            v2_rt::map_get(&intrinsic_method_index(), method_name.clone().unwrap())
};
let bridge = if (method_name.clone() == None) {
            None
} else {
            v2_rt::map_get(&runtime_bridge_method_index(), method_name.clone().unwrap())
};
if intrinsic_is(intrinsic.clone(), MethodMap) {
            match { let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }.first().cloned() {
    Some(lambda_arg) => {
                let lambda_ret = rt_type(arg_value(lambda_arg.clone()));
container_node(receiver_type_name.clone(), lambda_ret.clone())
},
    None => fallback.clone(),
}
} else {
            if intrinsic_is(intrinsic.clone(), MethodFlatMap) {
                match { let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }.first().cloned() {
    Some(lambda_arg) => match (*rt_node(arg_value(lambda_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
}
} else {
                if intrinsic_is(intrinsic.clone(), MethodFold) {
                    match { let mut __result = Vec::new(); for a in typed_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __result.push(a); } } __result }.first().cloned() {
    Some(lambda_arg) => match (*rt_node(arg_value(lambda_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
}
} else {
                    if ((bridge_is(bridge.clone(), BridgeListPush) && ((typed_args.clone().len() as i64) >= 1)) && ((receiver_type.children.clone().len() as i64) == 0)) {
                        match typed_args.clone().first().cloned() {
    Some(item_arg) => match (*rt_node(arg_value(item_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => container_node("List".to_string(), rt.clone()),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
}
} else {
                        if (((bridge_is(bridge.clone(), BridgeMapInsert) && ((receiver_type.children.clone().len() as i64) == 0)) && receiver_is_map.clone()) && ((typed_args.clone().len() as i64) >= 2)) {
                            {
                                let key_type = match typed_args.clone().first().cloned() {
    Some(key_arg) => match (*rt_node(arg_value(key_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
},
    None => fallback.clone(),
};
match typed_args.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(val_arg) => map_node(key_type.clone(), match (*rt_node(arg_value(val_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => fallback.clone(),
    NodeType::Untyped => fallback.clone(),
}),
    None => fallback.clone(),
}
}
} else {
                            if ((bridge_is(bridge.clone(), BridgeMapMerge) && receiver_is_map.clone()) && ((typed_args.clone().len() as i64) >= 1)) {
                                {
                                    let overlay_type = match typed_args.clone().first().cloned() {
    Some(overlay_arg) => match (*rt_node(arg_value(overlay_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => receiver_type.clone(),
    NodeType::Untyped => receiver_type.clone(),
},
    None => receiver_type.clone(),
};
if (((receiver_type.children.clone().len() as i64) == 0) && ((overlay_type.children.clone().len() as i64) > 0)) {
                                        overlay_type.clone()
} else {
                                        if ((receiver_type.children.clone().len() as i64) > 0) {
                                            receiver_type.clone()
} else {
                                            fallback.clone()
}
}
}
} else {
                                fallback.clone()
}
}
}
}
}
}
}
}

pub fn infer_arg_with_element_type(arg: Rc<Node>, element_type: Rc<Node>, scope: Rc<InferScope>) -> Rc<ArgInferResult> {
    if is_lambda_expr(arg_value(arg.clone())) {
        {
            let result = infer_lambda_with_element_type(arg_value(arg.clone()), element_type.clone(), scope.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(arg.clone()), result.typed.clone(), arg.span.clone()),
    diagnostics: result.diagnostics.clone(),
})
}
} else {
        {
            let ar = infer_expr(arg_value(arg.clone()), scope.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(arg.clone()), ar.typed.clone(), arg.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
}
}
}

pub fn infer_expr(texpr: Rc<Node>, scope: Rc<InferScope>) -> Rc<InferResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*texpr.expr_data.clone()).clone() {
    ExprData::ExprLiteral { value: lit, .. } => {
            let span = texpr.span.clone();
ok_infer(make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: lit.clone(),
}), vec![], Some(Rc::new(InferredNode::Resolved {
    node: infer_literal_node(lit.clone()),
})), span.clone()))
},
    ExprData::ExprError { kind: kind, message: message, .. } => {
            let span = texpr.span.clone();
Rc::new(InferResult {
    typed: make_expr_error_node(kind.clone(), message.clone(), span.clone()),
    diagnostics: vec![],
})
},
    ExprData::ExprVar { .. } => {
            let name = expr_var_name(texpr.clone());
            let span = texpr.span.clone();
match v2_rt::map_get(&scope.locals, name.clone()) {
    Some(binding) => {
                let binding_kind = infer_var_binding_kind(scope.clone(), name.clone());
ok_infer(make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
binding_kind: Some(binding_kind.clone()),
}), vec![], Some(Rc::new(InferredNode::Resolved {
    node: binding.resolved.clone(),
})), span.clone()))
},
    None => match lookup_func_sig(scope.func_env.clone(), name.clone()) {
    Some(fsig) => ok_infer(make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
binding_kind: Some(Rc::new(VarBindingKind::FunctionValueBinding)),
}), vec![], Some(Rc::new(InferredNode::Resolved {
    node: callable_node(fsig.params.clone(), fsig.inferred.clone()),
})), span.clone())),
    None => {
                let err_texpr = make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
binding_kind: None,
}), vec![], Some(Rc::new(InferredNode::Resolved {
    node: error_type_node(),
})), span.clone());
Rc::new(InferResult {
    typed: err_texpr.clone(),
    diagnostics: vec![inference_error(v2_rt::concat(v2_rt::concat("undefined variable '".to_string(), name.clone()), "'".to_string()), span.clone(), scope.module_name.clone())],
})
},
},
}
},
    ExprData::ExprFieldAccess { .. } => {
    let field_name = field_access_field(texpr.clone());
            let span = texpr.span.clone();
let base_expr = field_access_base(texpr.clone());
let base_result = infer_expr(base_expr.clone(), scope.clone());
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let base_rt = match (*rt_node(base_typed.clone())).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type_node(),
    NodeType::Untyped => leaf_node("Unit".to_string()),
};
let resolved_base = resolve_scrutinee_type_node(scope.type_env.clone(), base_rt.clone());
if (resolved_base.name.clone() == "Error".to_string()) {
                Rc::new(InferResult {
    typed: make_expr_error_node(ExprErrorKind::SemanticExprError, "error type cascade".to_string(), span.clone()),
    diagnostics: base_diags.clone(),
})
} else {
                match lookup_field_type_node(resolved_base.clone(), field_name.clone()) {
    Some(field_type) => {
                    let field_summary = field_summary_for_type(base_rt.clone(), scope.type_env.clone(), field_name.clone());
let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
summary: field_summary.clone(),
}), vec![base_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: field_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr.clone(),
    diagnostics: base_diags.clone(),
})
},
    None => if (resolved_base.name.clone() == "Dynamic".to_string()) {
                    {
                        let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
summary: Some(Rc::new(FieldSummary {
    access_style: FieldAccessStyle::StoredField,
    value_shape: FieldValueShape::PlainValue,
})),
}), vec![base_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Dynamic".to_string()),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr.clone(),
    diagnostics: base_diags.clone(),
})
}
} else {
                    match check_service_field_access_node(base_rt.clone(), field_name.clone(), scope.service_registry.clone()) {
    Some(svc_type) => {
                        let fa_texpr = make_named_expr_node(field_name.clone(), Rc::new(ExprData::ExprFieldAccess {
summary: Some(Rc::new(FieldSummary {
    access_style: FieldAccessStyle::StoredField,
    value_shape: FieldValueShape::PlainValue,
})),
}), vec![base_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: svc_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: fa_texpr.clone(),
    diagnostics: base_diags.clone(),
})
},
    None => {
                        let error_message = v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("no field '".to_string(), field_name.clone()), "' on type '".to_string()), resolved_base.name.clone()), "'".to_string());
let fa_texpr = make_expr_error_node(ExprErrorKind::SemanticExprError, error_message.clone(), span.clone());
Rc::new(InferResult {
    typed: fa_texpr.clone(),
    diagnostics: v2_rt::concat(base_diags.clone(), vec![inference_error(error_message.clone(), span.clone(), scope.module_name.clone())]),
})
},
}
},
}
}
},
    ExprData::ExprCall { .. } => {
            let func_name = expr_call_func(texpr.clone());
            let span = texpr.span.clone();
let call_args = texpr.children.clone();
let sig = lookup_func_sig(scope.func_env.clone(), func_name.clone());
let sig_params = match sig.clone() {
    Some(s) => s.params.clone(),
    None => vec![],
};
let has_lambda = { let mut __found = false; for a in call_args.clone().iter().cloned() { if is_lambda_expr(arg_value(a.clone())) { __found = true; break; } } __found };
let call_method_args = call_args.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let call_intrinsic = intrinsic_method_index().get(&func_name).cloned();
let call_fold_info = extract_fold_init_info(call_intrinsic.clone(), call_method_args.clone(), 2, scope.clone());
let call_fold_acc_type = match call_fold_info.clone() {
    Some(fi) => match (*rt_node(arg_value(fi.typed_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type_node(),
    NodeType::Untyped => error_type_node(),
},
    None => error_type_node(),
};
let arg_infer_results = if ((has_lambda.clone() && ((call_args.clone().len() as i64) >= 2)) && (sig.clone() == None)) {
                match call_args.clone().first().cloned() {
    Some(first_arg) => {
                    let first_result = infer_expr(arg_value(first_arg.clone()), scope.clone());
let first_type = rt_type(first_result.typed.clone());
let elem_type = for_each_element_type_node(first_type.clone());
let remaining_results = infer_method_args_with_fold(call_intrinsic.clone(), call_method_args.clone(), call_fold_info.clone(), call_fold_acc_type.clone(), elem_type.clone(), scope.clone());
v2_rt::concat(vec![Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(first_arg.clone()), first_result.typed.clone(), first_arg.span.clone()),
    diagnostics: first_result.diagnostics.clone(),
})], remaining_results.clone())
},
    None => vec![],
}
} else {
                { let mut __result = Vec::new(); for pair in call_args.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { __result.push({
                    let a = pair.1.clone();
let formal_param_type = match sig_params.clone().iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(p) => param_node_type_expr(p.clone()),
    None => leaf_node("Dynamic".to_string()),
};
let is_callable_formal = (formal_param_type.name.clone() == "Callable".to_string());
if (is_lambda_expr(arg_value(a.clone())) && is_callable_formal.clone()) {
                        infer_lambda_with_callable_type(arg_value(a.clone()), formal_param_type.clone(), arg_name(a.clone()), scope.clone())
} else {
                        {
                            let ar = infer_expr(arg_value(a.clone()), scope.clone());
Rc::new(ArgInferResult {
    typed_arg: make_arg_node(arg_name(a.clone()), ar.typed.clone(), a.span.clone()),
    diagnostics: ar.diagnostics.clone(),
})
}
}
}); } __result }
};
let typed_args = { let mut __result = Vec::new(); for air in arg_infer_results.clone().iter().cloned() { __result.push(air.typed_arg.clone()); } __result };
let arg_diags = { let mut __result = Vec::new(); for air in arg_infer_results.clone().iter().cloned() { __result.extend(air.diagnostics.clone()); } __result };
let typed_arg_nodes = typed_args.clone();
if (sig.clone() != None) {
                {
                    let resolved_type = match sig.clone() {
    Some(s) => s.inferred.clone(),
    None => error_type_node(),
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: resolved_type.clone(),
})), span.clone()),
    diagnostics: arg_diags.clone(),
})
}
} else {
                {
                    let first_arg_type = match typed_args.clone().first().cloned() {
    Some(ta) => rt_type(arg_value(ta.clone())),
    None => leaf_node("Unit".to_string()),
};
let method_receiver = match typed_args.clone().first().cloned() {
    Some(ta) => arg_value(ta.clone()),
    None => internal_expr_error_node("method bridge missing receiver".to_string(), span.clone()),
};
let method_resolution = resolve_known_method_node(method_receiver.clone(), first_arg_type.clone(), func_name.clone(), if (call_fold_info.clone() != None) {
                        Some(call_fold_acc_type.clone())
} else {
                        None
}, scope.service_registry.clone());
let is_known_method = (method_resolution.result_type.clone() != None);
if (is_known_method.clone() && ((typed_args.clone().len() as i64) > 0)) {
                        {
                            let receiver = method_receiver.clone();
let remaining = typed_args.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let base_result_type = match method_resolution.result_type.clone() {
    Some(mt) => mt.clone(),
    None => error_type_node(),
};
let bridge_result_type = refine_collection_result_type(method_resolution.semantics.clone(), remaining.clone(), first_arg_type.clone(), base_result_type.clone());
let remaining_arg_nodes = remaining.clone();
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprMethodCall {
method_semantics: method_resolution.semantics.clone(),
}), v2_rt::concat(vec![receiver.clone()], remaining_arg_nodes.clone()), Some(Rc::new(InferredNode::Resolved {
    node: bridge_result_type.clone(),
})), span.clone()),
    diagnostics: arg_diags.clone(),
})
}
} else {
                        if (func_name.clone() == "empty_map".to_string()) {
                            Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: bare_map_node(),
})), span.clone()),
    diagnostics: arg_diags.clone(),
})
} else {
                            if (infer_builtin_call_type(func_name.clone()) != None) {
                                {
                                    let bt = if ((func_name.clone() == "lookup".to_string()) || (func_name.clone() == "map_get".to_string())) {
                                        match typed_args.clone().first().cloned() {
    Some(receiver_arg) => match (*rt_node(arg_value(receiver_arg.clone()))).clone() {
    NodeType::Typed { node: receiver_type, .. } => match map_value_type_in_env(receiver_type.clone(), scope.type_env.clone()) {
    Some(value_type) => with_optional_cardinality(value_type.clone()),
    None => resolve_builtin_call_type(func_name.clone()),
},
    _ => resolve_builtin_call_type(func_name.clone()),
},
    None => resolve_builtin_call_type(func_name.clone()),
}
} else {
                                        resolve_builtin_call_type(func_name.clone())
};
let call_semantics = if (func_name.clone() == "lookup".to_string()) {
                                        Some(CallSemantics::LookupCallSemantics)
} else {
                                        Some(CallSemantics::PlainCallSemantics)
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
call_semantics: call_semantics.clone(),
}), typed_arg_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: bt.clone(),
})), span.clone()),
    diagnostics: arg_diags.clone(),
})
}
} else {
                                {
                                    let callable_local = match v2_rt::map_get(&scope.locals.clone(), func_name.clone()) {
    Some(binding) => if (binding.resolved.clone().name.clone() == "Callable".to_string()) {
                                        Some(binding.resolved.clone())
} else {
                                        None
},
    None => None,
};
if (callable_local.clone() != None) {
                                        {
                                            let callable_type = match callable_local.clone() {
    Some(ct) => ct.clone(),
    None => error_type_node(),
};
let resolved_type = callable_inferred(callable_type.clone());
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: resolved_type.clone(),
})), span.clone()),
    diagnostics: arg_diags.clone(),
})
}
} else {
                                        {
                                            let type_match = lookup_type(scope.type_env.clone(), func_name.clone());
let resolved_type = match type_match.clone() {
    Some(tn) => tn.clone(),
    None => error_type_node(),
};
let call_diags = match type_match.clone() {
    Some(_) => vec![],
    None => vec![inference_error(v2_rt::concat(v2_rt::concat("function '".to_string(), func_name.clone()), "' not found in scope".to_string()), span.clone(), scope.module_name.clone())],
};
Rc::new(InferResult {
    typed: make_named_expr_node(func_name.clone(), Rc::new(ExprData::ExprCall {
call_semantics: Some(CallSemantics::PlainCallSemantics),
}), typed_arg_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: resolved_type.clone(),
})), span.clone()),
    diagnostics: v2_rt::concat(arg_diags.clone(), call_diags.clone()),
})
}
}
}
}
}
}
}
}
},
    ExprData::ExprMethodCall { .. } => {
            let method_name = expr_method_name(texpr.clone());
            let span = texpr.span.clone();
let recv = method_receiver(texpr.clone());
let mc_args = method_arg_nodes(texpr.clone());
let recv_result = infer_expr(recv.clone(), scope.clone());
let recv_typed = recv_result.typed.clone();
let recv_diags = recv_result.diagnostics.clone();
let recv_rt = rt_type(recv_typed.clone());
let recv_elem_type = for_each_element_type_node(recv_rt.clone());
let mc_intrinsic = intrinsic_method_index().get(&method_name).cloned();
let fold_info = extract_fold_init_info(mc_intrinsic.clone(), mc_args.clone(), 2, scope.clone());
let fold_acc_type = match fold_info.clone() {
    Some(fi) => match (*rt_node(arg_value(fi.typed_arg.clone()))).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type_node(),
    NodeType::Untyped => error_type_node(),
},
    None => error_type_node(),
};
let mc_arg_infer_results = infer_method_args_with_fold(mc_intrinsic.clone(), mc_args.clone(), fold_info.clone(), fold_acc_type.clone(), recv_elem_type.clone(), scope.clone());
let typed_mc_args = { let mut __result = Vec::new(); for air in mc_arg_infer_results.clone().iter().cloned() { __result.push(air.typed_arg.clone()); } __result };
let mc_arg_diags = { let mut __result = Vec::new(); for air in mc_arg_infer_results.clone().iter().cloned() { __result.extend(air.diagnostics.clone()); } __result };
let method_resolution = resolve_known_method_node(recv_typed.clone(), recv_rt.clone(), method_name.clone(), if (fold_info.clone() != None) {
                Some(fold_acc_type.clone())
} else {
                None
}, scope.service_registry.clone());
let base_result_type = match method_resolution.result_type.clone() {
    Some(rt) => rt.clone(),
    None => recv_rt.clone(),
};
let result_type = refine_collection_result_type(method_resolution.semantics.clone(), typed_mc_args.clone(), recv_rt.clone(), base_result_type.clone());
let method_semantics = if (method_resolution.semantics.clone() != None) {
                method_resolution.semantics.clone()
} else {
                Some(Rc::new(MethodSemantics::PlainMethodSemantics))
};
let typed_mc_arg_nodes = typed_mc_args.clone();
let mc_texpr = make_named_expr_node(method_name.clone(), Rc::new(ExprData::ExprMethodCall {
method_semantics: method_semantics.clone(),
}), v2_rt::concat(vec![recv_typed.clone()], typed_mc_arg_nodes.clone()), Some(Rc::new(InferredNode::Resolved {
    node: result_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: mc_texpr.clone(),
    diagnostics: v2_rt::concat(recv_diags.clone(), mc_arg_diags.clone()),
})
},
    ExprData::ExprMatch => {
            let span = texpr.span.clone();
let scrut = match_scrutinee(texpr.clone());
let arm_nodes = match_arm_nodes(texpr.clone());
let scrut_result = infer_expr(scrut.clone(), scope.clone());
let scrut_typed = scrut_result.typed.clone();
let scrut_diags = scrut_result.diagnostics.clone();
let scrut_rt = rt_type(scrut_typed.clone());
let scrut_subject = pattern_subject_from_node_type(rt_node(scrut_typed.clone()));
let arm_infer_results = { let mut __result = Vec::new(); for arm_node in arm_nodes.clone().iter().cloned() { __result.push({
                let arm_pat = arm_pattern(arm_node.clone());
let arm_g = arm_guard(arm_node.clone());
let arm_b = arm_body(arm_node.clone());
let typed_pattern = annotate_pattern_parent_enums(arm_pat.clone(), scrut_subject.clone(), scope.clone());
let pattern_result = extend_scope_with_pattern_node(scope.clone(), typed_pattern.clone(), scrut_subject.clone());
let arm_scope = pattern_result.scope.clone();
let pattern_diags = pattern_result.diagnostics.clone();
let guard_result = if (arm_g.clone() != None) {
                    Some(infer_expr(arm_g.clone().clone().unwrap(), arm_scope.clone()))
} else {
                    None
};
let body_result = infer_expr(arm_b.clone(), arm_scope.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let guard_unwrapped = match guard_result.clone() {
    Some(gr) => gr.clone(),
    None => Rc::new(InferResult {
    typed: arm_b.clone(),
    diagnostics: vec![],
}),
};
let guard_diags = if (guard_result.clone() != None) {
                    guard_unwrapped.diagnostics.clone()
} else {
                    vec![]
};
Rc::new(ArmInferResult {
    typed_arm: make_arm_node(
        typed_pattern.clone(),
        if (guard_result.clone() != None) {
                    Some(guard_unwrapped.typed.clone())
} else {
                    None
},
        body_typed.clone(),
        span.clone(),
    ),
    diagnostics: v2_rt::concat(pattern_diags.clone(), v2_rt::concat(guard_diags.clone(), body_diags.clone())),
    body_type: rt_type(body_typed.clone()),
})
}); } __result };
let typed_arms = { let mut __result = Vec::new(); for ar in arm_infer_results.clone().iter().cloned() { __result.push(ar.typed_arm.clone()); } __result };
let arm_diags = { let mut __result = Vec::new(); for ar in arm_infer_results.clone().iter().cloned() { __result.extend(ar.diagnostics.clone()); } __result };
let result_type = match arm_infer_results.clone().first().cloned() {
    Some(ar) => ar.body_type.clone(),
    None => scrut_rt.clone(),
};
let empty_arms_diags = if ((arm_infer_results.clone().len() as i64) == 0) {
                vec![inference_error("match expression has no arms".to_string(), span.clone(), scope.module_name.clone())]
} else {
                vec![]
};
let exhaustiveness_diags = match (*resolve_pattern_subject(scope.clone(), scrut_subject.clone())).clone() {
    PatternSubject::PatternResolved { node: resolved_scrutinee, .. } => check_match_exhaustiveness(resolved_scrutinee.clone(), typed_arms.clone(), scope.type_env.clone(), span.clone(), scope.module_name.clone()),
    PatternSubject::PatternDynamic { .. } => vec![],
    PatternSubject::PatternLookupBlocked => vec![],
};
let match_texpr = make_expr_node(Rc::new(ExprData::ExprMatch), v2_rt::concat(vec![scrut_typed.clone()], typed_arms.clone()), Some(Rc::new(InferredNode::Resolved {
    node: result_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: match_texpr.clone(),
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(scrut_diags.clone(), arm_diags.clone()), empty_arms_diags.clone()), exhaustiveness_diags.clone()),
})
},
    ExprData::ExprIf => {
            let span = texpr.span.clone();
let cond = if_condition(texpr.clone());
let then_expr = if_then_branch(texpr.clone());
let else_expr = if_else_branch(texpr.clone());
let cond_result = infer_expr(cond.clone(), scope.clone());
let cond_typed = cond_result.typed.clone();
let cond_diags = cond_result.diagnostics.clone();
let then_result = infer_expr(then_expr.clone(), scope.clone());
let then_typed = then_result.typed.clone();
let then_diags = then_result.diagnostics.clone();
match else_expr.clone() {
    Some(else_branch) => {
                let else_result = infer_expr(else_branch.clone(), scope.clone());
let else_typed = else_result.typed.clone();
let else_diags = else_result.diagnostics.clone();
let then_rt = rt_type(then_typed.clone());
let else_rt = rt_type(else_typed.clone());
let branch_diags = if node_type_compatible(then_rt.clone(), else_rt.clone()) {
                    vec![]
} else {
                    vec![inference_error(v2_rt::concat(v2_rt::concat(v2_rt::concat("if branches resolve to incompatible types: ".to_string(), node_type_shape(then_rt.clone())), " vs ".to_string()), node_type_shape(else_rt.clone())), span.clone(), scope.module_name.clone())]
};
let resolved_type = prefer_specific_type(then_rt.clone(), else_rt.clone());
let if_texpr = make_expr_node(Rc::new(ExprData::ExprIf), vec![cond_typed.clone(), then_typed.clone(), else_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: resolved_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: if_texpr.clone(),
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(cond_diags.clone(), then_diags.clone()), else_diags.clone()), branch_diags.clone()),
})
},
    None => {
                let if_texpr2 = make_expr_node(Rc::new(ExprData::ExprIf), vec![cond_typed.clone(), then_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Unit".to_string()),
})), span.clone());
Rc::new(InferResult {
    typed: if_texpr2.clone(),
    diagnostics: v2_rt::concat(cond_diags.clone(), then_diags.clone()),
})
},
}
},
    ExprData::ExprLet => {
            let let_name = let_binding_name(texpr.clone());
            let span = texpr.span.clone();
let let_value = let_value(texpr.clone());
let let_body = let_body(texpr.clone());
let val_result = infer_expr(let_value.clone(), scope.clone());
let val_typed = val_result.typed.clone();
let val_diags = val_result.diagnostics.clone();
let val_type = rt_type(val_typed.clone());
if (let_body.clone() == None) {
                {
                    let let_texpr = make_named_expr_node(let_name.clone(), Rc::new(ExprData::ExprLet), vec![val_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: val_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: let_texpr.clone(),
    diagnostics: val_diags.clone(),
})
}
} else {
                {
                    let extended = extend_scope(scope.clone(), let_name.clone(), val_type.clone());
let body_result = infer_expr(let_body.clone().unwrap(), extended.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let let_texpr2 = make_named_expr_node(let_name.clone(), Rc::new(ExprData::ExprLet), vec![val_typed.clone(), body_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: let_texpr2.clone(),
    diagnostics: v2_rt::concat(val_diags.clone(), body_diags.clone()),
})
}
}
},
    ExprData::ExprRecordLit { .. } => {
            let tn = record_lit_type_name(texpr.clone());
            let span = texpr.span.clone();
let field_inits = texpr.children.clone();
infer_record_lit(tn.clone(), field_inits.clone(), span.clone(), scope.clone())
},
    ExprData::ExprListLit => {
            let span = texpr.span.clone();
let elements = texpr.children.clone();
let elem_results = { let mut __result = Vec::new(); for e in elements.clone().iter().cloned() { __result.push(infer_expr(e.clone(), scope.clone())); } __result };
let typed_elements = { let mut __result = Vec::new(); for r in elem_results.clone().iter().cloned() { __result.push(r.typed.clone()); } __result };
let elem_diags = { let mut __result = Vec::new(); for r in elem_results.clone().iter().cloned() { __result.extend(r.diagnostics.clone()); } __result };
let elem_type_node = if ((elem_results.clone().len() as i64) > 0) {
                match elem_results.clone().first().cloned() {
    Some(r) => rt_type(r.typed.clone()),
    None => leaf_node("Unit".to_string()),
}
} else {
                leaf_node("Unit".to_string())
};
let ll_texpr = make_expr_node(Rc::new(ExprData::ExprListLit), typed_elements.clone(), Some(Rc::new(InferredNode::Resolved {
    node: container_node("List".to_string(), elem_type_node.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: ll_texpr.clone(),
    diagnostics: elem_diags.clone(),
})
},
    ExprData::ExprBinOp { op: op, .. } => {
            let span = texpr.span.clone();
let left_expr = binop_left(texpr.clone());
let right_expr = binop_right(texpr.clone());
let left_result = infer_expr(left_expr.clone(), scope.clone());
let left_typed = left_result.typed.clone();
let left_diags = left_result.diagnostics.clone();
let right_result = infer_expr(right_expr.clone(), scope.clone());
let right_typed = right_result.typed.clone();
let right_diags = right_result.diagnostics.clone();
let result_type = infer_binop_type_node(op.clone(), rt_type(left_typed.clone()));
let bo_texpr = make_expr_node(Rc::new(ExprData::ExprBinOp {
    op: op.clone(),
}), vec![left_typed.clone(), right_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: result_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: bo_texpr.clone(),
    diagnostics: v2_rt::concat(left_diags.clone(), right_diags.clone()),
})
},
    ExprData::ExprUnaryOp { op: op, .. } => {
            let span = texpr.span.clone();
let operand_expr = unaryop_operand(texpr.clone());
let operand_result = infer_expr(operand_expr.clone(), scope.clone());
let operand_typed = operand_result.typed.clone();
let operand_diags = operand_result.diagnostics.clone();
let result_type = match op.clone() {
    UnaryOpKind::Not => leaf_node("Bool".to_string()),
    UnaryOpKind::Neg => rt_type(operand_typed.clone()),
};
let uo_texpr = make_expr_node(Rc::new(ExprData::ExprUnaryOp {
    op: op.clone(),
}), vec![operand_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: result_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: uo_texpr.clone(),
    diagnostics: operand_diags.clone(),
})
},
    ExprData::ExprLambda { .. } => {
            let lam_params = lambda_param_names(texpr.clone());
            let span = texpr.span.clone();
let lam_body = lambda_body(texpr.clone());
let lam_param_nodes: Vec<Rc<Node>> = texpr.children.iter().skip(1).cloned().collect();
let lam_scope = extend_scope_with_params(scope.clone(), lam_params.clone());
let body_result = infer_expr(lam_body.clone(), lam_scope.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let lam_texpr = make_expr_node(Rc::new(ExprData::ExprLambda {
semantics: Some(lambda_semantics_from_param_types(lambda_param_types_from_scope(lam_scope.clone(), lam_params.clone()))),
}), v2_rt::concat(vec![body_typed.clone()], { let mut __result = Vec::new(); for p in lam_params.clone().iter().cloned() { __result.push(leaf_node(p.clone())); } __result }), Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: lam_texpr.clone(),
    diagnostics: body_diags.clone(),
})
},
    ExprData::ExprStringInterp => {
            let span = texpr.span.clone();
let typed_part_nodes = { let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.push(match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => part_node.clone(),
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = infer_expr(inner.clone(), scope.clone());
make_interp_part_node(r.typed.clone(), part_node.span.clone())
},
    None => part_node.clone(),
},
}); } __result };
let interp_diags = { let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.extend(match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => vec![],
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = infer_expr(inner.clone(), scope.clone());
r.diagnostics.clone()
},
    None => vec![],
},
}); } __result };
let si_texpr = make_expr_node(Rc::new(ExprData::ExprStringInterp), typed_part_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("String".to_string()),
})), span.clone());
Rc::new(InferResult {
    typed: si_texpr.clone(),
    diagnostics: interp_diags.clone(),
})
},
    ExprData::ExprBlock => {
            let span = texpr.span.clone();
let stmts = texpr.children.clone();
if ((stmts.clone().len() as i64) > 0) {
                {
                    let state = infer_block_stmts(stmts.clone(), scope.clone(), vec![], vec![], leaf_node("Unit".to_string()));
let blk_texpr = make_expr_node(Rc::new(ExprData::ExprBlock), state.typed_stmts.clone(), Some(Rc::new(InferredNode::Resolved {
    node: state.last_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: blk_texpr.clone(),
    diagnostics: { let mut __result = Vec::new(); for c in state.diag_chunks.clone().iter().cloned() { __result.extend(c.clone()); } __result },
})
}
} else {
                ok_infer(make_expr_node(Rc::new(ExprData::ExprBlock), vec![], Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Unit".to_string()),
})), span.clone()))
}
},
    ExprData::ExprCast => {
            let span = texpr.span.clone();
let cast_inner = cast_expr(texpr.clone());
let target_type = cast_target(texpr.clone());
let inner_result = infer_expr(cast_inner.clone(), scope.clone());
let inner_typed = inner_result.typed.clone();
let inner_diags = inner_result.diagnostics.clone();
let cast_texpr = make_expr_node(Rc::new(ExprData::ExprCast), vec![inner_typed.clone(), target_type.clone()], Some(Rc::new(InferredNode::Resolved {
    node: target_type.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: cast_texpr.clone(),
    diagnostics: inner_diags.clone(),
})
},
    ExprData::ExprForEach => {
            let variable = foreach_variable(texpr.clone());
            let span = texpr.span.clone();
let coll = foreach_collection(texpr.clone());
let body_expr = foreach_body(texpr.clone());
let coll_result = infer_expr(coll.clone(), scope.clone());
let coll_typed = coll_result.typed.clone();
let coll_diags = coll_result.diagnostics.clone();
let elem_type_node = for_each_element_type_node(rt_type(coll_typed.clone()));
let body_scope = extend_scope(scope.clone(), variable.clone(), elem_type_node.clone());
let body_result = infer_expr(body_expr.clone(), body_scope.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let fe_texpr = make_named_expr_node(variable.clone(), Rc::new(ExprData::ExprForEach), vec![coll_typed.clone(), body_typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: rt_type(body_typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: fe_texpr.clone(),
    diagnostics: v2_rt::concat(coll_diags.clone(), body_diags.clone()),
})
},
    ExprData::ExprIndex => {
            let span = texpr.span.clone();
let base_expr = index_base(texpr.clone());
let idx_key = index_expr(texpr.clone());
let base_result = infer_expr(base_expr.clone(), scope.clone());
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let index_result = infer_expr(idx_key.clone(), scope.clone());
let index_typed = index_result.typed.clone();
let index_diags = index_result.diagnostics.clone();
let base_type_result = rt_node(base_typed.clone());
let index_type_result = rt_node(index_typed.clone());
let index_check = match (*base_type_result.clone()).clone() {
    NodeType::Typed { node: base_type, .. } => match (*index_type_result.clone()).clone() {
    NodeType::Typed { node: index_type, .. } => Some(check_index_access_node(base_type.clone(), index_type.clone(), span.clone(), scope.module_name.clone())),
    NodeType::InferError { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::Untyped => None,
};
let access_failure_source = match (*base_type_result.clone()).clone() {
    NodeType::Typed { .. } => index_type_result.clone(),
    NodeType::InferError { .. } => base_type_result.clone(),
    NodeType::Untyped => base_type_result.clone(),
};
let idx_inferred = match index_check.clone() {
    Some(checked) => checked.inferred.clone(),
    None => Some(inferred_from_node_type(access_failure_source.clone(), "invalid index access".to_string(), span.clone())),
};
let index_access_diags = match index_check.clone() {
    Some(checked) => checked.diagnostics.clone(),
    None => vec![],
};
let idx_texpr = make_expr_node(Rc::new(ExprData::ExprIndex), vec![base_typed.clone(), index_typed.clone()], idx_inferred.clone(), span.clone());
Rc::new(InferResult {
    typed: idx_texpr.clone(),
    diagnostics: v2_rt::concat(v2_rt::concat(base_diags.clone(), index_diags.clone()), index_access_diags.clone()),
})
},
    ExprData::ExprSlice => {
            let span = texpr.span.clone();
let base_expr = slice_base(texpr.clone());
let start_expr = slice_start(texpr.clone());
let end_expr = slice_end(texpr.clone());
let base_result = infer_expr(base_expr.clone(), scope.clone());
let base_typed = base_result.typed.clone();
let base_diags = base_result.diagnostics.clone();
let start_result = infer_expr(start_expr.clone(), scope.clone());
let start_typed = start_result.typed.clone();
let start_diags = start_result.diagnostics.clone();
let end_result = infer_expr(end_expr.clone(), scope.clone());
let end_typed = end_result.typed.clone();
let end_diags = end_result.diagnostics.clone();
let base_type_result = rt_node(base_typed.clone());
let start_type_result = rt_node(start_typed.clone());
let end_type_result = rt_node(end_typed.clone());
let slice_check = match (*base_type_result.clone()).clone() {
    NodeType::Typed { node: base_type, .. } => match (*start_type_result.clone()).clone() {
    NodeType::Typed { node: start_type, .. } => match (*end_type_result.clone()).clone() {
    NodeType::Typed { node: end_type, .. } => Some(check_slice_access_node(base_type.clone(), start_type.clone(), end_type.clone(), span.clone(), scope.module_name.clone())),
    NodeType::InferError { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::Untyped => None,
},
    NodeType::InferError { .. } => None,
    NodeType::Untyped => None,
};
let access_failure_source = match (*base_type_result.clone()).clone() {
    NodeType::Typed { .. } => match (*start_type_result.clone()).clone() {
    NodeType::Typed { .. } => end_type_result.clone(),
    NodeType::InferError { .. } => start_type_result.clone(),
    NodeType::Untyped => start_type_result.clone(),
},
    NodeType::InferError { .. } => base_type_result.clone(),
    NodeType::Untyped => base_type_result.clone(),
};
let slc_inferred = match slice_check.clone() {
    Some(checked) => checked.inferred.clone(),
    None => Some(inferred_from_node_type(access_failure_source.clone(), "invalid slice access".to_string(), span.clone())),
};
let slice_access_diags = match slice_check.clone() {
    Some(checked) => checked.diagnostics.clone(),
    None => vec![],
};
let slc_texpr = make_expr_node(Rc::new(ExprData::ExprSlice), vec![base_typed.clone(), start_typed.clone(), end_typed.clone()], slc_inferred.clone(), span.clone());
Rc::new(InferResult {
    typed: slc_texpr.clone(),
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(base_diags.clone(), start_diags.clone()), end_diags.clone()), slice_access_diags.clone()),
})
},
    ExprData::ExprReturn => {
            let span = texpr.span.clone();
let inner_expr = return_value(texpr.clone());
let inner_result = infer_expr(inner_expr.clone(), scope.clone());
let ret_texpr = make_expr_node(Rc::new(ExprData::ExprReturn), vec![inner_result.typed.clone()], Some(Rc::new(InferredNode::Resolved {
    node: rt_type(inner_result.typed.clone()),
})), span.clone());
Rc::new(InferResult {
    typed: ret_texpr.clone(),
    diagnostics: inner_result.diagnostics.clone(),
})
},
    _ => {
            let span = texpr.span.clone();
Rc::new(InferResult {
    typed: make_expr_error_node(ExprErrorKind::InternalExprError, "unhandled expression variant in infer_expr".to_string(), span.clone()),
    diagnostics: vec![inference_error("unhandled expression variant in infer_expr".to_string(), span.clone(), scope.module_name.clone())],
})
},
}
    })
}

pub fn infer_record_lit(type_name: Option<String>, field_inits: Vec<Rc<Node>>, span: Rc<SourceSpan>, scope: Rc<InferScope>) -> Rc<InferResult> {
    {
        let fi_infer_results = { let mut __result = Vec::new(); for fi in field_inits.clone().iter().cloned() { __result.push({
            let ar = infer_expr(field_init_node_value(fi.clone()), scope.clone());
let ar_typed = ar.typed.clone();
let ar_diags = ar.diagnostics.clone();
Rc::new(FieldInferResult {
    typed_field: make_field_init_node(field_init_node_name(fi.clone()), ar_typed.clone(), fi.span.clone()),
    infer_result: ar.clone(),
    diagnostics: ar_diags.clone(),
})
}); } __result };
let typed_fields = { let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.push(fir.typed_field.clone()); } __result };
let fi_diags = { let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.extend(fir.diagnostics.clone()); } __result };
if (type_name.clone() == None) {
            {
                let child_nodes = { let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { __result.push(Rc::new(Node {
    name: fir.typed_field.clone().name.clone(),
    span: no_span(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: rt_type(fir.infer_result.clone().typed.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})); } __result };
let anon_node = Rc::new(Node {
    name: "".to_string(),
    span: no_span(),
    children: child_nodes.clone(),
    connective: Some(Connective::Conj),
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let typed_field_nodes = typed_fields.clone();
let texpr = make_expr_node(Rc::new(ExprData::ExprRecordLit {
parent_enum: None,
}), typed_field_nodes.clone(), Some(Rc::new(InferredNode::Resolved {
    node: anon_node.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: texpr.clone(),
    diagnostics: fi_diags.clone(),
})
}
} else {
            {
                let type_lookup = lookup_type(scope.type_env.clone(), type_name.clone().unwrap());
let local_variant_parent = lookup_variant_parent_enum(scope.clone(), type_name.clone().unwrap());
let effective_lookup = match type_lookup.clone() {
    Some(_) => type_lookup.clone(),
    None => {
                    let local_lookup = lookup_in_scope(scope.locals.clone(), type_name.clone().unwrap());
match local_lookup.clone() {
    Some(local_node) => lookup_type(scope.type_env.clone(), local_node.name.clone()),
    None => None,
}
},
};
let raw_resolved = match effective_lookup.clone() {
    Some(tn) => tn.clone(),
    None => error_type_node(),
};
let expected_optional_parent = Some("Optional".to_string());
let is_some_ctor = ((type_name.clone().unwrap() == "Some".to_string()) && (local_variant_parent.clone() == expected_optional_parent.clone()));
let resolved_node = if is_some_ctor.clone() {
                    {
                        let val_field = { let mut __result = Vec::new(); for fir in fi_infer_results.clone().iter().cloned() { if (fir.typed_field.clone().name.clone() == "value".to_string()) { __result.push(fir); } } __result }.first().cloned();
match val_field.clone() {
    Some(val_fir) => with_optional_cardinality(rt_type(val_fir.infer_result.clone().typed.clone())),
    None => raw_resolved.clone(),
}
}
} else {
                    raw_resolved.clone()
};
let type_diags = match effective_lookup.clone() {
    Some(_) => vec![],
    None => vec![inference_error(v2_rt::concat(v2_rt::concat("type '".to_string(), type_name.clone().unwrap()), "' not found in scope".to_string()), span.clone(), scope.module_name.clone())],
};
let typed_field_nodes2 = typed_fields.clone();
let texpr = make_named_expr_node(type_name.clone().unwrap(), Rc::new(ExprData::ExprRecordLit {
parent_enum: local_variant_parent.clone(),
}), typed_field_nodes2.clone(), Some(Rc::new(InferredNode::Resolved {
    node: resolved_node.clone(),
})), span.clone());
Rc::new(InferResult {
    typed: texpr.clone(),
    diagnostics: v2_rt::concat(fi_diags.clone(), type_diags.clone()),
})
}
}
}
}

pub fn infer_item(item: Rc<Node>, scope: Rc<InferScope>) -> Rc<TypedItemResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let typed_anno = item.type_annotation.clone();
if (node_has_structure(item.clone()) && (item.transport.clone() == None)) {
                Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: { let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result },
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Unit".to_string()),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: item.connective.clone(),
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: vec![],
})
} else {
                if ((item.body.clone() != None) && ((item.params.clone().len() as i64) > 0)) {
                    {
                        let fn_scope = build_params_scope(scope.clone(), item.params.clone());
let body_result = infer_expr(item.body.clone().clone().unwrap(), fn_scope.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let resolved_ret = if (item.inferred.clone() == None) {
                            rt_type(body_typed.clone())
} else {
                            rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: { let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result },
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret.clone(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: Some(body_typed.clone()),
    connective: None,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: expr_has_self_call(body_typed.clone(), item.name.clone()),
    has_non_tail_self_call: expr_has_non_tail_self_call(body_typed.clone(), item.name.clone(), true),
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: body_diags.clone(),
})
}
} else {
                    if (((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.inferred.clone() != None)) {
                        {
                            let fn_scope = scope.clone();
let body_result = infer_expr(item.body.clone().clone().unwrap(), fn_scope.clone());
let body_typed = body_result.typed.clone();
let body_diags = body_result.diagnostics.clone();
let resolved_ret = rt_type(item.clone());
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret.clone(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: Some(body_typed.clone()),
    connective: None,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: body_diags.clone(),
})
}
} else {
                        if ((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) {
                            {
                                let val_result = infer_expr(item.body.clone().clone().unwrap(), scope.clone());
let val_typed = val_result.typed.clone();
let val_diags = val_result.diagnostics.clone();
let resolved_ret = if (item.type_annotation.clone() == None) {
                                    rt_type(val_typed.clone())
} else {
                                    item.type_annotation.clone().clone().unwrap()
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret.clone(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: Some(val_typed.clone()),
    connective: None,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: val_diags.clone(),
})
}
} else {
                            if (((item.params.clone().len() as i64) > 0) && (item.body.clone() == None)) {
                                {
                                    let resolved_ret = if (item.inferred.clone() == None) {
                                        leaf_node("Unit".to_string())
} else {
                                        rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: { let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result },
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret.clone(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: None,
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: vec![],
})
}
} else {
                                {
                                    let resolved_ret = if (item.inferred.clone() == None) {
                                        leaf_node("Unit".to_string())
} else {
                                        rt_type(item.clone())
};
Rc::new(TypedItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: { let mut __result = Vec::new(); for c in item.children.clone().iter().cloned() { __result.push(infer_item(c.clone(), scope.clone()).item.clone()); } __result },
    params: item.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: resolved_ret.clone(),
})),
    return_cardinality: item.return_cardinality.clone(),
    uses: item.uses.clone(),
    body: None,
    connective: item.connective.clone(),
    transport: item.transport.clone(),
    properties: item.properties.clone(),
    type_annotation: typed_anno.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: vec![],
})
}
}
}
}
}
}
}
    })
}

pub fn infer_items(items: Vec<Rc<Node>>, scope: Rc<InferScope>) -> Vec<Rc<TypedItemResult>> {
    { let mut __result = Vec::new(); for item in items.clone().iter().cloned() { __result.push(infer_item(item.clone(), scope.clone())); } __result }
}

pub fn build_type_env(module: Rc<ResolvedModule>, parent_index: HashMap<String, Rc<TypedModule>>) -> Rc<BuildTypeEnvResult> {
    {
        let zero_span = SourceSpan::new(0, 0);
let kernel_bindings = KERNEL_TYPES.clone().iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: Rc::new(Node {
    name: name.clone(),
    span: no_span(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
})));
let some_value_field = Rc::new(Node {
    name: "value".to_string(),
    span: zero_span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Dynamic".to_string()),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let some_variant = Rc::new(Node {
    name: "Some".to_string(),
    span: zero_span.clone(),
    children: vec![some_value_field.clone()],
    connective: None,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_optional = Rc::new(Node {
    name: "Optional".to_string(),
    span: zero_span.clone(),
    children: vec![some_variant.clone(), leaf_node("None".to_string())],
    connective: Some(Connective::Disj),
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_bindings = v2_rt::map_insert(kernel_bindings.clone(), "Optional".to_string(), Rc::new(TypeBinding {
    name: "Optional".to_string(),
    resolved: kernel_optional.clone(),
}));
let kernel = Rc::new(TypeEnv {
    bindings: kernel_bindings.clone(),
    recursive_types: vec![],
    recursive_type_set: <HashMap<_, _>>::new(),
});
let parent_envs = { let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend(match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => vec![typed_parent.type_env.clone()],
    None => vec![],
}); } __result };
let import_bindings = parent_envs.clone().iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, env: Rc<TypeEnv>| v2_rt::map_merge(acc.clone(), env.bindings.clone()));
let import_recursive = parent_envs.clone().iter().cloned().fold(vec![], |acc: _, env: Rc<TypeEnv>| v2_rt::concat(acc.clone(), env.recursive_types.clone()));
let import_recursive_set = parent_envs.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, env: Rc<TypeEnv>| v2_rt::map_merge(acc.clone(), env.recursive_type_set.clone()));
let import_env = Rc::new(TypeEnv {
    bindings: import_bindings.clone(),
    recursive_types: import_recursive.clone(),
    recursive_type_set: import_recursive_set.clone(),
});
let import_diags = { let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend(match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(_) => vec![],
    None => vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError { message: format!("missing parent environment for imported module '{}' while typechecking '{}'", imp.module_path, module.module.name), span: imp.target_span.clone().clone().unwrap() }), module.module.clone().name.clone())],
}); } __result };
let local_bindings = module_items(module.module.clone()).iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, item: Rc<Node>| if node_has_structure(item.clone()) {
            {
                let type_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: item.children.clone(),
    connective: item.connective.clone(),
    params: item.params.clone(),
    inferred: None,
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: type_node.clone(),
}))
}
} else {
            if (((item.inferred.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.body.clone() == None)) {
                {
                    let alias_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: item.inferred.clone(),
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: alias_node.clone(),
}))
}
} else {
                if ((item.transport.clone() == None) && ((item.children.clone().len() as i64) > 0)) {
                    {
                        let ref_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_node(item.name.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: ref_node.clone(),
}))
}
} else {
                    if (((((item.params.clone().len() as i64) > 0) && (node_has_structure(item.clone()) == false)) && (item.body.clone() == None)) && (item.transport.clone() == None)) {
                        {
                            let bare_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    connective: None,
    params: item.params.clone(),
    inferred: None,
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: bare_node.clone(),
}))
}
} else {
                        if ((((((item.properties.clone().len() as i64) > 0) && (node_has_structure(item.clone()) == false)) && (item.transport.clone() == None)) && (item.inferred.clone() == None)) && ((item.params.clone().len() as i64) == 0)) {
                            v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: leaf_node(item.name.clone()),
}))
} else {
                            acc.clone()
}
}
}
}
});
let param_bindings = module_items(module.module.clone()).iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, item: Rc<Node>| {
            let is_type_decl = match v2_rt::map_get(&local_bindings, item.name.clone()) {
    Some(_) => true,
    None => false,
};
if (((item.params.clone().len() as i64) > 0) && is_type_decl.clone()) {
                {
                    let result = item.params.clone().iter().cloned().fold(acc.clone(), |pacc: _, p: Rc<Node>| v2_rt::map_insert(pacc.clone(), p.name.clone(), Rc::new(TypeBinding {
    name: p.name.clone(),
    resolved: leaf_node(p.name.clone()),
})));
result.clone()
}
} else {
                acc.clone()
}
});
let local_name_set = v2_rt::map_values(&local_bindings).iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, b: Rc<TypeBinding>| v2_rt::map_insert(acc.clone(), b.name.clone(), true));
let all_local_bindings = v2_rt::map_merge(local_bindings.clone(), param_bindings.clone());
let local_env = Rc::new(TypeEnv {
    bindings: all_local_bindings.clone(),
    recursive_types: vec![],
    recursive_type_set: <HashMap<_, _>>::new(),
});
let merged = merge_envs(vec![kernel.clone(), import_env.clone(), local_env.clone()]);
let all_deps_map = v2_rt::map_values(&merged.bindings.clone()).iter().cloned().fold(<HashMap<String, Vec<String>>>::new(), |acc: _, b: Rc<TypeBinding>| v2_rt::map_insert(acc.clone(), b.name.clone(), node_type_deps(b.resolved.clone())));
let cycle_set = detect_type_cycles_kahn(all_deps_map.clone(), merged.bindings.clone());
let cycle_map = cycle_set.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), true));
let unresolved_env = Rc::new(TypeEnv {
    bindings: merged.bindings.clone(),
    recursive_types: cycle_set.clone(),
    recursive_type_set: cycle_map.clone(),
});
let resolved = resolve_env_bindings(unresolved_env.clone(), module.module.clone().name.clone(), local_name_set.clone(), all_deps_map.clone());
let resolved_env_out = resolved.env.clone();
let resolved_diags = resolved.diagnostics.clone();
Rc::new(BuildTypeEnvResult {
    env: resolved_env_out.clone(),
    diagnostics: v2_rt::concat(import_diags.clone(), resolved_diags.clone()),
})
}
}

pub fn build_type_env_unresolved(module: Rc<ResolvedModule>, parent_index: HashMap<String, Rc<TypedModule>>) -> Rc<BuildTypeEnvResult> {
    {
        let zero_span = SourceSpan::new(0, 0);
let kernel_bindings = KERNEL_TYPES.clone().iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: Rc::new(Node {
    name: name.clone(),
    span: no_span(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
})));
let some_value_field = Rc::new(Node {
    name: "value".to_string(),
    span: zero_span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_node("Dynamic".to_string()),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let some_variant = Rc::new(Node {
    name: "Some".to_string(),
    span: zero_span.clone(),
    children: vec![some_value_field.clone()],
    connective: None,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_optional = Rc::new(Node {
    name: "Optional".to_string(),
    span: zero_span.clone(),
    children: vec![some_variant.clone(), leaf_node("None".to_string())],
    connective: Some(Connective::Disj),
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kernel_bindings = v2_rt::map_insert(kernel_bindings.clone(), "Optional".to_string(), Rc::new(TypeBinding {
    name: "Optional".to_string(),
    resolved: kernel_optional.clone(),
}));
let kernel = Rc::new(TypeEnv {
    bindings: kernel_bindings.clone(),
    recursive_types: vec![],
    recursive_type_set: <HashMap<_, _>>::new(),
});
let parent_envs = { let mut __result = Vec::new(); for imp in module.resolved_imports.clone().iter().cloned() { __result.extend(match v2_rt::map_get(&parent_index, imp.module_path.clone()) {
    Some(typed_parent) => vec![typed_parent.type_env.clone()],
    None => vec![],
}); } __result };
let import_bindings = parent_envs.clone().iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, env: Rc<TypeEnv>| v2_rt::map_merge(acc.clone(), env.bindings.clone()));
let import_recursive = parent_envs.clone().iter().cloned().fold(vec![], |acc: _, env: Rc<TypeEnv>| v2_rt::concat(acc.clone(), env.recursive_types.clone()));
let import_recursive_set = parent_envs.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, env: Rc<TypeEnv>| v2_rt::map_merge(acc.clone(), env.recursive_type_set.clone()));
let import_env = Rc::new(TypeEnv {
    bindings: import_bindings.clone(),
    recursive_types: import_recursive.clone(),
    recursive_type_set: import_recursive_set.clone(),
});
let local_bindings = module_items(module.module.clone()).iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, item: Rc<Node>| if node_has_structure(item.clone()) {
            {
                let type_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: item.children.clone(),
    connective: item.connective.clone(),
    params: vec![],
    inferred: None,
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: type_node.clone(),
}))
}
} else {
            if (((item.inferred.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.body.clone() == None)) {
                {
                    let alias_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: item.inferred.clone(),
    return_cardinality: item.return_cardinality.clone(),
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: alias_node.clone(),
}))
}
} else {
                if ((item.transport.clone() == None) && ((item.children.clone().len() as i64) > 0)) {
                    {
                        let ref_node = Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    children: vec![],
    connective: None,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_node(item.name.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: ref_node.clone(),
}))
}
} else {
                    if ((((((item.properties.clone().len() as i64) > 0) && (node_has_structure(item.clone()) == false)) && (item.transport.clone() == None)) && (item.inferred.clone() == None)) && ((item.params.clone().len() as i64) == 0)) {
                        v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: leaf_node(item.name.clone()),
}))
} else {
                        acc.clone()
}
}
}
});
let local_env = Rc::new(TypeEnv {
    bindings: local_bindings.clone(),
    recursive_types: vec![],
    recursive_type_set: <HashMap<_, _>>::new(),
});
let merged = merge_envs(vec![kernel.clone(), import_env.clone(), local_env.clone()]);
let all_deps_map = v2_rt::map_values(&merged.bindings.clone()).iter().cloned().fold(<HashMap<String, Vec<String>>>::new(), |acc: _, b: Rc<TypeBinding>| v2_rt::map_insert(acc.clone(), b.name.clone(), node_type_deps(b.resolved.clone())));
let cycle_set = detect_type_cycles_kahn(all_deps_map.clone(), merged.bindings.clone());
let cycle_map = cycle_set.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), true));
let unresolved_env = Rc::new(TypeEnv {
    bindings: merged.bindings.clone(),
    recursive_types: cycle_set.clone(),
    recursive_type_set: cycle_map.clone(),
});
Rc::new(BuildTypeEnvResult {
    env: unresolved_env.clone(),
    diagnostics: vec![],
})
}
}

pub fn build_item_info(item: Rc<Node>) -> Rc<ItemInfo> {
    {
        let kind = item_kind(item.clone());
let res_names = { let mut __result = Vec::new(); for u in item.uses.clone().iter().cloned() { __result.push(resource_use_name(u.clone())); } __result };
match kind.clone() {
    ItemKind::FuncItem => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: if (item.body.clone() == None) {
            vec![]
} else {
            collect_typed_service_calls(item.body.clone().clone().unwrap())
},
    resource_names: res_names.clone(),
    params: item.params.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
}),
    ItemKind::FnItem => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: vec![],
    resource_names: res_names.clone(),
    params: item.params.clone(),
    is_self_recursive: if (item.body.clone() == None) {
            false
} else {
            expr_has_self_call(item.body.clone().clone().unwrap(), item.name.clone())
},
    has_non_tail_self_call: if (item.body.clone() == None) {
            false
} else {
            expr_has_non_tail_self_call(item.body.clone().clone().unwrap(), item.name.clone(), true)
},
}),
    _ => Rc::new(ItemInfo {
    name: item.name.clone(),
    kind: kind.clone(),
    service_names: vec![],
    resource_names: res_names.clone(),
    params: item.params.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
}),
}
}
}

pub fn analyze_item(item: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ItemContribution> {
    {
        let resolved = resolve_item_types(item.clone(), env.clone(), module_name.clone());
let ritem = resolved.item.clone();
let is_func = ((ritem.params.clone().len() as i64) > 0);
let is_zero_arg_func = ((((ritem.params.clone().len() as i64) == 0) && (ritem.inferred.clone() != None)) && (ritem.body.clone() != None));
let func_sig = if (is_func.clone() || is_zero_arg_func.clone()) {
            {
                let declared_rt = if (ritem.inferred.clone() != None) {
                    Some(rt_type(ritem.clone()))
} else {
                    None
};
Some(Rc::new(DeclaredFuncSig {
    name: ritem.name.clone(),
    params: ritem.params.clone(),
    inferred: declared_rt.clone(),
    is_async: ((ritem.uses.clone().len() as i64) > 0),
}))
}
} else {
            None
};
let svc_entries = if ((ritem.transport.clone() != None) && ((ritem.children.clone().len() as i64) > 0)) {
            { let mut __result = Vec::new(); for c in ritem.children.clone().iter().cloned() { __result.push(service_op_entry(c.clone())); } __result }
} else {
            vec![]
};
let svc_local = if ((ritem.transport.clone() != None) && ((ritem.children.clone().len() as i64) > 0)) {
            {
                let root = namespace_root_from_properties(ritem.properties.clone(), ritem.name.clone());
Some(Rc::new(TypeBinding {
    name: root.clone(),
    resolved: leaf_node(root.clone()),
}))
}
} else {
            None
};
Rc::new(ItemContribution {
    resolved_item: ritem.clone(),
    resolve_diagnostics: resolved.diagnostics.clone(),
    func_sig: func_sig.clone(),
    svc_entries: svc_entries.clone(),
    svc_local: svc_local.clone(),
    item_info: build_item_info(ritem.clone()),
})
}
}

pub fn fold_module_contributions(mut remaining: Vec<Rc<ItemContribution>>, mut resolved_items: Vec<Rc<Node>>, mut func_sigs: HashMap<String, Rc<DeclaredFuncSig>>, mut svc_registry: HashMap<String, Vec<Rc<OpEntry>>>, mut svc_locals: HashMap<String, Rc<TypeBinding>>, mut item_registry: HashMap<String, Rc<ItemInfo>>, mut diag_chunks: Vec<Vec<Rc<ErrorNode>>>) -> Rc<LocalContributionState> {
    loop {
        match remaining.clone().first().cloned() {
    None => { break Rc::new(LocalContributionState {
    resolved_items: resolved_items.clone(),
    func_sigs: func_sigs.clone(),
    svc_registry: svc_registry.clone(),
    svc_locals: svc_locals.clone(),
    item_registry: item_registry.clone(),
    diag_chunks: diag_chunks.clone(),
}); },
    Some(contribution) => { let next_func_sigs = match contribution.func_sig.clone() {
    Some(sig) => v2_rt::map_insert(func_sigs.clone(), sig.name.clone(), sig.clone()),
    None => func_sigs.clone(),
};
let next_svc_registry = if ((contribution.svc_entries.clone().len() as i64) > 0) {
            v2_rt::map_insert(svc_registry.clone(), contribution.resolved_item.clone().name.clone(), contribution.svc_entries.clone())
} else {
            svc_registry.clone()
};
let next_svc_locals = match contribution.svc_local.clone() {
    Some(binding) => v2_rt::map_insert(svc_locals.clone(), binding.name.clone(), binding.clone()),
    None => svc_locals.clone(),
};
{
            let __tco_0 = remaining.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let __tco_1 = v2_rt::list_push(resolved_items.clone(), contribution.resolved_item.clone());
let __tco_2 = next_func_sigs.clone();
let __tco_3 = next_svc_registry.clone();
let __tco_4 = next_svc_locals.clone();
let __tco_5 = v2_rt::map_insert(item_registry.clone(), contribution.item_info.clone().name.clone(), contribution.item_info.clone());
let __tco_6 = v2_rt::list_push(diag_chunks.clone(), contribution.resolve_diagnostics.clone());
remaining = __tco_0;
resolved_items = __tco_1;
func_sigs = __tco_2;
svc_registry = __tco_3;
svc_locals = __tco_4;
item_registry = __tco_5;
diag_chunks = __tco_6;
continue;
} },
}
}
}

pub fn build_module_context(contributions: Vec<Rc<ItemContribution>>, parent_index: HashMap<String, Rc<TypedModule>>, resolved_imports: Vec<Rc<ResolvedImport>>, env: Rc<TypeEnv>, module_name: String) -> Rc<ModuleContext> {
    {
        let local = fold_module_contributions(contributions.clone(), vec![], <HashMap<_, _>>::new(), <HashMap<_, _>>::new(), <HashMap<_, _>>::new(), <HashMap<_, _>>::new(), vec![]);
let imported_variant_locals = v2_rt::map_values(&env.bindings.clone()).iter().cloned().fold(<HashMap<String, Rc<TypeBinding>>>::new(), |acc: _, binding: Rc<TypeBinding>| if node_is_coproduct(binding.resolved.clone()) {
            binding.resolved.clone().children.clone().iter().cloned().fold(acc.clone(), |vacc: _, child: Rc<Node>| v2_rt::map_insert(vacc.clone(), child.name.clone(), Rc::new(TypeBinding {
    name: child.name.clone(),
    resolved: binding.resolved.clone(),
})))
} else {
            acc.clone()
});
let env_variant_locals = variant_locals_from_items(local.resolved_items.clone(), imported_variant_locals.clone());
let merged_scope = merge_scope_from_imports(resolved_imports.clone(), parent_index.clone(), env.clone(), <HashMap<_, _>>::new(), local.svc_registry.clone(), local.svc_locals.clone());
let all_declared_sigs = v2_rt::map_merge(merged_scope.func_sigs.clone(), local.func_sigs.clone());
let resolve_result = resolve_func_sigs(all_declared_sigs.clone(), local.resolved_items.clone(), module_name.clone());
let all_locals = v2_rt::map_values(&merged_scope.svc_locals.clone()).iter().cloned().fold(env_variant_locals.clone(), |acc: _, binding: Rc<TypeBinding>| v2_rt::map_insert(acc.clone(), binding.name.clone(), binding.clone()));
Rc::new(ModuleContext {
    resolved_items: local.resolved_items.clone(),
    func_env: resolve_result.func_env.clone(),
    svc_registry: merged_scope.svc_registry.clone(),
    locals: all_locals.clone(),
    item_registry: local.item_registry.clone(),
    diagnostics: v2_rt::concat({ let mut __result = Vec::new(); for c in local.diag_chunks.clone().iter().cloned() { __result.extend(c.clone()); } __result }, resolve_result.diagnostics.clone()),
})
}
}

#[derive(Debug, Clone, PartialEq)]
pub struct TypecheckModuleResult {
    pub typed: Rc<TypedModule>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

pub fn typecheck_module(resolved: Rc<ResolvedModule>, parent_index: HashMap<String, Rc<TypedModule>>) -> Rc<TypecheckModuleResult> {
    {
        let env_result = build_type_env(resolved.clone(), parent_index.clone());
let env = env_result.env.clone();
let env_diags = env_result.diagnostics.clone();
let env_errors = { let mut __result = Vec::new(); for d in env_diags.clone().iter().cloned() { if is_error_diagnostic(d.diagnostic.clone()) { __result.push(d); } } __result };
if ((env_errors.clone().len() as i64) > 0) {
            return Rc::new(TypecheckModuleResult {
    typed: Rc::new(TypedModule {
    module: resolved.module.clone(),
    items: vec![],
    type_env: env.clone(),
    func_env: Rc::new(ResolvedFuncEnv {
    signatures: <HashMap<_, _>>::new(),
}),
    item_registry: <HashMap<_, _>>::new(),
}),
    diagnostics: env_diags.clone(),
})
}
let contributions = { let mut __result = Vec::new(); for item in module_items(resolved.module.clone()).iter().cloned() { __result.push(analyze_item(item.clone(), env.clone(), resolved.module.clone().name.clone())); } __result };
let ctx = build_module_context(contributions.clone(), parent_index.clone(), resolved.resolved_imports.clone(), env.clone(), resolved.module.clone().name.clone());
let data_locals = ctx.resolved_items.clone().iter().cloned().fold(ctx.locals.clone(), |acc: _, item: Rc<Node>| if ((((item.body.clone() != None) && ((item.params.clone().len() as i64) == 0)) && (item.inferred.clone() == None)) && (item.type_annotation.clone() != None)) {
            v2_rt::map_insert(acc.clone(), item.name.clone(), Rc::new(TypeBinding {
    name: item.name.clone(),
    resolved: item.type_annotation.clone().clone().unwrap(),
}))
} else {
            acc.clone()
});
let infer_scope = Rc::new(InferScope {
    type_env: env.clone(),
    func_env: ctx.func_env.clone(),
    locals: data_locals.clone(),
    module_name: resolved.module.clone().name.clone(),
    service_registry: ctx.svc_registry.clone(),
    item_registry: ctx.item_registry.clone(),
});
let typed_item_results = infer_items(ctx.resolved_items.clone(), infer_scope.clone());
let typed_items = { let mut __result = Vec::new(); for tir in typed_item_results.clone().iter().cloned() { __result.push(tir.item.clone()); } __result };
let infer_diags = { let mut __result = Vec::new(); for tir in typed_item_results.clone().iter().cloned() { __result.extend(tir.diagnostics.clone()); } __result };
let typed_module = module_node(resolved.module.clone().name.clone(), module_imports(resolved.module.clone()), ctx.resolved_items.clone(), resolved.module.clone().span.clone());
Rc::new(TypecheckModuleResult {
    typed: Rc::new(TypedModule {
    module: typed_module.clone(),
    items: typed_items.clone(),
    type_env: env.clone(),
    func_env: ctx.func_env.clone(),
    item_registry: ctx.item_registry.clone(),
}),
    diagnostics: v2_rt::concat(v2_rt::concat(env_diags.clone(), ctx.diagnostics.clone()), infer_diags.clone()),
})
}
}

#[derive(Debug, Clone, PartialEq)]
pub struct EnvResolveResult {
    pub env: Rc<TypeEnv>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct BindingResult {
    pub binding: Rc<TypeBinding>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct BindingsAccum {
    pub bindings: HashMap<String, Rc<TypeBinding>>,
    pub diagnostics: Vec<Rc<ErrorNode>>,
}

pub fn resolve_env_bindings(env: Rc<TypeEnv>, module_name: String, local_names: HashMap<String, bool>, deps_map: HashMap<String, Vec<String>>) -> Rc<EnvResolveResult> {
    {
        let remaining = { let mut __result = Vec::new(); for b in { let mut __result = Vec::new(); for b in v2_rt::map_values(&env.bindings.clone()).iter().cloned() { if emit_map_has(local_names.clone(), b.name.clone()) { __result.push(b); } } __result }.iter().cloned() { __result.push(b.name.clone()); } __result };
topo_resolve_types(remaining.clone(), env.clone(), module_name.clone(), vec![], local_names.clone(), deps_map.clone())
}
}

pub fn topo_resolve_types(mut remaining: Vec<String>, mut env: Rc<TypeEnv>, mut module_name: String, mut diagnostics: Vec<Rc<ErrorNode>>, mut local_names: HashMap<String, bool>, mut deps_map: HashMap<String, Vec<String>>) -> Rc<EnvResolveResult> {
    loop {
        if ((remaining.clone().len() as i64) == 0) {
            return Rc::new(EnvResolveResult {
    env: env.clone(),
    diagnostics: diagnostics.clone(),
})
}
let remaining_set = remaining.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), true));
let ready = { let mut __result = Vec::new(); for name in remaining.clone().iter().cloned() { if match v2_rt::map_get(&deps_map, name.clone()) {
    Some(deps) => { let mut __all = true; for dep in deps.clone().iter().cloned() { if !(((((is_kernel_type(dep.clone()) || (dep.clone() == "None".to_string())) || (dep.clone() == "".to_string())) || is_recursive_type(env.clone(), dep.clone())) || (emit_map_has(remaining_set.clone(), dep.clone()) == false))) { __all = false; break; } } __all },
    None => true,
} { __result.push(name); } } __result };
if ((ready.clone().len() as i64) == 0) {
            {
                let stuck_accum = remaining.clone().iter().cloned().fold(Rc::new(BindingsAccum {
    bindings: env.bindings.clone(),
    diagnostics: vec![],
}), |acc: _, name: String| match v2_rt::map_get(&env.bindings.clone(), name.clone()) {
    Some(binding) => {
                    let result = resolve_node(binding.resolved.clone(), env.clone(), module_name.clone());
Rc::new(BindingsAccum {
    bindings: v2_rt::map_insert(acc.bindings.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: result.resolved.clone(),
})),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), result.diagnostics.clone()),
})
},
    None => acc.clone(),
});
return Rc::new(EnvResolveResult {
    env: Rc::new(TypeEnv {
    bindings: stuck_accum.bindings.clone(),
    recursive_types: env.recursive_types.clone(),
    recursive_type_set: env.recursive_type_set.clone(),
}),
    diagnostics: v2_rt::concat(diagnostics.clone(), stuck_accum.diagnostics.clone()),
})
}
}
let ready_accum = ready.clone().iter().cloned().fold(Rc::new(BindingsAccum {
    bindings: env.bindings.clone(),
    diagnostics: vec![],
}), |acc: _, name: String| match v2_rt::map_get(&env.bindings.clone(), name.clone()) {
    Some(binding) => {
            let result = resolve_node(binding.resolved.clone(), env.clone(), module_name.clone());
Rc::new(BindingsAccum {
    bindings: v2_rt::map_insert(acc.bindings.clone(), name.clone(), Rc::new(TypeBinding {
    name: name.clone(),
    resolved: result.resolved.clone(),
})),
    diagnostics: v2_rt::concat(acc.diagnostics.clone(), result.diagnostics.clone()),
})
},
    None => acc.clone(),
});
let ready_set = ready.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, name: String| v2_rt::map_insert(acc.clone(), name.clone(), true));
let next_remaining = { let mut __result = Vec::new(); for name in remaining.clone().iter().cloned() { if (emit_map_has(ready_set.clone(), name.clone()) == false) { __result.push(name); } } __result };
{
            let __tco_0 = next_remaining.clone();
let __tco_1 = Rc::new(TypeEnv {
    bindings: ready_accum.bindings.clone(),
    recursive_types: env.recursive_types.clone(),
    recursive_type_set: env.recursive_type_set.clone(),
});
let __tco_2 = module_name.clone();
let __tco_3 = v2_rt::concat(diagnostics.clone(), ready_accum.diagnostics.clone());
let __tco_4 = local_names.clone();
let __tco_5 = deps_map.clone();
remaining = __tco_0;
env = __tco_1;
module_name = __tco_2;
diagnostics = __tco_3;
local_names = __tco_4;
deps_map = __tco_5;
continue;
}
}
}

pub fn collect_parent_envs(resolved: Rc<ResolvedModule>, module_index: HashMap<String, Rc<TypedModule>>) -> Rc<ParentModulesResult> {
    {
        let modules = { let mut __result = Vec::new(); for imp in resolved.resolved_imports.clone().iter().cloned() { __result.extend(match v2_rt::map_get(&module_index, imp.module_path.clone()) {
    Some(typed) => vec![typed.clone()],
    None => vec![],
}); } __result };
let diagnostics = { let mut __result = Vec::new(); for imp in resolved.resolved_imports.clone().iter().cloned() { __result.extend(match v2_rt::map_get(&module_index, imp.module_path.clone()) {
    Some(_) => vec![],
    None => vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError { message: format!("missing parent environment for imported module '{}' while ordering '{}'", imp.module_path, resolved.module.name), span: imp.target_span.clone().clone().unwrap() }), resolved.module.clone().name.clone())],
}); } __result };
Rc::new(ParentModulesResult {
    modules: modules.clone(),
    diagnostics: diagnostics.clone(),
})
}
}

pub fn build_emit_graph_info(modules: Vec<Rc<TypedModule>>) -> Rc<EmitGraphInfo> {
    {
        let init = Rc::new(EmitInfoBuildState {
    type_summaries: <HashMap<_, _>>::new(),
    variant_to_enum: <HashMap<_, _>>::new(),
    enum_variant_membership: <HashMap<_, _>>::new(),
    field_type_names: <HashMap<_, _>>::new(),
});
let built = modules.clone().iter().cloned().fold(init.clone(), |state: _, typed_module: Rc<TypedModule>| typed_module.items.clone().iter().cloned().fold(state.clone(), |inner_state: _, item: Rc<Node>| add_emit_item_summary(inner_state.clone(), item.clone())));
let all_recursive = modules.clone().iter().cloned().fold(<HashMap<String, bool>>::new(), |acc: _, m: Rc<TypedModule>| v2_rt::map_merge(acc.clone(), m.type_env.clone().recursive_type_set.clone()));
Rc::new(EmitGraphInfo {
    type_summaries: built.type_summaries.clone(),
    variant_to_enum: built.variant_to_enum.clone(),
    enum_variant_membership: built.enum_variant_membership.clone(),
    field_type_names: built.field_type_names.clone(),
    recursive_type_set: all_recursive.clone(),
})
}
}

pub fn typecheck(graph: Rc<ModuleGraph>) -> Rc<TypedGraph> {
    typecheck_modules(graph.modules.clone(), vec![], <HashMap<_, _>>::new(), <HashMap<_, _>>::new(), vec![])
}

pub fn typecheck_modules(mut remaining: Vec<Rc<ResolvedModule>>, mut modules: Vec<Rc<TypedModule>>, mut module_index: HashMap<String, Rc<TypedModule>>, mut item_registry: HashMap<String, Rc<ItemInfo>>, mut diag_chunks: Vec<Vec<Rc<ErrorNode>>>) -> Rc<TypedGraph> {
    loop {
        match remaining.clone().first().cloned() {
    None => { let expanded_registry = expand_transitive_services(modules.clone(), item_registry.clone(), 5);
break Rc::new(TypedGraph {
    modules: modules.clone(),
    item_registry: expanded_registry.clone(),
    diagnostics: { let mut __result = Vec::new(); for c in diag_chunks.clone().iter().cloned() { __result.extend(c.clone()); } __result },
}); },
    Some(resolved) => { let parent_result = collect_parent_envs(resolved.clone(), module_index.clone());
let tc_result = typecheck_module(resolved.clone(), module_index.clone());
let typed = tc_result.typed.clone();
let tc_diags = tc_result.diagnostics.clone();
{
            let __tco_0 = remaining.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>();
let __tco_1 = v2_rt::list_push(modules.clone(), typed.clone());
let __tco_2 = v2_rt::map_insert(module_index.clone(), typed.module.clone().name.clone(), typed.clone());
let __tco_3 = v2_rt::map_merge(item_registry.clone(), typed.item_registry.clone());
let __tco_4 = v2_rt::list_push(v2_rt::list_push(diag_chunks.clone(), parent_result.diagnostics.clone()), tc_diags.clone());
remaining = __tco_0;
modules = __tco_1;
module_index = __tco_2;
item_registry = __tco_3;
diag_chunks = __tco_4;
continue;
} },
}
}
}

pub fn reconcile(graph: Rc<ModuleGraph>) -> Rc<ResolvedGraph> {
    {
        let typed = typecheck(graph.clone());
let emit_info = build_emit_graph_info(typed.modules.clone());
Rc::new(ResolvedGraph {
    modules: typed.modules.clone(),
    item_registry: typed.item_registry.clone(),
    diagnostics: typed.diagnostics.clone(),
    emit_graph_info: emit_info.clone(),
})
}
}
