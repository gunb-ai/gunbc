// Generated by v2 compiler -- do not edit.
// Source module: std.coercion

use crate::v2_rt;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use std::collections::BTreeSet;
use std::collections::HashMap;
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeCheckpoint {
    pub dag_name: compile_error!("UNRESOLVED_CompilerError"),
    pub target_type: compile_error!("UNRESOLVED_CompilerError"),
    pub default_expr: compile_error!("UNRESOLVED_CompilerError"),
    pub is_copy: compile_error!("UNRESOLVED_CompilerError"),
    pub literal_suffix: compile_error!("UNRESOLVED_CompilerError"),
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct InhabitantDecl {
    pub algebra: compile_error!("UNRESOLVED_CompilerError"),
    pub template: compile_error!("UNRESOLVED_CompilerError"),
    pub arity: compile_error!("UNRESOLVED_CompilerError"),
    pub identity_expr: compile_error!("UNRESOLVED_CompilerError"),
    pub import_path: compile_error!("UNRESOLVED_CompilerError"),
    pub is_copy: compile_error!("UNRESOLVED_CompilerError"),
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CallableRepr {
    pub template: compile_error!("UNRESOLVED_CompilerError"),
    pub param_separator: compile_error!("UNRESOLVED_CompilerError"),
    pub return_separator: compile_error!("UNRESOLVED_CompilerError"),
    pub import_path: compile_error!("UNRESOLVED_CompilerError"),
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CastRule {
    pub from_type: compile_error!("UNRESOLVED_CompilerError"),
    pub to_type: compile_error!("UNRESOLVED_CompilerError"),
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CastSyntax {
    pub template: compile_error!("UNRESOLVED_CompilerError"),
    pub cast_rules: Rc<compile_error!("UNRESOLVED_CompilerError")>,
}

pub fn dag_cast_rules() -> List<CastRule> {
    thread_local! {
        static CACHED: List<CastRule> = {
            serde_json::from_value(serde_json::json!([{"from_type": "Int", "to_type": "Int"}, {"from_type": "Int", "to_type": "Float"}, {"from_type": "Float", "to_type": "Int"}, {"from_type": "Float", "to_type": "Float"}, {"from_type": "Bool", "to_type": "Int"}]))
                .expect("valid data definition")
        };
    }
    CACHED.with(|c: &List<CastRule>| c.clone())
}

pub fn dag_can_cast(source_type: String, target_type: String) -> bool {
    {
        let mut __found = false;
        for r in dag_cast_rules().iter().cloned() {
            if ((r.from_type.clone().as_str() == source_type.clone().as_str())
                && (r.to_type.clone().as_str() == target_type.clone().as_str()))
            {
                __found = true;
                break;
            }
        }
        __found
    }
}

pub fn is_dag_cast_domain_type(name: String) -> bool {
    {
        let mut __found = false;
        for r in dag_cast_rules().iter().cloned() {
            if ((r.from_type.clone().as_str() == name.clone().as_str())
                || (r.to_type.clone().as_str() == name.clone().as_str()))
            {
                __found = true;
                break;
            }
        }
        __found
    }
}
