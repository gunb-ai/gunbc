// Generated by v2 compiler -- do not edit.
// Source module: std.http_path

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;
use crate::NonEmptyVec;
use crate::NonEmptyBTreeSet;
use UrlPathToken::*;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum UrlPathToken {
    LiteralToken {
        text: String,
    },
    ParamToken {
        name: String,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PathTemplate {
    pub tokens: Rc<Vec<Rc<UrlPathToken>>>,
}

pub fn parse_path_template(raw: &String) -> Option<Rc<PathTemplate>> {
    {
        let path_only = match Rc::new(raw.clone().split(&"?".to_string()).map(|s| s.to_string()).collect::<Vec<_>>()).first().cloned() {
    Some(p) => p.clone(),
    None => raw.clone(),
};
let segments = Rc::new({ let mut __result = Vec::new(); for s in Rc::new(path_only.split(&"/".to_string()).map(|s| s.to_string()).collect::<Vec<_>>()).iter().cloned() { if (s.clone().as_str() != "".to_string().as_str()) { __result.push(s); } } __result });
match segments.first().cloned() {
    None => Some(Rc::new(PathTemplate {
    tokens: Rc::new(vec![]),
})),
    Some(first_seg) => match parse_segment_tokens(&first_seg) {
    None => None,
    Some(first_tokens) => {
    let tokens = segments.iter().skip(1).cloned().fold(Some(first_tokens.clone()), |acc: Option<Rc<Vec<Rc<UrlPathToken>>>>, seg: String| match acc.clone() {
    None => None,
    Some(parsed) => match parse_segment_tokens(&seg) {
    Some(seg_tokens) => Some(v2_rt::concat(parsed.clone(), seg_tokens.clone())),
    None => None,
},
});
match tokens {
    Some(parsed) => Some(Rc::new(PathTemplate {
    tokens: parsed.clone(),
})),
    None => None,
}
},
}
}
}
}

pub fn parse_segment_tokens(seg: &String) -> Option<Rc<Vec<Rc<UrlPathToken>>>> {
    if !v2_rt::contains(seg.clone(), "{".to_string()) {
        if v2_rt::contains(seg.clone(), "}".to_string()) {
            None
        } else {
            Some(Rc::new(vec![Rc::new(UrlPathToken::LiteralToken {
    text: seg.clone(),
})]))
        }
    } else {
        {
            let before_and_rest = Rc::new(seg.clone().split(&"{".to_string()).map(|s| s.to_string()).collect::<Vec<_>>());
if ((before_and_rest.clone().len() as i64) != 2) {
                return None
            }
let prefix = match before_and_rest.clone().first().cloned() {
    Some(p) => p.clone(),
    None => "".to_string(),
};
let after_open = match before_and_rest.clone().get(1 as usize).cloned() {
    Some(r) => r.clone(),
    None => "".to_string(),
};
let name_and_suffix = Rc::new(after_open.split(&"}".to_string()).map(|s| s.to_string()).collect::<Vec<_>>());
if (v2_rt::contains(prefix.clone(), "}".to_string()) || ((name_and_suffix.clone().len() as i64) != 2)) {
                return None
            }
let param_name = match name_and_suffix.clone().first().cloned() {
    Some(p) => p.clone(),
    None => "".to_string(),
};
let suffix = match name_and_suffix.clone().get(1 as usize).cloned() {
    Some(s) => s.clone(),
    None => "".to_string(),
};
let prefix_tokens = if (prefix.clone().as_str() != "".to_string().as_str()) {
                Rc::new(vec![Rc::new(UrlPathToken::LiteralToken {
    text: prefix.clone(),
})])
            } else {
                Rc::new(vec![])
            };
let param_tokens = if (param_name.clone().as_str() != "".to_string().as_str()) {
                Rc::new(vec![Rc::new(UrlPathToken::ParamToken {
    name: param_name.clone(),
})])
            } else {
                Rc::new(vec![])
            };
let suffix_tokens = if (suffix.clone().as_str() != "".to_string().as_str()) {
                Rc::new(vec![Rc::new(UrlPathToken::LiteralToken {
    text: suffix.clone(),
})])
            } else {
                Rc::new(vec![])
            };
if (((param_name.clone().as_str() == "".to_string().as_str()) || v2_rt::contains(suffix.clone(), "{".to_string())) || v2_rt::contains(suffix.clone(), "}".to_string())) {
                None
            } else {
                Some(v2_rt::concat(v2_rt::concat(prefix_tokens, param_tokens), suffix_tokens))
            }
}
    }
}

pub fn has_path_params(template: Rc<PathTemplate>) -> bool {
    { let mut __found = false; for t in template.tokens.clone().iter().cloned() { if match (*t.clone()).clone() {
    UrlPathToken::ParamToken { .. } => true,
    UrlPathToken::LiteralToken { .. } => false,
} { __found = true; break; } } __found }
}

pub fn last_path_param(template: Rc<PathTemplate>) -> Option<String> {
    {
        let params = Rc::new({ let mut __result = Vec::new(); for t in template.tokens.clone().iter().cloned() { if match (*t.clone()).clone() {
    UrlPathToken::ParamToken { .. } => true,
    UrlPathToken::LiteralToken { .. } => false,
} { __result.push(t); } } __result });
match params.last().cloned() {
    Some(tok) => match (*tok.clone()).clone() {
    UrlPathToken::ParamToken { name: n, .. } => Some(n.clone()),
    UrlPathToken::LiteralToken { .. } => None,
},
    None => None,
}
}
}
