// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.tokenize

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}


#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}

pub use crate::std_types::{SourceSpan};
pub use crate::v2_std_core::{Token, TokenShape};
use crate::v2_std_core::TokenShape::{ShKwModule, ShKwImport, ShKwType, ShKwFn, ShKwFunc, ShKwService, ShKwResource, ShKwData, ShKwExtern, ShKwInterface, ShKwPipeline, ShKwProfile, ShKwPattern, ShKwLet, ShKwReturn, ShKwMatch, ShKwIf, ShKwElse, ShKwFor, ShKwIn, ShKwWhere, ShKwWith, ShKwTrue, ShKwFalse, ShKwNone, ShKwAcquire, ShKwRelease, ShKwCapability, ShKwOperation, ShKwInput, ShKwOutput, ShKwIdempotent, ShKwReadonly, ShKwHermetic, ShLBrace, ShRBrace, ShLParen, ShRParen, ShLBracket, ShRBracket, ShLt, ShGt, ShLe, ShGe, ShFatArrow, ShArrow, ShColon, ShComma, ShDot, ShDotDot, ShEq, ShEqEq, ShNe, ShPlus, ShMinus, ShStar, ShSlash, ShPercent, ShBang, ShAnd, ShOr, ShQuestion, ShNullCoalesce, ShPipe, ShPipeArrow, ShLitStr, ShLitInt, ShLitFloat, ShIdent, ShStrBegin, ShStrMid, ShStrEnd, ShNewline, ShEof, ShUnknown};
use StringScanResult::*;

lazy_static::lazy_static! {
    pub static ref KEYWORDS: HashMap<String, TokenShape> = {
        let mut __m = HashMap::new();
        __m.insert("module".to_string(), TokenShape::ShKwModule);
        __m.insert("import".to_string(), TokenShape::ShKwImport);
        __m.insert("type".to_string(), TokenShape::ShKwType);
        __m.insert("fn".to_string(), TokenShape::ShKwFn);
        __m.insert("func".to_string(), TokenShape::ShKwFunc);
        __m.insert("service".to_string(), TokenShape::ShKwService);
        __m.insert("resource".to_string(), TokenShape::ShKwResource);
        __m.insert("data".to_string(), TokenShape::ShKwData);
        __m.insert("extern".to_string(), TokenShape::ShKwExtern);
        __m.insert("interface".to_string(), TokenShape::ShKwInterface);
        __m.insert("pipeline".to_string(), TokenShape::ShKwPipeline);
        __m.insert("profile".to_string(), TokenShape::ShKwProfile);
        __m.insert("pattern".to_string(), TokenShape::ShKwPattern);
        __m.insert("let".to_string(), TokenShape::ShKwLet);
        __m.insert("return".to_string(), TokenShape::ShKwReturn);
        __m.insert("match".to_string(), TokenShape::ShKwMatch);
        __m.insert("if".to_string(), TokenShape::ShKwIf);
        __m.insert("else".to_string(), TokenShape::ShKwElse);
        __m.insert("for".to_string(), TokenShape::ShKwFor);
        __m.insert("in".to_string(), TokenShape::ShKwIn);
        __m.insert("where".to_string(), TokenShape::ShKwWhere);
        __m.insert("with".to_string(), TokenShape::ShKwWith);
        __m.insert("true".to_string(), TokenShape::ShKwTrue);
        __m.insert("false".to_string(), TokenShape::ShKwFalse);
        __m.insert("none".to_string(), TokenShape::ShKwNone);
        __m.insert("null".to_string(), TokenShape::ShKwNone);
        __m.insert("acquire".to_string(), TokenShape::ShKwAcquire);
        __m.insert("release".to_string(), TokenShape::ShKwRelease);
        __m.insert("capability".to_string(), TokenShape::ShKwCapability);
        __m.insert("operation".to_string(), TokenShape::ShKwOperation);
        __m.insert("input".to_string(), TokenShape::ShKwInput);
        __m.insert("output".to_string(), TokenShape::ShKwOutput);
        __m.insert("idempotent".to_string(), TokenShape::ShKwIdempotent);
        __m.insert("readonly".to_string(), TokenShape::ShKwReadonly);
        __m.insert("hermetic".to_string(), TokenShape::ShKwHermetic);
        __m
    };
}

lazy_static::lazy_static! {
    pub static ref SINGLE_PUNCT: HashMap<String, TokenShape> = {
        let mut __m = HashMap::new();
        __m.insert("(".to_string(), TokenShape::ShLParen);
        __m.insert(")".to_string(), TokenShape::ShRParen);
        __m.insert("[".to_string(), TokenShape::ShLBracket);
        __m.insert("]".to_string(), TokenShape::ShRBracket);
        __m.insert(":".to_string(), TokenShape::ShColon);
        __m.insert(",".to_string(), TokenShape::ShComma);
        __m.insert(".".to_string(), TokenShape::ShDot);
        __m.insert("+".to_string(), TokenShape::ShPlus);
        __m.insert("*".to_string(), TokenShape::ShStar);
        __m.insert("%".to_string(), TokenShape::ShPercent);
        __m.insert("/".to_string(), TokenShape::ShSlash);
        __m
    };
}

#[derive(Debug, Clone, PartialEq)]
pub struct TokenizerState {
    pub pos: i64,
    pub tokens: Vec<Rc<Token>>,
    pub interp_depth: Vec<i64>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct TokPos {
    pub pos: i64,
    pub interp_depth: Vec<i64>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct ScanResult {
    pub pos: i64,
    pub token: Rc<Token>,
    pub interp_depth: Vec<i64>,
}

#[derive(Debug, Clone, PartialEq)]
pub struct SourceRef {
    pub text: String,
}

pub fn make_token(text: String, span: Rc<SourceSpan>, shape: TokenShape) -> Rc<Token> {
    Rc::new(Token {
    text: text.clone(),
    span: span.clone(),
    shape: shape.clone(),
})
}

pub fn tokenize(source: String) -> Vec<Rc<Token>> {
    {
        let src = Rc::new(SourceRef {
    text: source.clone(),
});
let initial = Rc::new(TokPos {
    pos: 0,
    interp_depth: vec![],
});
let final_state = tokenize_loop(src.clone(), vec![], initial.clone());
let eof_span = Rc::new(SourceSpan {
    start: final_state.pos.clone(),
    end: final_state.pos.clone(),
});
v2_rt::list_push(final_state.tokens.clone(), make_token("".to_string(), eof_span.clone(), TokenShape::ShEof))
}
}

pub fn tokenize_loop(source: Rc<SourceRef>, tokens: Vec<Rc<Token>>, pos: Rc<TokPos>) -> Rc<TokenizerState> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let s = skip_spaces_and_comments(source.clone(), pos.clone());
if (s.pos.clone() >= v2_rt::string_length(source.text.clone())) {
                return Rc::new(TokenizerState {
    pos: s.pos.clone(),
    tokens: tokens.clone(),
    interp_depth: s.interp_depth.clone(),
})
}
let ch = v2_rt::char_at(source.text.clone(), s.pos.clone());
if (ch.clone() == "
".to_string()) {
                {
                    let tok = make_token("
".to_string(), Rc::new(SourceSpan {
    start: s.pos.clone(),
    end: (s.pos.clone() + 1),
}), TokenShape::ShNewline);
return tokenize_loop(source.clone(), v2_rt::list_push(tokens.clone(), tok.clone()), Rc::new(TokPos {
    pos: (s.pos.clone() + 1),
    interp_depth: s.interp_depth.clone(),
}))
}
}
if ((ch.clone() == "}".to_string()) && ((s.interp_depth.clone().len() as i64) > 0)) {
                {
                    let top = s.interp_depth.clone().last().cloned().clone().unwrap();
if (top.clone() == 0) {
                        {
                            let popped = drop_last(s.interp_depth.clone());
let cont_pos = Rc::new(TokPos {
    pos: (s.pos.clone() + 1),
    interp_depth: popped.clone(),
});
let result = scan_str_cont(source.clone(), cont_pos.clone(), s.pos.clone());
return tokenize_loop(source.clone(), v2_rt::list_push(tokens.clone(), result.token.clone()), Rc::new(TokPos {
    pos: result.pos.clone(),
    interp_depth: result.interp_depth.clone(),
}))
}
} else {
                        {
                            let new_depth = replace_last(s.interp_depth.clone(), (top.clone() - 1));
let tok = make_token("}".to_string(), Rc::new(SourceSpan {
    start: s.pos.clone(),
    end: (s.pos.clone() + 1),
}), TokenShape::ShRBrace);
return tokenize_loop(source.clone(), v2_rt::list_push(tokens.clone(), tok.clone()), Rc::new(TokPos {
    pos: (s.pos.clone() + 1),
    interp_depth: new_depth.clone(),
}))
}
}
}
}
let result = scan_token(source.clone(), s.clone(), ch.clone());
tokenize_loop(source.clone(), v2_rt::list_push(tokens.clone(), result.token.clone()), Rc::new(TokPos {
    pos: result.pos.clone(),
    interp_depth: result.interp_depth.clone(),
}))
}
    })
}

pub fn scan_token(source: Rc<SourceRef>, pos: Rc<TokPos>, ch: String) -> Rc<ScanResult> {
    {
        if (ch.clone() == "\"".to_string()) {
            return scan_string(source.clone(), pos.clone())
}
if is_digit(ch.clone()) {
            return scan_number(source.clone(), pos.clone())
}
if is_ident_start(ch.clone()) {
            return scan_ident(source.clone(), pos.clone())
}
let next_ch = if ((pos.pos.clone() + 1) < v2_rt::string_length(source.text.clone())) {
            v2_rt::char_at(source.text.clone(), (pos.pos.clone() + 1))
} else {
            "".to_string()
};
if ((ch.clone() == "=".to_string()) && (next_ch.clone() == ">".to_string())) {
            return emit(pos.clone(), TokenShape::ShFatArrow, "=>".to_string(), 2)
}
if ((ch.clone() == "-".to_string()) && (next_ch.clone() == ">".to_string())) {
            return emit(pos.clone(), TokenShape::ShArrow, "->".to_string(), 2)
}
if ((ch.clone() == "=".to_string()) && (next_ch.clone() == "=".to_string())) {
            return emit(pos.clone(), TokenShape::ShEqEq, "==".to_string(), 2)
}
if ((ch.clone() == "!".to_string()) && (next_ch.clone() == "=".to_string())) {
            return emit(pos.clone(), TokenShape::ShNe, "!=".to_string(), 2)
}
if ((ch.clone() == "<".to_string()) && (next_ch.clone() == "=".to_string())) {
            return emit(pos.clone(), TokenShape::ShLe, "<=".to_string(), 2)
}
if ((ch.clone() == ">".to_string()) && (next_ch.clone() == "=".to_string())) {
            return emit(pos.clone(), TokenShape::ShGe, ">=".to_string(), 2)
}
if ((ch.clone() == "&".to_string()) && (next_ch.clone() == "&".to_string())) {
            return emit(pos.clone(), TokenShape::ShAnd, "&&".to_string(), 2)
}
if ((ch.clone() == "|".to_string()) && (next_ch.clone() == "|".to_string())) {
            return emit(pos.clone(), TokenShape::ShOr, "||".to_string(), 2)
}
if ((ch.clone() == "|".to_string()) && (next_ch.clone() == ">".to_string())) {
            return emit(pos.clone(), TokenShape::ShPipeArrow, "|>".to_string(), 2)
}
if (ch.clone() == "|".to_string()) {
            return emit(pos.clone(), TokenShape::ShPipe, "|".to_string(), 1)
}
if ((ch.clone() == "?".to_string()) && (next_ch.clone() == "?".to_string())) {
            return emit(pos.clone(), TokenShape::ShNullCoalesce, "??".to_string(), 2)
}
if ((ch.clone() == ".".to_string()) && (next_ch.clone() == ".".to_string())) {
            return emit(pos.clone(), TokenShape::ShDotDot, "..".to_string(), 2)
}
if (ch.clone() == "=".to_string()) {
            return emit(pos.clone(), TokenShape::ShEq, "=".to_string(), 1)
}
if (ch.clone() == "<".to_string()) {
            return emit(pos.clone(), TokenShape::ShLt, "<".to_string(), 1)
}
if (ch.clone() == ">".to_string()) {
            return emit(pos.clone(), TokenShape::ShGt, ">".to_string(), 1)
}
if (ch.clone() == "-".to_string()) {
            return emit(pos.clone(), TokenShape::ShMinus, "-".to_string(), 1)
}
if (ch.clone() == "!".to_string()) {
            return emit(pos.clone(), TokenShape::ShBang, "!".to_string(), 1)
}
if (ch.clone() == "?".to_string()) {
            return emit(pos.clone(), TokenShape::ShQuestion, "?".to_string(), 1)
}
if (ch.clone() == "{".to_string()) {
            {
                let new_depth = if ((pos.interp_depth.clone().len() as i64) > 0) {
                    replace_last(pos.interp_depth.clone(), (pos.interp_depth.clone().last().cloned().clone().unwrap() + 1))
} else {
                    pos.interp_depth.clone()
};
let tok = make_token("{".to_string(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: (pos.pos.clone() + 1),
}), TokenShape::ShLBrace);
return Rc::new(ScanResult {
    pos: (pos.pos.clone() + 1),
    token: tok.clone(),
    interp_depth: new_depth.clone(),
})
}
}
if (ch.clone() == "}".to_string()) {
            {
                let tok = make_token("}".to_string(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: (pos.pos.clone() + 1),
}), TokenShape::ShRBrace);
return Rc::new(ScanResult {
    pos: (pos.pos.clone() + 1),
    token: tok.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
}
match v2_rt::lookup(&SINGLE_PUNCT, ch.clone()) {
    Some(sh) => emit(pos.clone(), sh.clone(), ch.clone(), 1),
    None => emit(pos.clone(), TokenShape::ShUnknown, ch.clone(), 1),
}
}
}

pub fn emit(pos: Rc<TokPos>, shape: TokenShape, text: String, len: i64) -> Rc<ScanResult> {
    {
        let token = make_token(text.clone(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: (pos.pos.clone() + len.clone()),
}), shape.clone());
Rc::new(ScanResult {
    pos: (pos.pos.clone() + len.clone()),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
}

pub fn scan_ident(source: Rc<SourceRef>, pos: Rc<TokPos>) -> Rc<ScanResult> {
    {
        let end = v2_rt::scan_while(source.text.clone(), pos.pos.clone(), is_ident_char);
let text = v2_rt::substring(source.text.clone(), pos.pos.clone(), end.clone());
let shape = match v2_rt::lookup(&KEYWORDS, text.clone()) {
    Some(sh) => sh.clone(),
    None => TokenShape::ShIdent,
};
let token = make_token(text.clone(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: end.clone(),
}), shape.clone());
Rc::new(ScanResult {
    pos: end.clone(),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
}

pub fn scan_number(source: Rc<SourceRef>, pos: Rc<TokPos>) -> Rc<ScanResult> {
    {
        let int_end = v2_rt::scan_while(source.text.clone(), pos.pos.clone(), is_digit);
if ((((int_end.clone() + 1) < v2_rt::string_length(source.text.clone())) && (v2_rt::char_at(source.text.clone(), int_end.clone()) == ".".to_string())) && is_digit(v2_rt::char_at(source.text.clone(), (int_end.clone() + 1)))) {
            {
                let frac_end = v2_rt::scan_while(source.text.clone(), (int_end.clone() + 1), is_digit);
let text = v2_rt::substring(source.text.clone(), pos.pos.clone(), frac_end.clone());
let token = make_token(text.clone(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: frac_end.clone(),
}), TokenShape::ShLitFloat);
return Rc::new(ScanResult {
    pos: frac_end.clone(),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
}
let text = v2_rt::substring(source.text.clone(), pos.pos.clone(), int_end.clone());
let parsed = v2_rt::parse_int(text.clone());
let shape = match parsed.clone() {
    Some(_) => TokenShape::ShLitInt,
    None => TokenShape::ShUnknown,
};
let token = make_token(text.clone(), Rc::new(SourceSpan {
    start: pos.pos.clone(),
    end: int_end.clone(),
}), shape.clone());
Rc::new(ScanResult {
    pos: int_end.clone(),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
}

#[derive(Debug, Clone, PartialEq)]
pub enum StringScanResult {
    ClosedString {
        content: String,
        end_pos: i64,
    },
    InterpolationStart {
        content: String,
        end_pos: i64,
    },
    UnterminatedString {
        content: String,
        end_pos: i64,
    },
}
impl StringScanResult {
    pub fn content(&self) -> String {
        match self {
            StringScanResult::ClosedString { content: __val, .. } => __val.clone(),
            StringScanResult::InterpolationStart { content: __val, .. } => __val.clone(),
            StringScanResult::UnterminatedString { content: __val, .. } => __val.clone(),
        }
    }
    pub fn end_pos(&self) -> i64 {
        match self {
            StringScanResult::ClosedString { end_pos: __val, .. } => __val.clone(),
            StringScanResult::InterpolationStart { end_pos: __val, .. } => __val.clone(),
            StringScanResult::UnterminatedString { end_pos: __val, .. } => __val.clone(),
        }
    }
}

pub fn scan_string(source: Rc<SourceRef>, pos: Rc<TokPos>) -> Rc<ScanResult> {
    {
        let span_start = pos.pos.clone();
let body_start = (pos.pos.clone() + 1);
let result = scan_string_body(source.clone(), body_start.clone(), vec![]);
match (*result.clone()).clone() {
    StringScanResult::ClosedString { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: (end_pos.clone() + 1),
}), TokenShape::ShLitStr);
Rc::new(ScanResult {
    pos: (end_pos.clone() + 1),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
},
    StringScanResult::InterpolationStart { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: (end_pos.clone() + 1),
}), TokenShape::ShStrBegin);
Rc::new(ScanResult {
    pos: (end_pos.clone() + 1),
    token: token.clone(),
    interp_depth: v2_rt::list_push(pos.interp_depth.clone(), 0),
})
},
    StringScanResult::UnterminatedString { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: end_pos.clone(),
}), TokenShape::ShUnknown);
Rc::new(ScanResult {
    pos: end_pos.clone(),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
},
}
}
}

pub fn scan_str_cont(source: Rc<SourceRef>, pos: Rc<TokPos>, span_start: i64) -> Rc<ScanResult> {
    {
        let result = scan_string_body(source.clone(), pos.pos.clone(), vec![]);
match (*result.clone()).clone() {
    StringScanResult::ClosedString { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: (end_pos.clone() + 1),
}), TokenShape::ShStrEnd);
Rc::new(ScanResult {
    pos: (end_pos.clone() + 1),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
},
    StringScanResult::InterpolationStart { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: (end_pos.clone() + 1),
}), TokenShape::ShStrMid);
Rc::new(ScanResult {
    pos: (end_pos.clone() + 1),
    token: token.clone(),
    interp_depth: v2_rt::list_push(pos.interp_depth.clone(), 0),
})
},
    StringScanResult::UnterminatedString { content: content, end_pos: end_pos, .. } => {
            let processed = process_escapes(content.clone());
let token = make_token(processed.clone(), Rc::new(SourceSpan {
    start: span_start.clone(),
    end: end_pos.clone(),
}), TokenShape::ShUnknown);
Rc::new(ScanResult {
    pos: end_pos.clone(),
    token: token.clone(),
    interp_depth: pos.interp_depth.clone(),
})
},
}
}
}

pub fn scan_string_body(mut source: Rc<SourceRef>, mut pos: i64, mut acc: Vec<String>) -> Rc<StringScanResult> {
    loop {
        if (pos.clone() >= v2_rt::string_length(source.text.clone())) {
            break Rc::new(StringScanResult::UnterminatedString {
    content: acc.clone().join(&"".to_string()),
    end_pos: pos.clone(),
});
} else {
            let ch = v2_rt::char_at(source.text.clone(), pos.clone());
if (ch.clone() == "\"".to_string()) {
                break Rc::new(StringScanResult::ClosedString {
    content: acc.clone().join(&"".to_string()),
    end_pos: pos.clone(),
});
} else {
                if (ch.clone() == "\\".to_string()) {
                    if ((pos.clone() + 1) < v2_rt::string_length(source.text.clone())) {
                        let escaped = v2_rt::char_at(source.text.clone(), (pos.clone() + 1));
{
                            let __tco_0 = source.clone();
let __tco_1 = (pos.clone() + 2);
let __tco_2 = v2_rt::list_push(v2_rt::list_push(acc.clone(), "\\".to_string()), escaped.clone());
source = __tco_0;
pos = __tco_1;
acc = __tco_2;
continue;
}
} else {
                        break Rc::new(StringScanResult::UnterminatedString {
    content: v2_rt::list_push(acc.clone(), "\\".to_string()).join(&"".to_string()),
    end_pos: (pos.clone() + 1),
});
}
} else {
                    if (ch.clone() == "{".to_string()) {
                        if should_start_interpolation(source.clone(), pos.clone()) {
                            break Rc::new(StringScanResult::InterpolationStart {
    content: acc.clone().join(&"".to_string()),
    end_pos: pos.clone(),
});
} else {
                            {
                                let __tco_0 = source.clone();
let __tco_1 = (pos.clone() + 1);
let __tco_2 = v2_rt::list_push(acc.clone(), "{".to_string());
source = __tco_0;
pos = __tco_1;
acc = __tco_2;
continue;
}
}
} else {
                        {
                            let __tco_0 = source.clone();
let __tco_1 = (pos.clone() + 1);
let __tco_2 = v2_rt::list_push(acc.clone(), ch.clone());
source = __tco_0;
pos = __tco_1;
acc = __tco_2;
continue;
}
}
}
}
}
}
}

pub fn should_start_interpolation(source: Rc<SourceRef>, pos: i64) -> bool {
    if ((pos.clone() + 1) >= v2_rt::string_length(source.text.clone())) {
        false
} else {
        {
            let next = v2_rt::char_at(source.text.clone(), (pos.clone() + 1));
(((is_ident_start(next.clone()) || (next.clone() == "(".to_string())) || (next.clone() == "!".to_string())) || (next.clone() == "-".to_string()))
}
}
}

pub fn process_escapes(raw: String) -> String {
    process_escapes_loop(raw.clone(), 0, vec![])
}

pub fn process_escapes_loop(mut source: String, mut pos: i64, mut acc: Vec<String>) -> String {
    loop {
        if (pos.clone() >= v2_rt::string_length(source.clone())) {
            break acc.clone().join(&"".to_string());
} else {
            let ch = v2_rt::char_at(source.clone(), pos.clone());
if ((ch.clone() == "\\".to_string()) && ((pos.clone() + 1) < v2_rt::string_length(source.clone()))) {
                let next = v2_rt::char_at(source.clone(), (pos.clone() + 1));
let resolved = if (next.clone() == "\"".to_string()) {
                    "\"".to_string()
} else {
                    if (next.clone() == "\\".to_string()) {
                        "\\".to_string()
} else {
                        if (next.clone() == "n".to_string()) {
                            "
".to_string()
} else {
                            if (next.clone() == "t".to_string()) {
                                "	".to_string()
} else {
                                v2_rt::concat("\\".to_string(), next.clone())
}
}
}
};
{
                    let __tco_0 = source.clone();
let __tco_1 = (pos.clone() + 2);
let __tco_2 = v2_rt::list_push(acc.clone(), resolved.clone());
source = __tco_0;
pos = __tco_1;
acc = __tco_2;
continue;
}
} else {
                {
                    let __tco_0 = source.clone();
let __tco_1 = (pos.clone() + 1);
let __tco_2 = v2_rt::list_push(acc.clone(), ch.clone());
source = __tco_0;
pos = __tco_1;
acc = __tco_2;
continue;
}
}
}
}
}

pub fn drop_last(stack: Vec<i64>) -> Vec<i64> {
    {
        let len = (stack.clone().len() as i64);
stack.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned().fold(vec![], |result: _, pair: (i64, i64)| if (pair.0.clone() < (len.clone() - 1)) {
            v2_rt::list_push(result.clone(), pair.1.clone())
} else {
            result.clone()
})
}
}

pub fn replace_last(stack: Vec<i64>, value: i64) -> Vec<i64> {
    {
        let prefix = drop_last(stack.clone());
v2_rt::list_push(prefix.clone(), value.clone())
}
}

pub fn skip_spaces_and_comments(source: Rc<SourceRef>, pos: Rc<TokPos>) -> Rc<TokPos> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let p = v2_rt::skip_horizontal_ws(source.text.clone(), pos.pos.clone());
if ((((p.clone() + 1) < v2_rt::string_length(source.text.clone())) && (v2_rt::char_at(source.text.clone(), p.clone()) == "/".to_string())) && (v2_rt::char_at(source.text.clone(), (p.clone() + 1)) == "/".to_string())) {
                {
                    let eol = v2_rt::scan_to_eol(source.text.clone(), p.clone());
return skip_spaces_and_comments(source.clone(), Rc::new(TokPos {
    pos: eol.clone(),
    interp_depth: pos.interp_depth.clone(),
}))
}
}
Rc::new(TokPos {
    pos: p.clone(),
    interp_depth: pos.interp_depth.clone(),
})
}
    })
}

pub fn is_digit(ch: String) -> bool {
    ((ch.clone() >= "0".to_string()) && (ch.clone() <= "9".to_string()))
}

pub fn is_ident_start(ch: String) -> bool {
    ((((ch.clone() >= "a".to_string()) && (ch.clone() <= "z".to_string())) || ((ch.clone() >= "A".to_string()) && (ch.clone() <= "Z".to_string()))) || (ch.clone() == "_".to_string()))
}

pub fn is_ident_char(ch: String) -> bool {
    (is_ident_start(ch.clone()) || is_digit(ch.clone()))
}
