// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.infer_resolve

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}
pub use crate::std_types::{SourceSpan, container_param_name};
pub use crate::v2_std_core::{Node, authored_name_at, NewlineIndex, make_param_node, param_node_name, param_node_type_expr, param_node_default_value, make_field_node, field_node_name, field_node_type_expr, field_node_cardinality, field_node_default_value, field_node_from_key, InferredNode, is_compiler_error, ErrorNode, make_error_node, Cardinality, StringPart, MatchPattern, ExprData, ExprErrorKind, make_expr_node, make_named_expr_node, make_expr_error_node, map_children, expr_var_name, field_access_field, expr_call_func, expr_method_name, let_binding_name, foreach_variable, lambda_param_names, record_lit_type_name, make_arg_node, arg_name, arg_value, make_arm_node, arm_pattern, arm_guard, arm_body, make_field_init_node, field_init_node_name, field_init_node_value, make_resource_use_node, resource_use_name, resource_use_resource, make_text_part_node, make_interp_part_node, make_transport_node, local_transport_node, is_local_transport, is_kernel_type, is_container_type, leaf_node, with_optional_cardinality, with_required_cardinality, Connective, no_span, unit_type, string_type, default_ident_span, node_name_span, CompilerDiagnostic};
use crate::v2_std_core::InferredNode::{Resolved, CompilerError, TypeVariable};
use crate::v2_std_core::CompilerDiagnostic::{InternalError, ArityMismatch, UnresolvedType};
use crate::v2_std_core::Cardinality::{Required, CardOptional};
use crate::v2_std_core::StringPart::{Text, Interpolation};
use crate::v2_std_core::MatchPattern::{Wildcard};
use crate::v2_std_core::ExprData::{NoExprData, ExprLiteral, ExprError, ExprVar, ExprFieldAccess, ExprCall, ExprMethodCall, ExprMatch, ExprIf, ExprLet, ExprRecordLit, ExprListLit, ExprBinOp, ExprUnaryOp, ExprLambda, ExprStringInterp, ExprBlock, ExprCast, ExprForEach, ExprIndex, ExprSlice, ExprReturn};
use crate::v2_std_core::ExprErrorKind::{SemanticExprError};
use crate::v2_std_core::Connective::{Conj, Disj, NoConnective};
pub use crate::v2_compiler_infer_types::{resolved_type, child_type_node, node_is_keyed_collection};
pub use crate::v2_compiler_infer_env::{TypeEnv, TypeBinding, lookup_type, lookup_type_for, is_recursive_type, is_recursive_type_for};
use AliasKind::*;

pub fn is_type_variable(inferred: Rc<InferredNode>) -> bool {
    match (*inferred).clone() {
    InferredNode::TypeVariable { .. } => true,
    _ => false,
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NodeResolveResult {
    pub resolved: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ItemResult {
    pub item: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldResult {
    pub field: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExprResolveResult {
    pub expr: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NamedArgResolveResult {
    pub arg: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MatchArmResolveResult {
    pub arm: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldInitResolveResult {
    pub field_init: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StringPartResolveResult {
    pub part: Rc<StringPart>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransportResolveResult {
    pub transport: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParamResult {
    pub param: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResourceUseResult {
    pub resource_use: Rc<Node>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

pub fn resolve_node(n: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<NodeResolveResult> {
    resolve_node_bounded(n, env, module_name, 0)
}

pub fn is_user_generic_use_site(n: Rc<Node>, env: Rc<TypeEnv>) -> bool {
    {
        let has_structure = (n.connective.clone() != Connective::NoConnective);
if has_structure {
            false
} else {
            match lookup_type_for(env, n.clone()) {
    Some(decl) => if ((decl.params.clone().len() as i64) > 0) {
                if is_container_type(n.name.clone()) {
                    false
} else {
                    if (decl.inferred.clone() != None) {
                        true
} else {
                        true
}
}
} else {
                false
},
    None => false,
}
}
}
}

pub fn substitute_type_slots(n: Rc<Node>, slot_bindings: Rc<HashMap<String, Rc<Node>>>, decl_name: String, source_index: Option<Rc<NewlineIndex>>) -> Rc<Node> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let is_slot = (((((n.children.clone().len() as i64) == 0) && (n.connective.clone() == Connective::NoConnective)) && (n.body.clone() == None)) && (n.inferred.clone() == None));
if is_slot {
                match v2_rt::map_get(&slot_bindings, n.name.clone()) {
    Some(concrete) => concrete.clone(),
    None => n.clone(),
}
} else {
                {
                    let new_children = Rc::new({ let mut __result = Vec::new(); for child in n.children.clone().iter().cloned() { __result.push(if (authored_name_at(source_index.clone(), child.clone()).as_str() == decl_name.clone().as_str()) {
                        {
                            let substituted_args = Rc::new({ let mut __result = Vec::new(); for arg in child.children.clone().iter().cloned() { __result.push(substitute_type_slots(arg.clone(), slot_bindings.clone(), decl_name.clone(), source_index.clone())); } __result });
Rc::new(Node {
    name: child.name.clone(),
    span: child.span.clone(),
    ident_span: child.ident_span.clone(),
    children: substituted_args.clone(),
    connective: child.connective.clone(),
    params: child.params.clone(),
    inferred: child.inferred.clone(),
    return_cardinality: child.return_cardinality.clone(),
    uses: child.uses.clone(),
    body: child.body.clone(),
    transport: child.transport.clone(),
    properties: child.properties.clone(),
    type_annotation: child.type_annotation.clone(),
    is_self_recursive: child.is_self_recursive.clone(),
    has_non_tail_self_call: child.has_non_tail_self_call.clone(),
    match_pattern: child.match_pattern.clone(),
    expr_data: child.expr_data.clone(),
})
}
} else {
                        substitute_type_slots(child.clone(), slot_bindings.clone(), decl_name.clone(), source_index.clone())
}); } __result });
let new_inferred = match n.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: rt, .. }) => Some(Rc::new(InferredNode::Resolved {
    node: substitute_type_slots(rt.clone(), slot_bindings.clone(), decl_name.clone(), source_index.clone()),
})),
    _ => n.inferred.clone(),
};
Rc::new(Node {
    name: n.name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: new_children,
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: new_inferred,
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: n.is_self_recursive.clone(),
    has_non_tail_self_call: n.has_non_tail_self_call.clone(),
    match_pattern: n.match_pattern.clone(),
    expr_data: n.expr_data.clone(),
})
}
}
}
    })
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]

pub enum AliasKind {
    AliasParameterized,
    AliasLeaf,
    AliasPassthrough,
}

pub fn classify_alias(target: Rc<Node>) -> AliasKind {
    {
        let nc = (target.connective.clone() == Connective::NoConnective);
if (((target.children.clone().len() as i64) > 0) && nc.clone()) {
            AliasKind::AliasParameterized
} else {
            if ((((target.children.clone().len() as i64) == 0) && nc.clone()) && (target.ident_span.clone() != None)) {
                AliasKind::AliasLeaf
} else {
                AliasKind::AliasPassthrough
}
}
}
}

pub fn resolve_alias_target(target: Rc<Node>, env: Rc<TypeEnv>, module_name: String, depth: i64) -> Rc<Node> {
    match classify_alias(target.clone()) {
    AliasKind::AliasParameterized => resolve_node_bounded(target.clone(), env, module_name, (depth + 1)).resolved.clone(),
    AliasKind::AliasLeaf => match lookup_type(env, target.name.clone()) {
    Some(env_target) => env_target.clone(),
    None => target.clone(),
},
    AliasKind::AliasPassthrough => target.clone(),
}
}

pub fn resolve_node_bounded(n: Rc<Node>, env: Rc<TypeEnv>, module_name: String, depth: i64) -> Rc<NodeResolveResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            if (depth.clone() > 100) {
                return Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: v2_rt::concat(v2_rt::concat("internal: type resolution exceeded depth 100 for '".to_string(), n.name.clone()), "'".to_string()),
    span: n.span.clone(),
}), module_name.clone())]),
})
}
let has_structure = (n.connective.clone() != Connective::NoConnective);
if has_structure {
                {
                    let is_product = (n.connective.clone() == Connective::Conj);
if is_product {
                        if (n.type_annotation.clone() != None) {
                            match n.children.clone().first().cloned() {
    Some(base) => {
                                let base_result = resolve_node_bounded(base.clone(), env.clone(), module_name.clone(), (depth.clone() + 1));
let base_resolved = base_result.resolved.clone();
let base_diags = base_result.diagnostics.clone();
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: n.name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: Rc::new(vec![base_resolved]),
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: base_diags,
})
},
    None => Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![]),
}),
}
} else {
                            {
                                let child_results = Rc::new({ let mut __result = Vec::new(); for child in n.children.clone().iter().cloned() { __result.push(if (child.inferred.clone() == None) {
                                    Rc::new(NodeResolveResult {
    resolved: child.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                                    {
                                        let child_rt = resolved_type(child.clone());
let rt_result = resolve_node_bounded(child_rt.clone(), env.clone(), module_name.clone(), (depth.clone() + 1));
let rt_resolved = rt_result.resolved.clone();
let rt_diags = rt_result.diagnostics.clone();
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: child.name.clone(),
    span: child.span.clone(),
    ident_span: child.ident_span.clone(),
    children: child.children.clone(),
    connective: child.connective.clone(),
    params: child.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: rt_resolved.clone(),
})),
    return_cardinality: child.return_cardinality.clone(),
    uses: child.uses.clone(),
    body: child.body.clone(),
    transport: child.transport.clone(),
    properties: child.properties.clone(),
    type_annotation: child.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: rt_diags.clone(),
})
}
}); } __result });
let resolved_children = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.push(cr.resolved.clone()); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.extend((*cr.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: n.name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: resolved_children,
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: all_diags,
})
}
}
} else {
                        {
                            let is_optional = (n.return_cardinality.clone() == Cardinality::CardOptional);
if is_optional {
                                {
                                    let inner = with_required_cardinality(n.clone());
let inner_result = resolve_node_bounded(inner, env.clone(), module_name.clone(), (depth.clone() + 1));
let inner_resolved = inner_result.resolved.clone();
let inner_diags = inner_result.diagnostics.clone();
Rc::new(NodeResolveResult {
    resolved: with_optional_cardinality(inner_resolved),
    diagnostics: inner_diags,
})
}
} else {
                                {
                                    let variant_results = Rc::new({ let mut __result = Vec::new(); for variant_child in n.children.clone().iter().cloned() { __result.push({
                                        let field_results = Rc::new({ let mut __result = Vec::new(); for field_child in variant_child.children.clone().iter().cloned() { __result.push(if (field_child.inferred.clone() == None) {
                                            Rc::new(NodeResolveResult {
    resolved: field_child.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                                            {
                                                let field_rt = resolved_type(field_child.clone());
let is_self_ref = ((field_rt.name.clone().as_str() == n.name.clone().as_str()) && ((field_rt.children.clone().len() as i64) > 0));
let rt_result = if is_self_ref.clone() {
                                                    Rc::new(NodeResolveResult {
    resolved: field_rt.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                                                    resolve_node_bounded(field_rt.clone(), env.clone(), module_name.clone(), (depth.clone() + 1))
};
let rt_resolved = rt_result.resolved.clone();
let rt_diags = rt_result.diagnostics.clone();
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: field_child.name.clone(),
    span: field_child.span.clone(),
    ident_span: field_child.ident_span.clone(),
    children: field_child.children.clone(),
    connective: field_child.connective.clone(),
    params: field_child.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: rt_resolved.clone(),
})),
    return_cardinality: field_child.return_cardinality.clone(),
    uses: field_child.uses.clone(),
    body: field_child.body.clone(),
    transport: field_child.transport.clone(),
    properties: field_child.properties.clone(),
    type_annotation: field_child.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: rt_diags.clone(),
})
}
}); } __result });
let resolved_fields = Rc::new({ let mut __result = Vec::new(); for fr in field_results.clone().iter().cloned() { __result.push(fr.resolved.clone()); } __result });
let field_diags = Rc::new({ let mut __result = Vec::new(); for fr in field_results.clone().iter().cloned() { __result.extend((*fr.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: variant_child.name.clone(),
    span: variant_child.span.clone(),
    ident_span: variant_child.ident_span.clone(),
    children: resolved_fields.clone(),
    connective: variant_child.connective.clone(),
    params: variant_child.params.clone(),
    inferred: variant_child.inferred.clone(),
    return_cardinality: variant_child.return_cardinality.clone(),
    uses: variant_child.uses.clone(),
    body: variant_child.body.clone(),
    transport: variant_child.transport.clone(),
    properties: variant_child.properties.clone(),
    type_annotation: variant_child.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: field_diags.clone(),
})
}); } __result });
let resolved_variants = Rc::new({ let mut __result = Vec::new(); for vr in variant_results.clone().iter().cloned() { __result.push(vr.resolved.clone()); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for vr in variant_results.clone().iter().cloned() { __result.extend((*vr.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: n.name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: resolved_variants,
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: all_diags,
})
}
}
}
}
}
} else {
                if (((n.children.clone().len() as i64) > 0) && is_user_generic_use_site(n.clone(), env.clone())) {
                    {
                        let type_name = n.name.clone();
let decl = match lookup_type_for(env.clone(), n.clone()) {
    Some(d) => d.clone(),
    None => n.clone(),
};
let expected_arity = (decl.params.clone().len() as i64);
let actual_arity = (n.children.clone().len() as i64);
let arity_diags = if (expected_arity.clone() != actual_arity.clone()) {
                            Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::ArityMismatch {
    name: type_name.clone(),
    expected: expected_arity.clone(),
    got: actual_arity.clone(),
    span: n.span.clone(),
}), module_name.clone())])
} else {
                            Rc::new(vec![])
};
let arg_results = Rc::new({ let mut __result = Vec::new(); for child in n.children.clone().iter().cloned() { __result.push(resolve_node_bounded(child.clone(), env.clone(), module_name.clone(), (depth.clone() + 1))); } __result });
let resolved_args = Rc::new({ let mut __result = Vec::new(); for ar in arg_results.clone().iter().cloned() { __result.push(ar.resolved.clone()); } __result });
let arg_diags = Rc::new({ let mut __result = Vec::new(); for ar in arg_results.clone().iter().cloned() { __result.extend((*ar.diagnostics.clone()).iter().cloned()); } __result });
let slot_bindings = Rc::new(decl.params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned().fold(v2_rt::rc_empty_map::<Rc<Node>>(), |acc: Rc<HashMap<String, Rc<Node>>>, pair: (i64, Rc<Node>)| {
                            let idx = pair.0.clone();
let slot_name = pair.1.clone().name.clone();
match Rc::new({ let mut __result = Vec::new(); for p in Rc::new({ let mut __result = Vec::new(); for p in Rc::new(resolved_args.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { if (p.0.clone() == idx.clone()) { __result.push(p); } } __result }).iter().cloned() { __result.push(p.1.clone()); } __result }).first().cloned() {
    Some(arg) => v2_rt::rc_map_insert(acc.clone(), slot_name.clone(), arg.clone()),
    None => acc.clone(),
}
});
let is_parameterized_alias = (((decl.inferred.clone() != None) && ((decl.children.clone().len() as i64) == 0)) && (decl.connective.clone() == Connective::NoConnective));
if is_parameterized_alias {
                            {
                                let alias_target = match decl.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: target, .. }) => substitute_type_slots(target.clone(), slot_bindings.clone(), type_name.clone(), env.source_index.clone()),
    _ => n.clone(),
};
let target_result = resolve_node_bounded(alias_target, env.clone(), module_name.clone(), (depth.clone() + 1));
let is_recursive = is_recursive_type(env.clone(), type_name.clone());
let resolved_node = Rc::new(Node {
    name: type_name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: target_result.resolved.clone().children.clone(),
    connective: target_result.resolved.clone().connective.clone(),
    params: Rc::new(vec![]),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: target_result.resolved.clone().properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: is_recursive,
    has_non_tail_self_call: n.has_non_tail_self_call.clone(),
    match_pattern: n.match_pattern.clone(),
    expr_data: n.expr_data.clone(),
});
Rc::new(NodeResolveResult {
    resolved: resolved_node,
    diagnostics: v2_rt::concat(v2_rt::concat(arity_diags, arg_diags), target_result.diagnostics.clone()),
})
}
} else {
                            {
                                let substituted_children = Rc::new({ let mut __result = Vec::new(); for child in decl.children.clone().iter().cloned() { __result.push(substitute_type_slots(child.clone(), slot_bindings.clone(), type_name.clone(), env.source_index.clone())); } __result });
let is_recursive = is_recursive_type(env.clone(), type_name.clone());
let result = Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: type_name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: substituted_children,
    connective: decl.connective.clone(),
    params: Rc::new(vec![]),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: decl.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: is_recursive,
    has_non_tail_self_call: n.has_non_tail_self_call.clone(),
    match_pattern: n.match_pattern.clone(),
    expr_data: n.expr_data.clone(),
}),
    diagnostics: v2_rt::concat(arity_diags, arg_diags),
});
result
}
}
}
} else {
                    {
                        let is_map = node_is_keyed_collection(n.clone());
if is_map {
                            {
                                let type_name = n.name.clone();
let key_child_node = match n.children.clone().first().cloned() {
    Some(k) => k.clone(),
    None => unit_type(),
};
let val_child_node = match n.children.clone().get(1 as usize).cloned() {
    Some(v) => v.clone(),
    None => unit_type(),
};
let key_type = child_type_node(key_child_node.clone());
let val_type = child_type_node(val_child_node.clone());
let key_result = resolve_node_bounded(key_type, env.clone(), module_name.clone(), (depth.clone() + 1));
let key_resolved = key_result.resolved.clone();
let key_diags = key_result.diagnostics.clone();
let val_result = resolve_node_bounded(val_type, env.clone(), module_name.clone(), (depth.clone() + 1));
let val_resolved = val_result.resolved.clone();
let val_diags = val_result.diagnostics.clone();
let key_param_name = if (key_child_node.inferred.clone() != None) {
                                    key_child_node.name.clone()
} else {
                                    match container_param_name(type_name.clone(), 0) {
    Some(pn) => pn.clone(),
    None => key_child_node.name.clone(),
}
};
let val_param_name = if (val_child_node.inferred.clone() != None) {
                                    val_child_node.name.clone()
} else {
                                    match container_param_name(type_name.clone(), 1) {
    Some(pn) => pn.clone(),
    None => val_child_node.name.clone(),
}
};
let resolved_key_child = Rc::new(Node {
    name: key_param_name,
    span: key_child_node.span.clone(),
    ident_span: key_child_node.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: key_resolved,
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let resolved_val_child = Rc::new(Node {
    name: val_param_name,
    span: val_child_node.span.clone(),
    ident_span: val_child_node.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: val_resolved,
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: type_name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: Rc::new(vec![resolved_key_child, resolved_val_child]),
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: v2_rt::concat(key_diags, val_diags),
})
}
} else {
                            if ((n.children.clone().len() as i64) == 1) {
                                {
                                    let type_name = n.name.clone();
match n.children.clone().first().cloned() {
    Some(child_node) => {
                                        let el_type = child_type_node(child_node.clone());
let el_result = resolve_node_bounded(el_type, env.clone(), module_name.clone(), (depth.clone() + 1));
let el_resolved = el_result.resolved.clone();
let el_diags = el_result.diagnostics.clone();
let el_param_name = if (child_node.inferred.clone() != None) {
                                            child_node.name.clone()
} else {
                                            match container_param_name(type_name.clone(), 0) {
    Some(pn) => pn.clone(),
    None => child_node.name.clone(),
}
};
let resolved_child = Rc::new(Node {
    name: el_param_name,
    span: child_node.span.clone(),
    ident_span: child_node.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: el_resolved,
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(NodeResolveResult {
    resolved: Rc::new(Node {
    name: type_name.clone(),
    span: n.span.clone(),
    ident_span: n.ident_span.clone(),
    children: Rc::new(vec![resolved_child]),
    connective: n.connective.clone(),
    params: n.params.clone(),
    inferred: n.inferred.clone(),
    return_cardinality: n.return_cardinality.clone(),
    uses: n.uses.clone(),
    body: n.body.clone(),
    transport: n.transport.clone(),
    properties: n.properties.clone(),
    type_annotation: n.type_annotation.clone(),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: el_diags,
})
},
    None => Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![]),
}),
}
}
} else {
                                if ((n.children.clone().len() as i64) == 0) {
                                    if is_recursive_type_for(env.clone(), n.clone()) {
                                        Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                                        match lookup_type_for(env.clone(), n.clone()) {
    Some(resolved) => {
                                            let structurally_resolved = if (((resolved.connective.clone() == Connective::NoConnective) && ((resolved.children.clone().len() as i64) == 0)) && (resolved.inferred.clone() != None)) {
                                                match resolved.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: target, .. }) => target.clone(),
    _ => resolved.clone(),
}
} else {
                                                resolved.clone()
};
let is_optional = (n.return_cardinality.clone() == Cardinality::CardOptional);
let final_resolved = if is_optional {
                                                with_optional_cardinality(structurally_resolved)
} else {
                                                structurally_resolved
};
Rc::new(NodeResolveResult {
    resolved: final_resolved,
    diagnostics: Rc::new(vec![]),
})
},
    None => {
                                            let n_is_error = if (n.inferred.clone() != None) {
                                                is_compiler_error(n.inferred.clone().clone().unwrap())
} else {
                                                false
};
let n_is_callable = ((n.params.clone().len() as i64) > 0);
let n_is_type_var = if (n.inferred.clone() != None) {
                                                is_type_variable(n.inferred.clone().clone().unwrap())
} else {
                                                false
};
if (((is_kernel_type(n.name.clone()) || n_is_type_var) || n_is_error) || n_is_callable) {
                                                Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                                                Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::UnresolvedType {
    name: n.name.clone(),
    span: n.span.clone(),
}), module_name.clone())]),
})
}
},
}
}
} else {
                                    Rc::new(NodeResolveResult {
    resolved: n.clone(),
    diagnostics: Rc::new(vec![]),
})
}
}
}
}
}
}
}
    })
}

pub fn resolve_optional_node(n: Option<Rc<InferredNode>>, env: Rc<TypeEnv>, module_name: String) -> Rc<NodeResolveResult> {
    if (n.clone() == None) {
        Rc::new(NodeResolveResult {
    resolved: unit_type(),
    diagnostics: Rc::new(vec![]),
})
} else {
        match (*n.clone().unwrap()).clone() {
    InferredNode::Resolved { node: inner, .. } => resolve_node(inner.clone(), env, module_name),
    InferredNode::CompilerError { message: msg, span: sp, .. } => Rc::new(NodeResolveResult {
    resolved: make_expr_error_node(ExprErrorKind::SemanticExprError, msg.clone(), sp.clone()),
    diagnostics: Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: msg.clone(),
    span: sp.clone(),
}), module_name)]),
}),
    InferredNode::TypeVariable { .. } => Rc::new(NodeResolveResult {
    resolved: unit_type(),
    diagnostics: Rc::new(vec![]),
}),
}
}
}

pub fn resolve_field(field: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<FieldResult> {
    {
        let type_result = resolve_node(field_node_type_expr(field.clone()), env.clone(), module_name.clone());
let type_resolved = type_result.resolved.clone();
let type_diags = type_result.diagnostics.clone();
let default_resolved = match field_node_default_value(field.clone()) {
    Some(default_value) => Some(resolve_expr_types(default_value.clone(), env.clone(), module_name.clone())),
    None => None,
};
let default_diags = match default_resolved.clone() {
    Some(result) => result.diagnostics.clone(),
    None => Rc::new(vec![]),
};
Rc::new(FieldResult {
    field: make_field_node(field_node_name(field.clone()), type_resolved, field_node_cardinality(field.clone()), match default_resolved.clone() {
    Some(result) => Some(result.expr.clone()),
    None => None,
}, field_node_from_key(field.clone()), field.span.clone(), node_name_span(field.clone())),
    diagnostics: v2_rt::concat(type_diags, default_diags),
})
}
}

pub fn resolve_param(param: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ParamResult> {
    {
        let type_result = resolve_node(param_node_type_expr(param.clone()), env.clone(), module_name.clone());
let type_resolved = type_result.resolved.clone();
let type_diags = type_result.diagnostics.clone();
let default_resolved = match param_node_default_value(param.clone()) {
    Some(default_value) => Some(resolve_expr_types(default_value.clone(), env.clone(), module_name.clone())),
    None => None,
};
let default_diags = match default_resolved.clone() {
    Some(result) => result.diagnostics.clone(),
    None => Rc::new(vec![]),
};
Rc::new(ParamResult {
    param: make_param_node(param_node_name(param.clone()), type_resolved, match default_resolved.clone() {
    Some(result) => Some(result.expr.clone()),
    None => None,
}, param.span.clone(), node_name_span(param.clone())),
    diagnostics: v2_rt::concat(type_diags, default_diags),
})
}
}

pub fn resolve_resource_use(ru: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ResourceUseResult> {
    {
        let type_result = resolve_node(resource_use_resource(ru.clone()), env, module_name);
let type_resolved = type_result.resolved.clone();
let type_diags = type_result.diagnostics.clone();
Rc::new(ResourceUseResult {
    resource_use: make_resource_use_node(resource_use_name(ru.clone()), type_resolved, ru.span.clone()),
    diagnostics: type_diags,
})
}
}

pub fn resolve_named_arg(arg: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<NamedArgResolveResult> {
    {
        let value_result = resolve_expr_types(arg_value(arg.clone()), env, module_name);
let value_expr = value_result.expr.clone();
let value_diags = value_result.diagnostics.clone();
Rc::new(NamedArgResolveResult {
    arg: make_arg_node(arg_name(arg.clone()), value_expr, arg.span.clone()),
    diagnostics: value_diags,
})
}
}

pub fn resolve_field_init(field_init: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<FieldInitResolveResult> {
    {
        let value_result = resolve_expr_types(field_init_node_value(field_init.clone()), env, module_name);
let value_expr = value_result.expr.clone();
let value_diags = value_result.diagnostics.clone();
Rc::new(FieldInitResolveResult {
    field_init: make_field_init_node(field_init_node_name(field_init.clone()), value_expr, field_init.span.clone(), node_name_span(field_init.clone())),
    diagnostics: value_diags,
})
}
}

pub fn resolve_match_arm(arm: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<MatchArmResolveResult> {
    {
        let arm_g = arm_guard(arm.clone());
let guard_result = match arm_g {
    Some(guard) => Some(resolve_expr_types(guard.clone(), env.clone(), module_name.clone())),
    None => None,
};
let guard_diags = match guard_result.clone() {
    Some(result) => result.diagnostics.clone(),
    None => Rc::new(vec![]),
};
let body_result = resolve_expr_types(arm_body(arm.clone()), env.clone(), module_name.clone());
let body_expr = body_result.expr.clone();
let body_diags = body_result.diagnostics.clone();
Rc::new(MatchArmResolveResult {
    arm: make_arm_node(arm_pattern(arm.clone()), match guard_result.clone() {
    Some(result) => Some(result.expr.clone()),
    None => None,
}, body_expr, arm.span.clone()),
    diagnostics: v2_rt::concat(guard_diags, body_diags),
})
}
}

pub fn resolve_string_part(part: Rc<StringPart>, env: Rc<TypeEnv>, module_name: String) -> Rc<StringPartResolveResult> {
    match (*part).clone() {
    StringPart::Text { value, .. } => Rc::new(StringPartResolveResult {
    part: Rc::new(StringPart::Text {
    value: value.clone(),
}),
    diagnostics: Rc::new(vec![]),
}),
    StringPart::Interpolation { expr, .. } => {
        let expr_result = resolve_expr_types(expr.clone(), env, module_name);
let resolved_expr = expr_result.expr.clone();
let expr_diags = expr_result.diagnostics.clone();
Rc::new(StringPartResolveResult {
    part: Rc::new(StringPart::Interpolation {
    expr: resolved_expr,
}),
    diagnostics: expr_diags,
})
},
}
}

pub fn resolve_transport_binding(transport: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<TransportResolveResult> {
    if is_local_transport(transport.clone()) {
        Rc::new(TransportResolveResult {
    transport: transport.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
        {
            let prop_results = Rc::new({ let mut __result = Vec::new(); for p in transport.properties.clone().iter().cloned() { __result.push({
                let val_result = resolve_expr_types(field_init_node_value(p.clone()), env.clone(), module_name.clone());
Rc::new(FieldInitResolveResult {
    field_init: make_field_init_node(field_init_node_name(p.clone()), val_result.expr.clone(), p.span.clone(), node_name_span(p.clone())),
    diagnostics: val_result.diagnostics.clone(),
})
}); } __result });
let resolved_props = Rc::new({ let mut __result = Vec::new(); for pr in prop_results.clone().iter().cloned() { __result.push(pr.field_init.clone()); } __result });
let prop_diags = Rc::new({ let mut __result = Vec::new(); for pr in prop_results.clone().iter().cloned() { __result.extend((*pr.diagnostics.clone()).iter().cloned()); } __result });
let child_results = Rc::new({ let mut __result = Vec::new(); for c in transport.children.clone().iter().cloned() { __result.push(resolve_expr_types(c.clone(), env.clone(), module_name.clone())); } __result });
let resolved_children = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.push(cr.expr.clone()); } __result });
let child_diags = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.extend((*cr.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(TransportResolveResult {
    transport: make_transport_node(resolved_props, resolved_children, transport.span.clone()),
    diagnostics: v2_rt::concat(prop_diags, child_diags),
})
}
}
}

pub fn resolve_expr_types(texpr: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ExprResolveResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*texpr.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
    ExprData::ExprError { kind, message, .. } => Rc::new(ExprResolveResult {
    expr: make_expr_error_node(kind.clone(), message.clone(), texpr.span.clone()),
    diagnostics: Rc::new(vec![make_error_node(Rc::new(CompilerDiagnostic::InternalError {
    message: message.clone(),
    span: texpr.span.clone(),
}), module_name.clone())]),
}),
    ExprData::ExprVar { .. } => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
    ExprData::ExprFieldAccess { .. } => {
            let r = match texpr.children.clone().first().cloned() {
    Some(base) => resolve_expr_types(base.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: map_children(texpr.clone(), |child| r.expr.clone()),
    diagnostics: r.diagnostics.clone(),
})
},
    ExprData::ExprCall { call_semantics: cs, .. } => {
            let resolved_children = Rc::new({ let mut __result = Vec::new(); for arg_node in texpr.children.clone().iter().cloned() { __result.push({
                let val = match arg_node.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => arg_node.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
make_arg_node(if (arg_node.ident_span.clone() == None) {
                    None
} else {
                    Some(arg_node.name.clone())
}, vr.expr.clone(), arg_node.span.clone())
}); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for arg_node in texpr.children.clone().iter().cloned() { __result.extend((*{
                let val = match arg_node.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => arg_node.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
vr.diagnostics.clone()
}).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_named_expr_node(expr_call_func(texpr.clone()), Rc::new(ExprData::ExprCall {
    call_semantics: cs.clone(),
    descent_evidence: None,
}), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprMethodCall { method_semantics: ms, .. } => {
            let resolved_children = Rc::new({ let mut __result = Vec::new(); for pair in Rc::new(texpr.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.push({
                let idx = pair.0.clone();
let child = pair.1.clone();
if (idx.clone() == 0) {
                    {
                        let rr = resolve_expr_types(child.clone(), env.clone(), module_name.clone());
rr.expr.clone()
}
} else {
                    {
                        let val = match child.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => child.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
make_arg_node(if (child.ident_span.clone() == None) {
                            None
} else {
                            Some(child.name.clone())
}, vr.expr.clone(), child.span.clone())
}
}
}); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for pair in Rc::new(texpr.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.extend((*{
                let idx = pair.0.clone();
let child = pair.1.clone();
if (idx.clone() == 0) {
                    {
                        let rr = resolve_expr_types(child.clone(), env.clone(), module_name.clone());
rr.diagnostics.clone()
}
} else {
                    {
                        let val = match child.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => child.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
vr.diagnostics.clone()
}
}
}).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_named_expr_node(expr_method_name(texpr.clone()), Rc::new(ExprData::ExprMethodCall {
    method_semantics: ms.clone(),
}), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprMatch => {
            let resolved_children = Rc::new({ let mut __result = Vec::new(); for pair in Rc::new(texpr.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.push({
                let idx = pair.0.clone();
let child = pair.1.clone();
if (idx.clone() == 0) {
                    {
                        let sr = resolve_expr_types(child.clone(), env.clone(), module_name.clone());
sr.expr.clone()
}
} else {
                    {
                        let arm_ch = child.children.clone();
let has_guard = ((arm_ch.clone().len() as i64) == 2);
if has_guard.clone() {
                            {
                                let guard_r = match arm_ch.clone().first().cloned() {
    Some(g) => resolve_expr_types(g.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let body_r = match arm_ch.clone().get(1 as usize).cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
make_arm_node(match child.match_pattern.clone() {
    Some(p) => p.clone(),
    None => Rc::new(MatchPattern::Wildcard),
}, Some(guard_r.expr.clone()), body_r.expr.clone(), child.span.clone())
}
} else {
                            {
                                let body_r = match arm_ch.clone().first().cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
make_arm_node(match child.match_pattern.clone() {
    Some(p) => p.clone(),
    None => Rc::new(MatchPattern::Wildcard),
}, None, body_r.expr.clone(), child.span.clone())
}
}
}
}
}); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for pair in Rc::new(texpr.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { __result.extend((*{
                let idx = pair.0.clone();
let child = pair.1.clone();
if (idx.clone() == 0) {
                    {
                        let sr = resolve_expr_types(child.clone(), env.clone(), module_name.clone());
sr.diagnostics.clone()
}
} else {
                    {
                        let arm_ch = child.children.clone();
let has_guard = ((arm_ch.clone().len() as i64) == 2);
if has_guard.clone() {
                            {
                                let guard_r = match arm_ch.clone().first().cloned() {
    Some(g) => resolve_expr_types(g.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let body_r = match arm_ch.clone().get(1 as usize).cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
v2_rt::concat(guard_r.diagnostics.clone(), body_r.diagnostics.clone())
}
} else {
                            {
                                let body_r = match arm_ch.clone().first().cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: child.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
body_r.diagnostics.clone()
}
}
}
}
}).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprMatch), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprIf => {
            let ch = texpr.children.clone();
let cr = match ch.clone().first().cloned() {
    Some(c) => resolve_expr_types(c.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let tr = match ch.clone().get(1 as usize).cloned() {
    Some(t) => resolve_expr_types(t.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let er = match ch.clone().get(2 as usize).cloned() {
    Some(e) => Some(resolve_expr_types(e.clone(), env.clone(), module_name.clone())),
    None => None,
};
let resolved_children = match er.clone() {
    Some(r) => Rc::new(vec![cr.expr.clone(), tr.expr.clone(), r.expr.clone()]),
    None => Rc::new(vec![cr.expr.clone(), tr.expr.clone()]),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIf), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(v2_rt::concat(cr.diagnostics.clone(), tr.diagnostics.clone()), match er.clone() {
    Some(r) => r.diagnostics.clone(),
    None => Rc::new(vec![]),
}),
})
},
    ExprData::ExprLet => {
            let ch = texpr.children.clone();
let vr = match ch.clone().first().cloned() {
    Some(v) => resolve_expr_types(v.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let br = match ch.clone().get(1 as usize).cloned() {
    Some(bd) => Some(resolve_expr_types(bd.clone(), env.clone(), module_name.clone())),
    None => None,
};
let resolved_children = match br.clone() {
    Some(r) => Rc::new(vec![vr.expr.clone(), r.expr.clone()]),
    None => Rc::new(vec![vr.expr.clone()]),
};
Rc::new(ExprResolveResult {
    expr: make_named_expr_node(let_binding_name(texpr.clone()), Rc::new(ExprData::ExprLet), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(vr.diagnostics.clone(), match br.clone() {
    Some(r) => r.diagnostics.clone(),
    None => Rc::new(vec![]),
}),
})
},
    ExprData::ExprRecordLit { parent_enum: pe, .. } => {
            let resolved_children = Rc::new({ let mut __result = Vec::new(); for fi_node in texpr.children.clone().iter().cloned() { __result.push({
                let val = match fi_node.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => fi_node.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
make_field_init_node(fi_node.name.clone(), vr.expr.clone(), fi_node.span.clone(), node_name_span(fi_node.clone()))
}); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for fi_node in texpr.children.clone().iter().cloned() { __result.extend((*{
                let val = match fi_node.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => fi_node.clone(),
};
let vr = resolve_expr_types(val.clone(), env.clone(), module_name.clone());
vr.diagnostics.clone()
}).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_named_expr_node(texpr.name.clone(), Rc::new(ExprData::ExprRecordLit {
    parent_enum: pe.clone(),
}), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprListLit => {
            let el_results = Rc::new({ let mut __result = Vec::new(); for el in texpr.children.clone().iter().cloned() { __result.push(resolve_expr_types(el.clone(), env.clone(), module_name.clone())); } __result });
let resolved_children = Rc::new({ let mut __result = Vec::new(); for r in el_results.clone().iter().cloned() { __result.push(r.expr.clone()); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for r in el_results.clone().iter().cloned() { __result.extend((*r.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprListLit), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprBinOp { op, .. } => {
            let ch = texpr.children.clone();
let lr = match ch.clone().first().cloned() {
    Some(l) => resolve_expr_types(l.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let rr = match ch.clone().get(1 as usize).cloned() {
    Some(r) => resolve_expr_types(r.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBinOp {
    op: op.clone(),
}), Rc::new(vec![lr.expr.clone(), rr.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(lr.diagnostics.clone(), rr.diagnostics.clone()),
})
},
    ExprData::ExprUnaryOp { op, .. } => {
            let r = match texpr.children.clone().first().cloned() {
    Some(o) => resolve_expr_types(o.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprUnaryOp {
    op: op.clone(),
}), Rc::new(vec![r.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: r.diagnostics.clone(),
})
},
    ExprData::ExprLambda { semantics: s, .. } => {
            let lam_param_nodes = Rc::new(texpr.children.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>());
let r = match texpr.children.clone().first().cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLambda {
    semantics: s.clone(),
}), v2_rt::concat(Rc::new(vec![r.expr.clone()]), lam_param_nodes), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: r.diagnostics.clone(),
})
},
    ExprData::ExprStringInterp => {
            let resolved_children = Rc::new({ let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.push(match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => part_node.clone(),
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = resolve_expr_types(inner.clone(), env.clone(), module_name.clone());
make_interp_part_node(r.expr.clone(), part_node.span.clone())
},
    None => part_node.clone(),
},
}); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for part_node in texpr.children.clone().iter().cloned() { __result.extend((*match (*part_node.expr_data.clone()).clone() {
    ExprData::ExprLiteral { .. } => Rc::new(vec![]),
    _ => match part_node.children.clone().first().cloned() {
    Some(inner) => {
                let r = resolve_expr_types(inner.clone(), env.clone(), module_name.clone());
r.diagnostics.clone()
},
    None => Rc::new(vec![]),
},
}).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprStringInterp), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprBlock => {
            let stmt_results = Rc::new({ let mut __result = Vec::new(); for s in texpr.children.clone().iter().cloned() { __result.push(resolve_expr_types(s.clone(), env.clone(), module_name.clone())); } __result });
let resolved_children = Rc::new({ let mut __result = Vec::new(); for r in stmt_results.clone().iter().cloned() { __result.push(r.expr.clone()); } __result });
let all_diags = Rc::new({ let mut __result = Vec::new(); for r in stmt_results.clone().iter().cloned() { __result.extend((*r.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), resolved_children, texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: all_diags,
})
},
    ExprData::ExprCast => {
            let ch = texpr.children.clone();
let r = match ch.clone().first().cloned() {
    Some(inner) => resolve_expr_types(inner.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let tr = match ch.clone().get(1 as usize).cloned() {
    Some(target) => resolve_node(target.clone(), env.clone(), module_name.clone()),
    None => Rc::new(NodeResolveResult {
    resolved: unit_type(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprCast), Rc::new(vec![r.expr.clone(), tr.resolved.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(r.diagnostics.clone(), tr.diagnostics.clone()),
})
},
    ExprData::ExprForEach => {
            let ch = texpr.children.clone();
let cr = match ch.clone().first().cloned() {
    Some(c) => resolve_expr_types(c.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let br = match ch.clone().get(1 as usize).cloned() {
    Some(b) => resolve_expr_types(b.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_named_expr_node(foreach_variable(texpr.clone()), Rc::new(ExprData::ExprForEach), Rc::new(vec![cr.expr.clone(), br.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(cr.diagnostics.clone(), br.diagnostics.clone()),
})
},
    ExprData::ExprIndex => {
            let ch = texpr.children.clone();
let br = match ch.clone().first().cloned() {
    Some(base) => resolve_expr_types(base.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let ir = match ch.clone().get(1 as usize).cloned() {
    Some(index) => resolve_expr_types(index.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIndex), Rc::new(vec![br.expr.clone(), ir.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(br.diagnostics.clone(), ir.diagnostics.clone()),
})
},
    ExprData::ExprSlice => {
            let ch = texpr.children.clone();
let br = match ch.clone().first().cloned() {
    Some(base) => resolve_expr_types(base.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let sr = match ch.clone().get(1 as usize).cloned() {
    Some(start) => resolve_expr_types(start.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
let er = match ch.clone().get(2 as usize).cloned() {
    Some(end_e) => resolve_expr_types(end_e.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprSlice), Rc::new(vec![br.expr.clone(), sr.expr.clone(), er.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: v2_rt::concat(v2_rt::concat(br.diagnostics.clone(), sr.diagnostics.clone()), er.diagnostics.clone()),
})
},
    ExprData::ExprReturn => {
            let r = match texpr.children.clone().first().cloned() {
    Some(inner) => resolve_expr_types(inner.clone(), env.clone(), module_name.clone()),
    None => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
};
Rc::new(ExprResolveResult {
    expr: make_expr_node(Rc::new(ExprData::ExprReturn), Rc::new(vec![r.expr.clone()]), texpr.inferred.clone(), texpr.span.clone()),
    diagnostics: r.diagnostics.clone(),
})
},
    ExprData::NoExprData => Rc::new(ExprResolveResult {
    expr: texpr.clone(),
    diagnostics: Rc::new(vec![]),
}),
}
    })
}

pub fn fn_type_param_names(item: Rc<Node>) -> Rc<Vec<String>> {
    Rc::new({ let mut __result = Vec::new(); for p in Rc::new({ let mut __result = Vec::new(); for p in item.params.clone().iter().cloned() { if (param_node_name(p.clone()).as_str() == param_node_type_expr(p.clone()).name.clone().as_str()) { __result.push(p); } } __result }).iter().cloned() { __result.push(param_node_name(p.clone())); } __result })
}

pub fn resolve_item_types(item: Rc<Node>, env: Rc<TypeEnv>, module_name: String) -> Rc<ItemResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let tp_names = fn_type_param_names(item.clone());
let env = tp_names.iter().cloned().fold(env.clone(), |e: Rc<TypeEnv>, tp_name: String| { let e = Rc::try_unwrap(e).unwrap_or_else(|rc| (*rc).clone()); Rc::new(TypeEnv {
    bindings: v2_rt::rc_map_insert(e.bindings, tp_name.clone(), Rc::new(TypeBinding {
    name: tp_name.clone(),
    resolved: Rc::new(Node {
    name: tp_name.clone(),
    span: no_span(),
    ident_span: default_ident_span(tp_name.clone(), no_span()),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::TypeVariable {
    id: tp_name.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
})),
    recursive_types: e.recursive_types,
    recursive_type_set: e.recursive_type_set,
    inductive_fields: e.inductive_fields,
    source_index: e.source_index,
}) });
let param_results = Rc::new({ let mut __result = Vec::new(); for p in item.params.clone().iter().cloned() { __result.push(resolve_param(p.clone(), env.clone(), module_name.clone())); } __result });
let resolved_params = Rc::new({ let mut __result = Vec::new(); for pr in param_results.clone().iter().cloned() { __result.push(pr.param.clone()); } __result });
let param_diags = Rc::new({ let mut __result = Vec::new(); for pr in param_results.clone().iter().cloned() { __result.extend((*pr.diagnostics.clone()).iter().cloned()); } __result });
let ret_result = resolve_optional_node(item.inferred.clone(), env.clone(), module_name.clone());
let ret_resolved = ret_result.resolved.clone();
let ret_diags = ret_result.diagnostics.clone();
let resolved_ret = if (item.inferred.clone() == None) {
                None
} else {
                Some(Rc::new(InferredNode::Resolved {
    node: ret_resolved,
}))
};
let use_results = Rc::new({ let mut __result = Vec::new(); for u in item.uses.clone().iter().cloned() { __result.push(resolve_resource_use(u.clone(), env.clone(), module_name.clone())); } __result });
let resolved_uses = Rc::new({ let mut __result = Vec::new(); for ur in use_results.clone().iter().cloned() { __result.push(ur.resource_use.clone()); } __result });
let use_diags = Rc::new({ let mut __result = Vec::new(); for ur in use_results.clone().iter().cloned() { __result.extend((*ur.diagnostics.clone()).iter().cloned()); } __result });
let body_resolved = if (item.body.clone() == None) {
                Rc::new(ExprResolveResult {
    expr: item.clone(),
    diagnostics: Rc::new(vec![]),
})
} else {
                resolve_expr_types(item.body.clone().clone().unwrap(), env.clone(), module_name.clone())
};
let resolved_body = if (item.body.clone() == None) {
                None
} else {
                Some(body_resolved.expr.clone())
};
let body_diags = if (item.body.clone() == None) {
                Rc::new(vec![])
} else {
                body_resolved.diagnostics.clone()
};
let anno_resolved = if (item.type_annotation.clone() == None) {
                Rc::new(NodeResolveResult {
    resolved: unit_type(),
    diagnostics: Rc::new(vec![]),
})
} else {
                resolve_node(item.type_annotation.clone().clone().unwrap(), env.clone(), module_name.clone())
};
let resolved_anno = if (item.type_annotation.clone() == None) {
                None
} else {
                Some(anno_resolved.resolved.clone())
};
let anno_diags = anno_resolved.diagnostics.clone();
let transport_resolved = if (item.transport.clone() == None) {
                Rc::new(TransportResolveResult {
    transport: local_transport_node(no_span()),
    diagnostics: Rc::new(vec![]),
})
} else {
                resolve_transport_binding(item.transport.clone().clone().unwrap(), env.clone(), module_name.clone())
};
let resolved_transport = if (item.transport.clone() == None) {
                None
} else {
                Some(transport_resolved.transport.clone())
};
let transport_diags = transport_resolved.diagnostics.clone();
let prop_results = Rc::new({ let mut __result = Vec::new(); for p in item.properties.clone().iter().cloned() { __result.push(resolve_field_init(p.clone(), env.clone(), module_name.clone())); } __result });
let resolved_props = Rc::new({ let mut __result = Vec::new(); for pr in prop_results.clone().iter().cloned() { __result.push(pr.field_init.clone()); } __result });
let prop_diags = Rc::new({ let mut __result = Vec::new(); for pr in prop_results.clone().iter().cloned() { __result.extend((*pr.diagnostics.clone()).iter().cloned()); } __result });
let child_results = Rc::new({ let mut __result = Vec::new(); for child in item.children.clone().iter().cloned() { __result.push(resolve_item_types(child.clone(), env.clone(), module_name.clone())); } __result });
let resolved_children = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.push(cr.item.clone()); } __result });
let child_diags = Rc::new({ let mut __result = Vec::new(); for cr in child_results.clone().iter().cloned() { __result.extend((*cr.diagnostics.clone()).iter().cloned()); } __result });
Rc::new(ItemResult {
    item: Rc::new(Node {
    name: item.name.clone(),
    span: item.span.clone(),
    ident_span: item.ident_span.clone(),
    children: resolved_children,
    params: resolved_params,
    inferred: resolved_ret,
    return_cardinality: item.return_cardinality.clone(),
    uses: resolved_uses,
    body: resolved_body,
    connective: item.connective.clone(),
    transport: resolved_transport,
    properties: resolved_props,
    type_annotation: resolved_anno,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    diagnostics: v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(param_diags, ret_diags), use_diags), body_diags), anno_diags), transport_diags), prop_diags), child_diags),
})
}
    })
}
