// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.infer_types

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}

impl<'de, T> Deserialize<'de> for NonEmptyVec<T>
where
    T: Deserialize<'de>,
{
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let items = Vec::<T>::deserialize(deserializer)?;
        NonEmptyVec::new(items).map_err(serde::de::Error::custom)
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}

impl<'de, T> Deserialize<'de> for NonEmptyBTreeSet<T>
where
    T: Ord + Deserialize<'de>,
{
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let items = std::collections::BTreeSet::<T>::deserialize(deserializer)?;
        NonEmptyBTreeSet::new(items).map_err(serde::de::Error::custom)
    }
}
pub use crate::std_types::{SourceSpan};
pub use crate::v2_core::{Node, FieldInit, Param, Connective, CollectionKind, Cardinality, ExprErrorKind, make_expr_node, make_expr_error_node, LiteralValue, is_kernel_type, BinOpKind, InferredNode, rt_node, has_inferred, is_compiler_error, NodeType, leaf_node, no_span, with_optional_cardinality, with_required_cardinality, ExprData};
use crate::v2_core::Connective::{Conj, Disj, NoConnective};
use crate::v2_core::CollectionKind::{ListKind, SetKind, NonEmptyListKind, NonEmptySetKind, MapKind, NoCollection};
use crate::v2_core::Cardinality::{Required, CardOptional};
use crate::v2_core::ExprData::{NoExprData, ExprLiteral};
use crate::v2_core::ExprErrorKind::{SemanticExprError};
use crate::v2_core::LiteralValue::{LitStr, LitInt, LitFloat, LitBool, LitNull};
use crate::v2_core::BinOpKind::{BinEq, BinNe, BinLt, BinGt, BinLe, BinGe, BinAnd, BinOr, NullCoalesce};
use crate::v2_core::InferredNode::{Resolved, CompilerError, TypeVariable};
use crate::v2_core::NodeType::{Typed, InferError, InferVariable, Untyped};

pub fn is_type_variable(inferred: Rc<InferredNode>) -> Bool {
    match (*inferred.clone()).clone() {
    InferredNode::TypeVariable { .. } => true,
    _ => false,
}
}

pub fn child_inferred_or_name(ch: Rc<Node>) -> Rc<Node> {
    if (ch.inferred.clone() == None) {
        leaf_node(ch.name.clone())
} else {
        rt_type(ch.clone())
}
}

pub fn rt_type(n: Rc<Node>) -> Rc<Node> {
    match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => leaf_node("Unit".to_string()),
    NodeType::InferVariable { .. } => leaf_node("Unit".to_string()),
    NodeType::Untyped => leaf_node("Unit".to_string()),
}
}

pub fn collection_kind_for_name(name: String) -> CollectionKind {
    if (name.clone() == "List".to_string()) {
        CollectionKind::ListKind
} else {
        if (name.clone() == "Set".to_string()) {
            CollectionKind::SetKind
} else {
            if (name.clone() == "NonEmptyList".to_string()) {
                CollectionKind::NonEmptyListKind
} else {
                if (name.clone() == "NonEmptySet".to_string()) {
                    CollectionKind::NonEmptySetKind
} else {
                    if (name.clone() == "Map".to_string()) {
                        CollectionKind::MapKind
} else {
                        CollectionKind::NoCollection
}
}
}
}
}
}

pub fn container_node(kind_name: String, element: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: kind_name.clone(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![element.clone()],
    connective: Connective::NoConnective,
    collection_kind: collection_kind_for_name(kind_name.clone()),
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn tuple_node(first: Rc<Node>, second: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Tuple".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![Rc::new(Node {
    name: "first".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![],
    connective: Connective::NoConnective,
    collection_kind: CollectionKind::NoCollection,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: first.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}), Rc::new(Node {
    name: "second".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![],
    connective: Connective::NoConnective,
    collection_kind: CollectionKind::NoCollection,
    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: second.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})],
    connective: Connective::Conj,
    collection_kind: CollectionKind::NoCollection,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn map_node(key: Rc<Node>, value: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Map".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![key.clone(), value.clone()],
    connective: Connective::NoConnective,
    collection_kind: CollectionKind::MapKind,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn bare_map_node() -> Rc<Node> {
    Rc::new(Node {
    name: "Map".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![],
    connective: Connective::NoConnective,
    collection_kind: CollectionKind::MapKind,
    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn callable_node(func_params: Vec<Rc<Param>>, ret: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Callable".to_string(),
    span: Rc::new(SourceSpan {
    start: 0,
    end: 0,
}),
    children: vec![],
    connective: Connective::NoConnective,
    collection_kind: CollectionKind::NoCollection,
    params: func_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: ret.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn callable_inferred(n: Rc<Node>) -> Rc<Node> {
    {
        let is_callable = ((n.params.clone().len() as i64) > 0);
if is_callable.clone() {
            match n.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: ret, .. }) => ret.clone(),
    None => error_type_node(),
    _ => error_type_node(),
}
} else {
            n.clone()
}
}
}

pub fn error_type_node() -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::SemanticExprError, "unresolved type".to_string(), no_span())
}

pub fn normalize_access_type_node(mut n: Rc<Node>) -> Rc<Node> {
    loop {
        let has_structure = (n.connective.clone() != Connective::NoConnective);
let unwrapped = if ((n.name.clone() == "Refined".to_string()) && has_structure.clone()) {
            n.children.clone().first().cloned()
} else {
            None
};
match unwrapped.clone() {
    Some(base) => { {
            let __tco_0 = base.clone();
n = __tco_0;
continue;
} },
    None => { break n.clone(); },
}
}
}

pub fn node_type_shape(n: Rc<Node>) -> String {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let __is_leaf = (((n.connective.clone() == Connective::NoConnective) && ((n.children.clone().len() as i64) == 0)) && ((n.properties.clone().len() as i64) == 0));
if __is_leaf.clone() {
                {
                    let __is_named_ref = if (n.inferred.clone() == None) {
                        false
} else {
                        match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => (((((rt.connective.clone() == Connective::NoConnective) && ((rt.children.clone().len() as i64) == 0)) && (rt.name.clone() != "".to_string())) && (rt.name.clone() != "None".to_string())) && (is_kernel_type(rt.name.clone()) == false)),
    NodeType::InferError { .. } => false,
    NodeType::InferVariable { .. } => false,
    NodeType::Untyped => false,
}
};
if __is_named_ref.clone() {
                        v2_rt::concat(v2_rt::concat("Named(".to_string(), n.name.clone()), ")".to_string())
} else {
                        v2_rt::concat(v2_rt::concat("Primitive(".to_string(), n.name.clone()), ")".to_string())
}
}
} else {
                {
                    let is_product = (n.connective.clone() == Connective::Conj);
let is_coproduct = (n.connective.clone() == Connective::Disj);
if is_product.clone() {
                        if (n.name.clone() == "".to_string()) {
                            "Product(<anon>)".to_string()
} else {
                            v2_rt::concat(v2_rt::concat("Product(".to_string(), n.name.clone()), ")".to_string())
}
} else {
                        if is_coproduct.clone() {
                            if (n.name.clone() == "".to_string()) {
                                "Coproduct(<anon>)".to_string()
} else {
                                v2_rt::concat(v2_rt::concat("Coproduct(".to_string(), n.name.clone()), ")".to_string())
}
} else {
                            {
                                let ck = n.collection_kind.clone();
let __is_container = ((((ck.clone() == CollectionKind::ListKind) || (ck.clone() == CollectionKind::SetKind)) || (ck.clone() == CollectionKind::NonEmptyListKind)) || (ck.clone() == CollectionKind::NonEmptySetKind));
let is_optional = (n.return_cardinality.clone() == Cardinality::CardOptional);
let is_map = (n.collection_kind.clone() == CollectionKind::MapKind);
if __is_container.clone() {
                                    {
                                        let elem_shape = match n.children.clone().first().cloned() {
    Some(el) => node_type_shape(el.clone()),
    None => "?".to_string(),
};
v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("Container(".to_string(), n.name.clone()), ",".to_string()), elem_shape.clone()), ")".to_string())
}
} else {
                                    if is_optional.clone() {
                                        {
                                            let inner_shape = node_type_shape(with_required_cardinality(n.clone()));
v2_rt::concat(v2_rt::concat("Optional(".to_string(), inner_shape.clone()), ")".to_string())
}
} else {
                                        if is_map.clone() {
                                            "Map(...)".to_string()
} else {
                                            v2_rt::concat(v2_rt::concat("Node(".to_string(), n.name.clone()), ")".to_string())
}
}
}
}
}
}
}
}
}
    })
}

pub fn node_type_compatible(mut left: Rc<Node>, mut right: Rc<Node>) -> Bool {
    loop {
        let left_err = if (left.inferred.clone() != None) {
            is_compiler_error(left.inferred.clone().clone().unwrap())
} else {
            false
};
let right_err = if (right.inferred.clone() != None) {
            is_compiler_error(right.inferred.clone().clone().unwrap())
} else {
            false
};
let left_tv = if (left.inferred.clone() != None) {
            is_type_variable(left.inferred.clone().clone().unwrap())
} else {
            false
};
let right_tv = if (right.inferred.clone() != None) {
            is_type_variable(right.inferred.clone().clone().unwrap())
} else {
            false
};
let left_opt = (left.return_cardinality.clone() == Cardinality::CardOptional);
let right_opt = (right.return_cardinality.clone() == Cardinality::CardOptional);
let right_is_unit = ((right.connective.clone() == Connective::Conj) && ((right.children.clone().len() as i64) == 0));
let left_is_unit = ((left.connective.clone() == Connective::Conj) && ((left.children.clone().len() as i64) == 0));
if (left_err.clone() || right_err.clone()) {
            break true;
} else {
            if (left_tv.clone() || right_tv.clone()) {
                break true;
} else {
                if (left_opt.clone() && right_is_unit.clone()) {
                    break true;
} else {
                    if (left_is_unit.clone() && right_opt.clone()) {
                        break true;
} else {
                        let left_ck = left.collection_kind.clone();
let left_is_container = ((((left_ck.clone() == CollectionKind::ListKind) || (left_ck.clone() == CollectionKind::SetKind)) || (left_ck.clone() == CollectionKind::NonEmptyListKind)) || (left_ck.clone() == CollectionKind::NonEmptySetKind));
let right_ck = right.collection_kind.clone();
let right_is_container = ((((right_ck.clone() == CollectionKind::ListKind) || (right_ck.clone() == CollectionKind::SetKind)) || (right_ck.clone() == CollectionKind::NonEmptyListKind)) || (right_ck.clone() == CollectionKind::NonEmptySetKind));
if (left_is_container.clone() && right_is_container.clone()) {
                            if (left.name.clone() != right.name.clone()) {
                                break false;
} else {
                                match left.children.clone().first().cloned() {
    Some(left_el) => { match right.children.clone().first().cloned() {
    Some(right_el) => { let left_el_is_unit = ((left_el.connective.clone() == Connective::Conj) && ((left_el.children.clone().len() as i64) == 0));
let right_el_is_unit = ((right_el.connective.clone() == Connective::Conj) && ((right_el.children.clone().len() as i64) == 0));
if (left_el_is_unit.clone() || right_el_is_unit.clone()) {
                                    break true;
} else {
                                    {
                                        let __tco_0 = left_el.clone();
let __tco_1 = right_el.clone();
left = __tco_0;
right = __tco_1;
continue;
}
} },
    None => { break true; },
} },
    None => { break true; },
}
}
} else {
                            if (left_opt.clone() && right_opt.clone()) {
                                let left_inner = with_required_cardinality(left.clone());
let right_inner = with_required_cardinality(right.clone());
let left_inner_is_unit = ((left_inner.connective.clone() == Connective::Conj) && ((left_inner.children.clone().len() as i64) == 0));
let right_inner_is_unit = ((right_inner.connective.clone() == Connective::Conj) && ((right_inner.children.clone().len() as i64) == 0));
if (left_inner_is_unit.clone() || right_inner_is_unit.clone()) {
                                    break true;
} else {
                                    {
                                        let __tco_0 = left_inner.clone();
let __tco_1 = right_inner.clone();
left = __tco_0;
right = __tco_1;
continue;
}
}
} else {
                                if (left_opt.clone() || right_opt.clone()) {
                                    break false;
} else {
                                    break (left.name.clone() == right.name.clone());
}
}
}
}
}
}
}
}
}

pub fn prefer_specific_type(left: Rc<Node>, right: Rc<Node>) -> Rc<Node> {
    {
        let left_ck = left.collection_kind.clone();
let left_is_container = ((((left_ck.clone() == CollectionKind::ListKind) || (left_ck.clone() == CollectionKind::SetKind)) || (left_ck.clone() == CollectionKind::NonEmptyListKind)) || (left_ck.clone() == CollectionKind::NonEmptySetKind));
let left_is_optional = (left.return_cardinality.clone() == Cardinality::CardOptional);
let left_first_child = left.children.clone().first().cloned();
let left_norm_name = left.name.clone();
let left_is_unit_inner = if left_is_container.clone() {
            match left_first_child.clone() {
    Some(el) => {
                let el_is_unit = ((el.connective.clone() == Connective::Conj) && ((el.children.clone().len() as i64) == 0));
el_is_unit.clone()
},
    None => false,
}
} else {
            if left_is_optional.clone() {
                {
                    let left_is_unit = ((left.connective.clone() == Connective::Conj) && ((left.children.clone().len() as i64) == 0));
left_is_unit.clone()
}
} else {
                false
}
};
let right_ck = right.collection_kind.clone();
let right_is_container = ((((right_ck.clone() == CollectionKind::ListKind) || (right_ck.clone() == CollectionKind::SetKind)) || (right_ck.clone() == CollectionKind::NonEmptyListKind)) || (right_ck.clone() == CollectionKind::NonEmptySetKind));
let right_is_optional = (right.return_cardinality.clone() == Cardinality::CardOptional);
let same_kind = if (left_is_container.clone() && right_is_container.clone()) {
            (left_norm_name.clone() == right.name.clone())
} else {
            if (left_is_optional.clone() && right_is_optional.clone()) {
                true
} else {
                false
}
};
if (same_kind.clone() && left_is_unit_inner.clone()) {
            right.clone()
} else {
            left.clone()
}
}
}

pub fn node_type_equals(left: Rc<Node>, right: Rc<Node>) -> Bool {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let left_err = if (left.inferred.clone() != None) {
                is_compiler_error(left.inferred.clone().clone().unwrap())
} else {
                false
};
let right_err = if (right.inferred.clone() != None) {
                is_compiler_error(right.inferred.clone().clone().unwrap())
} else {
                false
};
let left_tv = if (left.inferred.clone() != None) {
                is_type_variable(left.inferred.clone().clone().unwrap())
} else {
                false
};
let right_tv = if (right.inferred.clone() != None) {
                is_type_variable(right.inferred.clone().clone().unwrap())
} else {
                false
};
let left_opt = (left.return_cardinality.clone() == Cardinality::CardOptional);
let right_opt = (right.return_cardinality.clone() == Cardinality::CardOptional);
let left_leaf = (((left.connective.clone() == Connective::NoConnective) && ((left.children.clone().len() as i64) == 0)) && ((left.properties.clone().len() as i64) == 0));
let right_leaf = (((right.connective.clone() == Connective::NoConnective) && ((right.children.clone().len() as i64) == 0)) && ((right.properties.clone().len() as i64) == 0));
let left_struct = (left.connective.clone() != Connective::NoConnective);
let right_struct = (right.connective.clone() != Connective::NoConnective);
let right_is_unit_eq = ((right.connective.clone() == Connective::Conj) && ((right.children.clone().len() as i64) == 0));
let left_is_unit_eq = ((left.connective.clone() == Connective::Conj) && ((left.children.clone().len() as i64) == 0));
if (left_err.clone() || right_err.clone()) {
                true
} else {
                if (left_tv.clone() && right_tv.clone()) {
                    true
} else {
                    if (left_tv.clone() || right_tv.clone()) {
                        false
} else {
                        if (left_opt.clone() && right_is_unit_eq.clone()) {
                            true
} else {
                            if (left_is_unit_eq.clone() && right_opt.clone()) {
                                true
} else {
                                if (left_leaf.clone() && right_leaf.clone()) {
                                    (left.name.clone() == right.name.clone())
} else {
                                    if (left_struct.clone() && right_struct.clone()) {
                                        if (left.name.clone() != right.name.clone()) {
                                            false
} else {
                                            if ((left.connective.clone() == Connective::Conj) != (right.connective.clone() == Connective::Conj)) {
                                                false
} else {
                                                if ((left.children.clone().len() as i64) != (right.children.clone().len() as i64)) {
                                                    false
} else {
                                                    { let mut __all = true; for pair in left.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { if !(match right.children.clone().iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(right_child) => node_type_equals(pair.1.clone(), right_child.clone()),
    None => false,
}) { __all = false; break; } } __all }
}
}
}
} else {
                                        if (left_leaf.clone() && right_struct.clone()) {
                                            (left.name.clone() == right.name.clone())
} else {
                                            if (left_struct.clone() && right_leaf.clone()) {
                                                (left.name.clone() == right.name.clone())
} else {
                                                {
                                                    let left_ck = left.collection_kind.clone();
let left_is_container = ((((left_ck.clone() == CollectionKind::ListKind) || (left_ck.clone() == CollectionKind::SetKind)) || (left_ck.clone() == CollectionKind::NonEmptyListKind)) || (left_ck.clone() == CollectionKind::NonEmptySetKind));
let right_ck = right.collection_kind.clone();
let right_is_container = ((((right_ck.clone() == CollectionKind::ListKind) || (right_ck.clone() == CollectionKind::SetKind)) || (right_ck.clone() == CollectionKind::NonEmptyListKind)) || (right_ck.clone() == CollectionKind::NonEmptySetKind));
if (left_is_container.clone() && right_is_container.clone()) {
                                                        if (left.name.clone() != right.name.clone()) {
                                                            false
} else {
                                                            match left.children.clone().first().cloned() {
    Some(left_el) => match right.children.clone().first().cloned() {
    Some(right_el) => node_type_equals(left_el.clone(), right_el.clone()),
    None => false,
},
    None => false,
}
}
} else {
                                                        if (left_opt.clone() && right_opt.clone()) {
                                                            node_type_equals(with_required_cardinality(left.clone()), with_required_cardinality(right.clone()))
} else {
                                                            {
                                                                let both_maps = ((left.collection_kind.clone() == CollectionKind::MapKind) && (right.collection_kind.clone() == CollectionKind::MapKind));
if both_maps.clone() {
                                                                    if (((left.children.clone().len() as i64) == 2) && ((right.children.clone().len() as i64) == 2)) {
                                                                        {
                                                                            let left_first = match left.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => left.clone(),
};
let right_first = match right.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => right.clone(),
};
let left_second = match left.children.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(v) => v.clone(),
    None => left.clone(),
};
let right_second = match right.children.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(v) => v.clone(),
    None => right.clone(),
};
(node_type_equals(left_first.clone(), right_first.clone()) && node_type_equals(left_second.clone(), right_second.clone()))
}
} else {
                                                                        false
}
} else {
                                                                    if (((left.params.clone().len() as i64) > 0) && ((right.params.clone().len() as i64) > 0)) {
                                                                        if ((left.params.clone().len() as i64) != (right.params.clone().len() as i64)) {
                                                                            false
} else {
                                                                            {
                                                                                let params_eq = { let mut __all = true; for pair in left.params.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { if !(match right.params.clone().iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(right_param) => node_type_equals(pair.1.clone().type_expr.clone(), right_param.type_expr.clone()),
    None => false,
}) { __all = false; break; } } __all };
if (params_eq.clone() == false) {
                                                                                    false
} else {
                                                                                    match left.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: left_ret, .. }) => match right.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: right_ret, .. }) => node_type_equals(left_ret.clone(), right_ret.clone()),
    None => false,
    _ => false,
},
    None => (right.inferred.clone() == None),
    _ => false,
}
}
}
}
} else {
                                                                        false
}
}
}
}
}
}
}
}
}
}
}
}
}
}
}
}
    })
}

pub fn node_type_deps(n: Rc<Node>) -> Vec<String> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let n_is_type_var = if (n.inferred.clone() != None) {
                is_type_variable(n.inferred.clone().clone().unwrap())
} else {
                false
};
if n_is_type_var.clone() {
                return vec![]
}
let __is_named_ref = if (n.inferred.clone() == None) {
                false
} else {
                match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => (((((rt.connective.clone() == Connective::NoConnective) && ((rt.children.clone().len() as i64) == 0)) && (rt.name.clone() != "".to_string())) && (rt.name.clone() != "None".to_string())) && (is_kernel_type(rt.name.clone()) == false)),
    NodeType::InferError { .. } => false,
    NodeType::InferVariable { .. } => false,
    NodeType::Untyped => false,
}
};
let has_structure = (n.connective.clone() != Connective::NoConnective);
if __is_named_ref.clone() {
                match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => vec![rt.name.clone()],
    NodeType::InferError { .. } => vec![],
    NodeType::InferVariable { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
                if has_structure.clone() {
                    { let mut __result = Vec::new(); for child in n.children.clone().iter().cloned() { __result.extend(if (child.inferred.clone() != None) {
                        match (*rt_node(child.clone())).clone() {
    NodeType::Typed { node: rt, .. } => node_type_deps(rt.clone()),
    NodeType::InferError { .. } => vec![],
    NodeType::InferVariable { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
                        node_type_deps(child.clone())
}); } __result }
} else {
                    if (n.inferred.clone() != None) {
                        match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => node_type_deps(rt.clone()),
    NodeType::InferError { .. } => vec![],
    NodeType::InferVariable { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
                        if ((n.children.clone().len() as i64) > 0) {
                            {
                                let child_deps = { let mut __result = Vec::new(); for child in n.children.clone().iter().cloned() { __result.extend(node_type_deps(child.clone())); } __result };
if ((n.name.clone() != "".to_string()) && (is_kernel_type(n.name.clone()) == false)) {
                                    v2_rt::concat(vec![n.name.clone()], child_deps.clone())
} else {
                                    child_deps.clone()
}
}
} else {
                            if ((is_kernel_type(n.name.clone()) || (n.name.clone() == "None".to_string())) || (n.name.clone() == "".to_string())) {
                                vec![]
} else {
                                vec![n.name.clone()]
}
}
}
}
}
}
    })
}

pub fn infer_literal_node(lit: Rc<LiteralValue>) -> Rc<Node> {
    match (*lit.clone()).clone() {
    LiteralValue::LitStr { .. } => leaf_node("String".to_string()),
    LiteralValue::LitInt { .. } => leaf_node("Int".to_string()),
    LiteralValue::LitFloat { .. } => leaf_node("Float".to_string()),
    LiteralValue::LitBool { .. } => leaf_node("Bool".to_string()),
    LiteralValue::LitNull => with_optional_cardinality(leaf_node("Unit".to_string())),
}
}

pub fn method_receiver_element_node(receiver_type: Rc<Node>) -> Rc<Node> {
    {
        let normed = normalize_access_type_node(receiver_type.clone());
if ((normed.connective.clone() == Connective::NoConnective) && ((normed.children.clone().len() as i64) == 1)) {
            match normed.children.clone().first().cloned() {
    Some(el) => el.clone(),
    None => receiver_type.clone(),
}
} else {
            {
                let is_map = (normed.collection_kind.clone() == CollectionKind::MapKind);
if is_map.clone() {
                    match normed.children.clone().iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(val_type) => val_type.clone(),
    None => receiver_type.clone(),
}
} else {
                    receiver_type.clone()
}
}
}
}
}

pub fn extract_optional_inner_node(n: Rc<Node>) -> Rc<Node> {
    {
        let is_optional = (n.return_cardinality.clone() == Cardinality::CardOptional);
if is_optional.clone() {
            with_required_cardinality(n.clone())
} else {
            n.clone()
}
}
}

pub fn binop_algebra_field(op: BinOpKind) -> String {
    match op.clone() {
    BinOpKind::Add => "add".to_string(),
    BinOpKind::Sub => "add".to_string(),
    BinOpKind::Mul => "mul".to_string(),
    BinOpKind::Div => "reciprocal".to_string(),
    BinOpKind::Mod => "add".to_string(),
    BinOpKind::BinEq => "compare".to_string(),
    BinOpKind::BinNe => "compare".to_string(),
    BinOpKind::BinLt => "compare".to_string(),
    BinOpKind::BinGt => "compare".to_string(),
    BinOpKind::BinLe => "compare".to_string(),
    BinOpKind::BinGe => "compare".to_string(),
    BinOpKind::BinAnd => "meet".to_string(),
    BinOpKind::BinOr => "join".to_string(),
    BinOpKind::NullCoalesce => "".to_string(),
}
}

pub fn infer_binop_type_node(op: BinOpKind, left_type: Rc<Node>) -> Rc<Node> {
    match op.clone() {
    BinOpKind::BinEq => leaf_node("Bool".to_string()),
    BinOpKind::BinNe => leaf_node("Bool".to_string()),
    BinOpKind::BinLt => leaf_node("Bool".to_string()),
    BinOpKind::BinGt => leaf_node("Bool".to_string()),
    BinOpKind::BinLe => leaf_node("Bool".to_string()),
    BinOpKind::BinGe => leaf_node("Bool".to_string()),
    BinOpKind::BinAnd => leaf_node("Bool".to_string()),
    BinOpKind::BinOr => leaf_node("Bool".to_string()),
    BinOpKind::NullCoalesce => extract_optional_inner_node(left_type.clone()),
    _ => {
        let field_name = binop_algebra_field(op.clone());
let is_product = (left_type.connective.clone() == Connective::Conj);
if is_product.clone() {
            {
                let matching = { let mut __result = Vec::new(); for c in left_type.children.clone().iter().cloned() { if (c.name.clone() == field_name.clone()) { __result.push(c); } } __result };
match matching.clone().first().cloned() {
    Some(field) => match field.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: rt, .. }) => if ((rt.params.clone().len() as i64) > 0) {
                    match rt.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: return_type, .. }) => return_type.clone(),
    _ => left_type.clone(),
}
} else {
                    rt.clone()
},
    _ => left_type.clone(),
},
    None => left_type.clone(),
}
}
} else {
            left_type.clone()
}
},
}
}

pub fn for_each_element_type_node(n: Rc<Node>) -> Rc<Node> {
    {
        let normed = normalize_access_type_node(n.clone());
let is_single_child = ((normed.connective.clone() == Connective::NoConnective) && ((normed.children.clone().len() as i64) == 1));
let extracted = if is_single_child.clone() {
            normed.children.clone().first().cloned()
} else {
            None
};
match extracted.clone() {
    Some(el) => el.clone(),
    None => if ((((normed.connective.clone() == Connective::NoConnective) && ((normed.children.clone().len() as i64) == 0)) && ((normed.properties.clone().len() as i64) == 0)) && (normed.name.clone() == "String".to_string())) {
            leaf_node("String".to_string())
} else {
            normed.clone()
},
}
}
}

pub fn emit_map_has(m: HashMap<String, Bool>, key: String) -> Bool {
    match v2_rt::map_get(&m, key.clone()) {
    Some(_) => true,
    None => false,
}
}
