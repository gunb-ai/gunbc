// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.parse

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}
pub use crate::v2_std_core::{module_node, import_node, Node, InferredNode, NodeType, rt_node, Connective, is_container_type, Cardinality, make_param_node, param_node_name, param_node_type_expr, param_node_default_value, make_field_node, field_node_name, field_node_type_expr, field_node_cardinality, field_node_default_value, field_node_from_key, make_variant_node, variant_node_name, variant_node_fields, leaf_node, leaf_node_with_span, ExprData, make_expr_node, make_named_expr_node, make_expr_error_node, expr_var_name, field_access_field, expr_call_func, expr_method_name, let_binding_name, foreach_variable, lambda_param_names, record_lit_type_name, make_arg_node, arg_name, arg_value, make_arm_node, make_field_init_node, make_resource_use_node, make_text_part_node, make_interp_part_node, MatchPattern, make_field_binding_node, field_binding_name, field_binding_pattern, LiteralValue, ExprErrorKind, BinOp, UnaryOpKind, StringPart, local_transport_node, rest_transport_node, shell_transport_node, file_transport_node, transport_url_key, transport_path_key, service_config_properties, OperationModifier, Token, TokenShape, SourceSpan, make_span, ErrorNode, make_error_node, with_required_cardinality, error_type, is_compiler_error, CompilerDiagnostic};
use crate::v2_std_core::InferredNode::{Resolved, CompilerError, TypeVariable};
use crate::v2_std_core::NodeType::{Typed, InferError, InferVariable, Untyped};
use crate::v2_std_core::Connective::{Conj, Disj, NoConnective};
use crate::v2_std_core::Cardinality::{Required, CardOptional};
use crate::v2_std_core::ExprData::{NoExprData, ExprLiteral, ExprVar, ExprFieldAccess, ExprCall, ExprMethodCall, ExprMatch, ExprIf, ExprLet, ExprRecordLit, ExprListLit, ExprBinOp, ExprUnaryOp, ExprLambda, ExprStringInterp, ExprBlock, ExprCast, ExprForEach, ExprIndex, ExprSlice, ExprReturn};
use crate::v2_std_core::MatchPattern::{Bind, LitPattern, VariantPattern, Wildcard};
use crate::v2_std_core::LiteralValue::{LitStr, LitInt, LitFloat, LitBool, LitNull};
use crate::v2_std_core::ExprErrorKind::{ParseRecoveryError};
use crate::v2_std_core::BinOp::{Add, Sub, Mul, Div, Mod, Eq, Ne, Lt, Gt, Le, Ge, And, Or, NullCoalesce};
use crate::v2_std_core::UnaryOpKind::{Not, Neg};
use crate::v2_std_core::StringPart::{Text, Interpolation};
use crate::v2_std_core::OperationModifier::{Idempotent, Readonly, Hermetic};
use crate::v2_std_core::TokenShape::{ShKeyword, ShLBrace, ShRBrace, ShLParen, ShRParen, ShLBracket, ShRBracket, ShLt, ShGt, ShLe, ShGe, ShFatArrow, ShArrow, ShColon, ShComma, ShDot, ShDotDot, ShEq, ShEqEq, ShNe, ShPlus, ShMinus, ShStar, ShSlash, ShPercent, ShBang, ShAnd, ShOr, ShQuestion, ShNullCoalesce, ShPipe, ShPipeArrow, ShLitStr, ShLitInt, ShLitFloat, ShIdent, ShStrBegin, ShStrMid, ShStrEnd, ShNewline, ShEof, ShUnknown};
use crate::v2_std_core::CompilerDiagnostic::{ParseError};
pub use crate::v2_compiler_languages::{ItemForm, BodyKind, OperatorSpec, SyntaxSpec};
use crate::v2_compiler_languages::BodyKind::{ExprBody, BlockBody, TypeBody, ValueBody, NoBody, ServiceBody, ResourceBody};
pub use crate::extdeps_languages_dag_syntax::{dag_syntax_spec};
use ExpectedToken::*;
use ParserHelperIdentity::*;
use ParserCallIdentity::*;
use ParserResultWitness::*;

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParserState {
    pub pos: i64,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParseResult {
    pub module: Option<Rc<Node>>,
    pub error: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AdvanceResult {
    pub token: Rc<Token>,
    pub state: ParserState,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct EatResult {
    pub consumed: bool,
    pub state: ParserState,
    pub token: Option<Rc<Token>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TokenResult {
    pub token: Rc<Token>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NameResult {
    pub name: String,
    pub span: Rc<SourceSpan>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExprResult {
    pub expr: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ItemResult {
    pub item: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeResult {
    pub type_expr: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ModuleResult {
    pub module: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ImportResult {
    pub import: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VariantResult {
    pub variant: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PredResult {
    pub predicate: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParamResult {
    pub param: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransportResult {
    pub transport: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OpResult {
    pub operation: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CapResult {
    pub capability: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PatternResult {
    pub pattern: Rc<MatchPattern>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArmResult {
    pub arm: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArgResult {
    pub arg: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldResult {
    pub field: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldInitResult {
    pub field: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResUseResult {
    pub resource_use: Rc<Node>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ItemPrefixResult {
    pub name: String,
    pub name_span: Rc<SourceSpan>,
    pub type_params: Rc<Vec<Rc<Node>>>,
    pub params: Rc<Vec<Rc<Node>>>,
    pub inferred: Option<Rc<InferredNode>>,
    pub uses: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ServiceConfig {
    pub endpoint: Rc<Node>,
    pub auth: Option<Rc<Node>>,
    pub rate_limit: Option<Rc<Node>>,
    pub retry: Option<Rc<Node>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConfigResult {
    pub config: Rc<ServiceConfig>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BindingPower {
    pub left: i64,
    pub right: i64,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]

pub enum ExpectedToken {
    ExpectKeyword {
        text: String,
    },
    ExpectLBrace,
    ExpectRBrace,
    ExpectLParen,
    ExpectRParen,
    ExpectLBracket,
    ExpectRBracket,
    ExpectLt,
    ExpectGt,
    ExpectFatArrow,
    ExpectArrow,
    ExpectColon,
    ExpectComma,
    ExpectDot,
    ExpectEq,
    ExpectQuestion,
    ExpectPipe,
}
impl ExpectedToken {
    pub fn text(&self) -> String {
        match self {
            ExpectedToken::ExpectKeyword { text: __val, .. } => __val.clone(),
            ExpectedToken::ExpectLBrace => panic!("no text on unit variant"),
            ExpectedToken::ExpectRBrace => panic!("no text on unit variant"),
            ExpectedToken::ExpectLParen => panic!("no text on unit variant"),
            ExpectedToken::ExpectRParen => panic!("no text on unit variant"),
            ExpectedToken::ExpectLBracket => panic!("no text on unit variant"),
            ExpectedToken::ExpectRBracket => panic!("no text on unit variant"),
            ExpectedToken::ExpectLt => panic!("no text on unit variant"),
            ExpectedToken::ExpectGt => panic!("no text on unit variant"),
            ExpectedToken::ExpectFatArrow => panic!("no text on unit variant"),
            ExpectedToken::ExpectArrow => panic!("no text on unit variant"),
            ExpectedToken::ExpectColon => panic!("no text on unit variant"),
            ExpectedToken::ExpectComma => panic!("no text on unit variant"),
            ExpectedToken::ExpectDot => panic!("no text on unit variant"),
            ExpectedToken::ExpectEq => panic!("no text on unit variant"),
            ExpectedToken::ExpectQuestion => panic!("no text on unit variant"),
            ExpectedToken::ExpectPipe => panic!("no text on unit variant"),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ImportsResult {
    pub imports: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ItemsResult {
    pub items: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NamesResult {
    pub names: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldsResult {
    pub fields: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FieldInitsResult {
    pub fields: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VariantsResult {
    pub variants: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PredsResult {
    pub predicates: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ParamsResult {
    pub params: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct UsesResult {
    pub uses: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArgsResult {
    pub args: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StmtsResult {
    pub stmts: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExprsResult {
    pub exprs: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ArmsResult {
    pub arms: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ModsResult {
    pub modifiers: Rc<Vec<OperationModifier>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BindingsResult {
    pub field_bindings: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

pub fn parse_recovery_expr(span: Rc<SourceSpan>, message: String) -> Rc<Node> {
    make_expr_error_node(ExprErrorKind::ParseRecoveryError, message, span)
}

pub fn parse_recovery_placeholder() -> Rc<Node> {
    parse_recovery_expr(make_span(0, 0), "parser recovery placeholder".to_string())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OptRetResult {
    pub inferred: Option<Rc<InferredNode>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GuardResult {
    pub guard: Option<Rc<Node>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FromKeyResult {
    pub from_key: Option<String>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PostfixResult {
    pub expr: Rc<Node>,
    pub changed: bool,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct LambdaCheckResult {
    pub is_lambda: bool,
    pub params: Rc<Vec<String>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IdentCollectResult {
    pub success: bool,
    pub params: Rc<Vec<String>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RangeArgsResult {
    pub min_val: Option<i64>,
    pub max_val: Option<i64>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NamedIntResult {
    pub arg_name: String,
    pub arg_value: i64,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ServiceBodyResult {
    pub config: Option<Rc<ServiceConfig>>,
    pub transport: Rc<Node>,
    pub operations: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IOResult {
    pub inputs: Rc<Vec<Rc<Node>>>,
    pub outputs: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResPropResult {
    pub properties: Rc<Vec<Rc<Node>>>,
    pub capabilities: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResponsesResult {
    pub responses: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MocksResult {
    pub mocks: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExitEntriesResult {
    pub entries: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RespEntriesResult {
    pub entries: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MockEntriesResult {
    pub entries: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OpBodyResult {
    pub inputs: Rc<Vec<Rc<Node>>>,
    pub outputs: Rc<Vec<Rc<Node>>>,
    pub modifier_props: Rc<Vec<Rc<Node>>>,
    pub transport: Option<Rc<Node>>,
    pub exit_props: Rc<Vec<Rc<Node>>>,
    pub response_props: Rc<Vec<Rc<Node>>>,
    pub mock_props: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct UnitResult {
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StringLitResult {
    pub value: String,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IntLitResult {
    pub value: i64,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]

pub enum ParserHelperIdentity {
    ParserHelperSkipNewlines,
    ParserHelperSkipContinuationNewlines,
    ParserHelperWith,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]

pub enum ParserCallIdentity {
    ParserCallHelper {
        helper: ParserHelperIdentity,
    },
    ParserCallFunction {
        name: String,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]

pub enum ParserResultWitness {
    ParserWitnessAdvance,
    ParserWitnessExpect,
    ParserWitnessEat,
    ParserWitnessCall {
        callee: Rc<ParserCallIdentity>,
    },
    ParserWitnessOpaque,
}
impl ParserResultWitness {
    pub fn callee(&self) -> Rc<ParserCallIdentity> {
        match self {
            ParserResultWitness::ParserWitnessAdvance => panic!("no callee on unit variant"),
            ParserResultWitness::ParserWitnessExpect => panic!("no callee on unit variant"),
            ParserResultWitness::ParserWitnessEat => panic!("no callee on unit variant"),
            ParserResultWitness::ParserWitnessCall { callee: __val, .. } => __val.clone(),
            ParserResultWitness::ParserWitnessOpaque => panic!("no callee on unit variant"),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DescResult {
    pub desc: Option<String>,
    pub state: ParserState,
}

pub fn peek(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Option<Rc<Token>> {
    tokens.get(state.pos.clone() as usize).cloned()
}

pub fn peek_shape(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Option<TokenShape> {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => Some(t.shape.clone()),
    None => None,
}
}
}

pub fn at_end(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    (state.pos.clone() >= (tokens.len() as i64))
}

pub fn current_span(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<SourceSpan> {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => t.span.clone(),
    None => make_span(0, 0),
}
}
}

pub fn advance(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<AdvanceResult> {
    {
        let tok = peek(tokens, state.clone());
match tok {
    Some(t) => {
            let next = ParserState { pos: (state.pos.clone() + 1), ..state.clone().clone() };
Rc::new(AdvanceResult {
    token: t.clone(),
    state: next,
})
},
    None => {
            let eof_tok = Rc::new(Token {
    text: "".to_string(),
    span: make_span(0, 0),
    shape: TokenShape::ShEof,
});
Rc::new(AdvanceResult {
    token: eof_tok,
    state: state.clone(),
})
},
}
}
}

pub fn parse_error(msg: String, span: Rc<SourceSpan>) -> Rc<ErrorNode> {
    make_error_node(Rc::new(CompilerDiagnostic::ParseError {
    message: msg,
    span: span,
}), "".to_string())
}

pub fn has_err(err: Option<Rc<ErrorNode>>) -> bool {
    match err {
    Some(_) => true,
    None => false,
}
}

pub fn parse_string_literal_value(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<StringLitResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
match tok {
    Some(t) => match t.shape.clone() {
    TokenShape::ShLitStr => {
            let adv = advance(tokens.clone(), state.clone());
Rc::new(StringLitResult {
    value: t.text.clone(),
    state: adv.state.clone(),
    err: None,
})
},
    _ => Rc::new(StringLitResult {
    value: "".to_string(),
    state: state.clone(),
    err: Some(parse_error("expected string literal".to_string(), current_span(tokens.clone(), state.clone()))),
}),
},
    None => Rc::new(StringLitResult {
    value: "".to_string(),
    state: state.clone(),
    err: Some(parse_error("expected string literal".to_string(), current_span(tokens.clone(), state.clone()))),
}),
}
}
}

pub fn parse_int_literal_value(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<IntLitResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
match tok {
    Some(t) => match t.shape.clone() {
    TokenShape::ShLitInt => {
            let n_opt = v2_rt::parse_int(t.text.clone());
match n_opt {
    Some(n) => {
                let adv = advance(tokens.clone(), state.clone());
Rc::new(IntLitResult {
    value: n.clone(),
    state: adv.state.clone(),
    err: None,
})
},
    None => Rc::new(IntLitResult {
    value: 0,
    state: state.clone(),
    err: Some(parse_error(v2_rt::concat(v2_rt::concat("internal: ShLitInt token text not parseable as int: '".to_string(), t.text.clone()), "'".to_string()), current_span(tokens.clone(), state.clone()))),
}),
}
},
    _ => Rc::new(IntLitResult {
    value: 0,
    state: state.clone(),
    err: Some(parse_error("expected integer literal".to_string(), current_span(tokens.clone(), state.clone()))),
}),
},
    None => Rc::new(IntLitResult {
    value: 0,
    state: state.clone(),
    err: Some(parse_error("expected integer literal".to_string(), current_span(tokens.clone(), state.clone()))),
}),
}
}
}

pub fn is_ident_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShIdent => true,
    _ => false,
}
}

pub fn is_lit_str_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLitStr => true,
    _ => false,
}
}

pub fn is_lit_int_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLitInt => true,
    _ => false,
}
}

pub fn is_lit_float_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLitFloat => true,
    _ => false,
}
}

pub fn is_str_begin_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShStrBegin => true,
    _ => false,
}
}

pub fn is_str_mid_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShStrMid => true,
    _ => false,
}
}

pub fn is_str_end_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShStrEnd => true,
    _ => false,
}
}

pub fn is_unknown_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShUnknown => true,
    _ => false,
}
}

pub fn is_newline_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShNewline => true,
    _ => false,
}
}

pub fn is_eof_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShEof => true,
    _ => false,
}
}

pub fn is_keyword_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShKeyword => true,
    _ => false,
}
}

pub fn peek_is_keyword(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, kw: String) -> bool {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => (is_keyword_shape(t.shape.clone()) && (t.text.clone().as_str() == kw.as_str())),
    None => false,
}
}
}

pub fn peek_keyword_text(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> String {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => if is_keyword_shape(t.shape.clone()) {
            t.text.clone()
} else {
            "".to_string()
},
    None => "".to_string(),
}
}
}

pub fn is_lbrace_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLBrace => true,
    _ => false,
}
}

pub fn is_rbrace_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShRBrace => true,
    _ => false,
}
}

pub fn is_lparen_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLParen => true,
    _ => false,
}
}

pub fn is_rparen_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShRParen => true,
    _ => false,
}
}

pub fn is_lbracket_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLBracket => true,
    _ => false,
}
}

pub fn is_rbracket_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShRBracket => true,
    _ => false,
}
}

pub fn is_lt_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLt => true,
    _ => false,
}
}

pub fn is_gt_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShGt => true,
    _ => false,
}
}

pub fn is_le_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShLe => true,
    _ => false,
}
}

pub fn is_ge_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShGe => true,
    _ => false,
}
}

pub fn is_fat_arrow_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShFatArrow => true,
    _ => false,
}
}

pub fn is_arrow_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShArrow => true,
    _ => false,
}
}

pub fn is_colon_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShColon => true,
    _ => false,
}
}

pub fn is_comma_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShComma => true,
    _ => false,
}
}

pub fn is_dot_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShDot => true,
    _ => false,
}
}

pub fn is_dot_dot_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShDotDot => true,
    _ => false,
}
}

pub fn is_eq_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShEq => true,
    _ => false,
}
}

pub fn is_eq_eq_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShEqEq => true,
    _ => false,
}
}

pub fn is_ne_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShNe => true,
    _ => false,
}
}

pub fn is_plus_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShPlus => true,
    _ => false,
}
}

pub fn is_minus_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShMinus => true,
    _ => false,
}
}

pub fn is_star_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShStar => true,
    _ => false,
}
}

pub fn is_slash_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShSlash => true,
    _ => false,
}
}

pub fn is_percent_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShPercent => true,
    _ => false,
}
}

pub fn is_bang_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShBang => true,
    _ => false,
}
}

pub fn is_and_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShAnd => true,
    _ => false,
}
}

pub fn is_or_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShOr => true,
    _ => false,
}
}

pub fn is_question_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShQuestion => true,
    _ => false,
}
}

pub fn is_null_coalesce_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShNullCoalesce => true,
    _ => false,
}
}

pub fn is_pipe_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShPipe => true,
    _ => false,
}
}

pub fn is_pipe_arrow_shape(shape: TokenShape) -> bool {
    match shape {
    TokenShape::ShPipeArrow => true,
    _ => false,
}
}

pub fn peek_is_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_ident_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_newline(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_newline_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_eof(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_eof_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_lit_str(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_lit_str_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_lbrace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_lbrace_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_rbrace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_rbrace_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_lparen(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_lparen_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_rparen(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_rparen_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_lbracket(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_lbracket_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_rbracket(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_rbracket_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_colon(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_colon_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_comma(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_comma_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_dot(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_dot_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_dot_dot(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_dot_dot_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_eq(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_eq_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_fat_arrow(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_fat_arrow_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_arrow(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_arrow_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_lt(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_lt_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_gt(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_gt_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_pipe(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_pipe_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_pipe_arrow(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_pipe_arrow_shape(shape.clone()),
    None => false,
}
}

pub fn peek_is_question(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match peek_shape(tokens, state) {
    Some(shape) => is_question_shape(shape.clone()),
    None => false,
}
}

pub fn shape_display_name(shape: TokenShape) -> String {
    match shape {
    TokenShape::ShKeyword => "keyword".to_string(),
    TokenShape::ShLBrace => "LBrace".to_string(),
    TokenShape::ShRBrace => "RBrace".to_string(),
    TokenShape::ShLParen => "LParen".to_string(),
    TokenShape::ShRParen => "RParen".to_string(),
    TokenShape::ShLBracket => "LBracket".to_string(),
    TokenShape::ShRBracket => "RBracket".to_string(),
    TokenShape::ShLt => "Lt".to_string(),
    TokenShape::ShGt => "Gt".to_string(),
    TokenShape::ShLe => "Le".to_string(),
    TokenShape::ShGe => "Ge".to_string(),
    TokenShape::ShFatArrow => "FatArrow".to_string(),
    TokenShape::ShArrow => "Arrow".to_string(),
    TokenShape::ShColon => "Colon".to_string(),
    TokenShape::ShComma => "Comma".to_string(),
    TokenShape::ShDot => "Dot".to_string(),
    TokenShape::ShDotDot => "DotDot".to_string(),
    TokenShape::ShEq => "Eq".to_string(),
    TokenShape::ShEqEq => "EqEq".to_string(),
    TokenShape::ShNe => "Ne".to_string(),
    TokenShape::ShPlus => "Plus".to_string(),
    TokenShape::ShMinus => "Minus".to_string(),
    TokenShape::ShStar => "Star".to_string(),
    TokenShape::ShSlash => "Slash".to_string(),
    TokenShape::ShPercent => "Percent".to_string(),
    TokenShape::ShBang => "Bang".to_string(),
    TokenShape::ShAnd => "And".to_string(),
    TokenShape::ShOr => "Or".to_string(),
    TokenShape::ShQuestion => "Question".to_string(),
    TokenShape::ShNullCoalesce => "NullCoalesce".to_string(),
    TokenShape::ShPipe => "Pipe".to_string(),
    TokenShape::ShPipeArrow => "PipeArrow".to_string(),
    TokenShape::ShLitStr => "LitStr".to_string(),
    TokenShape::ShLitInt => "LitInt".to_string(),
    TokenShape::ShLitFloat => "LitFloat".to_string(),
    TokenShape::ShIdent => "Ident".to_string(),
    TokenShape::ShStrBegin => "StrBegin".to_string(),
    TokenShape::ShStrMid => "StrMid".to_string(),
    TokenShape::ShStrEnd => "StrEnd".to_string(),
    TokenShape::ShNewline => "Newline".to_string(),
    TokenShape::ShEof => "Eof".to_string(),
    TokenShape::ShUnknown => "Unknown".to_string(),
}
}

pub fn token_display_name(token: Rc<Token>) -> String {
    if is_keyword_shape(token.shape.clone()) {
        v2_rt::concat("keyword '".to_string(), v2_rt::concat(token.text.clone(), "'".to_string()))
} else {
        shape_display_name(token.shape.clone())
}
}

pub fn expected_token_name(expected: Rc<ExpectedToken>) -> String {
    match (*expected).clone() {
    ExpectedToken::ExpectKeyword { text: kw, .. } => v2_rt::concat("keyword '".to_string(), v2_rt::concat(kw.clone(), "'".to_string())),
    ExpectedToken::ExpectLBrace => "LBrace".to_string(),
    ExpectedToken::ExpectRBrace => "RBrace".to_string(),
    ExpectedToken::ExpectLParen => "LParen".to_string(),
    ExpectedToken::ExpectRParen => "RParen".to_string(),
    ExpectedToken::ExpectLBracket => "LBracket".to_string(),
    ExpectedToken::ExpectRBracket => "RBracket".to_string(),
    ExpectedToken::ExpectLt => "Lt".to_string(),
    ExpectedToken::ExpectGt => "Gt".to_string(),
    ExpectedToken::ExpectFatArrow => "FatArrow".to_string(),
    ExpectedToken::ExpectArrow => "Arrow".to_string(),
    ExpectedToken::ExpectColon => "Colon".to_string(),
    ExpectedToken::ExpectComma => "Comma".to_string(),
    ExpectedToken::ExpectDot => "Dot".to_string(),
    ExpectedToken::ExpectEq => "Eq".to_string(),
    ExpectedToken::ExpectQuestion => "Question".to_string(),
    ExpectedToken::ExpectPipe => "Pipe".to_string(),
}
}

pub fn token_matches_expected(token: Rc<Token>, expected: Rc<ExpectedToken>) -> bool {
    match (*expected).clone() {
    ExpectedToken::ExpectKeyword { text: kw, .. } => (is_keyword_shape(token.shape.clone()) && (token.text.clone().as_str() == kw.clone().as_str())),
    ExpectedToken::ExpectLBrace => is_lbrace_shape(token.shape.clone()),
    ExpectedToken::ExpectRBrace => is_rbrace_shape(token.shape.clone()),
    ExpectedToken::ExpectLParen => is_lparen_shape(token.shape.clone()),
    ExpectedToken::ExpectRParen => is_rparen_shape(token.shape.clone()),
    ExpectedToken::ExpectLBracket => is_lbracket_shape(token.shape.clone()),
    ExpectedToken::ExpectRBracket => is_rbracket_shape(token.shape.clone()),
    ExpectedToken::ExpectLt => is_lt_shape(token.shape.clone()),
    ExpectedToken::ExpectGt => is_gt_shape(token.shape.clone()),
    ExpectedToken::ExpectFatArrow => is_fat_arrow_shape(token.shape.clone()),
    ExpectedToken::ExpectArrow => is_arrow_shape(token.shape.clone()),
    ExpectedToken::ExpectColon => is_colon_shape(token.shape.clone()),
    ExpectedToken::ExpectComma => is_comma_shape(token.shape.clone()),
    ExpectedToken::ExpectDot => is_dot_shape(token.shape.clone()),
    ExpectedToken::ExpectEq => is_eq_shape(token.shape.clone()),
    ExpectedToken::ExpectQuestion => is_question_shape(token.shape.clone()),
    ExpectedToken::ExpectPipe => is_pipe_shape(token.shape.clone()),
}
}

pub fn expect(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, expected: Rc<ExpectedToken>) -> Rc<TokenResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let matches = match tok.clone() {
    Some(t) => token_matches_expected(t.clone(), expected.clone()),
    None => false,
};
if matches {
            {
                let adv = advance(tokens.clone(), state.clone());
Rc::new(TokenResult {
    token: adv.token.clone(),
    state: adv.state.clone(),
    err: None,
})
}
} else {
            {
                let found = match tok.clone() {
    Some(t) => token_display_name(t.clone()),
    None => "EOF".to_string(),
};
let wanted = expected_token_name(expected.clone());
Rc::new(TokenResult {
    token: Rc::new(Token {
    text: "".to_string(),
    span: make_span(0, 0),
    shape: TokenShape::ShEof,
}),
    state: state.clone(),
    err: Some(parse_error(format!("expected {}, found {}", wanted, found), current_span(tokens.clone(), state.clone()))),
})
}
}
}
}

pub fn expect_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<NameResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh.clone() {
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
let span = tok.clone().unwrap().span.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(NameResult {
    name: n,
    span: span,
    state: adv.state.clone(),
    err: None,
})
},
    _ => {
            let found = match sh.clone() {
    Some(shape) => shape_display_name(shape.clone()),
    None => "EOF".to_string(),
};
Rc::new(NameResult {
    name: "".to_string(),
    span: current_span(tokens.clone(), state.clone()),
    state: state.clone(),
    err: Some(parse_error(v2_rt::concat("expected identifier, found ".to_string(), found), current_span(tokens.clone(), state.clone()))),
})
},
}
}
}

pub fn expect_name(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<NameResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
let span = tok.clone().unwrap().span.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(NameResult {
    name: n,
    span: span,
    state: adv.state.clone(),
    err: None,
})
},
    _ => {
            let kw_name = keyword_to_name(tokens.clone(), state.clone());
match kw_name {
    Some(n) => {
                let span = tok.clone().unwrap().span.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(NameResult {
    name: n,
    span: span,
    state: adv.state.clone(),
    err: None,
})
},
    None => {
                let found = match tok.clone() {
    Some(t) => token_display_name(t.clone()),
    None => "EOF".to_string(),
};
Rc::new(NameResult {
    name: "".to_string(),
    span: current_span(tokens.clone(), state.clone()),
    state: state.clone(),
    err: Some(parse_error(v2_rt::concat("expected name, found ".to_string(), found), current_span(tokens.clone(), state.clone()))),
})
},
}
},
}
}
}

pub fn keyword_to_name(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Option<String> {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => if is_name_keyword(t.clone()) {
            Some(t.text.clone())
} else {
            None
},
    None => None,
}
}
}

pub fn is_name_keyword(token: Rc<Token>) -> bool {
    if is_keyword_shape(token.shape.clone()) {
        {
            let is_literal = match v2_rt::lookup(&dag_syntax_spec().keyword_literals.clone(), token.text.clone()) {
    Some(_) => true,
    None => false,
};
let is_lifecycle = ((token.text.clone().as_str() == "acquire".to_string().as_str()) || (token.text.clone().as_str() == "release".to_string().as_str()));
(!is_literal && !is_lifecycle)
}
} else {
        false
}
}

pub fn skip_newlines(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState) -> ParserState {
    loop {
        if peek_is_newline(tokens.clone(), state.clone()) {
            let adv = advance(tokens.clone(), state.clone());
{
                let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
tokens = __tco_0;
state = __tco_1;
continue;
}
} else {
            break state.clone();
}
}
}

pub fn is_continuation_shape(shape: TokenShape) -> bool {
    (((is_pipe_arrow_shape(shape.clone()) || is_dot_shape(shape.clone())) || is_or_shape(shape.clone())) || is_and_shape(shape.clone()))
}

pub fn skip_continuation_newlines(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> ParserState {
    {
        let is_continuation = if peek_is_newline(tokens.clone(), state.clone()) {
            {
                let s = skip_newlines(tokens.clone(), state.clone());
match peek_shape(tokens.clone(), s) {
    Some(shape) => is_continuation_shape(shape.clone()),
    None => false,
}
}
} else {
            false
};
if is_continuation {
            skip_newlines(tokens.clone(), state.clone())
} else {
            state.clone()
}
}
}

pub fn eat(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, expected: Rc<ExpectedToken>) -> Rc<EatResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let matches = match tok {
    Some(t) => token_matches_expected(t.clone(), expected),
    None => false,
};
if matches {
            {
                let adv = advance(tokens.clone(), state.clone());
Rc::new(EatResult {
    consumed: true,
    state: adv.state.clone(),
    token: Some(adv.token.clone()),
})
}
} else {
            Rc::new(EatResult {
    consumed: false,
    state: state.clone(),
    token: None,
})
}
}
}

pub fn parser_result_base_var(expr: Rc<Node>) -> Option<String> {
    match (*expr.expr_data.clone()).clone() {
    ExprData::ExprFieldAccess { .. } => match expr.children.clone().first().cloned() {
    Some(base) => match (*base.expr_data.clone()).clone() {
    ExprData::ExprVar { .. } => Some(expr_var_name(base.clone())),
    _ => None,
},
    None => None,
},
    _ => None,
}
}

pub fn parser_helper_state_arg_expr(call_node: Rc<Node>) -> Option<Rc<Node>> {
    Rc::new(call_node.children.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned().fold(None, |acc: _, pair: (i64, Rc<Node>)| if (acc.clone() != None) {
        acc.clone()
} else {
        {
            let idx = pair.0.clone();
let arg_node = pair.1.clone();
let matches_state = match arg_name(arg_node.clone()) {
    Some(name) => (name.clone().as_str() == "state".to_string().as_str()),
    None => (idx.clone() == 1),
};
if matches_state.clone() {
                Some(arg_value(arg_node.clone()))
} else {
                None
}
}
})
}

pub fn parser_progress_flag_var(expr: Rc<Node>) -> Option<String> {
    match (*expr.expr_data.clone()).clone() {
    ExprData::ExprFieldAccess { .. } => {
        let field = field_access_field(expr.clone());
if ((field.clone().as_str() == "consumed".to_string().as_str()) || (field.clone().as_str() == "changed".to_string().as_str())) {
            parser_result_base_var(expr.clone())
} else {
            None
}
},
    _ => None,
}
}

pub fn parser_helper_identity(callee: String) -> Option<ParserHelperIdentity> {
    if (callee.clone().as_str() == "skip_newlines".to_string().as_str()) {
        Some(ParserHelperIdentity::ParserHelperSkipNewlines)
} else {
        if (callee.clone().as_str() == "skip_continuation_newlines".to_string().as_str()) {
            Some(ParserHelperIdentity::ParserHelperSkipContinuationNewlines)
} else {
            if (callee.clone().as_str() == "with".to_string().as_str()) {
                Some(ParserHelperIdentity::ParserHelperWith)
} else {
                None
}
}
}
}

pub fn parser_passthrough_state_expr(expr: Rc<Node>) -> Option<Rc<Node>> {
    match (*expr.expr_data.clone()).clone() {
    ExprData::ExprCall { .. } => match parser_helper_identity(expr_call_func(expr.clone())) {
    Some(ParserHelperIdentity::ParserHelperSkipNewlines) => parser_helper_state_arg_expr(expr.clone()),
    Some(ParserHelperIdentity::ParserHelperSkipContinuationNewlines) => parser_helper_state_arg_expr(expr.clone()),
    Some(ParserHelperIdentity::ParserHelperWith) => match expr.children.clone().first().cloned() {
    Some(base_arg) => Some(arg_value(base_arg.clone())),
    None => None,
},
    None => None,
},
    _ => None,
}
}

pub fn parser_result_witness(expr: Rc<Node>) -> Rc<ParserResultWitness> {
    match (*expr.expr_data.clone()).clone() {
    ExprData::ExprCall { .. } => match expr_call_func(expr.clone()) {
    ref __s if __s == "advance" => Rc::new(ParserResultWitness::ParserWitnessAdvance),
    ref __s if __s == "expect" => Rc::new(ParserResultWitness::ParserWitnessExpect),
    ref __s if __s == "eat" => Rc::new(ParserResultWitness::ParserWitnessEat),
    callee => match parser_helper_identity(callee.clone()) {
    Some(helper) => Rc::new(ParserResultWitness::ParserWitnessCall {
    callee: Rc::new(ParserCallIdentity::ParserCallHelper {
    helper: helper.clone(),
}),
}),
    None => Rc::new(ParserResultWitness::ParserWitnessCall {
    callee: Rc::new(ParserCallIdentity::ParserCallFunction {
    name: callee.clone(),
}),
}),
},
},
    _ => Rc::new(ParserResultWitness::ParserWitnessOpaque),
}
}

pub fn is_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    peek_is_ident(tokens, state)
}

pub fn is_keyword_name(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    match keyword_to_name(tokens, state) {
    Some(_) => true,
    None => false,
}
}

pub fn leaf_type_node(name: String, span: Rc<SourceSpan>) -> Rc<Node> {
    Rc::new(Node {
    name: name.clone(),
    span: span.clone(),
    ident_span: if (name.clone().as_str() == "".to_string().as_str()) {
        None
} else {
        Some(span.clone())
},
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn is_conj_with_children(n: Rc<Node>) -> bool {
    ((n.connective.clone() == Connective::Conj) && ((n.children.clone().len() as i64) > 0))
}

pub fn child_inferred_or_empty(ch: Rc<Node>) -> Rc<Node> {
    match (*rt_node(ch)).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => error_type(),
    NodeType::InferVariable { .. } => error_type(),
    NodeType::Untyped => error_type(),
}
}

pub fn node_inferred_to_outputs(rt: Rc<Node>) -> Rc<Vec<Rc<Node>>> {
    if is_conj_with_children(rt.clone()) {
        {
            let all_children_typed = { let mut __all = true; for ch in rt.children.clone().iter().cloned() { if !(match (*rt_node(ch.clone())).clone() {
    NodeType::Typed { .. } => true,
    _ => false,
}) { __all = false; break; } } __all };
if all_children_typed {
                Rc::new({ let mut __result = Vec::new(); for ch in rt.children.clone().iter().cloned() { __result.push(make_field_node(ch.name.clone(), child_inferred_or_empty(ch.clone()), Cardinality::Required, ch.body.clone(), None, ch.span.clone())); } __result })
} else {
                Rc::new(vec![])
}
}
} else {
        Rc::new(vec![make_field_node("value".to_string(), rt.clone(), Cardinality::Required, None, None, rt.span.clone())])
}
}

pub fn parse_dotted_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<NameResult> {
    {
        let r = expect_name(tokens.clone(), state);
if has_err(r.err.clone()) {
            return r.clone()
}
parse_dotted_ident_rest(tokens.clone(), r.state.clone(), r.name.clone(), r.span.clone())
}
}

pub fn parse_dotted_ident_rest(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: String, mut span: Rc<SourceSpan>) -> Rc<NameResult> {
    loop {
        let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectDot));
if e.consumed.clone() {
            let r = expect_name(tokens.clone(), e.state.clone());
if has_err(r.err.clone()) {
                return r.clone()
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::concat(v2_rt::concat(acc, ".".to_string()), r.name.clone());
let __tco_3 = span;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
span = __tco_3;
continue;
}
} else {
            break Rc::new(NameResult {
    name: acc.clone(),
    span: span.clone(),
    state: state.clone(),
    err: None,
});
}
}
}

pub fn parse(tokens: Rc<Vec<Rc<Token>>>) -> Rc<ParseResult> {
    {
        let state = ParserState {
    pos: 0,
};
let r = parse_module(tokens, state);
if has_err(r.err.clone()) {
            Rc::new(ParseResult {
    module: None,
    error: r.err.clone(),
})
} else {
            Rc::new(ParseResult {
    module: Some(r.module.clone()),
    error: None,
})
}
}
}

pub fn parse_module(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ModuleResult> {
    {
        let s = skip_newlines(tokens.clone(), state);
let start_span = current_span(tokens.clone(), s.clone());
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "module".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ModuleResult {
    module: module_node("".to_string(), Rc::new(vec![]), Rc::new(vec![]), start_span.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = r.state.clone();
let r = parse_dotted_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ModuleResult {
    module: module_node("".to_string(), Rc::new(vec![]), Rc::new(vec![]), start_span.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let mod_name = r.name.clone();
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = parse_imports(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ModuleResult {
    module: module_node("".to_string(), Rc::new(vec![]), Rc::new(vec![]), start_span.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let imports = r.imports.clone();
let s = r.state.clone();
let r = parse_items(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ModuleResult {
    module: module_node("".to_string(), Rc::new(vec![]), Rc::new(vec![]), start_span.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let items = r.items.clone();
let s = r.state.clone();
let r#mod = module_node(mod_name, imports, items, start_span.clone());
Rc::new(ModuleResult {
    module: r#mod,
    state: s.clone(),
    err: None,
})
}
}

pub fn parse_imports(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ImportsResult> {
    parse_imports_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_imports_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ImportsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if peek_is_keyword(tokens.clone(), s.clone(), "import".to_string()) {
            let r = parse_import(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ImportsResult {
    imports: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.import.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(ImportsResult {
    imports: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}

pub fn parse_items(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemsResult> {
    parse_items_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_items_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ItemsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (at_end(tokens.clone(), s.clone()) || peek_is_eof(tokens.clone(), s.clone())) {
            break Rc::new(ItemsResult {
    items: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_item(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ItemsResult {
    items: acc.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.item.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_import(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ImportResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let err_import = import_node("".to_string(), false, Rc::new(vec![]), start_span.clone());
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "import".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ImportResult {
    import: err_import.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = r.state.clone();
let r = parse_dotted_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ImportResult {
    import: err_import.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let mod_path = r.name.clone();
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if e.consumed.clone() {
            {
                let r = parse_import_names(tokens.clone(), e.state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ImportResult {
    import: err_import.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let names = r.names.clone();
let s = r.state.clone();
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r.err.clone()) {
                    return Rc::new(ImportResult {
    import: err_import.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let imp = import_node(mod_path, false, names, start_span.clone());
Rc::new(ImportResult {
    import: imp,
    state: s.clone(),
    err: None,
})
}
} else {
            {
                let s = skip_newlines(tokens.clone(), s.clone());
let imp = import_node(mod_path, true, Rc::new(vec![]), start_span.clone());
Rc::new(ImportResult {
    import: imp,
    state: s.clone(),
    err: None,
})
}
}
}
}

pub fn parse_import_names(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<NamesResult> {
    parse_import_names_acc(tokens, state, Rc::new(vec![]))
}

pub fn parsed_name_leaf(name: String, span: Rc<SourceSpan>) -> Rc<Node> {
    leaf_node_with_span(name, span)
}

pub fn parse_import_names_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<NamesResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if peek_is_rbrace(tokens.clone(), s.clone()) {
            break Rc::new(NamesResult {
    names: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let name_span = current_span(tokens.clone(), s.clone());
let r = parse_dotted_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(NamesResult {
    names: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name_node = parsed_name_leaf(r.name.clone(), name_span.clone());
let s = skip_newlines(tokens.clone(), r.state.clone());
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectComma));
let s = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                s.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s.clone();
let __tco_2 = v2_rt::rc_list_push(acc, name_node.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn find_item_form(forms: Rc<Vec<Rc<ItemForm>>>, keyword: String) -> Option<Rc<ItemForm>> {
    {
        let matches = Rc::new({ let mut __result = Vec::new(); for f in forms.iter().cloned() { if (f.keyword.clone().as_str() == keyword.clone().as_str()) { __result.push(f); } } __result });
match (matches.clone().len() as i64) {
    0 => None,
    _ => Some(matches.clone().first().cloned().clone().unwrap()),
}
}
}

pub fn parse_item(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let s = skip_newlines(tokens.clone(), state);
let kw = peek_keyword_text(tokens.clone(), s.clone());
let form = find_item_form(dag_syntax_spec().item_forms.clone(), kw);
match form {
    Some(f) => parse_item_by_form(tokens.clone(), s.clone(), f.clone()),
    None => Rc::new(ItemResult {
    item: Rc::new(Node {
    name: "<unknown>".to_string(),
    span: current_span(tokens.clone(), s.clone()),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
    state: s.clone(),
    err: Some(parse_error("expected item declaration (type, fn, func, service, resource, data, extern, pattern, interface)".to_string(), current_span(tokens.clone(), s.clone()))),
}),
}
}
}

pub fn parse_item_prefix(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, form: Rc<ItemForm>) -> Rc<ItemPrefixResult> {
    {
        let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemPrefixResult {
    name: "".to_string(),
    name_span: r.span.clone(),
    type_params: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    uses: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let tp_result = if form.has_type_params.clone() {
            parse_optional_type_params(tokens.clone(), r.state.clone())
} else {
            Rc::new(TypeParamsResult {
    params: Rc::new(vec![]),
    state: r.state.clone(),
})
};
let params_result = if form.has_params.clone() {
            parse_params(tokens.clone(), tp_result.state.clone())
} else {
            Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: tp_result.state.clone(),
    err: None,
})
};
if has_err(params_result.err.clone()) {
            return Rc::new(ItemPrefixResult {
    name: name.clone(),
    name_span: name_span.clone(),
    type_params: tp_result.params.clone(),
    params: Rc::new(vec![]),
    inferred: None,
    uses: Rc::new(vec![]),
    state: params_result.state.clone(),
    err: params_result.err.clone(),
})
}
let ret_result = if form.has_return_type.clone() {
            parse_optional_inferred(tokens.clone(), params_result.state.clone())
} else {
            Rc::new(OptRetResult {
    inferred: None,
    state: params_result.state.clone(),
    err: None,
})
};
if has_err(ret_result.err.clone()) {
            return Rc::new(ItemPrefixResult {
    name: name.clone(),
    name_span: name_span.clone(),
    type_params: tp_result.params.clone(),
    params: params_result.params.clone(),
    inferred: None,
    uses: Rc::new(vec![]),
    state: ret_result.state.clone(),
    err: ret_result.err.clone(),
})
}
let missing_required_ret = (form.return_required.clone() && match ret_result.inferred.clone() {
    Some(_) => false,
    None => true,
});
if missing_required_ret {
            return Rc::new(ItemPrefixResult {
    name: name.clone(),
    name_span: name_span.clone(),
    type_params: tp_result.params.clone(),
    params: params_result.params.clone(),
    inferred: None,
    uses: Rc::new(vec![]),
    state: ret_result.state.clone(),
    err: Some(parse_error(v2_rt::concat("expected return type annotation (-> Type) for '".to_string(), v2_rt::concat(form.keyword.clone(), v2_rt::concat(" ".to_string(), name.clone()))), current_span(tokens.clone(), ret_result.state.clone()))),
})
}
let uses_result = if form.has_uses.clone() {
            parse_uses_clause(tokens.clone(), skip_newlines(tokens.clone(), ret_result.state.clone()))
} else {
            Rc::new(UsesResult {
    uses: Rc::new(vec![]),
    state: ret_result.state.clone(),
    err: None,
})
};
if has_err(uses_result.err.clone()) {
            return Rc::new(ItemPrefixResult {
    name: name.clone(),
    name_span: name_span.clone(),
    type_params: tp_result.params.clone(),
    params: params_result.params.clone(),
    inferred: ret_result.inferred.clone(),
    uses: Rc::new(vec![]),
    state: uses_result.state.clone(),
    err: uses_result.err.clone(),
})
}
Rc::new(ItemPrefixResult {
    name: name.clone(),
    name_span: name_span.clone(),
    type_params: tp_result.params.clone(),
    params: params_result.params.clone(),
    inferred: ret_result.inferred.clone(),
    uses: uses_result.uses.clone(),
    state: uses_result.state.clone(),
    err: None,
})
}
}

pub fn parse_item_by_form(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, form: Rc<ItemForm>) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: form.keyword.clone(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
match form.body_kind.clone() {
    BodyKind::TypeBody => {
            let prefix = parse_item_prefix(tokens.clone(), r.state.clone(), form.clone());
if has_err(prefix.err.clone()) {
                return Rc::new(ItemResult {
    item: dummy.clone(),
    state: prefix.state.clone(),
    err: prefix.err.clone(),
})
}
parse_type_body_from_prefix(tokens.clone(), prefix.clone(), start_span.clone())
},
    BodyKind::ExprBody => {
            let prefix = parse_item_prefix(tokens.clone(), r.state.clone(), form.clone());
if has_err(prefix.err.clone()) {
                return Rc::new(ItemResult {
    item: dummy.clone(),
    state: prefix.state.clone(),
    err: prefix.err.clone(),
})
}
parse_fn_body_from_prefix(tokens.clone(), prefix.clone(), start_span.clone())
},
    BodyKind::BlockBody => {
            let prefix = parse_item_prefix(tokens.clone(), r.state.clone(), form.clone());
if has_err(prefix.err.clone()) {
                return Rc::new(ItemResult {
    item: dummy.clone(),
    state: prefix.state.clone(),
    err: prefix.err.clone(),
})
}
parse_block_body_from_prefix(tokens.clone(), prefix.clone(), start_span.clone())
},
    BodyKind::ServiceBody => parse_service_after_kw(tokens.clone(), r.state.clone(), start_span.clone()),
    BodyKind::ResourceBody => parse_resource_after_kw(tokens.clone(), r.state.clone(), start_span.clone()),
    BodyKind::ValueBody => parse_data_after_kw(tokens.clone(), r.state.clone(), start_span.clone()),
    BodyKind::NoBody => {
            let prefix = parse_item_prefix(tokens.clone(), r.state.clone(), form.clone());
if has_err(prefix.err.clone()) {
                return Rc::new(ItemResult {
    item: dummy.clone(),
    state: prefix.state.clone(),
    err: prefix.err.clone(),
})
}
parse_no_body_from_prefix(prefix.clone(), start_span.clone())
},
}
}
}

pub fn field_to_child_node(field: Rc<Node>) -> Rc<Node> {
    {
        let ret_type = field_node_type_expr(field.clone());
let props = match field_node_from_key(field.clone()) {
    Some(key) => Rc::new(vec![make_field_init_node("from_key".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: key.clone(),
}),
}), Rc::new(vec![]), None, field.span.clone()), field.span.clone())]),
    None => Rc::new(vec![]),
};
Rc::new(Node {
    name: field_node_name(field.clone()),
    span: field.span.clone(),
    ident_span: field.ident_span.clone(),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: ret_type,
})),
    return_cardinality: field_node_cardinality(field.clone()),
    uses: Rc::new(vec![]),
    body: field_node_default_value(field.clone()),
    transport: None,
    properties: props,
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}
}

pub fn variant_to_child_node(variant: Rc<Node>) -> Rc<Node> {
    {
        let fields = variant_node_fields(variant.clone());
let children = Rc::new({ let mut __result = Vec::new(); for f in fields.clone().iter().cloned() { __result.push(field_to_child_node(f.clone())); } __result });
Rc::new(Node {
    name: variant_node_name(variant.clone()),
    span: variant.span.clone(),
    ident_span: variant.ident_span.clone(),
    children: children,
    connective: if ((fields.clone().len() as i64) > 0) {
            Connective::Conj
} else {
            Connective::NoConnective
},
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}
}

pub fn outputs_to_inferred(outputs: Rc<Vec<Rc<Node>>>, span: Rc<SourceSpan>) -> Option<Rc<InferredNode>> {
    if ((outputs.clone().len() as i64) > 0) {
        Some(Rc::new(InferredNode::Resolved {
    node: Rc::new(Node {
    name: "".to_string(),
    span: span,
    ident_span: None,
    children: Rc::new({ let mut __result = Vec::new(); for f in outputs.clone().iter().cloned() { __result.push(field_to_child_node(f.clone())); } __result }),
    connective: Connective::Conj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}),
}))
} else {
        None
}
}

pub fn make_operation_node(name: String, ident_span: Option<Rc<SourceSpan>>, inputs: Rc<Vec<Rc<Node>>>, outputs: Rc<Vec<Rc<Node>>>, response_props: Rc<Vec<Rc<Node>>>, mock_props: Rc<Vec<Rc<Node>>>, exit_props: Rc<Vec<Rc<Node>>>, modifier_props: Rc<Vec<Rc<Node>>>, transport: Option<Rc<Node>>, span: Rc<SourceSpan>) -> Rc<Node> {
    {
        let all_props = v2_rt::concat(v2_rt::concat(v2_rt::concat(modifier_props, response_props), exit_props), mock_props);
Rc::new(Node {
    name: name,
    span: span.clone(),
    ident_span: ident_span,
    children: Rc::new(vec![]),
    params: Rc::new({ let mut __result = Vec::new(); for f in inputs.iter().cloned() { __result.push(make_param_node(field_node_name(f.clone()), field_node_type_expr(f.clone()), field_node_default_value(f.clone()), f.span.clone())); } __result }),
    inferred: outputs_to_inferred(outputs, span.clone()),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: transport,
    properties: all_props,
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}
}

pub fn make_capability_node(name: String, ident_span: Option<Rc<SourceSpan>>, inputs: Rc<Vec<Rc<Node>>>, outputs: Rc<Vec<Rc<Node>>>, span: Rc<SourceSpan>) -> Rc<Node> {
    Rc::new(Node {
    name: name,
    span: span.clone(),
    ident_span: ident_span,
    children: Rc::new(vec![]),
    params: Rc::new({ let mut __result = Vec::new(); for f in inputs.iter().cloned() { __result.push(make_param_node(field_node_name(f.clone()), field_node_type_expr(f.clone()), field_node_default_value(f.clone()), f.span.clone())); } __result }),
    inferred: outputs_to_inferred(outputs, span.clone()),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn parse_type_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "type".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
parse_type_after_kw(tokens.clone(), r.state.clone(), start_span.clone())
}
}

pub fn parse_type_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let type_params_result = parse_optional_type_params(tokens.clone(), r.state.clone());
let type_params = type_params_result.params.clone();
let s = skip_newlines(tokens.clone(), type_params_result.state.clone());
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if e.consumed.clone() {
            {
                let r = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), e.state.clone()));
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
if has_err(r.err.clone()) {
                    return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                    return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let type_children = Rc::new({ let mut __result = Vec::new(); for f in r.fields.clone().iter().cloned() { __result.push(field_to_child_node(f.clone())); } __result });
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: type_children,
    connective: Connective::Conj,
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r2.state.clone()),
    err: None,
})
}
} else {
            {
                let eq = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectEq));
if eq.consumed.clone() {
                    {
                        let s = skip_newlines(tokens.clone(), eq.state.clone());
parse_type_body_after_eq(tokens.clone(), s.clone(), name.clone(), name_span.clone(), start_span.clone(), type_params.clone())
}
} else {
                    {
                        let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: type_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_type_node(name.clone(), name_span.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: s.clone(),
    err: None,
})
}
}
}
}
}
}

pub fn parse_type_body_from_prefix(tokens: Rc<Vec<Rc<Token>>>, prefix: Rc<ItemPrefixResult>, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let name = prefix.name.clone();
let name_span = prefix.name_span.clone();
let type_params = prefix.type_params.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let s = skip_newlines(tokens.clone(), prefix.state.clone());
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if e.consumed.clone() {
            {
                let r = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), e.state.clone()));
if has_err(r.err.clone()) {
                    return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                    return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let type_children = Rc::new({ let mut __result = Vec::new(); for f in r.fields.clone().iter().cloned() { __result.push(field_to_child_node(f.clone())); } __result });
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: type_children,
    connective: Connective::Conj,
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r2.state.clone()),
    err: None,
})
}
} else {
            {
                let eq = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectEq));
if eq.consumed.clone() {
                    {
                        let s = skip_newlines(tokens.clone(), eq.state.clone());
parse_type_body_after_eq(tokens.clone(), s.clone(), name.clone(), name_span.clone(), start_span.clone(), type_params.clone())
}
} else {
                    {
                        let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: type_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: leaf_type_node(name.clone(), name_span.clone()),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: s.clone(),
    err: None,
})
}
}
}
}
}
}

pub fn parse_type_body_after_eq(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String, name_span: Rc<SourceSpan>, start_span: Rc<SourceSpan>, type_params: Rc<Vec<Rc<Node>>>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
if is_ident(tokens.clone(), state.clone()) {
            {
                let r = expect_ident(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let first_name = r.name.clone();
let first_name_span = r.span.clone();
let s = skip_newlines(tokens.clone(), r.state.clone());
if (peek_is_lbrace(tokens.clone(), s.clone()) || peek_is_pipe(tokens.clone(), s.clone())) {
                    {
                        let r = parse_variant_fields(tokens.clone(), s.clone(), first_name);
if has_err(r.err.clone()) {
                            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let first_variant = r.variant.clone();
let rest = parse_more_variants_acc(tokens.clone(), r.state.clone(), Rc::new(vec![first_variant]));
if has_err(rest.err.clone()) {
                            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: rest.state.clone(),
    err: rest.err.clone(),
})
}
let variants = rest.variants.clone();
let type_children = Rc::new({ let mut __result = Vec::new(); for v in variants.iter().cloned() { __result.push(variant_to_child_node(v.clone())); } __result });
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: type_children,
    connective: Connective::Disj,
    params: type_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), rest.state.clone()),
    err: None,
})
}
} else {
                    {
                        let r = finish_type_expr_from_name(tokens.clone(), s.clone(), first_name, first_name_span);
if has_err(r.err.clone()) {
                            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let wr = try_where_clause(tokens.clone(), r.state.clone(), r.type_expr.clone(), start_span.clone());
if has_err(wr.err.clone()) {
                            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: wr.state.clone(),
    err: wr.err.clone(),
})
}
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: type_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: wr.type_expr.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), wr.state.clone()),
    err: None,
})
}
}
}
} else {
            {
                let r = parse_type_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let wr = try_where_clause(tokens.clone(), r.state.clone(), r.type_expr.clone(), start_span.clone());
if has_err(wr.err.clone()) {
                    return Rc::new(ItemResult {
    item: dummy.clone(),
    state: wr.state.clone(),
    err: wr.err.clone(),
})
}
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: type_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: wr.type_expr.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), wr.state.clone()),
    err: None,
})
}
}
}
}

pub fn try_where_clause(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, base_te: Rc<Node>, start_span: Rc<SourceSpan>) -> Rc<TypeResult> {
    if peek_is_keyword(tokens.clone(), state.clone(), "where".to_string()) {
        {
            let adv = advance(tokens.clone(), state.clone());
let r = parse_predicates(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                return Rc::new(TypeResult {
    type_expr: base_te.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let refined = Rc::new(Node {
    name: "Refined".to_string(),
    span: start_span,
    ident_span: None,
    children: Rc::new(vec![base_te.clone()]),
    connective: Connective::Conj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: r.predicates.clone(),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(TypeResult {
    type_expr: refined,
    state: r.state.clone(),
    err: None,
})
}
} else {
        Rc::new(TypeResult {
    type_expr: base_te.clone(),
    state: state.clone(),
    err: None,
})
}
}

pub fn parse_predicates(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<PredsResult> {
    parse_predicates_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_predicates_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<PredsResult> {
    loop {
        let r = parse_single_predicate(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(PredsResult {
    predicates: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
acc = v2_rt::rc_list_push(acc.clone(), r.predicate.clone());
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            {
                let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), e.state.clone());
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(PredsResult {
    predicates: acc.clone(),
    state: r.state.clone(),
    err: None,
});
}
}
}

pub fn parse_single_predicate(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<PredResult> {
    {
        let zero_span = make_span(0, 0);
let dummy_pred = make_field_init_node("".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitBool {
    value: false,
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone());
let r = expect_name(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let pred_name = r.name.clone();
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLParen));
if e.consumed.clone() {
            match pred_name.clone().as_str() {
    "pattern" => {
                let r2 = parse_string_literal_value(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(PredResult {
    predicate: make_field_init_node("Pattern".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: r2.value.clone(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    "format" => {
                let r2 = expect_ident(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(PredResult {
    predicate: make_field_init_node("Format".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: r2.name.clone(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    "brand" => {
                let r2 = parse_string_literal_value(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(PredResult {
    predicate: make_field_init_node("Brand".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: r2.value.clone(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    "content" => {
                let r2 = expect_ident(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(PredResult {
    predicate: make_field_init_node("ContentEncoding".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: r2.name.clone(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    "domain" => {
                let r2 = expect_ident(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(PredResult {
    predicate: make_field_init_node("Domain".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: r2.name.clone(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    "range" => {
                let r2 = parse_named_int_args(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
                    return Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let min_fields = if (r2.min_val.clone() != None) {
                    Rc::new(vec![make_field_init_node("min".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: r2.min_val.clone().clone().unwrap(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone())])
} else {
                    Rc::new(vec![])
};
let max_fields = if (r2.max_val.clone() != None) {
                    Rc::new(vec![make_field_init_node("max".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: r2.max_val.clone().clone().unwrap(),
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone())])
} else {
                    Rc::new(vec![])
};
Rc::new(PredResult {
    predicate: make_field_init_node("Range".to_string(), make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), v2_rt::concat(min_fields, max_fields), None, zero_span.clone()), zero_span.clone()),
    state: r3.state.clone(),
    err: None,
})
},
    _ => Rc::new(PredResult {
    predicate: dummy_pred.clone(),
    state: e.state.clone(),
    err: Some(parse_error(v2_rt::concat(v2_rt::concat("unknown where predicate `".to_string(), pred_name.clone()), "`".to_string()), current_span(tokens.clone(), e.state.clone()))),
}),
}
} else {
            match pred_name.clone().as_str() {
    "non_empty" => Rc::new(PredResult {
    predicate: make_field_init_node("NonEmpty".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitBool {
    value: true,
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: s.clone(),
    err: None,
}),
    _ => Rc::new(PredResult {
    predicate: make_field_init_node(pred_name.clone(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitBool {
    value: true,
}),
}), Rc::new(vec![]), None, zero_span.clone()), zero_span.clone()),
    state: s.clone(),
    err: None,
}),
}
}
}
}

pub fn parse_named_int_args(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<RangeArgsResult> {
    {
        let r = parse_single_named_int(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(RangeArgsResult {
    min_val: None,
    max_val: None,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            {
                let r2 = parse_single_named_int(tokens.clone(), skip_newlines(tokens.clone(), e.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(RangeArgsResult {
    min_val: None,
    max_val: None,
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let min_val = if (r.arg_name.clone().as_str() == "min".to_string().as_str()) {
                    Some(r.arg_value.clone())
} else {
                    if (r2.arg_name.clone().as_str() == "min".to_string().as_str()) {
                        Some(r2.arg_value.clone())
} else {
                        None
}
};
let max_val = if (r.arg_name.clone().as_str() == "max".to_string().as_str()) {
                    Some(r.arg_value.clone())
} else {
                    if (r2.arg_name.clone().as_str() == "max".to_string().as_str()) {
                        Some(r2.arg_value.clone())
} else {
                        None
}
};
Rc::new(RangeArgsResult {
    min_val: min_val,
    max_val: max_val,
    state: r2.state.clone(),
    err: None,
})
}
} else {
            {
                let min_val = if (r.arg_name.clone().as_str() == "min".to_string().as_str()) {
                    Some(r.arg_value.clone())
} else {
                    None
};
let max_val = if (r.arg_name.clone().as_str() == "max".to_string().as_str()) {
                    Some(r.arg_value.clone())
} else {
                    None
};
Rc::new(RangeArgsResult {
    min_val: min_val,
    max_val: max_val,
    state: r.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_single_named_int(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<NamedIntResult> {
    {
        let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(NamedIntResult {
    arg_name: "".to_string(),
    arg_value: 0,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
            return Rc::new(NamedIntResult {
    arg_name: name.clone(),
    arg_value: 0,
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_int_literal_value(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return Rc::new(NamedIntResult {
    arg_name: name.clone(),
    arg_value: 0,
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(NamedIntResult {
    arg_name: name.clone(),
    arg_value: r3.value.clone(),
    state: r3.state.clone(),
    err: None,
})
}
}

pub fn parse_variant_fields(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, vname: String) -> Rc<VariantResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if e.consumed.clone() {
            {
                let r = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), e.state.clone()));
if has_err(r.err.clone()) {
                    return Rc::new(VariantResult {
    variant: make_variant_node(vname.clone(), Rc::new(vec![]), start_span.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                    return Rc::new(VariantResult {
    variant: make_variant_node(vname.clone(), Rc::new(vec![]), start_span.clone()),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let v = make_variant_node(vname.clone(), r.fields.clone(), start_span.clone());
Rc::new(VariantResult {
    variant: v,
    state: r2.state.clone(),
    err: None,
})
}
} else {
            {
                let v = make_variant_node(vname.clone(), Rc::new(vec![]), start_span.clone());
Rc::new(VariantResult {
    variant: v,
    state: state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_more_variants(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<VariantsResult> {
    parse_more_variants_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_more_variants_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<VariantsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectPipe));
if e.consumed.clone() {
            let s = skip_newlines(tokens.clone(), e.state.clone());
let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(VariantsResult {
    variants: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_variant_fields(tokens.clone(), r.state.clone(), r.name.clone());
if has_err(r2.err.clone()) {
                return Rc::new(VariantsResult {
    variants: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r2.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r2.variant.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(VariantsResult {
    variants: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}

pub fn parse_type_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TypeResult> {
    {
        let s = skip_newlines(tokens.clone(), state);
let tok = peek(tokens.clone(), s.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShLBrace) => {
            let adv = advance(tokens.clone(), s.clone());
let inline_start = current_span(tokens.clone(), s.clone());
let r = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), adv.state.clone()));
if has_err(r.err.clone()) {
                return Rc::new(TypeResult {
    type_expr: leaf_type_node("".to_string(), inline_start.clone()),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                return Rc::new(TypeResult {
    type_expr: leaf_type_node("".to_string(), inline_start.clone()),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let span = current_span(tokens.clone(), s.clone());
let te = Rc::new(Node {
    name: "".to_string(),
    span: span,
    ident_span: None,
    children: Rc::new({ let mut __result = Vec::new(); for f in r.fields.clone().iter().cloned() { __result.push(field_to_child_node(f.clone())); } __result }),
    connective: Connective::Conj,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(TypeResult {
    type_expr: te,
    state: r2.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShKeyword) => if (tok.clone().unwrap().text.clone().as_str() == "fn".to_string().as_str()) {
            {
                let adv = advance(tokens.clone(), s.clone());
let start_span = current_span(tokens.clone(), s.clone());
parse_callable_type_expr(tokens.clone(), adv.state.clone(), start_span)
}
} else {
            Rc::new(TypeResult {
    type_expr: leaf_type_node("".to_string(), current_span(tokens.clone(), s.clone())),
    state: s.clone(),
    err: Some(parse_error("expected type expression".to_string(), current_span(tokens.clone(), s.clone()))),
})
},
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), s.clone());
let span = current_span(tokens.clone(), s.clone());
finish_type_expr_from_name(tokens.clone(), adv.state.clone(), n, span)
},
    _ => Rc::new(TypeResult {
    type_expr: leaf_type_node("".to_string(), current_span(tokens.clone(), s.clone())),
    state: s.clone(),
    err: Some(parse_error("expected type expression".to_string(), current_span(tokens.clone(), s.clone()))),
}),
}
}
}

pub fn parse_callable_type_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<TypeResult> {
    {
        let dummy_te = leaf_type_node("".to_string(), start_span.clone());
let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLParen));
if has_err(r.err.clone()) {
            return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let params_result = if peek_is_rparen(tokens.clone(), s.clone()) {
            Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: s.clone(),
    err: None,
})
} else {
            parse_callable_param_types(tokens.clone(), s.clone(), Rc::new(vec![]))
};
if has_err(params_result.err.clone()) {
            return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: params_result.state.clone(),
    err: params_result.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), params_result.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRParen));
if has_err(r2.err.clone()) {
            return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectArrow));
if has_err(r3.err.clone()) {
            return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let ret = parse_type_expr(tokens.clone(), r3.state.clone());
if has_err(ret.err.clone()) {
            return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let te = Rc::new(Node {
    name: "Callable".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    connective: Connective::NoConnective,
    params: params_result.params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: ret.type_expr.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
maybe_optional(tokens.clone(), ret.state.clone(), te, start_span.clone())
}
}

pub fn parse_callable_param_types(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ParamsResult> {
    loop {
        let r = parse_type_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let param = make_param_node("".to_string(), r.type_expr.clone(), None, r.type_expr.clone().span.clone());
acc = v2_rt::rc_list_push(acc.clone(), param.clone());
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            let s = skip_newlines(tokens.clone(), e.state.clone());
if peek_is_rparen(tokens.clone(), s.clone()) {
                break Rc::new(ParamsResult {
    params: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
                {
                    let __tco_0 = tokens;
let __tco_1 = s.clone();
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
} else {
            break Rc::new(ParamsResult {
    params: acc.clone(),
    state: r.state.clone(),
    err: None,
});
}
}
}

pub fn finish_type_expr_from_name(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, type_name: String, start_span: Rc<SourceSpan>) -> Rc<TypeResult> {
    {
        let dummy_te = leaf_type_node(type_name.clone(), start_span.clone());
let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLt));
if e.consumed.clone() {
            {
                let r = parse_type_expr(tokens.clone(), e.state.clone());
if has_err(r.err.clone()) {
                    return r.clone()
}
let first_arg = r.type_expr.clone();
let s = r.state.clone();
let type_args = collect_type_args(tokens.clone(), s, Rc::new(vec![first_arg]));
if has_err(type_args.err.clone()) {
                    return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: type_args.state.clone(),
    err: type_args.err.clone(),
})
}
let r3 = expect(tokens.clone(), type_args.state.clone(), Rc::new(ExpectedToken::ExpectGt));
if has_err(r3.err.clone()) {
                    return Rc::new(TypeResult {
    type_expr: dummy_te.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let te = Rc::new(Node {
    name: type_name.clone(),
    span: start_span.clone(),
    ident_span: Some(start_span.clone()),
    children: type_args.args.clone(),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
maybe_optional(tokens.clone(), r3.state.clone(), te, start_span.clone())
}
} else {
            {
                let te = leaf_type_node(type_name.clone(), start_span.clone());
maybe_optional(tokens.clone(), state.clone(), te, start_span.clone())
}
}
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeParamsResult {
    pub params: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
}

pub fn parse_optional_type_params(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TypeParamsResult> {
    {
        let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLt));
if e.consumed.clone() {
            {
                let params_result = collect_type_param_names(tokens.clone(), e.state.clone(), Rc::new(vec![]));
let r = expect(tokens.clone(), params_result.state.clone(), Rc::new(ExpectedToken::ExpectGt));
Rc::new(TypeParamsResult {
    params: params_result.params.clone(),
    state: r.state.clone(),
})
}
} else {
            Rc::new(TypeParamsResult {
    params: Rc::new(vec![]),
    state: state.clone(),
})
}
}
}

pub fn collect_type_param_names(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut params: Rc<Vec<Rc<Node>>>) -> Rc<TypeParamsResult> {
    loop {
        if is_ident(tokens.clone(), state.clone()) {
            let r = expect_ident(tokens.clone(), state.clone());
let span = r.span.clone();
let param = make_param_node(r.name.clone(), leaf_type_node(r.name.clone(), span.clone()), None, span.clone());
let next_params = v2_rt::rc_list_push(params.clone(), param.clone());
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
                {
                    let __tco_0 = tokens;
let __tco_1 = e.state.clone();
let __tco_2 = next_params.clone();
tokens = __tco_0;
state = __tco_1;
params = __tco_2;
continue;
}
} else {
                break Rc::new(TypeParamsResult {
    params: next_params.clone(),
    state: r.state.clone(),
});
}
} else {
            break Rc::new(TypeParamsResult {
    params: params.clone(),
    state: state.clone(),
});
}
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeArgsResult {
    pub args: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

pub fn collect_type_args(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut args: Rc<Vec<Rc<Node>>>) -> Rc<TypeArgsResult> {
    loop {
        let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            let r = parse_type_expr(tokens.clone(), e.state.clone());
if has_err(r.err.clone()) {
                return Rc::new(TypeArgsResult {
    args: args.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(args, r.type_expr.clone());
tokens = __tco_0;
state = __tco_1;
args = __tco_2;
continue;
}
} else {
            break Rc::new(TypeArgsResult {
    args: args.clone(),
    state: state.clone(),
    err: None,
});
}
}
}

pub fn maybe_optional(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, te: Rc<Node>, start_span: Rc<SourceSpan>) -> Rc<TypeResult> {
    {
        let e = eat(tokens, state.clone(), Rc::new(ExpectedToken::ExpectQuestion));
if e.consumed.clone() {
            {
                let ote = Rc::new(Node {
    name: te.name.clone(),
    span: te.span.clone(),
    ident_span: te.ident_span.clone(),
    children: te.children.clone(),
    connective: te.connective.clone(),
    params: te.params.clone(),
    inferred: te.inferred.clone(),
    return_cardinality: Cardinality::CardOptional,
    uses: te.uses.clone(),
    body: te.body.clone(),
    transport: te.transport.clone(),
    properties: te.properties.clone(),
    type_annotation: te.type_annotation.clone(),
    is_self_recursive: te.is_self_recursive.clone(),
    has_non_tail_self_call: te.has_non_tail_self_call.clone(),
    match_pattern: te.match_pattern.clone(),
    expr_data: te.expr_data.clone(),
});
Rc::new(TypeResult {
    type_expr: ote,
    state: e.state.clone(),
    err: None,
})
}
} else {
            Rc::new(TypeResult {
    type_expr: te.clone(),
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_field_list(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<FieldsResult> {
    parse_field_list_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_field_list_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<FieldsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if ((peek_is_rbrace(tokens.clone(), s.clone()) || peek_is_rparen(tokens.clone(), s.clone())) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(FieldsResult {
    fields: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            if (is_ident(tokens.clone(), s.clone()) || is_keyword_name(tokens.clone(), s.clone())) {
                let r = parse_field(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(FieldsResult {
    fields: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = r.state.clone();
let e = eat(tokens.clone(), s2.clone(), Rc::new(ExpectedToken::ExpectComma));
let s3 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                    e.state.clone()
} else {
                    s2.clone()
});
{
                    let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.field.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                break Rc::new(FieldsResult {
    fields: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}
}

pub fn parse_field(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<FieldResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy_field = make_field_node("".to_string(), leaf_type_node("".to_string(), start_span.clone()), Cardinality::Required, None, None, start_span.clone());
let r = expect_name(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(FieldResult {
    field: dummy_field.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
            return Rc::new(FieldResult {
    field: dummy_field.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_type_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return Rc::new(FieldResult {
    field: dummy_field.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let wr = try_where_clause(tokens.clone(), r3.state.clone(), r3.type_expr.clone(), start_span.clone());
if has_err(wr.err.clone()) {
            return Rc::new(FieldResult {
    field: dummy_field.clone(),
    state: wr.state.clone(),
    err: wr.err.clone(),
})
}
let te = wr.type_expr.clone();
let s = wr.state.clone();
let from_r = parse_optional_from_key(tokens.clone(), s.clone());
let from_key = from_r.from_key.clone();
let s = from_r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectEq));
if e.consumed.clone() {
            {
                let r4 = parse_expr(tokens.clone(), e.state.clone());
if has_err(r4.err.clone()) {
                    return Rc::new(FieldResult {
    field: dummy_field.clone(),
    state: r4.state.clone(),
    err: r4.err.clone(),
})
}
let f = make_field_node(name, te.clone(), te.return_cardinality.clone(), Some(r4.expr.clone()), from_key, start_span.clone());
Rc::new(FieldResult {
    field: f,
    state: r4.state.clone(),
    err: None,
})
}
} else {
            {
                let f = make_field_node(name, te.clone(), te.return_cardinality.clone(), None, from_key, start_span.clone());
Rc::new(FieldResult {
    field: f,
    state: s.clone(),
    err: None,
})
}
}
}
}

pub fn parse_optional_from_key(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<FromKeyResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShIdent) => if (tok.clone().unwrap().text.clone().as_str() == "from".to_string().as_str()) {
            {
                let adv = advance(tokens.clone(), state.clone());
let tok2 = peek(tokens.clone(), adv.state.clone());
let sh2 = match tok2.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh2 {
    Some(TokenShape::ShLitStr) => {
                    let key = tok2.clone().unwrap().text.clone();
let adv2 = advance(tokens.clone(), adv.state.clone());
Rc::new(FromKeyResult {
    from_key: Some(key),
    state: adv2.state.clone(),
    err: None,
})
},
    _ => Rc::new(FromKeyResult {
    from_key: None,
    state: state.clone(),
    err: None,
}),
}
}
} else {
            Rc::new(FromKeyResult {
    from_key: None,
    state: state.clone(),
    err: None,
})
},
    _ => Rc::new(FromKeyResult {
    from_key: None,
    state: state.clone(),
    err: None,
}),
}
}
}

pub fn parse_fn_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "fn".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
parse_fn_after_kw(tokens.clone(), r.state.clone(), start_span.clone())
}
}

pub fn parse_fn_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = parse_params(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let params = r.params.clone();
let s = r.state.clone();
let ret = parse_optional_inferred(tokens.clone(), s.clone());
if has_err(ret.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let inferred = ret.inferred.clone();
let s = ret.state.clone();
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), s.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let body = r.expr.clone();
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: params,
    inferred: inferred,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: Some(body),
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r.state.clone()),
    err: None,
})
}
}

pub fn parse_fn_body_from_prefix(tokens: Rc<Vec<Rc<Token>>>, prefix: Rc<ItemPrefixResult>, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let name = prefix.name.clone();
let name_span = prefix.name_span.clone();
let type_params = prefix.type_params.clone();
let params = prefix.params.clone();
let inferred = prefix.inferred.clone();
let all_params = v2_rt::concat(type_params, params);
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: all_params.clone(),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), prefix.state.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let body = r.expr.clone();
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: all_params.clone(),
    inferred: inferred,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: Some(body),
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r.state.clone()),
    err: None,
})
}
}

pub fn parse_func_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let kw = peek_keyword_text(tokens.clone(), state.clone());
let r = if (kw.clone().as_str() == "func".to_string().as_str()) {
            expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "func".to_string(),
}))
} else {
            if (kw.clone().as_str() == "pattern".to_string().as_str()) {
                expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "pattern".to_string(),
}))
} else {
                if (kw.clone().as_str() == "interface".to_string().as_str()) {
                    expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "interface".to_string(),
}))
} else {
                    expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "func".to_string(),
}))
}
}
};
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let has_uses = (kw.clone().as_str() == "func".to_string().as_str());
let form = Rc::new(ItemForm {
    keyword: kw.clone(),
    has_type_params: false,
    has_params: true,
    has_return_type: true,
    return_required: false,
    has_uses: has_uses,
    body_kind: BodyKind::BlockBody,
});
let prefix = parse_item_prefix(tokens.clone(), r.state.clone(), form);
if has_err(prefix.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy.clone(),
    state: prefix.state.clone(),
    err: prefix.err.clone(),
})
}
parse_block_body_from_prefix(tokens.clone(), prefix.clone(), start_span.clone())
}
}

pub fn parse_block_item_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>, form: Rc<ItemForm>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = parse_params(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let params = r.params.clone();
let s = r.state.clone();
let ret = parse_optional_inferred(tokens.clone(), s.clone());
if has_err(ret.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let inferred = ret.inferred.clone();
let s = ret.state.clone();
let uses_r = if form.has_uses.clone() {
            parse_uses_clause(tokens.clone(), skip_newlines(tokens.clone(), s.clone()))
} else {
            Rc::new(UsesResult {
    uses: Rc::new(vec![]),
    state: s.clone(),
    err: None,
})
};
if has_err(uses_r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: uses_r.state.clone(),
    err: uses_r.err.clone(),
})
}
let uses = uses_r.uses.clone();
let s = uses_r.state.clone();
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), s.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let body = r.expr.clone();
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: params,
    inferred: inferred,
    return_cardinality: Cardinality::Required,
    uses: uses,
    body: Some(body),
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r.state.clone()),
    err: None,
})
}
}

pub fn parse_block_body_from_prefix(tokens: Rc<Vec<Rc<Token>>>, prefix: Rc<ItemPrefixResult>, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let name = prefix.name.clone();
let name_span = prefix.name_span.clone();
let params = prefix.params.clone();
let inferred = prefix.inferred.clone();
let uses = prefix.uses.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), prefix.state.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let body = r.expr.clone();
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: params,
    inferred: inferred,
    return_cardinality: Cardinality::Required,
    uses: uses,
    body: Some(body),
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r.state.clone()),
    err: None,
})
}
}

pub fn parse_no_body_from_prefix(prefix: Rc<ItemPrefixResult>, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let item = Rc::new(Node {
    name: prefix.name.clone(),
    span: start_span,
    ident_span: Some(prefix.name_span.clone()),
    children: Rc::new(vec![]),
    params: prefix.params.clone(),
    inferred: prefix.inferred.clone(),
    return_cardinality: Cardinality::Required,
    uses: prefix.uses.clone(),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: prefix.state.clone(),
    err: None,
})
}
}

pub fn parse_uses_clause(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<UsesResult> {
    {
        let s = skip_newlines(tokens.clone(), state);
let tok = peek(tokens.clone(), s.clone());
let is_uses = match tok {
    Some(t) => (is_ident_shape(t.shape.clone()) && (t.text.clone().as_str() == "uses".to_string().as_str())),
    None => false,
};
if is_uses {
            {
                let adv = advance(tokens.clone(), s.clone());
let r = parse_uses_list(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                    return r.clone()
}
Rc::new(UsesResult {
    uses: r.uses.clone(),
    state: r.state.clone(),
    err: None,
})
}
} else {
            Rc::new(UsesResult {
    uses: Rc::new(vec![]),
    state: s.clone(),
    err: None,
})
}
}
}

pub fn parse_uses_list(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<UsesResult> {
    parse_uses_list_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_uses_list_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<UsesResult> {
    loop {
        let r = parse_uses_entry(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(UsesResult {
    uses: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
acc = v2_rt::rc_list_push(acc.clone(), r.resource_use.clone());
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            {
                let __tco_0 = tokens;
let __tco_1 = e.state.clone();
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(UsesResult {
    uses: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}

pub fn parse_uses_entry(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ResUseResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = make_resource_use_node("".to_string(), leaf_type_node("".to_string(), start_span.clone()), start_span.clone());
let r = expect_ident(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ResUseResult {
    resource_use: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
            return Rc::new(ResUseResult {
    resource_use: dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_type_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return Rc::new(ResUseResult {
    resource_use: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectLParen));
if e.consumed.clone() {
            {
                let ar = parse_resource_config_args(tokens.clone(), e.state.clone());
if has_err(ar.err.clone()) {
                    return Rc::new(ResUseResult {
    resource_use: dummy.clone(),
    state: ar.state.clone(),
    err: ar.err.clone(),
})
}
let r4 = expect(tokens.clone(), ar.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r4.err.clone()) {
                    return Rc::new(ResUseResult {
    resource_use: dummy.clone(),
    state: r4.state.clone(),
    err: r4.err.clone(),
})
}
let res_node = Rc::new(Node {
    name: r3.type_expr.clone().name.clone(),
    span: r3.type_expr.clone().span.clone(),
    ident_span: r3.type_expr.clone().ident_span.clone(),
    children: r3.type_expr.clone().children.clone(),
    params: r3.type_expr.clone().params.clone(),
    inferred: r3.type_expr.clone().inferred.clone(),
    return_cardinality: r3.type_expr.clone().return_cardinality.clone(),
    uses: r3.type_expr.clone().uses.clone(),
    body: r3.type_expr.clone().body.clone(),
    connective: r3.type_expr.clone().connective.clone(),
    transport: r3.type_expr.clone().transport.clone(),
    properties: ar.fields.clone(),
    type_annotation: r3.type_expr.clone().type_annotation.clone(),
    is_self_recursive: r3.type_expr.clone().is_self_recursive.clone(),
    has_non_tail_self_call: r3.type_expr.clone().has_non_tail_self_call.clone(),
    match_pattern: r3.type_expr.clone().match_pattern.clone(),
    expr_data: r3.type_expr.clone().expr_data.clone(),
});
let ru = make_resource_use_node(name, res_node, start_span.clone());
Rc::new(ResUseResult {
    resource_use: ru,
    state: r4.state.clone(),
    err: None,
})
}
} else {
            {
                let ru = make_resource_use_node(name, r3.type_expr.clone(), start_span.clone());
Rc::new(ResUseResult {
    resource_use: ru,
    state: r3.state.clone(),
    err: None,
})
}
}
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResConfigResult {
    pub fields: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

pub fn parse_resource_config_args(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ResConfigResult> {
    parse_resource_config_acc(tokens.clone(), skip_newlines(tokens.clone(), state), Rc::new(vec![]))
}

pub fn parse_resource_config_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ResConfigResult> {
    loop {
        if peek_is_rparen(tokens.clone(), state.clone()) {
            return Rc::new(ResConfigResult {
    fields: acc.clone(),
    state: state.clone(),
    err: None,
})
}
let zero_span = make_span(0, 0);
let r = expect_name(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ResConfigResult {
    fields: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let field_name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
            return Rc::new(ResConfigResult {
    fields: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return Rc::new(ResConfigResult {
    fields: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let fi = make_field_init_node(field_name.clone(), r3.expr.clone(), zero_span.clone());
acc = v2_rt::rc_list_push(acc.clone(), fi.clone());
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            {
                let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), e.state.clone());
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(ResConfigResult {
    fields: acc.clone(),
    state: r3.state.clone(),
    err: None,
});
}
}
}

pub fn parse_optional_inferred(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<OptRetResult> {
    {
        let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectArrow));
if e.consumed.clone() {
            {
                let r = parse_type_expr(tokens.clone(), e.state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(OptRetResult {
    inferred: None,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(OptRetResult {
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: r.type_expr.clone(),
})),
    state: r.state.clone(),
    err: None,
})
}
} else {
            Rc::new(OptRetResult {
    inferred: None,
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_service_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "service".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
parse_service_after_kw(tokens.clone(), r.state.clone(), start_span.clone())
}
}

pub fn parse_service_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r_ns = expect_name(tokens.clone(), state);
if has_err(r_ns.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r_ns.state.clone(),
    err: r_ns.err.clone(),
})
}
let namespace_root = r_ns.name.clone();
let name_span = r_ns.span.clone();
let r = parse_dotted_ident_rest(tokens.clone(), r_ns.state.clone(), namespace_root.clone(), name_span.clone());
let name = r.name.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = parse_service_body(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let ns_prop = make_field_init_node("namespace_root".to_string(), make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: namespace_root.clone(),
}),
}), Rc::new(vec![]), None, start_span.clone()), start_span.clone());
let svc_props = match r.config.clone() {
    Some(cfg) => service_config_properties(cfg.endpoint.clone(), cfg.auth.clone(), cfg.rate_limit.clone(), cfg.retry.clone()),
    None => Rc::new(vec![]),
};
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: r.operations.clone(),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: Some(r.transport.clone()),
    properties: v2_rt::concat(Rc::new(vec![ns_prop]), svc_props),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r2.state.clone()),
    err: None,
})
}
}

pub fn parse_service_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ServiceBodyResult> {
    parse_service_entries(tokens.clone(), state.clone(), None, local_transport_node(current_span(tokens.clone(), state.clone())), Rc::new(vec![]))
}

pub fn parse_service_entries(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut config: Option<Rc<ServiceConfig>>, mut transport: Rc<Node>, mut operations: Rc<Vec<Rc<Node>>>) -> Rc<ServiceBodyResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let tok = peek(tokens.clone(), s.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh.clone() {
    Some(TokenShape::ShIdent) => { let id = tok.clone().unwrap().text.clone();
if (id.clone().as_str() == "config".to_string().as_str()) {
                let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_service_config_block(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r2.state.clone());
let r3 = expect(tokens.clone(), s2.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = Some(r2.config.clone());
let __tco_3 = transport;
let __tco_4 = operations;
tokens = __tco_0;
state = __tco_1;
config = __tco_2;
transport = __tco_3;
operations = __tco_4;
continue;
}
} else {
                if (id.clone().as_str() == "transport".to_string().as_str()) {
                    let adv = advance(tokens.clone(), s.clone());
let r = parse_transport_binding(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                        return Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                        let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = config;
let __tco_3 = r.transport.clone();
let __tco_4 = operations;
tokens = __tco_0;
state = __tco_1;
config = __tco_2;
transport = __tco_3;
operations = __tco_4;
continue;
}
} else {
                    break Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: s.clone(),
    err: Some(parse_error("expected config, transport, or operation in service block".to_string(), current_span(tokens.clone(), s.clone()))),
});
}
} },
    Some(TokenShape::ShKeyword) => { let kw_text = tok.clone().unwrap().text.clone();
if (kw_text.clone().as_str() == "operation".to_string().as_str()) {
                let r = parse_operation_def(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = config;
let __tco_3 = transport;
let __tco_4 = v2_rt::rc_list_push(operations, r.operation.clone());
tokens = __tco_0;
state = __tco_1;
config = __tco_2;
transport = __tco_3;
operations = __tco_4;
continue;
}
} else {
                break Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: s.clone(),
    err: Some(parse_error("expected config, transport, or operation in service block".to_string(), current_span(tokens.clone(), s.clone()))),
});
} },
    _ => { break Rc::new(ServiceBodyResult {
    config: config.clone(),
    transport: transport.clone(),
    operations: operations.clone(),
    state: s.clone(),
    err: Some(parse_error("expected config, transport, or operation in service block".to_string(), current_span(tokens.clone(), s.clone()))),
}); },
}
}
}
}

pub fn parse_service_config_block(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ConfigResult> {
    parse_config_fields(tokens, state, None, None, None, None)
}

pub fn parse_config_fields(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut endpoint: Option<Rc<Node>>, mut auth: Option<Rc<Node>>, mut rate_limit: Option<Rc<Node>>, mut retry: Option<Rc<Node>>) -> Rc<ConfigResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            let cfg = Rc::new(ServiceConfig {
    endpoint: match endpoint.clone() {
    Some(e) => e.clone(),
    None => make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: "".to_string(),
}),
}), Rc::new(vec![]), None, make_span(0, 0)),
},
    auth: auth.clone(),
    rate_limit: rate_limit.clone(),
    retry: retry.clone(),
});
break Rc::new(ConfigResult {
    config: cfg.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let dummy_cfg = Rc::new(ServiceConfig {
    endpoint: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: "".to_string(),
}),
}), Rc::new(vec![]), None, make_span(0, 0)),
    auth: None,
    rate_limit: None,
    retry: None,
});
let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ConfigResult {
    config: dummy_cfg.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fname = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                return Rc::new(ConfigResult {
    config: dummy_cfg.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(ConfigResult {
    config: dummy_cfg.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let s2 = r3.state.clone();
let e = eat(tokens.clone(), s2.clone(), Rc::new(ExpectedToken::ExpectComma));
let s3 = if e.consumed.clone() {
                e.state.clone()
} else {
                s2.clone()
};
match fname.clone().as_str() {
    "endpoint" => { {
                let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = Some(r3.expr.clone());
let __tco_3 = auth;
let __tco_4 = rate_limit;
let __tco_5 = retry;
tokens = __tco_0;
state = __tco_1;
endpoint = __tco_2;
auth = __tco_3;
rate_limit = __tco_4;
retry = __tco_5;
continue;
} },
    "auth" => { {
                let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = endpoint;
let __tco_3 = Some(r3.expr.clone());
let __tco_4 = rate_limit;
let __tco_5 = retry;
tokens = __tco_0;
state = __tco_1;
endpoint = __tco_2;
auth = __tco_3;
rate_limit = __tco_4;
retry = __tco_5;
continue;
} },
    "rate_limit" => { {
                let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = endpoint;
let __tco_3 = auth;
let __tco_4 = Some(r3.expr.clone());
let __tco_5 = retry;
tokens = __tco_0;
state = __tco_1;
endpoint = __tco_2;
auth = __tco_3;
rate_limit = __tco_4;
retry = __tco_5;
continue;
} },
    "retry" => { {
                let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = endpoint;
let __tco_3 = auth;
let __tco_4 = rate_limit;
let __tco_5 = Some(r3.expr.clone());
tokens = __tco_0;
state = __tco_1;
endpoint = __tco_2;
auth = __tco_3;
rate_limit = __tco_4;
retry = __tco_5;
continue;
} },
    _ => { {
                let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = endpoint;
let __tco_3 = auth;
let __tco_4 = rate_limit;
let __tco_5 = retry;
tokens = __tco_0;
state = __tco_1;
endpoint = __tco_2;
auth = __tco_3;
rate_limit = __tco_4;
retry = __tco_5;
continue;
} },
}
}
}
}

pub fn parse_transport_binding(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TransportResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy = local_transport_node(span);
let tok = peek(tokens.clone(), state.clone());
let tok_text = match tok.clone() {
    Some(t) => t.text.clone(),
    None => "".to_string(),
};
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShIdent) => if (tok_text.clone().as_str() == "rest".to_string().as_str()) {
            {
                let adv = advance(tokens.clone(), state.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_rest_binding_body(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r3.state.clone(),
    err: None,
})
}
} else {
            if (tok_text.clone().as_str() == "shell".to_string().as_str()) {
                {
                    let adv = advance(tokens.clone(), state.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                        return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_shell_binding_body(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                        return Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                        return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r3.state.clone(),
    err: None,
})
}
} else {
                if (tok_text.clone().as_str() == "file".to_string().as_str()) {
                    {
                        let adv = advance(tokens.clone(), state.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                            return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_file_binding_body(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                            return Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                            return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(TransportResult {
    transport: r2.transport.clone(),
    state: r3.state.clone(),
    err: None,
})
}
} else {
                    Rc::new(TransportResult {
    transport: dummy.clone(),
    state: state.clone(),
    err: None,
})
}
}
},
    _ => Rc::new(TransportResult {
    transport: dummy.clone(),
    state: state.clone(),
    err: None,
}),
}
}
}

pub fn parse_rest_binding_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TransportResult> {
    parse_rest_fields(tokens, state, None)
}

pub fn parse_rest_fields(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut base_url: Option<Rc<Node>>) -> Rc<TransportResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let span = current_span(tokens.clone(), s.clone());
let dummy = local_transport_node(span.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            let bu = match base_url.clone() {
    Some(e) => e.clone(),
    None => make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: "".to_string(),
}),
}), Rc::new(vec![]), None, make_span(0, 0)),
};
break Rc::new(TransportResult {
    transport: rest_transport_node(bu.clone(), Rc::new(vec![]), Rc::new(vec![]), span.clone()),
    state: s.clone(),
    err: None,
});
} else {
            let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fname = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = if e.consumed.clone() {
                e.state.clone()
} else {
                r3.state.clone()
};
match fname.clone() {
    transport_url_key => { {
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = Some(r3.expr.clone());
tokens = __tco_0;
state = __tco_1;
base_url = __tco_2;
continue;
} },
    _ => { {
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = base_url;
tokens = __tco_0;
state = __tco_1;
base_url = __tco_2;
continue;
} },
}
}
}
}

pub fn parse_shell_binding_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TransportResult> {
    parse_shell_fields(tokens, state, Rc::new(vec![]))
}

pub fn parse_shell_fields(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut argv: Rc<Vec<Rc<Node>>>) -> Rc<TransportResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let span = current_span(tokens.clone(), s.clone());
let dummy = local_transport_node(span.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(TransportResult {
    transport: shell_transport_node(argv.clone(), Rc::new(vec![]), span.clone()),
    state: s.clone(),
    err: None,
});
} else {
            let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fname = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
match fname.clone().as_str() {
    "argv" => { let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectLBracket));
if has_err(r3.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let r4 = parse_expr_list_until(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r4.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r4.state.clone(),
    err: r4.err.clone(),
})
}
let r5 = expect(tokens.clone(), r4.state.clone(), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r5.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r5.state.clone(),
    err: r5.err.clone(),
})
}
let e = eat(tokens.clone(), r5.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = if e.consumed.clone() {
                e.state.clone()
} else {
                r5.state.clone()
};
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = r4.exprs.clone();
tokens = __tco_0;
state = __tco_1;
argv = __tco_2;
continue;
} },
    _ => { let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = if e.consumed.clone() {
                e.state.clone()
} else {
                r3.state.clone()
};
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = argv;
tokens = __tco_0;
state = __tco_1;
argv = __tco_2;
continue;
} },
}
}
}
}

pub fn parse_file_binding_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<TransportResult> {
    parse_file_fields(tokens, state, None)
}

pub fn parse_file_fields(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut base_path: Option<Rc<Node>>) -> Rc<TransportResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let span = current_span(tokens.clone(), s.clone());
let dummy = local_transport_node(span.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            let bp = match base_path.clone() {
    Some(e) => e.clone(),
    None => make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: "".to_string(),
}),
}), Rc::new(vec![]), None, make_span(0, 0)),
};
break Rc::new(TransportResult {
    transport: file_transport_node(bp.clone(), span.clone()),
    state: s.clone(),
    err: None,
});
} else {
            let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fname = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(TransportResult {
    transport: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = if e.consumed.clone() {
                e.state.clone()
} else {
                r3.state.clone()
};
if ((fname.clone().as_str() == "path".to_string().as_str()) || (fname.clone().as_str() == transport_path_key().as_str())) {
                {
                    let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = Some(r3.expr.clone());
tokens = __tco_0;
state = __tco_1;
base_path = __tco_2;
continue;
}
} else {
                {
                    let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = base_path;
tokens = __tco_0;
state = __tco_1;
base_path = __tco_2;
continue;
}
}
}
}
}

pub fn parse_operation_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<OpResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy_op = make_operation_node("".to_string(), None, Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), None, start_span.clone());
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "operation".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = expect_ident(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let s = skip_newlines(tokens.clone(), r.state.clone());
if peek_is_lbrace(tokens.clone(), s.clone()) {
            parse_operation_v1_body(tokens.clone(), s.clone(), name, name_span, start_span.clone())
} else {
            parse_operation_v2_inline(tokens.clone(), s.clone(), name, name_span, start_span.clone())
}
}
}

pub fn parse_operation_v2_inline(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String, name_span: Rc<SourceSpan>, start_span: Rc<SourceSpan>) -> Rc<OpResult> {
    {
        let dummy_op = make_operation_node("".to_string(), None, Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), None, start_span.clone());
let mods_r = parse_operation_modifiers(tokens.clone(), state);
let modifiers = mods_r.modifiers.clone();
let mod_props = modifiers_to_props(modifiers, start_span.clone());
let s = mods_r.state.clone();
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLParen));
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let inputs = r.fields.clone();
let r = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = r.state.clone();
let ret = parse_optional_inferred(tokens.clone(), s.clone());
if has_err(ret.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let s = ret.state.clone();
let outputs = match ret.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: rt, .. }) => node_inferred_to_outputs(rt.clone()),
    _ => Rc::new(vec![]),
};
let s = skip_newlines(tokens.clone(), s.clone());
let resp_r = parse_optional_response_block(tokens.clone(), s.clone());
if has_err(resp_r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: resp_r.state.clone(),
    err: resp_r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), resp_r.state.clone());
let mock_r = parse_optional_mock_response_block(tokens.clone(), s.clone());
if has_err(mock_r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: mock_r.state.clone(),
    err: mock_r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), mock_r.state.clone());
let op = make_operation_node(name, Some(name_span), inputs, outputs, resp_r.responses.clone(), mock_r.mocks.clone(), Rc::new(vec![]), mod_props, None, start_span.clone());
Rc::new(OpResult {
    operation: op,
    state: s.clone(),
    err: None,
})
}
}

pub fn parse_operation_v1_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String, name_span: Rc<SourceSpan>, start_span: Rc<SourceSpan>) -> Rc<OpResult> {
    {
        let dummy_op = make_operation_node("".to_string(), None, Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), None, start_span.clone());
let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = parse_op_body_entries(tokens.clone(), s, Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]), None, Rc::new(vec![]), Rc::new(vec![]), Rc::new(vec![]));
if has_err(r2.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
            return Rc::new(OpResult {
    operation: dummy_op.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let op = make_operation_node(name, Some(name_span), r2.inputs.clone(), r2.outputs.clone(), r2.response_props.clone(), r2.mock_props.clone(), r2.exit_props.clone(), r2.modifier_props.clone(), r2.transport.clone(), start_span.clone());
Rc::new(OpResult {
    operation: op,
    state: skip_newlines(tokens.clone(), r3.state.clone()),
    err: None,
})
}
}

pub fn parse_op_body_entries(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut inputs: Rc<Vec<Rc<Node>>>, mut outputs: Rc<Vec<Rc<Node>>>, mut modifier_props: Rc<Vec<Rc<Node>>>, mut transport: Option<Rc<Node>>, mut exit_props: Rc<Vec<Rc<Node>>>, mut response_props: Rc<Vec<Rc<Node>>>, mut mock_props: Rc<Vec<Rc<Node>>>) -> Rc<OpBodyResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let mk_result = Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: s.clone(),
    err: None,
});
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break mk_result.clone();
} else {
            let tok = peek(tokens.clone(), s.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
let err_result = Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: s.clone(),
    err: None,
});
match sh.clone() {
    Some(TokenShape::ShKeyword) => { let kw_text = tok.clone().unwrap().text.clone();
if (kw_text.clone().as_str() == "input".to_string().as_str()) {
                let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = r2.fields.clone();
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                if (kw_text.clone().as_str() == "output".to_string().as_str()) {
                    let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                        let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = inputs;
let __tco_3 = r2.fields.clone();
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                    if (kw_text.clone().as_str() == "idempotent".to_string().as_str()) {
                        let adv = advance(tokens.clone(), s.clone());
let prop = modifier_to_prop("idempotent".to_string(), current_span(tokens.clone(), s.clone()));
{
                            let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = v2_rt::rc_list_push(modifier_props, prop.clone());
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                        if (kw_text.clone().as_str() == "readonly".to_string().as_str()) {
                            let adv = advance(tokens.clone(), s.clone());
let prop = modifier_to_prop("readonly".to_string(), current_span(tokens.clone(), s.clone()));
{
                                let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = v2_rt::rc_list_push(modifier_props, prop.clone());
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                            if (kw_text.clone().as_str() == "hermetic".to_string().as_str()) {
                                let adv = advance(tokens.clone(), s.clone());
let prop = modifier_to_prop("hermetic".to_string(), current_span(tokens.clone(), s.clone()));
{
                                    let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = v2_rt::rc_list_push(modifier_props, prop.clone());
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                                break err_result.clone();
}
}
}
}
} },
    Some(TokenShape::ShIdent) => { let id = tok.clone().unwrap().text.clone();
if (id.clone().as_str() == "transport".to_string().as_str()) {
                let adv = advance(tokens.clone(), s.clone());
let r = parse_transport_binding(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = Some(r.transport.clone());
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                if (id.clone().as_str() == "exit".to_string().as_str()) {
                    let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_exit_entries(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                        return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                        let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = r2.entries.clone();
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                    if (id.clone().as_str() == "response".to_string().as_str()) {
                        let r = parse_optional_response_block(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                            return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                            let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = r.responses.clone();
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                        if (id.clone().as_str() == "mock_response".to_string().as_str()) {
                            let r = parse_optional_mock_response_block(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = r.mocks.clone();
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                            if peek_is_colon_after_ident(tokens.clone(), s.clone()) {
                                let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                                    return Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                                    let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), r3.state.clone());
let __tco_2 = inputs;
let __tco_3 = outputs;
let __tco_4 = modifier_props;
let __tco_5 = transport;
let __tco_6 = exit_props;
let __tco_7 = response_props;
let __tco_8 = mock_props;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
modifier_props = __tco_4;
transport = __tco_5;
exit_props = __tco_6;
response_props = __tco_7;
mock_props = __tco_8;
continue;
}
} else {
                                break Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: s.clone(),
    err: Some(parse_error(format!("unexpected '{}' in operation body", id.clone()), current_span(tokens.clone(), s.clone()))),
});
}
}
}
}
} },
    _ => { break Rc::new(OpBodyResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    modifier_props: modifier_props.clone(),
    transport: transport.clone(),
    exit_props: exit_props.clone(),
    response_props: response_props.clone(),
    mock_props: mock_props.clone(),
    state: s.clone(),
    err: Some(parse_error("unexpected token in operation body".to_string(), current_span(tokens.clone(), s.clone()))),
}); },
}
}
}
}

pub fn modifier_to_prop(name: String, span: Rc<SourceSpan>) -> Rc<Node> {
    make_field_init_node(name, make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitBool {
    value: true,
}),
}), Rc::new(vec![]), None, span.clone()), span.clone())
}

pub fn modifiers_to_props(modifiers: Rc<Vec<OperationModifier>>, span: Rc<SourceSpan>) -> Rc<Vec<Rc<Node>>> {
    Rc::new({ let mut __result = Vec::new(); for m in modifiers.iter().cloned() { __result.push(match m.clone() {
    OperationModifier::Idempotent => modifier_to_prop("idempotent".to_string(), span.clone()),
    OperationModifier::Readonly => modifier_to_prop("readonly".to_string(), span.clone()),
    OperationModifier::Hermetic => modifier_to_prop("hermetic".to_string(), span.clone()),
}); } __result })
}

pub fn status_expr_to_str(expr: Rc<Node>) -> String {
    match (*expr.expr_data.clone()).clone() {
    ExprData::ExprLiteral { value: v, .. } => match (*v.clone()).clone() {
    LiteralValue::LitInt { value: n, .. } => int_to_string(n.clone()),
    LiteralValue::LitStr { value: s, .. } => s.clone(),
    _ => "_".to_string(),
},
    ExprData::ExprVar { .. } => expr_var_name(expr.clone()),
    _ => "_".to_string(),
}
}

pub fn int_to_string(value: i64) -> String {
    if (value.clone() == 0) {
        "0".to_string()
} else {
        {
            let digits = int_to_string_acc(value.clone(), Rc::new(vec![]));
digits.join(&"".to_string())
}
}
}

pub fn int_to_string_acc(mut value: i64, mut acc: Rc<Vec<String>>) -> Rc<Vec<String>> {
    loop {
        if (value.clone() == 0) {
            break acc.clone();
} else {
            let digit = (value.clone() % 10);
let rest = ((value.clone() - digit.clone()) / 10);
let digit_chars = Rc::new(vec!["0".to_string(), "1".to_string(), "2".to_string(), "3".to_string(), "4".to_string(), "5".to_string(), "6".to_string(), "7".to_string(), "8".to_string(), "9".to_string()]);
let ch = match Rc::new({ let mut __result = Vec::new(); for p in Rc::new(digit_chars.clone().iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>()).iter().cloned() { if (p.0.clone() == digit.clone()) { __result.push(p); } } __result }).first().cloned() {
    Some(p) => p.1.clone(),
    None => "?".to_string(),
};
{
                let __tco_0 = rest.clone();
let __tco_1 = v2_rt::rc_list_push(acc, ch.clone());
value = __tco_0;
acc = __tco_1;
continue;
}
}
}
}

pub fn first_child_or_self(n: Rc<Node>) -> Rc<Node> {
    match n.children.clone().first().cloned() {
    Some(ch) => ch.clone(),
    None => n.clone(),
}
}

pub fn last_child_or_self(n: Rc<Node>) -> Rc<Node> {
    match n.children.clone().last().cloned() {
    Some(ch) => ch.clone(),
    None => n.clone(),
}
}

pub fn node_to_name_str(n: Rc<Node>) -> String {
    {
        let is_optional = (n.return_cardinality.clone() == Cardinality::CardOptional);
if is_optional {
            v2_rt::concat("Optional_".to_string(), node_to_name_str_inner(with_required_cardinality(n.clone())))
} else {
            node_to_name_str_inner(n.clone())
}
}
}

pub fn node_to_name_str_inner(n: Rc<Node>) -> String {
    {
        let is_keyed_container = (is_container_type(n.name.clone()) && ((n.children.clone().len() as i64) == 2));
if is_keyed_container {
            match n.children.clone().last().cloned() {
    Some(ch) => v2_rt::concat(v2_rt::concat(n.name.clone(), "_".to_string()), node_to_name_str(ch.clone())),
    None => n.name.clone(),
}
} else {
            if (n.name.clone().as_str() == "Refined".to_string().as_str()) {
                match n.children.clone().first().cloned() {
    Some(ch) => node_to_name_str(ch.clone()),
    None => n.name.clone(),
}
} else {
                if is_container_type(n.name.clone()) {
                    match n.children.clone().first().cloned() {
    Some(ch) => v2_rt::concat("List_".to_string(), node_to_name_str(ch.clone())),
    None => n.name.clone(),
}
} else {
                    if (n.name.clone().as_str() == "".to_string().as_str()) {
                        {
                            let is_conj = (n.connective.clone() == Connective::Conj);
let is_disj = (n.connective.clone() == Connective::Disj);
if is_conj {
                                "Record".to_string()
} else {
                                if is_disj {
                                    "Union".to_string()
} else {
                                    n.name.clone()
}
}
}
} else {
                        n.name.clone()
}
}
}
}
}
}

pub fn parse_exit_entries(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExitEntriesResult> {
    parse_exit_entries_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_exit_entries_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ExitEntriesResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(ExitEntriesResult {
    entries: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_status_pattern(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ExitEntriesResult {
    entries: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let code = r.expr.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectFatArrow));
if has_err(r2.err.clone()) {
                return Rc::new(ExitEntriesResult {
    entries: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_type_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(ExitEntriesResult {
    entries: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let desc_tok = peek(tokens.clone(), r3.state.clone());
let desc_sh = match desc_tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
let desc_r = match desc_sh.clone() {
    Some(TokenShape::ShLitStr) => {
                let d = desc_tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), r3.state.clone());
Rc::new(DescResult {
    desc: Some(d.clone()),
    state: adv.state.clone(),
})
},
    _ => Rc::new(DescResult {
    desc: None,
    state: r3.state.clone(),
}),
};
let code_str = status_expr_to_str(code.clone());
let type_name = node_to_name_str(r3.type_expr.clone());
let prop_name = v2_rt::concat("exit_".to_string(), code_str.clone());
let entry = make_field_init_node(prop_name.clone(), make_named_expr_node(type_name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: None,
}), Rc::new(vec![]), None, r3.type_expr.clone().span.clone()), r3.type_expr.clone().span.clone());
let e = eat(tokens.clone(), desc_r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                desc_r.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, entry.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_operation_modifiers(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ModsResult> {
    parse_operation_modifiers_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_operation_modifiers_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<OperationModifier>>) -> Rc<ModsResult> {
    loop {
        let kw = peek_keyword_text(tokens.clone(), state.clone());
if (kw.clone().as_str() == "idempotent".to_string().as_str()) {
            let adv = advance(tokens.clone(), state.clone());
{
                let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, OperationModifier::Idempotent);
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            if (kw.clone().as_str() == "readonly".to_string().as_str()) {
                let adv = advance(tokens.clone(), state.clone());
{
                    let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, OperationModifier::Readonly);
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                if (kw.clone().as_str() == "hermetic".to_string().as_str()) {
                    let adv = advance(tokens.clone(), state.clone());
{
                        let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, OperationModifier::Hermetic);
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                    break Rc::new(ModsResult {
    modifiers: acc.clone(),
    state: state.clone(),
    err: None,
});
}
}
}
}
}

pub fn parse_status_pattern(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShLitInt) => {
            let n_opt = v2_rt::parse_int(tok.clone().unwrap().text.clone());
let n = match n_opt {
    Some(v) => v.clone(),
    None => return Rc::new(ExprResult {
    expr: make_expr_error_node(ExprErrorKind::InternalExprError, v2_rt::concat(v2_rt::concat("internal: ShLitInt token text not parseable as int: '".to_string(), tok.clone().unwrap().text.clone()), "'".to_string()), current_span(tokens.clone(), state.clone())),
    state: state.clone(),
    err: None,
}),
};
if ((state.pos.clone() + 1) < (tokens.clone().len() as i64)) {
                {
                    let next_tok = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
match next_tok {
    Some(t) => if (is_ident_shape(t.shape.clone()) && (t.text.clone().as_str() == "xx".to_string().as_str())) {
                        {
                            let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
let adv2 = advance(tokens.clone(), adv.state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: format!("{}xx", n),
}),
}), Rc::new(vec![]), None, span),
    state: adv2.state.clone(),
    err: None,
})
}
} else {
                        {
                            let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: n,
}),
}), Rc::new(vec![]), None, span),
    state: adv.state.clone(),
    err: None,
})
}
},
    None => {
                        let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: n,
}),
}), Rc::new(vec![]), None, span),
    state: adv.state.clone(),
    err: None,
})
},
}
}
} else {
                {
                    let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: n,
}),
}), Rc::new(vec![]), None, span),
    state: adv.state.clone(),
    err: None,
})
}
}
},
    Some(TokenShape::ShIdent) => {
            let id = tok.clone().unwrap().text.clone();
let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: id,
}),
}), Rc::new(vec![]), None, span),
    state: adv.state.clone(),
    err: None,
})
},
    _ => {
            let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: "_".to_string(),
}),
}), Rc::new(vec![]), None, span),
    state: adv.state.clone(),
    err: None,
})
},
}
}
}

pub fn parse_optional_response_block(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ResponsesResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let is_response = match tok {
    Some(t) => (is_ident_shape(t.shape.clone()) && (t.text.clone().as_str() == "response".to_string().as_str())),
    None => false,
};
if is_response {
            {
                let adv = advance(tokens.clone(), state.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(ResponsesResult {
    responses: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_response_entries(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(ResponsesResult {
    responses: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(ResponsesResult {
    responses: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(ResponsesResult {
    responses: r2.entries.clone(),
    state: r3.state.clone(),
    err: None,
})
}
} else {
            Rc::new(ResponsesResult {
    responses: Rc::new(vec![]),
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_response_entries(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<RespEntriesResult> {
    parse_response_entries_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_response_entries_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<RespEntriesResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(RespEntriesResult {
    entries: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_status_pattern(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(RespEntriesResult {
    entries: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let status = r.expr.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectFatArrow));
if has_err(r2.err.clone()) {
                return Rc::new(RespEntriesResult {
    entries: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_type_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(RespEntriesResult {
    entries: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let status_str = status_expr_to_str(status.clone());
let type_name = node_to_name_str(r3.type_expr.clone());
let prop_name = v2_rt::concat("response_".to_string(), status_str.clone());
let entry = make_field_init_node(prop_name.clone(), make_named_expr_node(type_name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: None,
}), Rc::new(vec![]), None, r3.type_expr.clone().span.clone()), r3.type_expr.clone().span.clone());
let e = eat(tokens.clone(), r3.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                r3.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, entry.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_optional_mock_response_block(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<MocksResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let is_mock = match tok {
    Some(t) => (is_ident_shape(t.shape.clone()) && (t.text.clone().as_str() == "mock_response".to_string().as_str())),
    None => false,
};
if is_mock {
            {
                let adv = advance(tokens.clone(), state.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(MocksResult {
    mocks: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_mock_response_entries(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(MocksResult {
    mocks: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(MocksResult {
    mocks: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
Rc::new(MocksResult {
    mocks: r2.entries.clone(),
    state: r3.state.clone(),
    err: None,
})
}
} else {
            Rc::new(MocksResult {
    mocks: Rc::new(vec![]),
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_mock_response_entries(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<MockEntriesResult> {
    parse_mock_response_entries_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_mock_response_entries_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<MockEntriesResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(MockEntriesResult {
    entries: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_status_pattern(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(MockEntriesResult {
    entries: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let status = r.expr.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectFatArrow));
if has_err(r2.err.clone()) {
                return Rc::new(MockEntriesResult {
    entries: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                return Rc::new(MockEntriesResult {
    entries: Rc::new(vec![]),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let body = r3.expr.clone();
let desc_tok = peek(tokens.clone(), r3.state.clone());
let desc_sh = match desc_tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
let desc_r = match desc_sh.clone() {
    Some(TokenShape::ShLitStr) => {
                let d = desc_tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), r3.state.clone());
Rc::new(DescResult {
    desc: Some(d.clone()),
    state: adv.state.clone(),
})
},
    _ => Rc::new(DescResult {
    desc: None,
    state: r3.state.clone(),
}),
};
let status_str = status_expr_to_str(status.clone());
let prop_name = v2_rt::concat("mock_".to_string(), status_str.clone());
let entry = make_field_init_node(prop_name.clone(), body.clone(), make_span(0, 0));
let e = eat(tokens.clone(), desc_r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                desc_r.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, entry.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_resource_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "resource".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
parse_resource_after_kw(tokens.clone(), r.state.clone(), start_span.clone())
}
}

pub fn parse_resource_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_resource_entries(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(vec![]), Rc::new(vec![]));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: r.capabilities.clone(),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: r.properties.clone(),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r2.state.clone()),
    err: None,
})
}
}

pub fn parse_resource_entries(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut properties: Rc<Vec<Rc<Node>>>, mut capabilities: Rc<Vec<Rc<Node>>>) -> Rc<ResPropResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let sh = peek_shape(tokens.clone(), s.clone());
match sh.clone() {
    Some(TokenShape::ShKeyword) => { let kw_text = peek_keyword_text(tokens.clone(), s.clone());
if (kw_text.clone().as_str() == "capability".to_string().as_str()) {
                let r = parse_capability(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = properties;
let __tco_3 = v2_rt::rc_list_push(capabilities, r.capability.clone());
tokens = __tco_0;
state = __tco_1;
properties = __tco_2;
capabilities = __tco_3;
continue;
}
} else {
                if (kw_text.clone().as_str() == "acquire".to_string().as_str()) {
                    let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                        return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = skip_until_rbrace(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                        return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                        let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), r3.state.clone());
let __tco_2 = properties;
let __tco_3 = capabilities;
tokens = __tco_0;
state = __tco_1;
properties = __tco_2;
capabilities = __tco_3;
continue;
}
} else {
                    if (kw_text.clone().as_str() == "release".to_string().as_str()) {
                        let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                            return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = skip_until_rbrace(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
let r3 = expect(tokens.clone(), r2.state.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                            return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                            let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), r3.state.clone());
let __tco_2 = properties;
let __tco_3 = capabilities;
tokens = __tco_0;
state = __tco_1;
properties = __tco_2;
capabilities = __tco_3;
continue;
}
} else {
                        break Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: s.clone(),
    err: Some(parse_error("unexpected keyword in resource block".to_string(), current_span(tokens.clone(), s.clone()))),
});
}
}
} },
    Some(TokenShape::ShIdent) => { if peek_is_colon_after_ident(tokens.clone(), s.clone()) {
                let r = expect_ident(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fname = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
                    return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
                    return Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let fi = make_field_init_node(fname.clone(), r3.expr.clone(), make_span(0, 0));
{
                    let __tco_0 = tokens.clone();
let __tco_1 = skip_newlines(tokens.clone(), r3.state.clone());
let __tco_2 = v2_rt::rc_list_push(properties, fi.clone());
let __tco_3 = capabilities;
tokens = __tco_0;
state = __tco_1;
properties = __tco_2;
capabilities = __tco_3;
continue;
}
} else {
                break Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: s.clone(),
    err: Some(parse_error("unexpected token in resource block".to_string(), current_span(tokens.clone(), s.clone()))),
});
} },
    _ => { break Rc::new(ResPropResult {
    properties: properties.clone(),
    capabilities: capabilities.clone(),
    state: s.clone(),
    err: Some(parse_error("expected capability, acquire, release, or property in resource block".to_string(), current_span(tokens.clone(), s.clone()))),
}); },
}
}
}
}

pub fn skip_until_rbrace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<UnitResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let s = skip_newlines(tokens.clone(), state);
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
                Rc::new(UnitResult {
    state: s.clone(),
    err: None,
})
} else {
                {
                    let sh = peek_shape(tokens.clone(), s.clone());
match sh {
    Some(TokenShape::ShLBrace) => {
                        let adv = advance(tokens.clone(), s.clone());
let inner = skip_until_rbrace(tokens.clone(), adv.state.clone());
let r = expect(tokens.clone(), inner.state.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r.err.clone()) {
                            return Rc::new(UnitResult {
    state: r.state.clone(),
    err: r.err.clone(),
})
}
skip_until_rbrace(tokens.clone(), r.state.clone())
},
    _ => {
                        let adv = advance(tokens.clone(), s.clone());
skip_until_rbrace(tokens.clone(), adv.state.clone())
},
}
}
}
}
    })
}

pub fn parse_capability(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<CapResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy_cap = make_capability_node("".to_string(), None, Rc::new(vec![]), Rc::new(vec![]), start_span.clone());
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "capability".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = expect_ident(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let s = r.state.clone();
if peek_is_lbrace(tokens.clone(), s.clone()) {
            {
                let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r2.err.clone()) {
                    return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let io = parse_input_output_blocks(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()));
if has_err(io.err.clone()) {
                    return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: io.state.clone(),
    err: io.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), io.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let cap = make_capability_node(name, Some(name_span), io.inputs.clone(), io.outputs.clone(), start_span.clone());
Rc::new(CapResult {
    capability: cap,
    state: skip_newlines(tokens.clone(), r3.state.clone()),
    err: None,
})
}
} else {
            if peek_is_lparen(tokens.clone(), s.clone()) {
                {
                    let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectLParen));
if has_err(r2.err.clone()) {
                        return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()));
if has_err(r3.err.clone()) {
                        return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let inputs = r3.fields.clone();
let r4 = expect(tokens.clone(), skip_newlines(tokens.clone(), r3.state.clone()), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r4.err.clone()) {
                        return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: r4.state.clone(),
    err: r4.err.clone(),
})
}
let ret = parse_optional_inferred(tokens.clone(), r4.state.clone());
if has_err(ret.err.clone()) {
                        return Rc::new(CapResult {
    capability: dummy_cap.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let outputs = match ret.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: rt, .. }) => node_inferred_to_outputs(rt.clone()),
    _ => Rc::new(vec![]),
};
let cap = make_capability_node(name, Some(name_span), inputs, outputs, start_span.clone());
Rc::new(CapResult {
    capability: cap,
    state: skip_newlines(tokens.clone(), ret.state.clone()),
    err: None,
})
}
} else {
                {
                    let cap = make_capability_node(name, Some(name_span), Rc::new(vec![]), Rc::new(vec![]), start_span.clone());
Rc::new(CapResult {
    capability: cap,
    state: skip_newlines(tokens.clone(), s.clone()),
    err: None,
})
}
}
}
}
}

pub fn parse_input_output_blocks(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<IOResult> {
    parse_io_blocks_acc(tokens, state, Rc::new(vec![]), Rc::new(vec![]))
}

pub fn parse_io_blocks_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut inputs: Rc<Vec<Rc<Node>>>, mut outputs: Rc<Vec<Rc<Node>>>) -> Rc<IOResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let kw = peek_keyword_text(tokens.clone(), s.clone());
if (kw.clone().as_str() == "input".to_string().as_str()) {
                let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                    return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                    return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                    return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = r2.fields.clone();
let __tco_3 = outputs;
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
continue;
}
} else {
                if (kw.clone().as_str() == "output".to_string().as_str()) {
                    let adv = advance(tokens.clone(), s.clone());
let r = expect(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
                        return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = parse_field_list(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r2.err.clone()) {
                        return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = expect(tokens.clone(), skip_newlines(tokens.clone(), r2.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r3.err.clone()) {
                        return Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
{
                        let __tco_0 = tokens;
let __tco_1 = r3.state.clone();
let __tco_2 = inputs;
let __tco_3 = r2.fields.clone();
tokens = __tco_0;
state = __tco_1;
inputs = __tco_2;
outputs = __tco_3;
continue;
}
} else {
                    let result = Rc::new(IOResult {
    inputs: inputs.clone(),
    outputs: outputs.clone(),
    state: s.clone(),
    err: None,
});
break result.clone();
}
}
}
}
}

pub fn parse_data_def(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ItemResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "data".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
parse_data_after_kw(tokens.clone(), r.state.clone(), start_span.clone())
}
}

pub fn parse_data_after_kw(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_span: Rc<SourceSpan>) -> Rc<ItemResult> {
    {
        let dummy = Rc::new(Node {
    name: "".to_string(),
    span: start_span.clone(),
    ident_span: None,
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect_ident(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: dummy,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let named_dummy = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_type_expr(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let te = r.type_expr.clone();
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectEq));
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ItemResult {
    item: named_dummy.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let item = Rc::new(Node {
    name: name.clone(),
    span: start_span.clone(),
    ident_span: Some(name_span.clone()),
    children: Rc::new(vec![]),
    params: Rc::new(vec![]),
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: Some(r.expr.clone()),
    connective: Connective::NoConnective,
    transport: None,
    properties: Rc::new(vec![]),
    type_annotation: Some(te),
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
});
Rc::new(ItemResult {
    item: item,
    state: skip_newlines(tokens.clone(), r.state.clone()),
    err: None,
})
}
}

pub fn parse_params(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ParamsResult> {
    {
        let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLParen));
if has_err(r.err.clone()) {
            return Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
if peek_is_rparen(tokens.clone(), s.clone()) {
            {
                let adv = advance(tokens.clone(), s.clone());
Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: adv.state.clone(),
    err: None,
})
}
} else {
            {
                let r = parse_param_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return r.clone()
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r2.err.clone()) {
                    return Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ParamsResult {
    params: r.params.clone(),
    state: r2.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_param_list(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ParamsResult> {
    parse_param_list_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_param_list_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ParamsResult> {
    loop {
        let r = parse_param(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ParamsResult {
    params: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
acc = v2_rt::rc_list_push(acc.clone(), r.param.clone());
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            let s2 = skip_newlines(tokens.clone(), e.state.clone());
if peek_is_rparen(tokens.clone(), s2.clone()) {
                break Rc::new(ParamsResult {
    params: acc.clone(),
    state: s2.clone(),
    err: None,
});
} else {
                {
                    let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
} else {
            break Rc::new(ParamsResult {
    params: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}

pub fn parse_param(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ParamResult> {
    {
        let start_span = current_span(tokens.clone(), state.clone());
let dummy_param = make_param_node("".to_string(), leaf_type_node("".to_string(), start_span.clone()), None, start_span.clone());
let r = expect_name(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ParamResult {
    param: dummy_param.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectColon));
if has_err(r2.err.clone()) {
            return Rc::new(ParamResult {
    param: dummy_param.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_type_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return Rc::new(ParamResult {
    param: dummy_param.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let wr = try_where_clause(tokens.clone(), r3.state.clone(), r3.type_expr.clone(), start_span.clone());
if has_err(wr.err.clone()) {
            return Rc::new(ParamResult {
    param: dummy_param.clone(),
    state: wr.state.clone(),
    err: wr.err.clone(),
})
}
let type_expr = wr.type_expr.clone();
let s = wr.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectEq));
if e.consumed.clone() {
            {
                let r4 = parse_expr(tokens.clone(), e.state.clone());
if has_err(r4.err.clone()) {
                    return Rc::new(ParamResult {
    param: dummy_param.clone(),
    state: r4.state.clone(),
    err: r4.err.clone(),
})
}
let p = make_param_node(name, type_expr, Some(r4.expr.clone()), start_span.clone());
Rc::new(ParamResult {
    param: p,
    state: r4.state.clone(),
    err: None,
})
}
} else {
            {
                let p = make_param_node(name, type_expr, None, start_span.clone());
Rc::new(ParamResult {
    param: p,
    state: s.clone(),
    err: None,
})
}
}
}
}

pub fn parse_block(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = parse_stmts(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let stmts = r.stmts.clone();
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
if ((stmts.clone().len() as i64) == 1) {
            Rc::new(ExprResult {
    expr: stmts.clone().first().cloned().clone().unwrap(),
    state: r.state.clone(),
    err: None,
})
} else {
            {
                let span = current_span(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), stmts.clone(), None, span),
    state: r.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_stmts(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<StmtsResult> {
    parse_stmts_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_stmts_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<StmtsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if ((peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) || peek_is_eof(tokens.clone(), s.clone())) {
            break Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_stmt(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.expr.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_stmt(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let sh = peek_shape(tokens.clone(), state.clone());
match sh {
    Some(TokenShape::ShKeyword) => {
            let kw_text = peek_keyword_text(tokens.clone(), state.clone());
if (kw_text.clone().as_str() == "let".to_string().as_str()) {
                parse_let(tokens.clone(), state.clone())
} else {
                if (kw_text.clone().as_str() == "return".to_string().as_str()) {
                    parse_return(tokens.clone(), state.clone())
} else {
                    if peek_is_eq_after_ident(tokens.clone(), state.clone()) {
                        parse_bare_assignment(tokens.clone(), state.clone())
} else {
                        parse_expr(tokens.clone(), state.clone())
}
}
}
},
    Some(TokenShape::ShIdent) => if (peek_text_is(tokens.clone(), state.clone(), "node".to_string()) && peek_is_node_decl(tokens.clone(), state.clone())) {
            parse_node_decl(tokens.clone(), state.clone())
} else {
            if peek_is_eq_after_ident(tokens.clone(), state.clone()) {
                parse_bare_assignment(tokens.clone(), state.clone())
} else {
                if is_constraint_bracket_after_ident(tokens.clone(), state.clone()) {
                    parse_constrained_assignment(tokens.clone(), state.clone())
} else {
                    parse_expr(tokens.clone(), state.clone())
}
}
},
    _ => parse_expr(tokens.clone(), state.clone()),
}
}
}

pub fn peek_is_eq_after_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    if ((state.pos.clone() + 1) < (tokens.clone().len() as i64)) {
        {
            let next_tok = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
match next_tok {
    Some(t) => is_eq_shape(t.shape.clone()),
    None => false,
}
}
} else {
        false
}
}

pub fn peek_is_node_decl(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    if ((state.pos.clone() + 2) < (tokens.clone().len() as i64)) {
        {
            let t1 = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
let t2 = tokens.clone().get((state.pos.clone() + 2) as usize).cloned();
let name_ok = match t1 {
    Some(t) => (is_ident_shape(t.shape.clone()) || is_name_keyword(t.clone())),
    None => false,
};
let decl_ok = match t2 {
    Some(t) => ((is_colon_shape(t.shape.clone()) || is_lbracket_shape(t.shape.clone())) || is_eq_shape(t.shape.clone())),
    None => false,
};
(name_ok && decl_ok)
}
} else {
        false
}
}

pub fn is_constraint_bracket_after_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    if ((state.pos.clone() + 1) < (tokens.clone().len() as i64)) {
        {
            let t1 = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
match t1 {
    Some(t) => (is_lbracket_shape(t.shape.clone()) && is_constraint_bracket(tokens.clone(), ParserState {
    pos: (state.pos.clone() + 1),
})),
    None => false,
}
}
} else {
        false
}
}

pub fn parse_constrained_assignment(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect_name(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let cr = try_constraint_annotations(tokens.clone(), r.state.clone());
if has_err(cr.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: cr.state.clone(),
    err: cr.err.clone(),
})
}
let r2 = expect(tokens.clone(), cr.state.clone(), Rc::new(ExpectedToken::ExpectEq));
if has_err(r2.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return r3.clone()
}
let node = make_named_expr_node(name, Rc::new(ExprData::ExprLet), Rc::new(vec![r3.expr.clone()]), None, span);
let node = Rc::new(Node {
    name: node.name.clone(),
    span: node.span.clone(),
    ident_span: node.ident_span.clone(),
    children: node.children.clone(),
    params: node.params.clone(),
    inferred: node.inferred.clone(),
    return_cardinality: node.return_cardinality.clone(),
    uses: node.uses.clone(),
    body: node.body.clone(),
    connective: node.connective.clone(),
    transport: node.transport.clone(),
    properties: cr.constraints.clone(),
    type_annotation: node.type_annotation.clone(),
    is_self_recursive: node.is_self_recursive.clone(),
    has_non_tail_self_call: node.has_non_tail_self_call.clone(),
    match_pattern: node.match_pattern.clone(),
    expr_data: node.expr_data.clone(),
});
Rc::new(ExprResult {
    expr: node.clone(),
    state: r3.state.clone(),
    err: None,
})
}
}

pub fn peek_text_is(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, expected: String) -> bool {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => (is_ident_shape(t.shape.clone()) && (t.text.clone().as_str() == expected.as_str())),
    None => false,
}
}
}

pub fn parse_node_decl(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let adv = advance(tokens.clone(), state.clone());
let r = expect_name(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let name_span = r.span.clone();
let cr = try_constraint_annotations(tokens.clone(), r.state.clone());
if has_err(cr.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: cr.state.clone(),
    err: cr.err.clone(),
})
}
let e_colon = eat(tokens.clone(), cr.state.clone(), Rc::new(ExpectedToken::ExpectColon));
let r2_state = if e_colon.consumed.clone() {
            e_colon.state.clone()
} else {
            {
                let r2 = expect(tokens.clone(), cr.state.clone(), Rc::new(ExpectedToken::ExpectEq));
if has_err(r2.err.clone()) {
                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
r2.state.clone()
}
};
let r3 = parse_expr(tokens.clone(), r2_state);
if has_err(r3.err.clone()) {
            return r3.clone()
}
let ret = parse_optional_inferred(tokens.clone(), r3.state.clone());
if has_err(ret.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: ret.state.clone(),
    err: ret.err.clone(),
})
}
let type_ann = if e_colon.consumed.clone() {
            Some(r3.expr.clone())
} else {
            None
};
let node = Rc::new(Node {
    name: name,
    span: span,
    ident_span: Some(name_span),
    children: Rc::new(vec![r3.expr.clone()]),
    connective: Connective::NoConnective,
    params: Rc::new(vec![]),
    inferred: ret.inferred.clone(),
    return_cardinality: Cardinality::Required,
    uses: Rc::new(vec![]),
    body: None,
    transport: None,
    properties: cr.constraints.clone(),
    type_annotation: type_ann,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::ExprLet),
});
Rc::new(ExprResult {
    expr: node,
    state: ret.state.clone(),
    err: None,
})
}
}

pub fn parse_bare_assignment(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect_name(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectEq));
if has_err(r2.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let r3 = parse_expr(tokens.clone(), r2.state.clone());
if has_err(r3.err.clone()) {
            return r3.clone()
}
let cr = try_constraint_annotations(tokens.clone(), r3.state.clone());
if has_err(cr.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: cr.state.clone(),
    err: cr.err.clone(),
})
}
let node = make_named_expr_node(name, Rc::new(ExprData::ExprLet), Rc::new(vec![r3.expr.clone()]), None, span);
if ((cr.constraints.clone().len() as i64) > 0) {
            {
                let node = Rc::new(Node {
    name: node.name.clone(),
    span: node.span.clone(),
    ident_span: node.ident_span.clone(),
    children: node.children.clone(),
    params: node.params.clone(),
    inferred: node.inferred.clone(),
    return_cardinality: node.return_cardinality.clone(),
    uses: node.uses.clone(),
    body: node.body.clone(),
    connective: node.connective.clone(),
    transport: node.transport.clone(),
    properties: cr.constraints.clone(),
    type_annotation: node.type_annotation.clone(),
    is_self_recursive: node.is_self_recursive.clone(),
    has_non_tail_self_call: node.has_non_tail_self_call.clone(),
    match_pattern: node.match_pattern.clone(),
    expr_data: node.expr_data.clone(),
});
Rc::new(ExprResult {
    expr: node.clone(),
    state: cr.state.clone(),
    err: None,
})
}
} else {
            Rc::new(ExprResult {
    expr: node.clone(),
    state: cr.state.clone(),
    err: None,
})
}
}
}

pub fn parse_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    parse_expr_bp(tokens, state, 0)
}

pub fn parse_expr_bp(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, min_bp: i64) -> Rc<ExprResult> {
    {
        let r = parse_prefix(tokens.clone(), state);
if has_err(r.err.clone()) {
            return r.clone()
}
let lhs = r.expr.clone();
let s = r.state.clone();
parse_expr_loop(tokens.clone(), s, lhs, min_bp)
}
}

pub fn parse_expr_loop(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut lhs: Rc<Node>, mut min_bp: i64) -> Rc<ExprResult> {
    loop {
        if (at_end(tokens.clone(), state.clone()) || peek_is_eof(tokens.clone(), state.clone())) {
            break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: state.clone(),
    err: None,
});
} else {
            let s = skip_continuation_newlines(tokens.clone(), state.clone());
let post = try_postfix(tokens.clone(), s.clone(), lhs.clone(), min_bp.clone());
if has_err(post.err.clone()) {
                break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: post.state.clone(),
    err: post.err.clone(),
});
} else {
                if post.changed.clone() {
                    {
                        let __tco_0 = tokens;
let __tco_1 = post.state.clone();
let __tco_2 = post.expr.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                    let bp = infix_bp(tokens.clone(), s.clone());
match bp.clone() {
    Some(bps) => { if (bps.left.clone() < min_bp.clone()) {
                        break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: s.clone(),
    err: None,
});
} else {
                        let adv = advance(tokens.clone(), s.clone());
let op_shape = adv.token.clone().shape.clone();
if is_dot_shape(op_shape.clone()) {
                            let r = expect_name(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                                return Rc::new(ExprResult {
    expr: lhs.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), state.clone());
let new_lhs = make_named_expr_node(r.name.clone(), Rc::new(ExprData::ExprFieldAccess {
    summary: None,
}), Rc::new(vec![lhs.clone()]), None, span.clone());
{
                                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = new_lhs.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                            if is_pipe_arrow_shape(op_shape.clone()) {
                                let span = current_span(tokens.clone(), state.clone());
let r = parse_pipe_rhs(tokens.clone(), adv.state.clone(), lhs.clone(), span.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = r.expr.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                                let rhs_state = skip_newlines(tokens.clone(), adv.state.clone());
let r = parse_expr_bp(tokens.clone(), rhs_state.clone(), bps.right.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), state.clone());
let binop_opt = find_operator_binop(dag_syntax_spec().operators.clone(), adv.token.clone().text.clone());
match binop_opt.clone() {
    Some(binop) => { let new_lhs = make_expr_node(Rc::new(ExprData::ExprBinOp {
    op: binop.clone(),
}), Rc::new(vec![lhs.clone(), r.expr.clone()]), None, span.clone());
{
                                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = new_lhs.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
} },
    None => { break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: r.state.clone(),
    err: Some(parse_error(v2_rt::concat("unknown binary operator: ".to_string(), adv.token.clone().text.clone()), span.clone())),
}); },
}
}
}
} },
    None => { break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: s.clone(),
    err: None,
}); },
}
}
}
}
}
}

pub fn find_operator_bp(ops: Rc<Vec<Rc<OperatorSpec>>>, symbol: String) -> Option<BindingPower> {
    {
        let matching = Rc::new({ let mut __result = Vec::new(); for op in ops.iter().cloned() { if (op.symbol.clone().as_str() == symbol.clone().as_str()) { __result.push(op); } } __result });
if ((matching.clone().len() as i64) > 0) {
            {
                let op = matching.clone().first().cloned().clone().unwrap();
Some(BindingPower {
    left: op.left_bp.clone(),
    right: op.right_bp.clone(),
})
}
} else {
            None
}
}
}

pub fn infix_bp(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Option<BindingPower> {
    {
        let tok = peek(tokens, state);
match tok {
    Some(t) => find_operator_bp(dag_syntax_spec().operators.clone(), t.text.clone()),
    None => None,
}
}
}

pub fn find_operator_binop(ops: Rc<Vec<Rc<OperatorSpec>>>, symbol: String) -> Option<BinOp> {
    {
        let matching = Rc::new({ let mut __result = Vec::new(); for op in ops.iter().cloned() { if (op.symbol.clone().as_str() == symbol.clone().as_str()) { __result.push(op); } } __result });
if ((matching.clone().len() as i64) > 0) {
            matching.clone().first().cloned().clone().unwrap().binop.clone()
} else {
            None
}
}
}

pub fn parse_pipe_rhs(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, receiver: Rc<Node>, span: Rc<SourceSpan>) -> Rc<ExprResult> {
    {
        let dummy_expr = parse_recovery_placeholder();
let r = expect_name(tokens.clone(), state);
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let method = r.name.clone();
let s = r.state.clone();
let s = skip_newlines(tokens.clone(), s.clone());
if peek_is_lparen(tokens.clone(), s.clone()) {
            {
                let r2 = parse_call_args(tokens.clone(), s.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_named_expr_node(method, Rc::new(ExprData::ExprMethodCall {
    method_semantics: None,
}), v2_rt::concat(Rc::new(vec![receiver]), Rc::new({ let mut __result = Vec::new(); for na in r2.args.clone().iter().cloned() { __result.push(make_arg_node(arg_name(na.clone()), arg_value(na.clone()), span.clone())); } __result })), None, span.clone()),
    state: r2.state.clone(),
    err: None,
})
}
} else {
            Rc::new(ExprResult {
    expr: make_named_expr_node(method, Rc::new(ExprData::ExprMethodCall {
    method_semantics: None,
}), Rc::new(vec![receiver]), None, span.clone()),
    state: s.clone(),
    err: None,
})
}
}
}

pub fn parse_prefix(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let sh = peek_shape(tokens.clone(), state.clone());
match sh {
    Some(TokenShape::ShBang) => {
            let adv = advance(tokens.clone(), state.clone());
let span = current_span(tokens.clone(), state.clone());
let r = parse_expr_bp(tokens.clone(), adv.state.clone(), 12);
if has_err(r.err.clone()) {
                return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprUnaryOp {
    op: UnaryOpKind::Not,
}), Rc::new(vec![r.expr.clone()]), None, span),
    state: r.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShMinus) => {
            let adv = advance(tokens.clone(), state.clone());
let span = current_span(tokens.clone(), state.clone());
let r = parse_expr_bp(tokens.clone(), adv.state.clone(), 12);
if has_err(r.err.clone()) {
                return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprUnaryOp {
    op: UnaryOpKind::Neg,
}), Rc::new(vec![r.expr.clone()]), None, span),
    state: r.state.clone(),
    err: None,
})
},
    _ => parse_primary(tokens.clone(), state.clone()),
}
}
}

pub fn parse_primary(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
let span = current_span(tokens.clone(), state.clone());
match sh.clone() {
    Some(TokenShape::ShKeyword) => {
            let kw_text = tok.clone().unwrap().text.clone();
let lit_val = v2_rt::lookup(&dag_syntax_spec().keyword_literals.clone(), kw_text.clone());
match lit_val {
    Some(lv) => {
                let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: lv.clone(),
}), Rc::new(vec![]), None, span.clone()),
    state: adv.state.clone(),
    err: None,
})
},
    None => if (kw_text.clone().as_str() == "match".to_string().as_str()) {
                parse_match(tokens.clone(), state.clone())
} else {
                if (kw_text.clone().as_str() == "if".to_string().as_str()) {
                    parse_if(tokens.clone(), state.clone())
} else {
                    if (kw_text.clone().as_str() == "for".to_string().as_str()) {
                        parse_for(tokens.clone(), state.clone())
} else {
                        if (kw_text.clone().as_str() == "let".to_string().as_str()) {
                            parse_let(tokens.clone(), state.clone())
} else {
                            if (kw_text.clone().as_str() == "return".to_string().as_str()) {
                                parse_return(tokens.clone(), state.clone())
} else {
                                if (kw_text.clone().as_str() == "fn".to_string().as_str()) {
                                    parse_fn_lambda(tokens.clone(), state.clone())
} else {
                                    {
                                        let kw_name = keyword_to_name(tokens.clone(), state.clone());
match kw_name {
    Some(n) => parse_ident_expr(tokens.clone(), state.clone(), n.clone()),
    None => Rc::new(ExprResult {
    expr: parse_recovery_expr(span.clone(), format!("expected expression, found keyword '{}'", kw_text.clone())),
    state: state.clone(),
    err: Some(parse_error(format!("expected expression, found keyword '{}'", kw_text.clone()), span.clone())),
}),
}
}
}
}
}
}
}
},
}
},
    Some(TokenShape::ShLitInt) => {
            let n_opt = v2_rt::parse_int(tok.clone().unwrap().text.clone());
let n = match n_opt {
    Some(v) => v.clone(),
    None => return Rc::new(ExprResult {
    expr: make_expr_error_node(ExprErrorKind::InternalExprError, v2_rt::concat(v2_rt::concat("internal: ShLitInt token text not parseable as int: '".to_string(), tok.clone().unwrap().text.clone()), "'".to_string()), current_span(tokens.clone(), state.clone())),
    state: state.clone(),
    err: None,
}),
};
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitInt {
    value: n.clone(),
}),
}), Rc::new(vec![]), None, span.clone()),
    state: adv.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShLitFloat) => {
            let f = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitFloat {
    value: f,
}),
}), Rc::new(vec![]), None, span.clone()),
    state: adv.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShLitStr) => {
            let s = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLiteral {
    value: Rc::new(LiteralValue::LitStr {
    value: s,
}),
}), Rc::new(vec![]), None, span.clone()),
    state: adv.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShStrBegin) => parse_string_interp(tokens.clone(), state.clone()),
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
parse_ident_expr(tokens.clone(), state.clone(), n.clone())
},
    Some(TokenShape::ShLParen) => parse_paren_expr(tokens.clone(), state.clone()),
    Some(TokenShape::ShLBracket) => parse_list_literal(tokens.clone(), state.clone()),
    Some(TokenShape::ShLBrace) => parse_brace_expr(tokens.clone(), state.clone()),
    _ => {
            let tag = match sh.clone() {
    Some(shape) => shape_display_name(shape.clone()),
    None => "EOF".to_string(),
};
Rc::new(ExprResult {
    expr: parse_recovery_expr(span.clone(), format!("expected expression, found {}", tag.clone())),
    state: state.clone(),
    err: Some(parse_error(format!("expected expression, found {}", tag.clone()), span.clone())),
})
},
}
}
}

pub fn parse_lambda_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let s = skip_newlines(tokens.clone(), state);
let is_block = (peek_is_keyword(tokens.clone(), s.clone(), "let".to_string()) || peek_is_keyword(tokens.clone(), s.clone(), "return".to_string()));
if is_block {
            {
                let r = parse_lambda_stmts(tokens.clone(), s.clone(), Rc::new(vec![]));
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: parse_recovery_placeholder(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), s.clone());
if ((r.stmts.clone().len() as i64) == 1) {
                    Rc::new(ExprResult {
    expr: r.stmts.clone().first().cloned().clone().unwrap(),
    state: r.state.clone(),
    err: None,
})
} else {
                    Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), r.stmts.clone(), None, span),
    state: r.state.clone(),
    err: None,
})
}
}
} else {
            parse_expr(tokens.clone(), s.clone())
}
}
}

pub fn parse_lambda_stmts(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<StmtsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (((peek_is_rparen(tokens.clone(), s.clone()) || peek_is_rbrace(tokens.clone(), s.clone())) || at_end(tokens.clone(), s.clone())) || peek_is_eof(tokens.clone(), s.clone())) {
            break Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_stmt(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.expr.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_ident_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let adv = advance(tokens.clone(), state.clone());
let s = adv.state.clone();
if peek_is_fat_arrow(tokens.clone(), s.clone()) {
            {
                let adv2 = advance(tokens.clone(), s.clone());
let r = parse_lambda_body(tokens.clone(), adv2.state.clone());
if has_err(r.err.clone()) {
                    return r.clone()
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLambda {
    semantics: None,
}), v2_rt::concat(Rc::new(vec![r.expr.clone()]), Rc::new(vec![parsed_name_leaf(name.clone(), span.clone())])), None, span.clone()),
    state: r.state.clone(),
    err: None,
})
}
} else {
            if (is_uppercase_start(name.clone()) && peek_is_lbrace(tokens.clone(), s.clone())) {
                parse_record_literal(tokens.clone(), s.clone(), name.clone(), span.clone())
} else {
                Rc::new(ExprResult {
    expr: make_named_expr_node(name.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: None,
}), Rc::new(vec![]), None, span.clone()),
    state: s.clone(),
    err: None,
})
}
}
}
}

pub fn is_uppercase_start(name: String) -> bool {
    {
        let ch = v2_rt::char_at(&name, 0);
((ch.clone() >= "A".to_string()) && (ch.clone() <= "Z".to_string()))
}
}

pub fn try_postfix(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, lhs: Rc<Node>, min_bp: i64) -> Rc<PostfixResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
let span = current_span(tokens.clone(), state.clone());
match sh {
    Some(TokenShape::ShLParen) => if (14 < min_bp) {
            Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
} else {
            {
                let r = parse_call_args(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let call_expr = make_call_expr(lhs.clone(), r.args.clone(), span);
Rc::new(PostfixResult {
    expr: call_expr,
    changed: true,
    state: r.state.clone(),
    err: None,
})
}
},
    Some(TokenShape::ShIdent) => if (tok.clone().unwrap().text.clone().as_str() == "as".to_string().as_str()) {
            if (13 < min_bp) {
                Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
} else {
                {
                    let adv = advance(tokens.clone(), state.clone());
let r = parse_type_expr(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                        return Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(PostfixResult {
    expr: make_expr_node(Rc::new(ExprData::ExprCast), Rc::new(vec![lhs.clone(), r.type_expr.clone()]), None, span),
    changed: true,
    state: r.state.clone(),
    err: None,
})
}
}
} else {
            Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
},
    Some(TokenShape::ShLBracket) => if (14 < min_bp) {
            Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
} else {
            if is_constraint_bracket(tokens.clone(), state.clone()) {
                Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
} else {
                {
                    let r = parse_index_or_slice(tokens.clone(), state.clone(), lhs.clone(), span);
if has_err(r.err.clone()) {
                        return Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(PostfixResult {
    expr: r.expr.clone(),
    changed: true,
    state: r.state.clone(),
    err: None,
})
}
}
},
    Some(TokenShape::ShLBrace) => match (*lhs.expr_data.clone()).clone() {
    ExprData::ExprVar { .. } => {
            let n = expr_var_name(lhs.clone());
if (is_uppercase_start(n.clone()) && (14 <= min_bp)) {
                Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
} else {
                if is_uppercase_start(n.clone()) {
                    {
                        let r = parse_record_literal(tokens.clone(), state.clone(), n.clone(), lhs.span.clone());
if has_err(r.err.clone()) {
                            return Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(PostfixResult {
    expr: r.expr.clone(),
    changed: true,
    state: r.state.clone(),
    err: None,
})
}
} else {
                    Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
})
}
}
},
    _ => Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
}),
},
    _ => Rc::new(PostfixResult {
    expr: lhs.clone(),
    changed: false,
    state: state.clone(),
    err: None,
}),
}
}
}

pub fn is_constraint_bracket(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    if ((state.pos.clone() + 1) < (tokens.clone().len() as i64)) {
        {
            let next_tok = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
match next_tok {
    Some(t) => if is_ident_shape(t.shape.clone()) {
                ((t.text.clone().as_str() == "after".to_string().as_str()) || (t.text.clone().as_str() == "when".to_string().as_str()))
} else {
                false
},
    None => false,
}
}
} else {
        false
}
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConstraintsResult {
    pub constraints: Rc<Vec<Rc<Node>>>,
    pub state: ParserState,
    pub err: Option<Rc<ErrorNode>>,
}

pub fn parse_constraint_annotations(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ConstraintsResult> {
    {
        let e = eat(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLBracket));
if e.consumed.clone() {
            {
                let r = parse_constraint_list(tokens.clone(), e.state.clone(), Rc::new(vec![]));
if has_err(r.err.clone()) {
                    return r.clone()
}
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r2.err.clone()) {
                    return Rc::new(ConstraintsResult {
    constraints: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ConstraintsResult {
    constraints: r.constraints.clone(),
    state: r2.state.clone(),
    err: None,
})
}
} else {
            Rc::new(ConstraintsResult {
    constraints: Rc::new(vec![]),
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_constraint_list(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ConstraintsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let tok = peek(tokens.clone(), s.clone());
let is_constraint_kw = match tok.clone() {
    Some(t) => (is_ident_shape(t.shape.clone()) && ((t.text.clone().as_str() == "after".to_string().as_str()) || (t.text.clone().as_str() == "when".to_string().as_str()))),
    None => false,
};
if !is_constraint_kw.clone() {
            return Rc::new(ConstraintsResult {
    constraints: acc.clone(),
    state: s.clone(),
    err: None,
})
}
let kw_name = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), s.clone());
let r = parse_expr(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ConstraintsResult {
    constraints: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fi = make_field_init_node(kw_name.clone(), r.expr.clone(), make_span(0, 0));
acc = v2_rt::rc_list_push(acc.clone(), fi.clone());
let ec = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if ec.consumed.clone() {
            {
                let __tco_0 = tokens;
let __tco_1 = ec.state.clone();
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
            break Rc::new(ConstraintsResult {
    constraints: acc.clone(),
    state: r.state.clone(),
    err: None,
});
}
}
}

pub fn try_constraint_annotations(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ConstraintsResult> {
    if is_constraint_bracket(tokens.clone(), state.clone()) {
        parse_constraint_annotations(tokens.clone(), state.clone())
} else {
        Rc::new(ConstraintsResult {
    constraints: Rc::new(vec![]),
    state: state.clone(),
    err: None,
})
}
}

pub fn make_call_expr(lhs: Rc<Node>, args: Rc<Vec<Rc<Node>>>, span: Rc<SourceSpan>) -> Rc<Node> {
    match (*lhs.expr_data.clone()).clone() {
    ExprData::ExprVar { .. } => make_named_expr_node(expr_var_name(lhs.clone()), Rc::new(ExprData::ExprCall {
    call_semantics: None,
}), args, None, lhs.span.clone()),
    ExprData::ExprFieldAccess { .. } => make_named_expr_node(field_access_field(lhs.clone()), Rc::new(ExprData::ExprMethodCall {
    method_semantics: None,
}), v2_rt::concat(Rc::new(vec![lhs.children.clone().first().cloned().clone().unwrap()]), args), None, lhs.span.clone()),
    _ => make_named_expr_node("<expr>".to_string(), Rc::new(ExprData::ExprCall {
    call_semantics: None,
}), args, None, lhs.span.clone()),
}
}

pub fn parse_index_or_slice(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, base: Rc<Node>, span: Rc<SourceSpan>) -> Rc<ExprResult> {
    {
        let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLBracket));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = r.state.clone();
let r = parse_expr(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let first_expr = r.expr.clone();
let s = r.state.clone();
if peek_is_dot_dot(tokens.clone(), s.clone()) {
            {
                let adv = advance(tokens.clone(), s.clone());
let r = parse_expr(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let end_expr = r.expr.clone();
let s = r.state.clone();
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprSlice), Rc::new(vec![base, first_expr, end_expr]), None, span),
    state: r.state.clone(),
    err: None,
})
}
} else {
            {
                let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIndex), Rc::new(vec![base, first_expr]), None, span),
    state: r.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_call_args(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ArgsResult> {
    {
        let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLParen));
if has_err(r.err.clone()) {
            return Rc::new(ArgsResult {
    args: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
if peek_is_rparen(tokens.clone(), s.clone()) {
            {
                let adv = advance(tokens.clone(), s.clone());
Rc::new(ArgsResult {
    args: Rc::new(vec![]),
    state: adv.state.clone(),
    err: None,
})
}
} else {
            {
                let r = parse_arg_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return r.clone()
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r2.err.clone()) {
                    return Rc::new(ArgsResult {
    args: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ArgsResult {
    args: r.args.clone(),
    state: r2.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_arg_list(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ArgsResult> {
    parse_arg_list_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_arg_list_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ArgsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let r = parse_single_arg(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ArgsResult {
    args: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
acc = v2_rt::rc_list_push(acc.clone(), r.arg.clone());
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
            let s2 = skip_newlines(tokens.clone(), e.state.clone());
if peek_is_rparen(tokens.clone(), s2.clone()) {
                break Rc::new(ArgsResult {
    args: acc.clone(),
    state: s2.clone(),
    err: None,
});
} else {
                {
                    let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = acc;
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
} else {
            break Rc::new(ArgsResult {
    args: acc.clone(),
    state: s.clone(),
    err: None,
});
}
}
}

pub fn parse_single_arg(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ArgResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_arg = make_arg_node(None, parse_recovery_placeholder(), span.clone());
let is_name_token = (is_ident(tokens.clone(), state.clone()) || is_keyword_name(tokens.clone(), state.clone()));
if is_name_token {
            {
                let save_pos = state.pos.clone();
let name_r = expect_name(tokens.clone(), state.clone());
if has_err(name_r.err.clone()) {
                    {
                        let r = parse_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                            return Rc::new(ArgResult {
    arg: dummy_arg,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arg = make_arg_node(None, r.expr.clone(), span.clone());
Rc::new(ArgResult {
    arg: arg,
    state: r.state.clone(),
    err: None,
})
}
} else {
                    if peek_is_colon(tokens.clone(), name_r.state.clone()) {
                        {
                            let adv = advance(tokens.clone(), name_r.state.clone());
let r = parse_expr(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                                return Rc::new(ArgResult {
    arg: dummy_arg,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arg = make_arg_node(Some(name_r.name.clone()), r.expr.clone(), span.clone());
Rc::new(ArgResult {
    arg: arg,
    state: r.state.clone(),
    err: None,
})
}
} else {
                        {
                            let restored = ParserState { pos: save_pos, ..state.clone().clone() };
let r = parse_expr(tokens.clone(), restored);
if has_err(r.err.clone()) {
                                return Rc::new(ArgResult {
    arg: dummy_arg,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arg = make_arg_node(None, r.expr.clone(), span.clone());
Rc::new(ArgResult {
    arg: arg,
    state: r.state.clone(),
    err: None,
})
}
}
}
}
} else {
            {
                let r = parse_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ArgResult {
    arg: dummy_arg,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arg = make_arg_node(None, r.expr.clone(), span.clone());
Rc::new(ArgResult {
    arg: arg,
    state: r.state.clone(),
    err: None,
})
}
}
}
}

pub fn parse_match(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "match".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr_no_brace(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return r.clone()
}
let scrutinee = r.expr.clone();
let r = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_match_arms(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arms = r.arms.clone();
let r = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprMatch), v2_rt::concat(Rc::new(vec![scrutinee]), arms), None, span),
    state: r.state.clone(),
    err: None,
})
}
}

pub fn parse_expr_no_brace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    parse_expr_bp_no_brace(tokens, state, 0)
}

pub fn parse_expr_bp_no_brace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, min_bp: i64) -> Rc<ExprResult> {
    {
        let r = parse_prefix(tokens.clone(), state);
if has_err(r.err.clone()) {
            return r.clone()
}
let lhs = r.expr.clone();
parse_expr_loop_no_brace(tokens.clone(), r.state.clone(), lhs, min_bp)
}
}

pub fn parse_expr_loop_no_brace(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut lhs: Rc<Node>, mut min_bp: i64) -> Rc<ExprResult> {
    loop {
        if ((at_end(tokens.clone(), state.clone()) || peek_is_eof(tokens.clone(), state.clone())) || peek_is_lbrace(tokens.clone(), state.clone())) {
            break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: state.clone(),
    err: None,
});
} else {
            let s = skip_continuation_newlines(tokens.clone(), state.clone());
if (peek_is_lparen(tokens.clone(), s.clone()) && (14 >= min_bp.clone())) {
                let r = parse_call_args(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: lhs.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), s.clone());
let new_lhs = make_call_expr(lhs.clone(), r.args.clone(), span.clone());
{
                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = new_lhs.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                if (peek_is_lbracket(tokens.clone(), s.clone()) && (14 >= min_bp.clone())) {
                    let span = current_span(tokens.clone(), s.clone());
let r = parse_index_or_slice(tokens.clone(), s.clone(), lhs.clone(), span.clone());
if has_err(r.err.clone()) {
                        return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                        let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = r.expr.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                    let bp = infix_bp(tokens.clone(), s.clone());
match bp.clone() {
    Some(bps) => { if (bps.left.clone() < min_bp.clone()) {
                        break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: s.clone(),
    err: None,
});
} else {
                        let adv = advance(tokens.clone(), s.clone());
let op_shape = adv.token.clone().shape.clone();
if is_dot_shape(op_shape.clone()) {
                            let r = expect_name(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                                return Rc::new(ExprResult {
    expr: lhs.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), s.clone());
let new_lhs = make_named_expr_node(r.name.clone(), Rc::new(ExprData::ExprFieldAccess {
    summary: None,
}), Rc::new(vec![lhs.clone()]), None, span.clone());
{
                                let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = new_lhs.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                            if is_pipe_arrow_shape(op_shape.clone()) {
                                let span = current_span(tokens.clone(), s.clone());
let r = parse_pipe_rhs(tokens.clone(), adv.state.clone(), lhs.clone(), span.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = r.expr.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
}
} else {
                                let rhs_state = skip_newlines(tokens.clone(), adv.state.clone());
let r = parse_expr_bp_no_brace(tokens.clone(), rhs_state.clone(), bps.right.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), s.clone());
let binop_opt = find_operator_binop(dag_syntax_spec().operators.clone(), adv.token.clone().text.clone());
match binop_opt.clone() {
    Some(binop) => { let new_lhs = make_expr_node(Rc::new(ExprData::ExprBinOp {
    op: binop.clone(),
}), Rc::new(vec![lhs.clone(), r.expr.clone()]), None, span.clone());
{
                                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = new_lhs.clone();
let __tco_3 = min_bp;
tokens = __tco_0;
state = __tco_1;
lhs = __tco_2;
min_bp = __tco_3;
continue;
} },
    None => { break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: r.state.clone(),
    err: Some(parse_error(v2_rt::concat("unknown binary operator: ".to_string(), adv.token.clone().text.clone()), span.clone())),
}); },
}
}
}
} },
    None => { break Rc::new(ExprResult {
    expr: lhs.clone(),
    state: s.clone(),
    err: None,
}); },
}
}
}
}
}
}

pub fn parse_match_arms(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ArmsResult> {
    parse_match_arms_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_match_arms_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ArmsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(ArmsResult {
    arms: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_match_arm(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ArmsResult {
    arms: acc.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                r.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.arm.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_match_arm(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ArmResult> {
    {
        let dummy_arm = make_arm_node(Rc::new(MatchPattern::Wildcard), None, parse_recovery_placeholder(), make_span(0, 0));
let r = parse_pattern(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ArmResult {
    arm: dummy_arm.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let pat = r.pattern.clone();
let s = r.state.clone();
let guard_r = parse_optional_guard(tokens.clone(), s.clone());
if has_err(guard_r.err.clone()) {
            return Rc::new(ArmResult {
    arm: dummy_arm.clone(),
    state: guard_r.state.clone(),
    err: guard_r.err.clone(),
})
}
let guard = guard_r.guard.clone();
let s = guard_r.state.clone();
let r = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectFatArrow));
if has_err(r.err.clone()) {
            return Rc::new(ArmResult {
    arm: dummy_arm.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = if peek_is_lbrace(tokens.clone(), s.clone()) {
            parse_block(tokens.clone(), s.clone())
} else {
            parse_match_arm_body(tokens.clone(), s.clone())
};
if has_err(r.err.clone()) {
            return Rc::new(ArmResult {
    arm: dummy_arm.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let arm = make_arm_node(pat, guard, r.expr.clone(), current_span(tokens.clone(), state.clone()));
Rc::new(ArmResult {
    arm: arm,
    state: r.state.clone(),
    err: None,
})
}
}

pub fn parse_match_arm_body(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let is_block = (peek_is_keyword(tokens.clone(), state.clone(), "let".to_string()) || peek_is_keyword(tokens.clone(), state.clone(), "return".to_string()));
if is_block {
            {
                let r = parse_match_arm_stmts(tokens.clone(), state.clone(), Rc::new(vec![]));
if has_err(r.err.clone()) {
                    return Rc::new(ExprResult {
    expr: parse_recovery_placeholder(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let span = current_span(tokens.clone(), state.clone());
if ((r.stmts.clone().len() as i64) == 1) {
                    Rc::new(ExprResult {
    expr: r.stmts.clone().first().cloned().clone().unwrap(),
    state: r.state.clone(),
    err: None,
})
} else {
                    Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), r.stmts.clone(), None, span),
    state: r.state.clone(),
    err: None,
})
}
}
} else {
            parse_expr(tokens.clone(), state.clone())
}
}
}

pub fn parse_match_arm_stmts(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<StmtsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if ((peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) || peek_is_eof(tokens.clone(), s.clone())) {
            break Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            if looks_like_arm_start(tokens.clone(), s.clone()) {
                break Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
                let r = parse_stmt(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                    return Rc::new(StmtsResult {
    stmts: acc.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
{
                    let __tco_0 = tokens;
let __tco_1 = r.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.expr.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}
}

pub fn looks_like_arm_start(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
if (n.clone().as_str() == "_".to_string().as_str()) {
                peek_is_fat_arrow_at(tokens.clone(), state.clone(), 1)
} else {
                if is_uppercase_start(n.clone()) {
                    if peek_is_fat_arrow_at(tokens.clone(), state.clone(), 1) {
                        true
} else {
                        if peek_is_expected_at(tokens.clone(), state.clone(), 1, Rc::new(ExpectedToken::ExpectLBrace)) {
                            scan_for_fat_arrow_after_braces(tokens.clone(), state.clone(), 2)
} else {
                            false
}
}
} else {
                    false
}
}
},
    _ => false,
}
}
}

pub fn peek_is_fat_arrow_at(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, offset: i64) -> bool {
    if ((state.pos.clone() + offset.clone()) < (tokens.clone().len() as i64)) {
        {
            let tok = tokens.clone().get((state.pos.clone() + offset.clone()) as usize).cloned();
match tok {
    Some(t) => is_fat_arrow_shape(t.shape.clone()),
    None => false,
}
}
} else {
        false
}
}

pub fn peek_is_expected_at(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, offset: i64, expected: Rc<ExpectedToken>) -> bool {
    if ((state.pos.clone() + offset.clone()) < (tokens.clone().len() as i64)) {
        {
            let tok = tokens.clone().get((state.pos.clone() + offset.clone()) as usize).cloned();
match tok {
    Some(t) => token_matches_expected(t.clone(), expected),
    None => false,
}
}
} else {
        false
}
}

pub fn scan_for_fat_arrow_after_braces(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, start_offset: i64) -> bool {
    scan_braces_depth(tokens, state.clone(), (state.pos.clone() + start_offset), 1)
}

pub fn scan_braces_depth(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut idx: i64, mut depth: i64) -> bool {
    loop {
        if ((idx.clone() >= (tokens.clone().len() as i64)) || (depth.clone() <= 0)) {
            if (depth.clone() == 0) {
                if (idx.clone() < (tokens.clone().len() as i64)) {
                    let tok = tokens.clone().get(idx.clone() as usize).cloned();
match tok.clone() {
    Some(t) => { break is_fat_arrow_shape(t.shape.clone()); },
    None => { break false; },
}
} else {
                    break false;
}
} else {
                break false;
}
} else {
            let tok = tokens.clone().get(idx.clone() as usize).cloned();
match tok.clone() {
    Some(t) => { if is_lbrace_shape(t.shape.clone()) {
                {
                    let __tco_0 = tokens;
let __tco_1 = state;
let __tco_2 = (idx + 1);
let __tco_3 = (depth + 1);
tokens = __tco_0;
state = __tco_1;
idx = __tco_2;
depth = __tco_3;
continue;
}
} else {
                if is_rbrace_shape(t.shape.clone()) {
                    {
                        let __tco_0 = tokens;
let __tco_1 = state;
let __tco_2 = (idx + 1);
let __tco_3 = (depth - 1);
tokens = __tco_0;
state = __tco_1;
idx = __tco_2;
depth = __tco_3;
continue;
}
} else {
                    {
                        let __tco_0 = tokens;
let __tco_1 = state;
let __tco_2 = (idx + 1);
let __tco_3 = depth;
tokens = __tco_0;
state = __tco_1;
idx = __tco_2;
depth = __tco_3;
continue;
}
}
} },
    None => { break false; },
}
}
}
}

pub fn parse_optional_guard(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<GuardResult> {
    if peek_is_keyword(tokens.clone(), state.clone(), "if".to_string()) {
        {
            let adv = advance(tokens.clone(), state.clone());
let r = parse_expr(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                return Rc::new(GuardResult {
    guard: None,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(GuardResult {
    guard: Some(r.expr.clone()),
    state: r.state.clone(),
    err: None,
})
}
} else {
        Rc::new(GuardResult {
    guard: None,
    state: state.clone(),
    err: None,
})
}
}

pub fn parse_pattern(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<PatternResult> {
    {
        let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShIdent) => {
            let n = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
if (n.clone().as_str() == "_".to_string().as_str()) {
                Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: adv.state.clone(),
    err: None,
})
} else {
                if is_uppercase_start(n.clone()) {
                    parse_variant_pattern(tokens.clone(), adv.state.clone(), n.clone())
} else {
                    Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Bind {
    name: n.clone(),
}),
    state: adv.state.clone(),
    err: None,
})
}
}
},
    Some(TokenShape::ShKeyword) => {
            let kw_text = tok.clone().unwrap().text.clone();
let lit_val = v2_rt::lookup(&dag_syntax_spec().keyword_literals.clone(), kw_text);
match lit_val {
    Some(lv) => {
                let adv = advance(tokens.clone(), state.clone());
Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::LitPattern {
    value: lv.clone(),
}),
    state: adv.state.clone(),
    err: None,
})
},
    None => Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: state.clone(),
    err: None,
}),
}
},
    Some(TokenShape::ShLitInt) => {
            let n_opt = v2_rt::parse_int(tok.clone().unwrap().text.clone());
let n = match n_opt {
    Some(v) => v.clone(),
    None => return Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: state.clone(),
    err: Some(parse_error(v2_rt::concat(v2_rt::concat("internal: ShLitInt token text not parseable as int: '".to_string(), tok.clone().unwrap().text.clone()), "'".to_string()), current_span(tokens.clone(), state.clone()))),
}),
};
let adv = advance(tokens.clone(), state.clone());
Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::LitPattern {
    value: Rc::new(LiteralValue::LitInt {
    value: n.clone(),
}),
}),
    state: adv.state.clone(),
    err: None,
})
},
    Some(TokenShape::ShLitStr) => {
            let s = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::LitPattern {
    value: Rc::new(LiteralValue::LitStr {
    value: s,
}),
}),
    state: adv.state.clone(),
    err: None,
})
},
    _ => Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: state.clone(),
    err: None,
}),
}
}
}

pub fn parse_variant_pattern(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String) -> Rc<PatternResult> {
    if peek_is_lbrace(tokens.clone(), state.clone()) {
        {
            let adv = advance(tokens.clone(), state.clone());
let r = parse_variant_bindings_brace(tokens.clone(), skip_newlines(tokens.clone(), adv.state.clone()));
if has_err(r.err.clone()) {
                return Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                return Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::VariantPattern {
    name: name,
    parent_enum: None,
    field_bindings: r.field_bindings.clone(),
}),
    state: r2.state.clone(),
    err: None,
})
}
} else {
        if peek_is_lparen(tokens.clone(), state.clone()) {
            {
                let adv = advance(tokens.clone(), state.clone());
let r = parse_pattern(tokens.clone(), adv.state.clone());
if has_err(r.err.clone()) {
                    return Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r2.err.clone()) {
                    return Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::Wildcard),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let fb = make_field_binding_node("0".to_string(), r.pattern.clone(), current_span(tokens.clone(), state.clone()));
Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::VariantPattern {
    name: name,
    parent_enum: None,
    field_bindings: Rc::new(vec![fb]),
}),
    state: r2.state.clone(),
    err: None,
})
}
} else {
            Rc::new(PatternResult {
    pattern: Rc::new(MatchPattern::VariantPattern {
    name: name,
    parent_enum: None,
    field_bindings: Rc::new(vec![]),
}),
    state: state.clone(),
    err: None,
})
}
}
}

pub fn parse_variant_bindings_brace(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<BindingsResult> {
    parse_variant_bindings_brace_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_variant_bindings_brace_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<BindingsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(BindingsResult {
    field_bindings: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = expect_name(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(BindingsResult {
    field_bindings: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let field_name = r.name.clone();
let s = r.state.clone();
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectColon));
if e.consumed.clone() {
                let r2 = parse_pattern(tokens.clone(), e.state.clone());
if has_err(r2.err.clone()) {
                    return Rc::new(BindingsResult {
    field_bindings: Rc::new(vec![]),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let s2 = r2.state.clone();
let e2 = eat(tokens.clone(), s2.clone(), Rc::new(ExpectedToken::ExpectComma));
let s3 = skip_newlines(tokens.clone(), if e2.consumed.clone() {
                    e2.state.clone()
} else {
                    s2.clone()
});
let fb = make_field_binding_node(field_name.clone(), r2.pattern.clone(), current_span(tokens.clone(), s.clone()));
{
                    let __tco_0 = tokens;
let __tco_1 = s3.clone();
let __tco_2 = v2_rt::rc_list_push(acc, fb.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                let e2 = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e2.consumed.clone() {
                    e2.state.clone()
} else {
                    s.clone()
});
let fb = make_field_binding_node(field_name.clone(), Rc::new(MatchPattern::Bind {
    name: field_name.clone(),
}), current_span(tokens.clone(), s.clone()));
{
                    let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, fb.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}
}

pub fn parse_if(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "if".to_string(),
}));
if has_err(r.err.clone()) {
                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr_no_brace(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let condition = r.expr.clone();
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r.err.clone()) {
                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let then_branch = r.expr.clone();
let s = skip_newlines(tokens.clone(), r.state.clone());
let e = eat(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "else".to_string(),
}));
if e.consumed.clone() {
                {
                    let s = skip_newlines(tokens.clone(), e.state.clone());
if peek_is_keyword(tokens.clone(), s.clone(), "if".to_string()) {
                        {
                            let r2 = parse_if(tokens.clone(), s.clone());
if has_err(r2.err.clone()) {
                                return Rc::new(ExprResult {
    expr: r2.expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIf), Rc::new(vec![condition, then_branch, r2.expr.clone()]), None, span),
    state: r2.state.clone(),
    err: None,
})
}
} else {
                        {
                            let r2 = parse_block(tokens.clone(), s.clone());
if has_err(r2.err.clone()) {
                                return Rc::new(ExprResult {
    expr: r2.expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIf), Rc::new(vec![condition, then_branch, r2.expr.clone()]), None, span),
    state: r2.state.clone(),
    err: None,
})
}
}
}
} else {
                Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprIf), Rc::new(vec![condition, then_branch]), None, span),
    state: s.clone(),
    err: None,
})
}
}
    })
}

pub fn parse_let(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "let".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = expect_name(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let name = r.name.clone();
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectEq));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return r.clone()
}
Rc::new(ExprResult {
    expr: make_named_expr_node(name, Rc::new(ExprData::ExprLet), Rc::new(vec![r.expr.clone()]), None, span),
    state: r.state.clone(),
    err: None,
})
}
}

pub fn parse_return(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "return".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr,
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return r.clone()
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprReturn), Rc::new(vec![r.expr.clone()]), None, span),
    state: r.state.clone(),
    err: None,
})
}
}

pub fn parse_for(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "for".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = expect_name(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let var_name = r.name.clone();
let r = expect(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "in".to_string(),
}));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr_no_brace(tokens.clone(), r.state.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let collection = r.expr.clone();
let r = parse_block(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let body = r.expr.clone();
let for_expr = make_named_expr_node(var_name, Rc::new(ExprData::ExprForEach), Rc::new(vec![collection, body]), None, span);
Rc::new(ExprResult {
    expr: for_expr,
    state: r.state.clone(),
    err: None,
})
}
}

pub fn parse_record_literal(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, name: String, span: Rc<SourceSpan>) -> Rc<ExprResult> {
    {
        let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state, Rc::new(ExpectedToken::ExpectLBrace));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r = parse_field_init_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_named_expr_node(name, Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), r.fields.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
}

pub fn parse_field_init_list(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<FieldInitsResult> {
    parse_field_init_list_acc(tokens, state, Rc::new(vec![]))
}

pub fn parse_field_init_list_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<FieldInitsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if (peek_is_rbrace(tokens.clone(), s.clone()) || at_end(tokens.clone(), s.clone())) {
            break Rc::new(FieldInitsResult {
    fields: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_field_init(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(FieldInitsResult {
    fields: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                r.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = v2_rt::rc_list_push(acc, r.field.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
}
}
}

pub fn parse_field_init(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<FieldInitResult> {
    {
        let zero_span = make_span(0, 0);
let dummy_fi = make_field_init_node("".to_string(), parse_recovery_placeholder(), zero_span);
if (is_ident(tokens.clone(), state.clone()) || is_keyword_name(tokens.clone(), state.clone())) {
            {
                let name_r = expect_name(tokens.clone(), state.clone());
if has_err(name_r.err.clone()) {
                    return Rc::new(FieldInitResult {
    field: dummy_fi.clone(),
    state: name_r.state.clone(),
    err: name_r.err.clone(),
})
}
let n = name_r.name.clone();
if peek_is_colon(tokens.clone(), name_r.state.clone()) {
                    {
                        let adv2 = advance(tokens.clone(), name_r.state.clone());
let r = parse_expr(tokens.clone(), adv2.state.clone());
if has_err(r.err.clone()) {
                            return Rc::new(FieldInitResult {
    field: dummy_fi.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fi = make_field_init_node(n.clone(), r.expr.clone(), current_span(tokens.clone(), state.clone()));
Rc::new(FieldInitResult {
    field: fi,
    state: r.state.clone(),
    err: None,
})
}
} else {
                    {
                        let fi = make_field_init_node(n.clone(), make_named_expr_node(n.clone(), Rc::new(ExprData::ExprVar {
    binding_kind: None,
}), Rc::new(vec![]), None, current_span(tokens.clone(), state.clone())), current_span(tokens.clone(), state.clone()));
Rc::new(FieldInitResult {
    field: fi,
    state: name_r.state.clone(),
    err: None,
})
}
}
}
} else {
            if (peek_is_lit_str(tokens.clone(), state.clone()) && peek_is_colon_after_ident(tokens.clone(), state.clone())) {
                {
                    let str_tok = peek(tokens.clone(), state.clone());
let str_name = match str_tok {
    Some(t) => t.text.clone(),
    None => "_".to_string(),
};
let adv = advance(tokens.clone(), state.clone());
let adv2 = advance(tokens.clone(), adv.state.clone());
let r = parse_expr(tokens.clone(), adv2.state.clone());
if has_err(r.err.clone()) {
                        return Rc::new(FieldInitResult {
    field: dummy_fi.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fi = make_field_init_node(str_name, r.expr.clone(), current_span(tokens.clone(), state.clone()));
Rc::new(FieldInitResult {
    field: fi,
    state: r.state.clone(),
    err: None,
})
}
} else {
                {
                    let r = parse_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
                        return Rc::new(FieldInitResult {
    field: dummy_fi.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let fi = make_field_init_node("_".to_string(), r.expr.clone(), current_span(tokens.clone(), state.clone()));
Rc::new(FieldInitResult {
    field: fi,
    state: r.state.clone(),
    err: None,
})
}
}
}
}
}

pub fn parse_list_literal(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLBracket));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r = parse_expr_list_until(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let r2 = expect(tokens.clone(), skip_newlines(tokens.clone(), r.state.clone()), Rc::new(ExpectedToken::ExpectRBracket));
if has_err(r2.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprListLit), r.exprs.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
}

pub fn parse_expr_list_until(tokens: Rc<Vec<Rc<Token>>>, state: ParserState, end_expected: Rc<ExpectedToken>) -> Rc<ExprsResult> {
    parse_expr_list_until_acc(tokens, state, end_expected, Rc::new(vec![]))
}

pub fn parse_expr_list_until_acc(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut end_expected: Rc<ExpectedToken>, mut acc: Rc<Vec<Rc<Node>>>) -> Rc<ExprsResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
let at_end_tag = match peek(tokens.clone(), s.clone()) {
    Some(t) => token_matches_expected(t.clone(), end_expected.clone()),
    None => false,
};
if (at_end_tag.clone() || at_end(tokens.clone(), s.clone())) {
            break Rc::new(ExprsResult {
    exprs: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let r = parse_expr(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                return Rc::new(ExprsResult {
    exprs: Rc::new(vec![]),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let e = eat(tokens.clone(), r.state.clone(), Rc::new(ExpectedToken::ExpectComma));
let s2 = skip_newlines(tokens.clone(), if e.consumed.clone() {
                e.state.clone()
} else {
                r.state.clone()
});
{
                let __tco_0 = tokens;
let __tco_1 = s2.clone();
let __tco_2 = end_expected;
let __tco_3 = v2_rt::rc_list_push(acc, r.expr.clone());
tokens = __tco_0;
state = __tco_1;
end_expected = __tco_2;
acc = __tco_3;
continue;
}
}
}
}

pub fn parse_paren_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let r = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectLParen));
if has_err(r.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
if peek_is_rparen(tokens.clone(), s.clone()) {
            {
                let adv = advance(tokens.clone(), s.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), Rc::new(vec![]), None, span.clone()),
    state: adv.state.clone(),
    err: None,
})
}
} else {
            {
                let lambda_r = try_lambda_params(tokens.clone(), s.clone());
if lambda_r.is_lambda.clone() {
                    {
                        let r = parse_lambda_body(tokens.clone(), lambda_r.state.clone());
if has_err(r.err.clone()) {
                            return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLambda {
    semantics: None,
}), v2_rt::concat(Rc::new(vec![r.expr.clone()]), Rc::new({ let mut __result = Vec::new(); for p in lambda_r.params.clone().iter().cloned() { __result.push(parsed_name_leaf(p.clone(), span.clone())); } __result })), None, span.clone()),
    state: r.state.clone(),
    err: None,
})
}
} else {
                    {
                        let r = parse_expr(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                            return Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r2.err.clone()) {
                            return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r2.state.clone(),
    err: None,
})
}
}
}
}
}
}

pub fn parse_fn_lambda(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy = parse_recovery_placeholder();
let r1 = expect(tokens.clone(), state.clone(), Rc::new(ExpectedToken::ExpectKeyword {
    text: "fn".to_string(),
}));
if has_err(r1.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy.clone(),
    state: r1.state.clone(),
    err: r1.err.clone(),
})
}
let r2 = expect(tokens.clone(), r1.state.clone(), Rc::new(ExpectedToken::ExpectLParen));
if has_err(r2.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
let params_r = collect_fn_lambda_params(tokens.clone(), r2.state.clone(), Rc::new(vec![]));
let r3 = expect(tokens.clone(), params_r.state.clone(), Rc::new(ExpectedToken::ExpectRParen));
if has_err(r3.err.clone()) {
            return Rc::new(ExprResult {
    expr: dummy.clone(),
    state: r3.state.clone(),
    err: r3.err.clone(),
})
}
let s = skip_newlines(tokens.clone(), r3.state.clone());
let body_r = parse_brace_expr(tokens.clone(), s);
if has_err(body_r.err.clone()) {
            return body_r.clone()
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprLambda {
    semantics: None,
}), v2_rt::concat(Rc::new(vec![body_r.expr.clone()]), Rc::new({ let mut __result = Vec::new(); for p in params_r.params.clone().iter().cloned() { __result.push(parsed_name_leaf(p.clone(), span.clone())); } __result })), None, span.clone()),
    state: body_r.state.clone(),
    err: None,
})
}
}

pub fn collect_fn_lambda_params(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<String>>) -> Rc<IdentCollectResult> {
    loop {
        let s = skip_newlines(tokens.clone(), state.clone());
if peek_is_rparen(tokens.clone(), s.clone()) {
            break Rc::new(IdentCollectResult {
    success: true,
    params: acc.clone(),
    state: s.clone(),
    err: None,
});
} else {
            let name_r = expect_ident(tokens.clone(), s.clone());
if has_err(name_r.err.clone()) {
                break Rc::new(IdentCollectResult {
    success: false,
    params: acc.clone(),
    state: name_r.state.clone(),
    err: name_r.err.clone(),
});
} else {
                let s2 = skip_newlines(tokens.clone(), name_r.state.clone());
if peek_is_comma(tokens.clone(), s2.clone()) {
                    let adv = advance(tokens.clone(), s2.clone());
{
                        let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = v2_rt::rc_list_push(acc, name_r.name.clone());
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                    break Rc::new(IdentCollectResult {
    success: true,
    params: v2_rt::rc_list_push(acc.clone(), name_r.name.clone()),
    state: s2.clone(),
    err: None,
});
}
}
}
}
}

pub fn try_lambda_params(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<LambdaCheckResult> {
    {
        let save_pos = state.pos.clone();
let r = collect_lambda_idents(tokens.clone(), state.clone(), Rc::new(vec![]));
if (r.success.clone() && ((r.params.clone().len() as i64) >= 2)) {
            if peek_is_rparen(tokens.clone(), r.state.clone()) {
                {
                    let adv = advance(tokens.clone(), r.state.clone());
if peek_is_fat_arrow(tokens.clone(), adv.state.clone()) {
                        {
                            let adv2 = advance(tokens.clone(), adv.state.clone());
Rc::new(LambdaCheckResult {
    is_lambda: true,
    params: r.params.clone(),
    state: adv2.state.clone(),
    err: None,
})
}
} else {
                        {
                            let restored = ParserState { pos: save_pos, ..state.clone().clone() };
Rc::new(LambdaCheckResult {
    is_lambda: false,
    params: Rc::new(vec![]),
    state: restored,
    err: None,
})
}
}
}
} else {
                {
                    let restored = ParserState { pos: save_pos, ..state.clone().clone() };
Rc::new(LambdaCheckResult {
    is_lambda: false,
    params: Rc::new(vec![]),
    state: restored,
    err: None,
})
}
}
} else {
            {
                let restored = ParserState { pos: save_pos, ..state.clone().clone() };
Rc::new(LambdaCheckResult {
    is_lambda: false,
    params: Rc::new(vec![]),
    state: restored,
    err: None,
})
}
}
}
}

pub fn collect_lambda_idents(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut acc: Rc<Vec<String>>) -> Rc<IdentCollectResult> {
    loop {
        if is_ident(tokens.clone(), state.clone()) {
            let tok = peek(tokens.clone(), state.clone());
let n = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
let new_acc = v2_rt::rc_list_push(acc.clone(), n.clone());
let e = eat(tokens.clone(), adv.state.clone(), Rc::new(ExpectedToken::ExpectComma));
if e.consumed.clone() {
                {
                    let __tco_0 = tokens;
let __tco_1 = e.state.clone();
let __tco_2 = new_acc.clone();
tokens = __tco_0;
state = __tco_1;
acc = __tco_2;
continue;
}
} else {
                break Rc::new(IdentCollectResult {
    success: true,
    params: new_acc.clone(),
    state: adv.state.clone(),
    err: None,
});
}
} else {
            break Rc::new(IdentCollectResult {
    success: false,
    params: acc.clone(),
    state: state.clone(),
    err: None,
});
}
}
}

pub fn parse_string_interp(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let tok = peek(tokens.clone(), state.clone());
let sh = match tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match sh {
    Some(TokenShape::ShStrBegin) => {
            let prefix = tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), state.clone());
let parts_init = if (v2_rt::string_length(&prefix) > 0) {
                Rc::new(vec![Rc::new(StringPart::Text {
    value: prefix.clone(),
})])
} else {
                Rc::new(vec![])
};
parse_interp_parts(tokens.clone(), adv.state.clone(), parts_init, span.clone())
},
    _ => Rc::new(ExprResult {
    expr: parse_recovery_expr(span.clone(), "expected string interpolation".to_string()),
    state: state.clone(),
    err: Some(parse_error("expected string interpolation".to_string(), span.clone())),
}),
}
}
}

pub fn parse_interp_parts(mut tokens: Rc<Vec<Rc<Token>>>, mut state: ParserState, mut parts: Rc<Vec<Rc<StringPart>>>, mut span: Rc<SourceSpan>) -> Rc<ExprResult> {
    loop {
        let r = parse_expr(tokens.clone(), state.clone());
if has_err(r.err.clone()) {
            return r.clone()
}
let new_parts = v2_rt::rc_list_push(parts.clone(), Rc::new(StringPart::Interpolation {
    expr: r.expr.clone(),
}));
let s = r.state.clone();
let interp_tok = peek(tokens.clone(), s.clone());
let interp_sh = match interp_tok.clone() {
    Some(t) => Some(t.shape.clone()),
    None => None,
};
match interp_sh.clone() {
    Some(TokenShape::ShStrMid) => { let mid = interp_tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), s.clone());
let mid_parts = if (v2_rt::string_length(&mid) > 0) {
            v2_rt::rc_list_push(new_parts.clone(), Rc::new(StringPart::Text {
    value: mid.clone(),
}))
} else {
            new_parts.clone()
};
{
            let __tco_0 = tokens;
let __tco_1 = adv.state.clone();
let __tco_2 = mid_parts.clone();
let __tco_3 = span;
tokens = __tco_0;
state = __tco_1;
parts = __tco_2;
span = __tco_3;
continue;
} },
    Some(TokenShape::ShStrEnd) => { let suffix = interp_tok.clone().unwrap().text.clone();
let adv = advance(tokens.clone(), s.clone());
let final_parts = if (v2_rt::string_length(&suffix) > 0) {
            v2_rt::rc_list_push(new_parts.clone(), Rc::new(StringPart::Text {
    value: suffix.clone(),
}))
} else {
            new_parts.clone()
};
break Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprStringInterp), Rc::new({ let mut __result = Vec::new(); for p in final_parts.clone().iter().cloned() { __result.push(match (*p.clone()).clone() {
    StringPart::Text { value: v, .. } => make_text_part_node(v.clone(), span.clone()),
    StringPart::Interpolation { expr: e, .. } => make_interp_part_node(e.clone(), span.clone()),
}); } __result }), None, span.clone()),
    state: adv.state.clone(),
    err: None,
}); },
    _ => { break Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprStringInterp), Rc::new({ let mut __result = Vec::new(); for p in new_parts.clone().iter().cloned() { __result.push(match (*p.clone()).clone() {
    StringPart::Text { value: v, .. } => make_text_part_node(v.clone(), span.clone()),
    StringPart::Interpolation { expr: e, .. } => make_interp_part_node(e.clone(), span.clone()),
}); } __result }), None, span.clone()),
    state: s.clone(),
    err: None,
}); },
}
}
}

pub fn parse_brace_expr(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> Rc<ExprResult> {
    {
        let span = current_span(tokens.clone(), state.clone());
let dummy_expr = parse_recovery_placeholder();
let adv = advance(tokens.clone(), state.clone());
let s = skip_newlines(tokens.clone(), adv.state.clone());
if peek_is_rbrace(tokens.clone(), s.clone()) {
            {
                let adv2 = advance(tokens.clone(), s.clone());
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), Rc::new(vec![]), None, span),
    state: adv2.state.clone(),
    err: None,
})
}
} else {
            {
                let sh = peek_shape(tokens.clone(), s.clone());
match sh {
    Some(TokenShape::ShKeyword) => {
                    let brace_kw = peek_keyword_text(tokens.clone(), s.clone());
if ((brace_kw.clone().as_str() == "let".to_string().as_str()) || (brace_kw.clone().as_str() == "return".to_string().as_str())) {
                        {
                            let r = parse_stmts(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
if ((r.stmts.clone().len() as i64) == 1) {
                                Rc::new(ExprResult {
    expr: r.stmts.clone().first().cloned().clone().unwrap(),
    state: r2.state.clone(),
    err: None,
})
} else {
                                Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), r.stmts.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
}
} else {
                        {
                            let ident_or_keyword = is_keyword_name(tokens.clone(), s.clone());
if (ident_or_keyword && peek_is_colon_after_ident(tokens.clone(), s.clone())) {
                                {
                                    let r = parse_field_init_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                        return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                        return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), r.fields.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
} else {
                                {
                                    let r = parse_expr(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                        return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                        return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: r.expr.clone(),
    state: r2.state.clone(),
    err: None,
})
}
}
}
}
},
    _ => {
                    let ident_or_keyword = (is_ident(tokens.clone(), s.clone()) || is_keyword_name(tokens.clone(), s.clone()));
if (ident_or_keyword && peek_is_colon_after_ident(tokens.clone(), s.clone())) {
                        {
                            let r = parse_field_init_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), r.fields.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
} else {
                        if (peek_is_lit_str(tokens.clone(), s.clone()) && peek_is_colon_after_ident(tokens.clone(), s.clone())) {
                            {
                                let r = parse_field_init_list(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprRecordLit {
    parent_enum: None,
}), r.fields.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
} else {
                            {
                                let r = parse_stmts(tokens.clone(), s.clone());
if has_err(r.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r.state.clone(),
    err: r.err.clone(),
})
}
let s2 = skip_newlines(tokens.clone(), r.state.clone());
let r2 = expect(tokens.clone(), s2, Rc::new(ExpectedToken::ExpectRBrace));
if has_err(r2.err.clone()) {
                                    return Rc::new(ExprResult {
    expr: dummy_expr.clone(),
    state: r2.state.clone(),
    err: r2.err.clone(),
})
}
if ((r.stmts.clone().len() as i64) == 1) {
                                    Rc::new(ExprResult {
    expr: r.stmts.clone().first().cloned().clone().unwrap(),
    state: r2.state.clone(),
    err: None,
})
} else {
                                    Rc::new(ExprResult {
    expr: make_expr_node(Rc::new(ExprData::ExprBlock), r.stmts.clone(), None, span),
    state: r2.state.clone(),
    err: None,
})
}
}
}
}
},
}
}
}
}
}

pub fn peek_is_colon_after_ident(tokens: Rc<Vec<Rc<Token>>>, state: ParserState) -> bool {
    if ((state.pos.clone() + 1) < (tokens.clone().len() as i64)) {
        {
            let next_tok = tokens.clone().get((state.pos.clone() + 1) as usize).cloned();
match next_tok {
    Some(t) => is_colon_shape(t.shape.clone()),
    None => false,
}
}
} else {
        false
}
}
