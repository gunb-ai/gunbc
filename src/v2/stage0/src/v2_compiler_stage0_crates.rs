// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.stage0_crates

use self::Stage0CrateDep::*;
use self::Stage0CrateKind::*;
use self::Stage0RootShim::*;
use crate::v2_rt;
pub use crate::v2_std_core::TextFile;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use std::collections::BTreeSet;
use std::collections::HashMap;
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum Stage0CrateKind {
    CoreCrate,
    EmitCoreCrate,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum Stage0RootShim {
    NonEmptyVecShim,
    NonEmptyBTreeSetShim,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum Stage0CrateDep {
    VersionDep {
        name: String,
        version: String,
        features: Rc<Vec<String>>,
    },
    PathDep {
        name: String,
        path: String,
    },
}
impl Stage0CrateDep {
    pub fn name(&self) -> String {
        match self {
            Stage0CrateDep::VersionDep { name: __val, .. } => __val.clone(),
            Stage0CrateDep::PathDep { name: __val, .. } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Stage0CrateSpec {
    pub package_name: String,
    pub crate_dir: String,
    pub kind: Stage0CrateKind,
    pub header_doc: String,
    pub modules: Rc<Vec<String>>,
    pub dependencies: Rc<Vec<Rc<Stage0CrateDep>>>,
    pub root_shims: Rc<Vec<Stage0RootShim>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Stage0CratePlan {
    pub crates: Rc<Vec<Rc<Stage0CrateSpec>>>,
}

pub fn stage0_crate_allow_block() -> String {
    Rc::new(vec![
        "#![allow(".to_string(),
        "    unused_imports,".to_string(),
        "    unused_variables,".to_string(),
        "    unused_mut,".to_string(),
        "    unused_parens,".to_string(),
        "    dead_code,".to_string(),
        "    unreachable_patterns,".to_string(),
        "    non_shorthand_field_patterns,".to_string(),
        "    suspicious_double_ref_op,".to_string(),
        "    clippy::all".to_string(),
        ")]".to_string(),
    ])
    .join(&"\n".to_string())
}

pub fn stage0_core_header_doc() -> String {
    Rc::new(vec![
        "//! Generated by regen_stage0 -- do not edit.".to_string(),
        "//!".to_string(),
        "//! Crate boundary for generated v2 stage0 core authorities.".to_string(),
        "//!".to_string(),
        "//! The module bodies remain generated into `src/v2/stage0/src`. This crate".to_string(),
        "//! exposes the lower std/runtime/artifact/language/infer support surface".to_string(),
        "//! without copying generated module bodies. The root `NonEmpty*` wrappers are"
            .to_string(),
        "//! mirrored from the stage0 lib root because generated modules address them as"
            .to_string(),
        "//! `crate::NonEmptyVec` and `crate::NonEmptyBTreeSet`.".to_string(),
    ])
    .join(&"\n".to_string())
}

pub fn stage0_emit_core_header_doc() -> String {
    Rc::new(vec![
        "//! Generated by regen_stage0 -- do not edit.".to_string(),
        "//!".to_string(),
        "//! Crate boundary proof for generated v2 emit core support.".to_string(),
        "//!".to_string(),
        "//! The semantic bodies remain generated from `src/v2/05_emit_core_support.dag`"
            .to_string(),
        "//! into the stage0 seed. This crate exposes that generated support surface".to_string(),
        "//! through `v2-stage0-core`, avoiding copied Rust semantics.".to_string(),
    ])
    .join(&"\n".to_string())
}

pub fn render_stage0_root_shim(shim: Stage0RootShim) -> String {
    match shim {
        Stage0RootShim::NonEmptyVecShim => Rc::new(vec![
            "#[derive(Debug, Clone, PartialEq)]".to_string(),
            "pub struct NonEmptyVec<T>(Vec<T>);".to_string(),
            "".to_string(),
            "impl<T> NonEmptyVec<T> {".to_string(),
            "    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {".to_string(),
            "        if items.is_empty() {".to_string(),
            "            Err(\"NonEmptyVec requires at least one element\")".to_string(),
            "        } else {".to_string(),
            "            Ok(Self(items))".to_string(),
            "        }".to_string(),
            "    }".to_string(),
            "".to_string(),
            "    pub fn as_slice(&self) -> &[T] {".to_string(),
            "        &self.0".to_string(),
            "    }".to_string(),
            "".to_string(),
            "    pub fn into_vec(self) -> Vec<T> {".to_string(),
            "        self.0".to_string(),
            "    }".to_string(),
            "}".to_string(),
        ])
        .join(&"\n".to_string()),
        Stage0RootShim::NonEmptyBTreeSetShim => Rc::new(vec![
            "#[derive(Debug, Clone, PartialEq)]".to_string(),
            "pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);".to_string(),
            "".to_string(),
            "impl<T: Ord> NonEmptyBTreeSet<T> {".to_string(),
            "    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {"
                .to_string(),
            "        if items.is_empty() {".to_string(),
            "            Err(\"NonEmptyBTreeSet requires at least one element\")".to_string(),
            "        } else {".to_string(),
            "            Ok(Self(items))".to_string(),
            "        }".to_string(),
            "    }".to_string(),
            "".to_string(),
            "    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {".to_string(),
            "        &self.0".to_string(),
            "    }".to_string(),
            "".to_string(),
            "    pub fn into_set(self) -> std::collections::BTreeSet<T> {".to_string(),
            "        self.0".to_string(),
            "    }".to_string(),
            "}".to_string(),
        ])
        .join(&"\n".to_string()),
    }
}

pub fn render_stage0_crate_dep(dep: Rc<Stage0CrateDep>) -> String {
    match (*dep).clone() {
        Stage0CrateDep::VersionDep {
            name,
            version,
            features,
            ..
        } => {
            if ((features.clone().len() as i64) > 0) {
                {
                    let feat_strs = Rc::new({
                        let mut __result = Vec::new();
                        for f in features.clone().iter().cloned() {
                            __result.push(v2_rt::concat(
                                v2_rt::concat("\"".to_string(), f.clone()),
                                "\"".to_string(),
                            ));
                        }
                        __result
                    });
                    v2_rt::concat(
                        v2_rt::concat(
                            v2_rt::concat(
                                v2_rt::concat(
                                    v2_rt::concat(name.clone(), " = { version = \"".to_string()),
                                    version.clone(),
                                ),
                                "\", features = [".to_string(),
                            ),
                            feat_strs.join(&", ".to_string()),
                        ),
                        "] }\n".to_string(),
                    )
                }
            } else {
                v2_rt::concat(
                    v2_rt::concat(
                        v2_rt::concat(name.clone(), " = \"".to_string()),
                        version.clone(),
                    ),
                    "\"\n".to_string(),
                )
            }
        }
        Stage0CrateDep::PathDep { name, path, .. } => v2_rt::concat(
            v2_rt::concat(
                v2_rt::concat(name.clone(), " = { path = \"".to_string()),
                path.clone(),
            ),
            "\" }\n".to_string(),
        ),
    }
}

pub fn emit_stage0_crate_manifest(spec: Rc<Stage0CrateSpec>) -> Rc<TextFile> {
    {
        let deps = Rc::new(
            spec.dependencies
                .clone()
                .iter()
                .cloned()
                .map(render_stage0_crate_dep)
                .collect::<Vec<_>>(),
        )
        .join(&"".to_string());
        let content = v2_rt::concat(
            v2_rt::concat(
                v2_rt::concat(
                    "[package]\nname = \"".to_string(),
                    spec.package_name.clone(),
                ),
                "\"\nversion = \"0.1.0\"\nedition = \"2021\"\n\n[dependencies]\n".to_string(),
            ),
            deps,
        );
        Rc::new(TextFile {
            path: v2_rt::concat(spec.crate_dir.clone(), "/Cargo.toml".to_string()),
            content: content,
        })
    }
}

pub fn render_stage0_core_lib(spec: Rc<Stage0CrateSpec>) -> String {
    {
        let includes = Rc::new({
            let mut __result = Vec::new();
            for m in spec.modules.clone().iter().cloned() {
                __result.push(v2_rt::concat(
                    v2_rt::concat(
                        v2_rt::concat(
                            v2_rt::concat("#[path = \"../../stage0/src/".to_string(), m.clone()),
                            ".rs\"]\npub mod ".to_string(),
                        ),
                        m.clone(),
                    ),
                    ";".to_string(),
                ));
            }
            __result
        })
        .join(&"\n".to_string());
        let shims = Rc::new(
            spec.root_shims
                .clone()
                .iter()
                .cloned()
                .map(render_stage0_root_shim)
                .collect::<Vec<_>>(),
        )
        .join(&"\n\n".to_string());
        v2_rt::concat(
            v2_rt::concat(
                v2_rt::concat(
                    v2_rt::concat(
                        v2_rt::concat(
                            v2_rt::concat(
                                v2_rt::concat(spec.header_doc.clone(), "\n\n".to_string()),
                                stage0_crate_allow_block(),
                            ),
                            "\n\n".to_string(),
                        ),
                        includes,
                    ),
                    "\n\n".to_string(),
                ),
                shims,
            ),
            "\n".to_string(),
        )
    }
}

pub fn render_stage0_emit_core_lib(spec: Rc<Stage0CrateSpec>) -> String {
    {
        let reexports = Rc::new({
            let mut __result = Vec::new();
            for m in spec.modules.clone().iter().cloned() {
                __result.push(v2_rt::concat(
                    v2_rt::concat(
                        v2_rt::concat(
                            v2_rt::concat("pub mod ".to_string(), m.clone()),
                            " {\n    pub use v2_stage0_core::".to_string(),
                        ),
                        m.clone(),
                    ),
                    "::*;\n}".to_string(),
                ));
            }
            __result
        })
        .join(&"\n\n".to_string());
        v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat(spec.header_doc.clone(), "\n\n".to_string()), stage0_crate_allow_block()), "\n\n".to_string()), "pub use v2_stage0_core::{NonEmptyBTreeSet, NonEmptyVec};".to_string()), "\n\n".to_string()), reexports), "\n\n".to_string()), "#[path = \"../../stage0/src/v2_compiler_emit_core_support.rs\"]\npub mod v2_compiler_emit_core_support;".to_string()), "\n\n".to_string()), "pub use v2_compiler_emit_core_support::*;".to_string()), "\n".to_string())
    }
}

pub fn emit_stage0_crate_lib(spec: Rc<Stage0CrateSpec>) -> Rc<TextFile> {
    {
        let content = match spec.kind.clone() {
            Stage0CrateKind::CoreCrate => render_stage0_core_lib(spec.clone()),
            Stage0CrateKind::EmitCoreCrate => render_stage0_emit_core_lib(spec.clone()),
        };
        Rc::new(TextFile {
            path: v2_rt::concat(spec.crate_dir.clone(), "/src/lib.rs".to_string()),
            content: content,
        })
    }
}

pub fn stage0_crate_plan() -> Rc<Stage0CratePlan> {
    Rc::new(Stage0CratePlan {
        crates: Rc::new(vec![
            Rc::new(Stage0CrateSpec {
                package_name: "v2-stage0-core".to_string(),
                crate_dir: "src/v2/stage0_core".to_string(),
                kind: Stage0CrateKind::CoreCrate,
                header_doc: stage0_core_header_doc(),
                modules: Rc::new(vec![
                    "extdeps_languages_dag_emit".to_string(),
                    "extdeps_languages_dag_syntax".to_string(),
                    "extdeps_languages_dag_types".to_string(),
                    "extdeps_languages_go_emit".to_string(),
                    "extdeps_languages_go_syntax".to_string(),
                    "extdeps_languages_go_types".to_string(),
                    "extdeps_languages_python_emit".to_string(),
                    "extdeps_languages_python_syntax".to_string(),
                    "extdeps_languages_python_types".to_string(),
                    "extdeps_languages_rust_emit".to_string(),
                    "extdeps_languages_rust_syntax".to_string(),
                    "extdeps_languages_rust_types".to_string(),
                    "std_algebra".to_string(),
                    "std_coercion".to_string(),
                    "std_computation".to_string(),
                    "std_effects".to_string(),
                    "std_error_primitives".to_string(),
                    "std_graph".to_string(),
                    "std_http_path".to_string(),
                    "std_induction".to_string(),
                    "std_iteration".to_string(),
                    "std_node".to_string(),
                    "std_syntax".to_string(),
                    "std_termination".to_string(),
                    "std_types".to_string(),
                    "v2_compiler_artifact".to_string(),
                    "v2_compiler_infer_emit_info".to_string(),
                    "v2_compiler_infer_env".to_string(),
                    "v2_compiler_infer_items".to_string(),
                    "v2_compiler_infer_service".to_string(),
                    "v2_compiler_infer_sigs".to_string(),
                    "v2_compiler_infer_types".to_string(),
                    "v2_compiler_languages".to_string(),
                    "v2_rt".to_string(),
                    "v2_std_core".to_string(),
                ]),
                dependencies: Rc::new(vec![
                    Rc::new(Stage0CrateDep::VersionDep {
                        name: "stacker".to_string(),
                        version: "0.1".to_string(),
                        features: Rc::new(vec![]),
                    }),
                    Rc::new(Stage0CrateDep::VersionDep {
                        name: "serde".to_string(),
                        version: "1".to_string(),
                        features: Rc::new(vec!["derive".to_string(), "rc".to_string()]),
                    }),
                    Rc::new(Stage0CrateDep::VersionDep {
                        name: "serde_json".to_string(),
                        version: "1".to_string(),
                        features: Rc::new(vec![]),
                    }),
                ]),
                root_shims: Rc::new(vec![
                    Stage0RootShim::NonEmptyVecShim,
                    Stage0RootShim::NonEmptyBTreeSetShim,
                ]),
            }),
            Rc::new(Stage0CrateSpec {
                package_name: "v2-stage0-emit-core".to_string(),
                crate_dir: "src/v2/stage0_emit_core".to_string(),
                kind: Stage0CrateKind::EmitCoreCrate,
                header_doc: stage0_emit_core_header_doc(),
                modules: Rc::new(vec![
                    "v2_compiler_artifact".to_string(),
                    "v2_compiler_infer_items".to_string(),
                    "v2_compiler_infer_service".to_string(),
                    "v2_compiler_infer_types".to_string(),
                    "v2_compiler_languages".to_string(),
                    "v2_rt".to_string(),
                    "v2_std_core".to_string(),
                ]),
                dependencies: Rc::new(vec![
                    Rc::new(Stage0CrateDep::VersionDep {
                        name: "stacker".to_string(),
                        version: "0.1".to_string(),
                        features: Rc::new(vec![]),
                    }),
                    Rc::new(Stage0CrateDep::VersionDep {
                        name: "serde".to_string(),
                        version: "1".to_string(),
                        features: Rc::new(vec!["derive".to_string(), "rc".to_string()]),
                    }),
                    Rc::new(Stage0CrateDep::PathDep {
                        name: "v2-stage0-core".to_string(),
                        path: "../stage0_core".to_string(),
                    }),
                ]),
                root_shims: Rc::new(vec![]),
            }),
        ]),
    })
}

pub fn emit_stage0_crate_boundary_files(plan: Rc<Stage0CratePlan>) -> Rc<Vec<Rc<TextFile>>> {
    Rc::new({
        let mut __result = Vec::new();
        for spec in plan.crates.clone().iter().cloned() {
            __result.extend(
                (*Rc::new(vec![
                    emit_stage0_crate_manifest(spec.clone()),
                    emit_stage0_crate_lib(spec.clone()),
                ]))
                .iter()
                .cloned(),
            );
        }
        __result
    })
}

pub fn stage0_crate_boundary_files() -> Rc<Vec<Rc<TextFile>>> {
    emit_stage0_crate_boundary_files(stage0_crate_plan())
}
