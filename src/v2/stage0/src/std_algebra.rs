// Generated by v2 compiler -- do not edit.
// Source module: std.algebra

use std::collections::BTreeMap;
use std::rc::Rc;
use serde::{Serialize, Deserialize};
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}

impl<'de, T> Deserialize<'de> for NonEmptyVec<T>
where
    T: Deserialize<'de>,
{
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let items = Vec::<T>::deserialize(deserializer)?;
        NonEmptyVec::new(items).map_err(serde::de::Error::custom)
    }
}

#[derive(Debug, Clone, PartialEq, Serialize)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}

impl<'de, T> Deserialize<'de> for NonEmptyBTreeSet<T>
where
    T: Ord + Deserialize<'de>,
{
    fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        let items = std::collections::BTreeSet::<T>::deserialize(deserializer)?;
        NonEmptyBTreeSet::new(items).map_err(serde::de::Error::custom)
    }
}
use Ordering::*;

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Magma {
    pub op: Rc<dyn Fn(T, T) -> T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Semigroup {
    pub op: Rc<dyn Fn(T, T) -> T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Monoid {
    pub op: Rc<dyn Fn(T, T) -> T>,
    pub identity: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CommutativeMonoid {
    pub op: Rc<dyn Fn(T, T) -> T>,
    pub identity: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Group {
    pub op: Rc<dyn Fn(T, T) -> T>,
    pub identity: Box<T>,
    pub inverse: Rc<dyn Fn(T) -> T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct AbelianGroup {
    pub op: Rc<dyn Fn(T, T) -> T>,
    pub identity: Box<T>,
    pub inverse: Rc<dyn Fn(T) -> T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Semiring {
    pub add: Rc<dyn Fn(T, T) -> T>,
    pub zero: Box<T>,
    pub mul: Rc<dyn Fn(T, T) -> T>,
    pub one: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Ring {
    pub add: Rc<dyn Fn(T, T) -> T>,
    pub zero: Box<T>,
    pub negate: Rc<dyn Fn(T) -> T>,
    pub mul: Rc<dyn Fn(T, T) -> T>,
    pub one: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct OrderedRing {
    pub add: Rc<dyn Fn(T, T) -> T>,
    pub zero: Box<T>,
    pub negate: Rc<dyn Fn(T) -> T>,
    pub mul: Rc<dyn Fn(T, T) -> T>,
    pub one: Box<T>,
    pub compare: Rc<dyn Fn(T, T) -> Ordering>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Field {
    pub add: Rc<dyn Fn(T, T) -> T>,
    pub zero: Box<T>,
    pub negate: Rc<dyn Fn(T) -> T>,
    pub mul: Rc<dyn Fn(T, T) -> T>,
    pub one: Box<T>,
    pub reciprocal: Rc<dyn Fn(T) -> T>,
    pub compare: Rc<dyn Fn(T, T) -> Ordering>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Lattice {
    pub meet: Rc<dyn Fn(T, T) -> T>,
    pub join: Rc<dyn Fn(T, T) -> T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct BoundedLattice {
    pub meet: Rc<dyn Fn(T, T) -> T>,
    pub join: Rc<dyn Fn(T, T) -> T>,
    pub top: Box<T>,
    pub bottom: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct BooleanAlgebra {
    pub meet: Rc<dyn Fn(T, T) -> T>,
    pub join: Rc<dyn Fn(T, T) -> T>,
    pub complement: Rc<dyn Fn(T) -> T>,
    pub top: Box<T>,
    pub bottom: Box<T>,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct FreeMonoid {
    pub concat: Rc<dyn Fn(free_monoid<T>, free_monoid<T>) -> free_monoid<T>>,
    pub empty: Rc<FreeMonoid>,
    pub index: Rc<dyn Fn(i64) -> T>,
    pub slice: Rc<dyn Fn(i64, i64) -> free_monoid<T>>,
    pub length: Callable,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct PartialFunction {
    pub lookup: Rc<dyn Fn(K) -> Option<V>>,
    pub empty: Rc<PartialFunction>,
}

#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]
#[serde(tag = "_variant")]
pub enum Ordering {
    Less,
    Equal,
    Greater,
}
