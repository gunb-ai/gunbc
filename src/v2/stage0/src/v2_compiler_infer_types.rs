// Generated by v2 compiler -- do not edit.
// Source module: v2.compiler.infer_types

use std::collections::HashMap;
use std::rc::Rc;
use crate::v2_rt;

#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyVec<T>(Vec<T>);

impl<T> NonEmptyVec<T> {
    pub fn new(items: Vec<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyVec requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_slice(&self) -> &[T] {
        &self.0
    }

    pub fn into_vec(self) -> Vec<T> {
        self.0
    }
}


#[derive(Debug, Clone, PartialEq)]
pub struct NonEmptyBTreeSet<T: Ord>(std::collections::BTreeSet<T>);

impl<T: Ord> NonEmptyBTreeSet<T> {
    pub fn new(items: std::collections::BTreeSet<T>) -> Result<Self, &'static str> {
        if items.is_empty() {
            Err("NonEmptyBTreeSet requires at least one element")
        } else {
            Ok(Self(items))
        }
    }

    pub fn as_set(&self) -> &std::collections::BTreeSet<T> {
        &self.0
    }

    pub fn into_set(self) -> std::collections::BTreeSet<T> {
        self.0
    }
}

pub use crate::std_types::{SourceSpan};
pub use crate::v2_std_core::{Node, Connective, Cardinality, make_expr_node, make_param_node, param_node_type_expr, LiteralValue, is_kernel_type, is_kernel_numeric, is_kernel_textual, BinOpKind, InferredNode, rt_node, has_inferred, NodeType, leaf_node, no_span, with_optional_cardinality, with_required_cardinality, node_is_product, node_is_coproduct, node_has_structure, ExprData};
use crate::v2_std_core::Connective::{Conj, Disj};
use crate::v2_std_core::Cardinality::{Required, CardOptional};
use crate::v2_std_core::ExprData::{NoExprData, ExprLiteral};
use crate::v2_std_core::LiteralValue::{LitStr, LitInt, LitFloat, LitBool, LitNull};
use crate::v2_std_core::BinOpKind::{BinEq, BinNe, BinLt, BinGt, BinLe, BinGe, BinAnd, BinOr, NullCoalesce};
use crate::v2_std_core::InferredNode::{Resolved, CompilerError};
use crate::v2_std_core::NodeType::{Typed, InferError, Untyped};


pub fn child_inferred_or_name(ch: Rc<Node>) -> Rc<Node> {
    if (ch.inferred.clone() == None) {
        leaf_node(ch.name.clone())
} else {
        rt_type(ch.clone())
}
}

pub fn rt_type(n: Rc<Node>) -> Rc<Node> {
    match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => rt.clone(),
    NodeType::InferError { .. } => leaf_node("Unit".to_string()),
    NodeType::Untyped => leaf_node("Unit".to_string()),
}
}


pub fn container_node(kind_name: String, element: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: kind_name.clone(),
    span: SourceSpan::new(0, 0),
    children: vec![element.clone()],
    connective: None,

    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn tuple_node(first: Rc<Node>, second: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Tuple".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![Rc::new(Node {
    name: "first".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![],
    connective: None,

    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: first.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
}), Rc::new(Node {
    name: "second".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![],
    connective: None,

    params: vec![],
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: second.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})],
    connective: Some(Connective::Conj),

    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn map_node(key: Rc<Node>, value: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Map".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![key.clone(), value.clone()],
    connective: None,

    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn bare_map_node() -> Rc<Node> {
    Rc::new(Node {
    name: "Map".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![],
    connective: None,

    params: vec![],
    inferred: None,
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

pub fn callable_node(func_params: Vec<Rc<Node>>, ret: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
    name: "Callable".to_string(),
    span: SourceSpan::new(0, 0),
    children: vec![],
    connective: None,

    params: func_params.clone(),
    inferred: Some(Rc::new(InferredNode::Resolved {
    node: ret.clone(),
})),
    return_cardinality: Cardinality::Required,
    uses: vec![],
    body: None,
    transport: None,
    properties: vec![],
    type_annotation: None,
    is_self_recursive: false,
    has_non_tail_self_call: false,
    match_pattern: None,
    expr_data: Rc::new(ExprData::NoExprData),
})
}

// AlgebraProfile: which algebraic structure a kernel type inhabits.
#[derive(Debug, Clone, PartialEq)]
pub enum AlgebraProfile {
    OrderedRingProfile,
    ApproximateFieldProfile,
    BooleanAlgebraProfile,
    FreeMonoidScalarProfile,
    FreeMonoidCollectionProfile,
    PartialFunctionProfile,
}

lazy_static::lazy_static! {
    pub static ref KERNEL_ALGEBRA_PROFILE: HashMap<String, AlgebraProfile> = {
        let mut m = HashMap::new();
        m.insert("Int".to_string(), AlgebraProfile::OrderedRingProfile);
        m.insert("Float".to_string(), AlgebraProfile::ApproximateFieldProfile);
        m.insert("Bool".to_string(), AlgebraProfile::BooleanAlgebraProfile);
        m.insert("String".to_string(), AlgebraProfile::FreeMonoidScalarProfile);
        m.insert("List".to_string(), AlgebraProfile::FreeMonoidCollectionProfile);
        m.insert("Set".to_string(), AlgebraProfile::FreeMonoidCollectionProfile);
        m.insert("NonEmptyList".to_string(), AlgebraProfile::FreeMonoidCollectionProfile);
        m.insert("NonEmptySet".to_string(), AlgebraProfile::FreeMonoidCollectionProfile);
        m.insert("Map".to_string(), AlgebraProfile::PartialFunctionProfile);
        m
    };
}

pub fn algebra_value_field(name: String, type_node: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
        name: name.clone(),
        span: no_span(),
        children: vec![],
        connective: None,
    
        params: vec![],
        inferred: Some(Rc::new(InferredNode::Resolved { node: type_node.clone() })),
        return_cardinality: Cardinality::Required,
        uses: vec![],
        body: None,
        transport: None,
        properties: vec![],
        type_annotation: None,
        is_self_recursive: false,
        has_non_tail_self_call: false,
        match_pattern: None,
        expr_data: Rc::new(ExprData::NoExprData),
    })
}

pub fn algebra_method_field(name: String, param_types: Vec<Rc<Node>>, return_type: Rc<Node>) -> Rc<Node> {
    let params: Vec<Rc<Node>> = param_types.iter().cloned().map(|t| {
        make_param_node("_".to_string(), t.clone(), None, no_span())
    }).collect();
    Rc::new(Node {
        name: name.clone(),
        span: no_span(),
        children: vec![],
        connective: None,
    
        params: vec![],
        inferred: Some(Rc::new(InferredNode::Resolved { node: callable_node(params, return_type.clone()) })),
        return_cardinality: Cardinality::Required,
        uses: vec![],
        body: None,
        transport: None,
        properties: vec![],
        type_annotation: None,
        is_self_recursive: false,
        has_non_tail_self_call: false,
        match_pattern: None,
        expr_data: Rc::new(ExprData::NoExprData),
    })
}

pub fn enrich_base_with_fields(name: String, base: Rc<Node>, fields: Vec<Rc<Node>>) -> Rc<Node> {
    Rc::new(Node { name: name.clone(), span: base.span.clone(), children: fields, connective: Some(Connective::Conj), params: base.params.clone(), inferred: base.inferred.clone(), return_cardinality: base.return_cardinality.clone(), uses: base.uses.clone(), body: base.body.clone(), transport: base.transport.clone(), properties: base.properties.clone(), type_annotation: base.type_annotation.clone(), is_self_recursive: base.is_self_recursive.clone(), has_non_tail_self_call: base.has_non_tail_self_call.clone(), match_pattern: base.match_pattern.clone(), expr_data: base.expr_data.clone() })
}

pub fn ordered_ring_fields(self_type: Rc<Node>) -> Vec<Rc<Node>> {
    let ordering_type = leaf_node("Ordering".to_string());
    vec![
        algebra_method_field("add".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_value_field("zero".to_string(), self_type.clone()),
        algebra_method_field("negate".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("mul".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_value_field("one".to_string(), self_type.clone()),
        algebra_method_field("compare".to_string(), vec![self_type.clone(), self_type.clone()], ordering_type.clone()),
    ]
}

pub fn approximate_field_fields(self_type: Rc<Node>) -> Vec<Rc<Node>> {
    let ordering_type = leaf_node("Ordering".to_string());
    vec![
        algebra_method_field("add".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_value_field("zero".to_string(), self_type.clone()),
        algebra_method_field("negate".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("mul".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_value_field("one".to_string(), self_type.clone()),
        algebra_method_field("reciprocal".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("compare".to_string(), vec![self_type.clone(), self_type.clone()], ordering_type.clone()),
    ]
}

pub fn boolean_algebra_fields(self_type: Rc<Node>) -> Vec<Rc<Node>> {
    vec![
        algebra_method_field("meet".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("join".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("complement".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_value_field("top".to_string(), self_type.clone()),
        algebra_value_field("bottom".to_string(), self_type.clone()),
    ]
}

pub fn free_monoid_scalar_fields(self_type: Rc<Node>) -> Vec<Rc<Node>> {
    let int_type = leaf_node("Int".to_string());
    let bool_type = leaf_node("Bool".to_string());
    let char_type = leaf_node("Char".to_string());
    let list_of_string = container_node("List".to_string(), self_type.clone());
    let list_of_char = container_node("List".to_string(), char_type.clone());
    vec![
        algebra_method_field("concat".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_value_field("empty".to_string(), self_type.clone()),
        algebra_method_field("length".to_string(), vec![self_type.clone()], int_type.clone()),
        algebra_method_field("chars".to_string(), vec![self_type.clone()], list_of_char.clone()),
        algebra_method_field("split".to_string(), vec![self_type.clone(), self_type.clone()], list_of_string.clone()),
        algebra_method_field("join".to_string(), vec![list_of_string.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("contains".to_string(), vec![self_type.clone(), self_type.clone()], bool_type.clone()),
        algebra_method_field("starts_with".to_string(), vec![self_type.clone(), self_type.clone()], bool_type.clone()),
        algebra_method_field("ends_with".to_string(), vec![self_type.clone(), self_type.clone()], bool_type.clone()),
        algebra_method_field("trim".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("to_lower".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("to_upper".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("replace".to_string(), vec![self_type.clone(), self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("substring".to_string(), vec![self_type.clone(), int_type.clone(), int_type.clone()], self_type.clone()),
        algebra_method_field("to_int".to_string(), vec![self_type.clone()], int_type.clone()),
        algebra_method_field("to_string".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("reverse".to_string(), vec![self_type.clone()], self_type.clone()),
    ]
}

pub fn free_monoid_collection_fields(self_type: Rc<Node>, elem: Rc<Node>) -> Vec<Rc<Node>> {
    let int_type = leaf_node("Int".to_string());
    let bool_type = leaf_node("Bool".to_string());
    let string_type = leaf_node("String".to_string());
    vec![
        algebra_method_field("map".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("filter".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("flat_map".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("fold".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("any".to_string(), vec![self_type.clone()], bool_type.clone()),
        algebra_method_field("all".to_string(), vec![self_type.clone()], bool_type.clone()),
        algebra_method_field("count".to_string(), vec![self_type.clone()], int_type.clone()),
        algebra_method_field("first".to_string(), vec![self_type.clone()], with_optional_cardinality(elem.clone())),
        algebra_method_field("last".to_string(), vec![self_type.clone()], with_optional_cardinality(elem.clone())),
        algebra_method_field("skip".to_string(), vec![self_type.clone(), int_type.clone()], self_type.clone()),
        algebra_method_field("take".to_string(), vec![self_type.clone(), int_type.clone()], self_type.clone()),
        algebra_method_field("sort_by".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("append".to_string(), vec![self_type.clone(), elem.clone()], self_type.clone()),
        algebra_method_field("contains".to_string(), vec![self_type.clone(), elem.clone()], bool_type.clone()),
        algebra_method_field("enumerate".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("reverse".to_string(), vec![self_type.clone()], self_type.clone()),
        algebra_method_field("join".to_string(), vec![self_type.clone(), string_type.clone()], string_type.clone()),
        algebra_method_field("concat".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("list_push".to_string(), vec![self_type.clone(), elem.clone()], self_type.clone()),
        algebra_method_field("length".to_string(), vec![self_type.clone()], int_type.clone()),
    ]
}

pub fn partial_function_fields(self_type: Rc<Node>, key_node: Rc<Node>, val_node: Rc<Node>) -> Vec<Rc<Node>> {
    let int_type = leaf_node("Int".to_string());
    let bool_type = leaf_node("Bool".to_string());
    let list_of_keys = container_node("List".to_string(), key_node.clone());
    let list_of_vals = container_node("List".to_string(), val_node.clone());
    vec![
        algebra_method_field("get".to_string(), vec![self_type.clone(), key_node.clone()], with_optional_cardinality(val_node.clone())),
        algebra_method_field("map_get".to_string(), vec![self_type.clone(), key_node.clone()], with_optional_cardinality(val_node.clone())),
        algebra_method_field("lookup".to_string(), vec![self_type.clone(), key_node.clone()], with_optional_cardinality(val_node.clone())),
        algebra_method_field("map_insert".to_string(), vec![self_type.clone(), key_node.clone(), val_node.clone()], self_type.clone()),
        algebra_method_field("map_merge".to_string(), vec![self_type.clone(), self_type.clone()], self_type.clone()),
        algebra_method_field("has".to_string(), vec![self_type.clone(), key_node.clone()], bool_type.clone()),
        algebra_method_field("map_has".to_string(), vec![self_type.clone(), key_node.clone()], bool_type.clone()),
        algebra_method_field("emit_map_has".to_string(), vec![self_type.clone(), key_node.clone()], bool_type.clone()),
        algebra_method_field("map_contains_key".to_string(), vec![self_type.clone(), key_node.clone()], bool_type.clone()),
        algebra_method_field("keys".to_string(), vec![self_type.clone()], list_of_keys.clone()),
        algebra_method_field("map_keys".to_string(), vec![self_type.clone()], list_of_keys.clone()),
        algebra_method_field("values".to_string(), vec![self_type.clone()], list_of_vals.clone()),
        algebra_method_field("map_values".to_string(), vec![self_type.clone()], list_of_vals.clone()),
        algebra_method_field("with".to_string(), vec![self_type.clone(), key_node.clone(), val_node.clone()], self_type.clone()),
        algebra_method_field("contains".to_string(), vec![self_type.clone(), key_node.clone()], bool_type.clone()),
        algebra_method_field("length".to_string(), vec![self_type.clone()], int_type.clone()),
    ]
}

pub fn enrich_kernel_type(name: String, base: Rc<Node>) -> Rc<Node> {
    let profile = KERNEL_ALGEBRA_PROFILE.get(&name);
    match profile {
        Some(p) => {
            let self_type = leaf_node(name.clone());
            let fields = match p {
                AlgebraProfile::OrderedRingProfile => ordered_ring_fields(self_type.clone()),
                AlgebraProfile::ApproximateFieldProfile => approximate_field_fields(self_type.clone()),
                AlgebraProfile::BooleanAlgebraProfile => boolean_algebra_fields(self_type.clone()),
                AlgebraProfile::FreeMonoidScalarProfile => free_monoid_scalar_fields(self_type.clone()),
                AlgebraProfile::FreeMonoidCollectionProfile => {
                    let elem = base.children.clone().first().cloned().unwrap_or_else(|| leaf_node("T".to_string()));
                    free_monoid_collection_fields(self_type.clone(), elem)
                },
                AlgebraProfile::PartialFunctionProfile => {
                    let key_node = base.children.clone().first().cloned().unwrap_or_else(|| leaf_node("K".to_string()));
                    let val_node = base.children.iter().cloned().skip(1).next().unwrap_or_else(|| leaf_node("V".to_string()));
                    partial_function_fields(self_type.clone(), key_node, val_node)
                },
            };
            enrich_base_with_fields(name, base, fields)
        },
        None => base.clone(),
    }
}

pub fn callable_inferred(n: Rc<Node>) -> Rc<Node> {
    if (n.name.clone() == "Callable".to_string()) {
        match n.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: ret, .. }) => ret.clone(),
    None => error_type_node(),
    _ => error_type_node(),
}
} else {
        n.clone()
}
}

pub fn error_type_node() -> Rc<Node> {
    leaf_node("Error".to_string())
}

pub fn node_is_bridge_error_name(n: Rc<Node>) -> bool {
    (n.name.clone() == "Error".to_string())
}

pub fn node_is_bridge_dynamic_name(n: Rc<Node>) -> bool {
    (n.name.clone() == "Dynamic".to_string())
}

pub fn node_is_collection(n: Rc<Node>) -> bool {
    ((n.children.clone().len() as i64) > 0) && (n.connective.clone() == None)
}

pub fn node_is_keyed_collection(n: Rc<Node>) -> bool {
    node_is_collection(n.clone()) && ((n.children.clone().len() as i64) == 2)
}

pub fn node_is_element_collection(n: Rc<Node>) -> bool {
    node_is_collection(n.clone()) && ((n.children.clone().len() as i64) == 1)
}

pub fn node_is_container(n: Rc<Node>) -> bool {
    node_is_element_collection(n.clone())
}

pub fn node_is_optional(n: Rc<Node>) -> bool {
    match n.return_cardinality.clone() {
    Cardinality::CardOptional => true,
    Cardinality::Required => false,
}
}

pub fn node_is_map(n: Rc<Node>) -> bool {
    node_is_keyed_collection(n.clone())
}

pub fn node_is_leaf(n: Rc<Node>) -> bool {
    ((((n.connective.clone() == None) && (node_has_structure(n.clone()) == false)) && ((n.children.clone().len() as i64) == 0)) && ((n.properties.clone().len() as i64) == 0))
}

pub fn node_is_named_ref(n: Rc<Node>) -> bool {
    if (n.inferred.clone() == None) {
        false
} else {
        match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => (((((node_has_structure(rt.clone()) == false) && ((rt.children.clone().len() as i64) == 0)) && (rt.name.clone() != "".to_string())) && (rt.name.clone() != "None".to_string())) && (is_kernel_type(rt.name.clone()) == false)),
    NodeType::InferError { .. } => false,
    NodeType::Untyped => false,
}
}
}

pub fn normalize_access_type_node(mut n: Rc<Node>) -> Rc<Node> {
    loop {
        let unwrapped = if ((n.name.clone() == "Refined".to_string()) && node_has_structure(n.clone())) {
            n.children.clone().first().cloned()
} else {
            None
};
match unwrapped.clone() {
    Some(base) => { {
            let __tco_0 = base.clone();
n = __tco_0;
continue;
} },
    None => { break n.clone(); },
}
}
}

pub fn node_type_shape(n: Rc<Node>) -> String {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if node_is_leaf(n.clone()) {
            if node_is_named_ref(n.clone()) {
                v2_rt::concat(v2_rt::concat("Named(".to_string(), n.name.clone()), ")".to_string())
} else {
                v2_rt::concat(v2_rt::concat("Primitive(".to_string(), n.name.clone()), ")".to_string())
}
} else {
            if node_is_product(n.clone()) {
                if (n.name.clone() == "".to_string()) {
                    "Product(<anon>)".to_string()
} else {
                    v2_rt::concat(v2_rt::concat("Product(".to_string(), n.name.clone()), ")".to_string())
}
} else {
                if node_is_coproduct(n.clone()) {
                    if (n.name.clone() == "".to_string()) {
                        "Coproduct(<anon>)".to_string()
} else {
                        v2_rt::concat(v2_rt::concat("Coproduct(".to_string(), n.name.clone()), ")".to_string())
}
} else {
                    if node_is_container(n.clone()) {
                        {
                            let elem_shape = match n.children.clone().first().cloned() {
    Some(el) => node_type_shape(el.clone()),
    None => "?".to_string(),
};
v2_rt::concat(v2_rt::concat(v2_rt::concat(v2_rt::concat("Container(".to_string(), n.name.clone()), ",".to_string()), elem_shape.clone()), ")".to_string())
}
} else {
                        if node_is_optional(n.clone()) {
                            {
                                let inner_shape = node_type_shape(with_required_cardinality(n.clone()));
v2_rt::concat(v2_rt::concat("Optional(".to_string(), inner_shape.clone()), ")".to_string())
}
} else {
                            if node_is_map(n.clone()) {
                                "Map(...)".to_string()
} else {
                                v2_rt::concat(v2_rt::concat("Node(".to_string(), n.name.clone()), ")".to_string())
}
}
}
}
}
}
    })
}

pub fn node_type_compatible(mut left: Rc<Node>, mut right: Rc<Node>) -> bool {
    loop {
        let left_opt = node_is_optional(left.clone());
let right_opt = node_is_optional(right.clone());
if (node_is_bridge_error_name(left.clone()) || node_is_bridge_error_name(right.clone())) {
            break true;
} else {
            if (node_is_bridge_dynamic_name(left.clone()) || node_is_bridge_dynamic_name(right.clone())) {
                break true;
} else {
                if (left_opt.clone() && (right.name.clone() == "Unit".to_string())) {
                    break true;
} else {
                    if ((left.name.clone() == "Unit".to_string()) && right_opt.clone()) {
                        break true;
} else {
                        if (node_is_container(left.clone()) && node_is_container(right.clone())) {
                            if (left.name.clone() != right.name.clone()) {
                                break false;
} else {
                                match left.children.clone().first().cloned() {
    Some(left_el) => { match right.children.clone().first().cloned() {
    Some(right_el) => { if ((left_el.name.clone() == "Unit".to_string()) || (right_el.name.clone() == "Unit".to_string())) {
                                    break true;
} else {
                                    {
                                        let __tco_0 = left_el.clone();
let __tco_1 = right_el.clone();
left = __tco_0;
right = __tco_1;
continue;
}
} },
    None => { break true; },
} },
    None => { break true; },
}
}
} else {
                            if (left_opt.clone() && right_opt.clone()) {
                                let left_inner = with_required_cardinality(left.clone());
let right_inner = with_required_cardinality(right.clone());
if ((left_inner.name.clone() == "Unit".to_string()) || (right_inner.name.clone() == "Unit".to_string())) {
                                    break true;
} else {
                                    {
                                        let __tco_0 = left_inner.clone();
let __tco_1 = right_inner.clone();
left = __tco_0;
right = __tco_1;
continue;
}
}
} else {
                                if (left_opt.clone() || right_opt.clone()) {
                                    break false;
} else {
                                    break (left.name.clone() == right.name.clone());
}
}
}
}
}
}
}
}
}

pub fn prefer_specific_type(left: Rc<Node>, right: Rc<Node>) -> Rc<Node> {
    {
        let left_is_container = node_is_container(left.clone());
let left_is_optional = node_is_optional(left.clone());
let left_first_child = left.children.clone().first().cloned();
let left_norm_name = left.name.clone();
let left_is_unit_inner = if left_is_container.clone() {
            match left_first_child.clone() {
    Some(el) => (el.name.clone() == "Unit".to_string()),
    None => false,
}
} else {
            if left_is_optional.clone() {
                (left.name.clone() == "Unit".to_string())
} else {
                false
}
};
let same_kind = if (left_is_container.clone() && node_is_container(right.clone())) {
            (left_norm_name.clone() == right.name.clone())
} else {
            if (left_is_optional.clone() && node_is_optional(right.clone())) {
                true
} else {
                false
}
};
if (same_kind.clone() && left_is_unit_inner.clone()) {
            right.clone()
} else {
            left.clone()
}
}
}

pub fn node_type_equals(left: Rc<Node>, right: Rc<Node>) -> bool {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        {
            let left_opt = node_is_optional(left.clone());
let right_opt = node_is_optional(right.clone());
let left_leaf = node_is_leaf(left.clone());
let right_leaf = node_is_leaf(right.clone());
let left_struct = node_has_structure(left.clone());
let right_struct = node_has_structure(right.clone());
if (node_is_bridge_error_name(left.clone()) || node_is_bridge_error_name(right.clone())) {
                true
} else {
                if (node_is_bridge_dynamic_name(left.clone()) && node_is_bridge_dynamic_name(right.clone())) {
                    true
} else {
                    if (node_is_bridge_dynamic_name(left.clone()) || node_is_bridge_dynamic_name(right.clone())) {
                        false
} else {
                        if (left_opt.clone() && (right.name.clone() == "Unit".to_string())) {
                            true
} else {
                            if ((left.name.clone() == "Unit".to_string()) && right_opt.clone()) {
                                true
} else {
                                if (left_leaf.clone() && right_leaf.clone()) {
                                    (left.name.clone() == right.name.clone())
} else {
                                    if (left_struct.clone() && right_struct.clone()) {
                                        if (left.name.clone() != right.name.clone()) {
                                            false
} else {
                                            if (node_is_product(left.clone()) != node_is_product(right.clone())) {
                                                false
} else {
                                                if ((left.children.clone().len() as i64) != (right.children.clone().len() as i64)) {
                                                    false
} else {
                                                    { let mut __all = true; for pair in left.children.iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { if !(match right.children.iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(right_child) => node_type_equals(pair.1.clone(), right_child.clone()),
    None => false,
}) { __all = false; break; } } __all }
}
}
}
} else {
                                        if (left_leaf.clone() && right_struct.clone()) {
                                            (left.name.clone() == right.name.clone())
} else {
                                            if (left_struct.clone() && right_leaf.clone()) {
                                                (left.name.clone() == right.name.clone())
} else {
                                                if (node_is_container(left.clone()) && node_is_container(right.clone())) {
                                                    if (left.name.clone() != right.name.clone()) {
                                                        false
} else {
                                                        match left.children.clone().first().cloned() {
    Some(left_el) => match right.children.clone().first().cloned() {
    Some(right_el) => node_type_equals(left_el.clone(), right_el.clone()),
    None => false,
},
    None => false,
}
}
} else {
                                                    if (left_opt.clone() && right_opt.clone()) {
                                                        node_type_equals(with_required_cardinality(left.clone()), with_required_cardinality(right.clone()))
} else {
                                                        if (node_is_map(left.clone()) && node_is_map(right.clone())) {
                                                            if (((left.children.clone().len() as i64) == 2) && ((right.children.clone().len() as i64) == 2)) {
                                                                {
                                                                    let left_first = match left.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => left.clone(),
};
let right_first = match right.children.clone().first().cloned() {
    Some(v) => v.clone(),
    None => right.clone(),
};
let left_second = match left.children.iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(v) => v.clone(),
    None => left.clone(),
};
let right_second = match right.children.iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(v) => v.clone(),
    None => right.clone(),
};
(node_type_equals(left_first.clone(), right_first.clone()) && node_type_equals(left_second.clone(), right_second.clone()))
}
} else {
                                                                false
}
} else {
                                                            if ((left.name.clone() == "Callable".to_string()) && (right.name.clone() == "Callable".to_string())) {
                                                                if ((left.params.clone().len() as i64) != (right.params.clone().len() as i64)) {
                                                                    false
} else {
                                                                    {
                                                                        let params_eq = { let mut __all = true; for pair in left.params.iter().cloned().enumerate().map(|(i, v)| (i as i64, v)).collect::<Vec<_>>().iter().cloned() { if !(match right.params.iter().cloned().skip(pair.0.clone() as usize).collect::<Vec<_>>().first().cloned() {
    Some(right_param) => node_type_equals(param_node_type_expr(pair.1.clone()), param_node_type_expr(right_param.clone())),
    None => false,
}) { __all = false; break; } } __all };
if (params_eq.clone() == false) {
                                                                            false
} else {
                                                                            match left.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: left_ret, .. }) => match right.inferred.clone().as_deref().cloned() {
    Some(InferredNode::Resolved { node: right_ret, .. }) => node_type_equals(left_ret.clone(), right_ret.clone()),
    None => false,
    _ => false,
},
    None => (right.inferred.clone() == None),
    _ => false,
}
}
}
}
} else {
                                                                false
}
}
}
}
}
}
}
}
}
}
}
}
}
}
    })
}

pub fn node_type_deps(n: Rc<Node>) -> Vec<String> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if node_is_named_ref(n.clone()) {
            match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => vec![rt.name.clone()],
    NodeType::InferError { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
            if node_has_structure(n.clone()) {
                { let mut __result = Vec::new(); for child in n.children.iter().cloned() { __result.extend(if (child.inferred.clone() != None) {
                    match (*rt_node(child.clone())).clone() {
    NodeType::Typed { node: rt, .. } => node_type_deps(rt.clone()),
    NodeType::InferError { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
                    node_type_deps(child.clone())
}); } __result }
} else {
                if (n.inferred.clone() != None) {
                    match (*rt_node(n.clone())).clone() {
    NodeType::Typed { node: rt, .. } => node_type_deps(rt.clone()),
    NodeType::InferError { .. } => vec![],
    NodeType::Untyped => vec![],
}
} else {
                    if ((n.children.clone().len() as i64) > 0) {
                        {
                            let child_deps = { let mut __result = Vec::new(); for child in n.children.iter().cloned() { __result.extend(node_type_deps(child.clone())); } __result };
if ((n.name.clone() != "".to_string()) && (is_kernel_type(n.name.clone()) == false)) {
                                v2_rt::concat(vec![n.name.clone()], child_deps.clone())
} else {
                                child_deps.clone()
}
}
} else {
                        if ((is_kernel_type(n.name.clone()) || (n.name.clone() == "None".to_string())) || (n.name.clone() == "".to_string())) {
                            vec![]
} else {
                            vec![n.name.clone()]
}
}
}
}
}
    })
}

pub fn is_int_type_node(n: Rc<Node>) -> bool {
    {
        let normed = normalize_access_type_node(n.clone());
((normed.name.clone() == "Int".to_string()) && node_is_leaf(normed.clone()))
}
}

pub fn is_string_type_node(n: Rc<Node>) -> bool {
    {
        let normed = normalize_access_type_node(n.clone());
((normed.name.clone() == "String".to_string()) && node_is_leaf(normed.clone()))
}
}

pub fn is_bool_type_node(n: Rc<Node>) -> bool {
    {
        let normed = normalize_access_type_node(n.clone());
((normed.name.clone() == "Bool".to_string()) && node_is_leaf(normed.clone()))
}
}

pub fn is_float_type_node(n: Rc<Node>) -> bool {
    {
        let normed = normalize_access_type_node(n.clone());
((normed.name.clone() == "Float".to_string()) && node_is_leaf(normed.clone()))
}
}

pub fn infer_literal_node(lit: Rc<LiteralValue>) -> Rc<Node> {
    match (*lit.clone()).clone() {
    LiteralValue::LitStr { .. } => leaf_node("String".to_string()),
    LiteralValue::LitInt { .. } => leaf_node("Int".to_string()),
    LiteralValue::LitFloat { .. } => leaf_node("Float".to_string()),
    LiteralValue::LitBool { .. } => leaf_node("Bool".to_string()),
    LiteralValue::LitNull => with_optional_cardinality(leaf_node("Unit".to_string())),
}
}

pub fn method_receiver_element_node(receiver_type: Rc<Node>) -> Rc<Node> {
    {
        let normed = normalize_access_type_node(receiver_type.clone());
if ((node_has_structure(normed.clone()) == false) && ((normed.children.clone().len() as i64) == 1)) {
            match normed.children.clone().first().cloned() {
    Some(el) => el.clone(),
    None => receiver_type.clone(),
}
} else {
            if node_is_map(normed.clone()) {
                match normed.children.iter().cloned().skip(1 as usize).collect::<Vec<_>>().first().cloned() {
    Some(val_type) => val_type.clone(),
    None => receiver_type.clone(),
}
} else {
                receiver_type.clone()
}
}
}
}

pub fn extract_optional_inner_node(n: Rc<Node>) -> Rc<Node> {
    if node_is_optional(n.clone()) {
        with_required_cardinality(n.clone())
} else {
        n.clone()
}
}

pub fn infer_binop_type_node(op: BinOpKind, left_type: Rc<Node>) -> Rc<Node> {
    match op.clone() {
    BinOpKind::BinEq => leaf_node("Bool".to_string()),
    BinOpKind::BinNe => leaf_node("Bool".to_string()),
    BinOpKind::BinLt => leaf_node("Bool".to_string()),
    BinOpKind::BinGt => leaf_node("Bool".to_string()),
    BinOpKind::BinLe => leaf_node("Bool".to_string()),
    BinOpKind::BinGe => leaf_node("Bool".to_string()),
    BinOpKind::BinAnd => leaf_node("Bool".to_string()),
    BinOpKind::BinOr => leaf_node("Bool".to_string()),
    BinOpKind::NullCoalesce => extract_optional_inner_node(left_type.clone()),
    _ => left_type.clone(),
}
}

pub fn for_each_element_type_node(n: Rc<Node>) -> Rc<Node> {
    {
        let normed = normalize_access_type_node(n.clone());
let is_single_child = ((node_has_structure(normed.clone()) == false) && ((normed.children.clone().len() as i64) == 1));
let extracted = if is_single_child.clone() {
            normed.children.clone().first().cloned()
} else {
            None
};
match extracted.clone() {
    Some(el) => el.clone(),
    None => if (node_is_leaf(normed.clone()) && (normed.name.clone() == "String".to_string())) {
            leaf_node("String".to_string())
} else {
            normed.clone()
},
}
}
}

pub fn emit_map_has(m: HashMap<String, bool>, key: String) -> bool {
    match v2_rt::map_get(&m, key.clone()) {
    Some(_) => true,
    None => false,
}
}
