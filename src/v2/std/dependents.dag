module v2.std.dependents


type Dependents
  = Untracked
  | Tracked { uses: std.types.List<std.occurrence_identity.OccurrenceId> }

fn no_dependents(d: Dependents) -> Bool {
  match d {
    Untracked => true
    Tracked { uses } => is_empty(xs: uses)
  }
}
