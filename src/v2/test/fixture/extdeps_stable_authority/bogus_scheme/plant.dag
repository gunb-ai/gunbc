// RED perturb fixture — external anchor with unrecognized Uri scheme constructor identity.

module extdeps.fixture.stable_authority_bogus_scheme

import extdeps.stable_authority { StableAuthority }
import extdeps.uri { Uri }

data extdeps_stable_authority_anchor: StableAuthority = StableAuthority {
  uri: Uri {
    scheme: Gopher
    locator: "example.invalid/spec"
  }
}
