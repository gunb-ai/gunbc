module v2.test.fixture.floor_skip.falsifier_divergence_control

import v2.std.logic { Bool }

data falsifier_divergence_control_note: String = "Control fixtures for the falsifier's divergence semantics (SelectionPredictOnly, signed design: docs/plans/affected-set-differential-falsifier.md). falsifier_red_control_holds is DELIBERATELY red: driven under predict-only with a diff that does not touch this file, it is predicted-unaffected and its red must surface as exactly one counted DIVERGENCE; driven with a diff that edits its own declaration line, it is predicted-affected and its red is an ordinary failure, NOT a divergence - that asymmetry is the discriminating fact. This file lives under test/fixture and is consumed only through explicit rosters (floor_skip_discovery_witness), never through scan-dir discovery."

test fn falsifier_red_control_holds() -> Bool {
  false
}

test fn falsifier_green_control_holds() -> Bool {
  true
}
