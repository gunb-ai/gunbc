// COMPILE STUB — empty facts for M1 / full-v2 typecheck only.
// Runtime by-execution authority: target/v2-layering-imports-manifest.dag
// (generated by tools.layering_imports_transport emit_manifest; never commit the target artifact).
// Consumers MUST pass --source-root "$(dirname "$manifest")" AFTER --source-root src/v2
// so build_module_index overlays this stub with the generated module (same module name).
// Test-seam: module v2.test.workflow.host_layering_import_manifest →
//   src/v2/test/claim/workflow/host_layering_import_manifest.dag (LAYOUT.md rule 3).

module v2.test.workflow.host_layering_import_manifest

import v2.std.collection { List }
import v2.lens.layering_imports { LayerImportFact }

data host_layer_import_facts: List<LayerImportFact> = []
