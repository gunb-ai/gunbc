module v2.test.long.direct_rust_door_production_group

import v2.compiler.self_host.v2_emitter_direct_rust_door_contract {
  direct_rust_door_production_group,
  direct_rust_door_production_group_holds,
}
import v2.std.logic { Bool }
import v2.std.text { String }

data direct_rust_door_production_group_test_note: String = "INTEGRATION OBSERVATION with an OPEN COST DEFECT (operator witness-cost policy 2026-08-05). This function executes the REAL compiler pipeline inside a witness — 12483ms semantic eval at af0130c268, about 25x the 500ms mandatory-migration threshold — so it is an integration receipt, not ordinary business-logic coverage. Three things this file must NOT be read as claiming: that living under long/ migrated it, that the 5000ms executor fail-stop is a health bar, or that collapsing four production walks into one discharged the cost defect. That collapse removed REPETITION; the remaining single walk is still the wrong shape. Exactly one test function lives here because every function here pays that walk. The leaf semantics are already migrated: eleven sub-millisecond controls over planted observations in v2.test.self_host.direct_rust_door_group_algebra discriminate each leaf law, the exact per-stage not-reached causes, and the parent composition, with zero production cost. What is NOT migrated is the REAL observation, still reconstructed per evaluation rather than materialized once under its subject identity — that is this row's replacement plan, owned by the semantic-result materialization slice, and its cost trigger is separate from the lane's semantic trigger. TIMING BASIS, separated per policy: semantic evaluation 12483ms; entry graph construction, load and resolve 40293ms, which is harness acquisition and is NOT part of the witness eval figure. Local recipe: claim_batch --source-root dag --source-root src/v2 --entry src/v2/test/claim/long/direct_rust_door_production_group_test.dag --functions direct_rust_door_production_group_closing_expectation_holds"

test fn direct_rust_door_production_group_closing_expectation_holds() -> Bool {
  direct_rust_door_production_group_holds(group: direct_rust_door_production_group())
}
