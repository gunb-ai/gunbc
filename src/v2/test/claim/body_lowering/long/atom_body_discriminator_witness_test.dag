module v2.test.claim.long.body_lowering_atom_body_discriminator_witness


// Atom-body vs arity discriminator: one tokenize→parse→normalize pass over the
// four-declaration module, then exact top-level Arrow identity join plus
// parameter_count/ArrowBodyForm rows on the joined tree (no second frontend
// pass). RED mirror classifiers are test-only mutation machinery — not a
// second production authority. Substrate long-lane enrolled (~18.6s thread-CPU
// locally; 5001ms is only the fast-lane budget-probe abort point, not
// completed cost): the mechanism is the production frontend crossing, not a
// synthetic fixture.

test fn atom_body_discriminator_single_pass_receipt_holds() -> Bool {
  v2.test.claim.body_lowering.atom_body_discriminator_helpers.atom_body_discriminator_batched_receipt_holds()
}
