module v2.test.claim.bootstrap

import tools.bootstrap_witness_transport { run_bootstrap_witness }
import v2.std.logic { Bool }

data bootstrap_witness_provenance: String = "Floor witness migrated from src/v1/tests/src/bootstrap.rs (29 v1 #[test] fns across floor/expensive/ci/l4 suites): v1 bootstrap self-host oracle (stage0 cargo check, compile diagnostic ratchet, dag-target compile, ephemeral generated source-root import, stage0→stage1 emit check, fixed-point stage1==stage2, performance ratchet, ci_full_dag hard-diagnostic zero, ci freshness/fixed-point, weather L4 structural emit+run) executed via bootstrap_witness host physics until v2.workflow.bootstrap self-host gates are witness-layer importable. Floor runs --suite floor (stage0_cargo_check); full v1 parity via bootstrap_witness --suite all."

test fn bootstrap_witness_keystone_holds() -> Bool {
  run_bootstrap_witness()
}
