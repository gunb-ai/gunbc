module identity_escape_probe

fn probe_j(node: Node) -> Optional<Node> {
  match find_named_child(root: node, name: some_name) {
    Accepted { value: id_node } => Absent
    Rejected {} => Absent
  }
}
