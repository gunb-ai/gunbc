module identity_escape_probe

fn leaky_identity_probe(node: Node) -> Optional<Node> {
  match node.children {
    Foo { value: id_node, diagnostics: _ } => Absent
    Bar { diagnostics: _ } => Absent
  }
}
