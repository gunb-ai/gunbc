module v2.test.lens_non_fold_residue.non_fold_residue_test

import v2.lens.non_fold_residue { nfr_roster_receipt_holds }
import v2.std.live_tree { LiveTreeDisposition, ReadsLiveTree }

data live_tree_disposition: LiveTreeDisposition = ReadsLiveTree

data non_fold_residue_always_on_note: String = "Always-on PR floor receipt (operator brief 2026-07-22, class 4): nfr_roster_receipt_holds walks the live corpus via host-fed counters — MUST declare ReadsLiveTree so affected-set selection never predict-skips it (the lying SubstrateInputsOnly stamp class that kept the falsifier dark for 6 days). Detection-only: roster rows need reason + dissolution trigger; never auto-delete enrollments."

test fn non_fold_residue_no_unrostered_or_stale() -> Bool {
  return nfr_roster_receipt_holds()
}

test fn non_fold_residue_synthetic_unrostered_red_holds() -> Bool {
  return non_fold_residue_synthetic_unrostered_red_holds()
}

test fn non_fold_residue_wildcard_red_fixture_holds() -> Bool {
  return non_fold_residue_wildcard_red_fixture_holds()
}
