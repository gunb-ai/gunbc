// Throwaway: solo resolve bisect — ONLY languages/dag (dag_language_model_wave1).
// dissolve-on: #5146-class N_v2 resolve hang root-caused — delete this bisect DAG + ignored Rust bisect tests.
module v2.test.manual.nv2_gate_resolve_bisect_languages_dag

import v2.extdeps.languages.dag { dag_language_model_wave1 }
import v2.std.logic { Bool }

fn bisect() -> Bool {
  true
}
