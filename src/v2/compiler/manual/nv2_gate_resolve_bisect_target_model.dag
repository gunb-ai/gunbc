// Throwaway: solo resolve bisect — ONLY std.compilers.target_model (#5146 suspect).
// dissolve-on: #5146-class N_v2 resolve hang root-caused — delete this bisect DAG + ignored Rust bisect tests.
module v2.test.manual.nv2_gate_resolve_bisect_target_model

import v2.std.compilers.target_model { TargetModel }
import v2.std.logic { Bool }

fn bisect() -> Bool {
  true
}
