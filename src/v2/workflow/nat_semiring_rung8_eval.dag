module v2.test.workflow.nat_semiring_rung8_eval

import v2.std.algebra { is_empty }
import v2.std.logic { Bool }
import std.nat { Zero }
import v2.test.nat_semiring.rung_8 { phase1_nat_semiring_rung8_runtime_value_rows }
import v2.test.workflow.testclaim_corpus_runner {
  CorpusEvalReport,
  corpus_entries_from_node_runtime_value_runs,
  corpus_report_tally
}

fn run_nat_semiring_rung8_eval() -> CorpusEvalReport {
  CorpusEvalReport {
    entries: corpus_entries_from_node_runtime_value_runs(
      runs: phase1_nat_semiring_rung8_runtime_value_rows
    )
  }
}

fn nat_semiring_rung8_gate(report: CorpusEvalReport) -> Bool {
  if is_empty(xs: report.entries) {
    false
  } else {
    nat_semiring_rung8_zero_deferred(report: report)
  }
}

fn nat_semiring_rung8_zero_deferred(report: CorpusEvalReport) -> Bool {
  corpus_report_tally(r: report).deferred == Zero
}

data witness_nat_semiring_rung8_zero_deferred_closed: Bool = nat_semiring_rung8_zero_deferred(report: run_nat_semiring_rung8_eval())
