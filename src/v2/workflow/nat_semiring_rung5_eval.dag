module v2.test.workflow.nat_semiring_rung5_eval


fn run_nat_semiring_rung5_eval() -> v2.test.workflow.testclaim_corpus_runner.CorpusEvalReport {
  v2.test.workflow.testclaim_corpus_runner.CorpusEvalReport {
    entries: corpus_entries_from_node_runtime_value_runs(
      runs: phase1_nat_semiring_rung5_full_law_roster_runtime_value_rows
    )
  }
}

fn nat_semiring_rung5_report_has_evidence(report: v2.test.workflow.testclaim_corpus_runner.CorpusEvalReport) -> Bool {
  match is_empty(xs: report.entries) {
    true => false
    false => true
  }
}

fn nat_semiring_rung5_gate(report: v2.test.workflow.testclaim_corpus_runner.CorpusEvalReport) -> Bool {
  if nat_semiring_rung5_report_has_evidence(report: report) {
    let tally = corpus_report_tally(r: report)
    tally.fail == Zero && tally.deferred == Zero
  } else {
    false
  }
}
