// Generated tests for std_width_generated_tests DAG.
// 
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 28 obligations (4 discharged, 24 testable [0 compiler gaps]: A=8, B=16, C=0, D=0)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: a223c88659e37e24ce8548a46aa8343c50a4b268b8449eb3871860e93e54973f
// 
// Probe-Observer Coverage:
//   Probes: 6
//     std.width::truncate_text (boundary mock)
//     std.unicode::display_width_columns (boundary mock)
//     std.unicode::in_block (boundary mock)
//     std.unicode::char_width (boundary mock)
//     std.unicode::string_display_width (boundary mock)
//     param_source_std_unicode_char_width_c (input mock)
//   
//   Observers: 4
//     std.unicode::char_width [terminal] (return: NonEmpty)
//     std.unicode::in_block [terminal] (return: NonEmpty)
//     std.unicode::string_display_width [terminal] (return: NonEmpty)
//     std.width::truncate_text [terminal] (return: NonEmpty)
//   
//   Integration tests: 2
//     param_source_std_unicode_char_width_c -> std.unicode::char_width (depth 4)
//     std.unicode::display_width_columns -> std.unicode::char_width (depth 2)


use gunbc_exec::{ExecuteConfig, ExecutionMode, execute_dag, lower};
use gunbc_ir::{Value, detect_boundaries};
use gunbc_test::{FermiCost, MockSpec, TestClass, Window, apply_window_inputs, assert_boundary_mockable, assert_chain_outputs, assert_window_outputs, guard_test, window_subdag};
use std::collections::HashMap;

fn mock_spec() -> MockSpec {
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    gunbc_test::auto_mock_spec(&dag, "std-width")
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
// 
// Determinism obligations: 7 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'std.width::truncate_text': same inputs → same outputs
// - 'std.unicode::display_width_columns': same inputs → same outputs
// - 'std.unicode::in_block': same inputs → same outputs
// - 'std.unicode::char_display_width': same inputs → same outputs
// - 'std.unicode::char_width': same inputs → same outputs
// - 'std.unicode::string_display_width': same inputs → same outputs
// - 'param_source_std_unicode_char_width_c': same inputs → same outputs

/// DryRun execution completes without crash.
/// 
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    if !guard_test("test_dryrun_completion", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("DryRun execution should complete without crash");
    assert!(!log.entries.is_empty(), "execution should produce log entries");
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 7 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'std.width::truncate_text': valid inputs → valid outputs
// - 'std.unicode::display_width_columns': valid inputs → valid outputs
// - 'std.unicode::in_block': valid inputs → valid outputs
// - 'std.unicode::char_display_width': valid inputs → valid outputs
// - 'std.unicode::char_width': valid inputs → valid outputs
// - 'std.unicode::string_display_width': valid inputs → valid outputs
// - 'param_source_std_unicode_char_width_c': valid inputs → valid outputs
// 6 optional input handling obligations.
// Optional inputs must accept missing values and reject wrong-typed inputs.

/// Coercion coverage: std.unicode::char_display_width.return 1 → std.unicode::char_width.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.unicode::char_width.__deps.
#[test]
fn test_coercion_std_unicode_char_display_width_return_std_unicode_char_width_deps() {
    if !guard_test("test_coercion_std_unicode_char_display_width_return_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.unicode::char_display_width.return → std.unicode::char_width.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.unicode::char_width").expect("coercion target entry missing for std.unicode::char_width.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.unicode::char_width.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.unicode::char_width.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.unicode::char_width.__deps; got {:?}", received);
}

/// Coercion coverage: std.unicode::display_width_columns.return 1 → std.unicode::char_width.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.unicode::char_width.__deps.
#[test]
fn test_coercion_std_unicode_display_width_columns_return_std_unicode_char_width_deps() {
    if !guard_test("test_coercion_std_unicode_display_width_columns_return_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.unicode::display_width_columns.return → std.unicode::char_width.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.unicode::char_width").expect("coercion target entry missing for std.unicode::char_width.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.unicode::char_width.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.unicode::char_width.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.unicode::char_width.__deps; got {:?}", received);
}

/// Variant coverage: std.unicode::in_block.return with variant 'True' of type Bool.
/// 
/// Proves: DAG handles variant 'True' at boundary port std.unicode::in_block.return.
#[test]
fn test_variant_std_unicode_in_block_return_true() {
    if !guard_test("test_variant_std_unicode_in_block_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("True".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for std.unicode::in_block.return should not crash");
}

/// Variant coverage: std.unicode::in_block.return with variant 'False' of type Bool.
/// 
/// Proves: DAG handles variant 'False' at boundary port std.unicode::in_block.return.
#[test]
fn test_variant_std_unicode_in_block_return_false() {
    if !guard_test("test_variant_std_unicode_in_block_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("False".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for std.unicode::in_block.return should not crash");
}

/// Optional input: std.width::truncate_text.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_width_truncate_text_deps() {
    if !guard_test("test_optional_missing_std_width_truncate_text_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.width::truncate_text", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.width::truncate_text.__deps missing should not error");
}

/// Optional input: std.unicode::display_width_columns.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_display_width_columns_deps() {
    if !guard_test("test_optional_missing_std_unicode_display_width_columns_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("w".to_string(), Value::Str("<DisplayWidth>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::display_width_columns", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::display_width_columns.__deps missing should not error");
}

/// Optional input: std.unicode::in_block.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_in_block_deps() {
    if !guard_test("test_optional_missing_std_unicode_in_block_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("block".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("cp".to_string(), Value::Int(1));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::in_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::in_block.__deps missing should not error");
}

/// Optional input: std.unicode::char_display_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_char_display_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_char_display_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("<DisplayWidth>".to_string()));
    inputs.insert("c".to_string(), Value::Int(1));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_display_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::char_display_width.__deps missing should not error");
}

/// Optional input: std.unicode::char_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_char_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("c".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::char_width.__deps missing should not error");
}

/// Optional input: std.unicode::string_display_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_string_display_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_string_display_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("s".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::string_display_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::string_display_width.__deps missing should not error");
}

// =========================================================================
// Boundary Tests (per-node mockability)
// =========================================================================

/// Test that all boundaries can be mocked.
#[test]
fn test_boundaries_mockable() {
    if !guard_test("test_boundaries_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(result.is_ok(), "Boundaries should be mockable: {:?}", result.error);
}

/// Test that std.unicode::char_width boundary can be mocked.
#[test]
fn test_boundary_std_unicode_char_width_mockable() {
    if !guard_test("test_boundary_std_unicode_char_width_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::char_width".into()), "std.unicode::char_width should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::char_width", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::char_width").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::display_width_columns boundary can be mocked.
#[test]
fn test_boundary_std_unicode_display_width_columns_mockable() {
    if !guard_test("test_boundary_std_unicode_display_width_columns_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::display_width_columns".into()), "std.unicode::display_width_columns should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::display_width_columns", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::display_width_columns").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::in_block boundary can be mocked.
#[test]
fn test_boundary_std_unicode_in_block_mockable() {
    if !guard_test("test_boundary_std_unicode_in_block_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::in_block".into()), "std.unicode::in_block should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("True".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::in_block").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::string_display_width boundary can be mocked.
#[test]
fn test_boundary_std_unicode_string_display_width_mockable() {
    if !guard_test("test_boundary_std_unicode_string_display_width_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::string_display_width".into()), "std.unicode::string_display_width should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::string_display_width", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::string_display_width").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.width::truncate_text boundary can be mocked.
#[test]
fn test_boundary_std_width_truncate_text_mockable() {
    if !guard_test("test_boundary_std_width_truncate_text_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.width::truncate_text".into()), "std.width::truncate_text should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.width::truncate_text", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.width::truncate_text").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

// =========================================================================
// Chain Validation Tests
// =========================================================================

// These tests verify that mock outputs satisfy downstream input expectations.

/// Test that this tool's mock spec is self-consistent.
#[test]
fn test_mock_spec_self_consistent() {
    if !guard_test("test_mock_spec_self_consistent", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let spec = mock_spec();
    // Verify all boundary mocks are present
    assert!(spec.get_boundary_mock("std.width::truncate_text", "return").is_some(), "MockSpec should have boundary mock for std.width::truncate_text.return");
    assert!(spec.get_boundary_mock("std.unicode::display_width_columns", "return").is_some(), "MockSpec should have boundary mock for std.unicode::display_width_columns.return");
    assert!(spec.get_boundary_mock("std.unicode::in_block", "return").is_some(), "MockSpec should have boundary mock for std.unicode::in_block.return");
    assert!(spec.get_boundary_mock("std.unicode::char_width", "return").is_some(), "MockSpec should have boundary mock for std.unicode::char_width.return");
    assert!(spec.get_boundary_mock("std.unicode::string_display_width", "return").is_some(), "MockSpec should have boundary mock for std.unicode::string_display_width.return");
}

// =========================================================================
// Windowed Segment Tests
// =========================================================================

// These tests execute contiguous windows of the DAG using baseline DryRun
// values as injected inputs, then verify window exit outputs match baseline.

/// Window: std.unicode::char_display_width -> std.unicode::display_width_columns
#[test]
fn test_window_std_unicode_char_display_width_through_std_unicode_display_width_columns() {
    if !guard_test("test_window_std_unicode_char_display_width_through_std_unicode_display_width_columns", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.unicode::char_display_width", "std.unicode::display_width_columns"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

/// Window: std.unicode::char_display_width -> std.unicode::char_width
#[test]
fn test_window_std_unicode_char_display_width_through_std_unicode_char_width() {
    if !guard_test("test_window_std_unicode_char_display_width_through_std_unicode_char_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.unicode::char_display_width", "std.unicode::display_width_columns", "std.unicode::char_width"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

// =========================================================================
// Probe-Observer Integration Tests
// =========================================================================

// Probes: 6 | Observers: 4 | Tests: 2
// All terminal nodes are observed.

/// Chain test: param_source_std_unicode_char_width_c -> std.unicode::char_width (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_unicode_char_width_c_to_std_unicode_char_width() {
    if !guard_test("test_chain_param_source_std_unicode_char_width_c_to_std_unicode_char_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_unicode_char_width_c", "std.unicode::char_display_width", "std.unicode::char_width", "std.unicode::display_width_columns"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.unicode::char_width (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.unicode::char_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.unicode::char_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_unicode_char_width_c -> std.unicode::char_width should satisfy observer matchers");
}

/// Chain test: std.unicode::display_width_columns -> std.unicode::char_width (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_unicode_display_width_columns_to_std_unicode_char_width() {
    if !guard_test("test_chain_std_unicode_display_width_columns_to_std_unicode_char_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.unicode::char_width", "std.unicode::display_width_columns"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.unicode::char_width (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.unicode::char_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.unicode::char_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.unicode::display_width_columns -> std.unicode::char_width should satisfy observer matchers");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: std.width::truncate_text (example 0)
/// 
/// Tests that node 'std.width::truncate_text' produces expected outputs for given inputs.
#[test]
fn test_example_std_width_truncate_text_0() {
    if !guard_test("test_example_std_width_truncate_text_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.width::truncate_text", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.width::truncate_text' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.unicode::in_block (example 1)
/// 
/// Tests that node 'std.unicode::in_block' produces expected outputs for given inputs.
#[test]
fn test_example_std_unicode_in_block_1() {
    if !guard_test("test_example_std_unicode_in_block_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("block".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("cp".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("True".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::in_block", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.unicode::in_block' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.unicode::char_width (example 2)
/// 
/// Tests that node 'std.unicode::char_width' produces expected outputs for given inputs.
#[test]
fn test_example_std_unicode_char_width_2() {
    if !guard_test("test_example_std_unicode_char_width_2", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("c".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Int(1));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_width", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.unicode::char_width' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.unicode::string_display_width (example 3)
/// 
/// Tests that node 'std.unicode::string_display_width' produces expected outputs for given inputs.
#[test]
fn test_example_std_unicode_string_display_width_3() {
    if !guard_test("test_example_std_unicode_string_display_width_3", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/width.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Int(1));
    inputs.insert("s".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::string_display_width", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.unicode::string_display_width' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

