// Generated tests for extdeps_gitignore_render_generated_tests DAG.
// 
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 226 obligations (41 discharged, 185 testable [5 compiler gaps]: A=61, B=124, C=0, D=0)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: 14041222b3ae1004efc977ef696a0b21237ee66e8769876fa9436811d020aefb
// 
// Probe-Observer Coverage:
//   Probes: 50
//     extdeps.gitignore_render::render_category_header (boundary mock)
//     extdeps.gitignore_render::render_category (boundary mock)
//     extdeps.gitignore_render::render_gitignore_content (boundary mock)
//     extdeps.gitignore_render::render_gitignore_file (boundary mock)
//     extdeps.gitignore::filter_categories (boundary mock)
//     std.render::span_width (boundary mock)
//     std.render::truncate_spans (boundary mock)
//     std.render::constrain_line (boundary mock)
//     std.render::constrain_frame (boundary mock)
//     std.render::comment_prefix_for (boundary mock)
//     std.render::config_comment_prefix (boundary mock)
//     std.render::language_comment_prefix (boundary mock)
//     std.render::format_header (boundary mock)
//     std.render::config_format_header (boundary mock)
//     std.render::language_format_header (boundary mock)
//     std.render::format_comment (boundary mock)
//     std.render::format_section_header (boundary mock)
//     std.render::indent_text (boundary mock)
//     std.render::indented_kv (boundary mock)
//     std.render::list_item (boundary mock)
//     std.render::indented_list_item (boundary mock)
//     std.render::text_block (boundary mock)
//     std.render::list_block (boundary mock)
//     std.render::kv_block (boundary mock)
//     std.width::truncate_text (boundary mock)
//     std.unicode::display_width_columns (boundary mock)
//     std.unicode::in_block (boundary mock)
//     std.unicode::char_width (boundary mock)
//     std.unicode::string_display_width (boundary mock)
//     std.symbols::resolve_symbol (boundary mock)
//     std.symbols::symbol_color (boundary mock)
//     std.symbols::ansi_code (boundary mock)
//     param_source_extdeps_gitignore_render_render_category_cat (input mock)
//     param_source_extdeps_gitignore_render_render_gitignore_file_file (input mock)
//     param_source_std_render_span_width_span (input mock)
//     param_source_std_render_constrain_line_line (input mock)
//     param_source_std_render_constrain_line_max_width (input mock)
//     param_source_std_render_constrain_line_tier (input mock)
//     param_source_std_render_config_comment_prefix_format (input mock)
//     param_source_std_render_config_format_header_format (input mock)
//     param_source_std_render_config_format_header_generator (input mock)
//     param_source_std_render_config_format_header_regen_command (input mock)
//     param_source_std_render_indented_kv_level (input mock)
//     param_source_std_render_indented_kv_unit (input mock)
//     param_source_std_render_indented_kv_key (input mock)
//     param_source_std_render_indented_kv_sep (input mock)
//     param_source_std_render_indented_kv_value (input mock)
//     param_source_std_render_indented_list_item_prefix (input mock)
//     param_source_std_render_indented_list_item_value (input mock)
//     param_source_std_unicode_char_width_c (input mock)
//   
//   Observers: 23
//     extdeps.gitignore::filter_categories [terminal] (return: NonEmpty)
//     extdeps.gitignore_render::render_category [terminal] (return: NonEmpty)
//     extdeps.gitignore_render::render_gitignore_file [terminal] (return: NonEmpty)
//     std.render::config_comment_prefix [terminal] (return: IsString)
//     std.render::config_format_header [terminal] (return: IsString)
//     std.render::constrain_frame [terminal] (return: NonEmpty)
//     std.render::constrain_line [terminal] (return: NonEmpty)
//     std.render::format_comment [terminal] (return: IsString)
//     std.render::format_section_header [terminal] (return: IsString)
//     std.render::indented_kv [terminal] (return: IsString)
//     std.render::indented_list_item [terminal] (return: IsString)
//     std.render::kv_block [terminal] (return: NonEmpty)
//     std.render::language_comment_prefix [terminal] (return: IsString)
//     std.render::language_format_header [terminal] (return: IsString)
//     std.render::list_block [terminal] (return: NonEmpty)
//     std.render::span_width [terminal] (return: NonEmpty)
//     std.render::text_block [terminal] (return: NonEmpty)
//     std.symbols::ansi_code [terminal] (return: NonEmpty)
//     std.symbols::resolve_symbol [terminal] (return: NonEmpty)
//     std.symbols::symbol_color [terminal] (return: NonEmpty)
//     std.unicode::char_width [terminal] (return: NonEmpty)
//     std.unicode::in_block [terminal] (return: NonEmpty)
//     std.width::truncate_text [terminal] (return: NonEmpty)
//   
//   Integration tests: 47
//     extdeps.gitignore_render::render_category_header -> extdeps.gitignore_render::render_category (depth 2)
//     extdeps.gitignore_render::render_gitignore_content -> extdeps.gitignore_render::render_gitignore_file (depth 2)
//     param_source_extdeps_gitignore_render_render_category_cat -> extdeps.gitignore_render::render_category (depth 3)
//     param_source_extdeps_gitignore_render_render_gitignore_file_file -> extdeps.gitignore_render::render_gitignore_file (depth 3)
//     param_source_std_render_config_comment_prefix_format -> std.render::config_comment_prefix (depth 4)
//     param_source_std_render_config_comment_prefix_format -> std.render::config_format_header (depth 5)
//     param_source_std_render_config_comment_prefix_format -> std.render::format_comment (depth 4)
//     param_source_std_render_config_comment_prefix_format -> std.render::format_section_header (depth 4)
//     param_source_std_render_config_comment_prefix_format -> std.render::language_comment_prefix (depth 4)
//     param_source_std_render_config_comment_prefix_format -> std.render::language_format_header (depth 5)
//     param_source_std_render_config_format_header_format -> std.render::config_format_header (depth 4)
//     param_source_std_render_config_format_header_format -> std.render::language_format_header (depth 4)
//     param_source_std_render_config_format_header_generator -> std.render::config_format_header (depth 3)
//     param_source_std_render_config_format_header_generator -> std.render::language_format_header (depth 3)
//     param_source_std_render_config_format_header_regen_command -> std.render::config_format_header (depth 3)
//     param_source_std_render_config_format_header_regen_command -> std.render::language_format_header (depth 3)
//     param_source_std_render_constrain_line_line -> std.render::constrain_line (depth 7)
//     param_source_std_render_constrain_line_max_width -> std.render::constrain_line (depth 4)
//     param_source_std_render_constrain_line_tier -> std.render::constrain_line (depth 3)
//     param_source_std_render_indented_kv_key -> std.render::indented_kv (depth 4)
//     param_source_std_render_indented_kv_key -> std.render::indented_list_item (depth 4)
//     param_source_std_render_indented_kv_level -> std.render::indented_kv (depth 3)
//     param_source_std_render_indented_kv_level -> std.render::indented_list_item (depth 3)
//     param_source_std_render_indented_kv_sep -> std.render::indented_kv (depth 4)
//     param_source_std_render_indented_kv_sep -> std.render::indented_list_item (depth 4)
//     param_source_std_render_indented_kv_unit -> std.render::indented_kv (depth 3)
//     param_source_std_render_indented_kv_unit -> std.render::indented_list_item (depth 3)
//     param_source_std_render_indented_kv_value -> std.render::indented_kv (depth 4)
//     param_source_std_render_indented_kv_value -> std.render::indented_list_item (depth 4)
//     param_source_std_render_indented_list_item_prefix -> std.render::indented_list_item (depth 3)
//     param_source_std_render_indented_list_item_value -> std.render::indented_list_item (depth 3)
//     param_source_std_render_span_width_span -> std.render::span_width (depth 4)
//     param_source_std_unicode_char_width_c -> std.unicode::char_width (depth 4)
//     std.render::comment_prefix_for -> std.render::config_comment_prefix (depth 2)
//     std.render::comment_prefix_for -> std.render::config_format_header (depth 3)
//     std.render::comment_prefix_for -> std.render::format_comment (depth 2)
//     std.render::comment_prefix_for -> std.render::format_section_header (depth 2)
//     std.render::comment_prefix_for -> std.render::language_comment_prefix (depth 2)
//     std.render::comment_prefix_for -> std.render::language_format_header (depth 3)
//     std.render::format_header -> std.render::config_format_header (depth 2)
//     std.render::format_header -> std.render::language_format_header (depth 2)
//     std.render::indent_text -> std.render::indented_kv (depth 2)
//     std.render::indent_text -> std.render::indented_list_item (depth 2)
//     std.render::list_item -> std.render::indented_list_item (depth 2)
//     std.render::truncate_spans -> std.render::constrain_line (depth 2)
//     std.unicode::display_width_columns -> std.unicode::char_width (depth 2)
//     std.unicode::string_display_width -> std.render::span_width (depth 2)


use gunbc_exec::{ExecuteConfig, ExecutionMode, execute_dag, lower};
use gunbc_ir::{Value, detect_boundaries};
use gunbc_test::{FermiCost, MockSpec, TestClass, Window, apply_window_inputs, assert_boundary_mockable, assert_chain_outputs, assert_window_outputs, guard_test, window_subdag};
use std::collections::HashMap;

fn mock_spec() -> MockSpec {
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    gunbc_test::auto_mock_spec(&dag, "extdeps-gitignore_render")
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
// 
// Determinism obligations: 60 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'extdeps.gitignore_render::render_category_header': same inputs → same outputs
// - 'extdeps.gitignore_render::render_category': same inputs → same outputs
// - 'extdeps.gitignore_render::render_gitignore_content': same inputs → same outputs
// - 'extdeps.gitignore_render::render_gitignore_file': same inputs → same outputs
// - 'extdeps.gitignore::filter_categories': same inputs → same outputs
// - 'std.render::span_width': same inputs → same outputs
// - 'std.render::truncate_spans': same inputs → same outputs
// - 'std.render::constrain_line': same inputs → same outputs
// - 'std.render::constrain_frame': same inputs → same outputs
// - 'std.render::comment_prefix_for': same inputs → same outputs
// - 'std.render::config_comment_prefix': same inputs → same outputs
// - 'std.render::language_comment_prefix': same inputs → same outputs
// - 'std.render::format_header': same inputs → same outputs
// - 'std.render::config_format_header': same inputs → same outputs
// - 'std.render::language_format_header': same inputs → same outputs
// - 'std.render::format_comment': same inputs → same outputs
// - 'std.render::format_section_header': same inputs → same outputs
// - 'std.render::indent_text': same inputs → same outputs
// - 'std.render::kv_pair': same inputs → same outputs
// - 'std.render::indented_kv': same inputs → same outputs
// - 'std.render::list_item': same inputs → same outputs
// - 'std.render::indented_list_item': same inputs → same outputs
// - 'std.render::text_block': same inputs → same outputs
// - 'std.render::list_block': same inputs → same outputs
// - 'std.render::kv_block': same inputs → same outputs
// - 'std.width::truncate_text': same inputs → same outputs
// - 'std.unicode::display_width_columns': same inputs → same outputs
// - 'std.unicode::in_block': same inputs → same outputs
// - 'std.unicode::char_display_width': same inputs → same outputs
// - 'std.unicode::char_width': same inputs → same outputs
// - 'std.unicode::string_display_width': same inputs → same outputs
// - 'std.symbols::resolve_symbol': same inputs → same outputs
// - 'std.symbols::symbol_color': same inputs → same outputs
// - 'std.symbols::ansi_code': same inputs → same outputs
// - 'param_source_extdeps_gitignore_render_render_category_cat': same inputs → same outputs
// - 'param_source_extdeps_gitignore_render_render_gitignore_file_file': same inputs → same outputs
// - 'get_field_std_render_span_width_span_text_arg_0': same inputs → same outputs
// - 'param_source_std_render_span_width_span': same inputs → same outputs
// - 'get_field_std_render_constrain_line_line_spans_arg_0': same inputs → same outputs
// - 'param_source_std_render_constrain_line_line': same inputs → same outputs
// - 'param_source_std_render_constrain_line_max_width': same inputs → same outputs
// - 'get_field_std_render_constrain_line_line_indent_arg_1_rhs_lhs': same inputs → same outputs
// - 'literal_source_std_render_constrain_line_budget_rhs_rhs_arg_1_rhs_rhs': same inputs → same outputs
// - 'binary_op_std_render_constrain_line_budget_rhs_arg_1_rhs': same inputs → same outputs
// - 'binary_op_std_render_constrain_line_budget_arg_1': same inputs → same outputs
// - 'param_source_std_render_constrain_line_tier': same inputs → same outputs
// - 'get_field_std_render_config_comment_prefix_format_comment_arg_0': same inputs → same outputs
// - 'param_source_std_render_config_comment_prefix_format': same inputs → same outputs
// - 'get_field_std_render_config_format_header_format_comment_arg_0': same inputs → same outputs
// - 'param_source_std_render_config_format_header_format': same inputs → same outputs
// - 'param_source_std_render_config_format_header_generator': same inputs → same outputs
// - 'param_source_std_render_config_format_header_regen_command': same inputs → same outputs
// - 'param_source_std_render_indented_kv_level': same inputs → same outputs
// - 'param_source_std_render_indented_kv_unit': same inputs → same outputs
// - 'param_source_std_render_indented_kv_key': same inputs → same outputs
// - 'param_source_std_render_indented_kv_sep': same inputs → same outputs
// - 'param_source_std_render_indented_kv_value': same inputs → same outputs
// - 'param_source_std_render_indented_list_item_prefix': same inputs → same outputs
// - 'param_source_std_render_indented_list_item_value': same inputs → same outputs
// - 'param_source_std_unicode_char_width_c': same inputs → same outputs

/// DryRun execution completes without crash.
/// 
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    if !guard_test("test_dryrun_completion", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("DryRun execution should complete without crash");
    assert!(!log.entries.is_empty(), "execution should produce log entries");
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 5 edge predicate entailment obligations (Unknown).
// Full entailment tests require contract tower witnesses (Tier 3 infra).
// For now, these are documented as obligations:
// - get_field_std_render_span_width_span_text_arg_0.result → std.unicode::string_display_width.s: Edge get_field_std_render_span_width_span_text_arg_0.result → std.unicode::string_display_width.s: predicate entailment unknown (source is 'Any' but target 'String' has specific constraints)
// - get_field_std_render_constrain_line_line_spans_arg_0.result → std.render::truncate_spans.spans: Edge get_field_std_render_constrain_line_line_spans_arg_0.result → std.render::truncate_spans.spans: predicate entailment unknown (source is 'Any' but target 'List<Span>' has specific constraints)
// - binary_op_std_render_constrain_line_budget_arg_1.result → std.render::truncate_spans.budget: Edge binary_op_std_render_constrain_line_budget_arg_1.result → std.render::truncate_spans.budget: predicate entailment unknown (source is 'Any' but target 'Int' has specific constraints)
// - get_field_std_render_config_comment_prefix_format_comment_arg_0.result → std.render::comment_prefix_for.syntax: Edge get_field_std_render_config_comment_prefix_format_comment_arg_0.result → std.render::comment_prefix_for.syntax: predicate entailment unknown (source is 'Any' but target 'CommentSyntax' has specific constraints)
// - get_field_std_render_config_format_header_format_comment_arg_0.result → std.render::format_header.syntax: Edge get_field_std_render_config_format_header_format_comment_arg_0.result → std.render::format_header.syntax: predicate entailment unknown (source is 'Any' but target 'CommentSyntax' has specific constraints)
// 
// 59 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'extdeps.gitignore_render::render_category_header': valid inputs → valid outputs
// - 'extdeps.gitignore_render::render_category': valid inputs → valid outputs
// - 'extdeps.gitignore_render::render_gitignore_content': valid inputs → valid outputs
// - 'extdeps.gitignore_render::render_gitignore_file': valid inputs → valid outputs
// - 'extdeps.gitignore::filter_categories': valid inputs → valid outputs
// - 'std.render::span_width': valid inputs → valid outputs
// - 'std.render::truncate_spans': valid inputs → valid outputs
// - 'std.render::constrain_line': valid inputs → valid outputs
// - 'std.render::constrain_frame': valid inputs → valid outputs
// - 'std.render::comment_prefix_for': valid inputs → valid outputs
// - 'std.render::config_comment_prefix': valid inputs → valid outputs
// - 'std.render::language_comment_prefix': valid inputs → valid outputs
// - 'std.render::format_header': valid inputs → valid outputs
// - 'std.render::config_format_header': valid inputs → valid outputs
// - 'std.render::language_format_header': valid inputs → valid outputs
// - 'std.render::format_comment': valid inputs → valid outputs
// - 'std.render::format_section_header': valid inputs → valid outputs
// - 'std.render::indent_text': valid inputs → valid outputs
// - 'std.render::kv_pair': valid inputs → valid outputs
// - 'std.render::indented_kv': valid inputs → valid outputs
// - 'std.render::list_item': valid inputs → valid outputs
// - 'std.render::indented_list_item': valid inputs → valid outputs
// - 'std.render::text_block': valid inputs → valid outputs
// - 'std.render::list_block': valid inputs → valid outputs
// - 'std.render::kv_block': valid inputs → valid outputs
// - 'std.width::truncate_text': valid inputs → valid outputs
// - 'std.unicode::display_width_columns': valid inputs → valid outputs
// - 'std.unicode::in_block': valid inputs → valid outputs
// - 'std.unicode::char_display_width': valid inputs → valid outputs
// - 'std.unicode::char_width': valid inputs → valid outputs
// - 'std.unicode::string_display_width': valid inputs → valid outputs
// - 'std.symbols::resolve_symbol': valid inputs → valid outputs
// - 'std.symbols::symbol_color': valid inputs → valid outputs
// - 'std.symbols::ansi_code': valid inputs → valid outputs
// - 'param_source_extdeps_gitignore_render_render_category_cat': valid inputs → valid outputs
// - 'param_source_extdeps_gitignore_render_render_gitignore_file_file': valid inputs → valid outputs
// - 'get_field_std_render_span_width_span_text_arg_0': valid inputs → valid outputs
// - 'param_source_std_render_span_width_span': valid inputs → valid outputs
// - 'get_field_std_render_constrain_line_line_spans_arg_0': valid inputs → valid outputs
// - 'param_source_std_render_constrain_line_line': valid inputs → valid outputs
// - 'param_source_std_render_constrain_line_max_width': valid inputs → valid outputs
// - 'get_field_std_render_constrain_line_line_indent_arg_1_rhs_lhs': valid inputs → valid outputs
// - 'binary_op_std_render_constrain_line_budget_rhs_arg_1_rhs': valid inputs → valid outputs
// - 'binary_op_std_render_constrain_line_budget_arg_1': valid inputs → valid outputs
// - 'param_source_std_render_constrain_line_tier': valid inputs → valid outputs
// - 'get_field_std_render_config_comment_prefix_format_comment_arg_0': valid inputs → valid outputs
// - 'param_source_std_render_config_comment_prefix_format': valid inputs → valid outputs
// - 'get_field_std_render_config_format_header_format_comment_arg_0': valid inputs → valid outputs
// - 'param_source_std_render_config_format_header_format': valid inputs → valid outputs
// - 'param_source_std_render_config_format_header_generator': valid inputs → valid outputs
// - 'param_source_std_render_config_format_header_regen_command': valid inputs → valid outputs
// - 'param_source_std_render_indented_kv_level': valid inputs → valid outputs
// - 'param_source_std_render_indented_kv_unit': valid inputs → valid outputs
// - 'param_source_std_render_indented_kv_key': valid inputs → valid outputs
// - 'param_source_std_render_indented_kv_sep': valid inputs → valid outputs
// - 'param_source_std_render_indented_kv_value': valid inputs → valid outputs
// - 'param_source_std_render_indented_list_item_prefix': valid inputs → valid outputs
// - 'param_source_std_render_indented_list_item_value': valid inputs → valid outputs
// - 'param_source_std_unicode_char_width_c': valid inputs → valid outputs
// 39 optional input handling obligations.
// Optional inputs must accept missing values and reject wrong-typed inputs.
// Optional input tests skipped for 'std.render::truncate_spans': std.render::truncate_spans.__out:return (type: List<Span>).

/// Cardinality coverage: extdeps.gitignore::filter_categories.return with 0 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port extdeps.gitignore::filter_categories.return.
#[test]
fn test_cardinality_extdeps_gitignore_filter_categories_return_empty_0() {
    if !guard_test("test_cardinality_extdeps_gitignore_filter_categories_return_empty_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore::filter_categories", "return", Value::List(vec![]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: extdeps.gitignore::filter_categories.return with 1 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=1 (one) for boundary port extdeps.gitignore::filter_categories.return.
#[test]
fn test_cardinality_extdeps_gitignore_filter_categories_return_one_1() {
    if !guard_test("test_cardinality_extdeps_gitignore_filter_categories_return_one_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore::filter_categories", "return", Value::List(vec![Value::Json(serde_json::json!(null))]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: extdeps.gitignore::filter_categories.return with 3 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=3 (many) for boundary port extdeps.gitignore::filter_categories.return.
#[test]
fn test_cardinality_extdeps_gitignore_filter_categories_return_many_3() {
    if !guard_test("test_cardinality_extdeps_gitignore_filter_categories_return_many_3", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore::filter_categories", "return", Value::List(vec![Value::Json(serde_json::json!(null)), Value::Json(serde_json::json!(null)), Value::Json(serde_json::json!(null))]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=3 (many) should not crash");
}

/// Cardinality coverage: std.render::truncate_spans.return with 0 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=0 (empty) for boundary port std.render::truncate_spans.return.
#[test]
fn test_cardinality_std_render_truncate_spans_return_empty_0() {
    if !guard_test("test_cardinality_std_render_truncate_spans_return_empty_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::truncate_spans", "return", Value::List(vec![]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=0 (empty) should not crash");
}

/// Cardinality coverage: std.render::truncate_spans.return with 1 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=1 (one) for boundary port std.render::truncate_spans.return.
#[test]
fn test_cardinality_std_render_truncate_spans_return_one_1() {
    if !guard_test("test_cardinality_std_render_truncate_spans_return_one_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::truncate_spans", "return", Value::List(vec![Value::Json(serde_json::json!(null))]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=1 (one) should not crash");
}

/// Cardinality coverage: std.render::truncate_spans.return with 3 element(s) (cardinality: 0..*).
/// 
/// Proves: DAG handles count=3 (many) for boundary port std.render::truncate_spans.return.
#[test]
fn test_cardinality_std_render_truncate_spans_return_many_3() {
    if !guard_test("test_cardinality_std_render_truncate_spans_return_many_3", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::truncate_spans", "return", Value::List(vec![Value::Json(serde_json::json!(null)), Value::Json(serde_json::json!(null)), Value::Json(serde_json::json!(null))]));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("cardinality count=3 (many) should not crash");
}

/// Coercion coverage: extdeps.gitignore_render::render_category_header.return 1 → extdeps.gitignore_render::render_category.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at extdeps.gitignore_render::render_category.__deps.
#[test]
fn test_coercion_extdeps_gitignore_render_render_category_header_return_extdeps_gitignore_render_render_category_deps() {
    if !guard_test("test_coercion_extdeps_gitignore_render_render_category_header_return_extdeps_gitignore_render_render_category_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.gitignore_render::render_category_header.return → extdeps.gitignore_render::render_category.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "extdeps.gitignore_render::render_category").expect("coercion target entry missing for extdeps.gitignore_render::render_category.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target extdeps.gitignore_render::render_category.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target extdeps.gitignore_render::render_category.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to extdeps.gitignore_render::render_category.__deps; got {:?}", received);
}

/// Coercion coverage: extdeps.gitignore_render::render_gitignore_content.return 1 → extdeps.gitignore_render::render_gitignore_file.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at extdeps.gitignore_render::render_gitignore_file.__deps.
#[test]
fn test_coercion_extdeps_gitignore_render_render_gitignore_content_return_extdeps_gitignore_render_render_gitignore_file_deps() {
    if !guard_test("test_coercion_extdeps_gitignore_render_render_gitignore_content_return_extdeps_gitignore_render_render_gitignore_file_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.gitignore_render::render_gitignore_content.return → extdeps.gitignore_render::render_gitignore_file.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "extdeps.gitignore_render::render_gitignore_file").expect("coercion target entry missing for extdeps.gitignore_render::render_gitignore_file.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target extdeps.gitignore_render::render_gitignore_file.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target extdeps.gitignore_render::render_gitignore_file.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to extdeps.gitignore_render::render_gitignore_file.__deps; got {:?}", received);
}

/// Coercion coverage: std.unicode::string_display_width.return 1 → std.render::span_width.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::span_width.__deps.
#[test]
fn test_coercion_std_unicode_string_display_width_return_std_render_span_width_deps() {
    if !guard_test("test_coercion_std_unicode_string_display_width_return_std_render_span_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.unicode::string_display_width.return → std.render::span_width.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::span_width").expect("coercion target entry missing for std.render::span_width.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::span_width.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::span_width.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::span_width.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::comment_prefix_for.return 1 → std.render::config_comment_prefix.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::config_comment_prefix.__deps.
#[test]
fn test_coercion_std_render_comment_prefix_for_return_std_render_config_comment_prefix_deps() {
    if !guard_test("test_coercion_std_render_comment_prefix_for_return_std_render_config_comment_prefix_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::comment_prefix_for.return → std.render::config_comment_prefix.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::config_comment_prefix").expect("coercion target entry missing for std.render::config_comment_prefix.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::config_comment_prefix.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::config_comment_prefix.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::config_comment_prefix.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::comment_prefix_for.return 1 → std.render::language_comment_prefix.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::language_comment_prefix.__deps.
#[test]
fn test_coercion_std_render_comment_prefix_for_return_std_render_language_comment_prefix_deps() {
    if !guard_test("test_coercion_std_render_comment_prefix_for_return_std_render_language_comment_prefix_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::comment_prefix_for.return → std.render::language_comment_prefix.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::language_comment_prefix").expect("coercion target entry missing for std.render::language_comment_prefix.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::language_comment_prefix.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::language_comment_prefix.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::language_comment_prefix.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::comment_prefix_for.return 1 → std.render::format_header.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::format_header.__deps.
#[test]
fn test_coercion_std_render_comment_prefix_for_return_std_render_format_header_deps() {
    if !guard_test("test_coercion_std_render_comment_prefix_for_return_std_render_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::comment_prefix_for.return → std.render::format_header.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::format_header").expect("coercion target entry missing for std.render::format_header.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::format_header.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::format_header.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::format_header.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::format_header.return 1 → std.render::config_format_header.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::config_format_header.__deps.
#[test]
fn test_coercion_std_render_format_header_return_std_render_config_format_header_deps() {
    if !guard_test("test_coercion_std_render_format_header_return_std_render_config_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::format_header.return → std.render::config_format_header.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::config_format_header").expect("coercion target entry missing for std.render::config_format_header.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::config_format_header.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::config_format_header.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::config_format_header.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::format_header.return 1 → std.render::language_format_header.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::language_format_header.__deps.
#[test]
fn test_coercion_std_render_format_header_return_std_render_language_format_header_deps() {
    if !guard_test("test_coercion_std_render_format_header_return_std_render_language_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::format_header.return → std.render::language_format_header.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::language_format_header").expect("coercion target entry missing for std.render::language_format_header.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::language_format_header.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::language_format_header.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::language_format_header.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::comment_prefix_for.return 1 → std.render::format_comment.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::format_comment.__deps.
#[test]
fn test_coercion_std_render_comment_prefix_for_return_std_render_format_comment_deps() {
    if !guard_test("test_coercion_std_render_comment_prefix_for_return_std_render_format_comment_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::comment_prefix_for.return → std.render::format_comment.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::format_comment").expect("coercion target entry missing for std.render::format_comment.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::format_comment.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::format_comment.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::format_comment.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::comment_prefix_for.return 1 → std.render::format_section_header.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::format_section_header.__deps.
#[test]
fn test_coercion_std_render_comment_prefix_for_return_std_render_format_section_header_deps() {
    if !guard_test("test_coercion_std_render_comment_prefix_for_return_std_render_format_section_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::comment_prefix_for.return → std.render::format_section_header.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::format_section_header").expect("coercion target entry missing for std.render::format_section_header.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::format_section_header.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::format_section_header.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::format_section_header.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::indent_text.return 1 → std.render::indented_kv.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::indented_kv.__deps.
#[test]
fn test_coercion_std_render_indent_text_return_std_render_indented_kv_deps() {
    if !guard_test("test_coercion_std_render_indent_text_return_std_render_indented_kv_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::indent_text.return → std.render::indented_kv.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::indented_kv").expect("coercion target entry missing for std.render::indented_kv.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::indented_kv.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::indented_kv.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::indented_kv.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::kv_pair.return 1 → std.render::indented_kv.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::indented_kv.__deps.
#[test]
fn test_coercion_std_render_kv_pair_return_std_render_indented_kv_deps() {
    if !guard_test("test_coercion_std_render_kv_pair_return_std_render_indented_kv_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::kv_pair.return → std.render::indented_kv.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::indented_kv").expect("coercion target entry missing for std.render::indented_kv.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::indented_kv.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::indented_kv.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::indented_kv.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::indent_text.return 1 → std.render::indented_list_item.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::indented_list_item.__deps.
#[test]
fn test_coercion_std_render_indent_text_return_std_render_indented_list_item_deps() {
    if !guard_test("test_coercion_std_render_indent_text_return_std_render_indented_list_item_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::indent_text.return → std.render::indented_list_item.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::indented_list_item").expect("coercion target entry missing for std.render::indented_list_item.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::indented_list_item.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::indented_list_item.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::indented_list_item.__deps; got {:?}", received);
}

/// Coercion coverage: std.render::list_item.return 1 → std.render::indented_list_item.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::indented_list_item.__deps.
#[test]
fn test_coercion_std_render_list_item_return_std_render_indented_list_item_deps() {
    if !guard_test("test_coercion_std_render_list_item_return_std_render_indented_list_item_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.render::list_item.return → std.render::indented_list_item.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::indented_list_item").expect("coercion target entry missing for std.render::indented_list_item.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::indented_list_item.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.render::indented_list_item.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::indented_list_item.__deps; got {:?}", received);
}

/// Coercion coverage: std.unicode::char_display_width.return 1 → std.unicode::char_width.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.unicode::char_width.__deps.
#[test]
fn test_coercion_std_unicode_char_display_width_return_std_unicode_char_width_deps() {
    if !guard_test("test_coercion_std_unicode_char_display_width_return_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.unicode::char_display_width.return → std.unicode::char_width.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.unicode::char_width").expect("coercion target entry missing for std.unicode::char_width.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.unicode::char_width.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.unicode::char_width.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.unicode::char_width.__deps; got {:?}", received);
}

/// Coercion coverage: std.unicode::display_width_columns.return 1 → std.unicode::char_width.__deps 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.unicode::char_width.__deps.
#[test]
fn test_coercion_std_unicode_display_width_columns_return_std_unicode_char_width_deps() {
    if !guard_test("test_coercion_std_unicode_display_width_columns_return_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at std.unicode::display_width_columns.return → std.unicode::char_width.__deps should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.unicode::char_width").expect("coercion target entry missing for std.unicode::char_width.__deps");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.unicode::char_width.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect("coercion target std.unicode::char_width.__deps should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.unicode::char_width.__deps; got {:?}", received);
}

/// Coercion coverage: get_field_std_render_constrain_line_line_spans_arg_0.result 1 → std.render::truncate_spans.spans 0..* (WrapScalar).
/// 
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at std.render::truncate_spans.spans.
#[test]
fn test_coercion_get_field_std_render_constrain_line_line_spans_arg_0_result_std_render_truncate_spans_spans() {
    if !guard_test("test_coercion_get_field_std_render_constrain_line_line_spans_arg_0_result_std_render_truncate_spans_spans", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at get_field_std_render_constrain_line_line_spans_arg_0.result → std.render::truncate_spans.spans should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "std.render::truncate_spans").expect("coercion target entry missing for std.render::truncate_spans.spans");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target std.render::truncate_spans.spans should capture inputs");
    let received = target_inputs.get("spans").expect("coercion target std.render::truncate_spans.spans should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to std.render::truncate_spans.spans; got {:?}", received);
}

/// Variant coverage: std.unicode::in_block.return with variant 'True' of type Bool.
/// 
/// Proves: DAG handles variant 'True' at boundary port std.unicode::in_block.return.
#[test]
fn test_variant_std_unicode_in_block_return_true() {
    if !guard_test("test_variant_std_unicode_in_block_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("True".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for std.unicode::in_block.return should not crash");
}

/// Variant coverage: std.unicode::in_block.return with variant 'False' of type Bool.
/// 
/// Proves: DAG handles variant 'False' at boundary port std.unicode::in_block.return.
#[test]
fn test_variant_std_unicode_in_block_return_false() {
    if !guard_test("test_variant_std_unicode_in_block_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("False".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for std.unicode::in_block.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Default' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Default' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_default() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_default", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Default".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Default for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Success' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Success' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_success() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_success", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Success".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Success for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Warning' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Warning' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_warning() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_warning", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Warning".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Warning for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Error' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Error' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_error() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_error", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Error".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Error for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Info' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Info' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_info() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_info", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Info".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Info for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Dim' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Dim' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_dim() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_dim", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Dim".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Dim for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Active' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Active' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_active() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_active", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Active".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Active for std.symbols::symbol_color.return should not crash");
}

/// Variant coverage: std.symbols::symbol_color.return with variant 'Accent' of type SemanticColor.
/// 
/// Proves: DAG handles variant 'Accent' at boundary port std.symbols::symbol_color.return.
#[test]
fn test_variant_std_symbols_symbol_color_return_accent() {
    if !guard_test("test_variant_std_symbols_symbol_color_return_accent", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Accent".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant Accent for std.symbols::symbol_color.return should not crash");
}

/// Optional input: extdeps.gitignore_render::render_category_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_render_render_category_header_deps() {
    if !guard_test("test_optional_missing_extdeps_gitignore_render_render_category_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("cat".to_string(), Value::Str("<GitignoreCategory>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_category_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore_render::render_category_header.__deps missing should not error");
}

/// Optional input: extdeps.gitignore_render::render_category.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_render_render_category_deps() {
    if !guard_test("test_optional_missing_extdeps_gitignore_render_render_category_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("cat".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_category", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore_render::render_category.__deps missing should not error");
}

/// Optional input: extdeps.gitignore_render::render_gitignore_content.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_render_render_gitignore_content_deps() {
    if !guard_test("test_optional_missing_extdeps_gitignore_render_render_gitignore_content_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("file".to_string(), Value::Str("<GitignoreFile>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_gitignore_content", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore_render::render_gitignore_content.__deps missing should not error");
}

/// Optional input: extdeps.gitignore_render::render_gitignore_file.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_render_render_gitignore_file_deps() {
    if !guard_test("test_optional_missing_extdeps_gitignore_render_render_gitignore_file_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("file".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_gitignore_file", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore_render::render_gitignore_file.__deps missing should not error");
}

/// Optional input: extdeps.gitignore::filter_categories.categories (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_filter_categories_categories() {
    if !guard_test("test_optional_missing_extdeps_gitignore_filter_categories_categories", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("build_system".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore::filter_categories", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore::filter_categories.categories missing should not error");
}

/// Optional input: extdeps.gitignore::filter_categories.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_extdeps_gitignore_filter_categories_deps() {
    if !guard_test("test_optional_missing_extdeps_gitignore_filter_categories_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("build_system".to_string(), Value::Str("example".to_string()));
    inputs.insert("categories".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore::filter_categories", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input extdeps.gitignore::filter_categories.__deps missing should not error");
}

/// Optional input: std.render::span_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_span_width_deps() {
    if !guard_test("test_optional_missing_std_render_span_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("span".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::span_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::span_width.__deps missing should not error");
}

/// Optional input: std.render::constrain_line.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_constrain_line_deps() {
    if !guard_test("test_optional_missing_std_render_constrain_line_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("line".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::constrain_line", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::constrain_line.__deps missing should not error");
}

/// Optional input: std.render::constrain_frame.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_constrain_frame_deps() {
    if !guard_test("test_optional_missing_std_render_constrain_frame_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("frame".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    inputs.insert("viewport".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::constrain_frame", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::constrain_frame.__deps missing should not error");
}

/// Optional input: std.render::comment_prefix_for.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_comment_prefix_for_deps() {
    if !guard_test("test_optional_missing_std_render_comment_prefix_for_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Str("<CommentSyntax>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::comment_prefix_for", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::comment_prefix_for.__deps missing should not error");
}

/// Optional input: std.render::config_comment_prefix.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_config_comment_prefix_deps() {
    if !guard_test("test_optional_missing_std_render_config_comment_prefix_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("format".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::config_comment_prefix", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::config_comment_prefix.__deps missing should not error");
}

/// Optional input: std.render::language_comment_prefix.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_language_comment_prefix_deps() {
    if !guard_test("test_optional_missing_std_render_language_comment_prefix_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("lang".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::language_comment_prefix", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::language_comment_prefix.__deps missing should not error");
}

/// Optional input: std.render::format_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_format_header_deps() {
    if !guard_test("test_optional_missing_std_render_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Str("<CommentSyntax>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::format_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::format_header.__deps missing should not error");
}

/// Optional input: std.render::config_format_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_config_format_header_deps() {
    if !guard_test("test_optional_missing_std_render_config_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("format".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::config_format_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::config_format_header.__deps missing should not error");
}

/// Optional input: std.render::language_format_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_language_format_header_deps() {
    if !guard_test("test_optional_missing_std_render_language_format_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("lang".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::language_format_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::language_format_header.__deps missing should not error");
}

/// Optional input: std.render::format_comment.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_format_comment_deps() {
    if !guard_test("test_optional_missing_std_render_format_comment_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::format_comment", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::format_comment.__deps missing should not error");
}

/// Optional input: std.render::format_section_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_format_section_header_deps() {
    if !guard_test("test_optional_missing_std_render_format_section_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("title".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::format_section_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::format_section_header.__deps missing should not error");
}

/// Optional input: std.render::indent_text.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_indent_text_deps() {
    if !guard_test("test_optional_missing_std_render_indent_text_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::indent_text", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::indent_text.__deps missing should not error");
}

/// Optional input: std.render::kv_pair.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_kv_pair_deps() {
    if !guard_test("test_optional_missing_std_render_kv_pair_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("key".to_string(), Value::Str("example".to_string()));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::kv_pair", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::kv_pair.__deps missing should not error");
}

/// Optional input: std.render::indented_kv.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_indented_kv_deps() {
    if !guard_test("test_optional_missing_std_render_indented_kv_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("key".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::indented_kv", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::indented_kv.__deps missing should not error");
}

/// Optional input: std.render::list_item.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_list_item_deps() {
    if !guard_test("test_optional_missing_std_render_list_item_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::list_item", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::list_item.__deps missing should not error");
}

/// Optional input: std.render::indented_list_item.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_indented_list_item_deps() {
    if !guard_test("test_optional_missing_std_render_indented_list_item_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::indented_list_item", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::indented_list_item.__deps missing should not error");
}

/// Optional input: std.render::text_block.items (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_text_block_items() {
    if !guard_test("test_optional_missing_std_render_text_block_items", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::text_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::text_block.items missing should not error");
}

/// Optional input: std.render::text_block.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_text_block_deps() {
    if !guard_test("test_optional_missing_std_render_text_block_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("items".to_string(), Value::List(vec![Value::Str("example".to_string())]));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::text_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::text_block.__deps missing should not error");
}

/// Optional input: std.render::list_block.items (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_list_block_items() {
    if !guard_test("test_optional_missing_std_render_list_block_items", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::list_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::list_block.items missing should not error");
}

/// Optional input: std.render::list_block.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_list_block_deps() {
    if !guard_test("test_optional_missing_std_render_list_block_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("items".to_string(), Value::List(vec![Value::Str("example".to_string())]));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::list_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::list_block.__deps missing should not error");
}

/// Optional input: std.render::kv_block.entries (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_kv_block_entries() {
    if !guard_test("test_optional_missing_std_render_kv_block_entries", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::kv_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::kv_block.entries missing should not error");
}

/// Optional input: std.render::kv_block.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_render_kv_block_deps() {
    if !guard_test("test_optional_missing_std_render_kv_block_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("entries".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.render::kv_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.render::kv_block.__deps missing should not error");
}

/// Optional input: std.width::truncate_text.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_width_truncate_text_deps() {
    if !guard_test("test_optional_missing_std_width_truncate_text_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.width::truncate_text", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.width::truncate_text.__deps missing should not error");
}

/// Optional input: std.unicode::display_width_columns.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_display_width_columns_deps() {
    if !guard_test("test_optional_missing_std_unicode_display_width_columns_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("w".to_string(), Value::Str("<DisplayWidth>".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::display_width_columns", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::display_width_columns.__deps missing should not error");
}

/// Optional input: std.unicode::in_block.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_in_block_deps() {
    if !guard_test("test_optional_missing_std_unicode_in_block_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("block".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("cp".to_string(), Value::Int(1));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::in_block", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::in_block.__deps missing should not error");
}

/// Optional input: std.unicode::char_display_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_char_display_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_char_display_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("<DisplayWidth>".to_string()));
    inputs.insert("c".to_string(), Value::Int(1));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_display_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::char_display_width.__deps missing should not error");
}

/// Optional input: std.unicode::char_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_char_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_char_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("c".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::char_width.__deps missing should not error");
}

/// Optional input: std.unicode::string_display_width.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_unicode_string_display_width_deps() {
    if !guard_test("test_optional_missing_std_unicode_string_display_width_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("s".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::string_display_width", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.unicode::string_display_width.__deps missing should not error");
}

/// Optional input: std.symbols::resolve_symbol.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_symbols_resolve_symbol_deps() {
    if !guard_test("test_optional_missing_std_symbols_resolve_symbol_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("id".to_string(), Value::Str("NodePending".to_string()));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::resolve_symbol", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.symbols::resolve_symbol.__deps missing should not error");
}

/// Optional input: std.symbols::symbol_color.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_symbols_symbol_color_deps() {
    if !guard_test("test_optional_missing_std_symbols_symbol_color_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("Default".to_string()));
    inputs.insert("id".to_string(), Value::Str("NodePending".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::symbol_color", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.symbols::symbol_color.__deps missing should not error");
}

/// Optional input: std.symbols::ansi_code.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_symbols_ansi_code_deps() {
    if !guard_test("test_optional_missing_std_symbols_ansi_code_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("c".to_string(), Value::Str("Default".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::ansi_code", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.symbols::ansi_code.__deps missing should not error");
}

// =========================================================================
// Boundary Tests (per-node mockability)
// =========================================================================

/// Test that all boundaries can be mocked.
#[test]
fn test_boundaries_mockable() {
    if !guard_test("test_boundaries_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(result.is_ok(), "Boundaries should be mockable: {:?}", result.error);
}

/// Test that extdeps.gitignore::filter_categories boundary can be mocked.
#[test]
fn test_boundary_extdeps_gitignore_filter_categories_mockable() {
    if !guard_test("test_boundary_extdeps_gitignore_filter_categories_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"extdeps.gitignore::filter_categories".into()), "extdeps.gitignore::filter_categories should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore::filter_categories", "return", Value::List(vec![Value::Str("mock".to_string())]));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("extdeps.gitignore::filter_categories").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that extdeps.gitignore_render::render_category boundary can be mocked.
#[test]
fn test_boundary_extdeps_gitignore_render_render_category_mockable() {
    if !guard_test("test_boundary_extdeps_gitignore_render_render_category_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"extdeps.gitignore_render::render_category".into()), "extdeps.gitignore_render::render_category should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore_render::render_category", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("extdeps.gitignore_render::render_category").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that extdeps.gitignore_render::render_category_header boundary can be mocked.
#[test]
fn test_boundary_extdeps_gitignore_render_render_category_header_mockable() {
    if !guard_test("test_boundary_extdeps_gitignore_render_render_category_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"extdeps.gitignore_render::render_category_header".into()), "extdeps.gitignore_render::render_category_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore_render::render_category_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("extdeps.gitignore_render::render_category_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that extdeps.gitignore_render::render_gitignore_content boundary can be mocked.
#[test]
fn test_boundary_extdeps_gitignore_render_render_gitignore_content_mockable() {
    if !guard_test("test_boundary_extdeps_gitignore_render_render_gitignore_content_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"extdeps.gitignore_render::render_gitignore_content".into()), "extdeps.gitignore_render::render_gitignore_content should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore_render::render_gitignore_content", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("extdeps.gitignore_render::render_gitignore_content").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that extdeps.gitignore_render::render_gitignore_file boundary can be mocked.
#[test]
fn test_boundary_extdeps_gitignore_render_render_gitignore_file_mockable() {
    if !guard_test("test_boundary_extdeps_gitignore_render_render_gitignore_file_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"extdeps.gitignore_render::render_gitignore_file".into()), "extdeps.gitignore_render::render_gitignore_file should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("extdeps.gitignore_render::render_gitignore_file", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("extdeps.gitignore_render::render_gitignore_file").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::comment_prefix_for boundary can be mocked.
#[test]
fn test_boundary_std_render_comment_prefix_for_mockable() {
    if !guard_test("test_boundary_std_render_comment_prefix_for_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::comment_prefix_for".into()), "std.render::comment_prefix_for should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::comment_prefix_for", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::comment_prefix_for").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::config_comment_prefix boundary can be mocked.
#[test]
fn test_boundary_std_render_config_comment_prefix_mockable() {
    if !guard_test("test_boundary_std_render_config_comment_prefix_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::config_comment_prefix".into()), "std.render::config_comment_prefix should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::config_comment_prefix", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::config_comment_prefix").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::config_format_header boundary can be mocked.
#[test]
fn test_boundary_std_render_config_format_header_mockable() {
    if !guard_test("test_boundary_std_render_config_format_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::config_format_header".into()), "std.render::config_format_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::config_format_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::config_format_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::constrain_frame boundary can be mocked.
#[test]
fn test_boundary_std_render_constrain_frame_mockable() {
    if !guard_test("test_boundary_std_render_constrain_frame_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::constrain_frame".into()), "std.render::constrain_frame should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::constrain_frame", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::constrain_frame").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::constrain_line boundary can be mocked.
#[test]
fn test_boundary_std_render_constrain_line_mockable() {
    if !guard_test("test_boundary_std_render_constrain_line_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::constrain_line".into()), "std.render::constrain_line should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::constrain_line", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::constrain_line").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::format_comment boundary can be mocked.
#[test]
fn test_boundary_std_render_format_comment_mockable() {
    if !guard_test("test_boundary_std_render_format_comment_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::format_comment".into()), "std.render::format_comment should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::format_comment", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::format_comment").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::format_header boundary can be mocked.
#[test]
fn test_boundary_std_render_format_header_mockable() {
    if !guard_test("test_boundary_std_render_format_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::format_header".into()), "std.render::format_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::format_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::format_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::format_section_header boundary can be mocked.
#[test]
fn test_boundary_std_render_format_section_header_mockable() {
    if !guard_test("test_boundary_std_render_format_section_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::format_section_header".into()), "std.render::format_section_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::format_section_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::format_section_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::indent_text boundary can be mocked.
#[test]
fn test_boundary_std_render_indent_text_mockable() {
    if !guard_test("test_boundary_std_render_indent_text_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::indent_text".into()), "std.render::indent_text should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::indent_text", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::indent_text").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::indented_kv boundary can be mocked.
#[test]
fn test_boundary_std_render_indented_kv_mockable() {
    if !guard_test("test_boundary_std_render_indented_kv_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::indented_kv".into()), "std.render::indented_kv should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::indented_kv", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::indented_kv").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::indented_list_item boundary can be mocked.
#[test]
fn test_boundary_std_render_indented_list_item_mockable() {
    if !guard_test("test_boundary_std_render_indented_list_item_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::indented_list_item".into()), "std.render::indented_list_item should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::indented_list_item", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::indented_list_item").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::kv_block boundary can be mocked.
#[test]
fn test_boundary_std_render_kv_block_mockable() {
    if !guard_test("test_boundary_std_render_kv_block_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::kv_block".into()), "std.render::kv_block should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::kv_block", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::kv_block").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::language_comment_prefix boundary can be mocked.
#[test]
fn test_boundary_std_render_language_comment_prefix_mockable() {
    if !guard_test("test_boundary_std_render_language_comment_prefix_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::language_comment_prefix".into()), "std.render::language_comment_prefix should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::language_comment_prefix", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::language_comment_prefix").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::language_format_header boundary can be mocked.
#[test]
fn test_boundary_std_render_language_format_header_mockable() {
    if !guard_test("test_boundary_std_render_language_format_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::language_format_header".into()), "std.render::language_format_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::language_format_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::language_format_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::list_block boundary can be mocked.
#[test]
fn test_boundary_std_render_list_block_mockable() {
    if !guard_test("test_boundary_std_render_list_block_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::list_block".into()), "std.render::list_block should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::list_block", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::list_block").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::list_item boundary can be mocked.
#[test]
fn test_boundary_std_render_list_item_mockable() {
    if !guard_test("test_boundary_std_render_list_item_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::list_item".into()), "std.render::list_item should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::list_item", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::list_item").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::span_width boundary can be mocked.
#[test]
fn test_boundary_std_render_span_width_mockable() {
    if !guard_test("test_boundary_std_render_span_width_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::span_width".into()), "std.render::span_width should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::span_width", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::span_width").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::text_block boundary can be mocked.
#[test]
fn test_boundary_std_render_text_block_mockable() {
    if !guard_test("test_boundary_std_render_text_block_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::text_block".into()), "std.render::text_block should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::text_block", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::text_block").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.render::truncate_spans boundary can be mocked.
#[test]
fn test_boundary_std_render_truncate_spans_mockable() {
    if !guard_test("test_boundary_std_render_truncate_spans_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.render::truncate_spans".into()), "std.render::truncate_spans should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.render::truncate_spans", "return", Value::List(vec![Value::Str("mock".to_string())]));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.render::truncate_spans").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.symbols::ansi_code boundary can be mocked.
#[test]
fn test_boundary_std_symbols_ansi_code_mockable() {
    if !guard_test("test_boundary_std_symbols_ansi_code_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.symbols::ansi_code".into()), "std.symbols::ansi_code should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::ansi_code", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.symbols::ansi_code").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.symbols::resolve_symbol boundary can be mocked.
#[test]
fn test_boundary_std_symbols_resolve_symbol_mockable() {
    if !guard_test("test_boundary_std_symbols_resolve_symbol_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.symbols::resolve_symbol".into()), "std.symbols::resolve_symbol should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::resolve_symbol", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.symbols::resolve_symbol").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.symbols::symbol_color boundary can be mocked.
#[test]
fn test_boundary_std_symbols_symbol_color_mockable() {
    if !guard_test("test_boundary_std_symbols_symbol_color_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.symbols::symbol_color".into()), "std.symbols::symbol_color should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.symbols::symbol_color", "return", Value::Str("Default".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.symbols::symbol_color").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::char_width boundary can be mocked.
#[test]
fn test_boundary_std_unicode_char_width_mockable() {
    if !guard_test("test_boundary_std_unicode_char_width_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::char_width".into()), "std.unicode::char_width should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::char_width", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::char_width").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::display_width_columns boundary can be mocked.
#[test]
fn test_boundary_std_unicode_display_width_columns_mockable() {
    if !guard_test("test_boundary_std_unicode_display_width_columns_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::display_width_columns".into()), "std.unicode::display_width_columns should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::display_width_columns", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::display_width_columns").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::in_block boundary can be mocked.
#[test]
fn test_boundary_std_unicode_in_block_mockable() {
    if !guard_test("test_boundary_std_unicode_in_block_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::in_block".into()), "std.unicode::in_block should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::in_block", "return", Value::Str("True".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::in_block").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.unicode::string_display_width boundary can be mocked.
#[test]
fn test_boundary_std_unicode_string_display_width_mockable() {
    if !guard_test("test_boundary_std_unicode_string_display_width_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.unicode::string_display_width".into()), "std.unicode::string_display_width should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.unicode::string_display_width", "return", Value::Int(1));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.unicode::string_display_width").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.width::truncate_text boundary can be mocked.
#[test]
fn test_boundary_std_width_truncate_text_mockable() {
    if !guard_test("test_boundary_std_width_truncate_text_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.width::truncate_text".into()), "std.width::truncate_text should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.width::truncate_text", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.width::truncate_text").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

// =========================================================================
// Chain Validation Tests
// =========================================================================

// These tests verify that mock outputs satisfy downstream input expectations.

/// Test that this tool's mock spec is self-consistent.
#[test]
fn test_mock_spec_self_consistent() {
    if !guard_test("test_mock_spec_self_consistent", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let spec = mock_spec();
    // Verify all boundary mocks are present
    assert!(spec.get_boundary_mock("extdeps.gitignore_render::render_category_header", "return").is_some(), "MockSpec should have boundary mock for extdeps.gitignore_render::render_category_header.return");
    assert!(spec.get_boundary_mock("extdeps.gitignore_render::render_category", "return").is_some(), "MockSpec should have boundary mock for extdeps.gitignore_render::render_category.return");
    assert!(spec.get_boundary_mock("extdeps.gitignore_render::render_gitignore_content", "return").is_some(), "MockSpec should have boundary mock for extdeps.gitignore_render::render_gitignore_content.return");
    assert!(spec.get_boundary_mock("extdeps.gitignore_render::render_gitignore_file", "return").is_some(), "MockSpec should have boundary mock for extdeps.gitignore_render::render_gitignore_file.return");
    assert!(spec.get_boundary_mock("extdeps.gitignore::filter_categories", "return").is_some(), "MockSpec should have boundary mock for extdeps.gitignore::filter_categories.return");
    assert!(spec.get_boundary_mock("std.render::span_width", "return").is_some(), "MockSpec should have boundary mock for std.render::span_width.return");
    assert!(spec.get_boundary_mock("std.render::truncate_spans", "return").is_some(), "MockSpec should have boundary mock for std.render::truncate_spans.return");
    assert!(spec.get_boundary_mock("std.render::constrain_line", "return").is_some(), "MockSpec should have boundary mock for std.render::constrain_line.return");
    assert!(spec.get_boundary_mock("std.render::constrain_frame", "return").is_some(), "MockSpec should have boundary mock for std.render::constrain_frame.return");
    assert!(spec.get_boundary_mock("std.render::comment_prefix_for", "return").is_some(), "MockSpec should have boundary mock for std.render::comment_prefix_for.return");
    assert!(spec.get_boundary_mock("std.render::config_comment_prefix", "return").is_some(), "MockSpec should have boundary mock for std.render::config_comment_prefix.return");
    assert!(spec.get_boundary_mock("std.render::language_comment_prefix", "return").is_some(), "MockSpec should have boundary mock for std.render::language_comment_prefix.return");
    assert!(spec.get_boundary_mock("std.render::format_header", "return").is_some(), "MockSpec should have boundary mock for std.render::format_header.return");
    assert!(spec.get_boundary_mock("std.render::config_format_header", "return").is_some(), "MockSpec should have boundary mock for std.render::config_format_header.return");
    assert!(spec.get_boundary_mock("std.render::language_format_header", "return").is_some(), "MockSpec should have boundary mock for std.render::language_format_header.return");
    assert!(spec.get_boundary_mock("std.render::format_comment", "return").is_some(), "MockSpec should have boundary mock for std.render::format_comment.return");
    assert!(spec.get_boundary_mock("std.render::format_section_header", "return").is_some(), "MockSpec should have boundary mock for std.render::format_section_header.return");
    assert!(spec.get_boundary_mock("std.render::indent_text", "return").is_some(), "MockSpec should have boundary mock for std.render::indent_text.return");
    assert!(spec.get_boundary_mock("std.render::indented_kv", "return").is_some(), "MockSpec should have boundary mock for std.render::indented_kv.return");
    assert!(spec.get_boundary_mock("std.render::list_item", "return").is_some(), "MockSpec should have boundary mock for std.render::list_item.return");
    assert!(spec.get_boundary_mock("std.render::indented_list_item", "return").is_some(), "MockSpec should have boundary mock for std.render::indented_list_item.return");
    assert!(spec.get_boundary_mock("std.render::text_block", "return").is_some(), "MockSpec should have boundary mock for std.render::text_block.return");
    assert!(spec.get_boundary_mock("std.render::list_block", "return").is_some(), "MockSpec should have boundary mock for std.render::list_block.return");
    assert!(spec.get_boundary_mock("std.render::kv_block", "return").is_some(), "MockSpec should have boundary mock for std.render::kv_block.return");
    assert!(spec.get_boundary_mock("std.width::truncate_text", "return").is_some(), "MockSpec should have boundary mock for std.width::truncate_text.return");
    assert!(spec.get_boundary_mock("std.unicode::display_width_columns", "return").is_some(), "MockSpec should have boundary mock for std.unicode::display_width_columns.return");
    assert!(spec.get_boundary_mock("std.unicode::in_block", "return").is_some(), "MockSpec should have boundary mock for std.unicode::in_block.return");
    assert!(spec.get_boundary_mock("std.unicode::char_width", "return").is_some(), "MockSpec should have boundary mock for std.unicode::char_width.return");
    assert!(spec.get_boundary_mock("std.unicode::string_display_width", "return").is_some(), "MockSpec should have boundary mock for std.unicode::string_display_width.return");
    assert!(spec.get_boundary_mock("std.symbols::resolve_symbol", "return").is_some(), "MockSpec should have boundary mock for std.symbols::resolve_symbol.return");
    assert!(spec.get_boundary_mock("std.symbols::symbol_color", "return").is_some(), "MockSpec should have boundary mock for std.symbols::symbol_color.return");
    assert!(spec.get_boundary_mock("std.symbols::ansi_code", "return").is_some(), "MockSpec should have boundary mock for std.symbols::ansi_code.return");
}

// =========================================================================
// Windowed Segment Tests
// =========================================================================

// These tests execute contiguous windows of the DAG using baseline DryRun
// values as injected inputs, then verify window exit outputs match baseline.

/// Window: std.render::truncate_spans -> std.render::constrain_line
#[test]
fn test_window_std_render_truncate_spans_through_std_render_constrain_line() {
    if !guard_test("test_window_std_render_truncate_spans_through_std_render_constrain_line", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::truncate_spans", "std.render::constrain_line"));
    let mut mocks = mock_spec().to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("window inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("window execution should succeed");
    assert_window_outputs(&flat, &window, &baseline, &log).expect("window outputs should match baseline");
}

// =========================================================================
// Probe-Observer Integration Tests
// =========================================================================

// Probes: 50 | Observers: 23 | Tests: 47
// All terminal nodes are observed.

/// Chain test: extdeps.gitignore_render::render_category_header -> extdeps.gitignore_render::render_category (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_extdeps_gitignore_render_render_category_header_to_extdeps_gitignore_render_render_category() {
    if !guard_test("test_chain_extdeps_gitignore_render_render_category_header_to_extdeps_gitignore_render_render_category", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("extdeps.gitignore_render::render_category", "extdeps.gitignore_render::render_category_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: extdeps.gitignore_render::render_category (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "extdeps.gitignore_render::render_category") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_category".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "extdeps.gitignore_render::render_category") {
if leo.matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_category".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain extdeps.gitignore_render::render_category_header -> extdeps.gitignore_render::render_category should satisfy observer matchers");
}

/// Chain test: extdeps.gitignore_render::render_gitignore_content -> extdeps.gitignore_render::render_gitignore_file (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_extdeps_gitignore_render_render_gitignore_content_to_extdeps_gitignore_render_render_gitignore_file() {
    if !guard_test("test_chain_extdeps_gitignore_render_render_gitignore_content_to_extdeps_gitignore_render_render_gitignore_file", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("extdeps.gitignore_render::render_gitignore_content", "extdeps.gitignore_render::render_gitignore_file"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: extdeps.gitignore_render::render_gitignore_file (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "extdeps.gitignore_render::render_gitignore_file") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_gitignore_file".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "extdeps.gitignore_render::render_gitignore_file") {
if leo.matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_gitignore_file".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain extdeps.gitignore_render::render_gitignore_content -> extdeps.gitignore_render::render_gitignore_file should satisfy observer matchers");
}

/// Chain test: param_source_extdeps_gitignore_render_render_category_cat -> extdeps.gitignore_render::render_category (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_extdeps_gitignore_render_render_category_cat_to_extdeps_gitignore_render_render_category() {
    if !guard_test("test_chain_param_source_extdeps_gitignore_render_render_category_cat_to_extdeps_gitignore_render_render_category", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("extdeps.gitignore_render::render_category", "extdeps.gitignore_render::render_category_header", "param_source_extdeps_gitignore_render_render_category_cat"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: extdeps.gitignore_render::render_category (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "extdeps.gitignore_render::render_category") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_category".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "extdeps.gitignore_render::render_category") {
if leo.matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_category".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_extdeps_gitignore_render_render_category_cat -> extdeps.gitignore_render::render_category should satisfy observer matchers");
}

/// Chain test: param_source_extdeps_gitignore_render_render_gitignore_file_file -> extdeps.gitignore_render::render_gitignore_file (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_extdeps_gitignore_render_render_gitignore_file_file_to_extdeps_gitignore_render_render_gitignore_file() {
    if !guard_test("test_chain_param_source_extdeps_gitignore_render_render_gitignore_file_file_to_extdeps_gitignore_render_render_gitignore_file", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("extdeps.gitignore_render::render_gitignore_content", "extdeps.gitignore_render::render_gitignore_file", "param_source_extdeps_gitignore_render_render_gitignore_file_file"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: extdeps.gitignore_render::render_gitignore_file (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "extdeps.gitignore_render::render_gitignore_file") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_gitignore_file".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "extdeps.gitignore_render::render_gitignore_file") {
if leo.matcher.is_chain_safe() {
matchers.insert(("extdeps.gitignore_render::render_gitignore_file".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_extdeps_gitignore_render_render_gitignore_file_file -> extdeps.gitignore_render::render_gitignore_file should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::config_comment_prefix (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_config_comment_prefix() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_config_comment_prefix", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::config_comment_prefix"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_comment_prefix (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_comment_prefix") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_comment_prefix".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_comment_prefix") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_comment_prefix".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::config_comment_prefix should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::config_format_header (depth 5)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_config_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 5)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::format_comment (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_format_comment() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_format_comment", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::format_comment"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::format_comment (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::format_comment") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::format_comment".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::format_comment") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::format_comment".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::format_comment should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::format_section_header (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_format_section_header() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_format_section_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::format_section_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::format_section_header (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::format_section_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::format_section_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::format_section_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::format_section_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::format_section_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::language_comment_prefix (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_language_comment_prefix() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_language_comment_prefix", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::language_comment_prefix"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_comment_prefix (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_comment_prefix") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_comment_prefix".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_comment_prefix") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_comment_prefix".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::language_comment_prefix should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_comment_prefix_format -> std.render::language_format_header (depth 5)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_language_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_comment_prefix_format_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_comment_prefix_format_comment_arg_0", "param_source_std_render_config_comment_prefix_format", "std.render::comment_prefix_for", "std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 5)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_comment_prefix_format -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_format -> std.render::config_format_header (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_format_to_std_render_config_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_format_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_format_header_format_comment_arg_0", "param_source_std_render_config_format_header_format", "std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_format -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_format -> std.render::language_format_header (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_format_to_std_render_language_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_format_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_config_format_header_format_comment_arg_0", "param_source_std_render_config_format_header_format", "std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_format -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_generator -> std.render::config_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_generator_to_std_render_config_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_generator_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_config_format_header_generator", "std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_generator -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_generator -> std.render::language_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_generator_to_std_render_language_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_generator_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_config_format_header_generator", "std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_generator -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_regen_command -> std.render::config_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_regen_command_to_std_render_config_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_regen_command_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_config_format_header_regen_command", "std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_regen_command -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_config_format_header_regen_command -> std.render::language_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_config_format_header_regen_command_to_std_render_language_format_header() {
    if !guard_test("test_chain_param_source_std_render_config_format_header_regen_command_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_config_format_header_regen_command", "std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_config_format_header_regen_command -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: param_source_std_render_constrain_line_line -> std.render::constrain_line (depth 7)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_constrain_line_line_to_std_render_constrain_line() {
    if !guard_test("test_chain_param_source_std_render_constrain_line_line_to_std_render_constrain_line", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("binary_op_std_render_constrain_line_budget_arg_1", "binary_op_std_render_constrain_line_budget_rhs_arg_1_rhs", "get_field_std_render_constrain_line_line_indent_arg_1_rhs_lhs", "get_field_std_render_constrain_line_line_spans_arg_0", "param_source_std_render_constrain_line_line", "std.render::constrain_line", "std.render::truncate_spans"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::constrain_line (depth 7)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::constrain_line") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::constrain_line") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_constrain_line_line -> std.render::constrain_line should satisfy observer matchers");
}

/// Chain test: param_source_std_render_constrain_line_max_width -> std.render::constrain_line (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_constrain_line_max_width_to_std_render_constrain_line() {
    if !guard_test("test_chain_param_source_std_render_constrain_line_max_width_to_std_render_constrain_line", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("binary_op_std_render_constrain_line_budget_arg_1", "param_source_std_render_constrain_line_max_width", "std.render::constrain_line", "std.render::truncate_spans"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::constrain_line (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::constrain_line") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::constrain_line") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_constrain_line_max_width -> std.render::constrain_line should satisfy observer matchers");
}

/// Chain test: param_source_std_render_constrain_line_tier -> std.render::constrain_line (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_constrain_line_tier_to_std_render_constrain_line() {
    if !guard_test("test_chain_param_source_std_render_constrain_line_tier_to_std_render_constrain_line", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_constrain_line_tier", "std.render::constrain_line", "std.render::truncate_spans"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::constrain_line (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::constrain_line") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::constrain_line") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_constrain_line_tier -> std.render::constrain_line should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_key -> std.render::indented_kv (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_key_to_std_render_indented_kv() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_key_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_key", "std.render::indent_text", "std.render::indented_kv", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_key -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_key -> std.render::indented_list_item (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_key_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_key_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_key", "std.render::indent_text", "std.render::indented_list_item", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_key -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_level -> std.render::indented_kv (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_level_to_std_render_indented_kv() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_level_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_level", "std.render::indent_text", "std.render::indented_kv"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_level -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_level -> std.render::indented_list_item (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_level_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_level_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_level", "std.render::indent_text", "std.render::indented_list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_level -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_sep -> std.render::indented_kv (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_sep_to_std_render_indented_kv() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_sep_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_sep", "std.render::indent_text", "std.render::indented_kv", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_sep -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_sep -> std.render::indented_list_item (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_sep_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_sep_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_sep", "std.render::indent_text", "std.render::indented_list_item", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_sep -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_unit -> std.render::indented_kv (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_unit_to_std_render_indented_kv() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_unit_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_unit", "std.render::indent_text", "std.render::indented_kv"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_unit -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_unit -> std.render::indented_list_item (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_unit_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_unit_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_unit", "std.render::indent_text", "std.render::indented_list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_unit -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_value -> std.render::indented_kv (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_value_to_std_render_indented_kv() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_value_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_value", "std.render::indent_text", "std.render::indented_kv", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_value -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_kv_value -> std.render::indented_list_item (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_kv_value_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_kv_value_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_kv_value", "std.render::indent_text", "std.render::indented_list_item", "std.render::kv_pair"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_kv_value -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_list_item_prefix -> std.render::indented_list_item (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_list_item_prefix_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_list_item_prefix_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_list_item_prefix", "std.render::indented_list_item", "std.render::list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_list_item_prefix -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_indented_list_item_value -> std.render::indented_list_item (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_indented_list_item_value_to_std_render_indented_list_item() {
    if !guard_test("test_chain_param_source_std_render_indented_list_item_value_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_render_indented_list_item_value", "std.render::indented_list_item", "std.render::list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_indented_list_item_value -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: param_source_std_render_span_width_span -> std.render::span_width (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_render_span_width_span_to_std_render_span_width() {
    if !guard_test("test_chain_param_source_std_render_span_width_span_to_std_render_span_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("get_field_std_render_span_width_span_text_arg_0", "param_source_std_render_span_width_span", "std.render::span_width", "std.unicode::string_display_width"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::span_width (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::span_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::span_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::span_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::span_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_render_span_width_span -> std.render::span_width should satisfy observer matchers");
}

/// Chain test: param_source_std_unicode_char_width_c -> std.unicode::char_width (depth 4)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_std_unicode_char_width_c_to_std_unicode_char_width() {
    if !guard_test("test_chain_param_source_std_unicode_char_width_c_to_std_unicode_char_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("param_source_std_unicode_char_width_c", "std.unicode::char_display_width", "std.unicode::char_width", "std.unicode::display_width_columns"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.unicode::char_width (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.unicode::char_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.unicode::char_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain param_source_std_unicode_char_width_c -> std.unicode::char_width should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::config_comment_prefix (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_config_comment_prefix() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_config_comment_prefix", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::config_comment_prefix"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_comment_prefix (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_comment_prefix") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_comment_prefix".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_comment_prefix") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_comment_prefix".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::config_comment_prefix should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::config_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_config_format_header() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::format_comment (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_format_comment() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_format_comment", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::format_comment"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::format_comment (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::format_comment") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::format_comment".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::format_comment") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::format_comment".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::format_comment should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::format_section_header (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_format_section_header() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_format_section_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::format_section_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::format_section_header (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::format_section_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::format_section_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::format_section_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::format_section_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::format_section_header should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::language_comment_prefix (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_language_comment_prefix() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_language_comment_prefix", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::language_comment_prefix"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_comment_prefix (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_comment_prefix") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_comment_prefix".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_comment_prefix") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_comment_prefix".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::language_comment_prefix should satisfy observer matchers");
}

/// Chain test: std.render::comment_prefix_for -> std.render::language_format_header (depth 3)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_comment_prefix_for_to_std_render_language_format_header() {
    if !guard_test("test_chain_std_render_comment_prefix_for_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::comment_prefix_for", "std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::comment_prefix_for -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: std.render::format_header -> std.render::config_format_header (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_format_header_to_std_render_config_format_header() {
    if !guard_test("test_chain_std_render_format_header_to_std_render_config_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::config_format_header", "std.render::format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::config_format_header (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::config_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::config_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::config_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::format_header -> std.render::config_format_header should satisfy observer matchers");
}

/// Chain test: std.render::format_header -> std.render::language_format_header (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_format_header_to_std_render_language_format_header() {
    if !guard_test("test_chain_std_render_format_header_to_std_render_language_format_header", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::format_header", "std.render::language_format_header"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::language_format_header (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::language_format_header") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::language_format_header") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::language_format_header".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::format_header -> std.render::language_format_header should satisfy observer matchers");
}

/// Chain test: std.render::indent_text -> std.render::indented_kv (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_indent_text_to_std_render_indented_kv() {
    if !guard_test("test_chain_std_render_indent_text_to_std_render_indented_kv", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::indent_text", "std.render::indented_kv"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_kv (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_kv") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_kv") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_kv".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::indent_text -> std.render::indented_kv should satisfy observer matchers");
}

/// Chain test: std.render::indent_text -> std.render::indented_list_item (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_indent_text_to_std_render_indented_list_item() {
    if !guard_test("test_chain_std_render_indent_text_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::indent_text", "std.render::indented_list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::indent_text -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: std.render::list_item -> std.render::indented_list_item (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_list_item_to_std_render_indented_list_item() {
    if !guard_test("test_chain_std_render_list_item_to_std_render_indented_list_item", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::indented_list_item", "std.render::list_item"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::indented_list_item (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::indented_list_item") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::indented_list_item") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::indented_list_item".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::list_item -> std.render::indented_list_item should satisfy observer matchers");
}

/// Chain test: std.render::truncate_spans -> std.render::constrain_line (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_render_truncate_spans_to_std_render_constrain_line() {
    if !guard_test("test_chain_std_render_truncate_spans_to_std_render_constrain_line", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::constrain_line", "std.render::truncate_spans"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::constrain_line (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::constrain_line") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::constrain_line") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::constrain_line".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.render::truncate_spans -> std.render::constrain_line should satisfy observer matchers");
}

/// Chain test: std.unicode::display_width_columns -> std.unicode::char_width (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_unicode_display_width_columns_to_std_unicode_char_width() {
    if !guard_test("test_chain_std_unicode_display_width_columns_to_std_unicode_char_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.unicode::char_width", "std.unicode::display_width_columns"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.unicode::char_width (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.unicode::char_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.unicode::char_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.unicode::char_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.unicode::display_width_columns -> std.unicode::char_width should satisfy observer matchers");
}

/// Chain test: std.unicode::string_display_width -> std.render::span_width (depth 2)
/// 
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_std_unicode_string_display_width_to_std_render_span_width() {
    if !guard_test("test_chain_std_unicode_string_display_width_to_std_render_span_width", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(&flat, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("baseline DryRun should succeed");
    let window = Window::from_nodes(&flat, vec!("std.render::span_width", "std.unicode::string_display_width"));
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks).expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(&window_dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("chain execution should succeed");

    // Verify observer: std.render::span_width (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec.node_examples.iter().filter(|e| e.node_id == "std.render::span_width") {
for (port, matcher) in &ex.outputs {
if matcher.is_chain_safe() {
matchers.insert(("std.render::span_width".to_string(), port.clone()), matcher.clone());
}
}
}
    for leo in spec.live_expected_outputs.iter().filter(|e| e.node == "std.render::span_width") {
if leo.matcher.is_chain_safe() {
matchers.insert(("std.render::span_width".to_string(), leo.port.clone()), leo.matcher.clone());
}
}
    assert_chain_outputs(&log, &matchers).expect("chain std.unicode::string_display_width -> std.render::span_width should satisfy observer matchers");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: extdeps.gitignore_render::render_category (example 0)
/// 
/// Tests that node 'extdeps.gitignore_render::render_category' produces expected outputs for given inputs.
#[test]
fn test_example_extdeps_gitignore_render_render_category_0() {
    if !guard_test("test_example_extdeps_gitignore_render_render_category_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("cat".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_category", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'extdeps.gitignore_render::render_category' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: extdeps.gitignore_render::render_gitignore_file (example 1)
/// 
/// Tests that node 'extdeps.gitignore_render::render_gitignore_file' produces expected outputs for given inputs.
#[test]
fn test_example_extdeps_gitignore_render_render_gitignore_file_1() {
    if !guard_test("test_example_extdeps_gitignore_render_render_gitignore_file_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("file".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore_render::render_gitignore_file", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'extdeps.gitignore_render::render_gitignore_file' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: extdeps.gitignore::filter_categories (example 2)
/// 
/// Tests that node 'extdeps.gitignore::filter_categories' produces expected outputs for given inputs.
#[test]
fn test_example_extdeps_gitignore_filter_categories_2() {
    if !guard_test("test_example_extdeps_gitignore_filter_categories_2", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("build_system".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let outputs = gunbc_exec::execute_single_node(&dag, "extdeps.gitignore::filter_categories", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'extdeps.gitignore::filter_categories' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::span_width (example 3)
/// 
/// Tests that node 'std.render::span_width' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_span_width_3() {
    if !guard_test("test_example_std_render_span_width_3", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Int(1));
    inputs.insert("span".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::span_width", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::span_width' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::constrain_line (example 4)
/// 
/// Tests that node 'std.render::constrain_line' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_constrain_line_4() {
    if !guard_test("test_example_std_render_constrain_line_4", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("line".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::constrain_line", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::constrain_line' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::constrain_frame (example 5)
/// 
/// Tests that node 'std.render::constrain_frame' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_constrain_frame_5() {
    if !guard_test("test_example_std_render_constrain_frame_5", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("frame".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    inputs.insert("viewport".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::constrain_frame", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::constrain_frame' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::config_comment_prefix (example 6)
/// 
/// Tests that node 'std.render::config_comment_prefix' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_config_comment_prefix_6() {
    if !guard_test("test_example_std_render_config_comment_prefix_6", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("format".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::config_comment_prefix", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::config_comment_prefix' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::language_comment_prefix (example 7)
/// 
/// Tests that node 'std.render::language_comment_prefix' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_language_comment_prefix_7() {
    if !guard_test("test_example_std_render_language_comment_prefix_7", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("lang".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::language_comment_prefix", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::language_comment_prefix' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::config_format_header (example 8)
/// 
/// Tests that node 'std.render::config_format_header' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_config_format_header_8() {
    if !guard_test("test_example_std_render_config_format_header_8", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("format".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::config_format_header", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::config_format_header' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::language_format_header (example 9)
/// 
/// Tests that node 'std.render::language_format_header' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_language_format_header_9() {
    if !guard_test("test_example_std_render_language_format_header_9", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("generator".to_string(), Value::Str("example".to_string()));
    inputs.insert("lang".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("regen_command".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::language_format_header", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::language_format_header' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::format_comment (example 10)
/// 
/// Tests that node 'std.render::format_comment' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_format_comment_10() {
    if !guard_test("test_example_std_render_format_comment_10", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::format_comment", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::format_comment' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::format_section_header (example 11)
/// 
/// Tests that node 'std.render::format_section_header' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_format_section_header_11() {
    if !guard_test("test_example_std_render_format_section_header_11", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("syntax".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("title".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::format_section_header", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::format_section_header' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::indented_kv (example 12)
/// 
/// Tests that node 'std.render::indented_kv' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_indented_kv_12() {
    if !guard_test("test_example_std_render_indented_kv_12", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("key".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::indented_kv", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::indented_kv' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::indented_list_item (example 13)
/// 
/// Tests that node 'std.render::indented_list_item' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_indented_list_item_13() {
    if !guard_test("test_example_std_render_indented_list_item_13", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    inputs.insert("value".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::indented_list_item", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::indented_list_item' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: std.render::text_block (example 14)
/// 
/// Tests that node 'std.render::text_block' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_text_block_14() {
    if !guard_test("test_example_std_render_text_block_14", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::text_block", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::text_block' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::list_block (example 15)
/// 
/// Tests that node 'std.render::list_block' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_list_block_15() {
    if !guard_test("test_example_std_render_list_block_15", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::list_block", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::list_block' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.render::kv_block (example 16)
/// 
/// Tests that node 'std.render::kv_block' produces expected outputs for given inputs.
#[test]
fn test_example_std_render_kv_block_16() {
    if !guard_test("test_example_std_render_kv_block_16", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("level".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("sep".to_string(), Value::Str("example".to_string()));
    inputs.insert("unit".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.render::kv_block", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.render::kv_block' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.width::truncate_text (example 17)
/// 
/// Tests that node 'std.width::truncate_text' produces expected outputs for given inputs.
#[test]
fn test_example_std_width_truncate_text_17() {
    if !guard_test("test_example_std_width_truncate_text_17", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("max_width".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.width::truncate_text", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.width::truncate_text' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.unicode::in_block (example 18)
/// 
/// Tests that node 'std.unicode::in_block' produces expected outputs for given inputs.
#[test]
fn test_example_std_unicode_in_block_18() {
    if !guard_test("test_example_std_unicode_in_block_18", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("block".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("cp".to_string(), Value::Int(1));
    inputs.insert("return".to_string(), Value::Str("True".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::in_block", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.unicode::in_block' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.unicode::char_width (example 19)
/// 
/// Tests that node 'std.unicode::char_width' produces expected outputs for given inputs.
#[test]
fn test_example_std_unicode_char_width_19() {
    if !guard_test("test_example_std_unicode_char_width_19", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Int(1));
    inputs.insert("c".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Int(1));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.unicode::char_width", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.unicode::char_width' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.symbols::resolve_symbol (example 20)
/// 
/// Tests that node 'std.symbols::resolve_symbol' produces expected outputs for given inputs.
#[test]
fn test_example_std_symbols_resolve_symbol_20() {
    if !guard_test("test_example_std_symbols_resolve_symbol_20", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("id".to_string(), Value::Str("NodePending".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("tier".to_string(), Value::Str("Emoji".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::resolve_symbol", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.symbols::resolve_symbol' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.symbols::symbol_color (example 21)
/// 
/// Tests that node 'std.symbols::symbol_color' produces expected outputs for given inputs.
#[test]
fn test_example_std_symbols_symbol_color_21() {
    if !guard_test("test_example_std_symbols_symbol_color_21", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("Default".to_string()));
    inputs.insert("id".to_string(), Value::Str("NodePending".to_string()));
    inputs.insert("return".to_string(), Value::Str("Default".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::symbol_color", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.symbols::symbol_color' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.symbols::ansi_code (example 22)
/// 
/// Tests that node 'std.symbols::ansi_code' produces expected outputs for given inputs.
#[test]
fn test_example_std_symbols_ansi_code_22() {
    if !guard_test("test_example_std_symbols_ansi_code_22", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("extdeps/gitignore_render.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("c".to_string(), Value::Str("Default".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "std.symbols::ansi_code", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.symbols::ansi_code' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

