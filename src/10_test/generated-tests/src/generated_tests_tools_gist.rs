// Generated tests for tools_gist_generated_tests DAG.
//
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 312 obligations (109 discharged, 4 INVALID, 199 testable [10 compiler gaps]: A=56, B=109, C=34, D=0)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: 1983c6487f679e9412e86617a4071bbbcbbb45221f6ac8d539ef30b1e61e4d6a
//
// Probe-Observer Coverage:
//   Probes: 39
//     execute_transport_extdeps_git_git_Core_CurrentBranch (transport mock)
//     execute_transport_extdeps_git_git_Core_LsFiles (transport mock)
//     execute_transport_extdeps_git_git_Core_Diff (transport mock)
//     execute_transport_extdeps_git_git_Core_RevListBase (transport mock)
//     execute_transport_extdeps_git_git_Core_Show (transport mock)
//     execute_transport_extdeps_github_gists_github_Gist_Create (transport mock)
//     execute_transport_extdeps_git_git_Core_CurrentBranch_c1 (transport mock)
//     execute_transport_extdeps_github_gists_github_Gist_Create_c1 (transport mock)
//     execute_transport_extdeps_git_git_Core_CurrentBranch_c2 (transport mock)
//     execute_transport_extdeps_git_git_Core_Diff_c1 (transport mock)
//     execute_transport_extdeps_github_gists_github_Gist_Create_c2 (transport mock)
//     acquire_resource_std_resources_Filesystem (boundary mock)
//     acquire_resource_std_resources_Network (boundary mock)
//     acquire_resource_std_resources_Clock (boundary mock)
//     acquire_resource_std_resources_AuthContext (boundary mock)
//     fs_env (boundary mock)
//     tools.gist::lang_for_path (boundary mock)
//     tools.gist::render_file_section (boundary mock)
//     tools.gist::gist (boundary mock)
//     tools.gist::gist_diff (boundary mock)
//     tools.gist::gist_recent (boundary mock)
//     parse_transport_extdeps_git_git_Core_Show (boundary mock)
//     parse_transport_extdeps_github_gists_github_Gist_Create (boundary mock)
//     parse_transport_extdeps_github_gists_github_Gist_Create_c1 (boundary mock)
//     parse_transport_extdeps_git_git_Core_Diff_c1 (boundary mock)
//     parse_transport_extdeps_github_gists_github_Gist_Create_c2 (boundary mock)
//     release_resource_std_resources_Filesystem (boundary mock)
//     release_resource_std_resources_Network (boundary mock)
//     release_resource_std_resources_Clock (boundary mock)
//     release_resource_std_resources_AuthContext (boundary mock)
//     prepare_transport_extdeps_git_git_Core_Diff (input mock)
//     prepare_transport_extdeps_git_git_Core_Show (input mock)
//     param_source_tools_gist_render_file_section_path (input mock)
//     param_source_tools_gist_gist_public (input mock)
//     param_source_tools_gist_gist_diff_base_ref (input mock)
//     param_source_tools_gist_gist_diff_public (input mock)
//     param_source_tools_gist_gist_recent_since (input mock)
//     prepare_transport_extdeps_git_git_Core_Diff_c1 (input mock)
//     param_source_tools_gist_gist_recent_public (input mock)
//
//   Observers: 6
//     parse_transport_extdeps_git_git_Core_Show [terminal] (content: IsString)
//     release_resource_std_resources_AuthContext [terminal] (released: NonEmpty)
//     release_resource_std_resources_Clock [terminal] (released: NonEmpty)
//     release_resource_std_resources_Filesystem [terminal] (released: NonEmpty)
//     release_resource_std_resources_Network [terminal] (released: NonEmpty)
//     tools.gist::render_file_section [terminal] (return: IsString)
//
//   Integration tests: 34
//     acquire_resource_std_resources_AuthContext -> release_resource_std_resources_AuthContext (depth 2)
//     acquire_resource_std_resources_Clock -> release_resource_std_resources_Clock (depth 2)
//     acquire_resource_std_resources_Filesystem -> release_resource_std_resources_Filesystem (depth 2)
//     acquire_resource_std_resources_Network -> release_resource_std_resources_Network (depth 12)
//     execute_transport_extdeps_git_git_Core_CurrentBranch -> release_resource_std_resources_Network (depth 8)
//     execute_transport_extdeps_git_git_Core_CurrentBranch_c1 -> release_resource_std_resources_Network (depth 12)
//     execute_transport_extdeps_git_git_Core_CurrentBranch_c2 -> release_resource_std_resources_Network (depth 7)
//     execute_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network (depth 12)
//     execute_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 4)
//     execute_transport_extdeps_git_git_Core_LsFiles -> release_resource_std_resources_Network (depth 10)
//     execute_transport_extdeps_git_git_Core_RevListBase -> release_resource_std_resources_Network (depth 7)
//     execute_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show (depth 2)
//     execute_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network (depth 4)
//     execute_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network (depth 4)
//     execute_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network (depth 4)
//     fs_env -> parse_transport_extdeps_git_git_Core_Show (depth 3)
//     fs_env -> release_resource_std_resources_Network (depth 33)
//     param_source_tools_gist_gist_diff_base_ref -> release_resource_std_resources_Network (depth 14)
//     param_source_tools_gist_gist_diff_public -> release_resource_std_resources_Network (depth 6)
//     param_source_tools_gist_gist_public -> release_resource_std_resources_Network (depth 6)
//     param_source_tools_gist_gist_recent_public -> release_resource_std_resources_Network (depth 6)
//     param_source_tools_gist_gist_recent_since -> release_resource_std_resources_Network (depth 9)
//     param_source_tools_gist_render_file_section_path -> tools.gist::render_file_section (depth 3)
//     parse_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 3)
//     parse_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network (depth 3)
//     parse_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network (depth 3)
//     parse_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network (depth 3)
//     prepare_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network (depth 13)
//     prepare_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 5)
//     prepare_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show (depth 3)
//     tools.gist::gist -> release_resource_std_resources_Network (depth 2)
//     tools.gist::gist_diff -> release_resource_std_resources_Network (depth 2)
//     tools.gist::gist_recent -> release_resource_std_resources_Network (depth 2)
//     tools.gist::lang_for_path -> tools.gist::render_file_section (depth 2)

use gunbc_exec::{execute_dag, lower, ExecuteConfig, ExecutionMode};
use gunbc_ir::{detect_boundaries, Value};
use gunbc_test::{
    apply_window_inputs, assert_boundary_mockable, assert_chain_outputs, guard_test, window_subdag,
    FermiCost, MockSpec, TestClass, Window,
};
use std::collections::HashMap;

fn mock_spec() -> MockSpec {
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    gunbc_test::auto_mock_spec(&dag, "tools-gist")
}

// =========================================================================
// INVALID OBLIGATIONS — structural errors detected during analysis
// =========================================================================

/// INVALID: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')
#[test]
#[should_panic]
fn test_invalid_obligation_0() {
    panic!("Structural error: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')");
}

/// INVALID: Edge parse_transport_extdeps_git_git_Core_LsFiles.files → tools.gist::gist::cf_for_0.items: coercion invalid (output 0..* is incompatible with input 1 (both min and max mismatch))
#[test]
#[should_panic]
fn test_invalid_obligation_1() {
    panic!("Structural error: Edge parse_transport_extdeps_git_git_Core_LsFiles.files → tools.gist::gist::cf_for_0.items: coercion invalid (output 0..* is incompatible with input 1 (both min and max mismatch))");
}

/// INVALID: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create_c1.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')
#[test]
#[should_panic]
fn test_invalid_obligation_2() {
    panic!("Structural error: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create_c1.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')");
}

/// INVALID: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create_c2.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')
#[test]
#[should_panic]
fn test_invalid_obligation_3() {
    panic!("Structural error: Edge extdeps.github.auth::github_token.token → execute_transport_extdeps_github_gists_github_Gist_Create_c2.res:credential: coercion invalid (base type 'Secret' cannot upcast to 'Credential')");
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
//
// Determinism obligations: 44 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'tools.gist::lang_for_path': same inputs → same outputs
// - 'tools.gist::render_file_section': same inputs → same outputs
// - 'tools.gist::build_snapshot_content': same inputs → same outputs
// - 'tools.gist::render_diff_markdown': same inputs → same outputs
// - 'tools.gist::gist': same inputs → same outputs
// - 'tools.gist::gist_diff': same inputs → same outputs
// - 'tools.gist::gist_recent': same inputs → same outputs
// - 'extdeps.github.auth::github_token': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_CurrentBranch': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_LsFiles': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_LsFiles': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_Diff': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_Diff': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_RevListBase': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_RevListBase': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_Show': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_Show': same inputs → same outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create': same inputs → same outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create': same inputs → same outputs
// - 'tools.gist::gist::cf_for_0': same inputs → same outputs
// - 'param_source_tools_gist_render_file_section_path': same inputs → same outputs
// - 'param_source_tools_gist_gist_public': same inputs → same outputs
// - 'literal_source_tools_gist_gist_skipped_fn_2': same inputs → same outputs
// - 'literal_source_tools_gist_gist_project_id_default_project_id': same inputs → same outputs
// - 'literal_source_tools_gist_gist_secret_name_default_secret_name': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_CurrentBranch_c1': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch_c1': same inputs → same outputs
// - 'param_source_tools_gist_gist_diff_base_ref': same inputs → same outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create_c1': same inputs → same outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create_c1': same inputs → same outputs
// - 'param_source_tools_gist_gist_diff_public': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_CurrentBranch_c2': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch_c2': same inputs → same outputs
// - 'param_source_tools_gist_gist_recent_since': same inputs → same outputs
// - 'prepare_transport_extdeps_git_git_Core_Diff_c1': same inputs → same outputs
// - 'parse_transport_extdeps_git_git_Core_Diff_c1': same inputs → same outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create_c2': same inputs → same outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create_c2': same inputs → same outputs
// - 'param_source_tools_gist_gist_recent_public': same inputs → same outputs
// - 'release_resource_std_resources_Filesystem': same inputs → same outputs
// - 'release_resource_std_resources_Network': same inputs → same outputs
// - 'release_resource_std_resources_Clock': same inputs → same outputs
// - 'release_resource_std_resources_AuthContext': same inputs → same outputs

/// DryRun execution completes without crash.
///
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    if !guard_test(
        "test_dryrun_completion",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("DryRun execution should complete without crash");
    assert!(
        !log.entries.is_empty(),
        "execution should produce log entries"
    );
}

/// All transport executors are intercepted in DryRun.
///
/// Proves: every transport executor is interceptable; DryRun won't
/// accidentally perform real I/O.
#[test]
fn test_transport_interception() {
    if !guard_test(
        "test_transport_interception",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(
        result.is_ok(),
        "All transports should be interceptable: {:?}",
        result.error
    );
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_CurrentBranch"), "transport executor 'execute_transport_extdeps_git_git_Core_CurrentBranch' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_LsFiles"), "transport executor 'execute_transport_extdeps_git_git_Core_LsFiles' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_Diff"), "transport executor 'execute_transport_extdeps_git_git_Core_Diff' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_RevListBase"), "transport executor 'execute_transport_extdeps_git_git_Core_RevListBase' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_Show"), "transport executor 'execute_transport_extdeps_git_git_Core_Show' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_github_gists_github_Gist_Create"), "transport executor 'execute_transport_extdeps_github_gists_github_Gist_Create' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_CurrentBranch_c1"), "transport executor 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_github_gists_github_Gist_Create_c1"), "transport executor 'execute_transport_extdeps_github_gists_github_Gist_Create_c1' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_CurrentBranch_c2"), "transport executor 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_git_git_Core_Diff_c1"), "transport executor 'execute_transport_extdeps_git_git_Core_Diff_c1' should be in intercepted list");
    assert!(result.boundary_nodes.iter().any(|n| n == "execute_transport_extdeps_github_gists_github_Gist_Create_c2"), "transport executor 'execute_transport_extdeps_github_gists_github_Gist_Create_c2' should be in intercepted list");
}

/// Transport behavior specs cover all core executor families.
///
/// Proves: testgen is wired to the shared transport behavior catalog
/// used to drive behavioral transport contract checks.
#[test]
fn test_transport_behavior_specs_cover_core_families() {
    if !guard_test(
        "test_transport_behavior_specs_cover_core_families",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let specs = gunbc_ir::default_transport_behaviors();
    assert_eq!(
        specs.len(),
        6,
        "transport behavior catalog should cover tcp/http/rest/file/shell/local"
    );
    assert!(
        specs
            .iter()
            .any(|s| matches!(s.transport, gunbc_ir::TransportKind::Tcp)),
        "missing tcp transport behavior spec"
    );
    assert!(
        specs
            .iter()
            .any(|s| matches!(s.transport, gunbc_ir::TransportKind::Http)),
        "missing http transport behavior spec"
    );
    assert!(
        specs
            .iter()
            .any(|s| matches!(s.transport, gunbc_ir::TransportKind::Rest)),
        "missing rest transport behavior spec"
    );
    assert!(
        specs
            .iter()
            .any(|s| matches!(s.transport, gunbc_ir::TransportKind::File)),
        "missing file transport behavior spec"
    );
    assert!(
        specs
            .iter()
            .any(|s| matches!(s.transport, gunbc_ir::TransportKind::Shell)),
        "missing shell transport behavior spec"
    );
    let tcp = specs
        .iter()
        .find(|s| matches!(s.transport, gunbc_ir::TransportKind::Tcp))
        .expect("tcp behavior spec should exist");
    assert!(
        tcp.field_routes
            .iter()
            .any(|route| route.request_field == "read_timeout_ms"
                && route.operation == "set_read_timeout"),
        "tcp behavior spec should pin read_timeout_ms routing"
    );
    assert!(
        tcp.field_routes
            .iter()
            .any(|route| route.request_field == "write_timeout_ms"
                && route.operation == "set_write_timeout"),
        "tcp behavior spec should pin write_timeout_ms routing"
    );
    let models = gunbc_ir::default_system_models();
    let contract_specs = gunbc_ir::derive_contract_test_specs(&models);
    assert!(
        !contract_specs.is_empty(),
        "system model contract specs should derive at least one test spec"
    );
    assert!(
        contract_specs
            .iter()
            .any(|s| matches!(s.phase, gunbc_ir::UpsertPhase::Check)),
        "derived contract specs should include check-phase coverage"
    );
    let harnesses = gunbc_ir::generate_contract_test_harnesses(&contract_specs);
    assert_eq!(
        harnesses.len(),
        contract_specs.len(),
        "each contract test spec should render a harness signature"
    );
    assert!(
        harnesses.iter().all(|h| h.starts_with("fn contract_")),
        "rendered harnesses should be generated contract function signatures"
    );
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 10 edge predicate entailment obligations (Unknown).
// Full entailment tests require contract tower witnesses (Tier 3 infra).
// For now, these are documented as obligations:
// - tools.gist::gist::cf_for_0.result → tools.gist::build_snapshot_content.files: Edge tools.gist::gist::cf_for_0.result → tools.gist::build_snapshot_content.files: predicate entailment unknown (source is 'Any' but target 'List<Record>' has specific constraints)
// - literal_source_tools_gist_gist_skipped_fn_2.skipped → tools.gist::build_snapshot_content.skipped: Edge literal_source_tools_gist_gist_skipped_fn_2.skipped → tools.gist::build_snapshot_content.skipped: predicate entailment unknown (source is 'Any' but target 'List<String>' has specific constraints)
// - literal_source_tools_gist_gist_project_id_default_project_id.project_id → extdeps.github.auth::github_token.project_id: Edge literal_source_tools_gist_gist_project_id_default_project_id.project_id → extdeps.github.auth::github_token.project_id: predicate entailment unknown (source is 'Any' but target 'NonEmptyStr' has specific constraints)
// - literal_source_tools_gist_gist_secret_name_default_secret_name.secret_name → extdeps.github.auth::github_token.secret_name: Edge literal_source_tools_gist_gist_secret_name_default_secret_name.secret_name → extdeps.github.auth::github_token.secret_name: predicate entailment unknown (source is 'Any' but target 'NonEmptyStr' has specific constraints)
// - param_source_tools_gist_gist_diff_base_ref.base_ref → prepare_transport_extdeps_git_git_Core_Diff.base: Edge param_source_tools_gist_gist_diff_base_ref.base_ref → prepare_transport_extdeps_git_git_Core_Diff.base: predicate entailment unknown (type(s) not in registry: from='CommitSha', to='GitRef')
// - parse_transport_extdeps_git_git_Core_RevListBase.base_ref → prepare_transport_extdeps_git_git_Core_Diff_c1.base: Edge parse_transport_extdeps_git_git_Core_RevListBase.base_ref → prepare_transport_extdeps_git_git_Core_Diff_c1.base: predicate entailment unknown (type(s) not in registry: from='CommitSha', to='GitRef')
// - tools.gist::gist.url → release_resource_std_resources_Network.resource_handle: Edge tools.gist::gist.url → release_resource_std_resources_Network.resource_handle: predicate entailment unknown (type(s) not in registry: from='Url', to='ResourceHandle')
// - tools.gist::gist_diff.url → release_resource_std_resources_Network.resource_handle: Edge tools.gist::gist_diff.url → release_resource_std_resources_Network.resource_handle: predicate entailment unknown (type(s) not in registry: from='Url', to='ResourceHandle')
// - tools.gist::gist_recent.url → release_resource_std_resources_Network.resource_handle: Edge tools.gist::gist_recent.url → release_resource_std_resources_Network.resource_handle: predicate entailment unknown (type(s) not in registry: from='Url', to='ResourceHandle')
// - extdeps.github.auth::github_token.token → release_resource_std_resources_Network.resource_handle: Edge extdeps.github.auth::github_token.token → release_resource_std_resources_Network.resource_handle: predicate entailment unknown (type(s) not in registry: from='Secret', to='ResourceHandle')
//
// 48 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'tools.gist::lang_for_path': valid inputs → valid outputs
// - 'tools.gist::render_file_section': valid inputs → valid outputs
// - 'tools.gist::build_snapshot_content': valid inputs → valid outputs
// - 'tools.gist::render_diff_markdown': valid inputs → valid outputs
// - 'tools.gist::gist': valid inputs → valid outputs
// - 'tools.gist::gist_diff': valid inputs → valid outputs
// - 'tools.gist::gist_recent': valid inputs → valid outputs
// - 'extdeps.github.auth::github_token': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_CurrentBranch': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_LsFiles': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_LsFiles': valid inputs → valid outputs
// - 'prepare_transport_extdeps_git_git_Core_Diff': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_Diff': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_Diff': valid inputs → valid outputs
// - 'prepare_transport_extdeps_git_git_Core_RevListBase': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_RevListBase': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_RevListBase': valid inputs → valid outputs
// - 'prepare_transport_extdeps_git_git_Core_Show': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_Show': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_Show': valid inputs → valid outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create': valid inputs → valid outputs
// - 'execute_transport_extdeps_github_gists_github_Gist_Create': valid inputs → valid outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create': valid inputs → valid outputs
// - 'tools.gist::gist::cf_for_0': valid inputs → valid outputs
// - 'param_source_tools_gist_render_file_section_path': valid inputs → valid outputs
// - 'param_source_tools_gist_gist_public': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch_c1': valid inputs → valid outputs
// - 'param_source_tools_gist_gist_diff_base_ref': valid inputs → valid outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create_c1': valid inputs → valid outputs
// - 'execute_transport_extdeps_github_gists_github_Gist_Create_c1': valid inputs → valid outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create_c1': valid inputs → valid outputs
// - 'param_source_tools_gist_gist_diff_public': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_CurrentBranch_c2': valid inputs → valid outputs
// - 'param_source_tools_gist_gist_recent_since': valid inputs → valid outputs
// - 'prepare_transport_extdeps_git_git_Core_Diff_c1': valid inputs → valid outputs
// - 'execute_transport_extdeps_git_git_Core_Diff_c1': valid inputs → valid outputs
// - 'parse_transport_extdeps_git_git_Core_Diff_c1': valid inputs → valid outputs
// - 'prepare_transport_extdeps_github_gists_github_Gist_Create_c2': valid inputs → valid outputs
// - 'execute_transport_extdeps_github_gists_github_Gist_Create_c2': valid inputs → valid outputs
// - 'parse_transport_extdeps_github_gists_github_Gist_Create_c2': valid inputs → valid outputs
// - 'param_source_tools_gist_gist_recent_public': valid inputs → valid outputs
// - 'release_resource_std_resources_Filesystem': valid inputs → valid outputs
// - 'release_resource_std_resources_Network': valid inputs → valid outputs
// - 'release_resource_std_resources_Clock': valid inputs → valid outputs
// - 'release_resource_std_resources_AuthContext': valid inputs → valid outputs
// 20 optional input handling obligations.
// Optional inputs must accept missing values and reject wrong-typed inputs.
// Optional input tests skipped for 'tools.gist::build_snapshot_content': tools.gist::build_snapshot_content.files (type: List<Record>); tools.gist::build_snapshot_content.skipped (type: List<String>).
// Optional input tests skipped for 'tools.gist::render_diff_markdown': tools.gist::render_diff_markdown.base_ref (type: CommitSha).
// Optional input tests skipped for 'extdeps.github.auth::github_token': extdeps.github.auth::github_token.project_id (type: NonEmptyStr); extdeps.github.auth::github_token.secret_name (type: NonEmptyStr).

/// Coercion coverage: tools.gist::lang_for_path.return 1 → tools.gist::render_file_section.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::render_file_section.__deps.
#[test]
fn test_coercion_tools_gist_lang_for_path_return_tools_gist_render_file_section_deps() {
    if !guard_test(
        "test_coercion_tools_gist_lang_for_path_return_tools_gist_render_file_section_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::lang_for_path.return → tools.gist::render_file_section.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::render_file_section")
        .expect("coercion target entry missing for tools.gist::render_file_section.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::render_file_section.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect(
        "coercion target tools.gist::render_file_section.__deps should receive coerced value",
    );
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::render_file_section.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::build_snapshot_content.return 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_tools_gist_build_snapshot_content_return_tools_gist_gist_deps() {
    if !guard_test(
        "test_coercion_tools_gist_build_snapshot_content_return_tools_gist_gist_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::build_snapshot_content.return → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: extdeps.github.auth::github_token.token 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_deps() {
    if !guard_test(
        "test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.github.auth::github_token.token → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::gist::cf_for_0.result 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_tools_gist_gist_cf_for_0_result_tools_gist_gist_deps() {
    if !guard_test(
        "test_coercion_tools_gist_gist_cf_for_0_result_tools_gist_gist_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::gist::cf_for_0.result → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: extdeps.github.auth::github_token.token 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_diff_deps() {
    if !guard_test(
        "test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_diff_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.github.auth::github_token.token → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::render_diff_markdown.return 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_tools_gist_render_diff_markdown_return_tools_gist_gist_diff_deps() {
    if !guard_test(
        "test_coercion_tools_gist_render_diff_markdown_return_tools_gist_gist_diff_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::render_diff_markdown.return → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: extdeps.github.auth::github_token.token 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_recent_deps() {
    if !guard_test(
        "test_coercion_extdeps_github_auth_github_token_token_tools_gist_gist_recent_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.github.auth::github_token.token → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::render_diff_markdown.return 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_tools_gist_render_diff_markdown_return_tools_gist_gist_recent_deps() {
    if !guard_test(
        "test_coercion_tools_gist_render_diff_markdown_return_tools_gist_gist_recent_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::render_diff_markdown.return → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_CurrentBranch.branch 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_current_branch_branch_tools_gist_gist_deps() {
    if !guard_test("test_coercion_parse_transport_extdeps_git_git_core_current_branch_branch_tools_gist_gist_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_CurrentBranch.branch → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_github_gists_github_Gist_Create.id 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_github_gists_github_gist_create_id_tools_gist_gist_deps() {
    if !guard_test("test_coercion_parse_transport_extdeps_github_gists_github_gist_create_id_tools_gist_gist_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_github_gists_github_Gist_Create.id → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::gist::cf_for_0.result 1 → tools.gist::build_snapshot_content.files 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::build_snapshot_content.files.
#[test]
fn test_coercion_tools_gist_gist_cf_for_0_result_tools_gist_build_snapshot_content_files() {
    if !guard_test(
        "test_coercion_tools_gist_gist_cf_for_0_result_tools_gist_build_snapshot_content_files",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::gist::cf_for_0.result → tools.gist::build_snapshot_content.files should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::build_snapshot_content")
        .expect("coercion target entry missing for tools.gist::build_snapshot_content.files");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::build_snapshot_content.files should capture inputs");
    let received = target_inputs.get("files").expect(
        "coercion target tools.gist::build_snapshot_content.files should receive coerced value",
    );
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::build_snapshot_content.files; got {:?}",
        received
    );
}

/// Coercion coverage: literal_source_tools_gist_gist_skipped_fn_2.skipped 1 → tools.gist::build_snapshot_content.skipped 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::build_snapshot_content.skipped.
#[test]
fn test_coercion_literal_source_tools_gist_gist_skipped_fn_2_skipped_tools_gist_build_snapshot_content_skipped(
) {
    if !guard_test("test_coercion_literal_source_tools_gist_gist_skipped_fn_2_skipped_tools_gist_build_snapshot_content_skipped", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at literal_source_tools_gist_gist_skipped_fn_2.skipped → tools.gist::build_snapshot_content.skipped should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::build_snapshot_content")
        .expect("coercion target entry missing for tools.gist::build_snapshot_content.skipped");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::build_snapshot_content.skipped should capture inputs");
    let received = target_inputs.get("skipped").expect(
        "coercion target tools.gist::build_snapshot_content.skipped should receive coerced value",
    );
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::build_snapshot_content.skipped; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_CurrentBranch_c1.branch 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_current_branch_c1_branch_tools_gist_gist_diff_deps(
) {
    if !guard_test("test_coercion_parse_transport_extdeps_git_git_core_current_branch_c1_branch_tools_gist_gist_diff_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_CurrentBranch_c1.branch → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_Diff.diff 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_diff_diff_tools_gist_gist_diff_deps() {
    if !guard_test(
        "test_coercion_parse_transport_extdeps_git_git_core_diff_diff_tools_gist_gist_diff_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_Diff.diff → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_github_gists_github_Gist_Create_c1.id 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_github_gists_github_gist_create_c1_id_tools_gist_gist_diff_deps(
) {
    if !guard_test("test_coercion_parse_transport_extdeps_github_gists_github_gist_create_c1_id_tools_gist_gist_diff_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_github_gists_github_Gist_Create_c1.id → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_CurrentBranch_c2.branch 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_current_branch_c2_branch_tools_gist_gist_recent_deps(
) {
    if !guard_test("test_coercion_parse_transport_extdeps_git_git_core_current_branch_c2_branch_tools_gist_gist_recent_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_CurrentBranch_c2.branch → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_RevListBase.base_ref 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_rev_list_base_base_ref_tools_gist_gist_recent_deps(
) {
    if !guard_test("test_coercion_parse_transport_extdeps_git_git_core_rev_list_base_base_ref_tools_gist_gist_recent_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_RevListBase.base_ref → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_git_git_Core_Diff_c1.diff 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_git_git_core_diff_c1_diff_tools_gist_gist_recent_deps() {
    if !guard_test("test_coercion_parse_transport_extdeps_git_git_core_diff_c1_diff_tools_gist_gist_recent_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_git_git_Core_Diff_c1.diff → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: parse_transport_extdeps_github_gists_github_Gist_Create_c2.id 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_parse_transport_extdeps_github_gists_github_gist_create_c2_id_tools_gist_gist_recent_deps(
) {
    if !guard_test("test_coercion_parse_transport_extdeps_github_gists_github_gist_create_c2_id_tools_gist_gist_recent_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at parse_transport_extdeps_github_gists_github_Gist_Create_c2.id → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: acquire_resource_std_resources_Filesystem.resource_handle 1 → release_resource_std_resources_Filesystem.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Filesystem.resource_handle.
#[test]
fn test_coercion_acquire_resource_std_resources_filesystem_resource_handle_release_resource_std_resources_filesystem_resource_handle(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_filesystem_resource_handle_release_resource_std_resources_filesystem_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Filesystem.resource_handle → release_resource_std_resources_Filesystem.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Filesystem").expect("coercion target entry missing for release_resource_std_resources_Filesystem.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Filesystem.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Filesystem.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Filesystem.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Network.resource_handle 1 → release_resource_std_resources_Network.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Network.resource_handle.
#[test]
fn test_coercion_acquire_resource_std_resources_network_resource_handle_release_resource_std_resources_network_resource_handle(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_network_resource_handle_release_resource_std_resources_network_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Network.resource_handle → release_resource_std_resources_Network.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Network").expect("coercion target entry missing for release_resource_std_resources_Network.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Network.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Network.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Network.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Clock.resource_handle 1 → release_resource_std_resources_Clock.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Clock.resource_handle.
#[test]
fn test_coercion_acquire_resource_std_resources_clock_resource_handle_release_resource_std_resources_clock_resource_handle(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_clock_resource_handle_release_resource_std_resources_clock_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Clock.resource_handle → release_resource_std_resources_Clock.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Clock").expect("coercion target entry missing for release_resource_std_resources_Clock.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Clock.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Clock.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Clock.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_AuthContext.resource_handle 1 → release_resource_std_resources_AuthContext.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_AuthContext.resource_handle.
#[test]
fn test_coercion_acquire_resource_std_resources_auth_context_resource_handle_release_resource_std_resources_auth_context_resource_handle(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_auth_context_resource_handle_release_resource_std_resources_auth_context_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_AuthContext.resource_handle → release_resource_std_resources_AuthContext.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_AuthContext").expect("coercion target entry missing for release_resource_std_resources_AuthContext.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_AuthContext.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_AuthContext.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_AuthContext.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Network.resource_handle 1 → tools.gist::gist.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist.__deps.
#[test]
fn test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_deps() {
    if !guard_test(
        "test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Network.resource_handle → tools.gist::gist.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist")
        .expect("coercion target entry missing for tools.gist::gist.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::gist.url 1 → release_resource_std_resources_Network.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Network.resource_handle.
#[test]
fn test_coercion_tools_gist_gist_url_release_resource_std_resources_network_resource_handle() {
    if !guard_test(
        "test_coercion_tools_gist_gist_url_release_resource_std_resources_network_resource_handle",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::gist.url → release_resource_std_resources_Network.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Network").expect("coercion target entry missing for release_resource_std_resources_Network.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Network.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Network.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Network.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Network.resource_handle 1 → tools.gist::gist_diff.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_diff.__deps.
#[test]
fn test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_diff_deps()
{
    if !guard_test("test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_diff_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Network.resource_handle → tools.gist::gist_diff.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_diff")
        .expect("coercion target entry missing for tools.gist::gist_diff.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_diff.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_diff.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_diff.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::gist_diff.url 1 → release_resource_std_resources_Network.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Network.resource_handle.
#[test]
fn test_coercion_tools_gist_gist_diff_url_release_resource_std_resources_network_resource_handle() {
    if !guard_test("test_coercion_tools_gist_gist_diff_url_release_resource_std_resources_network_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::gist_diff.url → release_resource_std_resources_Network.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Network").expect("coercion target entry missing for release_resource_std_resources_Network.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Network.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Network.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Network.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Network.resource_handle 1 → tools.gist::gist_recent.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at tools.gist::gist_recent.__deps.
#[test]
fn test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_recent_deps(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_network_resource_handle_tools_gist_gist_recent_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Network.resource_handle → tools.gist::gist_recent.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "tools.gist::gist_recent")
        .expect("coercion target entry missing for tools.gist::gist_recent.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target tools.gist::gist_recent.__deps should capture inputs");
    let received = target_inputs
        .get("__deps")
        .expect("coercion target tools.gist::gist_recent.__deps should receive coerced value");
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to tools.gist::gist_recent.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: tools.gist::gist_recent.url 1 → release_resource_std_resources_Network.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Network.resource_handle.
#[test]
fn test_coercion_tools_gist_gist_recent_url_release_resource_std_resources_network_resource_handle()
{
    if !guard_test("test_coercion_tools_gist_gist_recent_url_release_resource_std_resources_network_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at tools.gist::gist_recent.url → release_resource_std_resources_Network.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Network").expect("coercion target entry missing for release_resource_std_resources_Network.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Network.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Network.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Network.resource_handle; got {:?}", received);
}

/// Coercion coverage: acquire_resource_std_resources_Network.resource_handle 1 → extdeps.github.auth::github_token.__deps 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at extdeps.github.auth::github_token.__deps.
#[test]
fn test_coercion_acquire_resource_std_resources_network_resource_handle_extdeps_github_auth_github_token_deps(
) {
    if !guard_test("test_coercion_acquire_resource_std_resources_network_resource_handle_extdeps_github_auth_github_token_deps", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at acquire_resource_std_resources_Network.resource_handle → extdeps.github.auth::github_token.__deps should not crash");
    let target_entry = log
        .entries
        .iter()
        .rev()
        .find(|entry| entry.node_id == "extdeps.github.auth::github_token")
        .expect("coercion target entry missing for extdeps.github.auth::github_token.__deps");
    let target_inputs = target_entry
        .inputs
        .as_ref()
        .expect("coercion target extdeps.github.auth::github_token.__deps should capture inputs");
    let received = target_inputs.get("__deps").expect(
        "coercion target extdeps.github.auth::github_token.__deps should receive coerced value",
    );
    assert!(
        matches!(received, Value::List(_)),
        "WrapScalar should deliver list to extdeps.github.auth::github_token.__deps; got {:?}",
        received
    );
}

/// Coercion coverage: extdeps.github.auth::github_token.token 1 → release_resource_std_resources_Network.resource_handle 0..* (WrapScalar).
///
/// Proves: engine correctly applies WrapScalar coercion at this edge and delivers the expected input shape at release_resource_std_resources_Network.resource_handle.
#[test]
fn test_coercion_extdeps_github_auth_github_token_token_release_resource_std_resources_network_resource_handle(
) {
    if !guard_test("test_coercion_extdeps_github_auth_github_token_token_release_resource_std_resources_network_resource_handle", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mocks = mock_spec().to_boundary_mocks();
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("coercion WrapScalar at extdeps.github.auth::github_token.token → release_resource_std_resources_Network.resource_handle should not crash");
    let target_entry = log.entries.iter().rev().find(|entry| entry.node_id == "release_resource_std_resources_Network").expect("coercion target entry missing for release_resource_std_resources_Network.resource_handle");
    let target_inputs = target_entry.inputs.as_ref().expect("coercion target release_resource_std_resources_Network.resource_handle should capture inputs");
    let received = target_inputs.get("resource_handle").expect("coercion target release_resource_std_resources_Network.resource_handle should receive coerced value");
    assert!(matches!(received, Value::List(_)), "WrapScalar should deliver list to release_resource_std_resources_Network.resource_handle; got {:?}", received);
}

/// Optional input: tools.gist::lang_for_path.__deps (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_tools_gist_lang_for_path_deps() {
    if !guard_test(
        "test_optional_missing_tools_gist_lang_for_path_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert(
        "__out:return".to_string(),
        Value::Str("example".to_string()),
    );
    inputs.insert("path".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::lang_for_path",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("optional input tools.gist::lang_for_path.__deps missing should not error");
}

/// Optional input: tools.gist::render_file_section.__deps (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_tools_gist_render_file_section_deps() {
    if !guard_test(
        "test_optional_missing_tools_gist_render_file_section_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("mock".to_string()));
    inputs.insert("content".to_string(), Value::Str("mock".to_string()));
    inputs.insert("path".to_string(), Value::Str("mock".to_string()));
    let _outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::render_file_section",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("optional input tools.gist::render_file_section.__deps missing should not error");
}

/// Optional input: tools.gist::gist.__deps (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_tools_gist_gist_deps() {
    if !guard_test(
        "test_optional_missing_tools_gist_gist_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert(
        "__out:url".to_string(),
        Value::Str("https://example.com".to_string()),
    );
    inputs.insert("public".to_string(), Value::Bool(true));
    let _outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::gist",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("optional input tools.gist::gist.__deps missing should not error");
}

/// Optional input: tools.gist::gist_diff.__deps (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_tools_gist_gist_diff_deps() {
    if !guard_test(
        "test_optional_missing_tools_gist_gist_diff_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert(
        "__out:url".to_string(),
        Value::Str("https://example.com".to_string()),
    );
    inputs.insert(
        "base_ref".to_string(),
        Value::Json(serde_json::json!({"mock":true})),
    );
    inputs.insert("public".to_string(), Value::Bool(true));
    let _outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::gist_diff",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("optional input tools.gist::gist_diff.__deps missing should not error");
}

/// Optional input: tools.gist::gist_recent.__deps (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_tools_gist_gist_recent_deps() {
    if !guard_test(
        "test_optional_missing_tools_gist_gist_recent_deps",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert(
        "__out:url".to_string(),
        Value::Str("https://example.com".to_string()),
    );
    inputs.insert("public".to_string(), Value::Bool(true));
    inputs.insert("since".to_string(), Value::Str("mock".to_string()));
    let _outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::gist_recent",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("optional input tools.gist::gist_recent.__deps missing should not error");
}

/// Optional input: release_resource_std_resources_Filesystem.resource_handle (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_release_resource_std_resources_filesystem_resource_handle() {
    if !guard_test(
        "test_optional_missing_release_resource_std_resources_filesystem_resource_handle",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let inputs = std::collections::HashMap::new();
    let _outputs = gunbc_exec::execute_single_node(&dag, "release_resource_std_resources_Filesystem", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input release_resource_std_resources_Filesystem.resource_handle missing should not error");
}

/// Optional input: release_resource_std_resources_Network.resource_handle (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_release_resource_std_resources_network_resource_handle() {
    if !guard_test(
        "test_optional_missing_release_resource_std_resources_network_resource_handle",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let inputs = std::collections::HashMap::new();
    let _outputs = gunbc_exec::execute_single_node(&dag, "release_resource_std_resources_Network", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input release_resource_std_resources_Network.resource_handle missing should not error");
}

/// Optional input: release_resource_std_resources_Clock.resource_handle (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_release_resource_std_resources_clock_resource_handle() {
    if !guard_test(
        "test_optional_missing_release_resource_std_resources_clock_resource_handle",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let inputs = std::collections::HashMap::new();
    let _outputs = gunbc_exec::execute_single_node(&dag, "release_resource_std_resources_Clock", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input release_resource_std_resources_Clock.resource_handle missing should not error");
}

/// Optional input: release_resource_std_resources_AuthContext.resource_handle (cardinality: 0..*).
///
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_release_resource_std_resources_auth_context_resource_handle() {
    if !guard_test(
        "test_optional_missing_release_resource_std_resources_auth_context_resource_handle",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let inputs = std::collections::HashMap::new();
    let _outputs = gunbc_exec::execute_single_node(&dag, "release_resource_std_resources_AuthContext", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input release_resource_std_resources_AuthContext.resource_handle missing should not error");
}

// =========================================================================
// Bucket C: Scenario Coverage
// =========================================================================

// N+1 scenarios: one success + one per-transport failure + guard toggles.
// 11 single-failure scenarios (one per transport executor).
// Full failure scenarios require per-transport failure mocks (Tier 0 infra).
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_CurrentBranch'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_CurrentBranch.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_LsFiles'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_LsFiles.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_Diff'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_Diff.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_RevListBase'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_RevListBase.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_Show'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_Show.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_github_gists_github_Gist_Create'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_github_gists_github_Gist_Create.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_CurrentBranch_c1.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_github_gists_github_Gist_Create_c1'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_github_gists_github_Gist_Create_c1.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_CurrentBranch_c2.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_git_git_Core_Diff_c1'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_git_git_Core_Diff_c1.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//
// Guard branch: 'execute_transport_extdeps_github_gists_github_Gist_Create_c2'.request (type: TransportRequest)
//   fed by: prepare_transport_extdeps_github_gists_github_Gist_Create_c2.request
//   upstream is pure — needs per-node isolation (Tier 1) for full test
//

/// Happy path: all transports succeed.
///
/// Proves: workflow reaches terminal outputs with all transports mocked as success.
#[test]
fn test_scenario_all_succeed() {
    if !guard_test(
        "test_scenario_all_succeed",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("all-succeed scenario should complete");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_CurrentBranch")
        .expect("'execute_transport_extdeps_git_git_Core_CurrentBranch' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_CurrentBranch' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_LsFiles")
        .expect("'execute_transport_extdeps_git_git_Core_LsFiles' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_LsFiles' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_Diff")
        .expect("'execute_transport_extdeps_git_git_Core_Diff' should be in log");
    assert!(
        entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)),
        "'execute_transport_extdeps_git_git_Core_Diff' should be intercepted or skipped in DryRun"
    );
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_RevListBase")
        .expect("'execute_transport_extdeps_git_git_Core_RevListBase' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_RevListBase' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_Show")
        .expect("'execute_transport_extdeps_git_git_Core_Show' should be in log");
    assert!(
        entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)),
        "'execute_transport_extdeps_git_git_Core_Show' should be intercepted or skipped in DryRun"
    );
    let entry = log
        .get("execute_transport_extdeps_github_gists_github_Gist_Create")
        .expect("'execute_transport_extdeps_github_gists_github_Gist_Create' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_github_gists_github_Gist_Create' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_CurrentBranch_c1")
        .expect("'execute_transport_extdeps_git_git_Core_CurrentBranch_c1' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_CurrentBranch_c1' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_github_gists_github_Gist_Create_c1")
        .expect("'execute_transport_extdeps_github_gists_github_Gist_Create_c1' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_github_gists_github_Gist_Create_c1' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_CurrentBranch_c2")
        .expect("'execute_transport_extdeps_git_git_Core_CurrentBranch_c2' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_CurrentBranch_c2' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_git_git_Core_Diff_c1")
        .expect("'execute_transport_extdeps_git_git_Core_Diff_c1' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_git_git_Core_Diff_c1' should be intercepted or skipped in DryRun");
    let entry = log
        .get("execute_transport_extdeps_github_gists_github_Gist_Create_c2")
        .expect("'execute_transport_extdeps_github_gists_github_Gist_Create_c2' should be in log");
    assert!(entry.was_intercepted || entry.outputs.values().all(|v| matches!(v, Value::Skipped)), "'execute_transport_extdeps_github_gists_github_Gist_Create_c2' should be intercepted or skipped in DryRun");
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_CurrentBranch' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_current_branch_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_current_branch_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_CurrentBranch'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_LsFiles' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_ls_files_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_ls_files_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_LsFiles'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_LsFiles",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_Diff' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_diff_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_diff_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_Diff'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Diff",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_RevListBase' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_rev_list_base_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_rev_list_base_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_RevListBase'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_RevListBase",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_Show' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_show_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_show_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_Show'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Show",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_github_gists_github_Gist_Create' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_github_gists_github_gist_create_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_github_gists_github_gist_create_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_github_gists_github_Gist_Create'
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_current_branch_c1_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_current_branch_c1_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch_c1",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_github_gists_github_Gist_Create_c1' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_github_gists_github_gist_create_c1_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_github_gists_github_gist_create_c1_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_github_gists_github_Gist_Create_c1'
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_current_branch_c2_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_current_branch_c2_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch_c2",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_git_git_Core_Diff_c1' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_git_git_core_diff_c1_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_git_git_core_diff_c1_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_git_git_Core_Diff_c1'
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Diff_c1",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Single failure: 'execute_transport_extdeps_github_gists_github_Gist_Create_c2' transport fails.
///
/// Proves: failure propagation semantics are consistent.
#[test]
fn test_scenario_execute_transport_extdeps_github_gists_github_gist_create_c2_fails() {
    if !guard_test(
        "test_scenario_execute_transport_extdeps_github_gists_github_gist_create_c2_fails",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    // Inject failure at 'execute_transport_extdeps_github_gists_github_Gist_Create_c2'
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
        "response",
        Value::Str("<TRANSPORT_FAILURE>".to_string()),
    );
    // Execution may succeed or fail depending on graph semantics;
    // the key property is that it doesn't crash/hang.
    let _result = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    );
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_CurrentBranch' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_git_git_Core_CurrentBranch").is_some(), "downstream 'parse_transport_extdeps_git_git_Core_CurrentBranch' should still appear in log");
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_LsFiles' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_ls_files() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_ls_files",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_LsFiles",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(
        log.get("parse_transport_extdeps_git_git_Core_LsFiles")
            .is_some(),
        "downstream 'parse_transport_extdeps_git_git_Core_LsFiles' should still appear in log"
    );
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_Diff' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_diff() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_diff",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Diff",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(
        log.get("parse_transport_extdeps_git_git_Core_Diff")
            .is_some(),
        "downstream 'parse_transport_extdeps_git_git_Core_Diff' should still appear in log"
    );
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_RevListBase' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_rev_list_base() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_rev_list_base",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_RevListBase",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(
        log.get("parse_transport_extdeps_git_git_Core_RevListBase")
            .is_some(),
        "downstream 'parse_transport_extdeps_git_git_Core_RevListBase' should still appear in log"
    );
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_Show' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_show() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_show",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Show",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(
        log.get("parse_transport_extdeps_git_git_Core_Show")
            .is_some(),
        "downstream 'parse_transport_extdeps_git_git_Core_Show' should still appear in log"
    );
}

/// Skip propagation: 'execute_transport_extdeps_github_gists_github_Gist_Create' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_github_gists_github_Gist_Create").is_some(), "downstream 'parse_transport_extdeps_github_gists_github_Gist_Create' should still appear in log");
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c1' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch_c1() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch_c1",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch_c1",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_git_git_Core_CurrentBranch_c1").is_some(), "downstream 'parse_transport_extdeps_git_git_Core_CurrentBranch_c1' should still appear in log");
}

/// Skip propagation: 'execute_transport_extdeps_github_gists_github_Gist_Create_c1' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create_c1() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create_c1",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_github_gists_github_Gist_Create_c1").is_some(), "downstream 'parse_transport_extdeps_github_gists_github_Gist_Create_c1' should still appear in log");
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_CurrentBranch_c2' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch_c2() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_current_branch_c2",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_CurrentBranch_c2",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_git_git_Core_CurrentBranch_c2").is_some(), "downstream 'parse_transport_extdeps_git_git_Core_CurrentBranch_c2' should still appear in log");
}

/// Skip propagation: 'execute_transport_extdeps_git_git_Core_Diff_c1' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_git_git_core_diff_c1() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_git_git_core_diff_c1",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_git_git_Core_Diff_c1",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(
        log.get("parse_transport_extdeps_git_git_Core_Diff_c1")
            .is_some(),
        "downstream 'parse_transport_extdeps_git_git_Core_Diff_c1' should still appear in log"
    );
}

/// Skip propagation: 'execute_transport_extdeps_github_gists_github_Gist_Create_c2' returns Skipped → downstream handles it.
///
/// Proves: when a transport's output is Skipped, downstream nodes
/// either skip themselves (guarded) or process the Skipped value
/// without crashing.
#[test]
fn test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create_c2() {
    if !guard_test(
        "test_skip_propagation_execute_transport_extdeps_github_gists_github_gist_create_c2",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
        "response",
        Value::Skipped,
    );
    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("skip propagation should not crash or hang");
    assert!(log.get("parse_transport_extdeps_github_gists_github_Gist_Create_c2").is_some(), "downstream 'parse_transport_extdeps_github_gists_github_Gist_Create_c2' should still appear in log");
}

// =========================================================================
// Boundary Tests (per-node mockability)
// =========================================================================

/// Test that all boundaries can be mocked.
#[test]
fn test_boundaries_mockable() {
    if !guard_test(
        "test_boundaries_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(
        result.is_ok(),
        "Boundaries should be mockable: {:?}",
        result.error
    );
}

/// Test that parse_transport_extdeps_git_git_Core_Diff_c1 boundary can be mocked.
#[test]
fn test_boundary_parse_transport_extdeps_git_git_core_diff_c1_mockable() {
    if !guard_test(
        "test_boundary_parse_transport_extdeps_git_git_core_diff_c1_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"parse_transport_extdeps_git_git_Core_Diff_c1".into()),
        "parse_transport_extdeps_git_git_Core_Diff_c1 should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "parse_transport_extdeps_git_git_Core_Diff_c1",
        "diff",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("parse_transport_extdeps_git_git_Core_Diff_c1")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that parse_transport_extdeps_git_git_Core_Show boundary can be mocked.
#[test]
fn test_boundary_parse_transport_extdeps_git_git_core_show_mockable() {
    if !guard_test(
        "test_boundary_parse_transport_extdeps_git_git_core_show_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"parse_transport_extdeps_git_git_Core_Show".into()),
        "parse_transport_extdeps_git_git_Core_Show should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "parse_transport_extdeps_git_git_Core_Show",
        "content",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("parse_transport_extdeps_git_git_Core_Show")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that release_resource_std_resources_AuthContext boundary can be mocked.
#[test]
fn test_boundary_release_resource_std_resources_auth_context_mockable() {
    if !guard_test(
        "test_boundary_release_resource_std_resources_auth_context_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"release_resource_std_resources_AuthContext".into()),
        "release_resource_std_resources_AuthContext should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "release_resource_std_resources_AuthContext",
        "released",
        Value::Bool(true),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("release_resource_std_resources_AuthContext")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that release_resource_std_resources_Clock boundary can be mocked.
#[test]
fn test_boundary_release_resource_std_resources_clock_mockable() {
    if !guard_test(
        "test_boundary_release_resource_std_resources_clock_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"release_resource_std_resources_Clock".into()),
        "release_resource_std_resources_Clock should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "release_resource_std_resources_Clock",
        "released",
        Value::Bool(true),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("release_resource_std_resources_Clock")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that release_resource_std_resources_Filesystem boundary can be mocked.
#[test]
fn test_boundary_release_resource_std_resources_filesystem_mockable() {
    if !guard_test(
        "test_boundary_release_resource_std_resources_filesystem_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"release_resource_std_resources_Filesystem".into()),
        "release_resource_std_resources_Filesystem should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "release_resource_std_resources_Filesystem",
        "released",
        Value::Bool(true),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("release_resource_std_resources_Filesystem")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that release_resource_std_resources_Network boundary can be mocked.
#[test]
fn test_boundary_release_resource_std_resources_network_mockable() {
    if !guard_test(
        "test_boundary_release_resource_std_resources_network_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"release_resource_std_resources_Network".into()),
        "release_resource_std_resources_Network should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "release_resource_std_resources_Network",
        "released",
        Value::Bool(true),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("release_resource_std_resources_Network")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that tools.gist::gist boundary can be mocked.
#[test]
fn test_boundary_tools_gist_gist_mockable() {
    if !guard_test(
        "test_boundary_tools_gist_gist_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"tools.gist::gist".into()),
        "tools.gist::gist should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("tools.gist::gist", "url", Value::Str("mock".to_string()));

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log.get("tools.gist::gist").expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that tools.gist::gist_diff boundary can be mocked.
#[test]
fn test_boundary_tools_gist_gist_diff_mockable() {
    if !guard_test(
        "test_boundary_tools_gist_gist_diff_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"tools.gist::gist_diff".into()),
        "tools.gist::gist_diff should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "tools.gist::gist_diff",
        "url",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("tools.gist::gist_diff")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that tools.gist::gist_recent boundary can be mocked.
#[test]
fn test_boundary_tools_gist_gist_recent_mockable() {
    if !guard_test(
        "test_boundary_tools_gist_gist_recent_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"tools.gist::gist_recent".into()),
        "tools.gist::gist_recent should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "tools.gist::gist_recent",
        "url",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("tools.gist::gist_recent")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that tools.gist::lang_for_path boundary can be mocked.
#[test]
fn test_boundary_tools_gist_lang_for_path_mockable() {
    if !guard_test(
        "test_boundary_tools_gist_lang_for_path_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"tools.gist::lang_for_path".into()),
        "tools.gist::lang_for_path should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "tools.gist::lang_for_path",
        "return",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("tools.gist::lang_for_path")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

/// Test that tools.gist::render_file_section boundary can be mocked.
#[test]
fn test_boundary_tools_gist_render_file_section_mockable() {
    if !guard_test(
        "test_boundary_tools_gist_render_file_section_mockable",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(
        boundaries.is_boundary_node(&"tools.gist::render_file_section".into()),
        "tools.gist::render_file_section should be a boundary"
    );

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value(
        "tools.gist::render_file_section",
        "return",
        Value::Str("mock".to_string()),
    );

    let log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .unwrap();
    let entry = log
        .get("tools.gist::render_file_section")
        .expect("node should be in log");
    assert!(
        entry.was_intercepted,
        "boundary should be intercepted in dry-run"
    );
}

// =========================================================================
// Chain Validation Tests
// =========================================================================

// These tests verify that mock outputs satisfy downstream input expectations.

/// Test that this tool's mock spec is self-consistent.
#[test]
fn test_mock_spec_self_consistent() {
    if !guard_test(
        "test_mock_spec_self_consistent",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let spec = mock_spec();
    // Verify all boundary mocks are present
    assert!(spec.get_boundary_mock("acquire_resource_std_resources_Filesystem", "resource_handle").is_some(), "MockSpec should have boundary mock for acquire_resource_std_resources_Filesystem.resource_handle");
    assert!(spec.get_boundary_mock("acquire_resource_std_resources_Network", "resource_handle").is_some(), "MockSpec should have boundary mock for acquire_resource_std_resources_Network.resource_handle");
    assert!(spec.get_boundary_mock("acquire_resource_std_resources_Clock", "resource_handle").is_some(), "MockSpec should have boundary mock for acquire_resource_std_resources_Clock.resource_handle");
    assert!(spec.get_boundary_mock("acquire_resource_std_resources_AuthContext", "resource_handle").is_some(), "MockSpec should have boundary mock for acquire_resource_std_resources_AuthContext.resource_handle");
    assert!(
        spec.get_boundary_mock("fs_env", "FilesystemHandle")
            .is_some(),
        "MockSpec should have boundary mock for fs_env.FilesystemHandle"
    );
    assert!(
        spec.get_boundary_mock("tools.gist::lang_for_path", "return")
            .is_some(),
        "MockSpec should have boundary mock for tools.gist::lang_for_path.return"
    );
    assert!(
        spec.get_boundary_mock("tools.gist::render_file_section", "return")
            .is_some(),
        "MockSpec should have boundary mock for tools.gist::render_file_section.return"
    );
    assert!(
        spec.get_boundary_mock("tools.gist::gist", "url").is_some(),
        "MockSpec should have boundary mock for tools.gist::gist.url"
    );
    assert!(
        spec.get_boundary_mock("tools.gist::gist_diff", "url")
            .is_some(),
        "MockSpec should have boundary mock for tools.gist::gist_diff.url"
    );
    assert!(
        spec.get_boundary_mock("tools.gist::gist_recent", "url")
            .is_some(),
        "MockSpec should have boundary mock for tools.gist::gist_recent.url"
    );
    assert!(
        spec.get_boundary_mock("parse_transport_extdeps_git_git_Core_Show", "content")
            .is_some(),
        "MockSpec should have boundary mock for parse_transport_extdeps_git_git_Core_Show.content"
    );
    assert!(spec.get_boundary_mock("parse_transport_extdeps_github_gists_github_Gist_Create", "id").is_some(), "MockSpec should have boundary mock for parse_transport_extdeps_github_gists_github_Gist_Create.id");
    assert!(spec.get_boundary_mock("parse_transport_extdeps_github_gists_github_Gist_Create_c1", "id").is_some(), "MockSpec should have boundary mock for parse_transport_extdeps_github_gists_github_Gist_Create_c1.id");
    assert!(
        spec.get_boundary_mock("parse_transport_extdeps_git_git_Core_Diff_c1", "diff")
            .is_some(),
        "MockSpec should have boundary mock for parse_transport_extdeps_git_git_Core_Diff_c1.diff"
    );
    assert!(spec.get_boundary_mock("parse_transport_extdeps_github_gists_github_Gist_Create_c2", "id").is_some(), "MockSpec should have boundary mock for parse_transport_extdeps_github_gists_github_Gist_Create_c2.id");
    assert!(
        spec.get_boundary_mock("release_resource_std_resources_Filesystem", "released")
            .is_some(),
        "MockSpec should have boundary mock for release_resource_std_resources_Filesystem.released"
    );
    assert!(
        spec.get_boundary_mock("release_resource_std_resources_Network", "released")
            .is_some(),
        "MockSpec should have boundary mock for release_resource_std_resources_Network.released"
    );
    assert!(
        spec.get_boundary_mock("release_resource_std_resources_Clock", "released")
            .is_some(),
        "MockSpec should have boundary mock for release_resource_std_resources_Clock.released"
    );
    assert!(spec.get_boundary_mock("release_resource_std_resources_AuthContext", "released").is_some(), "MockSpec should have boundary mock for release_resource_std_resources_AuthContext.released");
}

// =========================================================================
// Flow Verification Tests
// =========================================================================

// These tests execute the full DAG in DryRun mode with mocked transport
// responses, verifying that pure node logic produces expected outputs.

/// Flow verification: tools-gist scenario.
///
/// Builds the DAG, injects mocked transport responses via DryRun,
/// and verifies that the pure node chain produces expected terminal outputs.
#[test]
fn test_flow_tools_gist() {
    if !guard_test(
        "test_flow_tools_gist",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let spec = mock_spec();
    let mocks = spec.to_boundary_mocks();
    let _log = execute_dag(
        &dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("DryRun execution should succeed");
}

// =========================================================================
// Probe-Observer Integration Tests
// =========================================================================

// Probes: 39 | Observers: 6 | Tests: 34
// All terminal nodes are observed.

/// Chain test: acquire_resource_std_resources_AuthContext -> release_resource_std_resources_AuthContext (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_acquire_resource_std_resources_auth_context_to_release_resource_std_resources_auth_context(
) {
    if !guard_test("test_chain_acquire_resource_std_resources_auth_context_to_release_resource_std_resources_auth_context", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "acquire_resource_std_resources_AuthContext",
            "release_resource_std_resources_AuthContext",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_AuthContext (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_AuthContext")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_AuthContext".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_AuthContext")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_AuthContext".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain acquire_resource_std_resources_AuthContext -> release_resource_std_resources_AuthContext should satisfy observer matchers");
}

/// Chain test: acquire_resource_std_resources_Clock -> release_resource_std_resources_Clock (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_acquire_resource_std_resources_clock_to_release_resource_std_resources_clock() {
    if !guard_test(
        "test_chain_acquire_resource_std_resources_clock_to_release_resource_std_resources_clock",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "acquire_resource_std_resources_Clock",
            "release_resource_std_resources_Clock",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Clock (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Clock")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Clock".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Clock")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Clock".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain acquire_resource_std_resources_Clock -> release_resource_std_resources_Clock should satisfy observer matchers");
}

/// Chain test: acquire_resource_std_resources_Filesystem -> release_resource_std_resources_Filesystem (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_acquire_resource_std_resources_filesystem_to_release_resource_std_resources_filesystem(
) {
    if !guard_test("test_chain_acquire_resource_std_resources_filesystem_to_release_resource_std_resources_filesystem", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "acquire_resource_std_resources_Filesystem",
            "release_resource_std_resources_Filesystem",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Filesystem (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Filesystem")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Filesystem".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Filesystem")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Filesystem".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain acquire_resource_std_resources_Filesystem -> release_resource_std_resources_Filesystem should satisfy observer matchers");
}

/// Chain test: acquire_resource_std_resources_Network -> release_resource_std_resources_Network (depth 12)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_acquire_resource_std_resources_network_to_release_resource_std_resources_network() {
    if !guard_test("test_chain_acquire_resource_std_resources_network_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "acquire_resource_std_resources_Network",
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "extdeps.github.auth::github_token",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 12)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain acquire_resource_std_resources_Network -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_CurrentBranch -> release_resource_std_resources_Network (depth 8)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_current_branch_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_current_branch_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_CurrentBranch",
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "parse_transport_extdeps_git_git_Core_CurrentBranch",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "prepare_transport_extdeps_github_gists_github_Gist_Create",
            "release_resource_std_resources_Network",
            "tools.gist::build_snapshot_content",
            "tools.gist::gist",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 8)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_CurrentBranch -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_CurrentBranch_c1 -> release_resource_std_resources_Network (depth 12)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_current_branch_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_current_branch_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_CurrentBranch_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "parse_transport_extdeps_git_git_Core_CurrentBranch_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
            "tools.gist::render_diff_markdown",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 12)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_CurrentBranch_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_CurrentBranch_c2 -> release_resource_std_resources_Network (depth 7)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_current_branch_c2_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_current_branch_c2_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_CurrentBranch_c2",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "parse_transport_extdeps_git_git_Core_CurrentBranch_c2",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 7)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_CurrentBranch_c2 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network (depth 12)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_diff_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_diff_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "parse_transport_extdeps_git_git_Core_Diff",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
            "tools.gist::render_diff_markdown",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 12)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 4)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff_c1",
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_LsFiles -> release_resource_std_resources_Network (depth 10)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_ls_files_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_ls_files_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_LsFiles",
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "parse_transport_extdeps_git_git_Core_LsFiles",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "prepare_transport_extdeps_github_gists_github_Gist_Create",
            "release_resource_std_resources_Network",
            "tools.gist::build_snapshot_content",
            "tools.gist::gist",
            "tools.gist::gist::cf_for_0/pack",
            "tools.gist::gist::cf_for_0/unpack",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 10)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_LsFiles -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_RevListBase -> release_resource_std_resources_Network (depth 7)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_rev_list_base_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_rev_list_base_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff_c1",
            "execute_transport_extdeps_git_git_Core_RevListBase",
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "parse_transport_extdeps_git_git_Core_RevListBase",
            "prepare_transport_extdeps_git_git_Core_Diff_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 7)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_RevListBase -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_git_git_core_show_to_parse_transport_extdeps_git_git_core_show(
) {
    if !guard_test("test_chain_execute_transport_extdeps_git_git_core_show_to_parse_transport_extdeps_git_git_core_show", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Show",
            "parse_transport_extdeps_git_git_Core_Show",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: parse_transport_extdeps_git_git_Core_Show (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "parse_transport_extdeps_git_git_Core_Show")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "parse_transport_extdeps_git_git_Core_Show".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "parse_transport_extdeps_git_git_Core_Show")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "parse_transport_extdeps_git_git_Core_Show".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network (depth 4)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_github_gists_github_gist_create_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_github_gists_github_gist_create_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "release_resource_std_resources_Network",
            "tools.gist::gist",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network (depth 4)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_github_gists_github_gist_create_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_github_gists_github_gist_create_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: execute_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network (depth 4)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_execute_transport_extdeps_github_gists_github_gist_create_c2_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_execute_transport_extdeps_github_gists_github_gist_create_c2_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 4)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain execute_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: fs_env -> parse_transport_extdeps_git_git_Core_Show (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_fs_env_to_parse_transport_extdeps_git_git_core_show() {
    if !guard_test(
        "test_chain_fs_env_to_parse_transport_extdeps_git_git_core_show",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Show",
            "fs_env",
            "parse_transport_extdeps_git_git_Core_Show",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: parse_transport_extdeps_git_git_Core_Show (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "parse_transport_extdeps_git_git_Core_Show")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "parse_transport_extdeps_git_git_Core_Show".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "parse_transport_extdeps_git_git_Core_Show")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "parse_transport_extdeps_git_git_Core_Show".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain fs_env -> parse_transport_extdeps_git_git_Core_Show should satisfy observer matchers");
}

/// Chain test: fs_env -> release_resource_std_resources_Network (depth 33)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_fs_env_to_release_resource_std_resources_network() {
    if !guard_test(
        "test_chain_fs_env_to_release_resource_std_resources_network",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_CurrentBranch",
            "execute_transport_extdeps_git_git_Core_CurrentBranch_c1",
            "execute_transport_extdeps_git_git_Core_CurrentBranch_c2",
            "execute_transport_extdeps_git_git_Core_Diff",
            "execute_transport_extdeps_git_git_Core_Diff_c1",
            "execute_transport_extdeps_git_git_Core_LsFiles",
            "execute_transport_extdeps_git_git_Core_RevListBase",
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "fs_env",
            "parse_transport_extdeps_git_git_Core_CurrentBranch",
            "parse_transport_extdeps_git_git_Core_CurrentBranch_c1",
            "parse_transport_extdeps_git_git_Core_CurrentBranch_c2",
            "parse_transport_extdeps_git_git_Core_Diff",
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "parse_transport_extdeps_git_git_Core_LsFiles",
            "parse_transport_extdeps_git_git_Core_RevListBase",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_git_git_Core_Diff_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::build_snapshot_content",
            "tools.gist::gist",
            "tools.gist::gist::cf_for_0/pack",
            "tools.gist::gist::cf_for_0/unpack",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
            "tools.gist::render_diff_markdown",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 33)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect(
        "chain fs_env -> release_resource_std_resources_Network should satisfy observer matchers",
    );
}

/// Chain test: param_source_tools_gist_gist_diff_base_ref -> release_resource_std_resources_Network (depth 14)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_gist_diff_base_ref_to_release_resource_std_resources_network()
{
    if !guard_test("test_chain_param_source_tools_gist_gist_diff_base_ref_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "param_source_tools_gist_gist_diff_base_ref",
            "parse_transport_extdeps_git_git_Core_Diff",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_git_git_Core_Diff",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
            "tools.gist::render_diff_markdown",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 14)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_gist_diff_base_ref -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: param_source_tools_gist_gist_diff_public -> release_resource_std_resources_Network (depth 6)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_gist_diff_public_to_release_resource_std_resources_network() {
    if !guard_test("test_chain_param_source_tools_gist_gist_diff_public_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "param_source_tools_gist_gist_diff_public",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 6)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_gist_diff_public -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: param_source_tools_gist_gist_public -> release_resource_std_resources_Network (depth 6)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_gist_public_to_release_resource_std_resources_network() {
    if !guard_test(
        "test_chain_param_source_tools_gist_gist_public_to_release_resource_std_resources_network",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create",
            "param_source_tools_gist_gist_public",
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "prepare_transport_extdeps_github_gists_github_Gist_Create",
            "release_resource_std_resources_Network",
            "tools.gist::gist",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 6)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_gist_public -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: param_source_tools_gist_gist_recent_public -> release_resource_std_resources_Network (depth 6)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_gist_recent_public_to_release_resource_std_resources_network()
{
    if !guard_test("test_chain_param_source_tools_gist_gist_recent_public_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "param_source_tools_gist_gist_recent_public",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 6)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_gist_recent_public -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: param_source_tools_gist_gist_recent_since -> release_resource_std_resources_Network (depth 9)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_gist_recent_since_to_release_resource_std_resources_network()
{
    if !guard_test("test_chain_param_source_tools_gist_gist_recent_since_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff_c1",
            "execute_transport_extdeps_git_git_Core_RevListBase",
            "param_source_tools_gist_gist_recent_since",
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "parse_transport_extdeps_git_git_Core_RevListBase",
            "prepare_transport_extdeps_git_git_Core_Diff_c1",
            "prepare_transport_extdeps_git_git_Core_RevListBase",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 9)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_gist_recent_since -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: param_source_tools_gist_render_file_section_path -> tools.gist::render_file_section (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_param_source_tools_gist_render_file_section_path_to_tools_gist_render_file_section() {
    if !guard_test("test_chain_param_source_tools_gist_render_file_section_path_to_tools_gist_render_file_section", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "param_source_tools_gist_render_file_section_path",
            "tools.gist::lang_for_path",
            "tools.gist::render_file_section",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: tools.gist::render_file_section (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "tools.gist::render_file_section")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    ("tools.gist::render_file_section".to_string(), port.clone()),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "tools.gist::render_file_section")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "tools.gist::render_file_section".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain param_source_tools_gist_render_file_section_path -> tools.gist::render_file_section should satisfy observer matchers");
}

/// Chain test: parse_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_parse_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_parse_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain parse_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: parse_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_parse_transport_extdeps_github_gists_github_gist_create_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_parse_transport_extdeps_github_gists_github_gist_create_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "parse_transport_extdeps_github_gists_github_Gist_Create",
            "release_resource_std_resources_Network",
            "tools.gist::gist",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain parse_transport_extdeps_github_gists_github_Gist_Create -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: parse_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_parse_transport_extdeps_github_gists_github_gist_create_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_parse_transport_extdeps_github_gists_github_gist_create_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain parse_transport_extdeps_github_gists_github_Gist_Create_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: parse_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_parse_transport_extdeps_github_gists_github_gist_create_c2_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_parse_transport_extdeps_github_gists_github_gist_create_c2_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain parse_transport_extdeps_github_gists_github_Gist_Create_c2 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: prepare_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network (depth 13)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_prepare_transport_extdeps_git_git_core_diff_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_prepare_transport_extdeps_git_git_core_diff_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c1",
            "execute_transport_extdeps_github_gists_github_Gist_Create_c2",
            "parse_transport_extdeps_git_git_Core_Diff",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c1",
            "parse_transport_extdeps_github_gists_github_Gist_Create_c2",
            "prepare_transport_extdeps_git_git_Core_Diff",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c1",
            "prepare_transport_extdeps_github_gists_github_Gist_Create_c2",
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
            "tools.gist::gist_recent",
            "tools.gist::render_diff_markdown",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 13)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain prepare_transport_extdeps_git_git_Core_Diff -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: prepare_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network (depth 5)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_prepare_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network(
) {
    if !guard_test("test_chain_prepare_transport_extdeps_git_git_core_diff_c1_to_release_resource_std_resources_network", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Diff_c1",
            "parse_transport_extdeps_git_git_Core_Diff_c1",
            "prepare_transport_extdeps_git_git_Core_Diff_c1",
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 5)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain prepare_transport_extdeps_git_git_Core_Diff_c1 -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: prepare_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show (depth 3)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_prepare_transport_extdeps_git_git_core_show_to_parse_transport_extdeps_git_git_core_show(
) {
    if !guard_test("test_chain_prepare_transport_extdeps_git_git_core_show_to_parse_transport_extdeps_git_git_core_show", TestClass::Integration, FermiCost::L, &["http"], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "execute_transport_extdeps_git_git_Core_Show",
            "parse_transport_extdeps_git_git_Core_Show",
            "prepare_transport_extdeps_git_git_Core_Show",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: parse_transport_extdeps_git_git_Core_Show (depth 3)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "parse_transport_extdeps_git_git_Core_Show")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "parse_transport_extdeps_git_git_Core_Show".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "parse_transport_extdeps_git_git_Core_Show")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "parse_transport_extdeps_git_git_Core_Show".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain prepare_transport_extdeps_git_git_Core_Show -> parse_transport_extdeps_git_git_Core_Show should satisfy observer matchers");
}

/// Chain test: tools.gist::gist -> release_resource_std_resources_Network (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_tools_gist_gist_to_release_resource_std_resources_network() {
    if !guard_test(
        "test_chain_tools_gist_gist_to_release_resource_std_resources_network",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec!["release_resource_std_resources_Network", "tools.gist::gist"],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain tools.gist::gist -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: tools.gist::gist_diff -> release_resource_std_resources_Network (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_tools_gist_gist_diff_to_release_resource_std_resources_network() {
    if !guard_test(
        "test_chain_tools_gist_gist_diff_to_release_resource_std_resources_network",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "release_resource_std_resources_Network",
            "tools.gist::gist_diff",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain tools.gist::gist_diff -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: tools.gist::gist_recent -> release_resource_std_resources_Network (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_tools_gist_gist_recent_to_release_resource_std_resources_network() {
    if !guard_test(
        "test_chain_tools_gist_gist_recent_to_release_resource_std_resources_network",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "release_resource_std_resources_Network",
            "tools.gist::gist_recent",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: release_resource_std_resources_Network (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "release_resource_std_resources_Network")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    (
                        "release_resource_std_resources_Network".to_string(),
                        port.clone(),
                    ),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "release_resource_std_resources_Network")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "release_resource_std_resources_Network".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain tools.gist::gist_recent -> release_resource_std_resources_Network should satisfy observer matchers");
}

/// Chain test: tools.gist::lang_for_path -> tools.gist::render_file_section (depth 2)
///
/// Non-tautological: asserts observer matchers, not baseline values.
#[test]
fn test_chain_tools_gist_lang_for_path_to_tools_gist_render_file_section() {
    if !guard_test(
        "test_chain_tools_gist_lang_for_path_to_tools_gist_render_file_section",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let flat = lower(&dag).expect("lower should succeed").dag;
    let spec = mock_spec();
    // Full baseline DryRun to derive window entry inputs
    let baseline = execute_dag(
        &flat,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()),
            ..Default::default()
        },
    )
    .expect("baseline DryRun should succeed");
    let window = Window::from_nodes(
        &flat,
        vec![
            "tools.gist::lang_for_path",
            "tools.gist::render_file_section",
        ],
    );
    let mut mocks = spec.to_boundary_mocks();
    apply_window_inputs(&flat, &window, &baseline, &mut mocks)
        .expect("chain entry inputs should be derivable from baseline");
    let window_dag = window_subdag(&flat, &window);
    let log = execute_dag(
        &window_dag,
        ExecuteConfig {
            mode: ExecutionMode::DryRun(mocks),
            ..Default::default()
        },
    )
    .expect("chain execution should succeed");

    // Verify observer: tools.gist::render_file_section (depth 2)
    let mut matchers = HashMap::new();
    for ex in spec
        .node_examples
        .iter()
        .filter(|e| e.node_id == "tools.gist::render_file_section")
    {
        for (port, matcher) in &ex.outputs {
            if matcher.is_chain_safe() {
                matchers.insert(
                    ("tools.gist::render_file_section".to_string(), port.clone()),
                    matcher.clone(),
                );
            }
        }
    }
    for leo in spec
        .live_expected_outputs
        .iter()
        .filter(|e| e.node == "tools.gist::render_file_section")
    {
        if leo.matcher.is_chain_safe() {
            matchers.insert(
                (
                    "tools.gist::render_file_section".to_string(),
                    leo.port.clone(),
                ),
                leo.matcher.clone(),
            );
        }
    }
    assert_chain_outputs(&log, &matchers).expect("chain tools.gist::lang_for_path -> tools.gist::render_file_section should satisfy observer matchers");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: tools.gist::render_file_section (example 0)
///
/// Tests that node 'tools.gist::render_file_section' produces expected outputs for given inputs.
#[test]
fn test_example_tools_gist_render_file_section_0() {
    if !guard_test(
        "test_example_tools_gist_render_file_section_0",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("mock".to_string()));
    inputs.insert("content".to_string(), Value::Str("mock".to_string()));
    inputs.insert("path".to_string(), Value::Str("mock".to_string()));
    inputs.insert("return".to_string(), Value::Str("mock".to_string()));
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "tools.gist::render_file_section",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'tools.gist::render_file_section' should execute successfully");

    // Check output port 'return'
    let output_return = outputs
        .get("return")
        .expect("output port 'return' should exist");
    assert!(
        output_return.as_str().is_some()
            || matches!(*output_return, Value::Skipped | Value::Secret(_)),
        "expected String, Secret, or Skipped for output_return"
    );
}

/// Node example: parse_transport_extdeps_git_git_Core_Show (example 1)
///
/// Tests that node 'parse_transport_extdeps_git_git_Core_Show' produces expected outputs for given inputs.
#[test]
fn test_example_parse_transport_extdeps_git_git_core_show_1() {
    if !guard_test(
        "test_example_parse_transport_extdeps_git_git_core_show_1",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("content".to_string(), Value::Str("mock".to_string()));
    inputs.insert(
        "response".to_string(),
        Value::Response(gunbc_ir::transport::TransportResponse::Shell(
            gunbc_ir::transport::ShellResponse {
                exit_code: 0,
                stdout: "".to_string(),
                stderr: "".to_string(),
            },
        )),
    );
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "parse_transport_extdeps_git_git_Core_Show",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'parse_transport_extdeps_git_git_Core_Show' should execute successfully");

    // Check output port 'content'
    let output_content = outputs
        .get("content")
        .expect("output port 'content' should exist");
    assert!(
        output_content.as_str().is_some()
            || matches!(*output_content, Value::Skipped | Value::Secret(_)),
        "expected String, Secret, or Skipped for output_content"
    );
}

/// Node example: release_resource_std_resources_Filesystem (example 2)
///
/// Tests that node 'release_resource_std_resources_Filesystem' produces expected outputs for given inputs.
#[test]
fn test_example_release_resource_std_resources_filesystem_2() {
    if !guard_test(
        "test_example_release_resource_std_resources_filesystem_2",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("released".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "release_resource_std_resources_Filesystem",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'release_resource_std_resources_Filesystem' should execute successfully");

    // Check output port 'released'
    let output_released = outputs
        .get("released")
        .expect("output port 'released' should exist");
    assert!(
        !output_released.is_empty() || matches!(*output_released, Value::Skipped | Value::Unit),
        "expected non-empty value, Skipped, or Unit"
    );
}

/// Node example: release_resource_std_resources_Network (example 3)
///
/// Tests that node 'release_resource_std_resources_Network' produces expected outputs for given inputs.
#[test]
fn test_example_release_resource_std_resources_network_3() {
    if !guard_test(
        "test_example_release_resource_std_resources_network_3",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("released".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "release_resource_std_resources_Network",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'release_resource_std_resources_Network' should execute successfully");

    // Check output port 'released'
    let output_released = outputs
        .get("released")
        .expect("output port 'released' should exist");
    assert!(
        !output_released.is_empty() || matches!(*output_released, Value::Skipped | Value::Unit),
        "expected non-empty value, Skipped, or Unit"
    );
}

/// Node example: release_resource_std_resources_Clock (example 4)
///
/// Tests that node 'release_resource_std_resources_Clock' produces expected outputs for given inputs.
#[test]
fn test_example_release_resource_std_resources_clock_4() {
    if !guard_test(
        "test_example_release_resource_std_resources_clock_4",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("released".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "release_resource_std_resources_Clock",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'release_resource_std_resources_Clock' should execute successfully");

    // Check output port 'released'
    let output_released = outputs
        .get("released")
        .expect("output port 'released' should exist");
    assert!(
        !output_released.is_empty() || matches!(*output_released, Value::Skipped | Value::Unit),
        "expected non-empty value, Skipped, or Unit"
    );
}

/// Node example: release_resource_std_resources_AuthContext (example 5)
///
/// Tests that node 'release_resource_std_resources_AuthContext' produces expected outputs for given inputs.
#[test]
fn test_example_release_resource_std_resources_auth_context_5() {
    if !guard_test(
        "test_example_release_resource_std_resources_auth_context_5",
        TestClass::Integration,
        FermiCost::L,
        &["http"],
        &[],
    ) {
        return;
    };
    let dag = gunbc_resolve::builder::build_dsl_graph_dag(
        "tools/gist.dag",
        gunbc_resolve::BuildOpts::default(),
    )
    .expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("released".to_string(), Value::Bool(true));
    let outputs = gunbc_exec::execute_single_node(
        &dag,
        "release_resource_std_resources_AuthContext",
        inputs,
        gunbc_exec::ExecutionMode::Real,
    )
    .expect("node 'release_resource_std_resources_AuthContext' should execute successfully");

    // Check output port 'released'
    let output_released = outputs
        .get("released")
        .expect("output port 'released' should exist");
    assert!(
        !output_released.is_empty() || matches!(*output_released, Value::Skipped | Value::Unit),
        "expected non-empty value, Skipped, or Unit"
    );
}
