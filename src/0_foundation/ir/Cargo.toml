[package]
name = "gunbc-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core IR types for gunbc: Node, Dag, Edge, Port, and boundary detection"

[dependencies]
daglang-contract = { path = "../daglang-contract" }
gunbc-infra = { path = "../infra" }
gunbc-delegate-macros = { path = "../delegate-macros" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
inventory = "0.3"

[dev-dependencies]
proptest = { workspace = true }
glob = { workspace = true }
