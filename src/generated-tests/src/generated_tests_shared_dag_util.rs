// Generated tests for shared_dag_util_generated_tests DAG.
// 
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 60 obligations (0 discharged, 60 testable [0 compiler gaps]: A=18, B=42, C=0, D=0)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: 3fbae0c261b8607f01b07bbba4e6793121bf24f3413445b959963afa4dd839d9
// 
// Probe-Observer Coverage:
//   Probes: 17
//     shared.dag_util::aggregate_results (boundary mock)
//     shared.dag_util::all_succeeded (boundary mock)
//     shared.dag_util::format_report (boundary mock)
//     shared.dag_util::stage_result (boundary mock)
//     shared.dag_util::skipped_stage (boundary mock)
//     shared.dag_util::stage_from_output (boundary mock)
//     shared.dag_util::generated_header (boundary mock)
//     shared.dag_util::text_line (boundary mock)
//     shared.dag_util::comment_line (boundary mock)
//     shared.dag_util::blank_line (boundary mock)
//     shared.dag_util::section (boundary mock)
//     shared.dag_util::titled_section (boundary mock)
//     shared.dag_util::doc (boundary mock)
//     shared.dag_util::doc_with_header (boundary mock)
//     shared.dag_util::render_document_line (boundary mock)
//     shared.dag_util::render_document_section (boundary mock)
//     shared.dag_util::render_document (boundary mock)
//   
//   Observers: 17
//     shared.dag_util::aggregate_results [terminal] (return: NonEmpty)
//     shared.dag_util::all_succeeded [terminal] (return: IsBool)
//     shared.dag_util::blank_line [terminal] (return: NonEmpty)
//     shared.dag_util::comment_line [terminal] (return: NonEmpty)
//     shared.dag_util::doc [terminal] (return: NonEmpty)
//     shared.dag_util::doc_with_header [terminal] (return: NonEmpty)
//     shared.dag_util::format_report [terminal] (return: NonEmpty)
//     shared.dag_util::generated_header [terminal] (return: IsString)
//     shared.dag_util::render_document [terminal] (return: NonEmpty)
//     shared.dag_util::render_document_line [terminal] (return: IsString)
//     shared.dag_util::render_document_section [terminal] (return: NonEmpty)
//     shared.dag_util::section [terminal] (return: NonEmpty)
//     shared.dag_util::skipped_stage [terminal] (return: NonEmpty)
//     shared.dag_util::stage_from_output [terminal] (return: NonEmpty)
//     shared.dag_util::stage_result [terminal] (return: NonEmpty)
//     shared.dag_util::text_line [terminal] (return: NonEmpty)
//     shared.dag_util::titled_section [terminal] (return: NonEmpty)
//   
//   Integration tests: 0


use gunbc_exec::{ExecuteConfig, ExecutionMode, execute_dag, lower};
use gunbc_ir::{Value, detect_boundaries};
use gunbc_test::{FermiCost, MockSpec, TestClass, assert_boundary_mockable, guard_test};

fn mock_spec() -> MockSpec {
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    gunbc_test::auto_mock_spec(&dag, "shared-dag_util")
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
// 
// Determinism obligations: 17 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'shared.dag_util::aggregate_results': same inputs → same outputs
// - 'shared.dag_util::all_succeeded': same inputs → same outputs
// - 'shared.dag_util::format_report': same inputs → same outputs
// - 'shared.dag_util::stage_result': same inputs → same outputs
// - 'shared.dag_util::skipped_stage': same inputs → same outputs
// - 'shared.dag_util::stage_from_output': same inputs → same outputs
// - 'shared.dag_util::generated_header': same inputs → same outputs
// - 'shared.dag_util::text_line': same inputs → same outputs
// - 'shared.dag_util::comment_line': same inputs → same outputs
// - 'shared.dag_util::blank_line': same inputs → same outputs
// - 'shared.dag_util::section': same inputs → same outputs
// - 'shared.dag_util::titled_section': same inputs → same outputs
// - 'shared.dag_util::doc': same inputs → same outputs
// - 'shared.dag_util::doc_with_header': same inputs → same outputs
// - 'shared.dag_util::render_document_line': same inputs → same outputs
// - 'shared.dag_util::render_document_section': same inputs → same outputs
// - 'shared.dag_util::render_document': same inputs → same outputs

/// DryRun execution completes without crash.
/// 
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    if !guard_test("test_dryrun_completion", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("DryRun execution should complete without crash");
    assert!(!log.entries.is_empty(), "execution should produce log entries");
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 17 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'shared.dag_util::aggregate_results': valid inputs → valid outputs
// - 'shared.dag_util::all_succeeded': valid inputs → valid outputs
// - 'shared.dag_util::format_report': valid inputs → valid outputs
// - 'shared.dag_util::stage_result': valid inputs → valid outputs
// - 'shared.dag_util::skipped_stage': valid inputs → valid outputs
// - 'shared.dag_util::stage_from_output': valid inputs → valid outputs
// - 'shared.dag_util::generated_header': valid inputs → valid outputs
// - 'shared.dag_util::text_line': valid inputs → valid outputs
// - 'shared.dag_util::comment_line': valid inputs → valid outputs
// - 'shared.dag_util::blank_line': valid inputs → valid outputs
// - 'shared.dag_util::section': valid inputs → valid outputs
// - 'shared.dag_util::titled_section': valid inputs → valid outputs
// - 'shared.dag_util::doc': valid inputs → valid outputs
// - 'shared.dag_util::doc_with_header': valid inputs → valid outputs
// - 'shared.dag_util::render_document_line': valid inputs → valid outputs
// - 'shared.dag_util::render_document_section': valid inputs → valid outputs
// - 'shared.dag_util::render_document': valid inputs → valid outputs
// 24 optional input handling obligations.
// Optional inputs must accept missing values and reject wrong-typed inputs.

/// Variant coverage: shared.dag_util::all_succeeded.return with variant 'True' of type Bool.
/// 
/// Proves: DAG handles variant 'True' at boundary port shared.dag_util::all_succeeded.return.
#[test]
fn test_variant_shared_dag_util_all_succeeded_return_true() {
    if !guard_test("test_variant_shared_dag_util_all_succeeded_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::all_succeeded", "return", Value::Str("True".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for shared.dag_util::all_succeeded.return should not crash");
}

/// Variant coverage: shared.dag_util::all_succeeded.return with variant 'False' of type Bool.
/// 
/// Proves: DAG handles variant 'False' at boundary port shared.dag_util::all_succeeded.return.
#[test]
fn test_variant_shared_dag_util_all_succeeded_return_false() {
    if !guard_test("test_variant_shared_dag_util_all_succeeded_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::all_succeeded", "return", Value::Str("False".to_string()));
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for shared.dag_util::all_succeeded.return should not crash");
}

/// Optional input: shared.dag_util::aggregate_results.stages (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_aggregate_results_stages() {
    if !guard_test("test_optional_missing_shared_dag_util_aggregate_results_stages", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::aggregate_results", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::aggregate_results.stages missing should not error");
}

/// Optional input: shared.dag_util::aggregate_results.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_aggregate_results_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_aggregate_results_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("stages".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::aggregate_results", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::aggregate_results.__deps missing should not error");
}

/// Optional input: shared.dag_util::all_succeeded.stages (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_all_succeeded_stages() {
    if !guard_test("test_optional_missing_shared_dag_util_all_succeeded_stages", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::all_succeeded", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::all_succeeded.stages missing should not error");
}

/// Optional input: shared.dag_util::all_succeeded.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_all_succeeded_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_all_succeeded_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("stages".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::all_succeeded", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::all_succeeded.__deps missing should not error");
}

/// Optional input: shared.dag_util::format_report.stages (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_format_report_stages() {
    if !guard_test("test_optional_missing_shared_dag_util_format_report_stages", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("summary".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::format_report", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::format_report.stages missing should not error");
}

/// Optional input: shared.dag_util::format_report.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_format_report_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_format_report_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("stages".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("summary".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::format_report", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::format_report.__deps missing should not error");
}

/// Optional input: shared.dag_util::stage_result.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_stage_result_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_stage_result_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    inputs.insert("stderr".to_string(), Value::Str("example".to_string()));
    inputs.insert("success".to_string(), Value::Str("True".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::stage_result", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::stage_result.__deps missing should not error");
}

/// Optional input: shared.dag_util::skipped_stage.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_skipped_stage_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_skipped_stage_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::skipped_stage", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::skipped_stage.__deps missing should not error");
}

/// Optional input: shared.dag_util::stage_from_output.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_stage_from_output_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_stage_from_output_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    inputs.insert("skipped".to_string(), Value::Str("True".to_string()));
    inputs.insert("stderr".to_string(), Value::Str("example".to_string()));
    inputs.insert("stdout".to_string(), Value::Str("example".to_string()));
    inputs.insert("success".to_string(), Value::Str("True".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::stage_from_output", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::stage_from_output.__deps missing should not error");
}

/// Optional input: shared.dag_util::generated_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_generated_header_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_generated_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("tool".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::generated_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::generated_header.__deps missing should not error");
}

/// Optional input: shared.dag_util::text_line.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_text_line_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_text_line_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::text_line", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::text_line.__deps missing should not error");
}

/// Optional input: shared.dag_util::comment_line.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_comment_line_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_comment_line_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::comment_line", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::comment_line.__deps missing should not error");
}

/// Optional input: shared.dag_util::blank_line.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_blank_line_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_blank_line_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::blank_line", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::blank_line.__deps missing should not error");
}

/// Optional input: shared.dag_util::section.lines (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_section_lines() {
    if !guard_test("test_optional_missing_shared_dag_util_section_lines", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::section", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::section.lines missing should not error");
}

/// Optional input: shared.dag_util::section.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_section_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_section_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("lines".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::section", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::section.__deps missing should not error");
}

/// Optional input: shared.dag_util::titled_section.lines (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_titled_section_lines() {
    if !guard_test("test_optional_missing_shared_dag_util_titled_section_lines", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("title".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::titled_section", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::titled_section.lines missing should not error");
}

/// Optional input: shared.dag_util::titled_section.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_titled_section_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_titled_section_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("lines".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("title".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::titled_section", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::titled_section.__deps missing should not error");
}

/// Optional input: shared.dag_util::doc.sections (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_doc_sections() {
    if !guard_test("test_optional_missing_shared_dag_util_doc_sections", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::doc.sections missing should not error");
}

/// Optional input: shared.dag_util::doc.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_doc_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_doc_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("sections".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::doc.__deps missing should not error");
}

/// Optional input: shared.dag_util::doc_with_header.sections (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_doc_with_header_sections() {
    if !guard_test("test_optional_missing_shared_dag_util_doc_with_header_sections", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc_with_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::doc_with_header.sections missing should not error");
}

/// Optional input: shared.dag_util::doc_with_header.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_doc_with_header_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_doc_with_header_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("sections".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc_with_header", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::doc_with_header.__deps missing should not error");
}

/// Optional input: shared.dag_util::render_document_line.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_render_document_line_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_render_document_line_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("line".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document_line", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::render_document_line.__deps missing should not error");
}

/// Optional input: shared.dag_util::render_document_section.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_render_document_section_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_render_document_section_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("section".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document_section", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::render_document_section.__deps missing should not error");
}

/// Optional input: shared.dag_util::render_document.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_shared_dag_util_render_document_deps() {
    if !guard_test("test_optional_missing_shared_dag_util_render_document_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("document".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let _outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input shared.dag_util::render_document.__deps missing should not error");
}

// =========================================================================
// Boundary Tests (per-node mockability)
// =========================================================================

/// Test that all boundaries can be mocked.
#[test]
fn test_boundaries_mockable() {
    if !guard_test("test_boundaries_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(result.is_ok(), "Boundaries should be mockable: {:?}", result.error);
}

/// Test that shared.dag_util::aggregate_results boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_aggregate_results_mockable() {
    if !guard_test("test_boundary_shared_dag_util_aggregate_results_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::aggregate_results".into()), "shared.dag_util::aggregate_results should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::aggregate_results", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::aggregate_results").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::all_succeeded boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_all_succeeded_mockable() {
    if !guard_test("test_boundary_shared_dag_util_all_succeeded_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::all_succeeded".into()), "shared.dag_util::all_succeeded should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::all_succeeded", "return", Value::Str("True".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::all_succeeded").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::blank_line boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_blank_line_mockable() {
    if !guard_test("test_boundary_shared_dag_util_blank_line_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::blank_line".into()), "shared.dag_util::blank_line should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::blank_line", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::blank_line").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::comment_line boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_comment_line_mockable() {
    if !guard_test("test_boundary_shared_dag_util_comment_line_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::comment_line".into()), "shared.dag_util::comment_line should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::comment_line", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::comment_line").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::doc boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_doc_mockable() {
    if !guard_test("test_boundary_shared_dag_util_doc_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::doc".into()), "shared.dag_util::doc should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::doc", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::doc").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::doc_with_header boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_doc_with_header_mockable() {
    if !guard_test("test_boundary_shared_dag_util_doc_with_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::doc_with_header".into()), "shared.dag_util::doc_with_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::doc_with_header", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::doc_with_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::format_report boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_format_report_mockable() {
    if !guard_test("test_boundary_shared_dag_util_format_report_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::format_report".into()), "shared.dag_util::format_report should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::format_report", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::format_report").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::generated_header boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_generated_header_mockable() {
    if !guard_test("test_boundary_shared_dag_util_generated_header_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::generated_header".into()), "shared.dag_util::generated_header should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::generated_header", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::generated_header").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::render_document boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_render_document_mockable() {
    if !guard_test("test_boundary_shared_dag_util_render_document_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::render_document".into()), "shared.dag_util::render_document should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::render_document", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::render_document").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::render_document_line boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_render_document_line_mockable() {
    if !guard_test("test_boundary_shared_dag_util_render_document_line_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::render_document_line".into()), "shared.dag_util::render_document_line should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::render_document_line", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::render_document_line").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::render_document_section boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_render_document_section_mockable() {
    if !guard_test("test_boundary_shared_dag_util_render_document_section_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::render_document_section".into()), "shared.dag_util::render_document_section should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::render_document_section", "return", Value::Str("example".to_string()));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::render_document_section").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::section boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_section_mockable() {
    if !guard_test("test_boundary_shared_dag_util_section_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::section".into()), "shared.dag_util::section should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::section", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::section").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::skipped_stage boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_skipped_stage_mockable() {
    if !guard_test("test_boundary_shared_dag_util_skipped_stage_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::skipped_stage".into()), "shared.dag_util::skipped_stage should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::skipped_stage", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::skipped_stage").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::stage_from_output boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_stage_from_output_mockable() {
    if !guard_test("test_boundary_shared_dag_util_stage_from_output_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::stage_from_output".into()), "shared.dag_util::stage_from_output should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::stage_from_output", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::stage_from_output").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::stage_result boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_stage_result_mockable() {
    if !guard_test("test_boundary_shared_dag_util_stage_result_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::stage_result".into()), "shared.dag_util::stage_result should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::stage_result", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::stage_result").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::text_line boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_text_line_mockable() {
    if !guard_test("test_boundary_shared_dag_util_text_line_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::text_line".into()), "shared.dag_util::text_line should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::text_line", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::text_line").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that shared.dag_util::titled_section boundary can be mocked.
#[test]
fn test_boundary_shared_dag_util_titled_section_mockable() {
    if !guard_test("test_boundary_shared_dag_util_titled_section_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"shared.dag_util::titled_section".into()), "shared.dag_util::titled_section should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("shared.dag_util::titled_section", "return", Value::Json(serde_json::json!({"mock":true})));

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("shared.dag_util::titled_section").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

// =========================================================================
// Chain Validation Tests
// =========================================================================

// These tests verify that mock outputs satisfy downstream input expectations.

/// Test that this tool's mock spec is self-consistent.
#[test]
fn test_mock_spec_self_consistent() {
    if !guard_test("test_mock_spec_self_consistent", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let spec = mock_spec();
    // Verify all boundary mocks are present
    assert!(spec.get_boundary_mock("shared.dag_util::aggregate_results", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::aggregate_results.return");
    assert!(spec.get_boundary_mock("shared.dag_util::all_succeeded", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::all_succeeded.return");
    assert!(spec.get_boundary_mock("shared.dag_util::format_report", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::format_report.return");
    assert!(spec.get_boundary_mock("shared.dag_util::stage_result", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::stage_result.return");
    assert!(spec.get_boundary_mock("shared.dag_util::skipped_stage", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::skipped_stage.return");
    assert!(spec.get_boundary_mock("shared.dag_util::stage_from_output", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::stage_from_output.return");
    assert!(spec.get_boundary_mock("shared.dag_util::generated_header", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::generated_header.return");
    assert!(spec.get_boundary_mock("shared.dag_util::text_line", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::text_line.return");
    assert!(spec.get_boundary_mock("shared.dag_util::comment_line", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::comment_line.return");
    assert!(spec.get_boundary_mock("shared.dag_util::blank_line", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::blank_line.return");
    assert!(spec.get_boundary_mock("shared.dag_util::section", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::section.return");
    assert!(spec.get_boundary_mock("shared.dag_util::titled_section", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::titled_section.return");
    assert!(spec.get_boundary_mock("shared.dag_util::doc", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::doc.return");
    assert!(spec.get_boundary_mock("shared.dag_util::doc_with_header", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::doc_with_header.return");
    assert!(spec.get_boundary_mock("shared.dag_util::render_document_line", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::render_document_line.return");
    assert!(spec.get_boundary_mock("shared.dag_util::render_document_section", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::render_document_section.return");
    assert!(spec.get_boundary_mock("shared.dag_util::render_document", "return").is_some(), "MockSpec should have boundary mock for shared.dag_util::render_document.return");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: shared.dag_util::aggregate_results (example 0)
/// 
/// Tests that node 'shared.dag_util::aggregate_results' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_aggregate_results_0() {
    if !guard_test("test_example_shared_dag_util_aggregate_results_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::aggregate_results", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::aggregate_results' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::all_succeeded (example 1)
/// 
/// Tests that node 'shared.dag_util::all_succeeded' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_all_succeeded_1() {
    if !guard_test("test_example_shared_dag_util_all_succeeded_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("True".to_string()));
    inputs.insert("return".to_string(), Value::Str("True".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::all_succeeded", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::all_succeeded' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_bool().is_some() || matches!(*output_return, Value::Skipped), "expected Bool or Skipped for output_return");
}

/// Node example: shared.dag_util::format_report (example 2)
/// 
/// Tests that node 'shared.dag_util::format_report' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_format_report_2() {
    if !guard_test("test_example_shared_dag_util_format_report_2", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("summary".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::format_report", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::format_report' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::stage_result (example 3)
/// 
/// Tests that node 'shared.dag_util::stage_result' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_stage_result_3() {
    if !guard_test("test_example_shared_dag_util_stage_result_3", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("stderr".to_string(), Value::Str("example".to_string()));
    inputs.insert("success".to_string(), Value::Str("True".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::stage_result", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::stage_result' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::skipped_stage (example 4)
/// 
/// Tests that node 'shared.dag_util::skipped_stage' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_skipped_stage_4() {
    if !guard_test("test_example_shared_dag_util_skipped_stage_4", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::skipped_stage", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::skipped_stage' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::stage_from_output (example 5)
/// 
/// Tests that node 'shared.dag_util::stage_from_output' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_stage_from_output_5() {
    if !guard_test("test_example_shared_dag_util_stage_from_output_5", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("name".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("skipped".to_string(), Value::Str("True".to_string()));
    inputs.insert("stderr".to_string(), Value::Str("example".to_string()));
    inputs.insert("stdout".to_string(), Value::Str("example".to_string()));
    inputs.insert("success".to_string(), Value::Str("True".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::stage_from_output", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::stage_from_output' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::generated_header (example 6)
/// 
/// Tests that node 'shared.dag_util::generated_header' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_generated_header_6() {
    if !guard_test("test_example_shared_dag_util_generated_header_6", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("tool".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::generated_header", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::generated_header' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: shared.dag_util::text_line (example 7)
/// 
/// Tests that node 'shared.dag_util::text_line' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_text_line_7() {
    if !guard_test("test_example_shared_dag_util_text_line_7", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::text_line", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::text_line' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::comment_line (example 8)
/// 
/// Tests that node 'shared.dag_util::comment_line' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_comment_line_8() {
    if !guard_test("test_example_shared_dag_util_comment_line_8", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("text".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::comment_line", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::comment_line' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::blank_line (example 9)
/// 
/// Tests that node 'shared.dag_util::blank_line' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_blank_line_9() {
    if !guard_test("test_example_shared_dag_util_blank_line_9", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::blank_line", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::blank_line' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::section (example 10)
/// 
/// Tests that node 'shared.dag_util::section' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_section_10() {
    if !guard_test("test_example_shared_dag_util_section_10", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::section", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::section' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::titled_section (example 11)
/// 
/// Tests that node 'shared.dag_util::titled_section' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_titled_section_11() {
    if !guard_test("test_example_shared_dag_util_titled_section_11", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("title".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::titled_section", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::titled_section' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::doc (example 12)
/// 
/// Tests that node 'shared.dag_util::doc' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_doc_12() {
    if !guard_test("test_example_shared_dag_util_doc_12", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::doc' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::doc_with_header (example 13)
/// 
/// Tests that node 'shared.dag_util::doc_with_header' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_doc_with_header_13() {
    if !guard_test("test_example_shared_dag_util_doc_with_header_13", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("header".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::doc_with_header", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::doc_with_header' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::render_document_line (example 14)
/// 
/// Tests that node 'shared.dag_util::render_document_line' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_render_document_line_14() {
    if !guard_test("test_example_shared_dag_util_render_document_line_14", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("line".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document_line", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::render_document_line' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(output_return.as_str().is_some() || matches!(*output_return, Value::Skipped), "expected String or Skipped for output_return");
}

/// Node example: shared.dag_util::render_document_section (example 15)
/// 
/// Tests that node 'shared.dag_util::render_document_section' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_render_document_section_15() {
    if !guard_test("test_example_shared_dag_util_render_document_section_15", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("comment_prefix".to_string(), Value::Str("example".to_string()));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    inputs.insert("section".to_string(), Value::Json(serde_json::json!({"mock":true})));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document_section", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::render_document_section' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: shared.dag_util::render_document (example 16)
/// 
/// Tests that node 'shared.dag_util::render_document' produces expected outputs for given inputs.
#[test]
fn test_example_shared_dag_util_render_document_16() {
    if !guard_test("test_example_shared_dag_util_render_document_16", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("shared/dag_util.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Str("example".to_string()));
    inputs.insert("document".to_string(), Value::Json(serde_json::json!({"mock":true})));
    inputs.insert("return".to_string(), Value::Str("example".to_string()));
    let outputs = gunbc_exec::execute_single_node(&dag, "shared.dag_util::render_document", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'shared.dag_util::render_document' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

