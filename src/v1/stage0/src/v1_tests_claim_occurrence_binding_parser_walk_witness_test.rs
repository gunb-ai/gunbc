// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.occurrence_binding_parser_walk_witness_test

use self::ObcpwParsed::*;
use crate::std_occurrence_binding_candidates::AuthoredOrderIndexRefusal::MissingAuthoredOrderRow;
use crate::std_occurrence_binding_candidates::BoundReferencePopulation::AllReferencesBound;
use crate::std_occurrence_binding_candidates::DeclarationExposure::LexicalExposure;
use crate::std_occurrence_binding_candidates::DeclarationExposureIndexRefusal::MissingDeclarationExposure;
use crate::std_occurrence_binding_candidates::DirectModuleDependencyBuild::DirectModuleDependencyListReady;
use crate::std_occurrence_binding_candidates::OccurrenceCandidateIndexBuild::OccurrenceCandidateIndexReady;
use crate::std_occurrence_binding_candidates::ReferenceBindingProjection::{
    ReferenceBindingProjectionAmbiguous, ReferenceBindingProjectionAuthoredOrderRefused,
    ReferenceBindingProjectionBound, ReferenceBindingProjectionExposureRefused,
    ReferenceBindingProjectionTransportRefused, ReferenceBindingProjectionUnbound,
};
pub use crate::std_occurrence_binding_candidates::{
    candidate_occurrence_ids_for_reference, direct_module_dependencies_from_bound_population,
    occurrence_candidate_index_build, resolve_reference_binding_via_structural_candidates,
};
pub use crate::std_occurrence_binding_candidates::{
    AuthoredOrderIndexRefusal, AuthoredOrderRow, BoundReferencePopulation, BoundReferenceProvider,
    DeclarationExposure, DeclarationExposureIndexRefusal, DeclarationExposureRow,
    DirectModuleDependencyBuild, OccurrenceBindingCandidateInputs, OccurrenceCandidateIndexBuild,
    ReferenceBindingProjection,
};
pub use crate::std_occurrence_identity::{
    AuthoredTokenOrdinal, DeclarationOccurrence, OccurrenceId, OccurrenceIndexEntry,
    OccurrenceTransport, ReferenceOccurrence,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource::{
    ParsedOccurrenceBindingSourceReady, ParsedOccurrenceBindingSourceRefused,
};
use crate::v1_gunbc_occurrence_binding_parser_walk::StructuralBindingIndexRefusal::{
    StructuralBindingAuthoredOrderRefusal, StructuralBindingExposureRefusal,
};
use crate::v1_gunbc_occurrence_binding_parser_walk::StructuralBindingWalk::{
    StructuralBindingWalkReady, StructuralBindingWalkRefused,
};
pub use crate::v1_gunbc_occurrence_binding_parser_walk::{
    occurrence_binding_inputs_from_transport, occurrence_id_for_authored_name,
    parse_authored_occurrence_binding_source, reference_named, structural_binding_walk,
};
pub use crate::v1_gunbc_occurrence_binding_parser_walk::{
    ParsedOccurrenceBindingSource, StructuralBindingIndexRefusal, StructuralBindingWalk,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::sync::Arc as Rc;

pub fn occurrence_binding_parser_walk_witness_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Parser integration witness (namespace-reference-derived-closure merge bar): every case begins at authored source, reaches the P2 structural walk through v1.compiler.parse.parse_with_table transport only — no hand-constructed OccurrenceTransport. Case 6 discharges projection law (A) only: repeated bound-reference providers for one declaration project to one DirectModuleDependency edge — not identity collapse, collector deduplication, or section-13 repeated-discovery collapse. Collector re-observation dedup (B) stays a distinct deferred debt on ProductionOccurrenceCollectorMissing. Case 7 parses once in one graph scope, preserves the parser-minted reference occurrence on the positive arm, and substitutes an occurrence id unknown within that same transport on the negative arm — never comparing allocator-local OccurrenceId values across standalone parses.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn occurrence_binding_parser_walk_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE (requires v1 parser pool): target/release/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/occurrence_binding_parser_walk_witness_test.dag --functions namespace_reference_derived_closure_parser_integration_holds".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn occurrence_binding_parser_walk_dissolve_on() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVE-ON: witness_layer_roots admits src/v1 for parser-transport witnesses without Node ambiguity, or execution corpus carries per-entry source roots — then this recipe deletes and discovery enrollment suffices.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ObcpwParsed {
    ObcpwParsedReady {
        transport: Rc<OccurrenceTransport>,
        inputs: Rc<OccurrenceBindingCandidateInputs>,
        module_path: NonEmptyStr,
    },
    ObcpwParsedRefused,
}
impl ObcpwParsed {
    pub fn transport(&self) -> Rc<OccurrenceTransport> {
        match self {
            ObcpwParsed::ObcpwParsedReady {
                transport: __val, ..
            } => __val.clone(),
            ObcpwParsed::ObcpwParsedRefused => panic!("no transport on unit variant"),
        }
    }
    pub fn inputs(&self) -> Rc<OccurrenceBindingCandidateInputs> {
        match self {
            ObcpwParsed::ObcpwParsedReady { inputs: __val, .. } => __val.clone(),
            ObcpwParsed::ObcpwParsedRefused => panic!("no inputs on unit variant"),
        }
    }
    pub fn module_path(&self) -> NonEmptyStr {
        match self {
            ObcpwParsed::ObcpwParsedReady {
                module_path: __val, ..
            } => __val.clone(),
            ObcpwParsed::ObcpwParsedRefused => panic!("no module_path on unit variant"),
        }
    }
}

pub fn obcpw_parse(file: String, source: String) -> Rc<ObcpwParsed> {
    match (*parse_authored_occurrence_binding_source(file.clone(), source.clone())).clone() {
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceRefused => {
            Rc::new(ObcpwParsed::ObcpwParsedRefused)
        }
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceReady {
            transport,
            module_path,
            ..
        } => Rc::new(ObcpwParsed::ObcpwParsedReady {
            transport: transport.clone(),
            module_path: module_path.clone(),
            inputs: occurrence_binding_inputs_from_transport(
                module_path.clone(),
                transport.clone(),
            ),
        }),
    }
}

pub fn obcpw_same_module(left: String, right: String) -> bool {
    (left.clone() == right.clone())
}

pub fn obcpw_module_sibling_source() -> String {
    "module app.sibling_module\n\nfn fn_a() -> Int {\n  10\n}\n\nfn fn_b() -> Int {\n  fn_a()\n}\n"
        .to_string()
}

pub fn obcpw_pattern_arm_source() -> String {
    "module app.pattern_arm_module\n\ntype Side = Left { n: Int } | Right { n: Int }\n\nfn demo(t: Side) -> Int {\n  match t {\n    Left { n: x } => {\n      x + 1\n    }\n    Right { n: x } => {\n      x\n    }\n  }\n}\n".to_string()
}

pub fn obcpw_nested_lexical_source() -> String {
    "module app.block_module\n\nfn outer() -> Int {\n  let x = 1\n  x\n}\n\nfn inner() -> Int {\n  x\n}\n".to_string()
}

pub fn obcpw_source_order_source() -> String {
    "module app.order_module\n\nfn demo() -> Int {\n  fn_later()\n}\n\nfn fn_later() -> Int {\n  0\n}\n".to_string()
}

pub fn obcpw_ambiguous_source() -> String {
    "module app.ambiguous_module\n\nfn helper() -> Int {\n  0\n}\n\nfn helper() -> Int {\n  1\n}\n\nfn demo() -> Int {\n  helper()\n}\n".to_string()
}

pub fn obcpw_greeter_source() -> String {
    "module gunbc.example.greeter\n\nfn format_greeting() -> String {\n  \"hi\"\n}\n\nfn greet() -> String {\n  format_greeting()\n}\n\nfn greet_again() -> String {\n  format_greeting()\n}\n".to_string()
}

pub fn obcpw_declarations_named(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<Rc<DeclarationOccurrence>>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<Rc<DeclarationOccurrence>>>, declaration: Rc<DeclarationOccurrence>| {
            match index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(acc.clone(), Rc::new(vec![declaration.clone()])),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn index_entry_for_declaration(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == declaration.occurrence.clone().value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn obcpw_references_named(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<Rc<ReferenceOccurrence>>> {
    transport.references.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<Rc<ReferenceOccurrence>>>, reference: Rc<ReferenceOccurrence>| {
            match index_entry_for_reference(transport.clone(), reference.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(acc.clone(), Rc::new(vec![reference.clone()])),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn index_entry_for_reference(
    transport: Rc<OccurrenceTransport>,
    reference: Rc<ReferenceOccurrence>,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == reference.occurrence.clone().value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn obcpw_reference_binds_to_declaration(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
    reference: Rc<ReferenceOccurrence>,
    declaration: Rc<DeclarationOccurrence>,
) -> bool {
    match (*resolve_reference_binding_via_structural_candidates(
        transport.clone(),
        inputs.clone(),
        reference.clone(),
    ))
    .clone()
    {
        ReferenceBindingProjection::ReferenceBindingProjectionBound {
            provider: provider, ..
        } => {
            (provider.declaration_occurrence.clone().value.clone()
                == declaration.occurrence.clone().value.clone())
        }
        _ => false,
    }
}

pub fn obcpw_reference_with_occurrence(
    reference: Rc<ReferenceOccurrence>,
    occurrence: OccurrenceId,
) -> Rc<ReferenceOccurrence> {
    Rc::new(ReferenceOccurrence {
        occurrence: occurrence.clone(),
        containment: reference.containment.clone(),
        category: reference.category.clone(),
        diagnostic_span: reference.diagnostic_span.clone(),
    })
}

pub fn obcpw_reference_unbound_exactly(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
    reference: Rc<ReferenceOccurrence>,
) -> bool {
    match (*resolve_reference_binding_via_structural_candidates(
        transport.clone(),
        inputs.clone(),
        reference.clone(),
    ))
    .clone()
    {
        ReferenceBindingProjection::ReferenceBindingProjectionUnbound {
            occurrence: unbound,
            ..
        } => (unbound.value.clone() == reference.occurrence.clone().value.clone()),
        _ => false,
    }
}

pub fn obcpw_unknown_occurrence_transport_refuses(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
    reference: Rc<ReferenceOccurrence>,
) -> bool {
    match (*resolve_reference_binding_via_structural_candidates(
        transport.clone(),
        inputs.clone(),
        reference.clone(),
    ))
    .clone()
    {
        ReferenceBindingProjection::ReferenceBindingProjectionTransportRefused {
            refusal: _,
            ..
        } => true,
        _ => false,
    }
}

pub fn obcpw_candidate_ids_distinct(candidates: Rc<Vec<OccurrenceId>>) -> bool {
    match candidates.clone().first().cloned() {
        None => false,
        Some(first) => match v1_rt::reverse(candidates.clone()).first().cloned() {
            None => false,
            Some(second) => (first.value.clone() != second.value.clone()),
        },
    }
}

pub fn parser_module_sibling_visible_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-sibling.dag".to_string(),
        obcpw_module_sibling_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport,
            inputs,
            module_path,
            ..
        } => match reference_named(transport.clone(), "fn_a".to_string()) {
            None => false,
            Some(reference) => match (*resolve_reference_binding_via_structural_candidates(
                transport.clone(),
                inputs.clone(),
                reference.clone(),
            ))
            .clone()
            {
                ReferenceBindingProjection::ReferenceBindingProjectionBound {
                    provider: provider,
                    ..
                } => match occurrence_id_for_authored_name(transport.clone(), "fn_a".to_string()) {
                    Some(fn_a_id) => {
                        ((((provider.declaration_occurrence.clone().value.clone()
                            == fn_a_id.value.clone())
                            && (provider.reference_occurrence.clone().value.clone()
                                == reference.occurrence.clone().value.clone()))
                            && obcpw_same_module(
                                provider.consumer_module.clone(),
                                module_path.clone(),
                            ))
                            && obcpw_same_module(
                                provider.provider_module.clone(),
                                module_path.clone(),
                            ))
                    }
                    None => false,
                },
                _ => false,
            },
        },
    }
}

pub fn parser_sibling_match_arm_pattern_binder_holds_note() -> String {
    thread_local! {
        static CACHED: String = {
            "PARKED on B1 rollup: the §13 sibling-arm rule is discharged by occurrence_binding_candidates_witness (hand-built transport where the arm-body reference nests under the binder occurrence). Real v1.parse containment stamps match-arm bodies as siblings of pattern binders, not descendants, so containment-derived LexicalExposure cannot isolate arms from authored source alone. Dissolve-on: parser nests arm-body references under pattern-binder occurrences (or an equivalent exposure derivation that does not require that nesting) — then re-enroll this test in namespace_reference_derived_closure_parser_integration_holds.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn parser_sibling_match_arm_pattern_binder_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-pattern-arm.dag".to_string(),
        obcpw_pattern_arm_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => {
            let x_declarations = obcpw_declarations_named(transport.clone(), "x".to_string());
            let x_references = obcpw_references_named(transport.clone(), "x".to_string());
            ((((x_declarations.clone().len() as i64) == 2)
                && ((x_references.clone().len() as i64) == 2))
                && match x_declarations.clone().first().cloned() {
                    Some(arm_one_x) => {
                        match v1_rt::reverse(x_declarations.clone()).first().cloned() {
                            Some(arm_two_x) => {
                                ((arm_one_x.occurrence.clone().value.clone()
                                    != arm_two_x.occurrence.clone().value.clone())
                                    && match x_references.clone().first().cloned() {
                                        Some(arm_one_ref) => {
                                            match v1_rt::reverse(x_references.clone())
                                                .first()
                                                .cloned()
                                            {
                                                Some(arm_two_ref) => {
                                                    ((obcpw_reference_binds_to_declaration(
                                                        transport.clone(),
                                                        inputs.clone(),
                                                        arm_one_ref.clone(),
                                                        arm_one_x.clone(),
                                                    ) && obcpw_reference_binds_to_declaration(
                                                        transport.clone(),
                                                        inputs.clone(),
                                                        arm_two_ref.clone(),
                                                        arm_two_x.clone(),
                                                    )) && !obcpw_reference_binds_to_declaration(
                                                        transport.clone(),
                                                        inputs.clone(),
                                                        arm_two_ref.clone(),
                                                        arm_one_x.clone(),
                                                    ))
                                                }
                                                None => false,
                                            }
                                        }
                                        None => false,
                                    })
                            }
                            None => false,
                        }
                    }
                    None => false,
                })
        }
    }
}

pub fn parser_nested_lexical_only_inside_scope_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-nested-lexical.dag".to_string(),
        obcpw_nested_lexical_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => {
            let x_declarations = obcpw_declarations_named(transport.clone(), "x".to_string());
            let x_references = obcpw_references_named(transport.clone(), "x".to_string());
            ((((x_declarations.clone().len() as i64) == 1) && ((x_references.clone().len() as i64) == 2)) && match x_declarations.clone().first().cloned() {
    Some(outer_x) => match x_references.clone().first().cloned() {
    Some(outer_ref) => match v1_rt::reverse(x_references.clone()).first().cloned() {
    Some(inner_ref) => ((obcpw_reference_binds_to_declaration(transport.clone(), inputs.clone(), outer_ref.clone(), outer_x.clone()) && match (*resolve_reference_binding_via_structural_candidates(transport.clone(), inputs.clone(), inner_ref.clone())).clone() {
    ReferenceBindingProjection::ReferenceBindingProjectionUnbound { occurrence: unbound, .. } => (unbound.value.clone() == inner_ref.occurrence.clone().value.clone()),
    _ => false,
}) && !obcpw_reference_binds_to_declaration(transport.clone(), inputs.clone(), inner_ref.clone(), outer_x.clone())),
    None => false,
},
    None => false,
},
    None => false,
})
        }
    }
}

pub fn parser_declaration_below_occurrence_not_visible_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-source-order.dag".to_string(),
        obcpw_source_order_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => match reference_named(transport.clone(), "fn_later".to_string()) {
            None => false,
            Some(reference) => match (*resolve_reference_binding_via_structural_candidates(
                transport.clone(),
                inputs.clone(),
                reference.clone(),
            ))
            .clone()
            {
                ReferenceBindingProjection::ReferenceBindingProjectionUnbound {
                    occurrence: unbound,
                    ..
                } => (unbound.value.clone() == reference.occurrence.clone().value.clone()),
                _ => false,
            },
        },
    }
}

pub fn parser_same_text_distinct_identities_ambiguous_holds() -> bool {
    match (*obcpw_parse("obcpw-ambiguous.dag".to_string(), obcpw_ambiguous_source())).clone() {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => match reference_named(transport.clone(), "helper".to_string()) {
            None => false,
            Some(reference) => {
                match (*occurrence_candidate_index_build(transport.clone(), inputs.clone())).clone()
                {
                    OccurrenceCandidateIndexBuild::OccurrenceCandidateIndexReady {
                        index: index,
                        ..
                    } => {
                        let candidates = candidate_occurrence_ids_for_reference(
                            index.clone(),
                            reference.clone(),
                        );
                        ((((candidates.clone().len() as i64) == 2) && obcpw_candidate_ids_distinct(candidates.clone())) && match (*resolve_reference_binding_via_structural_candidates(transport.clone(), inputs.clone(), reference.clone())).clone() {
    ReferenceBindingProjection::ReferenceBindingProjectionAmbiguous { occurrence: ambiguous, .. } => (ambiguous.value.clone() == reference.occurrence.clone().value.clone()),
    _ => false,
})
                    }
                    _ => false,
                }
            }
        },
    }
}

pub fn parser_repeated_bindings_projection_law_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Case 6 — projection law (A) under the same-module ruling (slice A / PR 7515): two BoundReferenceProvider rows for one declaration (greet + greet_again → format_greeting) are retained; DirectModuleDependency emits NO M→M self-edge from those providers (edges count 0). Uses per-reference resolve — not structural_binding_walk over the whole transport — so incidental unbound type/name refs in the fixture cannot refuse the population. Cross-module one-edge dedup stays a distinct fixture when a multi-module authored source is available. Does NOT discharge Section13RepeatedDiscoverySameIdentityCollapse; collector re-observation dedup (B) remains deferred on ProductionOccurrenceCollectorMissing.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn parser_transport_scoped_identity_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Case 7 — single parse in one graph scope. Positive arm preserves the parser-minted reference occurrence; negative arm substitutes an occurrence id unknown within that same transport. OccurrenceId integers are allocator-local and must not be compared across standalone parses.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn obcpw_bound_providers_named(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
    name: String,
) -> Rc<Vec<Rc<BoundReferenceProvider>>> {
    v1_rt::reverse(
        obcpw_references_named(transport.clone(), name.clone())
            .iter()
            .cloned()
            .fold(
                Rc::new(vec![]),
                |acc: _, reference: Rc<ReferenceOccurrence>| {
                    match (*resolve_reference_binding_via_structural_candidates(
                        transport.clone(),
                        inputs.clone(),
                        reference.clone(),
                    ))
                    .clone()
                    {
                        ReferenceBindingProjection::ReferenceBindingProjectionBound {
                            provider: provider,
                            ..
                        } => v1_rt::concat(Rc::new(vec![provider.clone()]), acc.clone()),
                        _ => acc.clone(),
                    }
                },
            ),
    )
}

pub fn parser_repeated_bindings_project_one_module_edge_holds() -> bool {
    match (*obcpw_parse("obcpw-greeter.dag".to_string(), obcpw_greeter_source())).clone() {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport,
            inputs,
            module_path,
            ..
        } => {
            match occurrence_id_for_authored_name(transport.clone(), "format_greeting".to_string())
            {
                None => false,
                Some(format_greeting_id) => {
                    let providers = obcpw_bound_providers_named(
                        transport.clone(),
                        inputs.clone(),
                        "format_greeting".to_string(),
                    );
                    ((((providers.clone().len() as i64) == 2)
                        && match providers.clone().first().cloned() {
                            Some(first) => {
                                (((first.declaration_occurrence.clone().value.clone()
                                    == format_greeting_id.value.clone())
                                    && obcpw_same_module(
                                        first.consumer_module.clone(),
                                        module_path.clone(),
                                    ))
                                    && obcpw_same_module(
                                        first.provider_module.clone(),
                                        module_path.clone(),
                                    ))
                            }
                            None => false,
                        })
                        && match (*direct_module_dependencies_from_bound_population(Rc::new(
                            BoundReferencePopulation::AllReferencesBound {
                                providers: providers.clone(),
                            },
                        )))
                        .clone()
                        {
                            DirectModuleDependencyBuild::DirectModuleDependencyListReady {
                                edges: edges,
                                ..
                            } => ((edges.clone().len() as i64) == 0),
                            _ => false,
                        })
                }
            }
        }
    }
}

pub fn parser_transport_scoped_preserved_identity_and_unknown_occurrence_refuses_holds() -> bool {
    match (*obcpw_parse("obcpw-identity.dag".to_string(), obcpw_greeter_source())).clone() {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => match reference_named(transport.clone(), "format_greeting".to_string()) {
            None => false,
            Some(format_reference) => match occurrence_id_for_authored_name(
                transport.clone(),
                "format_greeting".to_string(),
            ) {
                Some(format_greeting_id) => {
                    let preserved = obcpw_reference_with_occurrence(
                        format_reference.clone(),
                        format_reference.occurrence.clone(),
                    );
                    let unknown = obcpw_reference_with_occurrence(
                        format_reference.clone(),
                        OccurrenceId { value: 999999 },
                    );
                    (match (*resolve_reference_binding_via_structural_candidates(
                        transport.clone(),
                        inputs.clone(),
                        preserved.clone(),
                    ))
                    .clone()
                    {
                        ReferenceBindingProjection::ReferenceBindingProjectionBound {
                            provider: provider,
                            ..
                        } => {
                            ((provider.reference_occurrence.clone().value.clone()
                                == format_reference.occurrence.clone().value.clone())
                                && (provider.declaration_occurrence.clone().value.clone()
                                    == format_greeting_id.value.clone()))
                        }
                        _ => false,
                    } && obcpw_unknown_occurrence_transport_refuses(
                        transport.clone(),
                        inputs.clone(),
                        unknown.clone(),
                    ))
                }
                None => false,
            },
        },
    }
}

pub fn parser_production_call_binds_with_edge_holds() -> bool {
    match (*obcpw_parse("obcpw-production.dag".to_string(), obcpw_greeter_source())).clone() {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport,
            inputs,
            module_path,
            ..
        } => match reference_named(transport.clone(), "format_greeting".to_string()) {
            None => false,
            Some(reference) => match (*resolve_reference_binding_via_structural_candidates(
                transport.clone(),
                inputs.clone(),
                reference.clone(),
            ))
            .clone()
            {
                ReferenceBindingProjection::ReferenceBindingProjectionBound {
                    provider: provider,
                    ..
                } => match occurrence_id_for_authored_name(
                    transport.clone(),
                    "format_greeting".to_string(),
                ) {
                    Some(format_greeting_id) => {
                        (((((provider.reference_occurrence.clone().value.clone()
                            == reference.occurrence.clone().value.clone())
                            && (provider.declaration_occurrence.clone().value.clone()
                                == format_greeting_id.value.clone()))
                            && obcpw_same_module(
                                provider.consumer_module.clone(),
                                module_path.clone(),
                            ))
                            && obcpw_same_module(
                                provider.provider_module.clone(),
                                module_path.clone(),
                            ))
                            && match (*direct_module_dependencies_from_bound_population(Rc::new(
                                BoundReferencePopulation::AllReferencesBound {
                                    providers: Rc::new(vec![provider.clone()]),
                                },
                            )))
                            .clone()
                            {
                                DirectModuleDependencyBuild::DirectModuleDependencyListReady {
                                    edges: edges,
                                    ..
                                } => ((edges.clone().len() as i64) == 0),
                                _ => false,
                            })
                    }
                    None => false,
                },
                _ => false,
            },
        },
    }
}

pub fn obcpw_declaration_for_occurrence(
    transport: Rc<OccurrenceTransport>,
    occurrence: OccurrenceId,
) -> Option<Rc<DeclarationOccurrence>> {
    transport.declarations.clone().iter().cloned().fold(
        None,
        |found: _, declaration: Rc<DeclarationOccurrence>| match found.clone() {
            Some(_) => found.clone(),
            None => {
                match (declaration.occurrence.clone().value.clone() == occurrence.value.clone()) {
                    true => Some(declaration.clone()),
                    false => None,
                }
            }
        },
    )
}

pub fn obcpw_inputs_with_lexical_fn_a(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    match occurrence_id_for_authored_name(transport.clone(), "fn_a".to_string()) {
        None => inputs.clone(),
        Some(fn_a_id) => match obcpw_declaration_for_occurrence(transport.clone(), fn_a_id.clone())
        {
            None => inputs.clone(),
            Some(fn_a_declaration) => Rc::new(OccurrenceBindingCandidateInputs {
                module_paths: inputs.module_paths.clone(),
                authored_order_rows: inputs.authored_order_rows.clone(),
                exposure_rows: v1_rt::reverse(inputs.exposure_rows.clone().iter().cloned().fold(
                    Rc::new(vec![]),
                    |acc: _, row: Rc<DeclarationExposureRow>| {
                        match (row.occurrence.clone().value.clone() == fn_a_id.value.clone()) {
                            true => v1_rt::concat(
                                Rc::new(vec![Rc::new(DeclarationExposureRow {
                                    occurrence: row.occurrence.clone(),
                                    exposure: Rc::new(DeclarationExposure::LexicalExposure {
                                        exposing_scope: fn_a_declaration.containment.clone(),
                                    }),
                                })]),
                                acc.clone(),
                            ),
                            false => v1_rt::concat(Rc::new(vec![row.clone()]), acc.clone()),
                        }
                    },
                )),
            }),
        },
    }
}

pub fn obcpw_inputs_without_fn_a_exposure(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    match occurrence_id_for_authored_name(transport.clone(), "fn_a".to_string()) {
        None => inputs.clone(),
        Some(fn_a_id) => Rc::new(OccurrenceBindingCandidateInputs {
            module_paths: inputs.module_paths.clone(),
            authored_order_rows: inputs.authored_order_rows.clone(),
            exposure_rows: v1_rt::reverse(inputs.exposure_rows.clone().iter().cloned().fold(
                Rc::new(vec![]),
                |acc: _, row: Rc<DeclarationExposureRow>| {
                    match (row.occurrence.clone().value.clone() == fn_a_id.value.clone()) {
                        true => acc.clone(),
                        false => v1_rt::concat(Rc::new(vec![row.clone()]), acc.clone()),
                    }
                },
            )),
        }),
    }
}

pub fn obcpw_inputs_without_reference_authored_order(
    transport: Rc<OccurrenceTransport>,
    inputs: Rc<OccurrenceBindingCandidateInputs>,
    reference: Rc<ReferenceOccurrence>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    Rc::new(OccurrenceBindingCandidateInputs {
        module_paths: inputs.module_paths.clone(),
        exposure_rows: inputs.exposure_rows.clone(),
        authored_order_rows: v1_rt::reverse(
            inputs.authored_order_rows.clone().iter().cloned().fold(
                Rc::new(vec![]),
                |acc: _, row: Rc<AuthoredOrderRow>| match (row.occurrence.clone().value.clone()
                    == reference.occurrence.clone().value.clone())
                {
                    true => acc.clone(),
                    false => v1_rt::concat(Rc::new(vec![row.clone()]), acc.clone()),
                },
            ),
        ),
    })
}

pub fn occurrence_binding_parser_walk_exact_red_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Blocker 5: each negative control asserts its exact typed outcome — remove ModuleExposure-equivalent by dropping the declaration exposure row => MissingDeclarationExposure for that declaration; drop the reference AuthoredOrderRow => MissingAuthoredOrderRow for that occurrence; wrong LexicalExposure on a module sibling => Unbound for the exact reference; unknown OccurrenceId => TransportRefused. Never accept anything-except-Bound.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn parser_module_sibling_without_module_exposure_reds() -> bool {
    match (*obcpw_parse(
        "obcpw-sibling-red.dag".to_string(),
        obcpw_module_sibling_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => match reference_named(transport.clone(), "fn_a".to_string()) {
            None => false,
            Some(reference) => obcpw_reference_unbound_exactly(
                transport.clone(),
                obcpw_inputs_with_lexical_fn_a(transport.clone(), inputs.clone()),
                reference.clone(),
            ),
        },
    }
}

pub fn parser_missing_declaration_exposure_refuses_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-missing-exposure.dag".to_string(),
        obcpw_module_sibling_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => {
            match occurrence_id_for_authored_name(transport.clone(), "fn_a".to_string()) {
                None => false,
                Some(fn_a_id) => match reference_named(transport.clone(), "fn_a".to_string()) {
                    None => false,
                    Some(reference) => (match (*resolve_reference_binding_via_structural_candidates(
                        transport.clone(),
                        obcpw_inputs_without_fn_a_exposure(transport.clone(), inputs.clone()),
                        reference.clone(),
                    ))
                    .clone()
                    {
                        ReferenceBindingProjection::ReferenceBindingProjectionExposureRefused {
                            ref refusal,
                            ..
                        } => {
                            let DeclarationExposureIndexRefusal::MissingDeclarationExposure {
                                occurrence: missing,
                                ..
                            } = refusal.as_ref()
                            else {
                                unreachable!()
                            };
                            (missing.value.clone() == fn_a_id.value.clone())
                        }
                        _ => false,
                    } && match (*structural_binding_walk(
                        transport.clone(),
                        obcpw_inputs_without_fn_a_exposure(transport.clone(), inputs.clone()),
                    ))
                    .clone()
                    {
                        StructuralBindingWalk::StructuralBindingWalkRefused {
                            refusal: walk_refusal,
                            ..
                        } => match (*walk_refusal.clone()).clone() {
                            StructuralBindingIndexRefusal::StructuralBindingExposureRefusal {
                                refusal: exposure_refusal,
                                ..
                            } => match (*exposure_refusal.clone()).clone() {
                                DeclarationExposureIndexRefusal::MissingDeclarationExposure {
                                    occurrence: missing,
                                    ..
                                } => (missing.value.clone() == fn_a_id.value.clone()),
                                _ => false,
                            },
                            _ => false,
                        },
                        _ => false,
                    }),
                },
            }
        }
    }
}

pub fn parser_missing_authored_order_refuses_holds() -> bool {
    match (*obcpw_parse(
        "obcpw-missing-order.dag".to_string(),
        obcpw_source_order_source(),
    ))
    .clone()
    {
        ObcpwParsed::ObcpwParsedRefused => false,
        ObcpwParsed::ObcpwParsedReady {
            transport, inputs, ..
        } => match reference_named(transport.clone(), "fn_later".to_string()) {
            None => false,
            Some(reference) => (match (*resolve_reference_binding_via_structural_candidates(
                transport.clone(),
                obcpw_inputs_without_reference_authored_order(
                    transport.clone(),
                    inputs.clone(),
                    reference.clone(),
                ),
                reference.clone(),
            ))
            .clone()
            {
                ReferenceBindingProjection::ReferenceBindingProjectionAuthoredOrderRefused {
                    ref refusal,
                    ..
                } => {
                    let AuthoredOrderIndexRefusal::MissingAuthoredOrderRow {
                        occurrence: missing,
                        ..
                    } = refusal.as_ref()
                    else {
                        unreachable!()
                    };
                    (missing.value.clone() == reference.occurrence.clone().value.clone())
                }
                _ => false,
            } && match (*structural_binding_walk(
                transport.clone(),
                obcpw_inputs_without_reference_authored_order(
                    transport.clone(),
                    inputs.clone(),
                    reference.clone(),
                ),
            ))
            .clone()
            {
                StructuralBindingWalk::StructuralBindingWalkRefused {
                    refusal: walk_refusal,
                    ..
                } => match (*walk_refusal.clone()).clone() {
                    StructuralBindingIndexRefusal::StructuralBindingAuthoredOrderRefusal {
                        refusal: order_refusal,
                        ..
                    } => match (*order_refusal.clone()).clone() {
                        AuthoredOrderIndexRefusal::MissingAuthoredOrderRow {
                            occurrence: missing,
                            ..
                        } => (missing.value.clone() == reference.occurrence.clone().value.clone()),
                        _ => false,
                    },
                    _ => false,
                },
                _ => false,
            }),
        },
    }
}

pub fn namespace_reference_derived_closure_parser_integration_holds() -> bool {
    (((((((((parser_module_sibling_visible_holds()
        && parser_nested_lexical_only_inside_scope_holds())
        && parser_declaration_below_occurrence_not_visible_holds())
        && parser_same_text_distinct_identities_ambiguous_holds())
        && parser_repeated_bindings_project_one_module_edge_holds())
        && parser_transport_scoped_preserved_identity_and_unknown_occurrence_refuses_holds())
        && parser_production_call_binds_with_edge_holds())
        && parser_module_sibling_without_module_exposure_reds())
        && parser_missing_declaration_exposure_refuses_holds())
        && parser_missing_authored_order_refuses_holds())
}
