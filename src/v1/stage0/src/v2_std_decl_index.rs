// Generated by v1 compiler -- do not edit.
// Source module: v2.std.decl_index

use self::ItemKind::*;
pub use crate::std_algebra::FreeMonoid;
use crate::std_decl_ref::DeclField::*;
pub use crate::std_decl_ref::{DeclField, DeclarationRef};
use crate::std_disposition::ConstructionMechanism::*;
use crate::std_disposition::Disposition::*;
pub use crate::std_disposition::{ConstructionMechanism, Disposition};
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_algebra::list_snoc_item;
pub use crate::v2_std_node::Node;
pub use crate::v2_std_node_query::{node_labeled_child_edges, node_positional_child_targets};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug,
    Clone,
    Copy,
    PartialEq,
    Eq,
    PartialOrd,
    Ord,
    serde::Serialize,
    serde::Deserialize,
    std::hash::Hash,
)]
#[serde(tag = "_variant")]
pub enum ItemKind {
    FnItem,
    FuncItem,
    TypeItem,
    DataItem,
    ServiceItem,
    OtherItem,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DeclFact {
    pub qualified_name: String,
    pub name: String,
    pub kind: ItemKind,
    pub node: Rc<Node>,
    pub rel_path: String,
}

pub fn decl_facts(mut pool_roots: Rc<Vec<String>>) -> Rc<Vec<Rc<DeclFact>>> {
    loop {
        {
            continue;
        }
    }
}

pub fn export_signature_facts(mut pool_roots: Rc<Vec<String>>) -> Rc<Vec<Rc<DeclFact>>> {
    loop {
        {
            continue;
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DataDeclTypeFact {
    pub module_path: String,
    pub decl_name: String,
    pub type_name: String,
    pub rel_path: String,
}

pub fn data_decl_type_facts(mut pool_roots: Rc<Vec<String>>) -> Rc<Vec<Rc<DataDeclTypeFact>>> {
    loop {
        {
            continue;
        }
    }
}

pub fn export_signature_facts_host_scaffold_dissolution_trigger() -> Rc<Disposition> {
    thread_local! {
            static CACHED: Rc<Disposition> = {
                Rc::new(Disposition::Scaffold {
        dissolves_to: ConstructionMechanism::SingleAuthority,
        bind: DeclarationRef {
        module_path: "std.interface_summary".to_string(),
        decl_name: "interface_summary_v0_dissolution_trigger".to_string(),
        field: Rc::new(DeclField::WholeDeclaration),
    },
    })
            };
        }
    CACHED.with(|c: &Rc<Disposition>| c.clone())
}

pub fn logical_qualified_name_from_module(module_name: String, name: String) -> String {
    {
        let start = if v1_rt::starts_with(module_name.clone(), "v2.".to_string()) {
            3
        } else {
            0
        };
        let logical = v1_rt::substring(
            &module_name,
            start.clone(),
            v1_rt::string_length(&module_name),
        );
        if (v1_rt::string_length(&logical) == 0) {
            name
        } else {
            v1_rt::concat(v1_rt::concat(logical.clone(), ".".to_string()), name)
        }
    }
}

pub fn decl_facts_is_fn_like(kind: ItemKind) -> bool {
    match kind.clone() {
        ItemKind::FnItem => true,
        ItemKind::FuncItem => true,
        _ => false,
    }
}

pub fn decl_fact_node_has_body(node: Rc<Node>) -> bool {
    (((crate::v2_std_node_query::node_labeled_child_edges(node.clone()).len() as i64) > 0)
        || ((crate::v2_std_node_query::node_positional_child_targets(node.clone()).len() as i64)
            > 0))
}

pub fn decl_facts_fn_items(facts: Rc<Vec<Rc<DeclFact>>>) -> Rc<Vec<Rc<DeclFact>>> {
    facts
        .clone()
        .iter()
        .cloned()
        .fold(Rc::new(vec![]), |acc: _, fact: _| {
            if decl_facts_is_fn_like(fact.kind.clone()) {
                list_snoc_item(acc.clone(), fact.clone())
            } else {
                acc.clone()
            }
        })
}

pub fn decl_facts_count(pool_roots: Rc<Vec<String>>) -> i64 {
    (decl_facts(pool_roots.clone()).len() as i64)
}

pub fn decl_facts_fn_item_count(pool_roots: Rc<Vec<String>>) -> i64 {
    (decl_facts_fn_items(decl_facts(pool_roots.clone())).len() as i64)
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FnItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FuncItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct TypeItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct DataItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ServiceItem;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct OtherItem;
