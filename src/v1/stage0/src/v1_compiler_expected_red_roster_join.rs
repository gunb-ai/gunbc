// Generated by v1 compiler -- do not edit.
// Source module: v1.compiler.expected_red_roster_join

use self::BudgetVerdictCompletion::*;
use self::ExpectedRedJoinDisposition::*;
use self::HermeticEffectGround::*;
use self::WitnessEvalVerdict::*;
pub use crate::v1_compiler_emit_core_support::to_string;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum BudgetVerdictCompletion {
    Interrupted,
    CompletedOverBudget,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum HermeticEffectGround {
    UnpublishedMockCase,
    NoMockResponse,
    FilesystemRemoval,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum WitnessEvalVerdict {
    Passed,
    BoolFalse,
    NotBool {
        got: String,
    },
    RuntimeError {
        message: String,
    },
    HostToolUnresolved {
        name: String,
        probed: Rc<Vec<String>>,
    },
    HostEffectRefused {
        operation: String,
        ground: HermeticEffectGround,
    },
    BudgetExceeded {
        elapsed_ms: i64,
        budget_ms: i64,
        kind: String,
        completion: BudgetVerdictCompletion,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ExpectedRedJoinDisposition {
    StillRed,
    NowPasses,
    NotEvaluated { reason: String },
}
impl ExpectedRedJoinDisposition {
    pub fn reason(&self) -> String {
        match self {
            ExpectedRedJoinDisposition::StillRed => panic!("no reason on unit variant"),
            ExpectedRedJoinDisposition::NowPasses => panic!("no reason on unit variant"),
            ExpectedRedJoinDisposition::NotEvaluated { reason: __val, .. } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExpectedRedRosterJoinRow {
    pub identity: String,
    pub disposition: Rc<ExpectedRedJoinDisposition>,
    pub detail: String,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExpectedRedRosterJoinReport {
    pub run_head: Option<String>,
    pub run_note: String,
    pub rows: Rc<Vec<Rc<ExpectedRedRosterJoinRow>>>,
}

pub fn new_expected_red_roster_join_report(
    run_head: Option<String>,
    run_note: String,
    roster: Rc<Vec<String>>,
) -> Rc<ExpectedRedRosterJoinReport> {
    Rc::new(ExpectedRedRosterJoinReport {
        run_head: run_head.clone(),
        run_note: run_note.clone(),
        rows: Rc::new({
            let mut __result = Vec::new();
            for identity in roster.iter().cloned() {
                __result.push(Rc::new(ExpectedRedRosterJoinRow {
                    identity: identity.clone(),
                    disposition: Rc::new(ExpectedRedJoinDisposition::NotEvaluated {
                        reason: "not_observed".to_string(),
                    }),
                    detail: "".to_string(),
                }));
            }
            __result
        }),
    })
}

pub fn expected_red_roster_join_roster_len(report: Rc<ExpectedRedRosterJoinReport>) -> i64 {
    (report.rows.clone().len() as i64)
}

pub fn is_still_red(disposition: Rc<ExpectedRedJoinDisposition>) -> bool {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::StillRed => true,
        _ => false,
    }
}

pub fn is_now_passes(disposition: Rc<ExpectedRedJoinDisposition>) -> bool {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::NowPasses => true,
        _ => false,
    }
}

pub fn is_not_evaluated(disposition: Rc<ExpectedRedJoinDisposition>) -> bool {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::NotEvaluated { reason: _, .. } => true,
        _ => false,
    }
}

pub fn expected_red_roster_join_still_red(report: Rc<ExpectedRedRosterJoinReport>) -> i64 {
    (Rc::new({
        let mut __result = Vec::new();
        for row in report.rows.clone().iter().cloned() {
            if is_still_red(row.disposition.clone()) {
                __result.push(row);
            }
        }
        __result
    })
    .len() as i64)
}

pub fn expected_red_roster_join_now_passes(report: Rc<ExpectedRedRosterJoinReport>) -> i64 {
    (Rc::new({
        let mut __result = Vec::new();
        for row in report.rows.clone().iter().cloned() {
            if is_now_passes(row.disposition.clone()) {
                __result.push(row);
            }
        }
        __result
    })
    .len() as i64)
}

pub fn expected_red_roster_join_not_evaluated(report: Rc<ExpectedRedRosterJoinReport>) -> i64 {
    (Rc::new({
        let mut __result = Vec::new();
        for row in report.rows.clone().iter().cloned() {
            if is_not_evaluated(row.disposition.clone()) {
                __result.push(row);
            }
        }
        __result
    })
    .len() as i64)
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VerdictClassification {
    pub disposition: Rc<ExpectedRedJoinDisposition>,
    pub detail: String,
}

pub fn hermetic_effect_ground_label(ground: HermeticEffectGround) -> String {
    match ground.clone() {
        HermeticEffectGround::UnpublishedMockCase => {
            "no published mock case for a corpus-governed service".to_string()
        }
        HermeticEffectGround::NoMockResponse => "operation declares no mock_response".to_string(),
        HermeticEffectGround::FilesystemRemoval => {
            "filesystem removal has no mock arm; only a wet route can run it".to_string()
        }
    }
}

pub fn classify_verdict(verdict: Rc<WitnessEvalVerdict>) -> Rc<VerdictClassification> {
    match (*verdict.clone()).clone() {
        WitnessEvalVerdict::Passed => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::NowPasses),
            detail: "witness returned Bool(true)".to_string(),
        }),
        WitnessEvalVerdict::BoolFalse => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::StillRed),
            detail: "witness returned Bool(false)".to_string(),
        }),
        WitnessEvalVerdict::NotBool { got: got, .. } => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::StillRed),
            detail: v1_rt::concat(
                v1_rt::concat("witness answered ".to_string(), got.clone()),
                ", not a Bool".to_string(),
            ),
        }),
        WitnessEvalVerdict::RuntimeError {
            message: message, ..
        } => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::StillRed),
            detail: message.clone(),
        }),
        WitnessEvalVerdict::HostToolUnresolved { name, probed, .. } => {
            Rc::new(VerdictClassification {
                disposition: Rc::new(ExpectedRedJoinDisposition::NotEvaluated {
                    reason: "host_tool_unresolved".to_string(),
                }),
                detail: v1_rt::concat(
                    v1_rt::concat(
                        v1_rt::concat(
                            v1_rt::concat("host tool unresolved: ".to_string(), name.clone()),
                            " (probed: ".to_string(),
                        ),
                        probed.clone().join(&", ".to_string()),
                    ),
                    ")".to_string(),
                ),
            })
        }
        WitnessEvalVerdict::HostEffectRefused {
            operation, ground, ..
        } => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::NotEvaluated {
                reason: "host_effect_refused".to_string(),
            }),
            detail: v1_rt::concat(
                v1_rt::concat(
                    v1_rt::concat(
                        v1_rt::concat(
                            "hermetic route has no arm for ".to_string(),
                            operation.clone(),
                        ),
                        " (".to_string(),
                    ),
                    hermetic_effect_ground_label(ground.clone()),
                ),
                ") — the witness never reached its subject, so this is a route gap, not a verdict"
                    .to_string(),
            ),
        }),
        WitnessEvalVerdict::BudgetExceeded {
            elapsed_ms,
            budget_ms,
            kind,
            completion: BudgetVerdictCompletion::Interrupted,
            ..
        } => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::NotEvaluated {
                reason: "budget_refused".to_string(),
            }),
            detail: v1_rt::concat(
                v1_rt::concat(
                    v1_rt::concat(
                        v1_rt::concat(
                            v1_rt::concat(
                                v1_rt::concat("exceeded ".to_string(), kind.clone()),
                                " budget (".to_string(),
                            ),
                            (elapsed_ms.clone()).to_string(),
                        ),
                        "ms elapsed against ".to_string(),
                    ),
                    (budget_ms.clone()).to_string(),
                ),
                "ms; interrupted — no subject verdict)".to_string(),
            ),
        }),
        WitnessEvalVerdict::BudgetExceeded {
            elapsed_ms,
            budget_ms,
            kind,
            completion: BudgetVerdictCompletion::CompletedOverBudget,
            ..
        } => Rc::new(VerdictClassification {
            disposition: Rc::new(ExpectedRedJoinDisposition::NowPasses),
            detail: v1_rt::concat(
                v1_rt::concat(
                    v1_rt::concat(
                        v1_rt::concat(
                            v1_rt::concat(
                                v1_rt::concat("passed then exceeded ".to_string(), kind.clone()),
                                " budget (".to_string(),
                            ),
                            (elapsed_ms.clone()).to_string(),
                        ),
                        "ms against ".to_string(),
                    ),
                    (budget_ms.clone()).to_string(),
                ),
                "ms; stale roster row — prune enrollment, cost debt is separate)".to_string(),
            ),
        }),
    }
}

pub fn disposition_label(disposition: Rc<ExpectedRedJoinDisposition>) -> String {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::StillRed => "still_red".to_string(),
        ExpectedRedJoinDisposition::NowPasses => "now_passes".to_string(),
        ExpectedRedJoinDisposition::NotEvaluated { reason: _, .. } => "not_evaluated".to_string(),
    }
}

pub fn not_evaluated_reason(disposition: Rc<ExpectedRedJoinDisposition>) -> String {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::NotEvaluated { reason: reason, .. } => reason.clone(),
        _ => "".to_string(),
    }
}

pub fn row_with_recorded_verdict(
    row: Rc<ExpectedRedRosterJoinRow>,
    verdict: Rc<WitnessEvalVerdict>,
) -> Rc<ExpectedRedRosterJoinRow> {
    {
        let classification = classify_verdict(verdict.clone());
        Rc::new(ExpectedRedRosterJoinRow {
            identity: row.identity.clone(),
            disposition: classification.disposition.clone(),
            detail: classification.detail.clone(),
        })
    }
}

pub fn record_observed(
    report: Rc<ExpectedRedRosterJoinReport>,
    identity: String,
    verdict: Rc<WitnessEvalVerdict>,
) -> Rc<ExpectedRedRosterJoinReport> {
    Rc::new(ExpectedRedRosterJoinReport {
        run_head: report.run_head.clone(),
        run_note: report.run_note.clone(),
        rows: Rc::new({
            let mut __result = Vec::new();
            for row in report.rows.clone().iter().cloned() {
                __result.push(if (row.identity.clone() == identity.clone()) {
                    row_with_recorded_verdict(row.clone(), verdict.clone())
                } else {
                    row.clone()
                });
            }
            __result
        }),
    })
}

pub fn is_not_observed(disposition: Rc<ExpectedRedJoinDisposition>) -> bool {
    match (*disposition.clone()).clone() {
        ExpectedRedJoinDisposition::NotEvaluated { reason: reason, .. } => {
            (reason.clone() == "not_observed".to_string())
        }
        _ => false,
    }
}

pub fn row_finalized_if_not_observed(
    row: Rc<ExpectedRedRosterJoinRow>,
) -> Rc<ExpectedRedRosterJoinRow> {
    if is_not_observed(row.disposition.clone()) {
        Rc::new(ExpectedRedRosterJoinRow {
            identity: row.identity.clone(),
            disposition: Rc::new(ExpectedRedJoinDisposition::NotEvaluated {
                reason: "not_in_executed_manifest".to_string(),
            }),
            detail: "enrolled on roster but no matching claim executed in this run".to_string(),
        })
    } else {
        row.clone()
    }
}

pub fn finalize_not_observed(
    report: Rc<ExpectedRedRosterJoinReport>,
) -> Rc<ExpectedRedRosterJoinReport> {
    Rc::new(ExpectedRedRosterJoinReport {
        run_head: report.run_head.clone(),
        run_note: report.run_note.clone(),
        rows: Rc::new(
            report
                .rows
                .clone()
                .iter()
                .cloned()
                .map(row_finalized_if_not_observed)
                .collect::<Vec<_>>(),
        ),
    })
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Interrupted;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct CompletedOverBudget;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct UnpublishedMockCase;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NoMockResponse;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FilesystemRemoval;
