// Generated by v1 compiler -- do not edit.
// Source module: std.primitives

use self::CarrierCostSensitivity::*;
use self::Certainty::*;
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum Certainty {
    Proven,
    Amortized,
    Expected,
    Conservative,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CarrierCostSensitivity {
    CarrierInsensitive,
    CarrierSensitive {
        ephemeral_work: String,
        persistent_work: String,
    },
}
impl CarrierCostSensitivity {
    pub fn ephemeral_work(&self) -> String {
        match self {
            CarrierCostSensitivity::CarrierInsensitive => {
                panic!("no ephemeral_work on unit variant")
            }
            CarrierCostSensitivity::CarrierSensitive {
                ephemeral_work: __val,
                ..
            } => __val.clone(),
        }
    }
    pub fn persistent_work(&self) -> String {
        match self {
            CarrierCostSensitivity::CarrierInsensitive => {
                panic!("no persistent_work on unit variant")
            }
            CarrierCostSensitivity::CarrierSensitive {
                persistent_work: __val,
                ..
            } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PrimitiveContract {
    pub name: String,
    pub work: String,
    pub output_size: String,
    pub certainty: Certainty,
    pub carrier_cost: Rc<CarrierCostSensitivity>,
}

pub fn char_at_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "char_at".to_string(),
        work: "pos".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn string_length_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "string_length".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn substring_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "substring".to_string(),
        work: "end".to_string(),
        output_size: "end - start".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn string_contains_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "string_contains".to_string(),
        work: "n * m".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn starts_with_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "starts_with".to_string(),
        work: "m".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn ends_with_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "ends_with".to_string(),
        work: "m".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn trim_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "trim".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn concat_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "concat".to_string(),
        work: "n + m".to_string(),
        output_size: "n + m".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n + m".to_string(),
        persistent_work: "log(n + m)".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn split_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "split".to_string(),
        work: "n".to_string(),
        output_size: "k".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn repeat_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "repeat".to_string(),
        work: "m * n".to_string(),
        output_size: "m * n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn chars_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "chars".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn to_string_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "to_string".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn parse_int_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "parse_int".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn str_eq_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "str_eq".to_string(),
        work: "min(n, m)".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn replace_section_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "replace_section".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn code_point_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "code_point".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn from_code_point_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "from_code_point".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn scan_while_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "scan_while".to_string(),
        work: "k".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn skip_horizontal_ws_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "skip_horizontal_ws".to_string(),
        work: "k".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn scan_to_eol_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "scan_to_eol".to_string(),
        work: "k".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn scan_string_end_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "scan_string_end".to_string(),
        work: "k".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map".to_string(),
        work: "n * f_work".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn filter_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "filter".to_string(),
        work: "n * pred_work".to_string(),
        output_size: "k <= n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn filter_map_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "filter_map".to_string(),
        work: "n * f_work".to_string(),
        output_size: "k <= n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn flat_map_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "flat_map".to_string(),
        work: "n * f_work".to_string(),
        output_size: "sum(f_output_sizes)".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn fold_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "fold".to_string(),
        work: "n * f_work".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn any_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "any".to_string(),
        work: "k * pred_work".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn all_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "all".to_string(),
        work: "k * pred_work".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn contains_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "contains".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn join_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "join".to_string(),
        work: "n + total_length".to_string(),
        output_size: "total_length + (n - 1) * sep_length".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn sort_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "sort".to_string(),
        work: "n * log(n)".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn sort_by_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "sort_by".to_string(),
        work: "n * (key_work + log(n))".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn dedup_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "dedup".to_string(),
        work: "n * n".to_string(),
        output_size: "k <= n".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn len_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "len".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn count_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "count".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn sum_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "sum".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn first_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "first".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn last_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "last".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn get_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "get".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn max_by_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "max_by".to_string(),
        work: "n * f_work".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn reverse_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "reverse".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn list_push_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "list_push".to_string(),
        work: "1".to_string(),
        output_size: "n + 1".to_string(),
        certainty: Certainty::Amortized,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n".to_string(),
        persistent_work: "log n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn append_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "append".to_string(),
        work: "m".to_string(),
        output_size: "n + m".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n + m".to_string(),
        persistent_work: "log(n + m)".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn skip_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "skip".to_string(),
        work: "n".to_string(),
        output_size: "max(0, len - n)".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn zip_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "zip".to_string(),
        work: "min(n, m)".to_string(),
        output_size: "min(n, m)".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn enumerate_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "enumerate".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn empty_map_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "empty_map".to_string(),
        work: "1".to_string(),
        output_size: "0".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_insert_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_insert".to_string(),
        work: "1".to_string(),
        output_size: "n + 1".to_string(),
        certainty: Certainty::Amortized,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n".to_string(),
        persistent_work: "log32 n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_get_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_get".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Expected,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log32 n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_contains_key_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_contains_key".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Expected,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log32 n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn lookup_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "lookup".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Expected,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "1".to_string(),
        persistent_work: "log32 n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_merge_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_merge".to_string(),
        work: "m".to_string(),
        output_size: "n + m".to_string(),
        certainty: Certainty::Amortized,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n + m".to_string(),
        persistent_work: "m * log32(n + m)".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_keys_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_keys".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_values_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "map_values".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn index_by_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "index_by".to_string(),
        work: "n * key_fn_work".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Amortized,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn with_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "with".to_string(),
        work: "n + m".to_string(),
        output_size: "n + m".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierSensitive {
        ephemeral_work: "n + m".to_string(),
        persistent_work: "m * log32 n".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn filesystem_read_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "filesystem_read".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn to_bytes_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "to_bytes".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn to_json_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "to_json".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Conservative,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn hash_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "hash".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn hash_combine_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "hash_combine".to_string(),
        work: "1".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn atom_identity_hash_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "atom_identity_hash".to_string(),
        work: "n".to_string(),
        output_size: "1".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn contiguous_loop_elementwise_kernel_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "contiguous_loop_elementwise_kernel".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn contiguous_loop_elementwise_float_kernel_contract() -> Rc<PrimitiveContract> {
    thread_local! {
            static CACHED: Rc<PrimitiveContract> = {
                Rc::new(PrimitiveContract {
        name: "contiguous_loop_elementwise_float_kernel".to_string(),
        work: "n".to_string(),
        output_size: "n".to_string(),
        certainty: Certainty::Proven,
        carrier_cost: Rc::new(CarrierCostSensitivity::CarrierInsensitive),
    })
            };
        }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn builtin_registry_surface_names_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Enumerable registry surface names parallel to builtin_function_registry keys. Witness-time census cannot call map_keys on Map<String, Node> because Node is not a runtime Value. Relocated from v1.compiler.infer_method for dag/ compile scope (review 46064) — same parallel-enumeration debt, not a new fork. Dissolve-on: fold builtin_function_registry construction from this list as the single authority.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn builtin_registry_surface_names() -> Rc<Vec<String>> {
    Rc::new(vec![
        "count".to_string(),
        "string_length".to_string(),
        "code_point".to_string(),
        "to_int".to_string(),
        "scan_while".to_string(),
        "scan_string_end".to_string(),
        "scan_to_eol".to_string(),
        "skip_horizontal_ws".to_string(),
        "parse_int".to_string(),
        "char_at".to_string(),
        "substring".to_string(),
        "from_code_point".to_string(),
        "chars_to_string".to_string(),
        "record_source_chars_index_lookup".to_string(),
        "resolution_silent_pick_is_enabled".to_string(),
        "name_resolution_policy_is_namespace_only".to_string(),
        "resolution_silent_pick_record_global_bare_lcp_pick".to_string(),
        "resolution_silent_pick_record_global_bare_lcp_tie".to_string(),
        "resolution_silent_pick_record_fn_parent_first_hit".to_string(),
        "to_string".to_string(),
        "discriminant".to_string(),
        "concat".to_string(),
        "map_insert".to_string(),
        "map_merge".to_string(),
        "with".to_string(),
        "empty_set".to_string(),
        "set_insert".to_string(),
        "set_union".to_string(),
        "map_contains_key".to_string(),
        "map_has".to_string(),
        "map_is_empty".to_string(),
        "rc_ptr_eq".to_string(),
        "rc_vec_ptr_eq".to_string(),
        "emit_map_has".to_string(),
        "set_contains".to_string(),
        "lookup".to_string(),
        "map_get".to_string(),
        "Some".to_string(),
        "map_keys".to_string(),
        "sorted_map_keys".to_string(),
        "map_values".to_string(),
        "get".to_string(),
        "reverse".to_string(),
        "list_push".to_string(),
        "hash_combine".to_string(),
        "atom_identity_hash".to_string(),
        "trace_mark".to_string(),
        "observed_peak_resident_bytes".to_string(),
        "observed_monotonic_nanos".to_string(),
        "string_contains".to_string(),
        "length".to_string(),
        "starts_with".to_string(),
        "replace".to_string(),
        "filesystem_read".to_string(),
        "emit_host_run_transport".to_string(),
        "emit_host_run_transport_cached".to_string(),
        "emit_host_native_cache_evict".to_string(),
        "contiguous_loop_elementwise_kernel".to_string(),
        "contiguous_loop_elementwise_float_kernel".to_string(),
        "shell_materialize_operation_argv".to_string(),
        "shell_transport_operation_rows".to_string(),
        "extdeps_shape_transport_policy_facts_for_qualified_name".to_string(),
        "extdeps_qualified_name_resolves_in_derived_module_set".to_string(),
        "extdeps_external_authority_facts_for_qualified_name".to_string(),
        "extdeps_external_authority_live_clean_tree_holds".to_string(),
        "extdeps_external_authority_live_roster_module_count".to_string(),
        "seed_runner_bool_false_failure_detail".to_string(),
        "doc_graph_orphan_count".to_string(),
        "doc_graph_admitted_root_count".to_string(),
        "doc_graph_dangling_link_count".to_string(),
        "doc_graph_doc_count".to_string(),
        "compile_dag_rust_emit_check".to_string(),
        "witness_layer_roots_compile_clean_check".to_string(),
        "witness_layer_roots_compile_clean_emit_check".to_string(),
        "consume_floor_compile_clean_gate_verdict".to_string(),
        "consume_floor_compile_clean_gate_failure_detail".to_string(),
        "record_generated_artifact_drift_gate_failure_detail".to_string(),
        "consume_generated_artifact_drift_gate_failure_detail".to_string(),
        "witness_compile_clean_cli_floor_verdicts_agree".to_string(),
        "test_migration_debt_module_names".to_string(),
        "test_migration_legacy_behavior_ids".to_string(),
        "test_migration_witness_behavior_ids".to_string(),
        "test_migration_behavior_discovery_holds".to_string(),
        "inert_carrier_names_live".to_string(),
        "inert_carrier_declared_count".to_string(),
        "non_fold_residue_count".to_string(),
        "non_fold_residue_unrostered_count".to_string(),
        "non_fold_residue_stale_roster_count".to_string(),
        "non_fold_residue_coproduct_universe_count".to_string(),
        "commit_witness_claim_roster_unresolvable_count".to_string(),
        "commit_witness_claim_pair_resolvable".to_string(),
        "non_fold_residue_wildcard_red_fixture_holds".to_string(),
        "non_fold_residue_total_fold_green_fixture_holds".to_string(),
        "non_fold_residue_roster_red_fixture_holds".to_string(),
        "non_fold_residue_synthetic_unrostered_red_holds".to_string(),
        "complexity_linearity_syntactic_finding_count".to_string(),
        "complexity_linearity_wildcard_facts".to_string(),
        "complexity_linearity_syntactic_site_fired".to_string(),
        "fallback_arm_census_facts".to_string(),
        "fallback_arm_census_class_count".to_string(),
        "fallback_arm_census_total".to_string(),
        "fallback_arm_census_reconciliation_holds".to_string(),
        "census_corpus_roots_follow_layer_authority".to_string(),
        "transport_script_position_facts_for_path".to_string(),
        "layer_import_facts".to_string(),
        "import_resolution_facts".to_string(),
        "reference_resolution_facts".to_string(),
        "module_declaration_facts".to_string(),
        "concept_decl_facts".to_string(),
        "decl_facts".to_string(),
        "export_signature_facts".to_string(),
        "data_decl_type_facts".to_string(),
        "fact_cardinality_decl_facts".to_string(),
        "languages_consumer_census_data_decl_count".to_string(),
        "languages_consumer_census_per_language_row_count".to_string(),
        "languages_consumer_census_format_row_count".to_string(),
        "languages_consumer_census_external_consumer_count".to_string(),
        "languages_consumer_census_is_composition_only".to_string(),
        "languages_consumer_census_has_external_consumer".to_string(),
        "is_xid_start".to_string(),
        "is_xid_continue".to_string(),
        "is_emoji_ident".to_string(),
    ])
}

pub fn primitive_contract_roster_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Hand-maintained parallel enumeration of every *_contract data declaration in this module. A new foo_contract row silently under-reports on primitive_surface_census_derived until this roster is updated — the open-denominator class D0 is designed to expose. Dissolve-on: derive the roster by enumerating *_contract declarations once declaration introspection lands, so contract surfaces read the module rather than a second list.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn primitive_contract_roster() -> Rc<Vec<Rc<PrimitiveContract>>> {
    Rc::new(vec![
        char_at_contract(),
        string_length_contract(),
        substring_contract(),
        string_contains_contract(),
        starts_with_contract(),
        ends_with_contract(),
        trim_contract(),
        concat_contract(),
        split_contract(),
        repeat_contract(),
        chars_contract(),
        to_string_contract(),
        parse_int_contract(),
        str_eq_contract(),
        replace_section_contract(),
        code_point_contract(),
        from_code_point_contract(),
        scan_while_contract(),
        skip_horizontal_ws_contract(),
        scan_to_eol_contract(),
        scan_string_end_contract(),
        map_contract(),
        filter_contract(),
        filter_map_contract(),
        flat_map_contract(),
        fold_contract(),
        any_contract(),
        all_contract(),
        contains_contract(),
        join_contract(),
        sort_contract(),
        sort_by_contract(),
        dedup_contract(),
        len_contract(),
        count_contract(),
        sum_contract(),
        first_contract(),
        last_contract(),
        get_contract(),
        max_by_contract(),
        reverse_contract(),
        list_push_contract(),
        append_contract(),
        skip_contract(),
        zip_contract(),
        enumerate_contract(),
        empty_map_contract(),
        map_insert_contract(),
        map_get_contract(),
        map_contains_key_contract(),
        lookup_contract(),
        map_merge_contract(),
        map_keys_contract(),
        map_values_contract(),
        index_by_contract(),
        with_contract(),
        filesystem_read_contract(),
        to_bytes_contract(),
        to_json_contract(),
        hash_contract(),
        hash_combine_contract(),
        atom_identity_hash_contract(),
        contiguous_loop_elementwise_kernel_contract(),
        contiguous_loop_elementwise_float_kernel_contract(),
    ])
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Proven;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Amortized;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Expected;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Conservative;
