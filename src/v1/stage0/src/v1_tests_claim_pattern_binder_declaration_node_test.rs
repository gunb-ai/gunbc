// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.pattern_binder_declaration_node_test

use self::ParsedFixturePattern::*;
pub use crate::v1_compiler_parse::ParseContext;
pub use crate::v1_compiler_parse::{parse_pattern, token_stream_new};
pub use crate::v1_compiler_tokenize::tokenize;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::v1_std_core::ExprData::NoExprData;
use crate::v1_std_core::MatchPattern::{Bind, LitPattern, VariantPattern, Wildcard};
pub use crate::v1_std_core::{build_newline_index, empty_intern_table, field_binding_pattern};
pub use crate::v1_std_core::{ExprData, MatchPattern, NewlineIndex, Node};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn pattern_binder_declaration_node_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE: target/release/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/pattern_binder_declaration_node_test.dag --functions w_pattern_parser_materializes_dedicated_declaration_nodes,w_shorthand_wrapper_and_declaration_roles_remain_distinct".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pattern_binder_declaration_node_dissolve_on() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVE-ON: move this claim into normal discovery when v1 compiler claims are admitted under the regular witness source-root and cost budget.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ParsedFixturePattern {
    ParsedFixturePatternReady { pattern: Rc<MatchPattern> },
    ParsedFixturePatternRefused,
}
impl ParsedFixturePattern {
    pub fn pattern(&self) -> Rc<MatchPattern> {
        match self {
            ParsedFixturePattern::ParsedFixturePatternReady { pattern: __val, .. } => __val.clone(),
            ParsedFixturePattern::ParsedFixturePatternRefused => {
                panic!("no pattern on unit variant")
            }
        }
    }
}

pub fn parse_fixture_pattern(source: String) -> Rc<ParsedFixturePattern> {
    {
        let file = "pattern-binder-declaration-node.dag".to_string();
        let index = build_newline_index(file.clone(), source.clone());
        let result = parse_pattern(
            token_stream_new(tokenize(source.clone(), file.clone())),
            Rc::new(ParseContext {
                source_indices: v1_rt::rc_map_insert(
                    v1_rt::rc_empty_map::<String, Rc<NewlineIndex>>(),
                    file.clone(),
                    index.clone(),
                ),
                intern_table: empty_intern_table(),
            }),
        );
        match result.err.clone() {
            Some(_) => Rc::new(ParsedFixturePattern::ParsedFixturePatternRefused),
            None => Rc::new(ParsedFixturePattern::ParsedFixturePatternReady {
                pattern: result.pattern.clone(),
            }),
        }
    }
}

pub fn pattern_binder_named_count(pattern: Rc<MatchPattern>, name: String) -> i64 {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        match (*pattern.clone()).clone() {
            MatchPattern::Bind {
                declaration: declaration,
                ..
            } => {
                if (declaration.name.clone() == name.clone()) {
                    1
                } else {
                    0
                }
            }
            MatchPattern::LitPattern { value: _, .. } => 0,
            MatchPattern::Wildcard => 0,
            MatchPattern::VariantPattern {
                field_bindings: bindings,
                ..
            } => bindings
                .clone()
                .iter()
                .cloned()
                .fold(0, |count: i64, binding: Rc<Node>| {
                    (count
                        + pattern_binder_named_count(
                            field_binding_pattern(binding.clone()),
                            name.clone(),
                        ))
                }),
        }
    })
}

pub fn parsed_pattern_has_one_binder(source: String, name: String) -> bool {
    match (*parse_fixture_pattern(source.clone())).clone() {
        ParsedFixturePattern::ParsedFixturePatternRefused => false,
        ParsedFixturePattern::ParsedFixturePatternReady { pattern, .. } => {
            (pattern_binder_named_count(pattern.clone(), name.clone()) == 1)
        }
    }
}

pub fn w_pattern_parser_materializes_dedicated_declaration_nodes() -> bool {
    ((((parsed_pattern_has_one_binder("direct_bind".to_string(), "direct_bind".to_string())
        && parsed_pattern_has_one_binder(
            "Pair { left: explicit_bind }".to_string(),
            "explicit_bind".to_string(),
        ))
        && parsed_pattern_has_one_binder(
            "Shorthand { shorthand_only }".to_string(),
            "shorthand_only".to_string(),
        ))
        && parsed_pattern_has_one_binder(
            "Left(positional_bind)".to_string(),
            "positional_bind".to_string(),
        ))
        && parsed_pattern_has_one_binder(
            "Outer { pair: Pair { left: nested_bind } }".to_string(),
            "nested_bind".to_string(),
        ))
}

pub fn w_shorthand_wrapper_and_declaration_roles_remain_distinct() -> bool {
    match (*parse_fixture_pattern("Shorthand { shorthand_only }".to_string())).clone() {
        ParsedFixturePattern::ParsedFixturePatternRefused => false,
        ParsedFixturePattern::ParsedFixturePatternReady { pattern, .. } => match (*pattern.clone())
            .clone()
        {
            MatchPattern::VariantPattern {
                field_bindings: bindings,
                ..
            } => {
                (((bindings.clone().len() as i64) == 1)
                    && bindings.clone().iter().cloned().fold(
                        true,
                        |ok: bool, wrapper: Rc<Node>| {
                            ((ok && (wrapper.name.clone() == "shorthand_only".to_string()))
                                && match (*field_binding_pattern(wrapper.clone())).clone() {
                                    MatchPattern::Bind {
                                        declaration: declaration,
                                        ..
                                    } => {
                                        ((declaration.name.clone() == "shorthand_only".to_string())
                                            && match (*declaration.expr_data.clone()).clone() {
                                                ExprData::NoExprData => {
                                                    match declaration.match_pattern.clone() {
                                                        Some(_) => false,
                                                        None => true,
                                                    }
                                                }
                                                _ => false,
                                            })
                                    }
                                    _ => false,
                                })
                        },
                    ))
            }
            _ => false,
        },
    }
}
