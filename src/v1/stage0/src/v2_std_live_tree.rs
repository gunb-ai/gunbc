// Generated by v1 compiler -- do not edit.
// Source module: v2.std.live_tree

use self::LiveTreeDisposition::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn live_tree_disposition_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Declared witness input-domain disposition (ROADMAP 2-host-scaffold-classifier-defork): a witness entry declares whether its evaluation reads the live host tree (filesystem/corpus state outside its resolved substrate closure). The single authority is a `data live_tree_disposition: LiveTreeDisposition = ...` row in the WITNESS ENTRY FILE itself - entry grain, so the fact holds no matter where the read hides behind imports. Undeclared = ReadsLiveTree (fail-closed): a row must declare SubstrateInputsOnly to become affected-set selection-eligible. This dissolves the interim floor:host_scaffold marker and the Rust text classifier witness_test_fn_uses_live_host_scan (deleted with this model).\n\nCORRECTED 2026-08-11 (session lively-bat-548). This note previously read 'a row that declares SubstrateInputsOnly while actually reading live state is NOT re-checked by any text scan; the nightly affected-set falsifier is the enforcement'. Both halves were false in the live tree. A derived text scan over the entry's whole import closure DOES re-check the declaration - cli_run effect_reach_derived_reads_live_tree_for_entry, over effect_reach_host_sink_callee_symbols_v0 (v2.std.effect_reach) - and reads_live_tree_effective is the OR of declared and derived, so a lying row is caught at selection time rather than a cadence window later. The falsifier could not have been the enforcement for every subject either: its lanes resolve over dag plus src/v2, so a src/v1 witness entry is unresolvable there. Where a declaration is CONSUMED to admit expensive work, the OR is additionally too weak on its own: silently upgrading a stale SubstrateInputsOnly row to ReadsLiveTree hides a broken authority behind an always-run answer, so gunbc.scoped_floor_execution refuses that disagreement as DispositionAuthorityRefusal instead of degrading.\n\nFILE HOME. This module is authored under dag/ while keeping the module path v2.std.live_tree, because a witness entry under src/v1 must be able to declare its own disposition and the scoped witness subject universe is exactly dag plus src/v1 - src/v2 is deliberately excluded from it. Under src/v2 the carrier was invisible to half the corpus it classifies, so no src/v1 entry could declare the row at all and all of them inherited the fail-closed default. Paths are discriminators, not gospel (DESIGN section 3); the module path is unchanged, so every existing importer is untouched. Renaming the module to std.live_tree is a separate authority migration, not collateral to this repair.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn live_tree_disposition_stamp_provenance() -> String {
    thread_local! {
        static CACHED: String = {
            "Migration stamp provenance (2026-07-11, machine-stamped batch): the initial corpus-wide SubstrateInputsOnly rows were stamped from the deleted entry-text classifier's verdict (entry file contains none of: floor:host_scaffold, layer_import_facts, _live(, _facts_live(, filesystem_read) - ENTRY-TEXT GRAIN, so they inherit that classifier's declared blind spot: a live read hidden behind an import was invisible to it (the former cli_run.rs cross-file deficit). A stamped row is therefore machine-vouched, not author-vouched. Enforcement is the derived closure scan described in live_tree_disposition_note, which closes exactly the cross-file blind spot the entry-text classifier declared. Dissolves-at: G2 call-reachability live-read classification (import-closure walk over LiveReadCarrier registry + fn-arrow DependencyView) per docs/plans/live-read-witness-classification-design.md — when stamps can be re-derived from proven runtime-read closure.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum LiveTreeDisposition {
    ReadsLiveTree,
    SubstrateInputsOnly,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ReadsLiveTree;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct SubstrateInputsOnly;
