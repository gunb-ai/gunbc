// Generated by v1 compiler -- do not edit.
// Source module: v1.gunbc.nested_match_6777_length_classifier

use self::LengthClass::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn nested_match_6777_reproducer_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Reproducer for #6777 construction residue: two sibling Cons arms differing only by nested tail subpattern (Empty vs Cons). Authored match is correct; main emitter collapses to the first Cons arm body for every nonempty list. Permanent regression control once nested-match grouping lands (DESIGN §4b).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum LengthClass {
    Zero,
    One,
    Many,
}

pub fn mk_list(n: i64) -> Rc<Vec<i64>> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if (n.clone() <= 0) {
            Rc::new(vec![])
        } else {
            Rc::new({
                let mut __cons_v = (*mk_list((n.clone() - 1))).clone();
                __cons_v.insert(0, 1);
                __cons_v
            })
        }
    })
}

pub fn classify_length(xs: Rc<Vec<i64>>) -> LengthClass {
    {
        let __fm = xs.clone();
        if __fm.is_empty() {
            LengthClass::Zero
        } else {
            if __fm.len() == 1 {
                LengthClass::One
            } else {
                LengthClass::Many
            }
        }
    }
}

pub fn classify_length_tag(n: i64) -> String {
    match classify_length(mk_list(n.clone())) {
        LengthClass::Zero => "Zero".to_string(),
        LengthClass::One => "One".to_string(),
        LengthClass::Many => "Many".to_string(),
    }
}

pub fn classify_length_witness_holds() -> bool {
    ((((classify_length(mk_list(0)) == LengthClass::Zero)
        && (classify_length(mk_list(1)) == LengthClass::One))
        && (classify_length(mk_list(2)) == LengthClass::Many))
        && (classify_length(mk_list(3)) == LengthClass::Many))
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Zero;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct One;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Many;
