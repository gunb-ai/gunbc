// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.namespace_reference_derived_closure_production_admissions_witness_test

pub use crate::std_reference_binding_observation::ReferenceBindingObservation;
use crate::std_reference_binding_observation::ReferenceBindingObservation::{
    DistinctHomonymObservation, LaterDeclarationObservation, SameFileNeighbourObservation,
    SiblingBranchObservation,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List};
pub use crate::v1_gunbc_namespace_reference_derived_closure_production_admissions::namespace_structural_binding_observations_a_through_d;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn namespace_reference_derived_closure_production_admissions_witness_note() -> String {
    thread_local! {
        static CACHED: String = {
            "N3-A v1 executing witnesses: production returns neutral ReferenceBindingObservation carriers only — no admission types or assess_* imports in src/v1. Capability projection and closing-contract composition live at dag/test/claim witnesses.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn witness_structural_production_returns_four_observations() -> bool {
    ((namespace_structural_binding_observations_a_through_d().len() as i64) == 4)
}

pub fn witness_structural_production_variants_match_clauses_a_through_d() -> bool {
    {
        let observations = namespace_structural_binding_observations_a_through_d();
        ((({
            let mut __found = false;
            for obs in observations.clone().iter().cloned() {
                if match (*obs.clone()).clone() {
                    ReferenceBindingObservation::SameFileNeighbourObservation { .. } => true,
                    _ => false,
                } {
                    __found = true;
                    break;
                }
            }
            __found
        } && {
            let mut __found = false;
            for obs in observations.clone().iter().cloned() {
                if match (*obs.clone()).clone() {
                    ReferenceBindingObservation::SiblingBranchObservation { .. } => true,
                    _ => false,
                } {
                    __found = true;
                    break;
                }
            }
            __found
        }) && {
            let mut __found = false;
            for obs in observations.clone().iter().cloned() {
                if match (*obs.clone()).clone() {
                    ReferenceBindingObservation::LaterDeclarationObservation { .. } => true,
                    _ => false,
                } {
                    __found = true;
                    break;
                }
            }
            __found
        }) && {
            let mut __found = false;
            for obs in observations.clone().iter().cloned() {
                if match (*obs.clone()).clone() {
                    ReferenceBindingObservation::DistinctHomonymObservation { .. } => true,
                    _ => false,
                } {
                    __found = true;
                    break;
                }
            }
            __found
        })
    }
}
