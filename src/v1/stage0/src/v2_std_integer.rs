// Generated by v1 compiler -- do not edit.
// Source module: v2.std.integer

use self::DecimalDigit::*;
use self::DecimalMagnitude::*;
use self::IntegerBoundSign::*;
use self::IntegerIntervalBound::*;
use self::IntegerRangeEndpoint::*;
use self::NonZeroDecimalDigit::*;
use self::NonZeroDoubleLead::*;
use self::OverflowDisposition::*;
use self::Representation::*;
use self::StandardIntegerType::*;
use crate::std_algebra::Ordering::*;
pub use crate::std_algebra::{AbelianGroup, FreeMonoid, GroupCompletion, OrderedRing, Ordering};
pub use crate::std_bit::{Byte, Word128, Word16, Word32, Word64, Word8};
use crate::std_error_primitives::DivError::*;
use crate::std_error_primitives::Result::*;
pub use crate::std_error_primitives::{DivError, Result};
pub use crate::std_integer::Signedness;
use crate::std_integer::Signedness::*;
pub use crate::std_types::{Char, List};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_algebra::ListTailResult;
use crate::v2_std_algebra::ListTailResult::*;
pub use crate::v2_std_algebra::{fold_list, fold_list_node};
pub use crate::v2_std_collection::list_at_optional;
use crate::v2_std_diagnostic::Correction::*;
use crate::v2_std_diagnostic::Locus::*;
use crate::v2_std_diagnostic::NoCorrectionReason::*;
use crate::v2_std_diagnostic::Outcome::*;
pub use crate::v2_std_diagnostic::{
    diagnostics_singleton, node_locus, outcome_accepted, outcome_rejected, port_locus,
};
pub use crate::v2_std_diagnostic::{
    Correction, Diagnostic, Diagnostics, Locus, NoCorrectionReason, Outcome,
};
pub use crate::v2_std_machine::{MachineWidth, PointerWidth};
pub use crate::v2_std_nat::Nat;
use crate::v2_std_nat::Nat::*;
pub use crate::v2_std_node::is_empty_conj_root;
use crate::v2_std_node::Connective::*;
use crate::v2_std_node::NodeKind::*;
use crate::v2_std_node::NodeOccurrenceId::*;
pub use crate::v2_std_node::{Connective, Edge, Node, NodeKind, NodeOccurrenceId, Symbol};
pub use crate::v2_std_node_query::find_named_child;
pub use crate::v2_std_optional::Optional;
pub use crate::v2_std_optional::{optional_absent, optional_present};
pub use crate::v2_std_text::CharResult;
use crate::v2_std_text::CharResult::*;
pub use crate::v2_std_text::{string_head, string_tail};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub type Int = i64;

pub type UInt = Rc<Nat>;

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Compose<Carrier, Dimension>(pub std::marker::PhantomData<Carrier>);

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum Representation {
    TwosComplement,
    OnesComplement,
    SignMagnitude,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum OverflowDisposition {
    Wrapping,
    Saturating,
    UndefinedBehavior,
    Trapping,
}

pub type Int8 = Compose<i64, crate::v2_std_machine::MachineWidth<crate::std_bit::Word8>>;

pub type Int16 = Compose<i64, crate::v2_std_machine::MachineWidth<crate::std_bit::Word16>>;

pub type Int32 = Compose<i64, crate::v2_std_machine::MachineWidth<crate::std_bit::Word32>>;

pub type Int64 = Compose<i64, crate::v2_std_machine::MachineWidth<crate::std_bit::Word64>>;

pub type Int128 = Compose<i64, crate::v2_std_machine::MachineWidth<crate::std_bit::Word128>>;

pub type UInt8 = Compose<UInt, crate::v2_std_machine::MachineWidth<crate::std_bit::Word8>>;

pub type UInt16 = Compose<UInt, crate::v2_std_machine::MachineWidth<crate::std_bit::Word16>>;

pub type UInt32 = Compose<UInt, crate::v2_std_machine::MachineWidth<crate::std_bit::Word32>>;

pub type UInt64 = Compose<UInt, crate::v2_std_machine::MachineWidth<crate::std_bit::Word64>>;

pub type UInt128 = Compose<UInt, crate::v2_std_machine::MachineWidth<crate::std_bit::Word128>>;

pub type IntPlatform =
    Compose<i64, crate::v2_std_machine::MachineWidth<crate::v2_std_machine::PointerWidth>>;

pub type UIntPlatform =
    Compose<UInt, crate::v2_std_machine::MachineWidth<crate::v2_std_machine::PointerWidth>>;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum IntegerBoundSign {
    IntegerBoundNegative,
    IntegerBoundNonNegative,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum IntegerRangeEndpoint {
    IntegerRangeInclusiveMin,
    IntegerRangeInclusiveMax,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum DecimalDigit {
    D0,
    D1,
    D2,
    D3,
    D4,
    D5,
    D6,
    D7,
    D8,
    D9,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum NonZeroDecimalDigit {
    NonZeroD1,
    NonZeroD2,
    NonZeroD3,
    NonZeroD4,
    NonZeroD5,
    NonZeroD6,
    NonZeroD7,
    NonZeroD8,
    NonZeroD9,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DecimalNonZeroMagnitude {
    pub lead: NonZeroDecimalDigit,
    pub tail: Rc<Vec<DecimalDigit>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum DecimalMagnitude {
    DecimalZero,
    DecimalNonZero {
        magnitude: Rc<DecimalNonZeroMagnitude>,
    },
}
impl DecimalMagnitude {
    pub fn magnitude(&self) -> Rc<DecimalNonZeroMagnitude> {
        match self {
            DecimalMagnitude::DecimalZero => panic!("no magnitude on unit variant"),
            DecimalMagnitude::DecimalNonZero {
                magnitude: __val, ..
            } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum IntegerIntervalBound {
    IntegerConcreteZero,
    IntegerConcreteBound {
        sign: IntegerBoundSign,
        magnitude: Rc<DecimalNonZeroMagnitude>,
    },
    IntegerPointerWidthBound {
        signedness: Signedness,
        endpoint: IntegerRangeEndpoint,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IntegerIntervalSpec {
    pub inclusive_min: Rc<IntegerIntervalBound>,
    pub inclusive_max: Rc<IntegerIntervalBound>,
}

pub fn integer_standard_integer_type_decode_invalid_diagnostic(
    source_carrier: Rc<Node>,
) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "integer_standard_integer_type_decode_invalid".to_string(),
        at: node_locus(source_carrier.clone()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn integer_signed_i32_le_decode_invalid_diagnostic() -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "integer_standard_integer_type_decode_invalid".to_string(),
        at: port_locus("integer_signed_i32_le_decode_invalid".to_string()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn decimal_digit_value(d: DecimalDigit) -> Int {
    match d.clone() {
        DecimalDigit::D0 => 0,
        DecimalDigit::D1 => 1,
        DecimalDigit::D2 => 2,
        DecimalDigit::D3 => 3,
        DecimalDigit::D4 => 4,
        DecimalDigit::D5 => 5,
        DecimalDigit::D6 => 6,
        DecimalDigit::D7 => 7,
        DecimalDigit::D8 => 8,
        DecimalDigit::D9 => 9,
    }
}

pub fn decimal_digit_roster() -> Rc<Vec<DecimalDigit>> {
    thread_local! {
        static CACHED: Rc<Vec<DecimalDigit>> = {
            Rc::new(vec![DecimalDigit::D0, DecimalDigit::D1, DecimalDigit::D2, DecimalDigit::D3, DecimalDigit::D4, DecimalDigit::D5, DecimalDigit::D6, DecimalDigit::D7, DecimalDigit::D8, DecimalDigit::D9])
        };
    }
    CACHED.with(|c: &Rc<Vec<DecimalDigit>>| c.clone())
}

pub fn nonzero_decimal_digit_roster() -> Rc<Vec<NonZeroDecimalDigit>> {
    thread_local! {
        static CACHED: Rc<Vec<NonZeroDecimalDigit>> = {
            Rc::new(vec![NonZeroDecimalDigit::NonZeroD1, NonZeroDecimalDigit::NonZeroD2, NonZeroDecimalDigit::NonZeroD3, NonZeroDecimalDigit::NonZeroD4, NonZeroDecimalDigit::NonZeroD5, NonZeroDecimalDigit::NonZeroD6, NonZeroDecimalDigit::NonZeroD7, NonZeroDecimalDigit::NonZeroD8, NonZeroDecimalDigit::NonZeroD9])
        };
    }
    CACHED.with(|c: &Rc<Vec<NonZeroDecimalDigit>>| c.clone())
}

pub fn decimal_digit_of_units(mut value: Int) -> DecimalDigit {
    loop {
        if (value.clone() >= 10) {
            {
                let __tco_0 = (value - 10);
                value = __tco_0;
                continue;
            }
        } else {
            break fold_list(decimal_digit_roster(), DecimalDigit::D0, |acc, d| {
                if (decimal_digit_value(d.clone()) == value.clone()) {
                    d.clone()
                } else {
                    acc.clone()
                }
            });
        }
    }
}

pub fn nonzero_decimal_digit_of_positive_units(value: Int) -> NonZeroDecimalDigit {
    fold_list(
        nonzero_decimal_digit_roster(),
        NonZeroDecimalDigit::NonZeroD1,
        |acc, d| {
            if (decimal_digit_value(integer_nonzero_decimal_digit_widen(d.clone()))
                == value.clone())
            {
                d.clone()
            } else {
                acc.clone()
            }
        },
    )
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DecimalDigitStep {
    pub digit: DecimalDigit,
    pub carry: bool,
}

pub fn decimal_digit_double(d: DecimalDigit, carry_in: bool) -> Rc<DecimalDigitStep> {
    {
        let doubled = ((2 * decimal_digit_value(d.clone())) + if carry_in.clone() { 1 } else { 0 });
        Rc::new(DecimalDigitStep {
            digit: decimal_digit_of_units(doubled.clone()),
            carry: (doubled.clone() >= 10),
        })
    }
}

pub fn decimal_digit_sub_borrow(d: DecimalDigit, borrow_in: bool) -> Rc<DecimalDigitStep> {
    {
        let difference = (decimal_digit_value(d.clone()) - if borrow_in.clone() { 1 } else { 0 });
        Rc::new(DecimalDigitStep {
            digit: decimal_digit_of_units((difference.clone() + 10)),
            carry: (difference.clone() < 0),
        })
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DecimalDigitsStep {
    pub digits: Rc<Vec<DecimalDigit>>,
    pub carry: bool,
}

pub fn decimal_digits_double(digits: Rc<Vec<DecimalDigit>>) -> Rc<DecimalDigitsStep> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || match digits.clone() {
        FreeMonoid::Empty => Rc::new(DecimalDigitsStep {
            digits: Rc::new(vec![]),
            carry: false,
        }),
        FreeMonoid::Cons { head, tail, .. } => match decimal_digits_double(tail.clone()) {
            DecimalDigitsStep {
                digits: doubled_tail,
                carry: tail_carry,
                ..
            } => match decimal_digit_double(head.clone(), tail_carry.clone()) {
                DecimalDigitStep { digit, carry, .. } => Rc::new(DecimalDigitsStep {
                    digits: Rc::new({
                        let mut __cons_v = (*doubled_tail.clone()).clone();
                        __cons_v.insert(0, digit.clone());
                        __cons_v
                    }),
                    carry: carry.clone(),
                }),
            },
        },
    })
}

pub fn decimal_digits_sub_one(digits: Rc<Vec<DecimalDigit>>) -> Rc<DecimalDigitsStep> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || match digits.clone() {
        FreeMonoid::Empty => Rc::new(DecimalDigitsStep {
            digits: Rc::new(vec![]),
            carry: true,
        }),
        FreeMonoid::Cons { head, tail, .. } => match decimal_digits_sub_one(tail.clone()) {
            DecimalDigitsStep {
                digits: sub_tail,
                carry: tail_borrow,
                ..
            } => match decimal_digit_sub_borrow(head.clone(), tail_borrow.clone()) {
                DecimalDigitStep { digit, carry, .. } => Rc::new(DecimalDigitsStep {
                    digits: Rc::new({
                        let mut __cons_v = (*sub_tail.clone()).clone();
                        __cons_v.insert(0, digit.clone());
                        __cons_v
                    }),
                    carry: carry.clone(),
                }),
            },
        },
    })
}

pub fn decimal_digits_to_magnitude(mut digits: Rc<Vec<DecimalDigit>>) -> Rc<DecimalMagnitude> {
    loop {
        match digits.clone() {
            FreeMonoid::Empty => {
                break Rc::new(DecimalMagnitude::DecimalZero);
            }
            FreeMonoid::Cons { head, tail, .. } => {
                if (decimal_digit_value(head.clone()) == 0) {
                    {
                        let __tco_0 = tail.clone();
                        digits = __tco_0;
                        continue;
                    }
                } else {
                    break Rc::new(DecimalMagnitude::DecimalNonZero {
                        magnitude: Rc::new(DecimalNonZeroMagnitude {
                            lead: nonzero_decimal_digit_of_positive_units(decimal_digit_value(
                                head.clone(),
                            )),
                            tail: tail.clone(),
                        }),
                    });
                }
            }
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum NonZeroDoubleLead {
    NonZeroDoubleStays { lead: NonZeroDecimalDigit },
    NonZeroDoubleOverflows { pushed: DecimalDigit },
}

pub fn nonzero_decimal_digit_double_lead(
    lead: NonZeroDecimalDigit,
    carry_in: bool,
) -> Rc<NonZeroDoubleLead> {
    {
        let doubled =
            ((2 * decimal_digit_value(integer_nonzero_decimal_digit_widen(lead.clone())))
                + if carry_in.clone() { 1 } else { 0 });
        if (doubled.clone() >= 10) {
            Rc::new(NonZeroDoubleLead::NonZeroDoubleOverflows {
                pushed: decimal_digit_of_units(doubled.clone()),
            })
        } else {
            Rc::new(NonZeroDoubleLead::NonZeroDoubleStays {
                lead: nonzero_decimal_digit_of_positive_units(doubled.clone()),
            })
        }
    }
}

pub fn decimal_nonzero_double(m: Rc<DecimalNonZeroMagnitude>) -> Rc<DecimalNonZeroMagnitude> {
    match decimal_digits_double(m.tail.clone()) {
        DecimalDigitsStep {
            digits: doubled_tail,
            carry: tail_carry,
            ..
        } => match (*nonzero_decimal_digit_double_lead(m.lead.clone(), tail_carry.clone())).clone()
        {
            NonZeroDoubleLead::NonZeroDoubleStays { lead: lead, .. } => {
                Rc::new(DecimalNonZeroMagnitude {
                    lead: lead.clone(),
                    tail: doubled_tail.clone(),
                })
            }
            NonZeroDoubleLead::NonZeroDoubleOverflows { pushed: pushed, .. } => {
                Rc::new(DecimalNonZeroMagnitude {
                    lead: NonZeroDecimalDigit::NonZeroD1,
                    tail: Rc::new({
                        let mut __cons_v = (*doubled_tail.clone()).clone();
                        __cons_v.insert(0, pushed.clone());
                        __cons_v
                    }),
                })
            }
        },
    }
}

pub fn nonzero_decimal_digit_minus_borrow(
    lead: NonZeroDecimalDigit,
    borrow_in: bool,
) -> DecimalDigit {
    decimal_digit_of_units(
        (decimal_digit_value(integer_nonzero_decimal_digit_widen(lead.clone()))
            - if borrow_in.clone() { 1 } else { 0 }),
    )
}

pub fn decimal_nonzero_sub_one(m: Rc<DecimalNonZeroMagnitude>) -> Rc<DecimalMagnitude> {
    match decimal_digits_sub_one(m.tail.clone()) {
        DecimalDigitsStep {
            digits: sub_tail,
            carry: tail_borrow,
            ..
        } => decimal_digits_to_magnitude(Rc::new({
            let mut __cons_v = (*sub_tail.clone()).clone();
            __cons_v.insert(
                0,
                nonzero_decimal_digit_minus_borrow(m.lead.clone(), tail_borrow.clone()),
            );
            __cons_v
        })),
    }
}

pub fn integer_pow2_magnitude(exponent: Int) -> Rc<DecimalNonZeroMagnitude> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if (exponent.clone() <= 0) {
            Rc::new(DecimalNonZeroMagnitude {
                lead: NonZeroDecimalDigit::NonZeroD1,
                tail: Rc::new(vec![]),
            })
        } else {
            decimal_nonzero_double(integer_pow2_magnitude((exponent.clone() - 1)))
        }
    })
}

pub fn decimal_digit_compare(left: DecimalDigit, right: DecimalDigit) -> Ordering {
    int_compare(
        decimal_digit_value(left.clone()),
        decimal_digit_value(right.clone()),
    )
}

pub fn decimal_digit_list_compare(
    mut left: Rc<Vec<DecimalDigit>>,
    mut right: Rc<Vec<DecimalDigit>>,
) -> Ordering {
    loop {
        match left.clone() {
            FreeMonoid::Empty => match right.clone() {
                FreeMonoid::Empty => {
                    break Ordering::Equal;
                }
                FreeMonoid::Cons { .. } => {
                    break Ordering::Less;
                }
            },
            FreeMonoid::Cons {
                head: left_head,
                tail: left_tail,
                ..
            } => match right.clone() {
                FreeMonoid::Empty => {
                    break Ordering::Greater;
                }
                FreeMonoid::Cons {
                    head: right_head,
                    tail: right_tail,
                    ..
                } => match decimal_digit_compare(left_head.clone(), right_head.clone()) {
                    Ordering::Equal => {
                        let __tco_0 = left_tail.clone();
                        let __tco_1 = right_tail.clone();
                        left = __tco_0;
                        right = __tco_1;
                        continue;
                    }
                    Ordering::Less => {
                        break Ordering::Less;
                    }
                    Ordering::Greater => {
                        break Ordering::Greater;
                    }
                },
            },
        }
    }
}

pub fn decimal_nonzero_magnitude_compare(
    left: Rc<DecimalNonZeroMagnitude>,
    right: Rc<DecimalNonZeroMagnitude>,
) -> Ordering {
    {
        let left_len = (1 + (left.tail.clone().len() as i64));
        let right_len = (1 + (right.tail.clone().len() as i64));
        if (left_len.clone() < right_len.clone()) {
            Ordering::Less
        } else {
            if (left_len.clone() > right_len.clone()) {
                Ordering::Greater
            } else {
                match decimal_digit_compare(
                    integer_nonzero_decimal_digit_widen(left.lead.clone()),
                    integer_nonzero_decimal_digit_widen(right.lead.clone()),
                ) {
                    Ordering::Equal => {
                        decimal_digit_list_compare(left.tail.clone(), right.tail.clone())
                    }
                    Ordering::Less => Ordering::Less,
                    Ordering::Greater => Ordering::Greater,
                }
            }
        }
    }
}

pub fn ordering_invert(ordering: Ordering) -> Ordering {
    match ordering.clone() {
        Ordering::Less => Ordering::Greater,
        Ordering::Equal => Ordering::Equal,
        Ordering::Greater => Ordering::Less,
    }
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum StandardIntegerType {
    Int128,
    Int64,
    Int32,
    Int16,
    Int8,
    UInt128,
    UInt64,
    UInt32,
    UInt16,
    UInt8,
}

pub fn integer_standard_integer_type_signedness(
    standard_integer_type: StandardIntegerType,
) -> Signedness {
    match standard_integer_type.clone() {
        StandardIntegerType::Int128 => Signedness::Signed,
        StandardIntegerType::Int64 => Signedness::Signed,
        StandardIntegerType::Int32 => Signedness::Signed,
        StandardIntegerType::Int16 => Signedness::Signed,
        StandardIntegerType::Int8 => Signedness::Signed,
        StandardIntegerType::UInt128 => Signedness::Unsigned,
        StandardIntegerType::UInt64 => Signedness::Unsigned,
        StandardIntegerType::UInt32 => Signedness::Unsigned,
        StandardIntegerType::UInt16 => Signedness::Unsigned,
        StandardIntegerType::UInt8 => Signedness::Unsigned,
    }
}

pub fn integer_standard_integer_type_width_bits(standard_integer_type: StandardIntegerType) -> Int {
    match standard_integer_type.clone() {
        StandardIntegerType::Int128 => 128,
        StandardIntegerType::Int64 => 64,
        StandardIntegerType::Int32 => 32,
        StandardIntegerType::Int16 => 16,
        StandardIntegerType::Int8 => 8,
        StandardIntegerType::UInt128 => 128,
        StandardIntegerType::UInt64 => 64,
        StandardIntegerType::UInt32 => 32,
        StandardIntegerType::UInt16 => 16,
        StandardIntegerType::UInt8 => 8,
    }
}

pub fn integer_magnitude_upper_bound(magnitude: Rc<DecimalMagnitude>) -> Rc<IntegerIntervalBound> {
    match (*magnitude.clone()).clone() {
        DecimalMagnitude::DecimalZero => Rc::new(IntegerIntervalBound::IntegerConcreteZero),
        DecimalMagnitude::DecimalNonZero {
            magnitude: magnitude,
            ..
        } => Rc::new(IntegerIntervalBound::IntegerConcreteBound {
            sign: IntegerBoundSign::IntegerBoundNonNegative,
            magnitude: magnitude.clone(),
        }),
    }
}

pub fn integer_machine_interval_spec(
    signedness: Signedness,
    width_bits: Int,
) -> Rc<IntegerIntervalSpec> {
    match signedness.clone() {
        Signedness::Signed => integer_interval(
            integer_concrete_bound(
                IntegerBoundSign::IntegerBoundNegative,
                integer_pow2_magnitude((width_bits.clone() - 1)),
            ),
            integer_magnitude_upper_bound(decimal_nonzero_sub_one(integer_pow2_magnitude(
                (width_bits.clone() - 1),
            ))),
        ),
        Signedness::Unsigned => integer_interval(
            integer_concrete_zero(),
            integer_magnitude_upper_bound(decimal_nonzero_sub_one(integer_pow2_magnitude(
                width_bits.clone(),
            ))),
        ),
    }
}

pub fn integer_standard_integer_type_interval_spec(
    standard_integer_type: StandardIntegerType,
) -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(
        integer_standard_integer_type_signedness(standard_integer_type.clone()),
        integer_standard_integer_type_width_bits(standard_integer_type.clone()),
    )
}

pub fn integer_standard_integer_type_roster() -> Rc<Vec<StandardIntegerType>> {
    thread_local! {
        static CACHED: Rc<Vec<StandardIntegerType>> = {
            Rc::new(vec![StandardIntegerType::Int128, StandardIntegerType::Int64, StandardIntegerType::Int32, StandardIntegerType::Int16, StandardIntegerType::Int8, StandardIntegerType::UInt128, StandardIntegerType::UInt64, StandardIntegerType::UInt32, StandardIntegerType::UInt16, StandardIntegerType::UInt8])
        };
    }
    CACHED.with(|c: &Rc<Vec<StandardIntegerType>>| c.clone())
}

pub fn integer_standard_integer_type_from_node(node: Rc<Node>) -> Rc<Outcome<StandardIntegerType>> {
    fold_list(
        integer_standard_integer_type_roster(),
        Rc::new(Outcome::Rejected {
            diagnostics: diagnostics_singleton(
                integer_standard_integer_type_decode_invalid_diagnostic(node.clone()),
            ),
        }),
        |acc, candidate| match (*acc.clone()).clone() {
            Outcome::Accepted { .. } => acc.clone(),
            Outcome::Rejected { diagnostics: _, .. } => {
                if (node.clone()
                    == integer_interval_spec_node(integer_standard_integer_type_interval_spec(
                        candidate.clone(),
                    )))
                {
                    Rc::new(Outcome::Accepted {
                        value: candidate.clone(),
                        diagnostics: None,
                    })
                } else {
                    acc.clone()
                }
            }
        },
    )
}

pub fn integer_pointer_width_bound_compare(
    left_signedness: Signedness,
    left_endpoint: IntegerRangeEndpoint,
    right_signedness: Signedness,
    right_endpoint: IntegerRangeEndpoint,
) -> Ordering {
    match left_signedness.clone() {
        Signedness::Signed => match right_signedness.clone() {
            Signedness::Signed => match left_endpoint.clone() {
                IntegerRangeEndpoint::IntegerRangeInclusiveMin => match right_endpoint.clone() {
                    IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Equal,
                    IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
                },
                IntegerRangeEndpoint::IntegerRangeInclusiveMax => match right_endpoint.clone() {
                    IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Greater,
                    IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Equal,
                },
            },
            Signedness::Unsigned => Ordering::Less,
        },
        Signedness::Unsigned => match right_signedness.clone() {
            Signedness::Signed => Ordering::Greater,
            Signedness::Unsigned => match left_endpoint.clone() {
                IntegerRangeEndpoint::IntegerRangeInclusiveMin => match right_endpoint.clone() {
                    IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Equal,
                    IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
                },
                IntegerRangeEndpoint::IntegerRangeInclusiveMax => match right_endpoint.clone() {
                    IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Greater,
                    IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Equal,
                },
            },
        },
    }
}

pub fn integer_concrete_zero_compare_to_pointer_width(
    right_signedness: Signedness,
    right_endpoint: IntegerRangeEndpoint,
) -> Ordering {
    match right_signedness.clone() {
        Signedness::Signed => match right_endpoint.clone() {
            IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Greater,
            IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
        },
        Signedness::Unsigned => match right_endpoint.clone() {
            IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Equal,
            IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
        },
    }
}

pub fn integer_concrete_bound_compare_to_pointer_width(
    left_sign: IntegerBoundSign,
    right_signedness: Signedness,
    right_endpoint: IntegerRangeEndpoint,
) -> Ordering {
    match right_signedness.clone() {
        Signedness::Signed => match right_endpoint.clone() {
            IntegerRangeEndpoint::IntegerRangeInclusiveMin => Ordering::Greater,
            IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
        },
        Signedness::Unsigned => match right_endpoint.clone() {
            IntegerRangeEndpoint::IntegerRangeInclusiveMin => match left_sign.clone() {
                IntegerBoundSign::IntegerBoundNegative => Ordering::Less,
                IntegerBoundSign::IntegerBoundNonNegative => Ordering::Greater,
            },
            IntegerRangeEndpoint::IntegerRangeInclusiveMax => Ordering::Less,
        },
    }
}

pub fn integer_interval_bound_compare(
    left: Rc<IntegerIntervalBound>,
    right: Rc<IntegerIntervalBound>,
) -> Ordering {
    match (*left.clone()).clone() {
        IntegerIntervalBound::IntegerConcreteZero => match (*right.clone()).clone() {
            IntegerIntervalBound::IntegerConcreteZero => Ordering::Equal,
            IntegerIntervalBound::IntegerConcreteBound { sign, .. } => match sign.clone() {
                IntegerBoundSign::IntegerBoundNegative => Ordering::Greater,
                IntegerBoundSign::IntegerBoundNonNegative => Ordering::Less,
            },
            IntegerIntervalBound::IntegerPointerWidthBound {
                signedness,
                endpoint,
                ..
            } => {
                integer_concrete_zero_compare_to_pointer_width(signedness.clone(), endpoint.clone())
            }
        },
        IntegerIntervalBound::IntegerConcreteBound {
            sign: left_sign,
            magnitude: left_magnitude,
            ..
        } => match (*right.clone()).clone() {
            IntegerIntervalBound::IntegerConcreteZero => match left_sign.clone() {
                IntegerBoundSign::IntegerBoundNegative => Ordering::Less,
                IntegerBoundSign::IntegerBoundNonNegative => Ordering::Greater,
            },
            IntegerIntervalBound::IntegerConcreteBound {
                sign: right_sign,
                magnitude: right_magnitude,
                ..
            } => match left_sign.clone() {
                IntegerBoundSign::IntegerBoundNegative => match right_sign.clone() {
                    IntegerBoundSign::IntegerBoundNegative => {
                        match decimal_nonzero_magnitude_compare(
                            left_magnitude.clone(),
                            right_magnitude.clone(),
                        ) {
                            Ordering::Equal => Ordering::Equal,
                            Ordering::Less => Ordering::Greater,
                            Ordering::Greater => Ordering::Less,
                        }
                    }
                    IntegerBoundSign::IntegerBoundNonNegative => Ordering::Less,
                },
                IntegerBoundSign::IntegerBoundNonNegative => match right_sign.clone() {
                    IntegerBoundSign::IntegerBoundNegative => Ordering::Greater,
                    IntegerBoundSign::IntegerBoundNonNegative => decimal_nonzero_magnitude_compare(
                        left_magnitude.clone(),
                        right_magnitude.clone(),
                    ),
                },
            },
            IntegerIntervalBound::IntegerPointerWidthBound {
                signedness,
                endpoint,
                ..
            } => integer_concrete_bound_compare_to_pointer_width(
                left_sign.clone(),
                signedness.clone(),
                endpoint.clone(),
            ),
        },
        IntegerIntervalBound::IntegerPointerWidthBound {
            signedness: left_signedness,
            endpoint: left_endpoint,
            ..
        } => match (*right.clone()).clone() {
            IntegerIntervalBound::IntegerConcreteZero => crate::v2_std_integer::ordering_invert(
                integer_concrete_zero_compare_to_pointer_width(
                    left_signedness.clone(),
                    left_endpoint.clone(),
                ),
            ),
            IntegerIntervalBound::IntegerConcreteBound {
                sign: right_sign,
                magnitude: right_magnitude,
                ..
            } => crate::v2_std_integer::ordering_invert(
                integer_concrete_bound_compare_to_pointer_width(
                    right_sign.clone(),
                    left_signedness.clone(),
                    left_endpoint.clone(),
                ),
            ),
            IntegerIntervalBound::IntegerPointerWidthBound {
                signedness: right_signedness,
                endpoint: right_endpoint,
                ..
            } => integer_pointer_width_bound_compare(
                left_signedness.clone(),
                left_endpoint.clone(),
                right_signedness.clone(),
                right_endpoint.clone(),
            ),
        },
    }
}

pub fn integer_interval_bound_less_or_equal(
    left: Rc<IntegerIntervalBound>,
    right: Rc<IntegerIntervalBound>,
) -> bool {
    match integer_interval_bound_compare(left.clone(), right.clone()) {
        Ordering::Less => true,
        Ordering::Equal => true,
        Ordering::Greater => false,
    }
}

pub fn integer_interval_bound_is_pointer_width(bound: Rc<IntegerIntervalBound>) -> bool {
    match (*bound.clone()).clone() {
        IntegerIntervalBound::IntegerPointerWidthBound { .. } => true,
        IntegerIntervalBound::IntegerConcreteZero => false,
        IntegerIntervalBound::IntegerConcreteBound { .. } => false,
    }
}

pub fn integer_interval_spec_is_pointer_width(spec: Rc<IntegerIntervalSpec>) -> bool {
    (integer_interval_bound_is_pointer_width(spec.inclusive_min.clone())
        && integer_interval_bound_is_pointer_width(spec.inclusive_max.clone()))
}

pub fn integer_interval_spec_is_concrete(spec: Rc<IntegerIntervalSpec>) -> bool {
    (!integer_interval_bound_is_pointer_width(spec.inclusive_min.clone())
        && !integer_interval_bound_is_pointer_width(spec.inclusive_max.clone()))
}

pub fn integer_interval_spec_contains(
    container: Rc<IntegerIntervalSpec>,
    contained: Rc<IntegerIntervalSpec>,
) -> bool {
    if integer_interval_spec_is_concrete(container.clone()) {
        ((integer_interval_spec_is_concrete(contained.clone())
            && integer_interval_bound_less_or_equal(
                container.inclusive_min.clone(),
                contained.inclusive_min.clone(),
            ))
            && integer_interval_bound_less_or_equal(
                contained.inclusive_max.clone(),
                container.inclusive_max.clone(),
            ))
    } else {
        if integer_interval_spec_is_pointer_width(container.clone()) {
            ((integer_interval_spec_is_pointer_width(contained.clone())
                && integer_interval_bound_less_or_equal(
                    container.inclusive_min.clone(),
                    contained.inclusive_min.clone(),
                ))
                && integer_interval_bound_less_or_equal(
                    contained.inclusive_max.clone(),
                    container.inclusive_max.clone(),
                ))
        } else {
            false
        }
    }
}

pub fn integer_interval_spec_strictly_contains(
    container: Rc<IntegerIntervalSpec>,
    contained: Rc<IntegerIntervalSpec>,
) -> bool {
    (integer_interval_spec_contains(container.clone(), contained.clone())
        && !integer_interval_spec_contains(contained.clone(), container.clone()))
}

pub fn integer_interval_spec_contains_decoded_node(
    container: Rc<IntegerIntervalSpec>,
    contained: StandardIntegerType,
) -> bool {
    integer_interval_spec_contains(
        container.clone(),
        integer_standard_integer_type_interval_spec(contained.clone()),
    )
}

pub fn integer_interval_spec_node_contains(container: Rc<Node>, contained: Rc<Node>) -> bool {
    match integer_standard_integer_type_from_node(container.clone()) {
        Outcome::Accepted {
            value: decoded_container,
            diagnostics: None,
            ..
        } => match integer_standard_integer_type_from_node(contained.clone()) {
            Outcome::Accepted {
                value: decoded_contained,
                diagnostics: None,
                ..
            } => integer_interval_spec_contains_decoded_node(
                integer_standard_integer_type_interval_spec(decoded_container.clone()),
                decoded_contained.clone(),
            ),
            Outcome::Rejected { diagnostics: _, .. } => false,
        },
        Outcome::Rejected { diagnostics: _, .. } => false,
    }
}

pub fn integer_interval(
    inclusive_min: Rc<IntegerIntervalBound>,
    inclusive_max: Rc<IntegerIntervalBound>,
) -> Rc<IntegerIntervalSpec> {
    Rc::new(IntegerIntervalSpec {
        inclusive_min: inclusive_min.clone(),
        inclusive_max: inclusive_max.clone(),
    })
}

pub fn integer_concrete_zero() -> Rc<IntegerIntervalBound> {
    Rc::new(IntegerIntervalBound::IntegerConcreteZero)
}

pub fn integer_concrete_bound(
    sign: IntegerBoundSign,
    magnitude: Rc<DecimalNonZeroMagnitude>,
) -> Rc<IntegerIntervalBound> {
    Rc::new(IntegerIntervalBound::IntegerConcreteBound {
        sign: sign.clone(),
        magnitude: magnitude.clone(),
    })
}

pub fn integer_pointer_width_bound(
    signedness: Signedness,
    endpoint: IntegerRangeEndpoint,
) -> Rc<IntegerIntervalBound> {
    Rc::new(IntegerIntervalBound::IntegerPointerWidthBound {
        signedness: signedness.clone(),
        endpoint: endpoint.clone(),
    })
}

pub fn int8_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Signed, 8)
}

pub fn int16_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Signed, 16)
}

pub fn int32_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Signed, 32)
}

pub fn int64_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Signed, 64)
}

pub fn int128_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Signed, 128)
}

pub fn uint8_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Unsigned, 8)
}

pub fn uint16_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Unsigned, 16)
}

pub fn uint32_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Unsigned, 32)
}

pub fn uint64_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Unsigned, 64)
}

pub fn uint128_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_machine_interval_spec(Signedness::Unsigned, 128)
}

pub fn int_platform_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_interval(
        integer_pointer_width_bound(
            Signedness::Signed,
            IntegerRangeEndpoint::IntegerRangeInclusiveMin,
        ),
        integer_pointer_width_bound(
            Signedness::Signed,
            IntegerRangeEndpoint::IntegerRangeInclusiveMax,
        ),
    )
}

pub fn uint_platform_interval_spec() -> Rc<IntegerIntervalSpec> {
    integer_interval(
        integer_pointer_width_bound(
            Signedness::Unsigned,
            IntegerRangeEndpoint::IntegerRangeInclusiveMin,
        ),
        integer_pointer_width_bound(
            Signedness::Unsigned,
            IntegerRangeEndpoint::IntegerRangeInclusiveMax,
        ),
    )
}

pub fn integer_inhabitant_atom(id: String) -> Rc<Node> {
    Rc::new(Node {
        kind: Rc::new(NodeKind::TypeNode {
            connective: Rc::new(Connective::Atom {
                identity: id.clone(),
            }),
        }),
        children: Rc::new(vec![]),
        occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
    })
}

pub fn integer_named_edge(name: String, target: Rc<Node>) -> Rc<Edge> {
    Rc::new(Edge {
        label: Rc::new(EdgeLabel::Named { name: name.clone() }),
        target: target.clone(),
    })
}

pub fn integer_bound_sign_node(sign: IntegerBoundSign) -> Rc<Node> {
    match sign.clone() {
        IntegerBoundSign::IntegerBoundNegative => {
            integer_inhabitant_atom("integer_tag_bound_negative".to_string())
        }
        IntegerBoundSign::IntegerBoundNonNegative => {
            integer_inhabitant_atom("integer_tag_bound_nonnegative".to_string())
        }
    }
}

pub fn integer_signedness_node(signedness: Signedness) -> Rc<Node> {
    match signedness.clone() {
        Signedness::Signed => integer_inhabitant_atom("integer_tag_signed".to_string()),
        Signedness::Unsigned => integer_inhabitant_atom("integer_tag_unsigned".to_string()),
    }
}

pub fn integer_range_endpoint_node(endpoint: IntegerRangeEndpoint) -> Rc<Node> {
    match endpoint.clone() {
        IntegerRangeEndpoint::IntegerRangeInclusiveMin => {
            integer_inhabitant_atom("integer_tag_range_inclusive_min".to_string())
        }
        IntegerRangeEndpoint::IntegerRangeInclusiveMax => {
            integer_inhabitant_atom("integer_tag_range_inclusive_max".to_string())
        }
    }
}

pub fn integer_decimal_digit_node(digit: DecimalDigit) -> Rc<Node> {
    match digit.clone() {
        DecimalDigit::D0 => integer_inhabitant_atom("integer_tag_digit_0".to_string()),
        DecimalDigit::D1 => integer_inhabitant_atom("integer_tag_digit_1".to_string()),
        DecimalDigit::D2 => integer_inhabitant_atom("integer_tag_digit_2".to_string()),
        DecimalDigit::D3 => integer_inhabitant_atom("integer_tag_digit_3".to_string()),
        DecimalDigit::D4 => integer_inhabitant_atom("integer_tag_digit_4".to_string()),
        DecimalDigit::D5 => integer_inhabitant_atom("integer_tag_digit_5".to_string()),
        DecimalDigit::D6 => integer_inhabitant_atom("integer_tag_digit_6".to_string()),
        DecimalDigit::D7 => integer_inhabitant_atom("integer_tag_digit_7".to_string()),
        DecimalDigit::D8 => integer_inhabitant_atom("integer_tag_digit_8".to_string()),
        DecimalDigit::D9 => integer_inhabitant_atom("integer_tag_digit_9".to_string()),
    }
}

pub fn integer_nonzero_decimal_digit_widen(digit: NonZeroDecimalDigit) -> DecimalDigit {
    match digit.clone() {
        NonZeroDecimalDigit::NonZeroD1 => DecimalDigit::D1,
        NonZeroDecimalDigit::NonZeroD2 => DecimalDigit::D2,
        NonZeroDecimalDigit::NonZeroD3 => DecimalDigit::D3,
        NonZeroDecimalDigit::NonZeroD4 => DecimalDigit::D4,
        NonZeroDecimalDigit::NonZeroD5 => DecimalDigit::D5,
        NonZeroDecimalDigit::NonZeroD6 => DecimalDigit::D6,
        NonZeroDecimalDigit::NonZeroD7 => DecimalDigit::D7,
        NonZeroDecimalDigit::NonZeroD8 => DecimalDigit::D8,
        NonZeroDecimalDigit::NonZeroD9 => DecimalDigit::D9,
    }
}

pub fn integer_decimal_nonzero_node(magnitude: Rc<DecimalNonZeroMagnitude>) -> Rc<Node> {
    match magnitude.clone() {
        DecimalNonZeroMagnitude { lead, tail, .. } => fold_list_node(
            Rc::new({
                let mut __cons_v = (*tail.clone()).clone();
                __cons_v.insert(0, integer_nonzero_decimal_digit_widen(lead.clone()));
                __cons_v
            }),
            |digit| integer_decimal_digit_node(digit.clone()),
        ),
    }
}

pub fn integer_decimal_magnitude_node(magnitude: Rc<DecimalMagnitude>) -> Rc<Node> {
    match (*magnitude.clone()).clone() {
        DecimalMagnitude::DecimalZero => fold_list_node(
            Rc::new({
                let mut __cons_v = (*Rc::new(vec![])).clone();
                __cons_v.insert(0, DecimalDigit::D0);
                __cons_v
            }),
            |digit| integer_decimal_digit_node(digit.clone()),
        ),
        DecimalMagnitude::DecimalNonZero {
            magnitude: magnitude,
            ..
        } => integer_decimal_nonzero_node(magnitude.clone()),
    }
}

pub fn integer_interval_bound_node(bound: Rc<IntegerIntervalBound>) -> Rc<Node> {
    match (*bound.clone()).clone() {
        IntegerIntervalBound::IntegerConcreteZero => Rc::new(Node {
            kind: Rc::new(NodeKind::TypeNode {
                connective: Rc::new(Connective::Conj),
            }),
            children: Rc::new(vec![integer_named_edge(
                "integer_bound_field_kind".to_string(),
                integer_inhabitant_atom("integer_tag_bound_zero".to_string()),
            )]),
            occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
        }),
        IntegerIntervalBound::IntegerConcreteBound {
            sign, magnitude, ..
        } => Rc::new(Node {
            kind: Rc::new(NodeKind::TypeNode {
                connective: Rc::new(Connective::Conj),
            }),
            children: Rc::new(vec![
                integer_named_edge(
                    "integer_bound_field_kind".to_string(),
                    integer_inhabitant_atom("integer_tag_bound_concrete".to_string()),
                ),
                integer_named_edge(
                    "integer_bound_field_sign".to_string(),
                    integer_bound_sign_node(sign.clone()),
                ),
                integer_named_edge(
                    "integer_bound_field_magnitude".to_string(),
                    integer_decimal_nonzero_node(magnitude.clone()),
                ),
            ]),
            occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
        }),
        IntegerIntervalBound::IntegerPointerWidthBound {
            signedness,
            endpoint,
            ..
        } => Rc::new(Node {
            kind: Rc::new(NodeKind::TypeNode {
                connective: Rc::new(Connective::Conj),
            }),
            children: Rc::new(vec![
                integer_named_edge(
                    "integer_bound_field_kind".to_string(),
                    integer_inhabitant_atom("integer_tag_bound_pointer_width".to_string()),
                ),
                integer_named_edge(
                    "integer_bound_field_signedness".to_string(),
                    integer_signedness_node(signedness.clone()),
                ),
                integer_named_edge(
                    "integer_bound_field_endpoint".to_string(),
                    integer_range_endpoint_node(endpoint.clone()),
                ),
            ]),
            occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
        }),
    }
}

pub fn integer_interval_spec_node(spec: Rc<IntegerIntervalSpec>) -> Rc<Node> {
    Rc::new(Node {
        kind: Rc::new(NodeKind::TypeNode {
            connective: Rc::new(Connective::Conj),
        }),
        children: Rc::new(vec![
            integer_named_edge(
                "integer_interval_field_inclusive_min".to_string(),
                integer_interval_bound_node(spec.inclusive_min.clone()),
            ),
            integer_named_edge(
                "integer_interval_field_inclusive_max".to_string(),
                integer_interval_bound_node(spec.inclusive_max.clone()),
            ),
        ]),
        occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
    })
}

pub fn int_add(a: Int, b: Int) -> Int {
    (a.clone() + b.clone())
}

pub fn int_negate(a: Int) -> Int {
    (0 - a.clone())
}

pub fn int_mul(a: Int, b: Int) -> Int {
    (a.clone() * b.clone())
}

pub fn int_compare(a: Int, b: Int) -> Ordering {
    if (a.clone() < b.clone()) {
        Ordering::Less
    } else {
        if (a.clone() == b.clone()) {
            Ordering::Equal
        } else {
            Ordering::Greater
        }
    }
}

pub fn int_is_zero(a: Int) -> bool {
    (a.clone() == 0)
}

pub fn int_sub(a: Int, b: Int) -> Int {
    int_add(a.clone(), int_negate(b.clone()))
}

pub fn int_eq(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Equal => true,
        _ => false,
    }
}

pub fn int_ne(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Equal => false,
        _ => true,
    }
}

pub fn int_lt(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Less => true,
        _ => false,
    }
}

pub fn int_le(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Less => true,
        Ordering::Equal => true,
        Ordering::Greater => false,
    }
}

pub fn int_gt(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Greater => true,
        _ => false,
    }
}

pub fn int_ge(a: Int, b: Int) -> bool {
    match int_compare(a.clone(), b.clone()) {
        Ordering::Greater => true,
        Ordering::Equal => true,
        Ordering::Less => false,
    }
}

pub fn int_div_result(a: Int, b: Int) -> Rc<Result<i64, DivError>> {
    if int_is_zero(b.clone()) {
        Rc::new(Result::Err {
            value: DivError::DivideByZero,
        })
    } else {
        Rc::new(Result::Ok {
            value: (a.clone() / b.clone()),
        })
    }
}

pub fn int_abelian_group() -> Rc<AbelianGroup<i64>> {
    thread_local! {
            static CACHED: Rc<AbelianGroup<i64>> = {
                Rc::new(AbelianGroup {
        op: int_add,
        identity: 0,
        inverse: int_negate,
    })
            };
        }
    CACHED.with(|c: &Rc<AbelianGroup<i64>>| c.clone())
}

pub fn int_ordered_ring() -> Rc<OrderedRing<i64>> {
    thread_local! {
            static CACHED: Rc<OrderedRing<i64>> = {
                Rc::new(OrderedRing {
        add: int_add,
        sub: int_sub,
        zero: 0,
        negate: int_negate,
        mul: int_mul,
        div: int_div_result,
        one: 1,
        compare: int_compare,
        eq: int_eq,
        ne: int_ne,
        lt: int_lt,
        le: int_le,
        gt: int_gt,
        ge: int_ge,
    })
            };
        }
    CACHED.with(|c: &Rc<OrderedRing<i64>>| c.clone())
}

pub fn integer_divide_by_zero_diagnostic(at: Rc<Locus>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "integer_div_by_zero".to_string(),
        at: at.clone(),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::AmbiguousIntent,
        }),
    })
}

pub fn integer_modulo_by_zero_diagnostic(at: Rc<Locus>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "integer_modulo_by_zero".to_string(),
        at: at.clone(),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::AmbiguousIntent,
        }),
    })
}

pub fn int_div(a: Int, b: Int, at: Rc<Locus>) -> Rc<Outcome<i64>> {
    if int_is_zero(b.clone()) {
        Rc::new(Outcome::Rejected {
            diagnostics: diagnostics_singleton(integer_divide_by_zero_diagnostic(at.clone())),
        })
    } else {
        Rc::new(Outcome::Accepted {
            value: (a.clone() / b.clone()),
            diagnostics: None,
        })
    }
}

pub fn int_mod(a: Int, b: Int, at: Rc<Locus>) -> Rc<Outcome<i64>> {
    if int_is_zero(b.clone()) {
        Rc::new(Outcome::Rejected {
            diagnostics: diagnostics_singleton(integer_modulo_by_zero_diagnostic(at.clone())),
        })
    } else {
        Rc::new(Outcome::Accepted {
            value: (a.clone() % b.clone()),
            diagnostics: None,
        })
    }
}

pub fn integer_char_code_to_decimal_digit_optional(c: i64) -> Option<DecimalDigit> {
    if (c.clone() == 48) {
        optional_present(DecimalDigit::D0)
    } else {
        if (c.clone() == 49) {
            optional_present(DecimalDigit::D1)
        } else {
            if (c.clone() == 50) {
                optional_present(DecimalDigit::D2)
            } else {
                if (c.clone() == 51) {
                    optional_present(DecimalDigit::D3)
                } else {
                    if (c.clone() == 52) {
                        optional_present(DecimalDigit::D4)
                    } else {
                        if (c.clone() == 53) {
                            optional_present(DecimalDigit::D5)
                        } else {
                            if (c.clone() == 54) {
                                optional_present(DecimalDigit::D6)
                            } else {
                                if (c.clone() == 55) {
                                    optional_present(DecimalDigit::D7)
                                } else {
                                    if (c.clone() == 56) {
                                        optional_present(DecimalDigit::D8)
                                    } else {
                                        if (c.clone() == 57) {
                                            optional_present(DecimalDigit::D9)
                                        } else {
                                            optional_absent()
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

pub fn integer_decimal_digits_snoc(
    xs: Rc<Vec<DecimalDigit>>,
    digit: DecimalDigit,
) -> Rc<Vec<DecimalDigit>> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || match xs.clone() {
        FreeMonoid::Empty => Rc::new({
            let mut __cons_v = (*Rc::new(vec![])).clone();
            __cons_v.insert(0, digit.clone());
            __cons_v
        }),
        FreeMonoid::Cons { head, tail, .. } => Rc::new({
            let mut __cons_v = (*integer_decimal_digits_snoc(tail.clone(), digit.clone())).clone();
            __cons_v.insert(0, head.clone());
            __cons_v
        }),
    })
}

pub fn integer_string_to_decimal_digits_step(
    mut s: String,
    mut acc: Rc<Vec<DecimalDigit>>,
) -> Option<Rc<FreeMonoid>> {
    loop {
        match (*crate::v2_std_text::string_head(s.clone())).clone() {
            CharResult::CharAbsent => {
                break optional_present(acc.clone());
            }
            CharResult::CharFound { value: c, .. } => {
                match integer_char_code_to_decimal_digit_optional(c.clone()) {
                    Some(d) => match (*crate::v2_std_text::string_tail(s.clone())).clone() {
                        ListTailResult::TailFound { tail: rest, .. } => {
                            let __tco_0 = rest.clone();
                            let __tco_1 = integer_decimal_digits_snoc(acc, d.clone());
                            s = __tco_0;
                            acc = __tco_1;
                            continue;
                        }
                        ListTailResult::TailAbsent => {
                            break optional_absent();
                        }
                    },
                    None => {
                        break optional_absent();
                    }
                }
            }
        }
    }
}

pub fn integer_string_to_decimal_digits_optional(s: String) -> Option<Rc<FreeMonoid>> {
    integer_string_to_decimal_digits_step(s.clone(), Rc::new(vec![]))
}

pub fn integer_decimal_nonzero_magnitude_to_int(m: Rc<DecimalNonZeroMagnitude>) -> Int {
    fold_list(
        m.tail.clone(),
        decimal_digit_value(integer_nonzero_decimal_digit_widen(m.lead.clone())),
        |acc, d| int_add(int_mul(acc.clone(), 10), decimal_digit_value(d.clone())),
    )
}

pub fn integer_decimal_magnitude_to_int(m: Rc<DecimalMagnitude>) -> Int {
    match (*m.clone()).clone() {
        DecimalMagnitude::DecimalZero => 0,
        DecimalMagnitude::DecimalNonZero {
            magnitude: magnitude,
            ..
        } => integer_decimal_nonzero_magnitude_to_int(magnitude.clone()),
    }
}

pub fn integer_lexeme_to_int_optional(lexeme: String) -> Option<i64> {
    match integer_string_to_decimal_digits_optional(lexeme.clone()) {
        Some(digits) => optional_present(integer_decimal_magnitude_to_int(
            decimal_digits_to_magnitude(digits.clone()),
        )),
        None => optional_absent(),
    }
}

pub fn integer_u8_unit_to_bit(value: Int, shift: Int) -> bool {
    (((value.clone() / shift.clone()) % 2) == 1)
}

pub fn integer_u8_to_byte(value: Int) -> Rc<Byte> {
    Rc::new(Byte {
        bits: Rc::new(vec![
            integer_u8_unit_to_bit(value.clone(), 1),
            integer_u8_unit_to_bit(value.clone(), 2),
            integer_u8_unit_to_bit(value.clone(), 4),
            integer_u8_unit_to_bit(value.clone(), 8),
            integer_u8_unit_to_bit(value.clone(), 16),
            integer_u8_unit_to_bit(value.clone(), 32),
            integer_u8_unit_to_bit(value.clone(), 64),
            integer_u8_unit_to_bit(value.clone(), 128),
        ]),
    })
}

pub fn integer_i32_modulus() -> Int {
    int_mul(65536, 65536)
}

pub fn integer_int_normalize_i32_wire_unsigned(mut value: Int) -> Int {
    loop {
        if (value.clone() < 0) {
            {
                let __tco_0 = int_add(value, integer_i32_modulus());
                value = __tco_0;
                continue;
            }
        } else {
            if (value.clone() >= integer_i32_modulus()) {
                {
                    let __tco_0 = int_add(value, int_negate(integer_i32_modulus()));
                    value = __tco_0;
                    continue;
                }
            } else {
                break value.clone();
            }
        }
    }
}

pub fn integer_int_to_signed_i32_le_bytes(value: Int) -> Rc<Vec<Rc<Byte>>> {
    {
        let wire = integer_int_normalize_i32_wire_unsigned(value.clone());
        Rc::new(vec![
            integer_u8_to_byte((wire.clone() % 256)),
            integer_u8_to_byte(((wire.clone() / 256) % 256)),
            integer_u8_to_byte(((wire.clone() / 65536) % 256)),
            integer_u8_to_byte(((wire.clone() / 16777216) % 256)),
        ])
    }
}

pub fn integer_byte_bit_at(b: Rc<Byte>, shift: Int) -> Int {
    match list_at_optional(b.bits.clone(), shift.clone()) {
        Some(bit) => {
            if bit.clone() {
                1
            } else {
                0
            }
        }
        None => 0,
    }
}

pub fn integer_u8_from_byte(b: Rc<Byte>) -> Int {
    int_add(
        int_add(
            int_add(
                int_add(
                    int_add(
                        int_add(
                            int_add(
                                integer_byte_bit_at(b.clone(), 0),
                                int_mul(2, integer_byte_bit_at(b.clone(), 1)),
                            ),
                            int_mul(4, integer_byte_bit_at(b.clone(), 2)),
                        ),
                        int_mul(8, integer_byte_bit_at(b.clone(), 3)),
                    ),
                    int_mul(16, integer_byte_bit_at(b.clone(), 4)),
                ),
                int_mul(32, integer_byte_bit_at(b.clone(), 5)),
            ),
            int_mul(64, integer_byte_bit_at(b.clone(), 6)),
        ),
        int_mul(128, integer_byte_bit_at(b.clone(), 7)),
    )
}

pub fn integer_signed_i32_le_bytes_to_int_value(b0: Int, b1: Int, b2: Int, b3: Int) -> Int {
    {
        let unsigned = int_add(
            int_add(
                int_add(b0.clone(), int_mul(256, b1.clone())),
                int_mul(65536, b2.clone()),
            ),
            int_mul(16777216, b3.clone()),
        );
        if (b3.clone() >= 128) {
            int_add(unsigned.clone(), int_negate(integer_i32_modulus()))
        } else {
            unsigned.clone()
        }
    }
}

pub fn integer_decimal_digit_from_tag_atom(id: String) -> Option<DecimalDigit> {
    if (id.clone() == "integer_tag_digit_0".to_string()) {
        optional_present(DecimalDigit::D0)
    } else {
        if (id.clone() == "integer_tag_digit_1".to_string()) {
            optional_present(DecimalDigit::D1)
        } else {
            if (id.clone() == "integer_tag_digit_2".to_string()) {
                optional_present(DecimalDigit::D2)
            } else {
                if (id.clone() == "integer_tag_digit_3".to_string()) {
                    optional_present(DecimalDigit::D3)
                } else {
                    if (id.clone() == "integer_tag_digit_4".to_string()) {
                        optional_present(DecimalDigit::D4)
                    } else {
                        if (id.clone() == "integer_tag_digit_5".to_string()) {
                            optional_present(DecimalDigit::D5)
                        } else {
                            if (id.clone() == "integer_tag_digit_6".to_string()) {
                                optional_present(DecimalDigit::D6)
                            } else {
                                if (id.clone() == "integer_tag_digit_7".to_string()) {
                                    optional_present(DecimalDigit::D7)
                                } else {
                                    if (id.clone() == "integer_tag_digit_8".to_string()) {
                                        optional_present(DecimalDigit::D8)
                                    } else {
                                        if (id.clone() == "integer_tag_digit_9".to_string()) {
                                            optional_present(DecimalDigit::D9)
                                        } else {
                                            optional_absent()
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

pub fn integer_decimal_digit_from_node_optional(node: Rc<Node>) -> Option<DecimalDigit> {
    match (*node.kind.clone()).clone() {
        NodeKind::TypeNode { ref connective, .. }
            if matches!(connective.as_ref(), Connective::Atom { .. }) =>
        {
            let Connective::Atom { identity: id, .. } = connective.as_ref() else {
                unreachable!()
            };
            integer_decimal_digit_from_tag_atom(id.clone())
        }
        _ => optional_absent(),
    }
}

pub fn integer_fold_list_node_digits_optional(node: Rc<Node>) -> Option<Rc<FreeMonoid>> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if is_empty_conj_root(node.clone()) {
            optional_present(Rc::new(vec![]))
        } else {
            match (*find_named_child(node.clone(), "fold_list_node_head".to_string())).clone() {
                Outcome::Accepted {
                    value: head_node, ..
                } => match integer_decimal_digit_from_node_optional(head_node.clone()) {
                    Some(digit) => {
                        match (*find_named_child(node.clone(), "fold_list_node_tail".to_string()))
                            .clone()
                        {
                            Outcome::Accepted {
                                value: tail_node, ..
                            } => match integer_fold_list_node_digits_optional(tail_node.clone()) {
                                Some(tail_digits) => optional_present(Rc::new({
                                    let mut __cons_v = (*tail_digits.clone()).clone();
                                    __cons_v.insert(0, digit.clone());
                                    __cons_v
                                })),
                                None => optional_absent(),
                            },
                            Outcome::Rejected { diagnostics: _, .. } => optional_absent(),
                        }
                    }
                    None => optional_absent(),
                },
                Outcome::Rejected { diagnostics: _, .. } => optional_absent(),
            }
        }
    })
}

pub fn integer_decimal_magnitude_node_to_int(node: Rc<Node>) -> Rc<Outcome<i64>> {
    match integer_fold_list_node_digits_optional(node.clone()) {
        Some(digits) => outcome_accepted(integer_decimal_magnitude_to_int(
            decimal_digits_to_magnitude(digits.clone()),
        )),
        None => outcome_rejected(integer_standard_integer_type_decode_invalid_diagnostic(
            node.clone(),
        )),
    }
}

pub fn integer_signed_i32_le_bytes_to_int(bytes: Rc<Vec<Rc<Byte>>>) -> Rc<Outcome<i64>> {
    if ((bytes.clone().len() as i64) == 4) {
        match list_at_optional(bytes.clone(), 0) {
            Some(b0) => match list_at_optional(bytes.clone(), 1) {
                Some(b1) => match list_at_optional(bytes.clone(), 2) {
                    Some(b2) => match list_at_optional(bytes.clone(), 3) {
                        Some(b3) => outcome_accepted(integer_signed_i32_le_bytes_to_int_value(
                            integer_u8_from_byte(b0.clone()),
                            integer_u8_from_byte(b1.clone()),
                            integer_u8_from_byte(b2.clone()),
                            integer_u8_from_byte(b3.clone()),
                        )),
                        None => outcome_rejected(integer_signed_i32_le_decode_invalid_diagnostic()),
                    },
                    None => outcome_rejected(integer_signed_i32_le_decode_invalid_diagnostic()),
                },
                None => outcome_rejected(integer_signed_i32_le_decode_invalid_diagnostic()),
            },
            None => outcome_rejected(integer_signed_i32_le_decode_invalid_diagnostic()),
        }
    } else {
        outcome_rejected(integer_signed_i32_le_decode_invalid_diagnostic())
    }
}

pub fn integer_nat_decimal_digit_char(digit: Rc<Nat>) -> String {
    if (digit.clone() == 0) {
        "0".to_string()
    } else {
        if (digit.clone() == 1) {
            "1".to_string()
        } else {
            if (digit.clone() == 2) {
                "2".to_string()
            } else {
                if (digit.clone() == 3) {
                    "3".to_string()
                } else {
                    if (digit.clone() == 4) {
                        "4".to_string()
                    } else {
                        if (digit.clone() == 5) {
                            "5".to_string()
                        } else {
                            if (digit.clone() == 6) {
                                "6".to_string()
                            } else {
                                if (digit.clone() == 7) {
                                    "7".to_string()
                                } else {
                                    if (digit.clone() == 8) {
                                        "8".to_string()
                                    } else {
                                        "9".to_string()
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

pub fn integer_nat_to_decimal_string(value: Rc<Nat>) -> String {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        if (value.clone() == 0) {
            "0".to_string()
        } else {
            if (value.clone() < 10) {
                integer_nat_decimal_digit_char(value.clone())
            } else {
                {
                    let rest = (value.clone() / 10);
                    let digit = (value.clone() - (rest.clone() * 10));
                    v1_rt::concat(
                        integer_nat_to_decimal_string(rest.clone()),
                        integer_nat_decimal_digit_char(digit.clone()),
                    )
                }
            }
        }
    })
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct TwosComplement;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct OnesComplement;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct SignMagnitude;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Wrapping;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Saturating;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct UndefinedBehavior;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Trapping;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerBoundNegative;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerBoundNonNegative;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerRangeInclusiveMin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerRangeInclusiveMax;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D0;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D1;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D2;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D3;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D4;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D5;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D6;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D7;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D8;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct D9;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD1;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD2;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD3;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD4;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD5;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD6;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD7;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD8;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NonZeroD9;
