// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.parser_declaration_matrix_support

use self::PdcTransport::*;
use crate::std_occurrence_identity::OccurrenceCategory::{
    CallableOccurrence, ConstructorOccurrence, FieldOccurrence, LexicalValueOccurrence,
    MethodOccurrence, NamespaceSegmentOccurrence, TypeOccurrence,
};
pub use crate::std_occurrence_identity::{
    DeclarationOccurrence, OccurrenceCategory, OccurrenceId, OccurrenceIndexEntry,
    OccurrenceTransport, ReferenceOccurrence,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
pub use crate::v1_gunbc_occurrence_binding_parser_walk::parse_authored_occurrence_binding_source;
pub use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource;
use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource::{
    ParsedOccurrenceBindingSourceReady, ParsedOccurrenceBindingSourceRefused,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn pdc_support_authority_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Single support authority for the parser declaration-category matrix, imported by both the enrolled green witness and its expected-red sibling. Owns the fixture source, the transport parse, the category projection, and the exact-identity rendering (authored name, category, byte span, containment chain). Declares no test fn and asserts nothing: the requirements live in the witness files, the instrument lives here once.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_matrix_source() -> String {
    (((((((((((((((((((((((((("module app.pdc_matrix\n".to_string()
        + &"\n".to_string())
        + &"type PdcProduct {\n".to_string())
        + &"  pdc_product_field: Int\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"type PdcSum = PdcNullary | PdcPayload { pdc_payload_field: Int }\n"
            .to_string())
        + &"\n".to_string())
        + &"data pdc_annotated_datum: Int = 7\n".to_string())
        + &"\n".to_string())
        + &"fn pdc_function(pdc_parameter: Int) -> Int {\n".to_string())
        + &"  let pdc_let_binder = pdc_parameter\n".to_string())
        + &"  pdc_let_binder\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"fn pdc_matcher(pdc_scrutinee: PdcSum) -> Int {\n".to_string())
        + &"  match pdc_scrutinee {\n".to_string())
        + &"    PdcNullary => {\n".to_string())
        + &"      0\n".to_string())
        + &"    }\n".to_string())
        + &"    PdcPayload { pdc_payload_field: pdc_pattern_binder } => {\n".to_string())
        + &"      pdc_pattern_binder\n".to_string())
        + &"    }\n".to_string())
        + &"  }\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"type PdcAlias = PdcProduct\n".to_string())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum PdcTransport {
    PdcTransportReady { transport: Rc<OccurrenceTransport> },
    PdcTransportRefused,
}
impl PdcTransport {
    pub fn transport(&self) -> Rc<OccurrenceTransport> {
        match self {
            PdcTransport::PdcTransportReady {
                transport: __val, ..
            } => __val.clone(),
            PdcTransport::PdcTransportRefused => panic!("no transport on unit variant"),
        }
    }
}

pub fn pdc_parse_matrix() -> Rc<PdcTransport> {
    match (*parse_authored_occurrence_binding_source(
        "app/pdc_matrix.dag".to_string(),
        pdc_matrix_source(),
    ))
    .clone()
    {
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceRefused => {
            Rc::new(PdcTransport::PdcTransportRefused)
        }
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceReady { transport, .. } => {
            Rc::new(PdcTransport::PdcTransportReady {
                transport: transport.clone(),
            })
        }
    }
}

pub fn pdc_category_tag(category: OccurrenceCategory) -> String {
    match category.clone() {
        OccurrenceCategory::LexicalValueOccurrence => "LexicalValueOccurrence".to_string(),
        OccurrenceCategory::TypeOccurrence => "TypeOccurrence".to_string(),
        OccurrenceCategory::CallableOccurrence => "CallableOccurrence".to_string(),
        OccurrenceCategory::ConstructorOccurrence => "ConstructorOccurrence".to_string(),
        OccurrenceCategory::NamespaceSegmentOccurrence => "NamespaceSegmentOccurrence".to_string(),
        OccurrenceCategory::FieldOccurrence => "FieldOccurrence".to_string(),
        OccurrenceCategory::MethodOccurrence => "MethodOccurrence".to_string(),
    }
}

pub fn pdc_index_entry_for_declaration(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == declaration.occurrence.clone().value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn pdc_declared_tags_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, declaration: Rc<DeclarationOccurrence>| {
            match pdc_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![pdc_category_tag(declaration.category.clone())]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdc_render_tags(tags: Rc<Vec<String>>) -> String {
    tags.clone()
        .iter()
        .cloned()
        .fold("".to_string(), |acc: String, tag: String| {
            ((acc + &"|".to_string()) + &tag.clone())
        })
}

pub fn pdc_containment_chain(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> String {
    declaration
        .containment
        .clone()
        .ancestors
        .clone()
        .iter()
        .cloned()
        .fold("".to_string(), |chain: String, ancestor: OccurrenceId| {
            ((chain + &">".to_string())
                + &pdc_named_or_anon(transport.clone(), ancestor.value.clone()))
        })
}

pub fn pdc_named_or_anon(transport: Rc<OccurrenceTransport>, occurrence_value: i64) -> String {
    {
        let found = pdc_name_for_occurrence(transport.clone(), occurrence_value.clone());
        match (found.clone() == "".to_string()) {
            true => "<anon>".to_string(),
            false => found.clone(),
        }
    }
}

pub fn pdc_name_for_occurrence(
    transport: Rc<OccurrenceTransport>,
    occurrence_value: i64,
) -> String {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            "<unknown>".to_string(),
            |found: String, entry: Rc<OccurrenceIndexEntry>| match (found.clone()
                == "<unknown>".to_string())
            {
                false => found.clone(),
                true => match (entry.projection.clone().occurrence.clone().value.clone()
                    == occurrence_value.clone())
                {
                    true => entry.projection.clone().authored_name.clone(),
                    false => found.clone(),
                },
            },
        )
}

pub fn pdc_render_occurrence(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
    entry: Rc<OccurrenceIndexEntry>,
) -> String {
    ((((((((entry.projection.clone().authored_name.clone() + &"#".to_string())
        + &pdc_category_tag(declaration.category.clone()))
        + &"#span:".to_string())
        + &(entry
            .projection
            .clone()
            .diagnostic_span
            .clone()
            .start
            .clone())
        .to_string())
        + &"-".to_string())
        + &(entry.projection.clone().diagnostic_span.clone().end.clone()).to_string())
        + &"#path:".to_string())
        + &pdc_containment_chain(transport.clone(), declaration.clone()))
}

pub fn pdc_identity_rows_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, declaration: Rc<DeclarationOccurrence>| {
            match pdc_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![pdc_render_occurrence(
                                transport.clone(),
                                declaration.clone(),
                                entry.clone(),
                            )]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdc_identity_population(transport: Rc<OccurrenceTransport>, name: String) -> String {
    pdc_render_tags(pdc_identity_rows_for_name(transport.clone(), name.clone()))
}

pub fn pdc_declares_exactly(
    transport: Rc<OccurrenceTransport>,
    name: String,
    expected: String,
) -> bool {
    (pdc_render_tags(pdc_declared_tags_for_name(transport.clone(), name.clone()))
        == expected.clone())
}

pub fn pdc_reference_rows_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.references.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, reference: Rc<ReferenceOccurrence>| {
            match pdc_index_entry_for_occurrence(
                transport.clone(),
                reference.occurrence.clone().value.clone(),
            ) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![
                                ((((((((entry.projection.clone().authored_name.clone()
                                    + &"#".to_string())
                                    + &pdc_category_tag(reference.category.clone()))
                                    + &"#span:".to_string())
                                    + &(entry
                                        .projection
                                        .clone()
                                        .diagnostic_span
                                        .clone()
                                        .start
                                        .clone())
                                    .to_string())
                                    + &"-".to_string())
                                    + &(entry
                                        .projection
                                        .clone()
                                        .diagnostic_span
                                        .clone()
                                        .end
                                        .clone())
                                    .to_string())
                                    + &"#path:".to_string())
                                    + &pdc_reference_chain(transport.clone(), reference.clone())),
                            ]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdc_reference_chain(
    transport: Rc<OccurrenceTransport>,
    reference: Rc<ReferenceOccurrence>,
) -> String {
    reference
        .containment
        .clone()
        .ancestors
        .clone()
        .iter()
        .cloned()
        .fold("".to_string(), |chain: String, ancestor: OccurrenceId| {
            ((chain + &">".to_string())
                + &pdc_named_or_anon(transport.clone(), ancestor.value.clone()))
        })
}

pub fn pdc_index_entry_for_occurrence(
    transport: Rc<OccurrenceTransport>,
    occurrence_value: i64,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == occurrence_value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn pdc_reference_population(transport: Rc<OccurrenceTransport>, name: String) -> String {
    pdc_render_tags(pdc_reference_rows_for_name(transport.clone(), name.clone()))
}

pub fn pdc_reference_row(name: String, expected: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => (pdc_reference_population(transport.clone(), name.clone()) == expected.clone()),
    }
}

pub fn pdc_declares_nothing(transport: Rc<OccurrenceTransport>, name: String) -> bool {
    ((pdc_declared_tags_for_name(transport.clone(), name.clone()).len() as i64) == 0)
}

pub fn pdc_row(name: String, expected: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declares_exactly(transport.clone(), name.clone(), expected.clone()),
    }
}

pub fn pdc_zero_row(name: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declares_nothing(transport.clone(), name.clone()),
    }
}

pub fn pdc_identity_row(name: String, expected: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => (pdc_identity_population(transport.clone(), name.clone()) == expected.clone()),
    }
}

pub fn pdc_non_type_item_source() -> String {
    (((((((((("module app.pdc_non_type\n".to_string() + &"\n".to_string())
        + &"data pdc_nt_datum: Int = 7\n".to_string())
        + &"\n".to_string())
        + &"fn pdc_nt_fn(pdc_nt_param: Int) -> Int {\n".to_string())
        + &"  pdc_nt_param\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"func pdc_nt_func(pdc_nt_func_param: Int) -> Int {\n".to_string())
        + &"  pdc_nt_func_param\n".to_string())
        + &"}\n".to_string())
}

pub fn pdc_parse_non_type_items() -> Rc<PdcTransport> {
    match (*parse_authored_occurrence_binding_source(
        "app/pdc_non_type.dag".to_string(),
        pdc_non_type_item_source(),
    ))
    .clone()
    {
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceRefused => {
            Rc::new(PdcTransport::PdcTransportRefused)
        }
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceReady { transport, .. } => {
            Rc::new(PdcTransport::PdcTransportReady {
                transport: transport.clone(),
            })
        }
    }
}

pub fn pdc_declared_category_count(transport: Rc<OccurrenceTransport>, tag: String) -> i64 {
    transport.declarations.clone().iter().cloned().fold(
        0,
        |acc: i64, declaration: Rc<DeclarationOccurrence>| match (pdc_category_tag(
            declaration.category.clone(),
        ) == tag.clone())
        {
            true => (acc.clone() + 1),
            false => acc.clone(),
        },
    )
}

pub fn pdc_matrix_category_count(tag: String) -> i64 {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => (0 - 1),
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declared_category_count(transport.clone(), tag.clone()),
    }
}

pub fn pdc_non_type_category_count(tag: String) -> i64 {
    match (*pdc_parse_non_type_items()).clone() {
        PdcTransport::PdcTransportRefused => (0 - 1),
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declared_category_count(transport.clone(), tag.clone()),
    }
}

pub fn pdc_declaration_count_row(name: String, expected: i64) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => {
            ((pdc_declared_tags_for_name(transport.clone(), name.clone()).len() as i64)
                == expected.clone())
        }
    }
}
