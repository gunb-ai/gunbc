// Generated by v1 compiler -- do not edit.
// Source module: std.content_hash

pub use crate::std_types::{ContentHash, NonEmptyStr};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::v2_std_witness::Witness;
use crate::v2_std_witness::{Holds, Violates};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn content_hash_atom(value: String) -> NonEmptyStr {
    v1_rt::atom_identity_hash(value.clone())
}

pub fn content_hash_combine(left: NonEmptyStr, right: NonEmptyStr) -> NonEmptyStr {
    v1_rt::hash_combine(left.clone(), right.clone())
}

pub fn content_hash_tagged(tag: String, payload: NonEmptyStr) -> NonEmptyStr {
    content_hash_combine(content_hash_atom(tag.clone()), payload.clone())
}
