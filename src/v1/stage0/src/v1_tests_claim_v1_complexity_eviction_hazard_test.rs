// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.v1_complexity_eviction_hazard_test

use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
use crate::v1_compiler_complexity::Certainty::Proven;
use crate::v1_compiler_complexity::CostExpr::CostConst;
pub use crate::v1_compiler_complexity::{
    cache_summary, empty_cost_intern_table, evict_summary, lookup_summary,
};
pub use crate::v1_compiler_complexity::{Certainty, ComplexitySummary, CostExpr, CostInternTable};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::sync::Arc as Rc;

pub fn eviction_hazard_home_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Homed under src/v1/tests/claim/ because it IMPORTS v1.compiler.complexity to execute the seed, and that is the only home with an EXECUTING consumer for a v1-importing witness: gunbc.ci_layer_roots v1_claim_scoped_witness_entries runs this directory on the scoped batch whose source roots are exactly dag plus src/v1. Two earlier homes were tried and both were wrong, each refuted by the floor rather than by argument. dag/test/claim/long/ failed compile-clean at the import line (unresolved import: module v1.compiler.complexity not found) — a dag/ entry cannot import v1 under compile-clean scope, though it CAN reach v1 declarations through decl_facts pool roots, which is how the sibling resolution census works. src/v2/test/manual/ compiled, but the witness-admission wall refused it: an OfflineLocalRecipe directory stopped being an admission answer on 2026-08-04, because a directory cannot decide that a witness runs, and moving a file under an offline path executes it NOWHERE. That refusal is the same coverage-by-illusion this census argues against, caught in the census itself.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn eviction_executing_control_note() -> String {
    thread_local! {
        static CACHED: String = {
            "CENSUS HAZARD, proven by EXECUTION rather than by matching source text (operator review 2026-08-05). The previous control asserted only that the source contained 'fn evict_summary' and a cache_summary call prefix — a safe implementation could keep both and the control would stay green, so it established nothing about the defect. This runs the seed: cache a summary whose work is nonzero, evict it, look it up again. The correct behavior is Absent — the entry was evicted, so it is NOT COMPUTED. The seed instead returns a Present summary whose work is zero and whose certainty is Proven, so a consumer reads 'costs nothing, proven' for a function whose cost was never derived. The assertions below pin that defect as it exists today; when the migrated form removes the entry or refuses, this witness REDS, which is the correct direction because the row exists to stop this realization carrying across.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn eviction_probe_table() -> Rc<CostInternTable> {
    cache_summary(
        empty_cost_intern_table(),
        "probe_fn".to_string(),
        Rc::new(ComplexitySummary {
            work: Rc::new(CostExpr::CostConst { value: 42 }),
            span: Rc::new(CostExpr::CostConst { value: 7 }),
            output_size: v1_rt::rc_empty_map::<String, Rc<CostExpr>>(),
            certainty: Certainty::Proven,
            peak_space: None,
        }),
    )
}

pub fn eviction_probe_caches_a_nonzero_summary() -> bool {
    match lookup_summary(eviction_probe_table(), "probe_fn".to_string()) {
        None => false,
        Some(s) => match (*s.work.clone()).clone() {
            CostExpr::CostConst { value: v, .. } => (v.clone() == 42),
            _ => false,
        },
    }
}

pub fn eviction_returns_a_proven_zero_instead_of_absent() -> bool {
    {
        let evicted = evict_summary(eviction_probe_table(), "probe_fn".to_string());
        match lookup_summary(evicted.clone(), "probe_fn".to_string()) {
            None => false,
            Some(s) => match (*s.work.clone()).clone() {
                CostExpr::CostConst { value: w, .. } => match (*s.span.clone()).clone() {
                    CostExpr::CostConst { value: sp, .. } => match s.certainty.clone() {
                        Certainty::Proven => ((w.clone() == 0) && (sp.clone() == 0)),
                        _ => false,
                    },
                    _ => false,
                },
                _ => false,
            },
        }
    }
}

pub fn eviction_correct_behavior_control_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The paired statement of what correct looks like, so the defect is described by contrast rather than only pinned. An entry that was never cached must look up Absent — the seed gets THIS case right, which is what makes the evicted case a genuine conflation rather than a uniform absence of the concept: lookup can express 'not computed', and eviction declines to use it.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn never_cached_function_is_absent() -> bool {
    match lookup_summary(
        eviction_probe_table(),
        "a_function_never_cached".to_string(),
    ) {
        None => true,
        Some(_) => false,
    }
}
