// Generated by v1 compiler -- do not edit.
// Source module: v1.compiler.workspace_members

use crate::v1_compiler_stage0_crates::Stage0CratePlanOutcome::{
    Stage0CratePlanOk, Stage0CratePlanRefused,
};
pub use crate::v1_compiler_stage0_crates::{
    stage0_crate_boundary_emit_refusal_message, stage0_crate_plan_outcome,
    stage0_map_package_dir_refusal_to_boundary,
};
pub use crate::v1_compiler_stage0_crates::{Stage0CratePlanOutcome, Stage0CrateSpec};
use crate::v1_rt;
use crate::v1_rt::Witness;
use crate::v1_rt::Witness::{Holds, Violates};
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;
use std::sync::Arc;

pub fn workspace_members_region_begin_marker() -> String {
    "    # BEGIN generated stage0 crate members -- regen_stage0 writes this region (authority: v1.compiler.workspace_members)".to_string()
}

pub fn workspace_members_region_end_marker() -> String {
    "    # END generated stage0 crate members".to_string()
}

pub fn stage0_member_entry(spec: Arc<Stage0CrateSpec>) -> String {
    v1_rt::concat(
        v1_rt::concat("    \"".to_string(), spec.crate_dir.clone()),
        "\",".to_string(),
    )
}

pub fn stage0_workspace_member_region() -> String {
    match (*stage0_crate_plan_outcome()).clone() {
        Stage0CratePlanOutcome::Stage0CratePlanOk { plan: plan, .. } => Arc::new(
            plan.crates
                .clone()
                .iter()
                .cloned()
                .map(stage0_member_entry)
                .collect::<Vec<_>>(),
        )
        .join(&"\n".to_string()),
        Stage0CratePlanOutcome::Stage0CratePlanRefused { cause: cause, .. } => v1_rt::concat(
            "    # REFUSED: ".to_string(),
            stage0_crate_boundary_emit_refusal_message(stage0_map_package_dir_refusal_to_boundary(
                cause.clone(),
            )),
        ),
    }
}
