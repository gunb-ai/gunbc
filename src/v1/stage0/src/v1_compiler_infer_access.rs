// Generated by v1 compiler -- do not edit.
// Source module: v1.compiler.infer_access

pub use crate::std_types::is_ordered_element_collection;
pub use crate::std_types::SourceSpan;
pub use crate::v1_compiler_infer_types::{
    for_each_element_type_node, node_is_element_collection, node_is_keyed_collection,
    node_type_equals, normalize_access_type_node, resolved_type,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::v1_std_core::CompilerDiagnostic::InternalError;
use crate::v1_std_core::InferredNode::{CompilerError, Resolved};
pub use crate::v1_std_core::{
    authored_name_at, diagnostic_to_message, int_type, make_error_node, string_type, unit_type,
    with_optional_cardinality,
};
pub use crate::v1_std_core::{CompilerDiagnostic, ErrorNode, InferredNode, NewlineIndex, Node};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AccessCheckResultNode {
    pub inferred: Option<Rc<InferredNode>>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct KeyedCollectionParts {
    pub key_type: Box<Rc<Node>>,
    pub value_type: Box<Rc<Node>>,
}

pub fn access_error(message: String, span: Rc<SourceSpan>, module_name: String) -> Rc<ErrorNode> {
    make_error_node(
        CompilerDiagnostic::InternalError {
            message: message.clone(),
            span: span.clone(),
        },
        module_name.clone(),
    )
}

pub fn access_result(
    inferred: Node,
    diagnostics: Rc<Vec<Rc<ErrorNode>>>,
    span: Rc<SourceSpan>,
    fallback_message: String,
) -> Rc<AccessCheckResultNode> {
    if ((diagnostics.clone().len() as i64) == 0) {
        AccessCheckResultNode {
            inferred: Some(InferredNode::Resolved {
                node: Box::new(inferred.clone()),
            }),
            diagnostics: diagnostics.clone(),
        }
    } else {
        {
            let message = match diagnostics.clone().first().cloned() {
                Some(diag) => diagnostic_to_message(diag.diagnostic.clone()),
                None => fallback_message.clone(),
            };
            AccessCheckResultNode {
                inferred: Some(InferredNode::CompilerError {
                    message: message.clone(),
                    span: span.clone(),
                }),
                diagnostics: diagnostics.clone(),
            }
        }
    }
}

pub fn keyed_collection_parts(
    n: Node,
    source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>,
) -> Option<Rc<KeyedCollectionParts>> {
    if (node_is_keyed_collection(n.clone(), source_indices.clone())
        && ((n.children.clone().len() as i64) >= 2))
    {
        match n.children.clone().first().cloned() {
            Some(key_child) => match n.children.clone().iter().cloned().skip(1 as usize).next() {
                Some(value_child) => Some(KeyedCollectionParts {
                    key_type: Box::new(resolved_type(key_child.clone())),
                    value_type: Box::new(resolved_type(value_child.clone())),
                }),
                None => None,
            },
            None => None,
        }
    } else {
        None
    }
}

pub fn check_index_access_node(
    base_type: Node,
    index_type: Node,
    span: Rc<SourceSpan>,
    module_name: String,
    source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>,
) -> Rc<AccessCheckResultNode> {
    {
        let normed = normalize_access_type_node(base_type.clone());
        let normed_index = normalize_access_type_node(index_type.clone());
        let base_is_string =
            node_type_equals(normed.clone(), string_type(), source_indices.clone());
        let index_is_int =
            node_type_equals(normed_index.clone(), int_type(), source_indices.clone());
        if base_is_string.clone() {
            {
                let diags = if index_is_int.clone() {
                    Rc::new(vec![])
                } else {
                    Rc::new(vec![access_error(
                        "string index requires an Int index".to_string(),
                        span.clone(),
                        module_name.clone(),
                    )])
                };
                access_result(
                    string_type(),
                    diags.clone(),
                    span.clone(),
                    "invalid string index access".to_string(),
                )
            }
        } else {
            match keyed_collection_parts(normed.clone(), source_indices.clone()) {
                Some(parts) => {
                    let key_diags = if node_type_equals(
                        parts.key_type.clone(),
                        normed_index.clone(),
                        source_indices.clone(),
                    ) {
                        Rc::new(vec![])
                    } else {
                        Rc::new(vec![access_error("keyed collection index key type does not match the collection key type".to_string(), span.clone(), module_name.clone())])
                    };
                    access_result(
                        with_optional_cardinality(parts.value_type.clone()),
                        key_diags.clone(),
                        span.clone(),
                        "invalid keyed collection index access".to_string(),
                    )
                }
                None => {
                    if node_is_keyed_collection(normed.clone(), source_indices.clone()) {
                        {
                            let malformed_diags = Rc::new(vec![access_error(
                                "malformed keyed collection type in index access".to_string(),
                                span.clone(),
                                module_name.clone(),
                            )]);
                            access_result(
                                unit_type(),
                                malformed_diags.clone(),
                                span.clone(),
                                "malformed keyed collection type in index access".to_string(),
                            )
                        }
                    } else {
                        if ((is_ordered_element_collection(authored_name_at(
                            source_indices.clone(),
                            normed.clone(),
                        )) && node_is_element_collection(
                            normed.clone(),
                            source_indices.clone(),
                        )) && index_is_int.clone())
                        {
                            {
                                let elem = for_each_element_type_node(
                                    normed.clone(),
                                    source_indices.clone(),
                                );
                                access_result(
                                    with_optional_cardinality(elem.clone()),
                                    Rc::new(vec![]),
                                    span.clone(),
                                    "list index access".to_string(),
                                )
                            }
                        } else {
                            {
                                let diags = Rc::new(vec![access_error("indexing is only supported for String, keyed collection, and list values".to_string(), span.clone(), module_name.clone())]);
                                access_result(
                                    unit_type(),
                                    diags.clone(),
                                    span.clone(),
                                    "invalid index access".to_string(),
                                )
                            }
                        }
                    }
                }
            }
        }
    }
}

pub fn check_slice_access_node(
    base_type: Node,
    start_type: Node,
    end_type: Node,
    span: Rc<SourceSpan>,
    module_name: String,
    source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>,
) -> Rc<AccessCheckResultNode> {
    {
        let normed_base = normalize_access_type_node(base_type.clone());
        let base_is_string =
            node_type_equals(normed_base.clone(), string_type(), source_indices.clone());
        let base_is_list =
            (is_ordered_element_collection(authored_name_at(
                source_indices.clone(),
                normed_base.clone(),
            )) && node_is_element_collection(normed_base.clone(), source_indices.clone()));
        let base_diags = if (base_is_string.clone() || base_is_list.clone()) {
            Rc::new(vec![])
        } else {
            Rc::new(vec![access_error(
                "slice is only supported for String and list values".to_string(),
                span.clone(),
                module_name.clone(),
            )])
        };
        let normed_start = normalize_access_type_node(start_type.clone());
        let start_diags =
            if node_type_equals(normed_start.clone(), int_type(), source_indices.clone()) {
                Rc::new(vec![])
            } else {
                Rc::new(vec![access_error(
                    "slice start requires an Int index".to_string(),
                    span.clone(),
                    module_name.clone(),
                )])
            };
        let normed_end = normalize_access_type_node(end_type.clone());
        let end_diags = if node_type_equals(normed_end.clone(), int_type(), source_indices.clone())
        {
            Rc::new(vec![])
        } else {
            Rc::new(vec![access_error(
                "slice end requires an Int index".to_string(),
                span.clone(),
                module_name.clone(),
            )])
        };
        let all_diags = v1_rt::concat(
            v1_rt::concat(base_diags.clone(), start_diags.clone()),
            end_diags.clone(),
        );
        let slice_result_type = if base_is_string.clone() {
            string_type()
        } else {
            if base_is_list.clone() {
                normed_base.clone()
            } else {
                string_type()
            }
        };
        access_result(
            slice_result_type.clone(),
            all_diags.clone(),
            span.clone(),
            "invalid slice access".to_string(),
        )
    }
}
