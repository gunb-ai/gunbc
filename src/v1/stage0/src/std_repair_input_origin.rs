// Generated by v1 compiler -- do not edit.
// Source module: std.repair_input_origin

use self::CandidateSpelling::*;
use self::NonSourceValueReason::*;
use self::RepairInputOriginCandidate::*;
use self::SourceCarrierOrigin::*;
pub use crate::std_decl_ref::DeclarationRef;
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CandidateSpelling {
    BareCandidate {
        name: String,
    },
    QualifiedCandidate {
        authored_qualifier: String,
        member: String,
    },
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum SourceCarrierOrigin {
    UnlistedTypeResolutionOrigin,
    ValueReferenceResolutionOrigin,
    ItemEmitSurfaceOrigin,
    FieldImportSurfaceOrigin,
    VariantPayloadSurfaceOrigin,
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RenderedTypeSurfaceOrigin;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum NonSourceValueReason {
    RuntimePrimitiveValue,
    ValueTargetUndetermined,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SourceCarrierCandidateIdentity {
    pub emitting_module: String,
    pub candidate_spelling: Rc<CandidateSpelling>,
    pub producer_origin: SourceCarrierOrigin,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SourceDeclarationCarrierIdentity {
    pub emitting_module: String,
    pub candidate_spelling: Rc<CandidateSpelling>,
    pub declaring_declaration: Rc<DeclarationRef>,
    pub producer_origin: SourceCarrierOrigin,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RenderedTargetVocabularyIdentity {
    pub emitting_module: String,
    pub rendered_identifier: String,
    pub producer_origin: RenderedTypeSurfaceOrigin,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum RepairInputOriginCandidate {
    SourceCarrierCandidate {
        identity: Rc<SourceCarrierCandidateIdentity>,
    },
    SourceDeclarationCarrier {
        identity: Rc<SourceDeclarationCarrierIdentity>,
    },
    NonSourceValueCompatibilityCandidate {
        emitting_module: String,
        candidate_spelling: Rc<CandidateSpelling>,
        reason: NonSourceValueReason,
    },
    RenderedTargetVocabularyCarrier {
        identity: Rc<RenderedTargetVocabularyIdentity>,
    },
}

pub fn source_declaration_carrier(
    emitting_module: String,
    candidate_spelling: Rc<CandidateSpelling>,
    declaring_declaration: Rc<DeclarationRef>,
    producer_origin: SourceCarrierOrigin,
) -> Rc<RepairInputOriginCandidate> {
    Rc::new(RepairInputOriginCandidate::SourceDeclarationCarrier {
        identity: Rc::new(SourceDeclarationCarrierIdentity {
            emitting_module: emitting_module.clone(),
            candidate_spelling: candidate_spelling.clone(),
            declaring_declaration: declaring_declaration.clone(),
            producer_origin: producer_origin.clone(),
        }),
    })
}

pub fn non_source_value_compatibility_candidate(
    emitting_module: String,
    candidate_spelling: Rc<CandidateSpelling>,
    reason: NonSourceValueReason,
) -> Rc<RepairInputOriginCandidate> {
    Rc::new(
        RepairInputOriginCandidate::NonSourceValueCompatibilityCandidate {
            emitting_module: emitting_module.clone(),
            candidate_spelling: candidate_spelling.clone(),
            reason: reason.clone(),
        },
    )
}

pub fn candidate_spelling_from_text(name: String) -> Rc<CandidateSpelling> {
    {
        let segments = Rc::new(
            name.clone()
                .split(&".".to_string())
                .map(|s| s.to_string())
                .collect::<Vec<_>>(),
        );
        if ((segments.clone().len() as i64) > 1) {
            Rc::new(CandidateSpelling::QualifiedCandidate {
                authored_qualifier: Rc::new(
                    segments
                        .clone()
                        .iter()
                        .cloned()
                        .take(((segments.clone().len() as i64) - 1) as usize)
                        .collect::<Vec<_>>(),
                )
                .join(&".".to_string()),
                member: match segments.clone().last().cloned() {
                    Some(member) => member.clone(),
                    None => "".to_string(),
                },
            })
        } else {
            Rc::new(CandidateSpelling::BareCandidate { name: name.clone() })
        }
    }
}

pub fn source_origin_candidates(
    emitting_module: String,
    names: Rc<Vec<String>>,
    origin: SourceCarrierOrigin,
) -> Rc<Vec<Rc<RepairInputOriginCandidate>>> {
    Rc::new({
        let mut __result = Vec::new();
        for name in names.iter().cloned() {
            __result.push(Rc::new(
                RepairInputOriginCandidate::SourceCarrierCandidate {
                    identity: Rc::new(SourceCarrierCandidateIdentity {
                        emitting_module: emitting_module.clone(),
                        candidate_spelling: candidate_spelling_from_text(name.clone()),
                        producer_origin: origin.clone(),
                    }),
                },
            ));
        }
        __result
    })
}

pub fn rendered_origin_candidates(
    emitting_module: String,
    names: Rc<Vec<String>>,
) -> Rc<Vec<Rc<RepairInputOriginCandidate>>> {
    Rc::new({
        let mut __result = Vec::new();
        for name in names.iter().cloned() {
            __result.push(Rc::new(
                RepairInputOriginCandidate::RenderedTargetVocabularyCarrier {
                    identity: Rc::new(RenderedTargetVocabularyIdentity {
                        emitting_module: emitting_module.clone(),
                        rendered_identifier: name.clone(),
                        producer_origin: RenderedTypeSurfaceOrigin {},
                    }),
                },
            ));
        }
        __result
    })
}

pub fn repair_input_origin_candidates(
    emitting_module: String,
    unlisted_type_candidates: Rc<Vec<Rc<RepairInputOriginCandidate>>>,
    value_reference_candidates: Rc<Vec<Rc<RepairInputOriginCandidate>>>,
    item_emit_surface_names: Rc<Vec<String>>,
    field_import_surface_names: Rc<Vec<String>>,
    variant_payload_surface_names: Rc<Vec<String>>,
    rendered_type_surface_names: Rc<Vec<String>>,
) -> Rc<Vec<Rc<RepairInputOriginCandidate>>> {
    v1_rt::concat(
        unlisted_type_candidates.clone(),
        v1_rt::concat(
            value_reference_candidates.clone(),
            v1_rt::concat(
                source_origin_candidates(
                    emitting_module.clone(),
                    item_emit_surface_names.clone(),
                    SourceCarrierOrigin::ItemEmitSurfaceOrigin,
                ),
                v1_rt::concat(
                    source_origin_candidates(
                        emitting_module.clone(),
                        field_import_surface_names.clone(),
                        SourceCarrierOrigin::FieldImportSurfaceOrigin,
                    ),
                    v1_rt::concat(
                        source_origin_candidates(
                            emitting_module.clone(),
                            variant_payload_surface_names.clone(),
                            SourceCarrierOrigin::VariantPayloadSurfaceOrigin,
                        ),
                        rendered_origin_candidates(
                            emitting_module.clone(),
                            rendered_type_surface_names.clone(),
                        ),
                    ),
                ),
            ),
        ),
    )
}

pub fn repair_input_origin_candidate_name(candidate: Rc<RepairInputOriginCandidate>) -> String {
    match (*candidate.clone()).clone() {
        RepairInputOriginCandidate::SourceCarrierCandidate {
            identity: identity, ..
        } => match (*identity.candidate_spelling.clone()).clone() {
            CandidateSpelling::BareCandidate { name: name, .. } => name.clone(),
            CandidateSpelling::QualifiedCandidate {
                authored_qualifier,
                member,
                ..
            } => v1_rt::concat(
                v1_rt::concat(authored_qualifier.clone(), ".".to_string()),
                member.clone(),
            ),
        },
        RepairInputOriginCandidate::SourceDeclarationCarrier {
            identity: identity, ..
        } => match (*identity.candidate_spelling.clone()).clone() {
            CandidateSpelling::BareCandidate { name: name, .. } => name.clone(),
            CandidateSpelling::QualifiedCandidate {
                authored_qualifier,
                member,
                ..
            } => v1_rt::concat(
                v1_rt::concat(authored_qualifier.clone(), ".".to_string()),
                member.clone(),
            ),
        },
        RepairInputOriginCandidate::NonSourceValueCompatibilityCandidate {
            candidate_spelling,
            ..
        } => match (*candidate_spelling.clone()).clone() {
            CandidateSpelling::BareCandidate { name: name, .. } => name.clone(),
            CandidateSpelling::QualifiedCandidate {
                authored_qualifier,
                member,
                ..
            } => v1_rt::concat(
                v1_rt::concat(authored_qualifier.clone(), ".".to_string()),
                member.clone(),
            ),
        },
        RepairInputOriginCandidate::RenderedTargetVocabularyCarrier {
            identity: identity, ..
        } => identity.rendered_identifier.clone(),
    }
}

pub fn repair_input_origin_compatibility_names(
    candidates: Rc<Vec<Rc<RepairInputOriginCandidate>>>,
) -> Rc<Vec<String>> {
    Rc::new({
        let mut __result = Vec::new();
        for candidate in candidates.iter().cloned() {
            __result.push(repair_input_origin_candidate_name(candidate.clone()));
        }
        __result
    })
}

pub fn qualified_item_emit_surface_compatibility_names(
    candidates: Rc<Vec<Rc<RepairInputOriginCandidate>>>,
) -> Rc<Vec<String>> {
    Rc::new({
        let mut __result = Vec::new();
        for candidate in candidates.iter().cloned() {
            __result.extend(
                (*match (*candidate.clone()).clone() {
                    RepairInputOriginCandidate::SourceCarrierCandidate {
                        identity: identity,
                        ..
                    } => match identity.producer_origin.clone() {
                        SourceCarrierOrigin::ItemEmitSurfaceOrigin => {
                            match (*identity.candidate_spelling.clone()).clone() {
                                CandidateSpelling::QualifiedCandidate {
                                    authored_qualifier,
                                    member,
                                    ..
                                } => Rc::new(vec![v1_rt::concat(
                                    v1_rt::concat(authored_qualifier.clone(), ".".to_string()),
                                    member.clone(),
                                )]),
                                CandidateSpelling::BareCandidate { name: _, .. } => Rc::new(vec![]),
                            }
                        }
                        SourceCarrierOrigin::UnlistedTypeResolutionOrigin => Rc::new(vec![]),
                        SourceCarrierOrigin::ValueReferenceResolutionOrigin => Rc::new(vec![]),
                        SourceCarrierOrigin::FieldImportSurfaceOrigin => Rc::new(vec![]),
                        SourceCarrierOrigin::VariantPayloadSurfaceOrigin => Rc::new(vec![]),
                    },
                    RepairInputOriginCandidate::SourceDeclarationCarrier {
                        identity: _, ..
                    } => Rc::new(vec![]),
                    RepairInputOriginCandidate::NonSourceValueCompatibilityCandidate { .. } => {
                        Rc::new(vec![])
                    }
                    RepairInputOriginCandidate::RenderedTargetVocabularyCarrier {
                        identity: _,
                        ..
                    } => Rc::new(vec![]),
                })
                .iter()
                .cloned(),
            );
        }
        __result
    })
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct UnlistedTypeResolutionOrigin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ValueReferenceResolutionOrigin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ItemEmitSurfaceOrigin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FieldImportSurfaceOrigin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct VariantPayloadSurfaceOrigin;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct RuntimePrimitiveValue;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ValueTargetUndetermined;
