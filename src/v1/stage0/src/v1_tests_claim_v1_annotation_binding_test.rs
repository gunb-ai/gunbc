// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.v1_annotation_binding_test

pub use crate::std_occurrence_identity::{OccurrenceId, OccurrenceIndexEntry, OccurrenceTransport};
use crate::std_source_annotation::AnnotationAttachmentRefusal::{
    BodyGrainNotModeled, TrailingNotModeled, UnattachedAtScopeEnd,
};
pub use crate::std_source_annotation::{
    annotation_attachment_result_graph, annotation_attachment_result_refusals,
    source_annotation_graph_rows,
};
pub use crate::std_source_annotation::{
    AnnotationAttachmentRefusal, AnnotationAttachmentResult, SourceAnnotationDebt,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List};
pub use crate::v1_compiler_annotation_bind::AdmittedSourceAnnotations;
pub use crate::v1_compiler_annotation_bind::{
    admit_source_annotations, bind_annotations, strip_line_comment_delimiter,
};
pub use crate::v1_compiler_parse::parse_with_table;
pub use crate::v1_compiler_tokenize::tokenize_artifact;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v1_std_core::NewlineIndex;
pub use crate::v1_std_core::{build_newline_index, empty_intern_table};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn v1_annotation_binding_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE: target/debug/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/v1_annotation_binding_test.dag --functions w_leading_prose_attaches_to_the_data_row_it_precedes,w_leading_prose_attaches_to_the_type_it_precedes,w_body_prose_refuses_rather_than_attaching_to_a_later_declaration,w_prose_above_module_attaches_to_the_module,w_trailing_prose_refuses,w_annotation_free_source_binds_nothing,w_delimiter_normalization_keeps_authored_indentation,w_a_single_refusal_withholds_the_entire_graph,w_the_same_prose_without_the_refusal_is_admitted,w_adjacent_comment_lines_form_one_block,w_blank_line_separated_comments_form_two_blocks".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn v1_annotation_binding_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The end-to-end .dag witness: real source through the real tokenizer, the real parser, and the real binder, asserting WHICH declaration each piece of prose landed on. Everything upstream of this — the capture channel, the attachment fold — is proven separately on synthetic inputs; this is what proves the three fit together on the language the corpus is actually written in.\n\nIt asserts against AUTHORED NAMES rather than occurrence id values. Ids are allocator-local and would make the expected string a transcription of whatever the parser happened to number things on the day it was written, so a test asserting `7` would fail the moment an unrelated declaration was added above — and would be repaired by transcribing the new number, which checks nothing. A name is the fact the reader cares about.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn binding_fixture_note() -> String {
    thread_local! {
        static CACHED: String = {
            "One module carrying prose in every position the rule distinguishes: above a data row, above a type, inside a function body, and trailing after code. The data row is the load-bearing case — every prose row this repository migrates is a module-scope `data` declaration, and those are absent from the parser's declaration-role projection, so a roster built from that projection would bind none of them.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn binding_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\n// about alpha\ndata alpha: String = \"a\"\n\n// about Thing\ntype Thing {\n  f: String\n}\n\nfn go(x: String) -> String {\n  // inside the body\n  let y = x\n  y\n}\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn clean_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\ndata alpha: String = \"a\"\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn header_source() -> String {
    thread_local! {
        static CACHED: String = {
            "// file header prose\nmodule p.q\n\ndata alpha: String = \"a\"\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn trailing_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\ndata alpha: String = \"a\" // trailing prose\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn transport_of(source: String) -> Rc<OccurrenceTransport> {
    parse_with_table(
        tokenize_artifact(source.clone(), "probe.dag".to_string())
            .tokens
            .clone(),
        v1_rt::rc_map_insert(
            v1_rt::rc_empty_map::<String, Rc<NewlineIndex>>(),
            "probe.dag".to_string(),
            build_newline_index("probe.dag".to_string(), source.clone()),
        ),
        empty_intern_table(),
    )
    .occurrence_transport
    .clone()
}

pub fn bound(source: String) -> Rc<AnnotationAttachmentResult> {
    bind_annotations(
        transport_of(source.clone()),
        tokenize_artifact(source.clone(), "probe.dag".to_string())
            .annotations
            .clone(),
        v1_rt::string_length(&source),
    )
}

pub fn name_lookup_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Resolves an occurrence back to the name the author wrote, through the same index the roster was built from. An unresolvable id renders as a marker rather than an empty string so a lookup miss cannot be mistaken for an anonymous declaration.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn name_of(transport: Rc<OccurrenceTransport>, id: OccurrenceId) -> String {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            "<unresolved>".to_string(),
            |acc: String, entry: Rc<OccurrenceIndexEntry>| {
                if (entry.projection.clone().occurrence.clone().value.clone() == id.value.clone()) {
                    entry.projection.clone().authored_name.clone()
                } else {
                    acc
                }
            },
        )
}

pub fn render(source: String) -> String {
    {
        let transport = transport_of(source.clone());
        source_annotation_graph_rows(annotation_attachment_result_graph(bound(source.clone())))
            .iter()
            .cloned()
            .fold(
                "".to_string(),
                |acc: String, row: Rc<SourceAnnotationDebt>| {
                    v1_rt::concat(
                        acc,
                        v1_rt::concat(
                            "|".to_string(),
                            v1_rt::concat(
                                name_of(transport.clone(), row.subject.clone()),
                                v1_rt::concat("=".to_string(), row.text.clone()),
                            ),
                        ),
                    )
                },
            )
    }
}

pub fn count_body_grain(refusals: Rc<Vec<Rc<AnnotationAttachmentRefusal>>>) -> i64 {
    refusals
        .clone()
        .iter()
        .cloned()
        .fold(
            0,
            |n: i64, refusal: Rc<AnnotationAttachmentRefusal>| match (*refusal.clone()).clone() {
                AnnotationAttachmentRefusal::BodyGrainNotModeled { origin: _, .. } => {
                    (n.clone() + 1)
                }
                AnnotationAttachmentRefusal::TrailingNotModeled { origin: _, .. } => n.clone(),
                AnnotationAttachmentRefusal::UnattachedAtScopeEnd { origin: _, .. } => n.clone(),
            },
        )
}

pub fn count_trailing(refusals: Rc<Vec<Rc<AnnotationAttachmentRefusal>>>) -> i64 {
    refusals
        .clone()
        .iter()
        .cloned()
        .fold(
            0,
            |n: i64, refusal: Rc<AnnotationAttachmentRefusal>| match (*refusal.clone()).clone() {
                AnnotationAttachmentRefusal::BodyGrainNotModeled { origin: _, .. } => n.clone(),
                AnnotationAttachmentRefusal::TrailingNotModeled { origin: _, .. } => {
                    (n.clone() + 1)
                }
                AnnotationAttachmentRefusal::UnattachedAtScopeEnd { origin: _, .. } => n.clone(),
            },
        )
}

pub fn w_leading_prose_attaches_to_the_data_row_it_precedes() -> bool {
    (render(binding_source()) == "|alpha=about alpha|Thing=about Thing".to_string())
}

pub fn w_leading_prose_attaches_to_the_type_it_precedes() -> bool {
    {
        let rows = source_annotation_graph_rows(annotation_attachment_result_graph(bound(
            binding_source(),
        )));
        let transport = transport_of(binding_source());
        rows.clone()
            .iter()
            .cloned()
            .fold(false, |found: bool, row: Rc<SourceAnnotationDebt>| {
                (found
                    || ((name_of(transport.clone(), row.subject.clone()) == "Thing".to_string())
                        && (row.text.clone() == "about Thing".to_string())))
            })
    }
}

pub fn body_grain_end_to_end_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The prose inside `go` names no module item. Without the containment rule it would resolve to whatever module item came next in the file, so this asserts BOTH that a refusal was counted AND that no row carries its text — a refusal count alone would still pass if the same prose had also been attached somewhere.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn w_body_prose_refuses_rather_than_attaching_to_a_later_declaration() -> bool {
    ((count_body_grain(annotation_attachment_result_refusals(bound(
        binding_source(),
    ))) == 1)
        && (render(binding_source()) == "|alpha=about alpha|Thing=about Thing".to_string()))
}

pub fn w_prose_above_module_attaches_to_the_module() -> bool {
    (render(header_source()) == "|p.q=file header prose".to_string())
}

pub fn w_trailing_prose_refuses() -> bool {
    ((count_trailing(annotation_attachment_result_refusals(bound(
        trailing_source(),
    ))) == 1)
        && (render(trailing_source()) == "".to_string()))
}

pub fn w_annotation_free_source_binds_nothing() -> bool {
    ((render(clean_source()) == "".to_string())
        && ((annotation_attachment_result_refusals(bound(clean_source())).len() as i64) == 0))
}

pub fn w_delimiter_normalization_keeps_authored_indentation() -> bool {
    (((strip_line_comment_delimiter("// plain".to_string()) == "plain".to_string())
        && (strip_line_comment_delimiter("//   indented".to_string()) == "  indented".to_string()))
        && (strip_line_comment_delimiter("//tight".to_string()) == "tight".to_string()))
}

pub fn admission_gate_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The P0 wall, by execution: unclassified prose does not ride along inside a successful result. A source whose only annotation is trailing yields NO graph rows and at least one blocking diagnostic, while the same source with the prose moved above the declaration yields the row and no diagnostic. The pair is what distinguishes a wall from a filter — a filter would have produced the admissible rows either way.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn admitted(source: String) -> Rc<AdmittedSourceAnnotations> {
    admit_source_annotations(
        transport_of(source.clone()),
        tokenize_artifact(source.clone(), "probe.dag".to_string())
            .annotations
            .clone(),
        v1_rt::string_length(&source),
    )
}

pub fn mixed_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\n// good prose\ndata alpha: String = \"a\" // stray trailing\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn w_a_single_refusal_withholds_the_entire_graph() -> bool {
    {
        let a = admitted(mixed_source());
        (((source_annotation_graph_rows(a.graph.clone()).len() as i64) == 0)
            && ((a.diagnostics.clone().len() as i64) == 1))
    }
}

pub fn w_the_same_prose_without_the_refusal_is_admitted() -> bool {
    {
        let a = admitted(header_source());
        (((source_annotation_graph_rows(a.graph.clone()).len() as i64) == 1)
            && ((a.diagnostics.clone().len() as i64) == 0))
    }
}

pub fn end_to_end_blank_line_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The blank-line ruling proven on REAL source through the real lexer, not on synthetic capture rows. The synthetic pair proves the fold honours the flag; this proves the v1 lexer actually observes it, which is the half that cannot be checked by constructing captures by hand.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn two_adjacent_blocks_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\n// first line\n// second line\ndata alpha: String = \"a\"\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn two_separated_blocks_source() -> String {
    thread_local! {
        static CACHED: String = {
            "module p.q\n\n// first block\n\n// second block\ndata alpha: String = \"a\"\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn w_adjacent_comment_lines_form_one_block() -> bool {
    (render(two_adjacent_blocks_source()) == "|alpha=first line\nsecond line".to_string())
}

pub fn w_blank_line_separated_comments_form_two_blocks() -> bool {
    (render(two_separated_blocks_source()) == "|alpha=first block|alpha=second block".to_string())
}
