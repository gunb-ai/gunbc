// Seed realization for v2.compiler.ingest (Gate-A flip wave, neat-swift-795).
// Hand-retained Rust oracle — independent of the self-emitted artifact under test.
// Dissolve-on: self-emit cutover retires this module when v2.compiler.ingest
// is emitted-only and the behavioral harness is modeled (si_scaffold_dissolution_trigger).

/// Mirrors `data ingest_module_note` in src/v2/compiler/03_ingest.dag.
pub fn ingest_module_note() -> String {
    "Gate-A flip wave lane: v2.compiler.ingest is the parse-tree-to-target bridge (neutralize → infer → emit over CoreNode). Curated seed-linked behavioral receipt compares this module-authority constant until full ingest-tree oracle greens under std_dup-resolved cargo.".to_string()
}
