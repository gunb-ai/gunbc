// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.v1_annotation_capture_test

use crate::std_source_annotation::AnnotationPlacement::{
    LeadingAfterLineIndent, TrailingAfterSemanticToken,
};
pub use crate::std_source_annotation::{AnnotationPlacement, UnboundAnnotationCapture};
pub use crate::v1_compiler_tokenize::{tokenize, tokenize_artifact};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn v1_annotation_capture_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE: target/release/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/v1_annotation_capture_test.dag --functions w_two_captures_preserved_in_source_order,w_captures_excluded_from_token_stream,w_eof_annotation_is_captured,w_leading_and_trailing_placements_differ,w_slash_slash_inside_string_is_not_an_annotation,w_annotation_free_source_captures_none".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn v1_annotation_capture_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The v1 SEED realization of the annotation channel, proven independently of v2. These execute against the .dag tokenizer source through the current stage0 binary, so they are pre-regen evidence: they establish that tokenize_artifact captures and that tokenize projects the captures away, BEFORE the long regen/rebuild cycle makes // live in real .dag source. The two realizations are tested separately on purpose — one shared test would pass if either alone were correct.\n\nTOKEN EXCLUSION IS ASSERTED BY COUNT AGAINST A CLEAN CONTROL, not by scanning token text for the comment's words. A text scan would pass if the capture leaked in under a different spelling; comparing the annotated stream to the same source with the comment removed fails on any leak at all, including one that preserves no recognizable substring.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_two_comments() -> String {
    thread_local! {
        static CACHED: String = {
            "// first\nfn a() -> Int { 1 }\n// second\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_two_comments_removed() -> String {
    thread_local! {
        static CACHED: String = {
            "\nfn a() -> Int { 1 }\n\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_leading() -> String {
    thread_local! {
        static CACHED: String = {
            "// note\nfn a() -> Int { 1 }\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_trailing() -> String {
    thread_local! {
        static CACHED: String = {
            "fn a() -> Int { 1 } // note\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_clean() -> String {
    thread_local! {
        static CACHED: String = {
            "fn a() -> Int { 1 }\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_eof_comment() -> String {
    thread_local! {
        static CACHED: String = {
            "fn a() -> Int { 1 }\n// at eof".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn src_url_in_string() -> String {
    thread_local! {
        static CACHED: String = {
            "data u: String = \"http://example.com\"\n".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn captures(source: String) -> Rc<Vec<Rc<UnboundAnnotationCapture>>> {
    tokenize_artifact(source.clone(), "probe.dag".to_string())
        .annotations
        .clone()
}

pub fn token_count(source: String) -> i64 {
    (tokenize(source.clone(), "probe.dag".to_string()).len() as i64)
}

pub fn w_two_captures_preserved_in_source_order() -> bool {
    {
        let caps = captures(src_two_comments());
        ((((caps.clone().len() as i64) == 2)
            && (caps.clone()[(0) as usize]
                .clone()
                .clone()
                .unwrap()
                .lexeme
                .clone()
                == "// first".to_string()))
            && (caps.clone()[(1) as usize]
                .clone()
                .clone()
                .unwrap()
                .lexeme
                .clone()
                == "// second".to_string()))
    }
}

pub fn w_captures_excluded_from_token_stream() -> bool {
    (token_count(src_two_comments()) == token_count(src_two_comments_removed()))
}

pub fn w_eof_annotation_is_captured() -> bool {
    ((captures(src_eof_comment()).len() as i64) == 1)
}

pub fn w_leading_and_trailing_placements_differ() -> bool {
    {
        let leading = captures(src_leading());
        let trailing = captures(src_trailing());
        (((((leading.clone().len() as i64) == 1) && ((trailing.clone().len() as i64) == 1))
            && (leading.clone()[(0) as usize]
                .clone()
                .clone()
                .unwrap()
                .placement
                .clone()
                == AnnotationPlacement::LeadingAfterLineIndent))
            && (trailing.clone()[(0) as usize]
                .clone()
                .clone()
                .unwrap()
                .placement
                .clone()
                == AnnotationPlacement::TrailingAfterSemanticToken))
    }
}

pub fn w_slash_slash_inside_string_is_not_an_annotation() -> bool {
    ((captures(src_url_in_string()).len() as i64) == 0)
}

pub fn w_annotation_free_source_captures_none() -> bool {
    ((captures(src_clean()).len() as i64) == 0)
}
