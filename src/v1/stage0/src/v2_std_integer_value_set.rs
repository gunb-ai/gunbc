// Generated by v1 compiler -- do not edit.
// Source module: v2.std.integer_value_set

use self::IntegerValueSet::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::v2_std_diagnostic::Correction::*;
use crate::v2_std_diagnostic::NoCorrectionReason::*;
use crate::v2_std_diagnostic::Outcome::*;
pub use crate::v2_std_diagnostic::{diagnostics_singleton, node_locus};
pub use crate::v2_std_diagnostic::{
    Correction, Diagnostic, Diagnostics, NoCorrectionReason, Outcome,
};
pub use crate::v2_std_integer::integer_interval_spec_node_contains;
pub use crate::v2_std_node::well_formed;
use crate::v2_std_node::Connective::*;
use crate::v2_std_node::NodeKind::*;
use crate::v2_std_node::NodeOccurrenceId::*;
pub use crate::v2_std_node::{Connective, Edge, Node, NodeKind, NodeOccurrenceId, Symbol};
pub use crate::v2_std_node_query::find_named_child;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum IntegerValueSet {
    IntegerValueSetUnbounded,
    IntegerValueSetInterval { interval: Rc<Node> },
}
impl IntegerValueSet {
    pub fn interval(&self) -> Rc<Node> {
        match self {
            IntegerValueSet::IntegerValueSetUnbounded => panic!("no interval on unit variant"),
            IntegerValueSet::IntegerValueSetInterval {
                interval: __val, ..
            } => __val.clone(),
        }
    }
}

pub fn integer_value_set_atom(id: String) -> Rc<Node> {
    Rc::new(Node {
        kind: Rc::new(NodeKind::TypeNode {
            connective: Rc::new(Connective::Atom {
                identity: id.clone(),
            }),
        }),
        children: Rc::new(vec![]),
        occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
    })
}

pub fn integer_unbounded_value_set_node() -> Rc<Node> {
    Rc::new(Node {
        kind: Rc::new(NodeKind::TypeNode {
            connective: Rc::new(Connective::Conj),
        }),
        children: Rc::new(vec![Rc::new(Edge {
            label: Rc::new(EdgeLabel::Named {
                name: "integer_value_set_field_kind".to_string(),
            }),
            target: integer_value_set_atom("integer_value_set_kind_unbounded".to_string()),
        })]),
        occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
    })
}

pub fn integer_interval_value_set_node(interval: Rc<Node>) -> Rc<Node> {
    Rc::new(Node {
        kind: Rc::new(NodeKind::TypeNode {
            connective: Rc::new(Connective::Conj),
        }),
        children: Rc::new(vec![
            Rc::new(Edge {
                label: Rc::new(EdgeLabel::Named {
                    name: "integer_value_set_field_kind".to_string(),
                }),
                target: integer_value_set_atom("integer_value_set_kind_interval".to_string()),
            }),
            Rc::new(Edge {
                label: Rc::new(EdgeLabel::Named {
                    name: "integer_value_set_field_interval".to_string(),
                }),
                target: interval.clone(),
            }),
        ]),
        occurrence_id: Rc::new(NodeOccurrenceId::SyntheticOccurrence),
    })
}

pub fn integer_value_set_decode_invalid_diagnostic(source_carrier: Rc<Node>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "integer_value_set_decode_invalid".to_string(),
        at: node_locus(source_carrier.clone()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn integer_value_set_from_node(value_set: Rc<Node>) -> Rc<Outcome<Rc<IntegerValueSet>>> {
    if (well_formed(value_set.clone()) == false) {
        Rc::new(Outcome::Rejected {
            diagnostics: diagnostics_singleton(integer_value_set_decode_invalid_diagnostic(
                value_set.clone(),
            )),
        })
    } else {
        match (*find_named_child(
            value_set.clone(),
            "integer_value_set_field_kind".to_string(),
        ))
        .clone()
        {
            Outcome::Accepted {
                value: found_kind,
                diagnostics: None,
                ..
            } => {
                if (found_kind.clone()
                    == integer_value_set_atom("integer_value_set_kind_unbounded".to_string()))
                {
                    if ((value_set.children.clone().len() as i64) == 1) {
                        match (*find_named_child(
                            value_set.clone(),
                            "integer_value_set_field_interval".to_string(),
                        ))
                        .clone()
                        {
                            Outcome::Accepted {
                                diagnostics: None, ..
                            } => Rc::new(Outcome::Rejected {
                                diagnostics: diagnostics_singleton(
                                    integer_value_set_decode_invalid_diagnostic(value_set.clone()),
                                ),
                            }),
                            Outcome::Rejected { diagnostics: _, .. } => {
                                Rc::new(Outcome::Accepted {
                                    value: Rc::new(IntegerValueSet::IntegerValueSetUnbounded),
                                    diagnostics: None,
                                })
                            }
                        }
                    } else {
                        Rc::new(Outcome::Rejected {
                            diagnostics: diagnostics_singleton(
                                integer_value_set_decode_invalid_diagnostic(value_set.clone()),
                            ),
                        })
                    }
                } else {
                    if (found_kind.clone()
                        == integer_value_set_atom("integer_value_set_kind_interval".to_string()))
                    {
                        if ((value_set.children.clone().len() as i64) == 2) {
                            match (*find_named_child(
                                value_set.clone(),
                                "integer_value_set_field_interval".to_string(),
                            ))
                            .clone()
                            {
                                Outcome::Accepted {
                                    value: interval,
                                    diagnostics: None,
                                    ..
                                } => {
                                    if crate::v2_std_integer::integer_interval_spec_node_contains(
                                        interval.clone(),
                                        interval.clone(),
                                    ) {
                                        Rc::new(Outcome::Accepted {
                                            value: Rc::new(
                                                IntegerValueSet::IntegerValueSetInterval {
                                                    interval: interval.clone(),
                                                },
                                            ),
                                            diagnostics: None,
                                        })
                                    } else {
                                        Rc::new(Outcome::Rejected {
                                            diagnostics: diagnostics_singleton(
                                                integer_value_set_decode_invalid_diagnostic(
                                                    value_set.clone(),
                                                ),
                                            ),
                                        })
                                    }
                                }
                                Outcome::Rejected { diagnostics: _, .. } => {
                                    Rc::new(Outcome::Rejected {
                                        diagnostics: diagnostics_singleton(
                                            integer_value_set_decode_invalid_diagnostic(
                                                value_set.clone(),
                                            ),
                                        ),
                                    })
                                }
                            }
                        } else {
                            Rc::new(Outcome::Rejected {
                                diagnostics: diagnostics_singleton(
                                    integer_value_set_decode_invalid_diagnostic(value_set.clone()),
                                ),
                            })
                        }
                    } else {
                        Rc::new(Outcome::Rejected {
                            diagnostics: diagnostics_singleton(
                                integer_value_set_decode_invalid_diagnostic(value_set.clone()),
                            ),
                        })
                    }
                }
            }
            Outcome::Rejected { diagnostics: _, .. } => Rc::new(Outcome::Rejected {
                diagnostics: diagnostics_singleton(integer_value_set_decode_invalid_diagnostic(
                    value_set.clone(),
                )),
            }),
        }
    }
}

pub fn integer_value_set_contains_decoded(
    container: Rc<IntegerValueSet>,
    contained: Rc<IntegerValueSet>,
) -> bool {
    match (*container.clone()).clone() {
        IntegerValueSet::IntegerValueSetUnbounded => true,
        IntegerValueSet::IntegerValueSetInterval {
            interval: container_interval,
            ..
        } => match (*contained.clone()).clone() {
            IntegerValueSet::IntegerValueSetUnbounded => false,
            IntegerValueSet::IntegerValueSetInterval {
                interval: contained_interval,
                ..
            } => crate::v2_std_integer::integer_interval_spec_node_contains(
                container_interval.clone(),
                contained_interval.clone(),
            ),
        },
    }
}

pub fn integer_value_set_contains(container: Rc<Node>, contained: Rc<Node>) -> bool {
    match integer_value_set_from_node(container.clone()) {
        Outcome::Accepted {
            value: decoded_container,
            diagnostics: None,
            ..
        } => match integer_value_set_from_node(contained.clone()) {
            Outcome::Accepted {
                value: decoded_contained,
                diagnostics: None,
                ..
            } => integer_value_set_contains_decoded(
                decoded_container.clone(),
                decoded_contained.clone(),
            ),
            Outcome::Rejected { diagnostics: _, .. } => false,
        },
        Outcome::Rejected { diagnostics: _, .. } => false,
    }
}

pub fn integer_value_set_strictly_contains(container: Rc<Node>, contained: Rc<Node>) -> bool {
    (integer_value_set_contains(container.clone(), contained.clone())
        && !integer_value_set_contains(contained.clone(), container.clone()))
}
