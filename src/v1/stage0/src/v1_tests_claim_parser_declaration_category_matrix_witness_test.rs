// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.parser_declaration_category_matrix_witness_test

pub use crate::std_occurrence_identity::DeclarationOccurrence;
pub use crate::std_types::Bool;
use crate::std_types::Bool::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v1_tests_claim_parser_declaration_matrix_support::PdcTransport;
use crate::v1_tests_claim_parser_declaration_matrix_support::PdcTransport::{
    PdcTransportReady, PdcTransportRefused,
};
pub use crate::v1_tests_claim_parser_declaration_matrix_support::{
    pdc_declares_nothing, pdc_identity_row, pdc_index_entry_for_declaration,
    pdc_matrix_category_count, pdc_non_type_category_count, pdc_parse_matrix, pdc_row,
    pdc_zero_row,
};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn pdc_matrix_witness_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Parser declaration-category denominator. Pins, per authored declaration form, the OccurrenceCategory population the v1 parser mints — including the forms that mint nothing. Observation-only: no resolution, exposure, or binding claim. The zero rows are the load-bearing half; minting a category later must edit this file.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE (requires v1 parser pool): target/release/claim_batch --roster-from-discovery --source-root dag --source-root src/v1 --scan-dir src/v1/tests/claim. The function list is deliberately NOT enumerated here: this file is enrolled file-grain, so the empty-function authority expands it, and a hand-copied list of test fn names beside a file that already names them is a second representation that rots on the next rename.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_dissolve_on() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVE-ON: witness_layer_roots admits src/v1 for parser-transport witnesses, or the execution corpus carries per-entry source roots — then this recipe deletes and discovery enrollment suffices.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn witness_matrix_fixture_parses() -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => {
            (((transport.declarations.clone().len() as i64) > 0)
                && ((transport.references.clone().len() as i64) > 0))
        }
    }
}

pub fn witness_function_declaration_mints_callable() -> bool {
    pdc_row(
        "pdc_function".to_string(),
        "|CallableOccurrence".to_string(),
    )
}

pub fn witness_second_function_declaration_mints_callable() -> bool {
    pdc_row("pdc_matcher".to_string(), "|CallableOccurrence".to_string())
}

pub fn witness_function_parameter_binds() -> bool {
    pdc_row(
        "pdc_parameter".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_match_scrutinee_parameter_binds() -> bool {
    pdc_row(
        "pdc_scrutinee".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_let_binder_binds() -> bool {
    pdc_row(
        "pdc_let_binder".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_pattern_binder_binds() -> bool {
    pdc_row(
        "pdc_pattern_binder".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_product_type_declaration_is_declared() -> bool {
    pdc_row("PdcProduct".to_string(), "|TypeOccurrence".to_string())
}

pub fn witness_coproduct_type_declaration_is_declared() -> bool {
    pdc_row("PdcSum".to_string(), "|TypeOccurrence".to_string())
}

pub fn witness_nullary_constructor_is_declared() -> bool {
    pdc_row(
        "PdcNullary".to_string(),
        "|ConstructorOccurrence".to_string(),
    )
}

pub fn witness_payload_constructor_is_declared() -> bool {
    pdc_row(
        "PdcPayload".to_string(),
        "|ConstructorOccurrence".to_string(),
    )
}

pub fn witness_product_field_is_declared() -> bool {
    pdc_row(
        "pdc_product_field".to_string(),
        "|FieldOccurrence".to_string(),
    )
}

pub fn witness_coproduct_payload_field_is_declared_at_its_declaration_site() -> bool {
    pdc_identity_row(
        "pdc_payload_field".to_string(),
        "|pdc_payload_field#FieldOccurrence#span:109-126#path:>app.pdc_matrix>PdcSum>PdcPayload"
            .to_string(),
    )
}

pub fn witness_pattern_field_position_is_not_a_declaration() -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => transport.declarations.clone().iter().cloned().fold(
            true,
            |acc: bool, declaration: Rc<DeclarationOccurrence>| {
                match pdc_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                    None => acc.clone(),
                    Some(entry) => {
                        (acc.clone()
                            && (entry
                                .projection
                                .clone()
                                .diagnostic_span
                                .clone()
                                .start
                                .clone()
                                != 394))
                    }
                }
            },
        ),
    }
}

pub fn witness_a_declaration_exists_at_the_coproduct_field_site() -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => transport.declarations.clone().iter().cloned().fold(
            false,
            |found: bool, declaration: Rc<DeclarationOccurrence>| {
                match pdc_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                    None => found.clone(),
                    Some(entry) => {
                        (found.clone()
                            || (entry
                                .projection
                                .clone()
                                .diagnostic_span
                                .clone()
                                .start
                                .clone()
                                == 109))
                    }
                }
            },
        ),
    }
}

pub fn witness_matrix_declares_exactly_its_two_named_types() -> bool {
    (pdc_matrix_category_count("TypeOccurrence".to_string()) == 2)
}

pub fn witness_no_non_type_item_form_becomes_a_type_declaration() -> bool {
    (pdc_non_type_category_count("TypeOccurrence".to_string()) == 0)
}

pub fn witness_non_type_item_fixture_declares_its_callables() -> bool {
    (pdc_non_type_category_count("CallableOccurrence".to_string()) == 2)
}
