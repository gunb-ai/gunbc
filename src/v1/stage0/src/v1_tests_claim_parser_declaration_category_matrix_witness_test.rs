// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.parser_declaration_category_matrix_witness_test

use self::PdcTransport::*;
use crate::std_occurrence_identity::OccurrenceCategory::{
    CallableOccurrence, ConstructorOccurrence, FieldOccurrence, LexicalValueOccurrence,
    MethodOccurrence, NamespaceSegmentOccurrence, TypeOccurrence,
};
pub use crate::std_occurrence_identity::{
    DeclarationOccurrence, OccurrenceCategory, OccurrenceIndexEntry, OccurrenceTransport,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
pub use crate::v1_gunbc_occurrence_binding_parser_walk::parse_authored_occurrence_binding_source;
pub use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource;
use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource::{
    ParsedOccurrenceBindingSourceReady, ParsedOccurrenceBindingSourceRefused,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn pdc_matrix_witness_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Parser declaration-category denominator. Pins, per authored declaration form, the OccurrenceCategory population the v1 parser mints — including the forms that mint nothing. Observation-only: no resolution, exposure, or binding claim. The zero rows are the load-bearing half; minting a category later must edit this file.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE (requires v1 parser pool): target/release/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/parser_declaration_category_matrix_witness_test.dag --functions witness_matrix_fixture_parses,witness_function_declaration_mints_callable,witness_second_function_declaration_mints_callable,witness_let_binder_mints_lexical_value,witness_pattern_field_name_mints_field,witness_pattern_binder_mints_lexical_value,witness_function_parameter_mints_lexical_value,witness_annotated_data_declaration_mints_nothing,witness_product_type_declaration_mints_nothing,witness_coproduct_type_declaration_mints_nothing,witness_nullary_constructor_mints_nothing,witness_payload_constructor_mints_nothing,witness_parser_mints_no_type_or_constructor_declarations".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_dissolve_on() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVE-ON: witness_layer_roots admits src/v1 for parser-transport witnesses, or the execution corpus carries per-entry source roots — then this recipe deletes and discovery enrollment suffices.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdc_matrix_source() -> String {
    (((((((((((((((((((((((("module app.pdc_matrix\n".to_string()
        + &"\n".to_string())
        + &"type PdcProduct {\n".to_string())
        + &"  pdc_product_field: Int\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"type PdcSum = PdcNullary | PdcPayload { pdc_payload_field: Int }\n"
            .to_string())
        + &"\n".to_string())
        + &"data pdc_annotated_datum: Int = 7\n".to_string())
        + &"\n".to_string())
        + &"fn pdc_function(pdc_parameter: Int) -> Int {\n".to_string())
        + &"  let pdc_let_binder = pdc_parameter\n".to_string())
        + &"  pdc_let_binder\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"fn pdc_matcher(pdc_scrutinee: PdcSum) -> Int {\n".to_string())
        + &"  match pdc_scrutinee {\n".to_string())
        + &"    PdcNullary => {\n".to_string())
        + &"      0\n".to_string())
        + &"    }\n".to_string())
        + &"    PdcPayload { pdc_payload_field: pdc_pattern_binder } => {\n".to_string())
        + &"      pdc_pattern_binder\n".to_string())
        + &"    }\n".to_string())
        + &"  }\n".to_string())
        + &"}\n".to_string())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum PdcTransport {
    PdcTransportReady { transport: Rc<OccurrenceTransport> },
    PdcTransportRefused,
}
impl PdcTransport {
    pub fn transport(&self) -> Rc<OccurrenceTransport> {
        match self {
            PdcTransport::PdcTransportReady {
                transport: __val, ..
            } => __val.clone(),
            PdcTransport::PdcTransportRefused => panic!("no transport on unit variant"),
        }
    }
}

pub fn pdc_parse_matrix() -> Rc<PdcTransport> {
    match (*parse_authored_occurrence_binding_source(
        "app/pdc_matrix.dag".to_string(),
        pdc_matrix_source(),
    ))
    .clone()
    {
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceRefused => {
            Rc::new(PdcTransport::PdcTransportRefused)
        }
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceReady { transport, .. } => {
            Rc::new(PdcTransport::PdcTransportReady {
                transport: transport.clone(),
            })
        }
    }
}

pub fn pdc_category_tag(category: OccurrenceCategory) -> String {
    match category.clone() {
        OccurrenceCategory::LexicalValueOccurrence => "LexicalValueOccurrence".to_string(),
        OccurrenceCategory::TypeOccurrence => "TypeOccurrence".to_string(),
        OccurrenceCategory::CallableOccurrence => "CallableOccurrence".to_string(),
        OccurrenceCategory::ConstructorOccurrence => "ConstructorOccurrence".to_string(),
        OccurrenceCategory::NamespaceSegmentOccurrence => "NamespaceSegmentOccurrence".to_string(),
        OccurrenceCategory::FieldOccurrence => "FieldOccurrence".to_string(),
        OccurrenceCategory::MethodOccurrence => "MethodOccurrence".to_string(),
    }
}

pub fn pdc_index_entry_for_declaration(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == declaration.occurrence.clone().value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn pdc_declared_tags_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, declaration: Rc<DeclarationOccurrence>| {
            match pdc_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![pdc_category_tag(declaration.category.clone())]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdc_render_tags(tags: Rc<Vec<String>>) -> String {
    tags.clone()
        .iter()
        .cloned()
        .fold("".to_string(), |acc: String, tag: String| {
            ((acc + &"|".to_string()) + &tag.clone())
        })
}

pub fn pdc_declares_exactly(
    transport: Rc<OccurrenceTransport>,
    name: String,
    expected: String,
) -> bool {
    (pdc_render_tags(pdc_declared_tags_for_name(transport.clone(), name.clone()))
        == expected.clone())
}

pub fn pdc_declares_nothing(transport: Rc<OccurrenceTransport>, name: String) -> bool {
    ((pdc_declared_tags_for_name(transport.clone(), name.clone()).len() as i64) == 0)
}

pub fn witness_matrix_fixture_parses() -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => {
            (((transport.declarations.clone().len() as i64) > 0)
                && ((transport.references.clone().len() as i64) > 0))
        }
    }
}

pub fn pdc_row(name: String, expected: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declares_exactly(transport.clone(), name.clone(), expected.clone()),
    }
}

pub fn pdc_zero_row(name: String) -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => pdc_declares_nothing(transport.clone(), name.clone()),
    }
}

pub fn witness_function_declaration_mints_callable() -> bool {
    pdc_row(
        "pdc_function".to_string(),
        "|CallableOccurrence".to_string(),
    )
}

pub fn witness_second_function_declaration_mints_callable() -> bool {
    pdc_row("pdc_matcher".to_string(), "|CallableOccurrence".to_string())
}

pub fn witness_let_binder_mints_lexical_value() -> bool {
    pdc_row(
        "pdc_let_binder".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_pattern_field_name_mints_field() -> bool {
    pdc_row(
        "pdc_payload_field".to_string(),
        "|FieldOccurrence".to_string(),
    )
}

pub fn witness_pattern_binder_mints_lexical_value() -> bool {
    pdc_row(
        "pdc_pattern_binder".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_function_parameter_mints_lexical_value() -> bool {
    pdc_row(
        "pdc_parameter".to_string(),
        "|LexicalValueOccurrence".to_string(),
    )
}

pub fn witness_annotated_data_declaration_mints_nothing() -> bool {
    pdc_zero_row("pdc_annotated_datum".to_string())
}

pub fn witness_product_type_declaration_mints_nothing() -> bool {
    pdc_zero_row("PdcProduct".to_string())
}

pub fn witness_coproduct_type_declaration_mints_nothing() -> bool {
    pdc_zero_row("PdcSum".to_string())
}

pub fn witness_nullary_constructor_mints_nothing() -> bool {
    pdc_zero_row("PdcNullary".to_string())
}

pub fn witness_payload_constructor_mints_nothing() -> bool {
    pdc_zero_row("PdcPayload".to_string())
}

pub fn witness_parser_mints_no_type_or_constructor_declarations() -> bool {
    match (*pdc_parse_matrix()).clone() {
        PdcTransport::PdcTransportRefused => false,
        PdcTransport::PdcTransportReady {
            transport: transport,
            ..
        } => transport.declarations.clone().iter().cloned().fold(
            true,
            |acc: bool, declaration: Rc<DeclarationOccurrence>| {
                ((acc
                    && (pdc_category_tag(declaration.category.clone())
                        != "TypeOccurrence".to_string()))
                    && (pdc_category_tag(declaration.category.clone())
                        != "ConstructorOccurrence".to_string()))
            },
        ),
    }
}
