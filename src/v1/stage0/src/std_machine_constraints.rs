// Generated by v1 compiler -- do not edit.
// Source module: std.machine_constraints

use self::WidthResolution::*;
pub use crate::std_constructors::Phantom;
pub use crate::std_dissolution::unbound_dissolution;
pub use crate::std_dissolution::DissolutionCondition;
use crate::std_dissolution::DissolutionCondition::*;
pub use crate::std_types::NonEmptyStr;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum WidthResolution {
    StaticWidthIndex,
    PointerWidth,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct MachineWidth<Bits>(pub std::marker::PhantomData<Bits>);

pub fn machine_width_literal_roster_frontier() -> Rc<DissolutionCondition> {
    thread_local! {
        static CACHED: Rc<DissolutionCondition> = {
            crate::std_dissolution::unbound_dissolution("Per-kind literal admissibility at a kinded type-argument position. A Nat literal is admitted by a language-level rule rather than by the kind's declared arms, so MachineWidth cannot express WHICH literals it admits and a nonsensical width typechecks exactly as a real one does. DISSOLVES ON a kind being able to declare its admissible literal domain -- SUFFICIENT FOR the wall to refuse a literal outside it at the same position and with the same located diagnostic it already gives a named non-inhabitant, so the literal and named halves reach one rung. Five carriers were tried and enumerated at this module's header; each fails because a literal is not a name, so admitting one needs a compiler-held convention mapping literals to an arm -- the stringly proxy this kind exists to remove. Not dissolved by adding an arm, and not by widening the wall to refuse every literal, which would refuse the four live MachineWidth sites the corpus writes as bare integers.".to_string())
        };
    }
    CACHED.with(|c: &Rc<DissolutionCondition>| c.clone())
}

pub type Compose<Algebra, MachineConstraint> =
    std::marker::PhantomData<(Algebra, MachineConstraint)>;

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct StaticWidthIndex;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct PointerWidth;
