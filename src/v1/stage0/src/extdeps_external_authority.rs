// Generated by v1 compiler -- do not edit.
// Source module: extdeps.external_authority

use self::ScopeAdmission::*;
use self::ScopeFinding::*;
use crate::extdeps_uri::UriScheme::File;
pub use crate::extdeps_uri::{Uri, UriScheme};
pub use crate::std_decl_ref::DeclarationRef;
pub use crate::std_types::NonEmptyStr;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExternalAuthority {
    pub uri: Rc<Uri>,
}

pub fn external_model_scope_law() -> String {
    thread_local! {
        static CACHED: String = {
            "DESIGN §3 'External upstream decomposition' (operator verdict on gunbc#7556): 'one concept, one authority' unifies a shared interface or formal shape; it does not merge independently governed entities that inhabit that shape. Each independently versioned upstream product, implementation, engine, vendor, or specification has its own extdeps module authority, declared here as one ExternalModelScope per module: ONE subject, at most one revision, one-or-more citations. A subject can have several citations; a module cannot silently have several independently governed subjects. A generic hub may define or re-export agnostic shapes but may not enumerate concrete products, dispatch among their authorities, carry product-specific version rows, or store consumer coverage state — those are the two refusal arms below. Observations produced by this repository are receipts in the observing product/workflow layer (downstream), never 'Unobserved' properties authored inside an upstream module.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn external_model_scope_citations_shape_note() -> String {
    thread_local! {
        static CACHED: String = {
            "citations is nonempty BY CONSTRUCTION (first_citation a required scalar + further_citations), the gunbc.doc_graph_roots primary_work/additional_works precedent — the operator sketch wrote NonEmptyList<ExternalAuthority>; no such generic carrier exists in std today, and the split-field shape makes the zero-citation scope unwritable rather than checked (DESIGN §5).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExternalSubjectRef {
    pub name: NonEmptyStr,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExternalRevision {
    pub label: NonEmptyStr,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExternalModelScope {
    pub subject: Rc<ExternalSubjectRef>,
    pub revision: Option<Rc<ExternalRevision>>,
    pub first_citation: Rc<ExternalAuthority>,
    pub further_citations: Rc<Vec<Rc<ExternalAuthority>>>,
}

pub fn external_model_scope_citations(
    scope: Rc<ExternalModelScope>,
) -> Rc<Vec<Rc<ExternalAuthority>>> {
    v1_rt::concat(
        Rc::new(vec![scope.first_citation.clone()]),
        scope.further_citations.clone(),
    )
}

pub fn external_subject_ref_eq(a: Rc<ExternalSubjectRef>, b: Rc<ExternalSubjectRef>) -> bool {
    (a.name.clone() == b.name.clone())
}

pub fn fact_authority_override_ref_note() -> String {
    thread_local! {
        static CACHED: String = {
            "FactAuthorityOverride.fact is a DeclarationRef, not prose (operator verdict on gunbc#7556; the §3 cite-the-symbol rule applied to the override carrier): the overridden fact is named by module + declaration in the containment tree's own key space, so a stale override is decidable by symbol resolution rather than rotting as free text.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FactAuthorityOverride {
    pub fact: Rc<DeclarationRef>,
    pub authority: Rc<ExternalAuthority>,
}

pub fn modeled_extdeps_module_facts_note() -> String {
    thread_local! {
        static CACHED: String = {
            "ModeledExtdepsModule is the admission gate's synthetic fact row for one extdeps module (the v2.lens.extdeps_shape_transport_policy lens-unit pattern): the module's declared scope, the per-row subject attribution of every product-/engine-/distribution-specific fact row it carries, and the names of any consumer-coverage/observation-state rows it stores. The gate is a pure fn over these rows; wiring the same verdict at the module-carrier grain is the staged mandatory-tag region named in gunbc.extdeps_scope_frontier.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ModeledExtdepsModule {
    pub module: NonEmptyStr,
    pub scope: Rc<ExternalModelScope>,
    pub product_fact_subjects: Rc<Vec<Rc<ExternalSubjectRef>>>,
    pub consumer_coverage_rows: Rc<Vec<NonEmptyStr>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ScopeFinding {
    ForeignSubjectRow {
        module: NonEmptyStr,
        declared: Rc<ExternalSubjectRef>,
        foreign: Rc<ExternalSubjectRef>,
    },
    ConsumerCoverageInUpstream {
        module: NonEmptyStr,
        coverage_row: NonEmptyStr,
    },
}
impl ScopeFinding {
    pub fn module(&self) -> NonEmptyStr {
        match self {
            ScopeFinding::ForeignSubjectRow { module: __val, .. } => __val.clone(),
            ScopeFinding::ConsumerCoverageInUpstream { module: __val, .. } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ScopeAdmission {
    ScopeAdmitted,
    ScopeRefused {
        finding_count: i64,
        findings: Rc<Vec<Rc<ScopeFinding>>>,
    },
}
impl ScopeAdmission {
    pub fn finding_count(&self) -> i64 {
        match self {
            ScopeAdmission::ScopeAdmitted => panic!("no finding_count on unit variant"),
            ScopeAdmission::ScopeRefused {
                finding_count: __val,
                ..
            } => __val.clone(),
        }
    }
    pub fn findings(&self) -> Rc<Vec<Rc<ScopeFinding>>> {
        match self {
            ScopeAdmission::ScopeAdmitted => panic!("no findings on unit variant"),
            ScopeAdmission::ScopeRefused {
                findings: __val, ..
            } => __val.clone(),
        }
    }
}

pub fn external_model_scope_foreign_subject_findings(
    m: Rc<ModeledExtdepsModule>,
) -> Rc<Vec<Rc<ScopeFinding>>> {
    Rc::new({
        let mut __result = Vec::new();
        for s in Rc::new({
            let mut __result = Vec::new();
            for s in m.product_fact_subjects.clone().iter().cloned() {
                if !external_subject_ref_eq(s.clone(), m.scope.clone().subject.clone()) {
                    __result.push(s);
                }
            }
            __result
        })
        .iter()
        .cloned()
        {
            __result.push(Rc::new(ScopeFinding::ForeignSubjectRow {
                module: m.module.clone(),
                declared: m.scope.clone().subject.clone(),
                foreign: s.clone(),
            }));
        }
        __result
    })
}

pub fn external_model_scope_coverage_findings(
    m: Rc<ModeledExtdepsModule>,
) -> Rc<Vec<Rc<ScopeFinding>>> {
    Rc::new({
        let mut __result = Vec::new();
        for r in m.consumer_coverage_rows.clone().iter().cloned() {
            __result.push(Rc::new(ScopeFinding::ConsumerCoverageInUpstream {
                module: m.module.clone(),
                coverage_row: r.clone(),
            }));
        }
        __result
    })
}

pub fn external_model_scope_findings(m: Rc<ModeledExtdepsModule>) -> Rc<Vec<Rc<ScopeFinding>>> {
    v1_rt::concat(
        external_model_scope_foreign_subject_findings(m.clone()),
        external_model_scope_coverage_findings(m.clone()),
    )
}

pub fn external_model_scope_admission_of_findings(
    findings: Rc<Vec<Rc<ScopeFinding>>>,
) -> Rc<ScopeAdmission> {
    {
        let n = (findings.clone().len() as i64);
        if (n.clone() == 0) {
            Rc::new(ScopeAdmission::ScopeAdmitted)
        } else {
            Rc::new(ScopeAdmission::ScopeRefused {
                finding_count: n.clone(),
                findings: findings.clone(),
            })
        }
    }
}

pub fn admit_external_model_scope(m: Rc<ModeledExtdepsModule>) -> Rc<ScopeAdmission> {
    external_model_scope_admission_of_findings(external_model_scope_findings(m.clone()))
}

pub fn admit_external_model_scope_portfolio(
    mods: Rc<Vec<Rc<ModeledExtdepsModule>>>,
) -> Rc<ScopeAdmission> {
    external_model_scope_admission_of_findings(mods.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<Rc<ScopeFinding>>>, m: Rc<ModeledExtdepsModule>| {
            v1_rt::concat(acc, external_model_scope_findings(m.clone()))
        },
    ))
}

pub fn downstream_support_roster_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Where the refused rows GO: a support/coverage roster is a downstream (product/workflow layer) fact join over the per-subject upstream modules — DESIGN §3's gunbc.served_surface_browser_support example. It is deliberately NOT gated by admit_external_model_scope: consumer policy and coverage state are exactly what an upstream module may not carry and a downstream module exists to carry.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DownstreamSupportRoster {
    pub module: NonEmptyStr,
    pub coverage_rows: Rc<Vec<NonEmptyStr>>,
}

pub fn extdeps_external_authority_anchor() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::File,
        locator: "DESIGN.md#3-single-authority".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}
