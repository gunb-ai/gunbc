// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.ordinary_frontend_observation_test

pub use crate::v1_compiler_compile::SourceFile;
pub use crate::v1_compiler_frontend_observation::observe_ordinary_frontend;
pub use crate::v1_compiler_frontend_observation::OrdinaryFrontendObservation;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_live_tree::LiveTreeDisposition;
use crate::v2_std_live_tree::LiveTreeDisposition::SubstrateInputsOnly;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn live_tree_disposition() -> LiveTreeDisposition {
    thread_local! {
        static CACHED: LiveTreeDisposition = {
            LiveTreeDisposition::SubstrateInputsOnly
        };
    }
    CACHED.with(|c: &LiveTreeDisposition| c.clone())
}

pub fn probe_keyed_literal() -> Rc<HashMap<String, bool>> {
    thread_local! {
        static CACHED: Rc<HashMap<String, bool>> = {
            let mut __m = HashMap::new();
            __m.insert("alpha".to_string(), true);
            __m.insert("beta".to_string(), false);
            __m.insert("gamma".to_string(), true);
            Rc::new(__m)
        };
    }
    CACHED.with(|c: &Rc<HashMap<String, bool>>| c.clone())
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ProbeRecord {
    pub alpha: bool,
    pub beta: bool,
}

pub fn probe_record_literal() -> ProbeRecord {
    thread_local! {
        static CACHED: ProbeRecord = {
            serde_json::from_value(serde_json::json!({"alpha": true, "beta": false}))
                .expect("valid data definition")
        };
    }
    CACHED.with(|c: &ProbeRecord| c.clone())
}

pub fn w_map_typed_literal_is_a_map() -> bool {
    {
        let keys = Rc::new({
            let mut __sorted: Vec<_> = Rc::new(v1_rt::map_keys(&probe_keyed_literal()))
                .iter()
                .cloned()
                .collect();
            __sorted.sort_by(|a: &String, b: &String| {
                let __ka = (|k: String| k.clone())(a.clone());
                let __kb = (|k: String| k.clone())(b.clone());
                __ka.partial_cmp(&__kb).unwrap_or(std::cmp::Ordering::Equal)
            });
            __sorted
        });
        ((((keys.clone().len() as i64) == 3)
            && (keys.clone()
                == Rc::new(vec![
                    "alpha".to_string(),
                    "beta".to_string(),
                    "gamma".to_string(),
                ])))
            && ((Rc::new({
                let mut __result = Vec::new();
                for v in Rc::new(v1_rt::map_values(&probe_keyed_literal()))
                    .iter()
                    .cloned()
                {
                    if v.clone() {
                        __result.push(v);
                    }
                }
                __result
            })
            .len() as i64)
                == 2))
    }
}

pub fn w_record_literal_is_still_a_record() -> bool {
    (probe_record_literal().alpha.clone() && !probe_record_literal().beta.clone())
}

pub fn tiny_subject() -> Rc<Vec<Rc<SourceFile>>> {
    Rc::new(vec![Rc::new(SourceFile {
        path: "tiny_subject.dag".to_string(),
        content: "module tiny.subject\nfn probe() -> Bool { true }\n".to_string(),
    })])
}

pub fn broken_subject() -> Rc<Vec<Rc<SourceFile>>> {
    Rc::new(vec![Rc::new(SourceFile {
        path: "broken_subject.dag".to_string(),
        content: "module tiny.broken\nfn probe( -> Bool { true }\n".to_string(),
    })])
}

pub fn w_ordinary_frontend_runs_on_a_tiny_subject() -> bool {
    {
        let observed = observe_ordinary_frontend(tiny_subject());
        (((((((observed.diagnostics.clone().len() as i64) == 0)
            && observed.receipt.clone().graph_present.clone())
            && (observed.receipt.clone().module_count.clone() == 1))
            && (observed.receipt.clone().occurrence_count.clone() > 0))
            && (observed.receipt.clone().declaration_count.clone() > 0))
            && ((observed.subject.clone().len() as i64) == 1))
    }
}

pub fn w_ordinary_frontend_reports_a_broken_subject() -> bool {
    {
        let observed = observe_ordinary_frontend(broken_subject());
        ((observed.diagnostics.clone().len() as i64) > 0)
    }
}
