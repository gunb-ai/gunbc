// Generated by v1 compiler -- do not edit.
// Source module: extdeps.shell

pub use crate::extdeps_external_authority::ExternalAuthority;
use crate::extdeps_uri::UriScheme::*;
pub use crate::extdeps_uri::{Uri, UriScheme};
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn extdeps_external_authority_anchor() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn find_files_and_symlinks_positional_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The script body is a FIXED literal owned here, and root arrives as the positional parameter $1 rather than being spliced into it — so no path can escape a quote, which is exactly what a caller-side concat into a single-quoted find string permitted (review 50535). The pipe to sort stays inside the operation because it is load-bearing: find walks directories in filesystem order, and the caller digests the resulting listing into a tree identity, so an unsorted read would make that identity depend on traversal order. It cannot move into the substrate today — std.primitives declares a sort contract but no callable sort exists (probed, 2026-08-08), which is the dissolve-on for folding this ordering back out of the shell. GNU find -printf keeps this operation Linux-only, per the caller's own linux_find_note.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn files_by_name_sorted_positional_note() -> String {
    thread_local! {
        static CACHED: String = {
            "shell.Find.FilesByNameSorted: root and name_glob arrive as positional parameters ($1, $2) rather than being spliced into the script body, for the same reason as FilesAndSymlinksWithMode above — no path or glob can escape a quote. The pipe to sort stays inside the operation because the caller digests the listing into a manifest and needs traversal-order independence (SHELL-DAG dissolution of package_delivery.dag's protocol-schema-bundle file listing, previously a retained_foreign concat-built find | sort script).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn socket_inode_holder_bracket_note() -> String {
    thread_local! {
        static CACHED: String = {
            "WHY THE PATTERN SPELLS THE BRACKETS AS `?` RATHER THAN ESCAPING THEM. A /proc fd symlink for a socket reads `socket:[N]`, and find's -lname argument is a GLOB, in which `[...]` is a character class -- so the obvious `-lname \"socket:[N]\"` silently matches nothing at all, which is the worst possible failure for a probe whose empty result is load-bearing evidence (it is exactly the clause that licenses unlinking a stale socket). Escaping the brackets works but puts backslashes through two layers of quoting; `?` matches the one literal bracket character in each position and is length-anchored, so `socket:?N?` matches `socket:[N]` and cannot match a different inode. Confirmed by execution 2026-08-21 against a live listening socket, WITH the negative control that a non-existent inode returns empty -- a probe whose only observed state is `found nothing` proves nothing, so the pair is the measurement.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone)]
pub struct ShellFind {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellFind {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellFind {
            working_dir: None,
            dry_run,
        }
    }

    /// Modifiers: readonly
    pub async fn list_dirs(
        &self,
        path: String,
        max_depth: i64,
        min_depth: i64,
    ) -> Result<(Rc<Vec<String>>), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] ListDirs");
            panic!("no mock data available for dry-run operation: ListDirs")
        } else {
            let output = std::process::Command::new("find")
                .arg(&format!("{}", path))
                .arg("-maxdepth")
                .arg(&format!("{}", max_depth))
                .arg("-mindepth")
                .arg(&format!("{}", min_depth))
                .arg("-type")
                .arg("d")
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn files_and_symlinks_with_mode(
        &self,
        root: String,
    ) -> Result<(bool, String, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] FilesAndSymlinksWithMode");
            panic!("no mock data available for dry-run operation: FilesAndSymlinksWithMode")
        } else {
            let output = std::process::Command::new("sh")
                .arg("-c")
                .arg("find \"$1\" \\( -type f -o -type l \\) -printf '%y\\t%m\\t%P\\t%l\\n' | sort")
                .arg("sh")
                .arg(&format!("{}", root))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Ok((output.status.success(), stdout.clone(), stderr.clone()))
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn files(
        &self,
        workdir: String,
        root: String,
        name_glob: String,
    ) -> Result<(bool, String, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Files");
            panic!("no mock data available for dry-run operation: Files")
        } else {
            let output = std::process::Command::new("env")
                .arg("-C")
                .arg(&format!("{}", workdir))
                .arg("find")
                .arg(&format!("{}", root))
                .arg("-name")
                .arg(&format!("{}", name_glob))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Ok((output.status.success(), stdout.clone(), stderr.clone()))
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn socket_inode_holders(
        &self,
        inode: String,
    ) -> Result<(bool, String, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] SocketInodeHolders");
            panic!("no mock data available for dry-run operation: SocketInodeHolders")
        } else {
            let output = std::process::Command::new("find")
                .arg("/proc")
                .arg("-maxdepth")
                .arg("3")
                .arg("-path")
                .arg("*/fd/*")
                .arg("-lname")
                .arg(&format!("socket:?{}?", inode))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Ok((output.status.success(), stdout.clone(), stderr.clone()))
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn files_by_name_sorted(
        &self,
        root: String,
        name_glob: String,
    ) -> Result<(bool, String, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] FilesByNameSorted");
            panic!("no mock data available for dry-run operation: FilesByNameSorted")
        } else {
            let output = std::process::Command::new("sh")
                .arg("-c")
                .arg("find \"$1\" -type f -name \"$2\" | sort")
                .arg("sh")
                .arg(&format!("{}", root))
                .arg(&format!("{}", name_glob))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Ok((output.status.success(), stdout.clone(), stderr.clone()))
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellEnv {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellEnv {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellEnv {
            working_dir: None,
            dry_run,
        }
    }

    /// Modifiers: readonly
    pub async fn get(&self, name: String) -> Result<(Option<String>), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Get");
            panic!("no mock data available for dry-run operation: Get")
        } else {
            let output = std::process::Command::new("printenv")
                .arg(&format!("{}", name))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Some(stdout.clone()),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

pub fn posix_command_v_semantic_note() -> String {
    thread_local! {
        static CACHED: String = {
            "`command -v` is a POSIX shell builtin (IEEE 1003.1-2017 section 2.9.1.1, Command Search and Execution, invoked here through `sh -c` rather than by name), present wherever a POSIX-conforming shell is -- unlike `which`, an EXTERNAL BINARY (part of debianutils on Debian/Ubuntu, not part of POSIX or a base busybox install) that may be absent on stripped appliance images, BMC firmware shells (Dropbear), or minimal containers. This operation's own assumption is not unconditional either: it still requires a POSIX shell at `sh` on the target's PATH. gunbc.fleet_intent's srv1-4 ComputeHost, and gunbc.ci_fleet's own CI/falsifier runner, both carry an `OperatingSystemSurface { distro_or_product: none, ... }` -- no distro or package-set authority anywhere in this fleet, our own runner included (crisp-wren-896 finding, PR #8590; the modeling gap is named at std.os.types operating_system_surface_package_authority_gap_note). This operation's assumption strictly dominates `which`'s -- every target that can run `which` can run `command -v`, and the converse fails on busybox and stripped images -- which is why the former shell.Which.Check operation was deleted rather than kept as a second, narrower-scope choice (crisp-wren-896, PR #8590): a selection rule between two operations is only worth its cost when each is right somewhere the other is wrong, and here nothing was. If even the POSIX-shell assumption cannot be established for a target, the honest realization is a typed refusal, not a silent fall-through inheriting this operation's result as ground truth.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn posix_command_v_argv_construction_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The probed command name is carried in its OWN argv slot (`{command}` bound to the shell's trailing positional parameter), never concatenated into the `-c` script text -- the same `-c script scriptname arg1` positional-parameter binding shell.Find.FilesAndSymlinksWithMode already uses. The value never touches the string parsed as shell code, so there is no escaping table to maintain and no quoting authority this operation depends on.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone)]
pub struct ShellPosixCommandV {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellPosixCommandV {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellPosixCommandV {
            working_dir: None,
            dry_run,
        }
    }

    /// Modifiers: readonly
    pub async fn check(
        &self,
        command: String,
    ) -> Result<(bool, Option<String>), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Check");
            panic!("no mock data available for dry-run operation: Check")
        } else {
            let output = std::process::Command::new("sh")
                .arg("-c")
                .arg("command -v \"$1\"")
                .arg("sh")
                .arg(&format!("{}", command))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((output.status.success(), Some(stdout.clone()))),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

pub fn posix_command_v_check_argv(command: String) -> Rc<Vec<String>> {
    Rc::new(vec![
        "sh".to_string(),
        "-c".to_string(),
        "command -v \"$1\"".to_string(),
        "sh".to_string(),
        command.clone(),
    ])
}

#[derive(Debug, Clone)]
pub struct ShellTest {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellTest {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellTest {
            working_dir: None,
            dry_run,
        }
    }

    /// Modifiers: readonly
    pub async fn is_executable(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] IsExecutable");
            panic!("no mock data available for dry-run operation: IsExecutable")
        } else {
            let output = std::process::Command::new("test")
                .arg("-x")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn is_non_empty(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] IsNonEmpty");
            panic!("no mock data available for dry-run operation: IsNonEmpty")
        } else {
            let output = std::process::Command::new("test")
                .arg("-s")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn is_file(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] IsFile");
            panic!("no mock data available for dry-run operation: IsFile")
        } else {
            let output = std::process::Command::new("test")
                .arg("-f")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn is_directory(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] IsDirectory");
            panic!("no mock data available for dry-run operation: IsDirectory")
        } else {
            let output = std::process::Command::new("test")
                .arg("-d")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    /// Modifiers: readonly
    pub async fn is_writable(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] IsWritable");
            panic!("no mock data available for dry-run operation: IsWritable")
        } else {
            let output = std::process::Command::new("test")
                .arg("-w")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                1 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

pub fn shell_test_is_executable_argv(path: String) -> Rc<Vec<String>> {
    Rc::new(vec!["test".to_string(), "-x".to_string(), path.clone()])
}

#[derive(Debug, Clone)]
pub struct ShellUname {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellUname {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellUname {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn kernel_name(&self) -> Result<(String, bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] KernelName");
            panic!("no mock data available for dry-run operation: KernelName")
        } else {
            let output = std::process::Command::new("uname")
                .arg("-s")
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((stdout.clone(), output.status.success())),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn machine(&self) -> Result<(String, bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Machine");
            panic!("no mock data available for dry-run operation: Machine")
        } else {
            let output = std::process::Command::new("uname")
                .arg("-m")
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((stdout.clone(), output.status.success())),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellMktemp {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellMktemp {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellMktemp {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn file_in_directory(
        &self,
        dir: String,
    ) -> Result<(String, bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] FileInDirectory");
            panic!("no mock data available for dry-run operation: FileInDirectory")
        } else {
            let output = std::process::Command::new("mktemp")
                .arg("-p")
                .arg(&format!("{}", dir))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((stdout.clone(), output.status.success())),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn dir(&self) -> Result<(String, bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Dir");
            panic!("no mock data available for dry-run operation: Dir")
        } else {
            let output = std::process::Command::new("mktemp")
                .arg("-d")
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((stdout.clone(), output.status.success())),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn dir_with_template(
        &self,
        template: String,
    ) -> Result<(String, bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] DirWithTemplate");
            panic!("no mock data available for dry-run operation: DirWithTemplate")
        } else {
            let output = std::process::Command::new("mktemp")
                .arg("-d")
                .arg(&format!("{}", template))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => Ok((stdout.clone(), output.status.success())),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellMkdir {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellMkdir {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellMkdir {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn parents(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Parents");
            panic!("no mock data available for dry-run operation: Parents")
        } else {
            let output = std::process::Command::new("mkdir")
                .arg("-p")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellLink {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellLink {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellLink {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn hard(
        &self,
        source: String,
        destination: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Hard");
            panic!("no mock data available for dry-run operation: Hard")
        } else {
            let output = std::process::Command::new("ln")
                .arg(&format!("{}", source))
                .arg(&format!("{}", destination))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellSymlink {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellSymlink {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellSymlink {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn atomic_replace(
        &self,
        target: String,
        link: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] AtomicReplace");
            panic!("no mock data available for dry-run operation: AtomicReplace")
        } else {
            let output = std::process::Command::new("ln")
                .arg("-sfnT")
                .arg(&format!("{}", target))
                .arg(&format!("{}", link))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellRemove {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellRemove {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellRemove {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn file_force(&self, path: String) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] FileForce");
            panic!("no mock data available for dry-run operation: FileForce")
        } else {
            let output = std::process::Command::new("rm")
                .arg("-f")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn recursive_force(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] RecursiveForce");
            panic!("no mock data available for dry-run operation: RecursiveForce")
        } else {
            let output = std::process::Command::new("rm")
                .arg("-rf")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn empty_directory(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] EmptyDirectory");
            panic!("no mock data available for dry-run operation: EmptyDirectory")
        } else {
            let output = std::process::Command::new("rmdir")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellMove {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellMove {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellMove {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn file(
        &self,
        source: String,
        destination: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] File");
            panic!("no mock data available for dry-run operation: File")
        } else {
            let output = std::process::Command::new("mv")
                .arg(&format!("{}", source))
                .arg(&format!("{}", destination))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn no_replace_directory(
        &self,
        source: String,
        destination: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] NoReplaceDirectory");
            panic!("no mock data available for dry-run operation: NoReplaceDirectory")
        } else {
            let output = std::process::Command::new("mv")
                .arg("-T")
                .arg(&format!("{}", source))
                .arg(&format!("{}", destination))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellChmod {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellChmod {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellChmod {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn owner_only_directory(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] OwnerOnlyDirectory");
            panic!("no mock data available for dry-run operation: OwnerOnlyDirectory")
        } else {
            let output = std::process::Command::new("chmod")
                .arg("0700")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn owner_read_write_file(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] OwnerReadWriteFile");
            panic!("no mock data available for dry-run operation: OwnerReadWriteFile")
        } else {
            let output = std::process::Command::new("chmod")
                .arg("0600")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn recursive_writable(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] RecursiveWritable");
            panic!("no mock data available for dry-run operation: RecursiveWritable")
        } else {
            let output = std::process::Command::new("chmod")
                .arg("-R")
                .arg("u+w")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn recursive_read_only(
        &self,
        path: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] RecursiveReadOnly");
            panic!("no mock data available for dry-run operation: RecursiveReadOnly")
        } else {
            let output = std::process::Command::new("chmod")
                .arg("-R")
                .arg("a-w")
                .arg(&format!("{}", path))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellCopy {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellCopy {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellCopy {
            working_dir: None,
            dry_run,
        }
    }

    pub async fn file(
        &self,
        source: String,
        destination: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] File");
            panic!("no mock data available for dry-run operation: File")
        } else {
            let output = std::process::Command::new("cp")
                .arg(&format!("{}", source))
                .arg(&format!("{}", destination))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }

    pub async fn recursive(
        &self,
        source: String,
        destination: String,
    ) -> Result<(bool), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Recursive");
            panic!("no mock data available for dry-run operation: Recursive")
        } else {
            let output = std::process::Command::new("cp")
                .arg("-r")
                .arg(&format!("{}", source))
                .arg(&format!("{}", destination))
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => stdout.clone(),
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}

#[derive(Debug, Clone)]
pub struct ShellGCloud {
    pub working_dir: Option<String>,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl ShellGCloud {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        ShellGCloud {
            working_dir: None,
            dry_run,
        }
    }

    /// Modifiers: readonly
    pub async fn auth_print_access_token(
        &self,
    ) -> Result<(String, bool, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] AuthPrintAccessToken");
            panic!("no mock data available for dry-run operation: AuthPrintAccessToken")
        } else {
            let output = std::process::Command::new("gcloud")
                .arg("auth")
                .arg("print-access-token")
                .current_dir(self.working_dir.as_deref().unwrap_or("."))
                .output()?;
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let exit_code = output.status.code().unwrap_or(-1);
            match exit_code {
                0 => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Ok((stdout.clone(), output.status.success(), stderr.clone()))
                }
                _ => {
                    let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                    Err(stderr)
                }
            }
        }
    }
}
