// Generated by v1 compiler -- do not edit.
// Source module: extdeps.uri

use self::ParsedHrefScheme::*;
use self::UriScheme::*;
pub use crate::std_types::NonEmptyStr;
use crate::v1_rt;
use crate::v1_rt::Witness;
use crate::v1_rt::Witness::{Holds, Violates};
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im_rc::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum UriScheme {
    Http,
    Https,
    File,
    Ftp,
    Javascript,
    Data,
    Vbscript,
    Mailto,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Uri {
    pub scheme: UriScheme,
    pub locator: NonEmptyStr,
}

pub fn uri_scheme_is_http(s: UriScheme) -> bool {
    match s {
        UriScheme::Http => true,
        UriScheme::Https => true,
        UriScheme::File => false,
        UriScheme::Ftp => false,
        UriScheme::Javascript => false,
        UriScheme::Data => false,
        UriScheme::Vbscript => false,
        UriScheme::Mailto => false,
    }
}

pub fn uri_is_url(uri: Rc<Uri>) -> bool {
    uri_scheme_is_http(uri.scheme.clone())
}

pub fn uri_scheme_wire(s: UriScheme) -> String {
    match s {
        UriScheme::Http => "http://".to_string(),
        UriScheme::Https => "https://".to_string(),
        UriScheme::File => "file://".to_string(),
        UriScheme::Ftp => "ftp://".to_string(),
        UriScheme::Javascript => "javascript:".to_string(),
        UriScheme::Data => "data:".to_string(),
        UriScheme::Vbscript => "vbscript:".to_string(),
        UriScheme::Mailto => "mailto:".to_string(),
    }
}

pub fn uri_wire(uri: Rc<Uri>) -> String {
    v1_rt::concat(uri_scheme_wire(uri.scheme.clone()), uri.locator.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ParsedHrefScheme {
    HrefScheme { scheme: UriScheme },
    RelativeHref,
    UnknownHref,
}
impl ParsedHrefScheme {
    pub fn scheme(&self) -> UriScheme {
        match self {
            ParsedHrefScheme::HrefScheme { scheme: __val, .. } => __val.clone(),
            ParsedHrefScheme::RelativeHref => panic!("no scheme on unit variant"),
            ParsedHrefScheme::UnknownHref => panic!("no scheme on unit variant"),
        }
    }
}

pub fn href_is_relative_reference(s: String) -> bool {
    ((((v1_rt::starts_with(s.clone(), "/".to_string())
        || v1_rt::starts_with(s.clone(), "#".to_string()))
        || v1_rt::starts_with(s.clone(), "./".to_string()))
        || v1_rt::starts_with(s.clone(), "../".to_string()))
        || !v1_rt::contains(s.clone(), ":".to_string()))
}

pub fn parse_href_scheme(url: String) -> Rc<ParsedHrefScheme> {
    {
        let s = v1_rt::trim(url);
        if v1_rt::starts_with(s.clone(), "//".to_string()) {
            Rc::new(ParsedHrefScheme::UnknownHref)
        } else {
            if href_is_relative_reference(s.clone()) {
                Rc::new(ParsedHrefScheme::RelativeHref)
            } else {
                if (v1_rt::starts_with(s.clone(), "javascript:".to_string())
                    || v1_rt::starts_with(s.clone(), "JAVASCRIPT:".to_string()))
                {
                    Rc::new(ParsedHrefScheme::HrefScheme {
                        scheme: UriScheme::Javascript,
                    })
                } else {
                    if (v1_rt::starts_with(s.clone(), "data:".to_string())
                        || v1_rt::starts_with(s.clone(), "DATA:".to_string()))
                    {
                        Rc::new(ParsedHrefScheme::HrefScheme {
                            scheme: UriScheme::Data,
                        })
                    } else {
                        if (v1_rt::starts_with(s.clone(), "vbscript:".to_string())
                            || v1_rt::starts_with(s.clone(), "VBSCRIPT:".to_string()))
                        {
                            Rc::new(ParsedHrefScheme::HrefScheme {
                                scheme: UriScheme::Vbscript,
                            })
                        } else {
                            if (v1_rt::starts_with(s.clone(), "https://".to_string())
                                || v1_rt::starts_with(s.clone(), "HTTPS://".to_string()))
                            {
                                Rc::new(ParsedHrefScheme::HrefScheme {
                                    scheme: UriScheme::Https,
                                })
                            } else {
                                if (v1_rt::starts_with(s.clone(), "http://".to_string())
                                    || v1_rt::starts_with(s.clone(), "HTTP://".to_string()))
                                {
                                    Rc::new(ParsedHrefScheme::HrefScheme {
                                        scheme: UriScheme::Http,
                                    })
                                } else {
                                    if (v1_rt::starts_with(s.clone(), "mailto:".to_string())
                                        || v1_rt::starts_with(s.clone(), "MAILTO:".to_string()))
                                    {
                                        Rc::new(ParsedHrefScheme::HrefScheme {
                                            scheme: UriScheme::Mailto,
                                        })
                                    } else {
                                        Rc::new(ParsedHrefScheme::UnknownHref)
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Http;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Https;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct File;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Ftp;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Javascript;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Data;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Vbscript;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Mailto;
