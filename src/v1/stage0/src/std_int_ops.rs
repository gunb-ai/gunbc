// Generated by v1 compiler -- do not edit.
// Source module: std.int_ops

use crate::v1_rt;
use crate::v1_rt::Witness;
use crate::v1_rt::Witness::{Holds, Violates};
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im_rc::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn int_min(a: i64, b: i64) -> i64 {
    if (a.clone() < b.clone()) {
        a.clone()
    } else {
        b.clone()
    }
}

pub fn int_max(a: i64, b: i64) -> i64 {
    if (a.clone() > b.clone()) {
        a.clone()
    } else {
        b.clone()
    }
}

pub fn int_ceil_div(numerator: i64, denominator: i64) -> Option<i64> {
    if (denominator.clone() <= 0) {
        None
    } else {
        if (numerator.clone() <= 0) {
            Some(0)
        } else {
            Some((((numerator.clone() + denominator.clone()) - 1) / denominator.clone()))
        }
    }
}
