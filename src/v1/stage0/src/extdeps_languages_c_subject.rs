// Generated by v1 compiler -- do not edit.
// Source module: extdeps.languages.c.subject

use self::C::*;
pub use crate::extdeps_external_authority::{
    ExternalAuthority, ExternalModelScope, ExternalSubjectRef,
};
use crate::extdeps_uri::UriScheme::*;
pub use crate::extdeps_uri::{Uri, UriScheme};
use crate::std_decl_ref::DeclField::*;
pub use crate::std_decl_ref::{DeclField, DeclarationRef};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn extdeps_external_authority_anchor() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Uri {
        scheme: UriScheme::Https,
        locator: "www.open-std.org/jtc1/sc22/wg14/".to_string(),
    },
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn c_language_subject_note() -> String {
    thread_local! {
        static CACHED: String = {
            "THE C LANGUAGE, cited to ISO/IEC JTC1/SC22/WG14 (verified live 2026-08-04 by fetching the cited locator and confirming its title and publisher from the response, per the D0 verify-or-refuse rule): the fetched committee page identifies itself as 'the international standardization working group for the programming language C' and designates the standard as 'ISO/IEC 9899: Programming Language C', noting C23 was adopted by ISO and IEC in 2024. WHY THE COMMITTEE PAGE AND NOT iso.org: the ISO catalogue refused this fetch with HTTP 403, so an iso.org locator could not be verified and is therefore not carried — the committee page is the authority that WAS readable, and citing what was actually verified is the rule this whole batch follows.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum C {
    CTarget,
}

pub fn c_language() -> C {
    thread_local! {
        static CACHED: C = {
            C::CTarget
        };
    }
    CACHED.with(|c: &C| c.clone())
}

pub fn c_language_model_scope() -> Rc<ExternalModelScope> {
    thread_local! {
            static CACHED: Rc<ExternalModelScope> = {
                Rc::new(ExternalModelScope {
        subject: ExternalSubjectRef {
        declaration: DeclarationRef {
        module_path: "extdeps.languages.c.subject".to_string(),
        decl_name: "c_language".to_string(),
        field: Rc::new(DeclField::WholeDeclaration),
    },
    },
        first_citation: crate::extdeps_languages_c_subject::extdeps_external_authority_anchor(),
        further_citations: Rc::new(vec![]),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalModelScope>| c.clone())
}

pub fn extdeps_model_scope() -> Rc<ExternalModelScope> {
    thread_local! {
        static CACHED: Rc<ExternalModelScope> = {
            c_language_model_scope()
        };
    }
    CACHED.with(|c: &Rc<ExternalModelScope>| c.clone())
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct CTarget;
