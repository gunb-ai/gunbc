// Generated by v1 compiler -- do not edit.
// Source module: std.occurrence_identity

use self::AuthoredOccurrence::*;
use self::AuthoredOccurrenceRole::*;
use self::DeclarationOccurrenceProjection::*;
use self::NodeOccurrenceIdentity::*;
use self::ReferenceOccurrenceProjection::*;
pub use crate::std_types::Bool;
use crate::std_types::Bool::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn occurrence_identity_scope_law() -> String {
    thread_local! {
        static CACHED: String = {
            "AuthoredOccurrenceId values are unique only inside one explicit graph-scoped occurrence space. Production parsing assigns every source a disjoint authored-token ordinal range. Identity is the packed authored-token ordinal plus a typed edge role; filename, SourceSpan, authored name, structural Node equality, content hash, and module-local reset are forbidden identity inputs.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn occurrence_identity_constructor_spelling_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Shared NodeOccurrenceIdentity and its neutral constructors are collision-free because .dag type and variant names are closure-global and signatures cannot qualify a colliding type: legacy v2 NodeOccurrenceId/SyntheticOccurrence/MintedOccurrence and identically named shared declarations cannot coexist in one conversion closure. Spelling is representation; this shared carrier remains the sole semantic authority.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn occurrence_identity_slots_per_authored_token() -> i64 {
    4
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AuthoredOccurrenceId {
    pub value: i64,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum NodeOccurrenceIdentity {
    OccurrenceSynthetic,
    OccurrenceMinted { id: AuthoredOccurrenceId },
}
impl NodeOccurrenceIdentity {
    pub fn id(&self) -> AuthoredOccurrenceId {
        match self {
            NodeOccurrenceIdentity::OccurrenceSynthetic => panic!("no id on unit variant"),
            NodeOccurrenceIdentity::OccurrenceMinted { id: __val, .. } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DeclarationOccurrence {
    pub identity: AuthoredOccurrenceId,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ReferenceOccurrence {
    pub identity: AuthoredOccurrenceId,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum AuthoredOccurrence {
    AuthoredDeclarationOccurrence {
        occurrence: Rc<DeclarationOccurrence>,
    },
    AuthoredReferenceOccurrence {
        occurrence: Rc<ReferenceOccurrence>,
    },
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum AuthoredOccurrenceRole {
    DeclarationOccurrenceRole,
    PrimaryReferenceOccurrenceRole,
    LabelReferenceOccurrenceRole,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum DeclarationOccurrenceProjection {
    DeclarationOccurrenceAccepted {
        occurrence: Rc<DeclarationOccurrence>,
    },
    DeclarationOccurrenceWrongCategory {
        identity: AuthoredOccurrenceId,
    },
    DeclarationOccurrenceMalformed {
        identity: AuthoredOccurrenceId,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ReferenceOccurrenceProjection {
    ReferenceOccurrenceAccepted { occurrence: Rc<ReferenceOccurrence> },
    ReferenceOccurrenceWrongCategory { identity: AuthoredOccurrenceId },
    ReferenceOccurrenceMalformed { identity: AuthoredOccurrenceId },
}

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AuthoredOccurrenceIdAllocator {
    pub next_id: i64,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AuthoredOccurrenceIdAllocResult {
    pub id: AuthoredOccurrenceId,
    pub alloc: AuthoredOccurrenceIdAllocator,
}

pub fn authored_occurrence_id_allocator_initial() -> AuthoredOccurrenceIdAllocator {
    AuthoredOccurrenceIdAllocator { next_id: 0 }
}

pub fn alloc_authored_occurrence_id(
    alloc: AuthoredOccurrenceIdAllocator,
) -> Rc<AuthoredOccurrenceIdAllocResult> {
    Rc::new(AuthoredOccurrenceIdAllocResult {
        id: AuthoredOccurrenceId {
            value: alloc.next_id.clone(),
        },
        alloc: AuthoredOccurrenceIdAllocator {
            next_id: (alloc.next_id.clone() + 1),
        },
    })
}

pub fn authored_occurrence_id_eq(left: AuthoredOccurrenceId, right: AuthoredOccurrenceId) -> bool {
    (left.value.clone() == right.value.clone())
}

pub fn authored_occurrence_id_allocator_advance_to(
    alloc: AuthoredOccurrenceIdAllocator,
    min_next: i64,
) -> AuthoredOccurrenceIdAllocator {
    if (alloc.next_id.clone() < min_next.clone()) {
        AuthoredOccurrenceIdAllocator {
            next_id: min_next.clone(),
        }
    } else {
        alloc
    }
}

pub fn authored_occurrence_role_slot(role: AuthoredOccurrenceRole) -> i64 {
    match role.clone() {
        AuthoredOccurrenceRole::DeclarationOccurrenceRole => 0,
        AuthoredOccurrenceRole::PrimaryReferenceOccurrenceRole => 1,
        AuthoredOccurrenceRole::LabelReferenceOccurrenceRole => 2,
    }
}

pub fn authored_occurrence_identity(
    token_ordinal: i64,
    role: AuthoredOccurrenceRole,
) -> AuthoredOccurrenceId {
    AuthoredOccurrenceId {
        value: ((token_ordinal.clone() * occurrence_identity_slots_per_authored_token())
            + authored_occurrence_role_slot(role.clone())),
    }
}

pub fn declaration_occurrence_for_token(token_ordinal: i64) -> Rc<DeclarationOccurrence> {
    Rc::new(DeclarationOccurrence {
        identity: authored_occurrence_identity(
            token_ordinal.clone(),
            AuthoredOccurrenceRole::DeclarationOccurrenceRole,
        ),
    })
}

pub fn primary_reference_occurrence_for_token(token_ordinal: i64) -> Rc<ReferenceOccurrence> {
    Rc::new(ReferenceOccurrence {
        identity: authored_occurrence_identity(
            token_ordinal.clone(),
            AuthoredOccurrenceRole::PrimaryReferenceOccurrenceRole,
        ),
    })
}

pub fn label_reference_occurrence_for_token(token_ordinal: i64) -> Rc<ReferenceOccurrence> {
    Rc::new(ReferenceOccurrence {
        identity: authored_occurrence_identity(
            token_ordinal.clone(),
            AuthoredOccurrenceRole::LabelReferenceOccurrenceRole,
        ),
    })
}

pub fn authored_declaration_occurrence(
    occurrence: Rc<DeclarationOccurrence>,
) -> Rc<AuthoredOccurrence> {
    Rc::new(AuthoredOccurrence::AuthoredDeclarationOccurrence {
        occurrence: occurrence.clone(),
    })
}

pub fn authored_reference_occurrence(
    occurrence: Rc<ReferenceOccurrence>,
) -> Rc<AuthoredOccurrence> {
    Rc::new(AuthoredOccurrence::AuthoredReferenceOccurrence {
        occurrence: occurrence.clone(),
    })
}

pub fn occurrence_identity_slot(identity: AuthoredOccurrenceId) -> i64 {
    (identity.value.clone() % occurrence_identity_slots_per_authored_token())
}

pub fn declaration_occurrence_from_identity(
    identity: AuthoredOccurrenceId,
) -> Rc<DeclarationOccurrenceProjection> {
    if ((identity.value.clone() < 0) || (occurrence_identity_slot(identity.clone()) == 3)) {
        Rc::new(
            DeclarationOccurrenceProjection::DeclarationOccurrenceMalformed {
                identity: identity.clone(),
            },
        )
    } else {
        if (occurrence_identity_slot(identity.clone()) == 0) {
            Rc::new(
                DeclarationOccurrenceProjection::DeclarationOccurrenceAccepted {
                    occurrence: Rc::new(DeclarationOccurrence {
                        identity: identity.clone(),
                    }),
                },
            )
        } else {
            Rc::new(
                DeclarationOccurrenceProjection::DeclarationOccurrenceWrongCategory {
                    identity: identity.clone(),
                },
            )
        }
    }
}

pub fn reference_occurrence_from_identity(
    identity: AuthoredOccurrenceId,
) -> Rc<ReferenceOccurrenceProjection> {
    if ((identity.value.clone() < 0) || (occurrence_identity_slot(identity.clone()) == 3)) {
        Rc::new(
            ReferenceOccurrenceProjection::ReferenceOccurrenceMalformed {
                identity: identity.clone(),
            },
        )
    } else {
        if (occurrence_identity_slot(identity.clone()) == 0) {
            Rc::new(
                ReferenceOccurrenceProjection::ReferenceOccurrenceWrongCategory {
                    identity: identity.clone(),
                },
            )
        } else {
            Rc::new(ReferenceOccurrenceProjection::ReferenceOccurrenceAccepted {
                occurrence: Rc::new(ReferenceOccurrence {
                    identity: identity.clone(),
                }),
            })
        }
    }
}

pub fn occurrence_node_minted(id: AuthoredOccurrenceId) -> Rc<NodeOccurrenceIdentity> {
    Rc::new(NodeOccurrenceIdentity::OccurrenceMinted { id: id.clone() })
}

pub fn occurrence_node_minted_from_value(value: i64) -> Rc<NodeOccurrenceIdentity> {
    occurrence_node_minted(AuthoredOccurrenceId {
        value: value.clone(),
    })
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct DeclarationOccurrenceRole;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct PrimaryReferenceOccurrenceRole;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct LabelReferenceOccurrenceRole;
