// Generated by v1 compiler -- do not edit.
// Source module: std.unicode.types

use self::CharClass::*;
use self::DisplayWidth::*;
pub use crate::std_types::Char;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn unicode_scalar_max_code_point() -> i64 {
    1114111
}

pub fn unicode_surrogate_first_code_point() -> i64 {
    55296
}

pub fn unicode_surrogate_last_code_point() -> i64 {
    57343
}

pub fn unicode_scalar_authority_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Unicode Standard 17.0 Core Specification section 3.9, definition D76 (https://www.unicode.org/versions/Unicode17.0.0/core-spec/chapter-3/#G7404): scalar values are U+0000..U+D7FF and U+E000..U+10FFFF inclusive. Therefore U+10FFFF is the maximum and the surrogate code points U+D800..U+DFFF are excluded.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn unicode_scalar(code_point: i64) -> bool {
    (((code_point.clone() >= 0) && (code_point.clone() <= unicode_scalar_max_code_point()))
        && !((code_point.clone() >= unicode_surrogate_first_code_point())
            && (code_point.clone() <= unicode_surrogate_last_code_point())))
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum CharClass {
    Whitespace,
    Digit,
    IdentStart,
    IdentContinue,
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum DisplayWidth {
    ZeroWidth,
    Narrow,
    Wide,
}

pub fn display_width_columns(w: DisplayWidth) -> i64 {
    match w.clone() {
        DisplayWidth::ZeroWidth => 0,
        DisplayWidth::Narrow => 1,
        DisplayWidth::Wide => 2,
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct UnicodeBlock {
    pub name: String,
    pub start: i64,
    pub end_inclusive: i64,
    pub default_width: DisplayWidth,
}

pub fn char_in_class(c: i64, class: CharClass) -> bool {
    match class.clone() {
        CharClass::Whitespace => {
            (((((unicode_char_code_point(c.clone()) == 9)
                || (unicode_char_code_point(c.clone()) == 10))
                || (unicode_char_code_point(c.clone()) == 12))
                || (unicode_char_code_point(c.clone()) == 13))
                || (unicode_char_code_point(c.clone()) == 32))
        }
        CharClass::Digit => {
            ((unicode_char_code_point(c.clone()) >= 48)
                && (unicode_char_code_point(c.clone()) <= 57))
        }
        CharClass::IdentStart => {
            ((((unicode_char_code_point(c.clone()) >= 65)
                && (unicode_char_code_point(c.clone()) <= 90))
                || ((unicode_char_code_point(c.clone()) >= 97)
                    && (unicode_char_code_point(c.clone()) <= 122)))
                || (unicode_char_code_point(c.clone()) == 95))
        }
        CharClass::IdentContinue => {
            (((((unicode_char_code_point(c.clone()) >= 48)
                && (unicode_char_code_point(c.clone()) <= 57))
                || ((unicode_char_code_point(c.clone()) >= 65)
                    && (unicode_char_code_point(c.clone()) <= 90)))
                || ((unicode_char_code_point(c.clone()) >= 97)
                    && (unicode_char_code_point(c.clone()) <= 122)))
                || (unicode_char_code_point(c.clone()) == 95))
        }
    }
}

pub fn unicode_char_code_point(c: i64) -> i64 {
    (c.clone() + 0)
}

pub fn in_block(cp: i64, block: Rc<UnicodeBlock>) -> bool {
    ((cp.clone() >= block.start.clone()) && (cp.clone() <= block.end_inclusive.clone()))
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Whitespace;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Digit;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IdentStart;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IdentContinue;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct ZeroWidth;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Narrow;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Wide;
