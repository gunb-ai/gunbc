// Generated by v1 compiler -- do not edit.
// Source module: std.workspace_artifact

use self::FootprintProvenance::*;
use self::IgnoreReason::*;
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum IgnoreReason {
    RegenerableFromSource,
    LocalCacheState,
    PerDeveloperState,
    HostOsMetadata,
    SessionRuntimeState,
    MayContainSecrets,
    OutsideSubstrate,
    SupersededAuthority { canonical: String },
}
impl IgnoreReason {
    pub fn canonical(&self) -> String {
        match self {
            IgnoreReason::RegenerableFromSource => panic!("no canonical on unit variant"),
            IgnoreReason::LocalCacheState => panic!("no canonical on unit variant"),
            IgnoreReason::PerDeveloperState => panic!("no canonical on unit variant"),
            IgnoreReason::HostOsMetadata => panic!("no canonical on unit variant"),
            IgnoreReason::SessionRuntimeState => panic!("no canonical on unit variant"),
            IgnoreReason::MayContainSecrets => panic!("no canonical on unit variant"),
            IgnoreReason::OutsideSubstrate => panic!("no canonical on unit variant"),
            IgnoreReason::SupersededAuthority {
                canonical: __val, ..
            } => __val.clone(),
        }
    }
}

pub fn ignore_reason_label(r: Rc<IgnoreReason>) -> String {
    match (*r.clone()).clone() {
        IgnoreReason::RegenerableFromSource => "regenerable from committed source".to_string(),
        IgnoreReason::LocalCacheState => "local cache; not project state".to_string(),
        IgnoreReason::PerDeveloperState => "per-developer state; not shared".to_string(),
        IgnoreReason::HostOsMetadata => "host OS metadata; not project state".to_string(),
        IgnoreReason::SessionRuntimeState => {
            "session/runtime state; not source-of-truth".to_string()
        }
        IgnoreReason::MayContainSecrets => "may contain secrets; never committed".to_string(),
        IgnoreReason::OutsideSubstrate => "outside the committed substrate".to_string(),
        IgnoreReason::SupersededAuthority { canonical: c, .. } => {
            v1_rt::concat("superseded; canonical home: ".to_string(), c.clone())
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WorkspaceArtifact {
    pub pattern: String,
    pub meaning: String,
    pub reason: Rc<IgnoreReason>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum FootprintProvenance {
    CitedUpstream {
        display: String,
        upstream: String,
    },
    RepoTool {
        display: String,
        owner_module: String,
    },
}
impl FootprintProvenance {
    pub fn display(&self) -> String {
        match self {
            FootprintProvenance::CitedUpstream { display: __val, .. } => __val.clone(),
            FootprintProvenance::RepoTool { display: __val, .. } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WorkspaceFootprint {
    pub provenance: Rc<FootprintProvenance>,
    pub artifacts: Rc<Vec<Rc<WorkspaceArtifact>>>,
}

pub fn footprint_display(p: Rc<FootprintProvenance>) -> String {
    match (*p.clone()).clone() {
        FootprintProvenance::CitedUpstream {
            display: d,
            upstream: u,
            ..
        } => v1_rt::concat(
            d.clone(),
            v1_rt::concat(
                " [cited: ".to_string(),
                v1_rt::concat(u.clone(), "]".to_string()),
            ),
        ),
        FootprintProvenance::RepoTool {
            display: d,
            owner_module: m,
            ..
        } => v1_rt::concat(
            d.clone(),
            v1_rt::concat(
                " [owner: ".to_string(),
                v1_rt::concat(m.clone(), "]".to_string()),
            ),
        ),
    }
}
