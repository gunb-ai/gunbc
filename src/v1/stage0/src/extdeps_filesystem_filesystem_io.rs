// Generated by v1 compiler -- do not edit.
// Source module: extdeps.filesystem.filesystem_io

pub use crate::extdeps_external_authority::ExternalAuthority;
use crate::extdeps_uri::UriScheme::Https;
pub use crate::extdeps_uri::{Uri, UriScheme};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn extdeps_external_authority_anchor() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "pubs.opengroup.org/onlinepubs/9699919799/functions/write.html".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

#[derive(Debug, Clone)]
pub struct Filesystem {
    pub base_path: String,

    pub dry_run: crate::dry_run::DryRunMode,
}

impl Filesystem {
    pub fn new(dry_run: crate::dry_run::DryRunMode) -> Self {
        Filesystem {
            base_path: ".".to_string(),
            dry_run,
        }
    }

    pub async fn write(
        &self,
        path: String,
        content: String,
    ) -> Result<(bool, i64, String, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Write");
            panic!("no mock data available for dry-run operation: Write")
        } else {
            let path = format!("{}/{}", self.base_path, "write");
            let content = std::fs::read_to_string(&path)?;
            let parsed: serde_json::Value = serde_json::from_str(&content)?;
            Ok(serde_json::from_value(parsed)?)
        }
    }

    /// Modifiers: readonly
    pub async fn read(
        &self,
        path: String,
    ) -> Result<(String, bool, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Read");
            panic!("no mock data available for dry-run operation: Read")
        } else {
            let path = format!("{}/{}", self.base_path, "read");
            let content = std::fs::read_to_string(&path)?;
            let parsed: serde_json::Value = serde_json::from_str(&content)?;
            Ok(serde_json::from_value(parsed)?)
        }
    }

    pub async fn delete(&self, path: String) -> Result<(bool, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] Delete");
            panic!("no mock data available for dry-run operation: Delete")
        } else {
            let path = format!("{}/{}", self.base_path, "delete");
            let content = std::fs::read_to_string(&path)?;
            let parsed: serde_json::Value = serde_json::from_str(&content)?;
            Ok(serde_json::from_value(parsed)?)
        }
    }

    /// Modifiers: readonly
    pub async fn list(
        &self,
        path: String,
    ) -> Result<(String, bool, String), Box<dyn std::error::Error>> {
        if self.dry_run.is_dry_run() {
            eprintln!("[dry-run] List");
            panic!("no mock data available for dry-run operation: List")
        } else {
            let path = format!("{}/{}", self.base_path, "list");
            let content = std::fs::read_to_string(&path)?;
            let parsed: serde_json::Value = serde_json::from_str(&content)?;
            Ok(serde_json::from_value(parsed)?)
        }
    }
}
