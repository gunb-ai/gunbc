// Generated by v1 compiler -- do not edit.
// Source module: v2.std.collection

use self::ListAtLookup::*;
pub use crate::extdeps_languages_c_subject::C;
use crate::extdeps_languages_c_subject::C::*;
pub use crate::std_primitives::PrimitiveContract;
pub use crate::std_primitives::{empty_map_contract, map_insert_contract};
pub use crate::std_types::{List, Map};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_algebra::fold_list;
use crate::v2_std_diagnostic::Correction::*;
use crate::v2_std_diagnostic::NoCorrectionReason::*;
use crate::v2_std_diagnostic::Outcome::*;
pub use crate::v2_std_diagnostic::{outcome_accepted, port_locus};
pub use crate::v2_std_diagnostic::{Correction, Diagnostic, NoCorrectionReason, Outcome};
pub use crate::v2_std_optional::Optional;
pub use crate::v2_std_optional::{optional_absent, optional_present};
pub use crate::v2_std_witness::witness_from_optional;
pub use crate::v2_std_witness::Witness;
use crate::v2_std_witness::Witness::*;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn optional_absent_unwrap_diagnostic() -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "optional_absent_unwrap".to_string(),
        at: port_locus("optional_present_witness_port".to_string()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn optional_present_witness<T: Clone>(opt: Optional<T>) -> Rc<Witness<T>> {
    witness_from_optional(opt.clone(), optional_absent_unwrap_diagnostic())
}

pub fn empty_map_host_binding() -> Rc<PrimitiveContract> {
    thread_local! {
        static CACHED: Rc<PrimitiveContract> = {
            crate::std_primitives::empty_map_contract()
        };
    }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

pub fn map_insert_host_binding() -> Rc<PrimitiveContract> {
    thread_local! {
        static CACHED: Rc<PrimitiveContract> = {
            crate::std_primitives::map_insert_contract()
        };
    }
    CACHED.with(|c: &Rc<PrimitiveContract>| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IndexedLookup<T: Clone> {
    pub index: i64,
    pub value: Rc<Witness<T>>,
    pub _phantom: std::marker::PhantomData<T>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ListAtLookup<T> {
    ListAtMissing { remaining: i64 },
    ListAtFound { value: T },
}

pub fn empty_map_primitive_delegate<K, V>() -> Rc<HashMap<K, V>> {
    loop {
        {
            continue;
        }
    }
}

pub fn empty_map<K, V>() -> Rc<HashMap<K, V>> {
    empty_map_primitive_delegate()
}

pub fn map_insert<K: Clone, V: Clone>(m: Rc<HashMap<K, V>>, key: K, value: V) -> Rc<HashMap<K, V>> {
    PartialFunction {
        lookup: |candidate| match (candidate.clone() == key.clone()) {
            true => Optional::Present {
                value: value.clone(),
            },
            false => v1_rt::lookup(&m, candidate.clone()),
        },
    }
}

pub fn map_lookup<K: Clone, V: Clone>(m: Rc<HashMap<K, V>>, key: K) -> Optional<V> {
    v1_rt::lookup(&m, key.clone())
}

pub fn map_get<K: Clone, V: Clone>(m: Rc<HashMap<K, V>>, key: K) -> Rc<Outcome<Optional<V>>> {
    outcome_accepted(map_lookup(m.clone(), key.clone()))
}

pub fn list_nth<T: Clone>(xs: Rc<Vec<T>>, wanted: i64, absent: Rc<Diagnostic>) -> Rc<Witness<T>> {
    fold_list(
        xs.clone(),
        Rc::new(IndexedLookup {
            index: 0,
            value: Rc::new(Witness::<T>::Violates {
                diagnostic: absent.clone(),
            }),
            _phantom: std::marker::PhantomData,
        }),
        |acc, item| {
            Rc::new(IndexedLookup {
                index: (acc.index.clone() + 1),
                value: if (acc.index.clone() == wanted.clone()) {
                    Rc::new(Witness::<T>::Holds {
                        value: item.clone(),
                    })
                } else {
                    acc.value.clone()
                },
                _phantom: std::marker::PhantomData,
            })
        },
    )
    .value
    .clone()
}

pub fn list_at_optional<T: Clone>(xs: Rc<Vec<T>>, index: i64) -> Optional<T> {
    match (*fold_list(
        xs.clone(),
        Rc::new(ListAtLookup::ListAtMissing {
            remaining: index.clone(),
        }),
        |acc, item| match (*acc.clone()).clone() {
            ListAtLookup::ListAtFound { value: _, .. } => acc.clone(),
            ListAtLookup::ListAtMissing { remaining: r, .. } => {
                if (r.clone() == 0) {
                    Rc::new(ListAtLookup::ListAtFound {
                        value: item.clone(),
                    })
                } else {
                    Rc::new(ListAtLookup::ListAtMissing {
                        remaining: (r.clone() - 1),
                    })
                }
            }
        },
    ))
    .clone()
    {
        ListAtLookup::ListAtFound { value: v, .. } => {
            crate::v2_std_optional::optional_present(v.clone())
        }
        ListAtLookup::ListAtMissing { remaining: _, .. } => {
            crate::v2_std_optional::optional_absent()
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FiniteSet<T: Clone> {
    pub head: T,
    pub tail: Rc<Vec<T>>,
    pub _phantom: std::marker::PhantomData<T>,
}

#[derive(Clone)]
pub struct TotalMap<K, V> {
    pub lookup: Rc<dyn Fn(K) -> V>,
    pub _phantom: std::marker::PhantomData<(K, V)>,
}

#[derive(Clone)]
pub struct TotalPolicy<K, Context, RowTemplate> {
    pub lookup: Rc<dyn Fn(K, Context) -> RowTemplate>,
    pub _phantom: std::marker::PhantomData<(K, Context, RowTemplate)>,
}
