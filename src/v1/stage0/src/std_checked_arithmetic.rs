// Generated by v1 compiler -- do not edit.
// Source module: std.checked_arithmetic

use self::CheckedInt::*;
use self::CheckedIntOperands::*;
use self::CheckedNat::*;
use self::IntegerArithmeticOperation::*;
pub use crate::std_nat::Nat;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn checked_arithmetic_bound_note() -> String {
    thread_local! {
        static CACHED: String = {
            "The bound is stated as i64's inclusive range because that is what the seed and the emitted Rust actually use. It is one literal pair here, replacing the six copies that were open-coded inside std.induction's checked operations - the same constant repeated per call site is the redundancy DESIGN section 2 names, and a repeated bound is worse than most because a single stale copy is a silently wrong answer rather than a compile error. GROUNDING FRONTIER, stated rather than implied: extdeps.languages.rust.primitives already carries i64's range as a cited row (range_min_inclusive / range_max_inclusive), and extdeps.languages.go.primitives carries the same numbers for its own int64. This module does NOT yet consume those rows, because they are String-typed wire values on language-surface rows and decoding them is its own increment. Until it does, this is a second representation of a cited fact - declared debt, not a claim of grounding.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn int_inclusive_max() -> i64 {
    9223372036854775807
}

pub fn int_inclusive_min() -> i64 {
    ((0 - int_inclusive_max()) - 1)
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum IntegerArithmeticOperation {
    IntegerAdd,
    IntegerSubtract,
    IntegerMultiply,
    IntegerNegate,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CheckedIntOperands {
    UnaryOperand { value: i64 },
    BinaryOperands { lhs: i64, rhs: i64 },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct IntegerOverflow {
    pub operation: IntegerArithmeticOperation,
    pub operands: Rc<CheckedIntOperands>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CheckedInt {
    CheckedIntReady { value: i64 },
    CheckedIntOverflow { cause: Rc<IntegerOverflow> },
}

pub fn checked_int_add(a: i64, b: i64) -> Rc<CheckedInt> {
    if (b.clone() > 0) {
        if (a.clone() > (int_inclusive_max() - b.clone())) {
            Rc::new(CheckedInt::CheckedIntOverflow {
                cause: Rc::new(IntegerOverflow {
                    operation: IntegerArithmeticOperation::IntegerAdd,
                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                        lhs: a.clone(),
                        rhs: b.clone(),
                    }),
                }),
            })
        } else {
            Rc::new(CheckedInt::CheckedIntReady {
                value: (a.clone() + b.clone()),
            })
        }
    } else {
        if (b.clone() < 0) {
            if (a.clone() < (int_inclusive_min() - b.clone())) {
                Rc::new(CheckedInt::CheckedIntOverflow {
                    cause: Rc::new(IntegerOverflow {
                        operation: IntegerArithmeticOperation::IntegerAdd,
                        operands: Rc::new(CheckedIntOperands::BinaryOperands {
                            lhs: a.clone(),
                            rhs: b.clone(),
                        }),
                    }),
                })
            } else {
                Rc::new(CheckedInt::CheckedIntReady {
                    value: (a.clone() + b.clone()),
                })
            }
        } else {
            Rc::new(CheckedInt::CheckedIntReady { value: a.clone() })
        }
    }
}

pub fn checked_int_subtract(a: i64, b: i64) -> Rc<CheckedInt> {
    if (b.clone() > 0) {
        if (a.clone() < (int_inclusive_min() + b.clone())) {
            Rc::new(CheckedInt::CheckedIntOverflow {
                cause: Rc::new(IntegerOverflow {
                    operation: IntegerArithmeticOperation::IntegerSubtract,
                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                        lhs: a.clone(),
                        rhs: b.clone(),
                    }),
                }),
            })
        } else {
            Rc::new(CheckedInt::CheckedIntReady {
                value: (a.clone() - b.clone()),
            })
        }
    } else {
        if (b.clone() < 0) {
            if (a.clone() > (int_inclusive_max() + b.clone())) {
                Rc::new(CheckedInt::CheckedIntOverflow {
                    cause: Rc::new(IntegerOverflow {
                        operation: IntegerArithmeticOperation::IntegerSubtract,
                        operands: Rc::new(CheckedIntOperands::BinaryOperands {
                            lhs: a.clone(),
                            rhs: b.clone(),
                        }),
                    }),
                })
            } else {
                Rc::new(CheckedInt::CheckedIntReady {
                    value: (a.clone() - b.clone()),
                })
            }
        } else {
            Rc::new(CheckedInt::CheckedIntReady { value: a.clone() })
        }
    }
}

pub fn checked_int_negate(a: i64) -> Rc<CheckedInt> {
    if (a.clone() == int_inclusive_min()) {
        Rc::new(CheckedInt::CheckedIntOverflow {
            cause: Rc::new(IntegerOverflow {
                operation: IntegerArithmeticOperation::IntegerNegate,
                operands: Rc::new(CheckedIntOperands::UnaryOperand { value: a.clone() }),
            }),
        })
    } else {
        Rc::new(CheckedInt::CheckedIntReady {
            value: (0 - a.clone()),
        })
    }
}

pub fn checked_int_multiply(a: i64, b: i64) -> Rc<CheckedInt> {
    if (b.clone() == 0) {
        Rc::new(CheckedInt::CheckedIntReady { value: 0 })
    } else {
        if (a.clone() == 0) {
            Rc::new(CheckedInt::CheckedIntReady { value: 0 })
        } else {
            if (b.clone() == (0 - 1)) {
                if (a.clone() == int_inclusive_min()) {
                    Rc::new(CheckedInt::CheckedIntOverflow {
                        cause: Rc::new(IntegerOverflow {
                            operation: IntegerArithmeticOperation::IntegerMultiply,
                            operands: Rc::new(CheckedIntOperands::BinaryOperands {
                                lhs: a.clone(),
                                rhs: b.clone(),
                            }),
                        }),
                    })
                } else {
                    Rc::new(CheckedInt::CheckedIntReady {
                        value: (0 - a.clone()),
                    })
                }
            } else {
                if (b.clone() > 0) {
                    if (a.clone() > 0) {
                        if (a.clone() > (int_inclusive_max() / b.clone())) {
                            Rc::new(CheckedInt::CheckedIntOverflow {
                                cause: Rc::new(IntegerOverflow {
                                    operation: IntegerArithmeticOperation::IntegerMultiply,
                                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                                        lhs: a.clone(),
                                        rhs: b.clone(),
                                    }),
                                }),
                            })
                        } else {
                            Rc::new(CheckedInt::CheckedIntReady {
                                value: (a.clone() * b.clone()),
                            })
                        }
                    } else {
                        if (a.clone() < (int_inclusive_min() / b.clone())) {
                            Rc::new(CheckedInt::CheckedIntOverflow {
                                cause: Rc::new(IntegerOverflow {
                                    operation: IntegerArithmeticOperation::IntegerMultiply,
                                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                                        lhs: a.clone(),
                                        rhs: b.clone(),
                                    }),
                                }),
                            })
                        } else {
                            Rc::new(CheckedInt::CheckedIntReady {
                                value: (a.clone() * b.clone()),
                            })
                        }
                    }
                } else {
                    if (a.clone() > 0) {
                        if (b.clone() < (int_inclusive_min() / a.clone())) {
                            Rc::new(CheckedInt::CheckedIntOverflow {
                                cause: Rc::new(IntegerOverflow {
                                    operation: IntegerArithmeticOperation::IntegerMultiply,
                                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                                        lhs: a.clone(),
                                        rhs: b.clone(),
                                    }),
                                }),
                            })
                        } else {
                            Rc::new(CheckedInt::CheckedIntReady {
                                value: (a.clone() * b.clone()),
                            })
                        }
                    } else {
                        if (b.clone() < (int_inclusive_max() / a.clone())) {
                            Rc::new(CheckedInt::CheckedIntOverflow {
                                cause: Rc::new(IntegerOverflow {
                                    operation: IntegerArithmeticOperation::IntegerMultiply,
                                    operands: Rc::new(CheckedIntOperands::BinaryOperands {
                                        lhs: a.clone(),
                                        rhs: b.clone(),
                                    }),
                                }),
                            })
                        } else {
                            Rc::new(CheckedInt::CheckedIntReady {
                                value: (a.clone() * b.clone()),
                            })
                        }
                    }
                }
            }
        }
    }
}

pub fn checked_int_optional(r: Rc<CheckedInt>) -> Option<i64> {
    match (*r.clone()).clone() {
        CheckedInt::CheckedIntReady { value: v, .. } => Some(v.clone()),
        CheckedInt::CheckedIntOverflow { cause: _, .. } => None,
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CheckedNat {
    CheckedNatReady { value: Nat },
    CheckedNatOverflow { cause: Rc<IntegerOverflow> },
}

pub fn checked_int_magnitude(a: i64) -> Rc<CheckedNat> {
    if (a.clone() == int_inclusive_min()) {
        Rc::new(CheckedNat::CheckedNatOverflow {
            cause: Rc::new(IntegerOverflow {
                operation: IntegerArithmeticOperation::IntegerNegate,
                operands: Rc::new(CheckedIntOperands::UnaryOperand { value: a.clone() }),
            }),
        })
    } else {
        Rc::new(CheckedNat::CheckedNatReady {
            value: nat_magnitude(a.clone()),
        })
    }
}

pub fn nat_magnitude_residue_note() -> String {
    thread_local! {
        static CACHED: String = {
            "WHY THIS IS A SEPARATE FUNCTION, and what remains unguarded about it. The Int-to-Nat crossing is admitted at a function RETURN position (std.coercion grounded_primitive_coproduct_identities registers Int -> Nat as an identity: a Nat IS an Int, grounded construction-side by the numeric tower), but not in a record-field position, so the crossing cannot happen inside the CheckedNatReady literal and has to be its own declaration. THE RESIDUE, named rather than left for a reader to find: nat_magnitude is correct for every input except the minimum representable value, whose magnitude is one past the maximum and where the realization's negation overflows. checked_int_magnitude is the sanctioned entry and refuses exactly that input. This is strictly better than the int_to_nat_nonnegative it replaces - no input maps to a fabricated zero, and the one unrepresentable case has a typed refusal rather than a silent clamp - but it is NOT a construction wall: nothing stops a caller invoking nat_magnitude directly. DISSOLVE-ON: a record-field-position crossing (which makes the guard and the crossing one expression) or sole_constructor over the magnitude carrier.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn nat_magnitude(a: i64) -> Nat {
    if (a.clone() < 0) {
        (0 - a.clone())
    } else {
        a.clone()
    }
}

pub fn checked_add_results(a: Rc<CheckedInt>, b: Rc<CheckedInt>) -> Rc<CheckedInt> {
    match (*a.clone()).clone() {
        CheckedInt::CheckedIntOverflow { cause: c, .. } => {
            Rc::new(CheckedInt::CheckedIntOverflow { cause: c.clone() })
        }
        CheckedInt::CheckedIntReady { value: av, .. } => match (*b.clone()).clone() {
            CheckedInt::CheckedIntOverflow { cause: c, .. } => {
                Rc::new(CheckedInt::CheckedIntOverflow { cause: c.clone() })
            }
            CheckedInt::CheckedIntReady { value: bv, .. } => {
                checked_int_add(av.clone(), bv.clone())
            }
        },
    }
}

pub fn checked_subtract_results(a: Rc<CheckedInt>, b: Rc<CheckedInt>) -> Rc<CheckedInt> {
    match (*a.clone()).clone() {
        CheckedInt::CheckedIntOverflow { cause: c, .. } => {
            Rc::new(CheckedInt::CheckedIntOverflow { cause: c.clone() })
        }
        CheckedInt::CheckedIntReady { value: av, .. } => match (*b.clone()).clone() {
            CheckedInt::CheckedIntOverflow { cause: c, .. } => {
                Rc::new(CheckedInt::CheckedIntOverflow { cause: c.clone() })
            }
            CheckedInt::CheckedIntReady { value: bv, .. } => {
                checked_int_subtract(av.clone(), bv.clone())
            }
        },
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerAdd;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerSubtract;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerMultiply;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct IntegerNegate;
