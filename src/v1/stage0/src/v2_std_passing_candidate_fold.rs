// Generated by v1 compiler -- do not edit.
// Source module: v2.std.passing_candidate_fold

use self::PassingCandidateFold::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn passing_candidate_fold_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Zero / one / many passing candidates. The candidate is a TYPE PARAMETER because the concept is about the CARDINALITY of a search result, not about what was searched for -- it was declared over Node when every consumer happened to fold over Node, and that accident became load-bearing the moment a consumer folded over an admitted carrier instead. Parameterising is the same move std/integer.dag makes for machine widths: one axis, not one type per inhabitant.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum PassingCandidateFold<T> {
    NoPassingCandidate,
    OnePassingCandidate { candidate: T },
    ManyPassingCandidates,
}
impl<T> PassingCandidateFold<T> {
    pub fn candidate(&self) -> T {
        match self {
            PassingCandidateFold::NoPassingCandidate => panic!("no candidate on unit variant"),
            PassingCandidateFold::OnePassingCandidate {
                candidate: __val, ..
            } => __val.clone(),
            PassingCandidateFold::ManyPassingCandidates => panic!("no candidate on unit variant"),
        }
    }
}
