// Generated by v1 compiler -- do not edit.
// Source module: extdeps.languages.rust.derive_contracts

pub use crate::extdeps_external_authority::ExternalAuthority;
use crate::extdeps_uri::UriScheme::Https;
pub use crate::extdeps_uri::{Uri, UriScheme};
use crate::std_decl_ref::DeclField::WholeDeclaration;
pub use crate::std_decl_ref::{DeclField, DeclarationRef};
use crate::std_disposition::ConstructionMechanism::SingleAuthority;
use crate::std_disposition::Disposition::Scaffold;
pub use crate::std_disposition::{ConstructionMechanism, Disposition};
pub use crate::std_trait_derive_shape::ReprGroundingDeriveTrait;
use crate::std_trait_derive_shape::ReprGroundingDeriveTrait::{
    ReprDeriveClone, ReprDeriveDebug, ReprDeriveDeserialize, ReprDeriveOrd, ReprDerivePartialEq,
    ReprDeriveSerialize,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn extdeps_external_authority_anchor() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/crate/im/15.1.0/source/src/vector/mod.rs#1701".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn im_15_1_0_vector_debug_impl_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/im/15.1.0/im/vector/struct.Vector.html#impl-Debug-for-Vector%3CA%3E".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn im_15_1_0_vector_clone_impl_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/crate/im/15.1.0/source/src/vector/mod.rs#1686".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn im_15_1_0_vector_partial_eq_impl_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/crate/im/15.1.0/source/src/vector/mod.rs#1716".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn im_15_1_0_vector_deserialize_impl_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/crate/im/15.1.0/source/src/ser.rs#235".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn im_15_1_0_vector_serialize_impl_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/crate/im/15.1.0/source/src/ser.rs#244".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn rust_1_93_btree_set_trait_implementations_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "doc.rust-lang.org/1.93.0/std/collections/struct.BTreeSet.html#trait-implementations".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn rust_1_93_hash_set_trait_implementations_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "doc.rust-lang.org/1.93.0/std/collections/struct.HashSet.html#trait-implementations".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn serde_1_0_228_btree_set_serialize_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/serde/1.0.228/serde/ser/trait.Serialize.html#impl-Serialize-for-BTreeSet%3CT%3E".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn serde_1_0_228_btree_set_deserialize_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/serde/1.0.228/serde/trait.Deserialize.html#impl-Deserialize%3C'de%3E-for-BTreeSet%3CT%3E".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn serde_1_0_228_hash_set_serialize_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/serde/1.0.228/serde/ser/trait.Serialize.html#impl-Serialize-for-HashSet%3CT,+H%3E".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn serde_1_0_228_hash_set_deserialize_authority() -> Rc<ExternalAuthority> {
    thread_local! {
            static CACHED: Rc<ExternalAuthority> = {
                Rc::new(ExternalAuthority {
        uri: Rc::new(Uri {
        scheme: UriScheme::Https,
        locator: "docs.rs/serde/1.0.228/serde/trait.Deserialize.html#impl-Deserialize%3C'de%3E-for-HashSet%3CT,+S%3E".to_string(),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<ExternalAuthority>| c.clone())
}

pub fn rust_vec_freemonoid_supplemental_generic_bound_rows_note() -> String {
    thread_local! {
        static CACHED: String = {
            "SINGLE AUTHORITY for the trait -> supplemental-generic-bound facts of the FreeMonoid/Vec realization (im-15.1.0 im::Vector): deriving Debug, PartialEq, Serialize or Deserialize for an item carrying an im::Vector<P> field additionally requires P: Clone, because each of those upstream impls is conditional on A: Clone (cited per row). Clone itself is deliberately NOT a row: derive(Clone) emits a conditional impl whose own A: Clone requirement is exactly im::Vector's, so no supplemental fact exists for it (im_15_1_0_vector_clone_impl_authority documents the upstream impl, consumed by the v2 contract map's requirement-free Clone entry). This module is dag-rooted so BOTH pools read one authority: the v1 seed emitter (regen pool [src/v1, dag]) consumes these rows in v1.compiler.trait_derive_emit, and v2.extdeps.languages.rust builds its typed target_model contract map (rust_vec_supplemental_generic_bound_contracts) by folding these same rows — the mapping formerly restated there is collapsed onto this list (coordinating-session ruling, smart-ram-730 (a session, not the operator) 2026-08-19: consume, do not mint a second copy or a witness to police one).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RustVecSupplementalGenericBoundRow {
    pub derive_trait: ReprGroundingDeriveTrait,
    pub required: ReprGroundingDeriveTrait,
    pub authority: Rc<ExternalAuthority>,
}

pub fn rust_vec_freemonoid_supplemental_generic_bound_rows(
) -> Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>> {
    thread_local! {
            static CACHED: Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>> = {
                Rc::new(vec![Rc::new(RustVecSupplementalGenericBoundRow {
        derive_trait: ReprGroundingDeriveTrait::ReprDeriveDebug,
        required: ReprGroundingDeriveTrait::ReprDeriveClone,
        authority: im_15_1_0_vector_debug_impl_authority(),
    }), Rc::new(RustVecSupplementalGenericBoundRow {
        derive_trait: ReprGroundingDeriveTrait::ReprDerivePartialEq,
        required: ReprGroundingDeriveTrait::ReprDeriveClone,
        authority: im_15_1_0_vector_partial_eq_impl_authority(),
    }), Rc::new(RustVecSupplementalGenericBoundRow {
        derive_trait: ReprGroundingDeriveTrait::ReprDeriveSerialize,
        required: ReprGroundingDeriveTrait::ReprDeriveClone,
        authority: im_15_1_0_vector_serialize_impl_authority(),
    }), Rc::new(RustVecSupplementalGenericBoundRow {
        derive_trait: ReprGroundingDeriveTrait::ReprDeriveDeserialize,
        required: ReprGroundingDeriveTrait::ReprDeriveClone,
        authority: im_15_1_0_vector_deserialize_impl_authority(),
    })])
            };
        }
    CACHED.with(|c: &Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>>| c.clone())
}

pub fn rust_btree_set_supplemental_generic_bound_rows_note() -> String {
    thread_local! {
        static CACHED: String = {
            "SINGLE AUTHORITY for the trait -> supplemental-generic-bound facts of the Set/BTreeSet realization (rustc 1.93 std::collections::BTreeSet): BTreeSet<T>'s own Debug, Clone, PartialEq and Serialize impls need only T's natural per-field bound (cited to rust_1_93_btree_set_trait_implementations_authority / serde_1_0_228_btree_set_serialize_authority in the v2 contract map, which does not fold this row list because those four require no element-type SUPPLEMENT beyond the derive's natural bound). Only Deserialize is conditional on an extra bound past its natural Deserialize<'de> requirement: impl<'de, T: Deserialize<'de> + Ord> Deserialize<'de> for BTreeSet<T> — BTreeSet must reconstruct its ordering invariant during deserialization, so Ord is required even though no derived Deserialize impl would otherwise ask for it. This module is dag-rooted so both pools read one authority: the v1 seed emitter (regen pool [src/v1, dag]) consumes this row in v1.compiler.trait_derive_emit, and v2.extdeps.languages.rust folds it into rust_btree_set_supplemental_generic_bound_contracts's Deserialize entry — mirroring rust_vec_freemonoid_supplemental_generic_bound_rows's row-authority split rather than restating the requirement inline a second time.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn rust_btree_set_supplemental_generic_bound_rows(
) -> Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>> {
    thread_local! {
            static CACHED: Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>> = {
                Rc::new(vec![Rc::new(RustVecSupplementalGenericBoundRow {
        derive_trait: ReprGroundingDeriveTrait::ReprDeriveDeserialize,
        required: ReprGroundingDeriveTrait::ReprDeriveOrd,
        authority: serde_1_0_228_btree_set_deserialize_authority(),
    })])
            };
        }
    CACHED.with(|c: &Rc<Vec<Rc<RustVecSupplementalGenericBoundRow>>>| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RustSupplementalGenericBoundContractCoverageDisposition {
    pub owner: Rc<DeclarationRef>,
    pub lane: Rc<DeclarationRef>,
    pub dissolution: Rc<Disposition>,
}

pub fn rust_hash_set_supplemental_generic_bound_contract_coverage_disposition(
) -> Rc<RustSupplementalGenericBoundContractCoverageDisposition> {
    thread_local! {
            static CACHED: Rc<RustSupplementalGenericBoundContractCoverageDisposition> = {
                Rc::new(RustSupplementalGenericBoundContractCoverageDisposition {
        owner: Rc::new(DeclarationRef {
        module_path: "v2.std.compilers.target_model".to_string(),
        decl_name: "TargetDeriveSupplementalGenericBoundRequirementSet".to_string(),
        field: Rc::new(DeclField::WholeDeclaration),
    }),
        lane: Rc::new(DeclarationRef {
        module_path: "v2.std.compilers.target_model".to_string(),
        decl_name: "RequiredTraitWitness".to_string(),
        field: Rc::new(DeclField::WholeDeclaration),
    }),
        dissolution: Rc::new(Disposition::Scaffold {
        dissolves_to: ConstructionMechanism::SingleAuthority,
        bind: Rc::new(DeclarationRef {
        module_path: "v2.std.compilers.target_model".to_string(),
        decl_name: "RequiredTraitWitness".to_string(),
        field: Rc::new(DeclField::WholeDeclaration),
    }),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<RustSupplementalGenericBoundContractCoverageDisposition>| c.clone())
}
