// Generated by v1 compiler -- do not edit.
// Source module: v1.gunbc.type_reference_containment_binding_production

pub use crate::gunbc_type_reference_binding_context::{
    build_graph_type_reference_binding_inputs, build_type_reference_binding_context,
    declaration_exposure_from_containment_tree, merge_occurrence_binding_input_rows,
    occurrence_binding_inputs_from_module_transport, type_reference_binding_context_authority_note,
};
pub use crate::gunbc_type_reference_binding_context::{
    ModuleOccurrenceBindingSource, TypeReferenceBindingContext,
};
pub use crate::std_types::NonEmptyStr;
pub use crate::v1_compiler_resolve::{ModuleGraph, ResolvedModule};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v1_std_core::authored_name_at;
pub use crate::v1_std_core::NewlineIndex;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn type_reference_containment_binding_production_note() -> String {
    thread_local! {
        static CACHED: String = {
            "N2 slice 2 PR-B adapter (quiet-hawk-219): projects v1 ModuleGraph ResolvedModule rows into gunbc.type_reference_binding_context.ModuleOccurrenceBindingSource for resolve_node_bounded threading on the RESOLVE path only. Emit-side item_registry leaf-key collision is a separate LIVE instance (#7705 RED control on main; warm-wolf-814 owns fix — cite in PR-B motivation per type_reference_binding_context_pr_b_motivation_note, do not fix here). PR-A authority: dag/gunbc/type_reference_binding_context.dag. DISSOLVE-ON: gunbc.type_reference_containment_binding type_reference_containment_binding_dissolve_on.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn module_occurrence_binding_sources_from_graph(
    modules: Rc<Vec<Rc<ResolvedModule>>>,
    source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>,
) -> Rc<Vec<Rc<ModuleOccurrenceBindingSource>>> {
    Rc::new({
        let mut __result = Vec::new();
        for resolved in modules.clone().iter().cloned() {
            __result.push(Rc::new(ModuleOccurrenceBindingSource {
                module_path: authored_name_at(source_indices.clone(), resolved.module.clone()),
                transport: resolved.occurrence_transport.clone(),
            }));
        }
        __result
    })
}

pub fn build_type_reference_binding_context_from_graph(
    graph: Rc<ModuleGraph>,
    source_indices: Rc<HashMap<String, Rc<NewlineIndex>>>,
) -> Rc<TypeReferenceBindingContext> {
    build_type_reference_binding_context(
        graph.occurrence_transport.clone(),
        module_occurrence_binding_sources_from_graph(graph.modules.clone(), source_indices.clone()),
    )
}
