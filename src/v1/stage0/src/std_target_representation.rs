// Generated by v1 compiler -- do not edit.
// Source module: std.target_representation

use self::CheckpointRowDisposition::*;
use self::ExactBindingResolution::*;
pub use crate::std_decl_ref::declaration_ref_eq;
pub use crate::std_decl_ref::DeclarationRef;
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SourceTypeTargetBinding<R: Clone> {
    pub source: Rc<DeclarationRef>,
    pub representation: R,
    pub _phantom: std::marker::PhantomData<R>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RepresentationSpelling<R: Clone> {
    pub representation: R,
    pub reference_position: NonEmptyStr,
    pub grounding_position: NonEmptyStr,
    pub _phantom: std::marker::PhantomData<R>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ExactBindingResolution<R: Clone> {
    ResolvedExactBinding {
        binding: Rc<SourceTypeTargetBinding<R>>,
    },
    ExactBindingAbsent,
    ExactBindingAmbiguous {
        candidate_count: i64,
    },
    ExactSourceIdentityUnavailable {
        cause: NonEmptyStr,
    },
}

pub fn exact_binding_candidates<R: Clone>(
    source: Rc<DeclarationRef>,
    rows: Rc<Vec<Rc<SourceTypeTargetBinding<R>>>>,
) -> Rc<Vec<Rc<SourceTypeTargetBinding<R>>>> {
    Rc::new({
        let mut __result = Vec::new();
        for r in rows.iter().cloned() {
            if crate::std_decl_ref::declaration_ref_eq(r.source.clone(), source.clone()) {
                __result.push(r);
            }
        }
        __result
    })
}

pub fn resolve_exact_binding<R: Clone>(
    source: Option<Rc<DeclarationRef>>,
    rows: Rc<Vec<Rc<SourceTypeTargetBinding<R>>>>,
) -> Rc<ExactBindingResolution<R>> {
    match source.clone() {
        std::option::Option::None => {
            Rc::new(ExactBindingResolution::ExactSourceIdentityUnavailable {
                cause: "the caller reached the declaration-keyed resolver with no DeclarationRef"
                    .to_string(),
            })
        }
        Some(s) => {
            let hits = exact_binding_candidates(s.clone(), rows.clone());
            let candidate_count = (hits.clone().len() as i64);
            if (candidate_count.clone() == 0) {
                Rc::new(ExactBindingResolution::ExactBindingAbsent)
            } else {
                if (candidate_count.clone() == 1) {
                    match hits.clone().first().cloned() {
                        Some(b) => Rc::new(ExactBindingResolution::ResolvedExactBinding {
                            binding: b.clone(),
                        }),
                        std::option::Option::None => {
                            Rc::new(ExactBindingResolution::ExactBindingAbsent)
                        }
                    }
                } else {
                    Rc::new(ExactBindingResolution::ExactBindingAmbiguous {
                        candidate_count: candidate_count.clone(),
                    })
                }
            }
        }
    }
}

pub fn exact_binding_is_resolved<R: Clone>(resolution: Rc<ExactBindingResolution<R>>) -> bool {
    match (*resolution.clone()).clone() {
        ExactBindingResolution::ResolvedExactBinding { binding: _, .. } => true,
        ExactBindingResolution::ExactBindingAbsent => false,
        ExactBindingResolution::ExactBindingAmbiguous {
            candidate_count: _, ..
        } => false,
        ExactBindingResolution::ExactSourceIdentityUnavailable { cause: _, .. } => false,
    }
}

pub fn representation_spelling_for<R: Clone>(
    representation: R,
    spellings: Rc<Vec<Rc<RepresentationSpelling<R>>>>,
    same_representation: impl Fn(R, R) -> bool + Clone,
) -> Option<Rc<RepresentationSpelling<R>>> {
    Rc::new({
        let mut __result = Vec::new();
        for s in spellings.iter().cloned() {
            if same_representation(s.representation.clone(), representation.clone()) {
                __result.push(s);
            }
        }
        __result
    })
    .first()
    .cloned()
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum CheckpointRowDisposition {
    MigratedToExactBinding {
        bindings: Rc<Vec<Rc<DeclarationRef>>>,
    },
    ProvenUniqueKernelBinding {
        proof: NonEmptyStr,
    },
    StillBareNameDebt {
        reason: NonEmptyStr,
        restoration_trigger: NonEmptyStr,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CheckpointRowMigration {
    pub dag_name: NonEmptyStr,
    pub disposition: Rc<CheckpointRowDisposition>,
}

pub fn checkpoint_row_disposition_keeps_bare_row(
    disposition: Rc<CheckpointRowDisposition>,
) -> bool {
    match (*disposition.clone()).clone() {
        CheckpointRowDisposition::MigratedToExactBinding { bindings: _, .. } => false,
        CheckpointRowDisposition::ProvenUniqueKernelBinding { proof: _, .. } => true,
        CheckpointRowDisposition::StillBareNameDebt { .. } => true,
    }
}

pub fn checkpoint_row_migration_for(
    dag_name: String,
    migrations: Rc<Vec<Rc<CheckpointRowMigration>>>,
) -> Option<Rc<CheckpointRowMigration>> {
    Rc::new({
        let mut __result = Vec::new();
        for m in migrations.iter().cloned() {
            if (m.dag_name.clone() == dag_name.clone()) {
                __result.push(m);
            }
        }
        __result
    })
    .first()
    .cloned()
}
