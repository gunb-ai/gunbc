// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.parser_declaration_requirements_expected_red_test

use self::PdrTransport::*;
use crate::std_occurrence_identity::OccurrenceCategory::{
    CallableOccurrence, ConstructorOccurrence, FieldOccurrence, LexicalValueOccurrence,
    MethodOccurrence, NamespaceSegmentOccurrence, TypeOccurrence,
};
pub use crate::std_occurrence_identity::{
    DeclarationOccurrence, OccurrenceCategory, OccurrenceId, OccurrenceIndexEntry,
    OccurrenceTransport,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
pub use crate::v1_gunbc_occurrence_binding_parser_walk::parse_authored_occurrence_binding_source;
pub use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource;
use crate::v1_gunbc_occurrence_binding_parser_walk::ParsedOccurrenceBindingSource::{
    ParsedOccurrenceBindingSourceReady, ParsedOccurrenceBindingSourceRefused,
};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn pdr_matrix_witness_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Parser declaration-category denominator. Pins, per authored declaration form, the OccurrenceCategory population the v1 parser mints — including the forms that mint nothing. Observation-only: no resolution, exposure, or binding claim. The zero rows are the load-bearing half; minting a category later must edit this file.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdr_offline_recipe() -> String {
    thread_local! {
        static CACHED: String = {
            "OFFLINE LOCAL RECIPE (requires v1 parser pool): target/release/claim_batch --source-root dag --source-root src/v1 --entry src/v1/tests/claim/parser_declaration_category_matrix_witness_test.dag --functions witness_matrix_fixture_parses,witness_function_declaration_mints_callable,witness_second_function_declaration_mints_callable,witness_function_parameter_binds,witness_match_scrutinee_parameter_binds,witness_let_binder_binds,witness_pattern_binder_binds,witness_product_type_declaration_is_declared,witness_coproduct_type_declaration_is_declared,witness_nullary_constructor_is_declared,witness_payload_constructor_is_declared,witness_product_field_is_declared,witness_coproduct_payload_field_is_declared_at_its_declaration_site,witness_pattern_field_position_is_not_a_declaration".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdr_dissolve_on() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVE-ON: witness_layer_roots admits src/v1 for parser-transport witnesses, or the execution corpus carries per-entry source roots — then this recipe deletes and discovery enrollment suffices.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdr_matrix_source() -> String {
    (((((((((((((((((((((((("module app.pdr_matrix\n".to_string()
        + &"\n".to_string())
        + &"type PdcProduct {\n".to_string())
        + &"  pdr_product_field: Int\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"type PdcSum = PdcNullary | PdcPayload { pdr_payload_field: Int }\n"
            .to_string())
        + &"\n".to_string())
        + &"data pdr_annotated_datum: Int = 7\n".to_string())
        + &"\n".to_string())
        + &"fn pdr_function(pdr_parameter: Int) -> Int {\n".to_string())
        + &"  let pdr_let_binder = pdr_parameter\n".to_string())
        + &"  pdr_let_binder\n".to_string())
        + &"}\n".to_string())
        + &"\n".to_string())
        + &"fn pdr_matcher(pdr_scrutinee: PdcSum) -> Int {\n".to_string())
        + &"  match pdr_scrutinee {\n".to_string())
        + &"    PdcNullary => {\n".to_string())
        + &"      0\n".to_string())
        + &"    }\n".to_string())
        + &"    PdcPayload { pdr_payload_field: pdr_pattern_binder } => {\n".to_string())
        + &"      pdr_pattern_binder\n".to_string())
        + &"    }\n".to_string())
        + &"  }\n".to_string())
        + &"}\n".to_string())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum PdrTransport {
    PdrTransportReady { transport: Rc<OccurrenceTransport> },
    PdrTransportRefused,
}
impl PdrTransport {
    pub fn transport(&self) -> Rc<OccurrenceTransport> {
        match self {
            PdrTransport::PdrTransportReady {
                transport: __val, ..
            } => __val.clone(),
            PdrTransport::PdrTransportRefused => panic!("no transport on unit variant"),
        }
    }
}

pub fn pdr_parse_matrix() -> Rc<PdrTransport> {
    match (*parse_authored_occurrence_binding_source(
        "app/pdr_matrix.dag".to_string(),
        pdr_matrix_source(),
    ))
    .clone()
    {
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceRefused => {
            Rc::new(PdrTransport::PdrTransportRefused)
        }
        ParsedOccurrenceBindingSource::ParsedOccurrenceBindingSourceReady { transport, .. } => {
            Rc::new(PdrTransport::PdrTransportReady {
                transport: transport.clone(),
            })
        }
    }
}

pub fn pdr_category_tag(category: OccurrenceCategory) -> String {
    match category.clone() {
        OccurrenceCategory::LexicalValueOccurrence => "LexicalValueOccurrence".to_string(),
        OccurrenceCategory::TypeOccurrence => "TypeOccurrence".to_string(),
        OccurrenceCategory::CallableOccurrence => "CallableOccurrence".to_string(),
        OccurrenceCategory::ConstructorOccurrence => "ConstructorOccurrence".to_string(),
        OccurrenceCategory::NamespaceSegmentOccurrence => "NamespaceSegmentOccurrence".to_string(),
        OccurrenceCategory::FieldOccurrence => "FieldOccurrence".to_string(),
        OccurrenceCategory::MethodOccurrence => "MethodOccurrence".to_string(),
    }
}

pub fn pdr_index_entry_for_declaration(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> Option<Rc<OccurrenceIndexEntry>> {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            None,
            |found: _, entry: Rc<OccurrenceIndexEntry>| match found.clone() {
                Some(_) => found.clone(),
                None => match (entry.projection.clone().occurrence.clone().value.clone()
                    == declaration.occurrence.clone().value.clone())
                {
                    true => Some(entry.clone()),
                    false => None,
                },
            },
        )
}

pub fn pdr_declared_tags_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, declaration: Rc<DeclarationOccurrence>| {
            match pdr_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![pdr_category_tag(declaration.category.clone())]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdr_render_tags(tags: Rc<Vec<String>>) -> String {
    tags.clone()
        .iter()
        .cloned()
        .fold("".to_string(), |acc: String, tag: String| {
            ((acc + &"|".to_string()) + &tag.clone())
        })
}

pub fn pdr_containment_chain(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
) -> String {
    declaration
        .containment
        .clone()
        .ancestors
        .clone()
        .iter()
        .cloned()
        .fold("".to_string(), |chain: String, ancestor: OccurrenceId| {
            ((chain + &">".to_string())
                + &pdr_named_or_anon(transport.clone(), ancestor.value.clone()))
        })
}

pub fn pdr_named_or_anon(transport: Rc<OccurrenceTransport>, occurrence_value: i64) -> String {
    {
        let found = pdr_name_for_occurrence(transport.clone(), occurrence_value.clone());
        match (found.clone() == "".to_string()) {
            true => "<anon>".to_string(),
            false => found.clone(),
        }
    }
}

pub fn pdr_name_for_occurrence(
    transport: Rc<OccurrenceTransport>,
    occurrence_value: i64,
) -> String {
    transport
        .index
        .clone()
        .entries
        .clone()
        .iter()
        .cloned()
        .fold(
            "<unknown>".to_string(),
            |found: String, entry: Rc<OccurrenceIndexEntry>| match (found.clone()
                == "<unknown>".to_string())
            {
                false => found.clone(),
                true => match (entry.projection.clone().occurrence.clone().value.clone()
                    == occurrence_value.clone())
                {
                    true => entry.projection.clone().authored_name.clone(),
                    false => found.clone(),
                },
            },
        )
}

pub fn pdr_render_occurrence(
    transport: Rc<OccurrenceTransport>,
    declaration: Rc<DeclarationOccurrence>,
    entry: Rc<OccurrenceIndexEntry>,
) -> String {
    ((((((((entry.projection.clone().authored_name.clone() + &"#".to_string())
        + &pdr_category_tag(declaration.category.clone()))
        + &"#span:".to_string())
        + &(entry
            .projection
            .clone()
            .diagnostic_span
            .clone()
            .start
            .clone())
        .to_string())
        + &"-".to_string())
        + &(entry.projection.clone().diagnostic_span.clone().end.clone()).to_string())
        + &"#path:".to_string())
        + &pdr_containment_chain(transport.clone(), declaration.clone()))
}

pub fn pdr_identity_rows_for_name(
    transport: Rc<OccurrenceTransport>,
    name: String,
) -> Rc<Vec<String>> {
    transport.declarations.clone().iter().cloned().fold(
        Rc::new(vec![]),
        |acc: Rc<Vec<String>>, declaration: Rc<DeclarationOccurrence>| {
            match pdr_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                None => acc.clone(),
                Some(entry) => {
                    match (entry.projection.clone().authored_name.clone() == name.clone()) {
                        true => v1_rt::concat(
                            acc.clone(),
                            Rc::new(vec![pdr_render_occurrence(
                                transport.clone(),
                                declaration.clone(),
                                entry.clone(),
                            )]),
                        ),
                        false => acc.clone(),
                    }
                }
            }
        },
    )
}

pub fn pdr_identity_population(transport: Rc<OccurrenceTransport>, name: String) -> String {
    pdr_render_tags(pdr_identity_rows_for_name(transport.clone(), name.clone()))
}

pub fn pdr_declares_exactly(
    transport: Rc<OccurrenceTransport>,
    name: String,
    expected: String,
) -> bool {
    (pdr_render_tags(pdr_declared_tags_for_name(transport.clone(), name.clone()))
        == expected.clone())
}

pub fn pdr_declares_nothing(transport: Rc<OccurrenceTransport>, name: String) -> bool {
    ((pdr_declared_tags_for_name(transport.clone(), name.clone()).len() as i64) == 0)
}

pub fn pdr_row(name: String, expected: String) -> bool {
    match (*pdr_parse_matrix()).clone() {
        PdrTransport::PdrTransportRefused => false,
        PdrTransport::PdrTransportReady {
            transport: transport,
            ..
        } => pdr_declares_exactly(transport.clone(), name.clone(), expected.clone()),
    }
}

pub fn pdr_zero_row(name: String) -> bool {
    match (*pdr_parse_matrix()).clone() {
        PdrTransport::PdrTransportRefused => false,
        PdrTransport::PdrTransportReady {
            transport: transport,
            ..
        } => pdr_declares_nothing(transport.clone(), name.clone()),
    }
}

pub fn pdr_identity_row(name: String, expected: String) -> bool {
    match (*pdr_parse_matrix()).clone() {
        PdrTransport::PdrTransportRefused => false,
        PdrTransport::PdrTransportReady {
            transport: transport,
            ..
        } => (pdr_identity_population(transport.clone(), name.clone()) == expected.clone()),
    }
}

pub fn pdr_duplicated_support_scaffold() -> String {
    thread_local! {
        static CACHED: String = {
            "DECLARED SCAFFOLD -- this file duplicates the matrix witness's entire helper prelude (fixture source, transport parse, category tag, identity rendering, containment chain) under a pdr_ prefix. That is a PARALLEL REPRESENTATION of one instrument and a DESIGN section 2/3 violation, carried deliberately rather than silently. WHY IT EXISTS: src/v1 witnesses are reachable only through the v1 claim scoped roster, which witness_v1_claim_scoped_batch_is_file_grain_and_batch_owned requires to be uniformly file-grain, so an enrolled file must be all-green and deliberately-red rows must live in a separate file reached by exact admission. WHY IT IS NOT MERELY MOVED: the duplication is the price of that split, not a consequence of the split being wrong. DISSOLVE-ON: extract the shared support into one non-test module both witness files import, at which point every pdr_ helper here deletes and only the test fns remain. Do that before adding a third file in this family -- two copies is a scaffold, three is a fork.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn pdr_expected_red_note() -> String {
    thread_local! {
        static CACHED: String = {
            "EXPECTED-RED BY CONSTRUCTION. Every test fn here asserts a requirement the .dag language imposes that the v1 parser does not yet meet. They are commit C's acceptance targets, and they are expected to FAIL until it lands. They live in their own file because the v1 claim scoped batch is required to be uniformly file-grain (witness_v1_claim_scoped_batch_is_file_grain_and_batch_owned asserts every entry has an empty function), so a file cannot mix green and deliberately-red rows. When commit C lands, these rows green and move to the enrolled green file as permanent regression controls -- they do not retire.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn witness_product_type_declaration_is_declared() -> bool {
    pdr_row("PdcProduct".to_string(), "|TypeOccurrence".to_string())
}

pub fn witness_coproduct_type_declaration_is_declared() -> bool {
    pdr_row("PdcSum".to_string(), "|TypeOccurrence".to_string())
}

pub fn witness_nullary_constructor_is_declared() -> bool {
    pdr_row(
        "PdcNullary".to_string(),
        "|ConstructorOccurrence".to_string(),
    )
}

pub fn witness_payload_constructor_is_declared() -> bool {
    pdr_row(
        "PdcPayload".to_string(),
        "|ConstructorOccurrence".to_string(),
    )
}

pub fn witness_product_field_is_declared() -> bool {
    pdr_row(
        "pdr_product_field".to_string(),
        "|FieldOccurrence".to_string(),
    )
}

pub fn witness_coproduct_payload_field_is_declared_at_its_declaration_site() -> bool {
    pdr_identity_row(
        "pdr_payload_field".to_string(),
        "|pdr_payload_field#FieldOccurrence#span:109-126#path:>app.pdr_matrix>PdcSum>PdcPayload"
            .to_string(),
    )
}

pub fn witness_pattern_field_position_is_not_a_declaration() -> bool {
    match (*pdr_parse_matrix()).clone() {
        PdrTransport::PdrTransportRefused => false,
        PdrTransport::PdrTransportReady {
            transport: transport,
            ..
        } => transport.declarations.clone().iter().cloned().fold(
            true,
            |acc: bool, declaration: Rc<DeclarationOccurrence>| {
                match pdr_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                    None => acc.clone(),
                    Some(entry) => {
                        (acc.clone()
                            && (entry
                                .projection
                                .clone()
                                .diagnostic_span
                                .clone()
                                .start
                                .clone()
                                != 394))
                    }
                }
            },
        ),
    }
}

pub fn witness_a_declaration_exists_at_the_coproduct_field_site() -> bool {
    match (*pdr_parse_matrix()).clone() {
        PdrTransport::PdrTransportRefused => false,
        PdrTransport::PdrTransportReady {
            transport: transport,
            ..
        } => transport.declarations.clone().iter().cloned().fold(
            false,
            |found: bool, declaration: Rc<DeclarationOccurrence>| {
                match pdr_index_entry_for_declaration(transport.clone(), declaration.clone()) {
                    None => found.clone(),
                    Some(entry) => {
                        (found.clone()
                            || (entry
                                .projection
                                .clone()
                                .diagnostic_span
                                .clone()
                                .start
                                .clone()
                                == 109))
                    }
                }
            },
        ),
    }
}
