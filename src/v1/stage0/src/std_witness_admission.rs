// Generated by v1 compiler -- do not edit.
// Source module: std.witness_admission

use self::EnrollmentCadenceWireCompare::*;
use self::FrozenBaselineStanding::*;
use self::ScheduledEnvelopeCadenceWireDecode::*;
use self::ScheduledEnvelopeMaxStalenessWireDecode::*;
use self::WitnessConsumerCadence::*;
use self::WitnessExecutionStanding::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn witness_admission_authority_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Phase 0(b) admission invariant (docs/plans/module-identity-storage-binding-design.md): every enrolled witness row names an executing consumer — discovery rows run per selection; SelfEmitted receipts run on the falsifier wet cadence; known-red quarantined rows run on a probe cadence expecting red (greening is a counted un-quarantine event); operator-ruled OFFLINE rows carry an explicit local-recipe consumer. Enrolled-with-zero-consumers is a typed refusal, never silent. The prior dissolve-on FIRED 2026-07-22: witness exclusion rows now carry derived consumer classification on this module's WitnessConsumerCadence authority (gunbc.ci_layer_roots.witness_exclusion_frontier; group-of-units ruling, gunbc.roster_registry). Homed in dag/std (std.witness_admission) so dag-tree consumers resolve it in dag-only closures — the authority moved here from src/v2/std 2026-07-23 when gunbc.ci_layer_roots became a consumer. This module remains the cadence vocabulary authority; dissolves into the enforcement-intent registry when that thread lands.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum WitnessConsumerCadence {
    DiscoverySelection,
    FalsifierSelfHostWet,
    FalsifierRehomedBinWet,
    FalsifierSubstrateLongLane,
    BinWitnessWet,
    QuarantineProbeExpectRed,
    OfflineLocalRecipe,
    FixtureExplicitRoster,
    NoConsumer,
}

pub fn witness_execution_standing_axis_note() -> String {
    thread_local! {
        static CACHED: String = {
            "TWO INDEPENDENT AXES, because ALLOWED TO REMAIN and HAS AN EXECUTING CONSUMER are different facts and one success arm cannot carry both (operator ruling on gunbc#7804, 2026-08-04). The first shape of the freeze wall returned WitnessAdmissionHolds — the same arm an executing consumer returns — for a frozen legacy row, and then reported the live gate green as evidence that every current row is admitted. But a frozen row has NO executing consumer; that is the entire content of its debt. Freezing establishes that the unexecuted hole existed at the cut and cannot grow. It does not establish coverage, and returning the success arm made the same fact answer differently depending on which door it entered: exact function-grain admission correctly REFUSES OfflineLocalRecipe because a local recipe is not an executing cadence, while path policy was returning success for exactly that.\n\nAXIS ONE — WitnessExecutionStanding, what actually runs this witness. WitnessHasExecutingConsumer is the only arm that means covered, and it carries WHICH cadence so the claim is checkable rather than a bare yes. LegacyFrozenPathDeferral means the identity is in the frozen baseline: tolerated by the migration ratchet, NOT covered, and anything downstream asking whether the behavior is covered must receive no from this arm. UnclassifiedPathDeferral is a row a broad OfflineLocalRecipe path policy claims but the freeze does not — the forward hole, refused. UnexecutedDeferredWitness is a row nothing claims at all; its remedy is to name a cadence or delete the witness, and it is nearly empty while the path-policy class is in the hundreds, which is why one arm would have hidden the larger population behind the rarer one.\n\nAXIS TWO — FrozenBaselineStanding, whether the migration ratchet still holds, a property of the ROSTER rather than of any row. Allowed and Shrank both permit the tolerated arm; Grew and Stale refuse regardless of any individual row, because a baseline that grew is the escape hatch reopening and a baseline naming a departed witness is an exemption the next witness to take that name inherits.\n\nTHE GATES DERIVE, and they are deliberately different: the coverage question admits ONLY WitnessHasExecutingConsumer, so seven hundred dark witnesses never read as covered; the floor gate additionally tolerates LegacyFrozenPathDeferral while axis two holds, so the tree stays green under the ratchet without anything claiming those rows execute.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum WitnessExecutionStanding {
    WitnessHasExecutingConsumer { cadence: WitnessConsumerCadence },
    LegacyFrozenPathDeferral { entry: String, function: String },
    UnclassifiedPathDeferral { entry: String, function: String },
    UnexecutedDeferredWitness { entry: String, function: String },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum FrozenBaselineStanding {
    FrozenBaselineDebtAllowed,
    FrozenBaselineDebtShrank,
    FrozenBaselineDebtGrew { added_count: i64 },
    FrozenBaselineDebtStale { stale_count: i64 },
}

pub fn witness_admission_predicate_dissolution_note() -> String {
    thread_local! {
        static CACHED: String = {
            "witness_consumer_cadence_is_live / witness_admission_refusal_is_holds dissolved per review 39760 — workflow and tests match the coproducts directly (integration 39758 scaffold superseded on this branch).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn scheduled_envelope_cadence_wire_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Temporary wire decoder for ScheduledWitnessEnvelope.cadence (String on the enrollment carrier until that field migrates to WitnessConsumerCadence). THE ONLY home for enrollment wire strings — decode_scheduled_envelope_cadence_wire and compare_declared_derived_enrollment_cadence_wires are the sole round-trip table (five wires: per_pr, 4h, falsifier_substrate_long_lane, offline_local_recipe, no_consumer). Cadences without an enrollment wire (BinWitnessWet, explicit roster, etc.) are carried as WitnessConsumerCadence on admission rows, never as envelope.cadence strings. Dissolve-on: envelope.cadence becomes the closed coproduct and this decoder deletes.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn scheduled_envelope_max_staleness_wire_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Temporary wire decoder for ScheduledWitnessEnvelope.max_staleness until std.measure lands a typed staleness/deadline carrier. Closed enrollment wires only: 0 (per-PR immediate) and 4h (scheduled tolerance). Anything else is MaxStalenessWireUnrecognized and placement/compare refuse rather than inferring from a bare-string sentinel.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ScheduledEnvelopeCadenceWireDecode {
    EnvelopeCadenceDecoded { cadence: WitnessConsumerCadence },
    EnvelopeCadenceWireUnrecognized { wire: String },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum ScheduledEnvelopeMaxStalenessWireDecode {
    MaxStalenessPerPrImmediate,
    MaxStalenessScheduledTolerance { wire: String },
    MaxStalenessWireUnrecognized { wire: String },
}
impl ScheduledEnvelopeMaxStalenessWireDecode {
    pub fn wire(&self) -> String {
        match self {
            ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessPerPrImmediate => {
                panic!("no wire on unit variant")
            }
            ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessScheduledTolerance {
                wire: __val,
                ..
            } => __val.clone(),
            ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessWireUnrecognized {
                wire: __val,
                ..
            } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum EnrollmentCadenceWireCompare {
    EnrollmentCadenceWiresMatch,
    EnrollmentCadenceWireMismatch {
        declared_cadence: String,
        derived_cadence: String,
    },
    EnrollmentCadenceWireUnrecognized {
        wire: String,
    },
}

pub fn decode_scheduled_envelope_max_staleness_wire(
    staleness: String,
) -> Rc<ScheduledEnvelopeMaxStalenessWireDecode> {
    if (staleness.clone() == "0".to_string()) {
        Rc::new(ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessPerPrImmediate)
    } else {
        if (staleness.clone() == "4h".to_string()) {
            Rc::new(
                ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessScheduledTolerance {
                    wire: staleness.clone(),
                },
            )
        } else {
            Rc::new(
                ScheduledEnvelopeMaxStalenessWireDecode::MaxStalenessWireUnrecognized {
                    wire: staleness.clone(),
                },
            )
        }
    }
}

pub fn compare_declared_derived_enrollment_cadence_wires(
    declared_cadence: String,
    derived_cadence: String,
) -> Rc<EnrollmentCadenceWireCompare> {
    match (*decode_scheduled_envelope_cadence_wire(declared_cadence.clone())).clone() {
        ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceWireUnrecognized {
            wire: wire, ..
        } => Rc::new(
            EnrollmentCadenceWireCompare::EnrollmentCadenceWireUnrecognized { wire: wire.clone() },
        ),
        ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded { cadence: _, .. } => {
            if (declared_cadence.clone() != derived_cadence.clone()) {
                Rc::new(
                    EnrollmentCadenceWireCompare::EnrollmentCadenceWireMismatch {
                        declared_cadence: declared_cadence.clone(),
                        derived_cadence: derived_cadence.clone(),
                    },
                )
            } else {
                Rc::new(EnrollmentCadenceWireCompare::EnrollmentCadenceWiresMatch)
            }
        }
    }
}

pub fn decode_scheduled_envelope_cadence_wire(
    cadence: String,
) -> Rc<ScheduledEnvelopeCadenceWireDecode> {
    if (cadence.clone() == "per_pr".to_string()) {
        Rc::new(ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
            cadence: WitnessConsumerCadence::DiscoverySelection,
        })
    } else {
        if (cadence.clone() == "4h".to_string()) {
            Rc::new(ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
                cadence: WitnessConsumerCadence::FalsifierSelfHostWet,
            })
        } else {
            if (cadence.clone() == "falsifier_substrate_long_lane".to_string()) {
                Rc::new(ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
                    cadence: WitnessConsumerCadence::FalsifierSubstrateLongLane,
                })
            } else {
                if (cadence.clone() == "offline_local_recipe".to_string()) {
                    Rc::new(ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
                        cadence: WitnessConsumerCadence::OfflineLocalRecipe,
                    })
                } else {
                    if (cadence.clone() == "no_consumer".to_string()) {
                        Rc::new(ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
                            cadence: WitnessConsumerCadence::NoConsumer,
                        })
                    } else {
                        Rc::new(
                            ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceWireUnrecognized {
                                wire: cadence.clone(),
                            },
                        )
                    }
                }
            }
        }
    }
}

pub fn witness_consumer_cadence_eq(a: WitnessConsumerCadence, b: WitnessConsumerCadence) -> bool {
    match a.clone() {
        WitnessConsumerCadence::DiscoverySelection => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => true,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::FalsifierSelfHostWet => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => true,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::FalsifierRehomedBinWet => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => true,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::FalsifierSubstrateLongLane => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => true,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::BinWitnessWet => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => true,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::QuarantineProbeExpectRed => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => true,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::OfflineLocalRecipe => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => true,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::FixtureExplicitRoster => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => true,
            WitnessConsumerCadence::NoConsumer => false,
        },
        WitnessConsumerCadence::NoConsumer => match b.clone() {
            WitnessConsumerCadence::DiscoverySelection => false,
            WitnessConsumerCadence::FalsifierSelfHostWet => false,
            WitnessConsumerCadence::FalsifierRehomedBinWet => false,
            WitnessConsumerCadence::FalsifierSubstrateLongLane => false,
            WitnessConsumerCadence::BinWitnessWet => false,
            WitnessConsumerCadence::QuarantineProbeExpectRed => false,
            WitnessConsumerCadence::OfflineLocalRecipe => false,
            WitnessConsumerCadence::FixtureExplicitRoster => false,
            WitnessConsumerCadence::NoConsumer => true,
        },
    }
}

pub fn witness_consumer_cadence_wire_round_trip_holds(cadence: WitnessConsumerCadence) -> bool {
    {
        let wire = witness_consumer_cadence_enrollment_wire(cadence.clone());
        match (*decode_scheduled_envelope_cadence_wire(wire.clone())).clone() {
            ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceDecoded {
                cadence: decoded, ..
            } => witness_consumer_cadence_eq(decoded.clone(), cadence.clone()),
            ScheduledEnvelopeCadenceWireDecode::EnvelopeCadenceWireUnrecognized {
                wire: _, ..
            } => false,
        }
    }
}

pub fn witness_consumer_cadence_enrollment_wire(cadence: WitnessConsumerCadence) -> String {
    match cadence.clone() {
        WitnessConsumerCadence::DiscoverySelection => "per_pr".to_string(),
        WitnessConsumerCadence::FalsifierSelfHostWet => "4h".to_string(),
        WitnessConsumerCadence::FalsifierRehomedBinWet => "falsifier_rehomed_bin_wet".to_string(),
        WitnessConsumerCadence::FalsifierSubstrateLongLane => {
            "falsifier_substrate_long_lane".to_string()
        }
        WitnessConsumerCadence::BinWitnessWet => "bin_witness_wet".to_string(),
        WitnessConsumerCadence::QuarantineProbeExpectRed => {
            "quarantine_probe_expect_red".to_string()
        }
        WitnessConsumerCadence::OfflineLocalRecipe => "offline_local_recipe".to_string(),
        WitnessConsumerCadence::FixtureExplicitRoster => "fixture_explicit_roster".to_string(),
        WitnessConsumerCadence::NoConsumer => "no_consumer".to_string(),
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct DiscoverySelection;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FalsifierSelfHostWet;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FalsifierRehomedBinWet;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FalsifierSubstrateLongLane;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct BinWitnessWet;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct QuarantineProbeExpectRed;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct OfflineLocalRecipe;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct FixtureExplicitRoster;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct NoConsumer;
