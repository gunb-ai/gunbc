// Generated by v1 compiler -- do not edit.
// Source module: std.coercion

use self::RealizationGround::*;
use self::RealizationRefusalCause::*;
use self::TypeDeclarationProvenance::*;
use self::TypeRealizationDecision::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeCheckpoint {
    pub dag_name: String,
    pub target_type: String,
    pub grounding_type: String,
    pub default_expr: Option<String>,
    pub is_copy: Option<bool>,
    pub literal_suffix: Option<String>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum TypeDeclarationProvenance {
    CorpusDeclared { decl_file: String },
    KernelMinted { minted_name: String },
    DeclarationIdentityAbsent,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum RealizationRefusalCause {
    DeclarationIdentityUnavailable,
    ExactSourceIdentityAbsent { detail: String },
    BareRowMigratedToExactBinding { dag_name: String },
    BoundRepresentationHasNoRealizationRow { decl_name: String },
    ExactBindingAmbiguousForOneDeclaration,
}

pub fn realization_refusal_label(cause: Rc<RealizationRefusalCause>) -> String {
    match (*cause.clone()).clone() {
    RealizationRefusalCause::DeclarationIdentityUnavailable => "no declaration identity was supplied".to_string(),
    RealizationRefusalCause::ExactSourceIdentityAbsent { detail: d, .. } => d.clone(),
    RealizationRefusalCause::BareRowMigratedToExactBinding { dag_name: n, .. } => v1_rt::concat(v1_rt::concat("declaration-keyed name `".to_string(), n.clone()), "` reached the spelling-keyed arm without a declaration: its bare row is migrated, answer from the exact binding".to_string()),
    RealizationRefusalCause::BoundRepresentationHasNoRealizationRow { decl_name: n, .. } => v1_rt::concat(v1_rt::concat("declaration `".to_string(), n.clone()), "` is bound to a target representation with no realization row".to_string()),
    RealizationRefusalCause::ExactBindingAmbiguousForOneDeclaration => "ambiguous exact binding: more than one row for one declaration".to_string(),
}
}

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum RealizationGround {
    GroundedByCheckpointRow,
    GroundedByHostNumericAlias,
    GroundedByUnidentifiedBareRow,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum TypeRealizationDecision {
    Realized {
        checkpoint: Rc<TypeCheckpoint>,
        ground: RealizationGround,
    },
    Unrealized,
    RealizationRefused {
        cause: Rc<RealizationRefusalCause>,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct InhabitantDecl {
    pub algebra: String,
    pub template: String,
    pub arity: i64,
    pub identity_expr: Option<String>,
    pub import_path: Option<String>,
    pub is_copy: Option<bool>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CallableRepr {
    pub template: String,
    pub param_separator: String,
    pub return_separator: String,
    pub import_path: Option<String>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CastRule {
    pub from_type: String,
    pub to_type: String,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CastSyntax {
    pub template: String,
    pub cast_rules: Rc<Vec<Rc<CastRule>>>,
}

pub fn dag_cast_rules() -> Rc<Vec<Rc<CastRule>>> {
    thread_local! {
        static CACHED: Rc<Vec<Rc<CastRule>>> = {
            serde_json::from_value(serde_json::json!([{"from_type": "Int", "to_type": "Int"}, {"from_type": "Int", "to_type": "Float"}, {"from_type": "Float", "to_type": "Int"}, {"from_type": "Float", "to_type": "Float"}, {"from_type": "Bool", "to_type": "Int"}]))
                .expect("valid data definition")
        };
    }
    CACHED.with(|c: &Rc<Vec<Rc<CastRule>>>| c.clone())
}

pub fn grounded_primitive_coproduct_identities() -> Rc<Vec<Rc<CastRule>>> {
    thread_local! {
        static CACHED: Rc<Vec<Rc<CastRule>>> = {
            serde_json::from_value(serde_json::json!([{"from_type": "Int", "to_type": "Nat"}, {"from_type": "String", "to_type": "FreeMonoid"}]))
                .expect("valid data definition")
        };
    }
    CACHED.with(|c: &Rc<Vec<Rc<CastRule>>>| c.clone())
}

pub fn dag_can_cast(source_type: String, target_type: String) -> bool {
    ({
        let mut __found = false;
        for r in dag_cast_rules().iter().cloned() {
            if ((r.from_type.clone() == source_type.clone())
                && (r.to_type.clone() == target_type.clone()))
            {
                __found = true;
                break;
            }
        }
        __found
    } || {
        let mut __found = false;
        for r in grounded_primitive_coproduct_identities().iter().cloned() {
            if ((r.from_type.clone() == source_type.clone())
                && (r.to_type.clone() == target_type.clone()))
            {
                __found = true;
                break;
            }
        }
        __found
    })
}

pub fn is_dag_cast_domain_type(name: String) -> bool {
    {
        let mut __found = false;
        for r in dag_cast_rules().iter().cloned() {
            if ((r.from_type.clone() == name.clone()) || (r.to_type.clone() == name.clone())) {
                __found = true;
                break;
            }
        }
        __found
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct GroundedByCheckpointRow;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct GroundedByHostNumericAlias;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct GroundedByUnidentifiedBareRow;
