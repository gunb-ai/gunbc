// Generated by v1 compiler -- do not edit.
// Source module: v1.tests.claim.algebra_application_refusal_test

use crate::std_algebra::AlgebraTypeTemplate::ReceiverValue;
use crate::std_algebra::CollectionSizeEffect::IdentityEffect;
use crate::std_algebra::CostShape::ShapeLinearScan;
pub use crate::std_algebra::{AlgebraTypeTemplate, CollectionSizeEffect, CostShape};
pub use crate::std_types::Bool;
use crate::std_types::Bool::*;
pub use crate::v1_compiler_infer_lookup::lookup_structural_method;
pub use crate::v1_compiler_infer_types::instantiate_algebra_type;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v1_std_core::is_error_diagnostic;
pub use crate::v1_std_core::CompilerDiagnostic;
use crate::v1_std_core::CompilerDiagnostic::AlgebraApplicationEvidenceUnavailable;
pub use crate::v1_std_core::NewlineIndex;
pub use crate::v1_tests_claim_algebra_application_evidence_test::evidence_expansion;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn structural_receiver_without_application_refuses_at_requested_position() -> bool {
    {
        let expanded =
            crate::v1_tests_claim_algebra_application_evidence_test::evidence_expansion();
        let result = crate::v1_compiler_infer_types::instantiate_algebra_type(
            Rc::new(AlgebraTypeTemplate::ReceiverValue),
            expanded.clone(),
            v1_rt::rc_empty_map::<String, Rc<NewlineIndex>>(),
        );
        match result.diagnostics.clone().first().cloned() {
            Some(error) => {
                ((((result.diagnostics.clone().len() as i64) == 1)
                    && crate::v1_std_core::is_error_diagnostic(error.diagnostic.clone()))
                    && match (*error.diagnostic.clone()).clone() {
                        CompilerDiagnostic::AlgebraApplicationEvidenceUnavailable {
                            receiver_type,
                            argument_index,
                            span,
                            ..
                        } => {
                            (((receiver_type.clone() == expanded.name.clone())
                                && (argument_index.clone() == 1))
                                && (span.clone() == expanded.span.clone()))
                        }
                        _ => false,
                    })
            }
            std::option::Option::None => false,
        }
    }
}

pub fn structural_receiver_self_method_uses_only_its_profile_template() -> bool {
    {
        let result = crate::v1_compiler_infer_lookup::lookup_structural_method(
            crate::v1_tests_claim_algebra_application_evidence_test::evidence_expansion(),
            "reverse".to_string(),
            v1_rt::rc_empty_map::<String, Rc<NewlineIndex>>(),
        );
        (((result.kernel_diagnostics.clone().len() as i64) == 0)
            && match result.resolution.clone() {
                Some(method) => {
                    (((method.size_effect.clone() == Some(CollectionSizeEffect::IdentityEffect))
                        && (method.cost_shape.clone() == Some(CostShape::ShapeLinearScan)))
                        && match method.algebra_template.clone() {
                            Some(template) => {
                                (((template.name.clone() == "reverse".to_string())
                                    && (template.size_effect.clone()
                                        == method.size_effect.clone()))
                                    && (template.cost_shape.clone() == method.cost_shape.clone()))
                            }
                            std::option::Option::None => false,
                        })
                }
                std::option::Option::None => false,
            })
    }
}

pub fn missing_application_diagnostic_prevents_method_resolution() -> bool {
    {
        let expanded =
            crate::v1_tests_claim_algebra_application_evidence_test::evidence_expansion();
        let result = crate::v1_compiler_infer_lookup::lookup_structural_method(
            expanded.clone(),
            "any".to_string(),
            v1_rt::rc_empty_map::<String, Rc<NewlineIndex>>(),
        );
        (((result.resolution.clone() == std::option::Option::None)
            && ((result.kernel_diagnostics.clone().len() as i64) > 0))
            && {
                let mut __all = true;
                for error in result.kernel_diagnostics.clone().iter().cloned() {
                    if !(crate::v1_std_core::is_error_diagnostic(error.diagnostic.clone())
                        && match (*error.diagnostic.clone()).clone() {
                            CompilerDiagnostic::AlgebraApplicationEvidenceUnavailable {
                                receiver_type,
                                argument_index,
                                span,
                                ..
                            } => {
                                (((receiver_type.clone() == expanded.name.clone())
                                    && (argument_index.clone() == 0))
                                    && (span.clone() == expanded.span.clone()))
                            }
                            _ => false,
                        })
                    {
                        __all = false;
                        break;
                    }
                }
                __all
            })
    }
}
