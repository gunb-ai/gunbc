// Generated by v1 compiler -- do not edit.
// Source module: v1.compiler.frontend_observation

pub use crate::std_occurrence_identity::OccurrenceTransport;
pub use crate::v1_compiler_compile::front_end_sources;
pub use crate::v1_compiler_compile::{FrontendResult, SourceFile};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v1_std_core::ErrorNode;
pub use crate::v1_std_core::Node;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, Copy, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FrontendPhaseReceipt {
    pub module_count: i64,
    pub occurrence_count: i64,
    pub declaration_count: i64,
    pub reference_count: i64,
    pub graph_present: bool,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OrdinaryFrontendObservation {
    pub subject: Rc<Vec<Rc<SourceFile>>>,
    pub occurrence_transport: Rc<OccurrenceTransport>,
    pub diagnostics: Rc<Vec<Rc<ErrorNode>>>,
    pub receipt: FrontendPhaseReceipt,
}

pub fn frontend_phase_receipt(frontend: Rc<FrontendResult>) -> FrontendPhaseReceipt {
    FrontendPhaseReceipt {
        module_count: match frontend.graph.clone() {
            Some(graph) => (graph.modules.clone().len() as i64),
            None => 0,
        },
        occurrence_count: (frontend
            .occurrence_transport
            .clone()
            .index
            .clone()
            .entries
            .clone()
            .len() as i64),
        declaration_count: (frontend
            .occurrence_transport
            .clone()
            .declarations
            .clone()
            .len() as i64),
        reference_count: (frontend
            .occurrence_transport
            .clone()
            .references
            .clone()
            .len() as i64),
        graph_present: (frontend.graph.clone() != None),
    }
}

pub fn observe_ordinary_frontend(
    sources: Rc<Vec<Rc<SourceFile>>>,
) -> Rc<OrdinaryFrontendObservation> {
    {
        let frontend = front_end_sources(sources.clone());
        Rc::new(OrdinaryFrontendObservation {
            subject: sources.clone(),
            occurrence_transport: frontend.occurrence_transport.clone(),
            diagnostics: frontend.diagnostics.clone(),
            receipt: frontend_phase_receipt(frontend.clone()),
        })
    }
}
