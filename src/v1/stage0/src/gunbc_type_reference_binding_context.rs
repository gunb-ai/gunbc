// Generated by v1 compiler -- do not edit.
// Source module: gunbc.type_reference_binding_context

pub use crate::std_algebra::FreeMonoid;
use crate::std_occurrence_binding_candidates::DeclarationExposure::{
    LexicalExposure, ModuleExposure, RootExposure,
};
pub use crate::std_occurrence_binding_candidates::{
    AuthoredOrderRow, DeclarationExposure, DeclarationExposureRow,
    OccurrenceBindingCandidateInputs, OccurrenceModulePathRow,
};
pub use crate::std_occurrence_identity::{
    AuthoredTokenOrdinal, DeclarationOccurrence, OccurrenceContainmentPath, OccurrenceId,
    OccurrenceIndexEntry, OccurrenceTransport,
};
pub use crate::std_types::{List, NonEmptyStr};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

pub fn type_reference_binding_context_path_scope_note() -> String {
    thread_local! {
        static CACHED: String = {
            "PATH SCOPE (quiet-hawk-219 / calm-badger-682, DESIGN 4b): the coincidence-binding defect class exists on TWO structurally distinct paths. THIS slice closes the RESOLVE path only — v1 typecheck pool-membership coincidence where flat symbol_index_lookup / lookup_type_for hits masquerade as binding (Class B, section 14.1). EMIT-SIDE LIVE INSTANCE (explicitly OUT OF SLICE; fix owned by warm-wolf-814 — cite, do not fix): emit_var_ref / emit_value_ref_ident key the flat item_registry on LEAF NAME ONLY, so module_a.homonym_value can bind module_b's ItemInfo when both declare the same leaf. #7685 did not repair the cross-module same-leaf case; the collision is LIVE on main. #7705's detector cross_module_same_leaf_emits_qualifier_module_not_registry_winner is RED on main (run 30769280389 at 2383069da) — bottom rung: invalid state occurs, detector exists, detection is not prevention. Emit maintains its own registry key and never consults this containment authority. Second grounded specimen of binding-by-insufficient-key on an independent stage; unlike resolve, not instrument-relative (one reproducible witness, positive and negative arms). Any claim that 'binding is now decided by containment' without 'on the resolve path' is rung inflation. Whether emit later consumes containment authority is a follow-on question, not N2.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn type_reference_binding_context_pr_b_motivation_note() -> String {
    thread_local! {
        static CACHED: String = {
            "PR-B motivation (resolve path): Class B pool-membership coincidence on masked type leaves — parallel independent specimen to the emit-side live instance above (#7705 RED control). Closing resolve via containment kernel + resolve_node_bounded changeover is the slice-2 obligation; emit-side repair is warm-wolf-814's subtree.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn type_reference_binding_context_authority_note() -> String {
    thread_local! {
        static CACHED: String = {
            "N2 slice 2 PR-A (quiet-hawk-219): TypeReferenceBindingContext + graph-wide P2 input projection from closure-merged parser OccurrenceTransport for the RESOLVE path only (see type_reference_binding_context_path_scope_note — emit-side leaf collision is a separate surviving instance). Exposure rows derive from the containment tree only — module-root declarations (empty ancestor chain) carry RootExposure; direct module children carry ModuleExposure; nested declarations carry LexicalExposure. SymbolIndex flat lookup and hand-authored module rosters are forbidden exposure authorities on this resolve-path builder. PR-B (post-7515) threads this context into v1.compiler.infer_resolve resolve_node_bounded's masked leaf arm. DISSOLVE-ON: gunbc.type_reference_containment_binding type_reference_containment_binding_dissolve_on.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeReferenceBindingContext {
    pub transport: Rc<OccurrenceTransport>,
    pub inputs: Rc<OccurrenceBindingCandidateInputs>,
}

pub fn type_reference_binding_context_absent_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Absent TypeReferenceBindingContext on resolve_node_bounded preserves the legacy lookup path unchanged (quiet-hawk-219 ruling 2). PR-B wires the masked leaf arm only when context is Present.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn occurrence_containment_parent_scope(
    ancestors: Rc<Vec<OccurrenceId>>,
) -> Option<Rc<OccurrenceContainmentPath>> {
    {
        let __fm = v1_rt::reverse(ancestors.clone());
        if __fm.is_empty() {
            None
        } else {
            let parent_terminal = (*__fm)[0].clone();
            let parent_ancestors_reversed: Rc<Vec<_>> =
                Rc::new((*__fm).iter().skip(1).cloned().collect());
            Some(Rc::new(OccurrenceContainmentPath {
                ancestors: v1_rt::reverse(parent_ancestors_reversed.clone()),
                terminal: parent_terminal.clone(),
            }))
        }
    }
}

pub fn declaration_exposure_from_containment_tree_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Exposure derivation site (PR-A): reads containment prefix shape and module path only — never OccurrenceCategory, never symbol_index flat lookup, never hand-authored module rosters. Empty ancestor chain => RootExposure (structural namespace root in the containment tree). Single-ancestor chain => ModuleExposure for the declaring module. Deeper nesting => LexicalExposure whose exposing_scope is the nearest ancestor containment prefix.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn declaration_exposure_from_containment_tree(
    module_path: String,
    containment: Rc<OccurrenceContainmentPath>,
) -> Rc<DeclarationExposure> {
    {
        let __fm = containment.ancestors.clone();
        if __fm.is_empty() {
            Rc::new(DeclarationExposure::RootExposure)
        } else {
            Rc::new(DeclarationExposure::ModuleExposure {
                module: module_path.clone(),
            })
        }
    }
}

pub fn authored_order_row_from_entry(entry: Rc<OccurrenceIndexEntry>) -> Rc<AuthoredOrderRow> {
    Rc::new(AuthoredOrderRow {
        occurrence: entry.projection.clone().occurrence.clone(),
        ordinal: AuthoredTokenOrdinal {
            value: entry
                .projection
                .clone()
                .diagnostic_span
                .clone()
                .start
                .clone(),
        },
    })
}

pub fn occurrence_binding_inputs_from_module_transport(
    module_path: String,
    transport: Rc<OccurrenceTransport>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    Rc::new(OccurrenceBindingCandidateInputs {
        module_paths: v1_rt::reverse(
            transport
                .index
                .clone()
                .entries
                .clone()
                .iter()
                .cloned()
                .fold(
                    Rc::new(vec![]),
                    |acc: _, entry: Rc<OccurrenceIndexEntry>| {
                        v1_rt::concat(
                            Rc::new(vec![Rc::new(OccurrenceModulePathRow {
                                occurrence: entry.projection.clone().occurrence.clone(),
                                module_path: module_path.clone(),
                            })]),
                            acc,
                        )
                    },
                ),
        ),
        exposure_rows: v1_rt::reverse(transport.declarations.clone().iter().cloned().fold(
            Rc::new(vec![]),
            |acc: _, declaration: Rc<DeclarationOccurrence>| {
                v1_rt::concat(
                    Rc::new(vec![Rc::new(DeclarationExposureRow {
                        occurrence: declaration.occurrence.clone(),
                        exposure: declaration_exposure_from_containment_tree(
                            module_path.clone(),
                            declaration.containment.clone(),
                        ),
                    })]),
                    acc,
                )
            },
        )),
        authored_order_rows: v1_rt::reverse(
            transport
                .index
                .clone()
                .entries
                .clone()
                .iter()
                .cloned()
                .fold(
                    Rc::new(vec![]),
                    |acc: _, entry: Rc<OccurrenceIndexEntry>| {
                        v1_rt::concat(
                            Rc::new(vec![authored_order_row_from_entry(entry.clone())]),
                            acc,
                        )
                    },
                ),
        ),
    })
}

pub fn merge_occurrence_binding_input_rows(
    left: Rc<OccurrenceBindingCandidateInputs>,
    right: Rc<OccurrenceBindingCandidateInputs>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    Rc::new(OccurrenceBindingCandidateInputs {
        module_paths: v1_rt::concat(left.module_paths.clone(), right.module_paths.clone()),
        exposure_rows: v1_rt::concat(left.exposure_rows.clone(), right.exposure_rows.clone()),
        authored_order_rows: v1_rt::concat(
            left.authored_order_rows.clone(),
            right.authored_order_rows.clone(),
        ),
    })
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ModuleOccurrenceBindingSource {
    pub module_path: NonEmptyStr,
    pub transport: Rc<OccurrenceTransport>,
}

pub fn build_graph_type_reference_binding_inputs(
    sources: Rc<Vec<Rc<ModuleOccurrenceBindingSource>>>,
) -> Rc<OccurrenceBindingCandidateInputs> {
    sources.clone().iter().cloned().fold(
        Rc::new(OccurrenceBindingCandidateInputs {
            module_paths: Rc::new(vec![]),
            exposure_rows: Rc::new(vec![]),
            authored_order_rows: Rc::new(vec![]),
        }),
        |acc: Rc<OccurrenceBindingCandidateInputs>, source: Rc<ModuleOccurrenceBindingSource>| {
            merge_occurrence_binding_input_rows(
                acc,
                occurrence_binding_inputs_from_module_transport(
                    source.module_path.clone(),
                    source.transport.clone(),
                ),
            )
        },
    )
}

pub fn build_type_reference_binding_context(
    transport: Rc<OccurrenceTransport>,
    sources: Rc<Vec<Rc<ModuleOccurrenceBindingSource>>>,
) -> Rc<TypeReferenceBindingContext> {
    Rc::new(TypeReferenceBindingContext {
        transport: transport.clone(),
        inputs: build_graph_type_reference_binding_inputs(sources.clone()),
    })
}
