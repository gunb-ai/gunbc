// Generated by v1 compiler -- do not edit.
// Source module: std.operator_realization

use self::OperandRealization::*;
use self::OperatorRealization::*;
use self::OperatorRealizationRefusal::*;
use self::OrderingTest::*;
use self::StructuralOrderingLookup::*;
pub use crate::std_decl_ref::declaration_ref_eq;
pub use crate::std_decl_ref::DeclarationRef;
pub use crate::std_syntax::BinOp;
use crate::std_syntax::BinOp::{
    Add, And, Div, Eq, Ge, Gt, Le, Lt, Mod, Mul, Ne, NullCoalesce, Or, Sub,
};
use crate::std_types::Bool::*;
pub use crate::std_types::{Bool, List, NonEmptyStr};
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum OperandRealization {
    HostNumericOperand,
    StructuralOperand { declaration: Rc<DeclarationRef> },
    OperandIdentityUnavailable,
}
impl OperandRealization {
    pub fn declaration(&self) -> Rc<DeclarationRef> {
        match self {
            OperandRealization::HostNumericOperand => panic!("no declaration on unit variant"),
            OperandRealization::StructuralOperand {
                declaration: __val, ..
            } => __val.clone(),
            OperandRealization::OperandIdentityUnavailable => {
                panic!("no declaration on unit variant")
            }
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StructuralOrderingBinding {
    pub carrier: Rc<DeclarationRef>,
    pub compare: Rc<DeclarationRef>,
    pub ordering: Rc<DeclarationRef>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum StructuralOrderingLookup {
    StructuralOrderingFound { row: Rc<StructuralOrderingBinding> },
    StructuralOrderingAbsent,
    StructuralOrderingAmbiguous { row_count: i64 },
}

pub fn structural_ordering_for(
    rows: Rc<Vec<Rc<StructuralOrderingBinding>>>,
    carrier: Rc<DeclarationRef>,
) -> Rc<StructuralOrderingLookup> {
    {
        let matching = Rc::new({
            let mut __result = Vec::new();
            for r in rows.iter().cloned() {
                if crate::std_decl_ref::declaration_ref_eq(r.carrier.clone(), carrier.clone()) {
                    __result.push(r);
                }
            }
            __result
        });
        let n = (matching.clone().len() as i64);
        if (n.clone() == 0) {
            Rc::new(StructuralOrderingLookup::StructuralOrderingAbsent)
        } else {
            if (n.clone() == 1) {
                match matching.clone().first().cloned() {
                    Some(row) => Rc::new(StructuralOrderingLookup::StructuralOrderingFound {
                        row: row.clone(),
                    }),
                    None => Rc::new(StructuralOrderingLookup::StructuralOrderingAbsent),
                }
            } else {
                Rc::new(StructuralOrderingLookup::StructuralOrderingAmbiguous {
                    row_count: n.clone(),
                })
            }
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum OrderingTest {
    OrderingIs { variant: NonEmptyStr },
    OrderingIsNot { variant: NonEmptyStr },
}
impl OrderingTest {
    pub fn variant(&self) -> NonEmptyStr {
        match self {
            OrderingTest::OrderingIs { variant: __val, .. } => __val.clone(),
            OrderingTest::OrderingIsNot { variant: __val, .. } => __val.clone(),
        }
    }
}

pub fn ordering_test_for(op: BinOp) -> Option<Rc<OrderingTest>> {
    match op.clone() {
        BinOp::Lt => Some(Rc::new(OrderingTest::OrderingIs {
            variant: "Less".to_string(),
        })),
        BinOp::Gt => Some(Rc::new(OrderingTest::OrderingIs {
            variant: "Greater".to_string(),
        })),
        BinOp::Le => Some(Rc::new(OrderingTest::OrderingIsNot {
            variant: "Greater".to_string(),
        })),
        BinOp::Ge => Some(Rc::new(OrderingTest::OrderingIsNot {
            variant: "Less".to_string(),
        })),
        BinOp::Add => None,
        BinOp::Sub => None,
        BinOp::Mul => None,
        BinOp::Div => None,
        BinOp::Mod => None,
        BinOp::Eq => None,
        BinOp::Ne => None,
        BinOp::And => None,
        BinOp::Or => None,
        BinOp::NullCoalesce => None,
    }
}

pub fn binop_is_equality(op: BinOp) -> bool {
    match op.clone() {
        BinOp::Eq => true,
        BinOp::Ne => true,
        BinOp::Add => false,
        BinOp::Sub => false,
        BinOp::Mul => false,
        BinOp::Div => false,
        BinOp::Mod => false,
        BinOp::Lt => false,
        BinOp::Gt => false,
        BinOp::Le => false,
        BinOp::Ge => false,
        BinOp::And => false,
        BinOp::Or => false,
        BinOp::NullCoalesce => false,
    }
}

pub fn binop_is_ordering(op: BinOp) -> bool {
    (ordering_test_for(op.clone()) != None)
}

pub fn binop_label(op: BinOp) -> String {
    match op.clone() {
        BinOp::Add => "+".to_string(),
        BinOp::Sub => "-".to_string(),
        BinOp::Mul => "*".to_string(),
        BinOp::Div => "/".to_string(),
        BinOp::Mod => "%".to_string(),
        BinOp::Eq => "==".to_string(),
        BinOp::Ne => "!=".to_string(),
        BinOp::Lt => "<".to_string(),
        BinOp::Gt => ">".to_string(),
        BinOp::Le => "<=".to_string(),
        BinOp::Ge => ">=".to_string(),
        BinOp::And => "&&".to_string(),
        BinOp::Or => "||".to_string(),
        BinOp::NullCoalesce => "??".to_string(),
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum OperatorRealizationRefusal {
    NoStructuralOperationDeclared {
        declaration: Rc<DeclarationRef>,
        operator: String,
    },
    StructuralOrderingDuplicated {
        declaration: Rc<DeclarationRef>,
        row_count: i64,
    },
}
impl OperatorRealizationRefusal {
    pub fn declaration(&self) -> Rc<DeclarationRef> {
        match self {
            OperatorRealizationRefusal::NoStructuralOperationDeclared {
                declaration: __val,
                ..
            } => __val.clone(),
            OperatorRealizationRefusal::StructuralOrderingDuplicated {
                declaration: __val, ..
            } => __val.clone(),
        }
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum OperatorRealization {
    HostOperator,
    StructuralEquality {
        declaration: Rc<DeclarationRef>,
    },
    StructuralComparison {
        declaration: Rc<DeclarationRef>,
        binding: Rc<StructuralOrderingBinding>,
        test: Rc<OrderingTest>,
    },
    OperatorRealizationRefused {
        cause: Rc<OperatorRealizationRefusal>,
    },
}

pub fn operator_realization_refusal_message(cause: Rc<OperatorRealizationRefusal>) -> String {
    match (*cause.clone()).clone() {
    OperatorRealizationRefusal::NoStructuralOperationDeclared { declaration: d, operator: o, .. } => v1_rt::concat(v1_rt::concat(v1_rt::concat(v1_rt::concat(v1_rt::concat(v1_rt::concat("operator realization: host operator `".to_string(), o.clone()), "` on structural operand ".to_string()), d.module_path.clone()), ".".to_string()), d.decl_name.clone()), " -- the declaration has no host realization and declares no operation for this operator; spell the operation as a call to the declared structural operation".to_string()),
    OperatorRealizationRefusal::StructuralOrderingDuplicated { declaration: d, row_count: n, .. } => v1_rt::concat(v1_rt::concat(v1_rt::concat(v1_rt::concat("operator realization: structural ordering for ".to_string(), d.module_path.clone()), ".".to_string()), d.decl_name.clone()), " has more than one declared comparison (gunbc.structural_realization_bindings authoring defect)".to_string()),
}
}

pub fn operator_realization_for(
    op: BinOp,
    operand: Rc<OperandRealization>,
    ordering_rows: Rc<Vec<Rc<StructuralOrderingBinding>>>,
) -> Rc<OperatorRealization> {
    match (*operand.clone()).clone() {
        OperandRealization::HostNumericOperand => Rc::new(OperatorRealization::HostOperator),
        OperandRealization::OperandIdentityUnavailable => {
            Rc::new(OperatorRealization::HostOperator)
        }
        OperandRealization::StructuralOperand { declaration: d, .. } => match op.clone() {
            BinOp::Eq => Rc::new(OperatorRealization::StructuralEquality {
                declaration: d.clone(),
            }),
            BinOp::Ne => Rc::new(OperatorRealization::StructuralEquality {
                declaration: d.clone(),
            }),
            BinOp::Lt => {
                structural_ordering_realization(op.clone(), d.clone(), ordering_rows.clone())
            }
            BinOp::Gt => {
                structural_ordering_realization(op.clone(), d.clone(), ordering_rows.clone())
            }
            BinOp::Le => {
                structural_ordering_realization(op.clone(), d.clone(), ordering_rows.clone())
            }
            BinOp::Ge => {
                structural_ordering_realization(op.clone(), d.clone(), ordering_rows.clone())
            }
            BinOp::And => Rc::new(OperatorRealization::HostOperator),
            BinOp::Or => Rc::new(OperatorRealization::HostOperator),
            BinOp::NullCoalesce => Rc::new(OperatorRealization::HostOperator),
            BinOp::Add => structural_arithmetic_refusal(op.clone(), d.clone()),
            BinOp::Sub => structural_arithmetic_refusal(op.clone(), d.clone()),
            BinOp::Mul => structural_arithmetic_refusal(op.clone(), d.clone()),
            BinOp::Div => structural_arithmetic_refusal(op.clone(), d.clone()),
            BinOp::Mod => structural_arithmetic_refusal(op.clone(), d.clone()),
        },
    }
}

pub fn structural_arithmetic_refusal(
    op: BinOp,
    declaration: Rc<DeclarationRef>,
) -> Rc<OperatorRealization> {
    Rc::new(OperatorRealization::OperatorRealizationRefused {
        cause: Rc::new(OperatorRealizationRefusal::NoStructuralOperationDeclared {
            declaration: declaration.clone(),
            operator: binop_label(op.clone()),
        }),
    })
}

pub fn structural_ordering_realization(
    op: BinOp,
    declaration: Rc<DeclarationRef>,
    ordering_rows: Rc<Vec<Rc<StructuralOrderingBinding>>>,
) -> Rc<OperatorRealization> {
    match ordering_test_for(op.clone()) {
        None => structural_arithmetic_refusal(op.clone(), declaration.clone()),
        Some(test) => {
            match (*structural_ordering_for(ordering_rows.clone(), declaration.clone())).clone() {
                StructuralOrderingLookup::StructuralOrderingFound { row: row, .. } => {
                    Rc::new(OperatorRealization::StructuralComparison {
                        declaration: declaration.clone(),
                        binding: row.clone(),
                        test: test.clone(),
                    })
                }
                StructuralOrderingLookup::StructuralOrderingAbsent => {
                    structural_arithmetic_refusal(op.clone(), declaration.clone())
                }
                StructuralOrderingLookup::StructuralOrderingAmbiguous { row_count: n, .. } => {
                    Rc::new(OperatorRealization::OperatorRealizationRefused {
                        cause: Rc::new(OperatorRealizationRefusal::StructuralOrderingDuplicated {
                            declaration: declaration.clone(),
                            row_count: n.clone(),
                        }),
                    })
                }
            }
        }
    }
}
