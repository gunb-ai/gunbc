// Generated by v1 compiler -- do not edit.
// Source module: v2.std.constraints

use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_constraint_satisfaction_predicate::constraint_satisfaction_preservation_holds;
use crate::v2_std_diagnostic::Correction::*;
use crate::v2_std_diagnostic::NoCorrectionReason::*;
use crate::v2_std_diagnostic::Outcome::*;
pub use crate::v2_std_diagnostic::{
    bind_outcome, node_locus, outcome_rejected, outcome_with_diagnostics,
};
pub use crate::v2_std_diagnostic::{
    Correction, Diagnostic, Diagnostics, NoCorrectionReason, Outcome,
};
pub use crate::v2_std_find_witness::find_witness;
use crate::v2_std_find_witness::MultiplicityPolicy::*;
pub use crate::v2_std_find_witness::{CandidateSet, MultiplicityPolicy, PreservationPredicate};
pub use crate::v2_std_node::well_formed;
pub use crate::v2_std_node::Node;
pub use crate::v2_std_witness::StructuralPropertyWitness;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConstraintGraph {
    pub root: Rc<Node>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CanonicalGrounding {
    pub node: Rc<Node>,
    pub witness: Rc<CanonicalGroundingWitness>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConstraintSatisfactionPredicate {
    pub algebra: Rc<Node>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConstraintSolvePolicy {
    pub graph: Rc<ConstraintGraph>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CanonicalGroundingCandidateSet {
    pub set: Rc<CandidateSet>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CanonicalGroundingWitness {
    pub structural: Rc<StructuralPropertyWitness>,
    pub closedness: Rc<StructuralPropertyWitness>,
}

pub fn constraints_not_realized_diagnostic(graph: Rc<ConstraintGraph>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "constraints_not_realized".to_string(),
        at: node_locus(graph.root.clone()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn constraints_ill_formed_root_diagnostic(graph: Rc<ConstraintGraph>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "constraints_ill_formed_root".to_string(),
        at: node_locus(graph.root.clone()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn grounding_evidence_is_source_diagnostic(node: Rc<Node>) -> Rc<Diagnostic> {
    Rc::new(Diagnostic {
        reason: "grounding_evidence_is_source".to_string(),
        at: node_locus(node.clone()),
        correction: Rc::new(Correction::Unavailable {
            reason: NoCorrectionReason::ExternalContractUnknown,
        }),
    })
}

pub fn canonical_grounding_from_derived_type(
    node: Rc<Node>,
    derived_type: Rc<Node>,
) -> Rc<Outcome<Rc<CanonicalGrounding>>> {
    if (derived_type.clone() == node.clone()) {
        outcome_rejected(grounding_evidence_is_source_diagnostic(node.clone()))
    } else {
        Rc::new(Outcome::Accepted {
            value: Rc::new(CanonicalGrounding {
                node: node.clone(),
                witness: Rc::new(CanonicalGroundingWitness {
                    structural: StructuralPropertyWitness {
                        property: "constraint_property_being_canonical".to_string(),
                        evidence: derived_type.clone(),
                    },
                    closedness: StructuralPropertyWitness {
                        property: "constraint_property_candidate_set_closedness".to_string(),
                        evidence: node.clone(),
                    },
                }),
            }),
            diagnostics: None,
        })
    }
}

pub fn constraint_satisfaction_holds(
    source_facts: Rc<Node>,
    candidate: Rc<Node>,
    predicate: Rc<ConstraintSatisfactionPredicate>,
) -> bool {
    crate::v2_std_constraint_satisfaction_predicate::constraint_satisfaction_preservation_holds(
        source_facts.clone(),
        candidate.clone(),
        predicate.algebra.clone(),
    )
}

pub fn constraint_satisfaction_predicate(algebra: Rc<Node>) -> Rc<ConstraintSatisfactionPredicate> {
    Rc::new(ConstraintSatisfactionPredicate {
        algebra: algebra.clone(),
    })
}

pub fn constraint_satisfaction_preservation_predicate(
    predicate: Rc<ConstraintSatisfactionPredicate>,
) -> Rc<PreservationPredicate> {
    Rc::new(PreservationPredicate {
        algebra: predicate.algebra.clone(),
        preservation_rule: "preservation_rule_constraint_satisfaction".to_string(),
    })
}

pub fn canonical_grounding_candidate_set_closedness(
    root: Rc<Node>,
) -> Rc<StructuralPropertyWitness> {
    Rc::new(StructuralPropertyWitness {
        property: "constraint_property_candidate_set_closedness".to_string(),
        evidence: root.clone(),
    })
}

pub fn canonical_grounding_candidates(
    policy: Rc<ConstraintSolvePolicy>,
) -> Rc<Outcome<Rc<CanonicalGroundingCandidateSet>>> {
    if well_formed(policy.graph.clone().root.clone()) {
        Rc::new(Outcome::Accepted {
            value: Rc::new(CanonicalGroundingCandidateSet {
                set: CandidateSet {
                    candidates: Rc::new(vec![policy.graph.clone().root.clone()]),
                    closedness: canonical_grounding_candidate_set_closedness(
                        policy.graph.clone().root.clone(),
                    ),
                },
            }),
            diagnostics: None,
        })
    } else {
        outcome_rejected(constraints_ill_formed_root_diagnostic(policy.graph.clone()))
    }
}

pub fn solve_constraints(policy: Rc<ConstraintSolvePolicy>) -> Rc<Outcome<Rc<CanonicalGrounding>>> {
    bind_outcome(
        canonical_grounding_candidates(policy.clone()),
        |grounding_candidates| match (*find_witness(
            policy.graph.clone().root.clone(),
            grounding_candidates.set.clone(),
            constraint_satisfaction_preservation_predicate(constraint_satisfaction_predicate(
                policy.graph.clone().root.clone(),
            )),
            Rc::new(MultiplicityPolicy::UniqueOnly),
        ))
        .clone()
        {
            Outcome::Accepted {
                value: fw,
                diagnostics: fw_d,
                ..
            } => outcome_with_diagnostics(
                Rc::new(CanonicalGrounding {
                    node: fw.candidate.clone(),
                    witness: Rc::new(CanonicalGroundingWitness {
                        structural: fw.witness.clone(),
                        closedness: fw.closedness.clone(),
                    }),
                }),
                fw_d.clone(),
            ),
            Outcome::Rejected { diagnostics: r, .. } => Rc::new(Outcome::Rejected {
                diagnostics: r.clone(),
            }),
        },
    )
}
