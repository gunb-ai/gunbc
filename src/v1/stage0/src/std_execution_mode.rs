// Generated by v1 compiler -- do not edit.
// Source module: std.execution_mode

use self::ExecutionMode::*;
pub use crate::std_types::Bool;
use crate::std_types::Bool::*;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(
    Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, serde::Serialize, serde::Deserialize,
)]
#[serde(tag = "_variant")]
pub enum ExecutionMode {
    Hermetic,
    Wet,
    Record,
}

pub fn execution_mode_is_wet_dispatch(mode: ExecutionMode) -> bool {
    match mode.clone() {
        ExecutionMode::Hermetic => false,
        ExecutionMode::Wet => true,
        ExecutionMode::Record => true,
    }
}

pub fn execution_mode_eq(left: ExecutionMode, right: ExecutionMode) -> bool {
    match left.clone() {
        ExecutionMode::Hermetic => match right.clone() {
            ExecutionMode::Hermetic => true,
            ExecutionMode::Wet => false,
            ExecutionMode::Record => false,
        },
        ExecutionMode::Wet => match right.clone() {
            ExecutionMode::Hermetic => false,
            ExecutionMode::Wet => true,
            ExecutionMode::Record => false,
        },
        ExecutionMode::Record => match right.clone() {
            ExecutionMode::Hermetic => false,
            ExecutionMode::Wet => false,
            ExecutionMode::Record => true,
        },
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Hermetic;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Wet;
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub struct Record;
