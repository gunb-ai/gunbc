// Generated by v1 compiler -- do not edit.
// Source module: v2.std.nat

use self::Nat::*;
use crate::std_algebra::Ordering::*;
pub use crate::std_algebra::{CommutativeMonoid, CommutativeSemiring, Ordering};
pub use crate::std_types::List;
use crate::v1_rt;
use crate::v1_rt::{VecCompat, VecJoin};
pub use crate::v2_std_node::Symbol;
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum Nat {
    Zero,
    Succ { prev: Rc<Nat> },
}
impl Nat {
    pub fn prev(&self) -> Rc<Nat> {
        match self {
            Nat::Zero => panic!("no prev on unit variant"),
            Nat::Succ { prev: __val, .. } => __val.clone(),
        }
    }
}

pub fn nat_cata<A: Clone>(n: Rc<Nat>, zero: A, succ: impl Fn(A) -> A + Clone) -> A {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || match (*n.clone()).clone() {
        Nat::Zero => zero,
        Nat::Succ { prev: p, .. } => succ.clone()(nat_cata(p.clone(), zero, succ.clone())),
    })
}

pub fn nat_add(a: Rc<Nat>, b: Rc<Nat>) -> Rc<Nat> {
    nat_cata(a.clone(), b.clone(), |acc| {
        Rc::new(Nat::Succ { prev: acc.clone() })
    })
}

pub fn nat_mul(a: Rc<Nat>, b: Rc<Nat>) -> Rc<Nat> {
    nat_cata(a.clone(), Rc::new(Nat::Zero), |acc| {
        nat_add(b.clone(), acc.clone())
    })
}

pub fn nat_compare(a: Rc<Nat>, b: Rc<Nat>) -> Ordering {
    if (a.clone() == b.clone()) {
        Ordering::Equal
    } else {
        if (a.clone() < b.clone()) {
            Ordering::Less
        } else {
            Ordering::Greater
        }
    }
}

pub fn is_zero(n: Rc<Nat>) -> bool {
    match (*n.clone()).clone() {
        Nat::Zero => true,
        Nat::Succ { prev: _, .. } => false,
    }
}

pub fn nat_lte(a: Rc<Nat>, b: Rc<Nat>) -> bool {
    match crate::v2_std_nat::nat_compare(a.clone(), b.clone()) {
        Ordering::Less => true,
        Ordering::Equal => true,
        Ordering::Greater => false,
    }
}

pub fn nat_max_two_nat_authorities_note() -> String {
    thread_local! {
        static CACHED: String = {
            "This is NOT a duplicate of std.nat nat_max, and the distinction is worth stating because the name alone suggests otherwise. std.nat Nat is CommutativeSemiring<Magnitude> - an algebra alias whose max is written with the host comparison operators - while v2.std.nat Nat is the Peano coproduct Zero | Succ, and the two are different types that no single function can serve. So this sits beside nat_add and nat_mul, which are forked for exactly the same reason and were not remarked on. What IS a real defect is that two Nat authorities exist at all; it predates this declaration and is the numeric-tower grounding thread's subject, not something a max function can close. Because v1-seed function names are not module-scoped, a bare nat_max reference in any closure containing both modules is ambiguous and must be qualified - v2.lens.cost.valuation does exactly that. Dissolve-on: one Nat authority, at which point one of the two nat_max declarations goes with it.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn nat_max(a: Rc<Nat>, b: Rc<Nat>) -> Rc<Nat> {
    match crate::v2_std_nat::nat_compare(a.clone(), b.clone()) {
        Ordering::Less => b.clone(),
        Ordering::Equal => a.clone(),
        Ordering::Greater => a.clone(),
    }
}

pub fn nat_gte(a: Rc<Nat>, b: Rc<Nat>) -> bool {
    match crate::v2_std_nat::nat_compare(a.clone(), b.clone()) {
        Ordering::Less => false,
        Ordering::Equal => true,
        Ordering::Greater => true,
    }
}

pub fn nat_additive_commutative_monoid() -> Rc<CommutativeMonoid<Rc<Nat>>> {
    thread_local! {
            static CACHED: Rc<CommutativeMonoid<Rc<Nat>>> = {
                Rc::new(CommutativeMonoid {
        op: nat_add,
        identity: Rc::new(Nat::Zero),
    })
            };
        }
    CACHED.with(|c: &Rc<CommutativeMonoid<Rc<Nat>>>| c.clone())
}

pub fn nat_semiring() -> Rc<CommutativeSemiring<Rc<Nat>>> {
    thread_local! {
            static CACHED: Rc<CommutativeSemiring<Rc<Nat>>> = {
                Rc::new(CommutativeSemiring {
        add: nat_add,
        zero: Rc::new(Nat::Zero),
        mul: nat_mul,
        one: Rc::new(Nat::Succ {
        prev: Rc::new(Nat::Zero),
    }),
    })
            };
        }
    CACHED.with(|c: &Rc<CommutativeSemiring<Rc<Nat>>>| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct NatAlgebraLawObligation {
    pub algebra: String,
    pub inhabitant: String,
    pub law: String,
}

pub fn nat_declared_algebra_law_obligations() -> Rc<Vec<Rc<NatAlgebraLawObligation>>> {
    Rc::new(vec![
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_additive_monoid".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_add_left_identity".to_string(),
        }),
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_additive_monoid".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_add_right_identity".to_string(),
        }),
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_additive_monoid".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_add_associativity".to_string(),
        }),
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_commutative_semiring".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_mul_left_identity".to_string(),
        }),
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_commutative_semiring".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_mul_annihilator".to_string(),
        }),
        Rc::new(NatAlgebraLawObligation {
            algebra: "nat_algebra_law_subject_symbol_commutative_semiring".to_string(),
            inhabitant: "nat_algebra_law_subject_symbol_inhabitant_nat".to_string(),
            law: "law_nat_mul_associativity".to_string(),
        }),
    ])
}
