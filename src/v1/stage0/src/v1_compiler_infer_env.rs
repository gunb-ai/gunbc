// Generated by v1 compiler -- do not edit.
// Source module: v1.compiler.infer_env

use self::GlobalBareLookupState::*;
use crate::std_induction::RecursionShape::{
    DirectRecursion, ListRecursion, MapValueRecursion, OptionalRecursion, SetRecursion,
};
use crate::std_induction::SubValueRelation::{PreservedValue, SubValueUnknown};
pub use crate::std_induction::{InductiveField, RecursionShape, SubValueRelation};
pub use crate::std_types::is_kernel_type;
pub use crate::std_types::SourceSpan;
use crate::v1_rt;
use crate::v1_rt::Witness;
use crate::v1_rt::Witness::{Holds, Violates};
use crate::v1_rt::{VecCompat, VecJoin};
use crate::v1_std_core::Cardinality::*;
use crate::v1_std_core::Connective::*;
use crate::v1_std_core::ExprData::*;
use crate::v1_std_core::InferredNode::*;
pub use crate::v1_std_core::{
    authored_name_at, empty_intern_table, find_child_named, intern, intern_find, intern_str,
    kernel_span, merge_intern_tables, module_path_segments, param_node_name_at,
    param_node_type_expr, source_text_at,
};
pub use crate::v1_std_core::{
    Cardinality, Connective, ExprData, InferredNode, InternTable, NewlineIndex, Node,
};
use crate::NonEmptyBTreeSet;
use crate::NonEmptyVec;
use im::{vector as vec, HashMap, OrdSet as BTreeSet, Vector as Vec};
use std::rc::Rc;
use std::sync::Arc;

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeEnv {
    pub bindings: Arc<HashMap<i64, Arc<TypeBinding>>>,
    pub str_bindings: Arc<HashMap<String, Arc<TypeBinding>>>,
    pub ancestry_str_bindings: Arc<HashMap<String, Arc<TypeBinding>>>,
    pub parents: Arc<Vec<Arc<TypeEnv>>>,
    pub recursive_types: Arc<Vec<i64>>,
    pub recursive_type_set: Arc<HashMap<i64, bool>>,
    pub inductive_fields: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>,
    pub source_indices: Arc<HashMap<String, Arc<NewlineIndex>>>,
    pub intern_table: Arc<InternTable>,
    pub source_visible_names: Arc<HashMap<String, bool>>,
    pub symbol_index: Arc<SymbolIndex>,
    pub module_path: String,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeBinding {
    pub name: String,
    pub resolved: Arc<Node>,
    pub provenance: Arc<SubValueRelation>,
}

pub fn global_bare_fallback_invariant() -> String {
    thread_local! {
        static CACHED: String = {
            "Corpus-wide bare-name census, resolved by ONE uniform containment walk (operator ruling 2026-07-18: global uniqueness is NOT a special tier — it is the shallowest level of the same walk; filepaths are irrelevant, the declared module path is the containment tree). SymbolIndex.global_bare is keyed on the bare declared name and built once by build_symbol_index_census over graph.modules before any module typechecks (order-independent). Tracking is decl-only via symbol_index_insert_decl / local_binding_for_item (type/fn/data names) plus corpus-globally-unique Disj variant aliases (unique across the WHOLE bare-name space: a variant whose name any type/fn/data decl claims stays qualified-only, never a global_bare candidate — else a type-vs-variant tie, e.g. actions.Job vs ExpressionContext.Job, refuses every far use of the TYPE); every homonym keeps its FULL candidate list (module_path + binding), never a candidate-free Ambiguous tombstone. Resolution (global_bare_lookup): a single candidate resolves from anywhere (the one-candidate degenerate case of the walk); multiple candidates resolve by nearest-ancestor containment — the candidate whose module path shares the strictly longest leading-segment prefix with the REFERENCING module (TypeEnv.module_path) wins; any tie at the max, including the all-disjoint lcp=0 case, REFUSES (Absent — fail-closed, never guesses, §5; the source must qualify by containment path). lookup_binding_by_name consults it only after str_bindings/ancestry_str_bindings/intern+bindings all miss. Variant arms additionally merge into per-module variant_locals via merge_global_bare_variant_locals (constructor_binding_authority — owner is the coproduct node).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn qualified_module_projection_invariant() -> String {
    thread_local! {
        static CACHED: String = {
            "Grammar lane G1/G1b: container.member module projection in value/type/pattern and fn/data/let type-annotation positions resolves via symbol_index_lookup on the full qualified path (module projection), not value field-access — v2 resolve: TypeNode Conj QN spines; v1 typecheck: lookup_binding_by_name after global_bare; v1 patterns: lookup_variant_in_type routes dotted variant names through symbol_index_lookup. build_symbol_index_census materializes declared-module item paths plus module-unique Disj variant aliases (qualified path always; corpus-unique variants also enter global_bare — mirrors v2 symbol_index_fill_unique_variant_aliases); lookup_qualified_module_projection runs only when the reference carries a dot (fail-closed on miss — never widens to field-access semantics).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GlobalBareCandidate {
    pub module_path: String,
    pub binding: Arc<TypeBinding>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(tag = "_variant")]
pub enum GlobalBareLookupState {
    GlobalBareUniqueBinding {
        module_path: String,
        binding: Arc<TypeBinding>,
    },
    GlobalBareAmbiguousBinding {
        candidates: Arc<Vec<Arc<GlobalBareCandidate>>>,
    },
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ImportBinding {
    pub name: String,
    pub resolved: Arc<Node>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ScopeBinding {
    pub name: String,
    pub resolved: Arc<Node>,
    pub provenance: Arc<SubValueRelation>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ServiceCensusEntry {
    pub module_path: String,
    pub item: Arc<Node>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SymbolIndex {
    pub entries: Arc<HashMap<String, Arc<Node>>>,
    pub global_bare: Arc<HashMap<String, Arc<GlobalBareLookupState>>>,
    pub services: Arc<HashMap<String, Arc<ServiceCensusEntry>>>,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Scope {
    pub locals: Arc<HashMap<String, Arc<ScopeBinding>>>,
}

pub fn empty_symbol_index() -> Arc<SymbolIndex> {
    Arc::new(SymbolIndex {
        entries: v1_rt::rc_empty_map::<String, Arc<Node>>(),
        global_bare: v1_rt::rc_empty_map::<String, Arc<GlobalBareLookupState>>(),
        services: v1_rt::rc_empty_map::<String, Arc<ServiceCensusEntry>>(),
    })
}

pub fn empty_type_env() -> Arc<TypeEnv> {
    Arc::new(TypeEnv {
        module_path: "".to_string(),
        bindings: v1_rt::rc_empty_map::<i64, Arc<TypeBinding>>(),
        str_bindings: v1_rt::rc_empty_map::<String, Arc<TypeBinding>>(),
        ancestry_str_bindings: v1_rt::rc_empty_map::<String, Arc<TypeBinding>>(),
        parents: Rc::new(vec![]),
        recursive_types: Rc::new(vec![]),
        recursive_type_set: v1_rt::rc_empty_map::<i64, bool>(),
        inductive_fields: v1_rt::rc_empty_map::<String, Arc<Vec<Arc<InductiveField>>>>(),
        source_indices: v1_rt::rc_empty_map::<String, Arc<NewlineIndex>>(),
        intern_table: empty_intern_table(),
        source_visible_names: v1_rt::rc_empty_map::<String, bool>(),
        symbol_index: empty_symbol_index(),
    })
}

pub fn symbol_index_lookup(index: Arc<SymbolIndex>, qualified_name: String) -> Option<Arc<Node>> {
    v1_rt::map_get(&index.entries.clone(), qualified_name.clone())
}

pub fn global_bare_candidates_contain(
    candidates: Arc<Vec<Arc<GlobalBareCandidate>>>,
    resolved: Arc<Node>,
) -> bool {
    {
        let mut __found = false;
        for c in candidates.clone().iter().cloned() {
            if (c.binding.clone().resolved.clone() == resolved.clone()) {
                __found = true;
                break;
            }
        }
        __found
    }
}

pub fn symbol_index_track_global_bare(
    global_bare: Arc<HashMap<String, Arc<GlobalBareLookupState>>>,
    module_path: String,
    binding: Arc<TypeBinding>,
) -> Arc<HashMap<String, Arc<GlobalBareLookupState>>> {
    match v1_rt::map_get(&global_bare, binding.name.clone())
        .as_deref()
        .cloned()
    {
        Some(GlobalBareLookupState::GlobalBareAmbiguousBinding {
            candidates: cands, ..
        }) => {
            if global_bare_candidates_contain(cands.clone(), binding.resolved.clone()) {
                global_bare.clone()
            } else {
                v1_rt::rc_map_insert(
                    global_bare.clone(),
                    binding.name.clone(),
                    Arc::new(GlobalBareLookupState::GlobalBareAmbiguousBinding {
                        candidates: v1_rt::concat(
                            cands.clone(),
                            Rc::new(vec![Arc::new(GlobalBareCandidate {
                                module_path: module_path.clone(),
                                binding: binding.clone(),
                            })]),
                        ),
                    }),
                )
            }
        }
        Some(GlobalBareLookupState::GlobalBareUniqueBinding {
            module_path: existing_path,
            binding: existing,
            ..
        }) => {
            if (existing.resolved.clone() == binding.resolved.clone()) {
                global_bare.clone()
            } else {
                v1_rt::rc_map_insert(
                    global_bare.clone(),
                    binding.name.clone(),
                    Arc::new(GlobalBareLookupState::GlobalBareAmbiguousBinding {
                        candidates: Rc::new(vec![
                            Arc::new(GlobalBareCandidate {
                                module_path: existing_path.clone(),
                                binding: existing.clone(),
                            }),
                            Arc::new(GlobalBareCandidate {
                                module_path: module_path.clone(),
                                binding: binding.clone(),
                            }),
                        ]),
                    }),
                )
            }
        }
        None => v1_rt::rc_map_insert(
            global_bare.clone(),
            binding.name.clone(),
            Arc::new(GlobalBareLookupState::GlobalBareUniqueBinding {
                module_path: module_path.clone(),
                binding: binding.clone(),
            }),
        ),
    }
}

pub fn symbol_index_insert(
    index: Arc<SymbolIndex>,
    qualified_name: String,
    resolved: Arc<Node>,
) -> Arc<SymbolIndex> {
    Arc::new(SymbolIndex {
        entries: v1_rt::rc_map_insert(
            index.entries.clone(),
            qualified_name.clone(),
            resolved.clone(),
        ),
        global_bare: index.global_bare.clone(),
        services: index.services.clone(),
    })
}

pub fn symbol_index_insert_decl(
    index: Arc<SymbolIndex>,
    module_path: String,
    binding: Arc<TypeBinding>,
) -> Arc<SymbolIndex> {
    Arc::new(SymbolIndex {
        entries: v1_rt::rc_map_insert(
            index.entries.clone(),
            v1_rt::concat(
                v1_rt::concat(module_path.clone(), ".".to_string()),
                binding.name.clone(),
            ),
            binding.resolved.clone(),
        ),
        global_bare: symbol_index_track_global_bare(
            index.global_bare.clone(),
            module_path.clone(),
            binding.clone(),
        ),
        services: index.services.clone(),
    })
}

pub fn symbol_index_insert_service(
    index: Arc<SymbolIndex>,
    name: String,
    module_path: String,
    item: Arc<Node>,
) -> Arc<SymbolIndex> {
    Arc::new(SymbolIndex {
        entries: index.entries.clone(),
        global_bare: index.global_bare.clone(),
        services: v1_rt::rc_map_insert(
            index.services.clone(),
            name.clone(),
            Arc::new(ServiceCensusEntry {
                module_path: module_path.clone(),
                item: item.clone(),
            }),
        ),
    })
}

pub fn empty_scope() -> Arc<Scope> {
    Arc::new(Scope {
        locals: v1_rt::rc_empty_map::<String, Arc<ScopeBinding>>(),
    })
}

pub fn scope_lookup(scope: Arc<Scope>, name: String) -> Option<Arc<ScopeBinding>> {
    v1_rt::map_get(&scope.locals.clone(), name.clone())
}

pub fn scope_push(scope: Arc<Scope>, binding: Arc<ScopeBinding>) -> Arc<Scope> {
    Arc::new(Scope {
        locals: v1_rt::rc_map_insert(scope.locals.clone(), binding.name.clone(), binding.clone()),
    })
}

pub fn ancestry_cache_sharing_dissolution_trigger() -> String {
    thread_local! {
        static CACHED: String = {
            "DISSOLVED (v1-run-stability-throughline, single-parent ancestry share): build_type_env / build_type_env_unresolved formerly recomputed ancestry_str_bindings as a fresh per-module map_merge(parent.interface.env.ancestry_str_bindings, parent.interface.env.str_bindings) in the single-import case — provably byte-identical to parent.interface.cache.str_bindings (interface.cache.str_bindings == map_merge(env.ancestry_str_bindings, env.str_bindings) by construction at both build sites, line 5973/6081), which is exactly ancestry_cache.str_bindings when count==1 (union of one parent cache is that cache, Rc-shared unchanged). So all three branches resolved to ancestry_cache.str_bindings and the conditional collapsed to `let ancestry_str_bindings = ancestry_cache.str_bindings` — one Rc-borrow, no fresh HAMT per module. Kills the O(M²) SipHash/map_merge churn on single-import chains and lets chains Rc-share the parent's already-retained str_bindings spine (§2 redundancy, §6 bare-minimum-cost; construction not validation — the recompute is now unwritable). Full SymbolIndex authority is the follow-on (type-env-single-authority-design.md).".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeEnvCache {
    pub deps_map: Arc<HashMap<String, Arc<Vec<String>>>>,
    pub str_bindings: Arc<HashMap<String, Arc<TypeBinding>>>,
    pub cycle_set_str: Arc<HashMap<String, bool>>,
    pub variant_locals: Arc<HashMap<String, Arc<TypeBinding>>>,
}

pub fn visible_bindings_invariant() -> String {
    thread_local! {
        static CACHED: String = {
            "B1 build_type_env populates ancestry_str_bindings for import chains; lookup_binding_by_name is fail-closed on miss (str_bindings then ancestry_str_bindings then intern+bindings only; 6de571f3 reverted in 80ab63a). flatten_visible_bindings and merge_envs are DELETED (2026-07-06 operator ruling: the parent-recurse counter was a parallel representation guarding a fallback; the fallback is now unwritable by construction — no whole-env flatten exists to call). Visibility reads are one-level: local str_bindings/ancestry_str_bindings, or a fold over parent.str_bindings only (see collect_unit_variant_phantom_matches). Dissolve-on: namespace-as-sole-authority ruling — env maps become a derived projection of namespace reachability.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn empty_type_env_cache() -> Arc<TypeEnvCache> {
    Arc::new(TypeEnvCache {
        deps_map: v1_rt::rc_empty_map::<String, Arc<Vec<String>>>(),
        str_bindings: v1_rt::rc_empty_map::<String, Arc<TypeBinding>>(),
        cycle_set_str: v1_rt::rc_empty_map::<String, bool>(),
        variant_locals: v1_rt::rc_empty_map::<String, Arc<TypeBinding>>(),
    })
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TypeEnvCacheMergeConflict {
    pub name: String,
    pub import_path: String,
    pub existing_site: String,
    pub incoming_site: String,
    pub span: Arc<SourceSpan>,
    pub same_tree: bool,
}

pub fn source_tree_partition_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Tree partition for the binding-fork ledger (lane ruling REVISED 2026-07-11: NOVELTY, not TREE, is the refusal axis). Tree does NOT decide refuse-vs-ledger anymore - it only LABELS the dissolution partition of an already-ledgered fork: same-tree (both decls under one tree, incl. dag-vs-dag across dirs) = a homonym/fork to consolidate within that tree; cross-tree (dag/ vs src/v2/) = the v1-seed-vs-v2 whole-file duplication the namespace census named; kernel-synthetic sites (<kernel:...>) are their own tree = the kernel-shadow class. ALL of these are LEDGERED (pre-existing forks already resolved on main by overlay-wins; refusing retroactively would regress working resolution), keeping the pre-guard import-order winner. Refusal is reserved for forks a PR NEWLY introduces (the novelty gate, a separate follow-up comparing this ledger to a drift-gated baseline). Partition on tree, never dir.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn source_tree_of(file: String) -> String {
    if v1_rt::contains(file.clone(), "<kernel:".to_string()) {
        "kernel".to_string()
    } else {
        if v1_rt::contains(file.clone(), "src/v2".to_string()) {
            "src/v2".to_string()
        } else {
            if v1_rt::contains(file.clone(), "src/v1".to_string()) {
                "src/v1".to_string()
            } else {
                if v1_rt::contains(file.clone(), "dag/".to_string()) {
                    "dag".to_string()
                } else {
                    "other".to_string()
                }
            }
        }
    }
}

pub fn conflict_is_cross_tree(c: Arc<TypeEnvCacheMergeConflict>) -> bool {
    (source_tree_of(c.existing_site.clone()) != source_tree_of(c.incoming_site.clone()))
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GuardedTypeEnvCacheMerge {
    pub cache: Arc<TypeEnvCache>,
    pub conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
}

pub fn guarded_cache_union_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Cross-parent cache union (S2a move 2 increment B; lane ruling REVISED 2026-07-11: NOVELTY, not TREE, is the refusal axis). A name reachable from two PEER imports with different bindings is a binding FORK. The union is order-INDEPENDENT by construction: keys present in both sides carrying the SAME binding (declaration-site span fast path, structural fallback) are SKIPPED (also the retained-bytes fix - the unguarded merge materialized a fresh O(|ancestry|) HAMT per module via rc_map_merge path-copy, the measured ~16.3MiB/module residency class); keys carrying DIFFERENT bindings are recorded as a typed, located conflict AND resolved by the pre-cut import-order overlay-wins winner (behavior-preserving: main already resolves these by overlay-wins, so refusing retroactively would regress working resolution, NOT close a fail-open). These conflicts are LEDGERED (out-of-band binding_forks channel, partitioned by tree for dissolution), never diagnostics; refusal is reserved for forks a PR NEWLY introduces (the separate novelty gate). Positional layers keep overlay-wins outside the guard: the kernel scope layer (merge_type_env_cache below) and locals-over-ancestry are lexical nearest-wins from the containment ruling, not peer-merge forks.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn binding_same_authority(a: Arc<TypeBinding>, b: Arc<TypeBinding>) -> bool {
    if (a.resolved.clone().span.clone() == b.resolved.clone().span.clone()) {
        true
    } else {
        (a.clone() == b.clone())
    }
}

pub fn union_base_choice_note() -> String {
    thread_local! {
        static CACHED: String = {
            "M1a base-choice (v1-run-stability-throughline §2.1, M0 receipt dup_factor=1.00): each union walks the SMALLER side's keys into the LARGER side's HAMT, so the result Rc-shares the large spine instead of freshly path-copying it key-by-key (the pre-cut accumulator was always the first parent - typically a small leaf - so every module materialized ~its whole closure's name surface fresh, across four maps). The winner is ORIENTATION-INDEPENDENT: each public union dispatches on count (O(1) on the persistent map) between an into-acc body (the pre-cut walk) and an into-overlay mirror whose conflict winner is identical (overlay-wins for str_bindings/deps_map/variant_locals, acc-wins for cycle_set_str); same-authority keys keep the retained side's copy - the span fast path declares the copies the same binding, and the whole-corpus fingerprint oracle is the drift check. Fold order stays import-order; conflict rows keep semantic roles (existing = accumulated side, incoming = overlay side) regardless of walk direction - the ledger's consumers are count/partition-grain, row order is not load-bearing.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn guarded_union_str_bindings_into_acc(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
    import_path: String,
    conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
) -> Arc<GuardedStrBindingsUnion> {
    Arc::new(v1_rt::map_keys(&overlay)).iter().cloned().fold(
        Arc::new(GuardedStrBindingsUnion {
            bindings: acc.clone(),
            conflicts: conflicts.clone(),
        }),
        |state: Arc<GuardedStrBindingsUnion>, name: String| match v1_rt::map_get(
            &overlay,
            name.clone(),
        ) {
            None => state.clone(),
            Some(incoming) => match v1_rt::map_get(&state.bindings.clone(), name.clone()) {
                None => Arc::new(GuardedStrBindingsUnion {
                    bindings: v1_rt::rc_map_insert(
                        state.bindings.clone(),
                        name.clone(),
                        incoming.clone(),
                    ),
                    conflicts: state.conflicts.clone(),
                }),
                Some(existing) => {
                    if binding_same_authority(existing.clone(), incoming.clone()) {
                        state.clone()
                    } else {
                        Arc::new(GuardedStrBindingsUnion {
                            bindings: v1_rt::rc_map_insert(
                                state.bindings.clone(),
                                name.clone(),
                                incoming.clone(),
                            ),
                            conflicts: v1_rt::concat(
                                state.conflicts.clone(),
                                Rc::new(vec![Arc::new(TypeEnvCacheMergeConflict {
                                    name: name.clone(),
                                    import_path: import_path.clone(),
                                    existing_site: existing
                                        .resolved
                                        .clone()
                                        .span
                                        .clone()
                                        .file
                                        .clone(),
                                    incoming_site: incoming
                                        .resolved
                                        .clone()
                                        .span
                                        .clone()
                                        .file
                                        .clone(),
                                    span: incoming.resolved.clone().span.clone(),
                                    same_tree: (source_tree_of(
                                        existing.resolved.clone().span.clone().file.clone(),
                                    ) == source_tree_of(
                                        incoming.resolved.clone().span.clone().file.clone(),
                                    )),
                                })]),
                            ),
                        })
                    }
                }
            },
        },
    )
}

pub fn guarded_union_str_bindings_into_overlay(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
    import_path: String,
    conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
) -> Arc<GuardedStrBindingsUnion> {
    Arc::new(v1_rt::map_keys(&acc)).iter().cloned().fold(
        Arc::new(GuardedStrBindingsUnion {
            bindings: overlay.clone(),
            conflicts: conflicts.clone(),
        }),
        |state: Arc<GuardedStrBindingsUnion>, name: String| match v1_rt::map_get(&acc, name.clone())
        {
            None => state.clone(),
            Some(accumulated) => match v1_rt::map_get(&state.bindings.clone(), name.clone()) {
                None => Arc::new(GuardedStrBindingsUnion {
                    bindings: v1_rt::rc_map_insert(
                        state.bindings.clone(),
                        name.clone(),
                        accumulated.clone(),
                    ),
                    conflicts: state.conflicts.clone(),
                }),
                Some(incoming) => {
                    if binding_same_authority(accumulated.clone(), incoming.clone()) {
                        state.clone()
                    } else {
                        Arc::new(GuardedStrBindingsUnion {
                            bindings: state.bindings.clone(),
                            conflicts: v1_rt::concat(
                                state.conflicts.clone(),
                                Rc::new(vec![Arc::new(TypeEnvCacheMergeConflict {
                                    name: name.clone(),
                                    import_path: import_path.clone(),
                                    existing_site: accumulated
                                        .resolved
                                        .clone()
                                        .span
                                        .clone()
                                        .file
                                        .clone(),
                                    incoming_site: incoming
                                        .resolved
                                        .clone()
                                        .span
                                        .clone()
                                        .file
                                        .clone(),
                                    span: incoming.resolved.clone().span.clone(),
                                    same_tree: (source_tree_of(
                                        accumulated.resolved.clone().span.clone().file.clone(),
                                    ) == source_tree_of(
                                        incoming.resolved.clone().span.clone().file.clone(),
                                    )),
                                })]),
                            ),
                        })
                    }
                }
            },
        },
    )
}

pub fn guarded_union_str_bindings(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
    import_path: String,
    conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
) -> Arc<GuardedStrBindingsUnion> {
    if ((acc.clone().len() as i64) >= (overlay.clone().len() as i64)) {
        guarded_union_str_bindings_into_acc(
            acc.clone(),
            overlay.clone(),
            import_path.clone(),
            conflicts.clone(),
        )
    } else {
        guarded_union_str_bindings_into_overlay(
            acc.clone(),
            overlay.clone(),
            import_path.clone(),
            conflicts.clone(),
        )
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GuardedStrBindingsUnion {
    pub bindings: Arc<HashMap<String, Arc<TypeBinding>>>,
    pub conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
}

pub fn union_deps_map_into_acc(
    acc: Arc<HashMap<String, Arc<Vec<String>>>>,
    overlay: Arc<HashMap<String, Arc<Vec<String>>>>,
) -> Arc<HashMap<String, Arc<Vec<String>>>> {
    Arc::new(v1_rt::map_keys(&overlay)).iter().cloned().fold(
        acc.clone(),
        |m: Arc<HashMap<String, Arc<Vec<String>>>>, name: String| match v1_rt::map_get(
            &overlay,
            name.clone(),
        ) {
            None => m.clone(),
            Some(incoming) => match v1_rt::map_get(&m, name.clone()) {
                None => v1_rt::rc_map_insert(m.clone(), name.clone(), incoming.clone()),
                Some(existing) => {
                    if (existing.clone() == incoming.clone()) {
                        m.clone()
                    } else {
                        v1_rt::rc_map_insert(m.clone(), name.clone(), incoming.clone())
                    }
                }
            },
        },
    )
}

pub fn union_deps_map_into_overlay(
    acc: Arc<HashMap<String, Arc<Vec<String>>>>,
    overlay: Arc<HashMap<String, Arc<Vec<String>>>>,
) -> Arc<HashMap<String, Arc<Vec<String>>>> {
    Arc::new(v1_rt::map_keys(&acc)).iter().cloned().fold(
        overlay.clone(),
        |m: Arc<HashMap<String, Arc<Vec<String>>>>, name: String| match v1_rt::map_get(
            &acc,
            name.clone(),
        ) {
            None => m.clone(),
            Some(accumulated) => match v1_rt::map_get(&m, name.clone()) {
                None => v1_rt::rc_map_insert(m.clone(), name.clone(), accumulated.clone()),
                Some(_incoming) => m.clone(),
            },
        },
    )
}

pub fn union_deps_map_skip_equal(
    acc: Arc<HashMap<String, Arc<Vec<String>>>>,
    overlay: Arc<HashMap<String, Arc<Vec<String>>>>,
) -> Arc<HashMap<String, Arc<Vec<String>>>> {
    if ((acc.clone().len() as i64) >= (overlay.clone().len() as i64)) {
        union_deps_map_into_acc(acc.clone(), overlay.clone())
    } else {
        union_deps_map_into_overlay(acc.clone(), overlay.clone())
    }
}

pub fn union_bool_set_into_acc(
    acc: Arc<HashMap<String, bool>>,
    overlay: Arc<HashMap<String, bool>>,
) -> Arc<HashMap<String, bool>> {
    Arc::new(v1_rt::map_keys(&overlay)).iter().cloned().fold(
        acc.clone(),
        |m: Arc<HashMap<String, bool>>, name: String| match v1_rt::map_get(&m, name.clone()) {
            Some(_) => m.clone(),
            None => match v1_rt::map_get(&overlay, name.clone()) {
                Some(v) => v1_rt::rc_map_insert(m.clone(), name.clone(), v.clone()),
                None => m.clone(),
            },
        },
    )
}

pub fn union_bool_set_into_overlay(
    acc: Arc<HashMap<String, bool>>,
    overlay: Arc<HashMap<String, bool>>,
) -> Arc<HashMap<String, bool>> {
    Arc::new(v1_rt::map_keys(&acc)).iter().cloned().fold(
        overlay.clone(),
        |m: Arc<HashMap<String, bool>>, name: String| match v1_rt::map_get(&acc, name.clone()) {
            None => m.clone(),
            Some(accumulated) => match v1_rt::map_get(&m, name.clone()) {
                None => v1_rt::rc_map_insert(m.clone(), name.clone(), accumulated.clone()),
                Some(incoming) => {
                    if (incoming.clone() == accumulated.clone()) {
                        m.clone()
                    } else {
                        v1_rt::rc_map_insert(m.clone(), name.clone(), accumulated.clone())
                    }
                }
            },
        },
    )
}

pub fn union_bool_set_skip_equal(
    acc: Arc<HashMap<String, bool>>,
    overlay: Arc<HashMap<String, bool>>,
) -> Arc<HashMap<String, bool>> {
    if ((acc.clone().len() as i64) >= (overlay.clone().len() as i64)) {
        union_bool_set_into_acc(acc.clone(), overlay.clone())
    } else {
        union_bool_set_into_overlay(acc.clone(), overlay.clone())
    }
}

pub fn union_variant_locals_into_acc(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
) -> Arc<HashMap<String, Arc<TypeBinding>>> {
    Arc::new(v1_rt::map_keys(&overlay)).iter().cloned().fold(
        acc.clone(),
        |m: Arc<HashMap<String, Arc<TypeBinding>>>, name: String| match v1_rt::map_get(
            &overlay,
            name.clone(),
        ) {
            None => m.clone(),
            Some(incoming) => match v1_rt::map_get(&m, name.clone()) {
                None => v1_rt::rc_map_insert(m.clone(), name.clone(), incoming.clone()),
                Some(_existing) => v1_rt::rc_map_insert(m.clone(), name.clone(), incoming.clone()),
            },
        },
    )
}

pub fn union_variant_locals_into_overlay(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
) -> Arc<HashMap<String, Arc<TypeBinding>>> {
    Arc::new(v1_rt::map_keys(&acc)).iter().cloned().fold(
        overlay.clone(),
        |m: Arc<HashMap<String, Arc<TypeBinding>>>, name: String| match v1_rt::map_get(
            &acc,
            name.clone(),
        ) {
            None => m.clone(),
            Some(accumulated) => match v1_rt::map_get(&m, name.clone()) {
                None => v1_rt::rc_map_insert(m.clone(), name.clone(), accumulated.clone()),
                Some(_incoming) => m.clone(),
            },
        },
    )
}

pub fn union_variant_locals_skip_equal(
    acc: Arc<HashMap<String, Arc<TypeBinding>>>,
    overlay: Arc<HashMap<String, Arc<TypeBinding>>>,
) -> Arc<HashMap<String, Arc<TypeBinding>>> {
    if ((acc.clone().len() as i64) >= (overlay.clone().len() as i64)) {
        union_variant_locals_into_acc(acc.clone(), overlay.clone())
    } else {
        union_variant_locals_into_overlay(acc.clone(), overlay.clone())
    }
}

pub fn merge_type_env_cache_guarded(
    base: Arc<TypeEnvCache>,
    overlay: Arc<TypeEnvCache>,
    import_path: String,
    conflicts: Arc<Vec<Arc<TypeEnvCacheMergeConflict>>>,
) -> Arc<GuardedTypeEnvCacheMerge> {
    {
        let str_union = guarded_union_str_bindings(
            base.str_bindings.clone(),
            overlay.str_bindings.clone(),
            import_path.clone(),
            conflicts.clone(),
        );
        Arc::new(GuardedTypeEnvCacheMerge {
            cache: Arc::new(TypeEnvCache {
                deps_map: union_deps_map_skip_equal(
                    base.deps_map.clone(),
                    overlay.deps_map.clone(),
                ),
                str_bindings: str_union.bindings.clone(),
                cycle_set_str: union_bool_set_skip_equal(
                    base.cycle_set_str.clone(),
                    overlay.cycle_set_str.clone(),
                ),
                variant_locals: union_variant_locals_skip_equal(
                    base.variant_locals.clone(),
                    overlay.variant_locals.clone(),
                ),
            }),
            conflicts: str_union.conflicts.clone(),
        })
    }
}

pub fn merge_type_env_cache(
    base: Arc<TypeEnvCache>,
    overlay: Arc<TypeEnvCache>,
) -> Arc<TypeEnvCache> {
    Arc::new(TypeEnvCache {
        deps_map: v1_rt::rc_map_merge(base.deps_map.clone(), overlay.deps_map.clone()),
        str_bindings: v1_rt::rc_map_merge(base.str_bindings.clone(), overlay.str_bindings.clone()),
        cycle_set_str: v1_rt::rc_map_merge(
            base.cycle_set_str.clone(),
            overlay.cycle_set_str.clone(),
        ),
        variant_locals: v1_rt::rc_map_merge(
            base.variant_locals.clone(),
            overlay.variant_locals.clone(),
        ),
    })
}

pub fn lookup_binding_local_by_name(env: Arc<TypeEnv>, name: String) -> Option<Arc<TypeBinding>> {
    match intern_find(env.intern_table.clone(), name.clone()) {
        Some(ident) => match v1_rt::map_get(&env.bindings.clone(), ident.clone()) {
            Some(binding) => Some(binding.clone()),
            None => None,
        },
        None => None,
    }
}

pub fn str_bindings_from_bindings(
    bindings: Arc<HashMap<i64, Arc<TypeBinding>>>,
) -> Arc<HashMap<String, Arc<TypeBinding>>> {
    Arc::new(v1_rt::map_values(&bindings)).iter().cloned().fold(
        v1_rt::rc_empty_map::<String, Arc<TypeBinding>>(),
        |acc: Arc<HashMap<String, Arc<TypeBinding>>>, binding: Arc<TypeBinding>| {
            v1_rt::rc_map_insert(acc, binding.name.clone(), binding.clone())
        },
    )
}

pub fn lookup_binding_by_name_local(env: Arc<TypeEnv>, name: String) -> Option<Arc<TypeBinding>> {
    match v1_rt::map_get(&env.str_bindings.clone(), name.clone()) {
        Some(binding) => Some(binding.clone()),
        None => match v1_rt::map_get(&env.ancestry_str_bindings.clone(), name.clone()) {
            Some(binding) => Some(binding.clone()),
            None => match intern_find(env.intern_table.clone(), name.clone()) {
                Some(id) => v1_rt::map_get(&env.bindings.clone(), id.clone()),
                None => None,
            },
        },
    }
}

pub fn lookup_binding_by_name(env: Arc<TypeEnv>, name: String) -> Option<Arc<TypeBinding>> {
    match lookup_binding_by_name_local(env.clone(), name.clone()) {
        Some(binding) => Some(binding.clone()),
        None => lookup_binding_after_global_bare(env.clone(), name.clone()),
    }
}

pub fn lookup_binding_after_global_bare(
    env: Arc<TypeEnv>,
    name: String,
) -> Option<Arc<TypeBinding>> {
    match global_bare_lookup(env.clone(), name.clone()) {
        Some(binding) => Some(binding.clone()),
        None => lookup_qualified_module_projection(env.clone(), name.clone()),
    }
}

pub fn lookup_qualified_module_projection(
    env: Arc<TypeEnv>,
    name: String,
) -> Option<Arc<TypeBinding>> {
    match v1_rt::contains(name.clone(), ".".to_string()) {
        false => None,
        true => match symbol_index_lookup(env.symbol_index.clone(), name.clone()) {
            Some(resolved) => Some(Arc::new(TypeBinding {
                name: name.clone(),
                resolved: resolved.clone(),
                provenance: Arc::new(SubValueRelation::SubValueUnknown),
            })),
            None => None,
        },
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SegmentLcpScan {
    pub remaining: Arc<Vec<String>>,
    pub matched: i64,
    pub live: bool,
}

pub fn segment_lcp_len(a: Arc<Vec<String>>, b: Arc<Vec<String>>) -> i64 {
    {
        let scan = a.clone().iter().cloned().fold(
            Arc::new(SegmentLcpScan {
                remaining: b.clone(),
                matched: 0,
                live: true,
            }),
            |acc: Arc<SegmentLcpScan>, seg: String| {
                if acc.live.clone() {
                    match acc.remaining.clone().first().cloned() {
                        Some(bh) => {
                            if (bh.clone() == seg.clone()) {
                                Arc::new(SegmentLcpScan {
                                    remaining: Arc::new(
                                        acc.remaining
                                            .clone()
                                            .iter()
                                            .cloned()
                                            .skip(1 as usize)
                                            .collect::<Vec<_>>(),
                                    ),
                                    matched: (acc.matched.clone() + 1),
                                    live: true,
                                })
                            } else {
                                Arc::new(SegmentLcpScan {
                                    remaining: acc.remaining.clone(),
                                    matched: acc.matched.clone(),
                                    live: false,
                                })
                            }
                        }
                        None => Arc::new(SegmentLcpScan {
                            remaining: acc.remaining.clone(),
                            matched: acc.matched.clone(),
                            live: false,
                        }),
                    }
                } else {
                    acc.clone()
                }
            },
        );
        scan.matched.clone()
    }
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GlobalBareNearestScan {
    pub best_lcp: i64,
    pub best: Option<Arc<TypeBinding>>,
    pub tie: bool,
}

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct GlobalBareNearestCandidateScan {
    pub best_lcp: i64,
    pub best: Option<Arc<GlobalBareCandidate>>,
    pub tie: bool,
}

pub fn global_bare_nearest_ancestor_candidate(
    env_module_path: String,
    candidates: Arc<Vec<Arc<GlobalBareCandidate>>>,
) -> Option<Arc<GlobalBareCandidate>> {
    {
        let env_segs = module_path_segments(env_module_path.clone());
        let scan = candidates.clone().iter().cloned().fold(
            Arc::new(GlobalBareNearestCandidateScan {
                best_lcp: (0 - 1),
                best: None,
                tie: false,
            }),
            |acc: Arc<GlobalBareNearestCandidateScan>, cand: Arc<GlobalBareCandidate>| {
                let l = segment_lcp_len(
                    env_segs.clone(),
                    module_path_segments(cand.module_path.clone()),
                );
                if (l.clone() > acc.best_lcp.clone()) {
                    Arc::new(GlobalBareNearestCandidateScan {
                        best_lcp: l.clone(),
                        best: Some(cand.clone()),
                        tie: false,
                    })
                } else {
                    if (l.clone() == acc.best_lcp.clone()) {
                        Arc::new(GlobalBareNearestCandidateScan {
                            best_lcp: acc.best_lcp.clone(),
                            best: acc.best.clone(),
                            tie: true,
                        })
                    } else {
                        acc.clone()
                    }
                }
            },
        );
        match scan.tie.clone() {
            true => None,
            false => scan.best.clone(),
        }
    }
}

pub fn global_bare_nearest_ancestor(
    env_module_path: String,
    candidates: Arc<Vec<Arc<GlobalBareCandidate>>>,
) -> Option<Arc<TypeBinding>> {
    match global_bare_nearest_ancestor_candidate(env_module_path.clone(), candidates.clone()) {
        Some(cand) => Some(cand.binding.clone()),
        None => None,
    }
}

pub fn global_bare_owner_module(
    env: Arc<TypeEnv>,
    owner_module_path: String,
    name: String,
) -> Option<String> {
    match v1_rt::map_get(&env.symbol_index.clone().global_bare.clone(), name.clone())
        .as_deref()
        .cloned()
    {
        Some(GlobalBareLookupState::GlobalBareUniqueBinding {
            module_path: mp,
            binding: b,
            ..
        }) => match binding_declares_name(b.clone(), name.clone(), env.source_indices.clone()) {
            true => Some(mp.clone()),
            false => None,
        },
        Some(GlobalBareLookupState::GlobalBareAmbiguousBinding {
            candidates: cands, ..
        }) => {
            match global_bare_nearest_ancestor_candidate(owner_module_path.clone(), cands.clone()) {
                Some(cand) => match binding_declares_name(
                    cand.binding.clone(),
                    name.clone(),
                    env.source_indices.clone(),
                ) {
                    true => Some(cand.module_path.clone()),
                    false => None,
                },
                None => None,
            }
        }
        None => None,
    }
}

pub fn binding_declares_name(
    binding: Arc<TypeBinding>,
    name: String,
    source_indices: Arc<HashMap<String, Arc<NewlineIndex>>>,
) -> bool {
    (authored_name_at(source_indices.clone(), binding.resolved.clone()) == name.clone())
}

pub fn borrowed_generic_param_names(
    params: Arc<Vec<Arc<Node>>>,
    source_indices: Arc<HashMap<String, Arc<NewlineIndex>>>,
) -> Arc<HashMap<String, bool>> {
    params.clone().iter().cloned().fold(
        v1_rt::rc_empty_map::<String, bool>(),
        |acc: Arc<HashMap<String, bool>>, p: Arc<Node>| {
            let pt = param_node_type_expr(p.clone());
            let pname = param_node_name_at(p.clone(), source_indices.clone());
            let tname = authored_name_at(source_indices.clone(), pt.clone());
            if (((((pt.children.clone().len() as i64) == 0)
                && (pt.connective.clone() == Connective::NoConnective))
                && (pname.clone() != "".to_string()))
                && ((pname.clone() == tname.clone()) || (pname.clone() == pt.name.clone())))
            {
                v1_rt::rc_map_insert(acc.clone(), pname.clone(), true)
            } else {
                acc.clone()
            }
        },
    )
}

pub fn qualify_declaration_position_invariant() -> String {
    thread_local! {
        static CACHED: String = {
            "qualify_borrowed_type_names rewrites REFERENCE positions only. Within a type expression, a NoConnective node's children are generic ARGS (references - rename-eligible); a STRUCTURED node's (Disj/Conj) children are DECLARATIONS (variant names, field names) whose names must never be rewritten - renaming them detaches bare patterns/projections from the decl (a projected coproduct's Independent became std.realization.Independent and every bare match arm missed). Declaration subtrees are walked by qualify_decl_reference_positions: keep the declared name, qualify the inferred payload (a type expr - reference position), recurse into child declarations. The census type-decl pass (census_upgrade_type_decl_binding) uses the SAME walk - one authority for the declaration/reference distinction.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn qualify_decl_reference_positions(
    n: Arc<Node>,
    owner_module_path: String,
    env: Arc<TypeEnv>,
    excluded: Arc<HashMap<String, bool>>,
) -> Arc<Node> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        let inf2 = qualify_borrowed_inferred(
            n.inferred.clone(),
            owner_module_path.clone(),
            env.clone(),
            excluded.clone(),
        );
        let ch2 = Arc::new({
            let mut __result = Vec::new();
            for c in n.children.clone().iter().cloned() {
                __result.push(qualify_decl_reference_positions(
                    c.clone(),
                    owner_module_path.clone(),
                    env.clone(),
                    excluded.clone(),
                ));
            }
            __result
        });
        node_with_children(node_with_inferred(n.clone(), inf2.clone()), ch2.clone())
    })
}

pub fn qualify_borrowed_type_names(
    n: Arc<Node>,
    owner_module_path: String,
    env: Arc<TypeEnv>,
    excluded: Arc<HashMap<String, bool>>,
) -> Arc<Node> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        let qualified_children = if (n.connective.clone() == Connective::NoConnective) {
            Arc::new({
                let mut __result = Vec::new();
                for c in n.children.clone().iter().cloned() {
                    __result.push(qualify_borrowed_type_names(
                        c.clone(),
                        owner_module_path.clone(),
                        env.clone(),
                        excluded.clone(),
                    ));
                }
                __result
            })
        } else {
            Arc::new({
                let mut __result = Vec::new();
                for c in n.children.clone().iter().cloned() {
                    __result.push(qualify_decl_reference_positions(
                        c.clone(),
                        owner_module_path.clone(),
                        env.clone(),
                        excluded.clone(),
                    ));
                }
                __result
            })
        };
        let name = authored_name_at(env.source_indices.clone(), n.clone());
        let is_type_var = match n.inferred.clone().as_deref().cloned() {
            Some(InferredNode::TypeVariable { id: _, .. }) => true,
            _ => false,
        };
        let rewrite = ((((((n.connective.clone() == Connective::NoConnective)
            && (name.clone() != "".to_string()))
            && !v1_rt::contains(name.clone(), ".".to_string()))
            && !is_kernel_type(name.clone()))
            && !v1_rt::map_has(&excluded, name.clone()))
            && !is_type_var.clone());
        let owner_hit = if rewrite.clone() {
            global_bare_owner_module(env.clone(), owner_module_path.clone(), name.clone())
        } else {
            None
        };
        match owner_hit.clone() {
            Some(mp) => {
                if (mp.clone() == "".to_string()) {
                    node_with_children(n.clone(), qualified_children.clone())
                } else {
                    {
                        let qname =
                            v1_rt::concat(v1_rt::concat(mp.clone(), ".".to_string()), name.clone());
                        Arc::new(Node {
                            name: qname.clone(),
                            span: n.span.clone(),
                            ident_span: Some(kernel_span(qname.clone())),
                            children: qualified_children.clone(),
                            connective: n.connective.clone(),
                            params: n.params.clone(),
                            inferred: n.inferred.clone(),
                            return_cardinality: n.return_cardinality.clone(),
                            uses: n.uses.clone(),
                            body: n.body.clone(),
                            transport: n.transport.clone(),
                            properties: n.properties.clone(),
                            type_annotation: n.type_annotation.clone(),
                            is_self_recursive: n.is_self_recursive.clone(),
                            has_non_tail_self_call: n.has_non_tail_self_call.clone(),
                            match_pattern: n.match_pattern.clone(),
                            expr_data: n.expr_data.clone(),
                            ident: None,
                        })
                    }
                }
            }
            None => node_with_children(n.clone(), qualified_children.clone()),
        }
    })
}

pub fn node_with_children(n: Arc<Node>, children: Arc<Vec<Arc<Node>>>) -> Arc<Node> {
    Arc::new(Node {
        name: n.name.clone(),
        ident: n.ident.clone(),
        span: n.span.clone(),
        ident_span: n.ident_span.clone(),
        children: children.clone(),
        connective: n.connective.clone(),
        params: n.params.clone(),
        inferred: n.inferred.clone(),
        return_cardinality: n.return_cardinality.clone(),
        uses: n.uses.clone(),
        body: n.body.clone(),
        transport: n.transport.clone(),
        properties: n.properties.clone(),
        type_annotation: n.type_annotation.clone(),
        is_self_recursive: n.is_self_recursive.clone(),
        has_non_tail_self_call: n.has_non_tail_self_call.clone(),
        match_pattern: n.match_pattern.clone(),
        expr_data: n.expr_data.clone(),
    })
}

pub fn node_with_inferred(n: Arc<Node>, inferred: Option<Arc<InferredNode>>) -> Arc<Node> {
    Arc::new(Node {
        name: n.name.clone(),
        ident: n.ident.clone(),
        span: n.span.clone(),
        ident_span: n.ident_span.clone(),
        children: n.children.clone(),
        connective: n.connective.clone(),
        params: n.params.clone(),
        inferred: inferred.clone(),
        return_cardinality: n.return_cardinality.clone(),
        uses: n.uses.clone(),
        body: n.body.clone(),
        transport: n.transport.clone(),
        properties: n.properties.clone(),
        type_annotation: n.type_annotation.clone(),
        is_self_recursive: n.is_self_recursive.clone(),
        has_non_tail_self_call: n.has_non_tail_self_call.clone(),
        match_pattern: n.match_pattern.clone(),
        expr_data: n.expr_data.clone(),
    })
}

pub fn qualify_borrowed_inferred(
    inferred: Option<Arc<InferredNode>>,
    owner_module_path: String,
    env: Arc<TypeEnv>,
    excluded: Arc<HashMap<String, bool>>,
) -> Option<Arc<InferredNode>> {
    match inferred.clone().as_deref().cloned() {
        Some(InferredNode::Resolved { node: t, .. }) => Some(Arc::new(InferredNode::Resolved {
            node: qualify_borrowed_type_names(
                t.clone(),
                owner_module_path.clone(),
                env.clone(),
                excluded.clone(),
            ),
        })),
        _ => inferred.clone(),
    }
}

pub fn qualified_all_but_last(name: String) -> String {
    {
        let segs = module_path_segments(name.clone());
        let seg_count = (segs.clone().len() as i64);
        if (seg_count.clone() <= 1) {
            "".to_string()
        } else {
            {
                let scan = Arc::new(
                    segs.clone()
                        .iter()
                        .cloned()
                        .take((seg_count.clone() - 1) as usize)
                        .collect::<Vec<_>>(),
                )
                .iter()
                .cloned()
                .fold("".to_string(), |acc: String, seg: String| {
                    if (acc.clone() == "".to_string()) {
                        seg.clone()
                    } else {
                        v1_rt::concat(v1_rt::concat(acc.clone(), ".".to_string()), seg.clone())
                    }
                });
                scan
            }
        }
    }
}

pub fn record_global_bare_ambiguous_silent_pick(
    env_module_path: String,
    name: String,
    cands: Arc<Vec<Arc<GlobalBareCandidate>>>,
) -> () {
    {
        let cand_count = (cands.clone().len() as i64);
        match global_bare_nearest_ancestor_candidate(env_module_path.clone(), cands.clone()) {
            Some(cand) => v1_rt::resolution_silent_pick_record_global_bare_lcp_pick(
                env_module_path.clone(),
                name.clone(),
                cand_count.clone(),
                cand.module_path.clone(),
            ),
            None => v1_rt::resolution_silent_pick_record_global_bare_lcp_tie(
                env_module_path.clone(),
                name.clone(),
                cand_count.clone(),
            ),
        }
    }
}

pub fn global_bare_lookup(env: Arc<TypeEnv>, name: String) -> Option<Arc<TypeBinding>> {
    match v1_rt::map_get(&env.symbol_index.clone().global_bare.clone(), name.clone())
        .as_deref()
        .cloned()
    {
        Some(GlobalBareLookupState::GlobalBareUniqueBinding { binding, .. }) => {
            Some(binding.clone())
        }
        Some(GlobalBareLookupState::GlobalBareAmbiguousBinding {
            candidates: cands, ..
        }) => {
            if (v1_rt::resolution_silent_pick_is_enabled() && ((cands.clone().len() as i64) >= 2)) {
                record_global_bare_ambiguous_silent_pick(
                    env.module_path.clone(),
                    name.clone(),
                    cands.clone(),
                )
            }
            global_bare_nearest_ancestor(env.module_path.clone(), cands.clone())
        }
        None => None,
    }
}

pub fn global_bare_is_ambiguous(env: Arc<TypeEnv>, name: String) -> bool {
    match v1_rt::map_get(&env.symbol_index.clone().global_bare.clone(), name.clone())
        .as_deref()
        .cloned()
    {
        Some(GlobalBareLookupState::GlobalBareAmbiguousBinding {
            candidates: cands, ..
        }) => (global_bare_nearest_ancestor(env.module_path.clone(), cands.clone()) == None),
        Some(GlobalBareLookupState::GlobalBareUniqueBinding { .. }) => false,
        None => false,
    }
}

pub fn lookup_binding(env: Arc<TypeEnv>, ident: i64) -> Option<Arc<TypeBinding>> {
    match v1_rt::map_get(&env.bindings.clone(), ident.clone()) {
        Some(binding) => Some(binding.clone()),
        None => {
            let name = intern_str(env.intern_table.clone(), ident.clone());
            lookup_binding_by_name(env.clone(), name.clone())
        }
    }
}

pub fn is_recursive_type(env: Arc<TypeEnv>, ident: i64) -> bool {
    match v1_rt::map_get(&env.recursive_type_set.clone(), ident.clone()) {
        Some(_) => true,
        None => false,
    }
}

pub fn is_recursive_type_by_name(env: Arc<TypeEnv>, name: String) -> bool {
    match intern_find(env.intern_table.clone(), name.clone()) {
        Some(id) => is_recursive_type(env.clone(), id.clone()),
        None => false,
    }
}

pub fn lookup_type(env: Arc<TypeEnv>, ident: i64) -> Option<Arc<Node>> {
    match lookup_binding(env.clone(), ident.clone()) {
        Some(binding) => Some(binding.resolved.clone()),
        None => None,
    }
}

pub fn lookup_type_by_name(env: Arc<TypeEnv>, name: String) -> Option<Arc<Node>> {
    match lookup_binding_by_name(env.clone(), name.clone()) {
        Some(binding) => match (v1_rt::contains(name.clone(), ".".to_string())
            || binding_declares_name(binding.clone(), name.clone(), env.source_indices.clone()))
        {
            true => Some(binding.resolved.clone()),
            false => match variant_arm_type_projection(
                env.clone(),
                binding.resolved.clone(),
                name.clone(),
            ) {
                Some(arm) => Some(arm.clone()),
                None => Some(binding.resolved.clone()),
            },
        },
        None => None,
    }
}

pub fn variant_arm_type_projection(
    env: Arc<TypeEnv>,
    owner: Arc<Node>,
    name: String,
) -> Option<Arc<Node>> {
    match owner.connective.clone() {
        Connective::Disj => {
            match find_child_named(owner.clone(), name.clone(), env.source_indices.clone()) {
                Some(arm) => match ((arm.children.clone().len() as i64) == 0) {
                    true => Some(arm.clone()),
                    false => None,
                },
                None => None,
            }
        }
        _ => None,
    }
}

pub fn authored_name(env: Arc<TypeEnv>, node: Arc<Node>) -> String {
    authored_name_at(env.source_indices.clone(), node.clone())
}

pub fn lookup_type_for(env: Arc<TypeEnv>, node: Arc<Node>) -> Option<Arc<Node>> {
    match node.ident.clone() {
        Some(id) => match lookup_type(env.clone(), id.clone()) {
            Some(resolved) => {
                let name = authored_name(env.clone(), node.clone());
                match ((resolved.connective.clone() == Connective::Disj)
                    && (authored_name_at(env.source_indices.clone(), resolved.clone())
                        != name.clone()))
                {
                    true => match variant_arm_type_projection(
                        env.clone(),
                        resolved.clone(),
                        name.clone(),
                    ) {
                        Some(arm) => Some(arm.clone()),
                        None => Some(resolved.clone()),
                    },
                    false => Some(resolved.clone()),
                }
            }
            None => None,
        },
        None => lookup_type_by_name(env.clone(), authored_name(env.clone(), node.clone())),
    }
}

pub fn is_recursive_type_for(env: Arc<TypeEnv>, node: Arc<Node>) -> bool {
    match node.ident.clone() {
        Some(id) => is_recursive_type(env.clone(), id.clone()),
        None => is_recursive_type_by_name(env.clone(), authored_name(env.clone(), node.clone())),
    }
}

pub fn inductive_fields_for(env: Arc<TypeEnv>, type_name: String) -> Arc<Vec<Arc<InductiveField>>> {
    stacker::maybe_grow(512 * 1024, 2 * 1024 * 1024, || {
        let local = match v1_rt::map_get(&env.inductive_fields.clone(), type_name.clone()) {
            Some(fields) => fields.clone(),
            None => Rc::new(vec![]),
        };
        if ((local.clone().len() as i64) > 0) {
            local.clone()
        } else {
            env.parents.clone().iter().cloned().fold(
                Rc::new(vec![]),
                |acc: Arc<Vec<Arc<InductiveField>>>, parent: Arc<TypeEnv>| {
                    if ((acc.clone().len() as i64) > 0) {
                        acc.clone()
                    } else {
                        inductive_fields_for(parent.clone(), type_name.clone())
                    }
                },
            )
        }
    })
}

pub fn is_inductive_field(
    env: Arc<TypeEnv>,
    type_name: String,
    variant_name: String,
    field_name: String,
) -> bool {
    {
        let mut __found = false;
        for f in inductive_fields_for(env.clone(), type_name.clone())
            .iter()
            .cloned()
        {
            if ((f.variant_name.clone() == variant_name.clone())
                && (f.field_name.clone() == field_name.clone()))
            {
                __found = true;
                break;
            }
        }
        __found
    }
}

pub fn put_inductive_field_guard_note() -> String {
    thread_local! {
        static CACHED: String = {
            "M1a base-case guard (PR #6528 review commitment): the merge layer (merge_inductive_fields below) keeps lists duplicate-free by induction, but its base case - the local collection through these two put functions - was OBSERVED duplicate-free, not constructed (a source declaring the same (variant, field, shape, element) fact twice within one module would seed a duplicate the merge then faithfully preserves). The guarded append closes the last hand-concat site of the class: a fact already present by identity is a skip, so duplicate-freedom holds by construction at every layer (DESIGN §5 - construction, not validation). Local lists are per-(module, type) and small, so the linear any-scan per insert is bounded by per-type field count.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn append_inductive_field_absent(
    existing: Arc<Vec<Arc<InductiveField>>>,
    incoming: Arc<InductiveField>,
) -> Arc<Vec<Arc<InductiveField>>> {
    if {
        let mut __found = false;
        for f in existing.clone().iter().cloned() {
            if (inductive_field_key(f.clone()) == inductive_field_key(incoming.clone())) {
                __found = true;
                break;
            }
        }
        __found
    } {
        existing.clone()
    } else {
        v1_rt::concat(existing.clone(), Rc::new(vec![incoming.clone()]))
    }
}

pub fn put_inductive_field(
    fields: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>,
    type_name: String,
    variant_name: String,
    field_name: String,
    shape: RecursionShape,
) -> Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>> {
    {
        let existing = match v1_rt::map_get(&fields, type_name.clone()) {
            Some(fs) => fs.clone(),
            None => Rc::new(vec![]),
        };
        v1_rt::rc_map_insert(
            fields.clone(),
            type_name.clone(),
            append_inductive_field_absent(
                existing.clone(),
                Arc::new(InductiveField {
                    type_name: type_name.clone(),
                    variant_name: variant_name.clone(),
                    field_name: field_name.clone(),
                    shape: shape.clone(),
                    element_type: type_name.clone(),
                }),
            ),
        )
    }
}

pub fn put_inductive_field_cross(
    fields: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>,
    type_name: String,
    variant_name: String,
    field_name: String,
    shape: RecursionShape,
    element_type: String,
) -> Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>> {
    {
        let existing = match v1_rt::map_get(&fields, type_name.clone()) {
            Some(fs) => fs.clone(),
            None => Rc::new(vec![]),
        };
        v1_rt::rc_map_insert(
            fields.clone(),
            type_name.clone(),
            append_inductive_field_absent(
                existing.clone(),
                Arc::new(InductiveField {
                    type_name: type_name.clone(),
                    variant_name: variant_name.clone(),
                    field_name: field_name.clone(),
                    shape: shape.clone(),
                    element_type: element_type.clone(),
                }),
            ),
        )
    }
}

pub fn merge_inductive_fields_dedupe_note() -> String {
    thread_local! {
        static CACHED: String = {
            "Set-semantics merge (v1-run-stability-throughline M1b + M1b-2, M0 receipt 2026-07-13): a type's inductive fields are a SET of (variant, field, shape, element) facts - multiplicity carries no information. The unguarded concat-on-collision compounded diamond ancestry multiplicatively along the import DAG (measured 409,240,584 retained InductiveField slots corpus-wide, ~3.3 GB of Vec slots alone; worst single module 22.6M entries at ~1,338 types), and even skip-if-equal alone left 95,050,553 slots because differing lists carried baked-in duplicate multiplicities (worst module still 4.1M at ~3k dupes/type). So the merge appends ONLY fields not already present (identity = variant|field|shape|element; type_name is the map key): equal or subset incoming lists SKIP (the sharing fast path - absent keys insert the incoming list Rc directly, never concat-with-empty), and every list is duplicate-free by induction (local collection is duplicate-free; merges never append a present fact). Genuinely new fields still append in the pre-cut encounter order. Same treatment class as guarded_union_str_bindings' skip-if-equal (#6487 cut 2); receipts on the throughline doc.".to_string()
        };
    }
    CACHED.with(|c: &String| c.clone())
}

pub fn recursion_shape_key(shape: RecursionShape) -> String {
    match shape.clone() {
        RecursionShape::DirectRecursion => "direct".to_string(),
        RecursionShape::ListRecursion => "list".to_string(),
        RecursionShape::OptionalRecursion => "optional".to_string(),
        RecursionShape::SetRecursion => "set".to_string(),
        RecursionShape::MapValueRecursion => "map_value".to_string(),
    }
}

pub fn inductive_field_key(f: Arc<InductiveField>) -> String {
    v1_rt::concat(
        v1_rt::concat(
            v1_rt::concat(
                v1_rt::concat(
                    v1_rt::concat(
                        v1_rt::concat(f.variant_name.clone(), "|".to_string()),
                        f.field_name.clone(),
                    ),
                    "|".to_string(),
                ),
                recursion_shape_key(f.shape.clone()),
            ),
            "|".to_string(),
        ),
        f.element_type.clone(),
    )
}

pub fn merge_inductive_fields(
    left: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>,
    right: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>,
) -> Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>> {
    Arc::new(v1_rt::map_keys(&right)).iter().cloned().fold(
        left.clone(),
        |acc: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>, type_name: String| {
            match v1_rt::map_get(&right, type_name.clone()) {
                Some(incoming) => match v1_rt::map_get(&acc, type_name.clone()) {
                    Some(existing) => {
                        if (existing.clone() == incoming.clone()) {
                            acc.clone()
                        } else {
                            {
                                let seen = existing.clone().iter().cloned().fold(
                                    v1_rt::rc_empty_set::<_>(),
                                    |s: _, f: Arc<InductiveField>| {
                                        v1_rt::rc_set_insert(s, inductive_field_key(f.clone()))
                                    },
                                );
                                let additions = Arc::new({
                                    let mut __result = Vec::new();
                                    for f in incoming.clone().iter().cloned() {
                                        if (v1_rt::set_contains(
                                            &seen,
                                            inductive_field_key(f.clone()),
                                        ) == false)
                                        {
                                            __result.push(f);
                                        }
                                    }
                                    __result
                                });
                                if ((additions.clone().len() as i64) == 0) {
                                    acc.clone()
                                } else {
                                    v1_rt::rc_map_insert(
                                        acc.clone(),
                                        type_name.clone(),
                                        v1_rt::concat(existing.clone(), additions.clone()),
                                    )
                                }
                            }
                        }
                    }
                    None => v1_rt::rc_map_insert(acc.clone(), type_name.clone(), incoming.clone()),
                },
                None => acc.clone(),
            }
        },
    )
}

pub fn inductive_fields_list_to_map(
    fields: Arc<Vec<Arc<InductiveField>>>,
) -> Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>> {
    fields.clone().iter().cloned().fold(
        v1_rt::rc_empty_map::<String, Arc<Vec<Arc<InductiveField>>>>(),
        |acc: Arc<HashMap<String, Arc<Vec<Arc<InductiveField>>>>>, field: Arc<InductiveField>| {
            let existing = match v1_rt::map_get(&acc, field.type_name.clone()) {
                Some(fs) => fs.clone(),
                None => Rc::new(vec![]),
            };
            v1_rt::rc_map_insert(
                acc.clone(),
                field.type_name.clone(),
                v1_rt::concat(existing.clone(), Rc::new(vec![field.clone()])),
            )
        },
    )
}

pub fn env_with_type_variable_bindings(
    env: Arc<TypeEnv>,
    tp_names: Arc<Vec<String>>,
) -> Arc<TypeEnv> {
    tp_names
        .clone()
        .iter()
        .cloned()
        .fold(env.clone(), |e: Arc<TypeEnv>, tp_name: String| {
            let tp_binding = Arc::new(TypeBinding {
                name: tp_name.clone(),
                resolved: Arc::new(Node {
                    name: tp_name.clone(),
                    span: kernel_span(tp_name.clone()),
                    ident_span: Some(kernel_span(tp_name.clone())),
                    children: Rc::new(vec![]),
                    connective: Connective::NoConnective,
                    params: Rc::new(vec![]),
                    inferred: Some(Arc::new(InferredNode::TypeVariable {
                        id: tp_name.clone(),
                    })),
                    return_cardinality: Cardinality::Required,
                    uses: Rc::new(vec![]),
                    body: None,
                    transport: None,
                    properties: Rc::new(vec![]),
                    type_annotation: None,
                    is_self_recursive: false,
                    has_non_tail_self_call: false,
                    match_pattern: None,
                    expr_data: Arc::new(ExprData::NoExprData),
                    ident: None,
                }),
                provenance: Arc::new(SubValueRelation::SubValueUnknown),
            });
            Arc::new(TypeEnv {
                module_path: e.module_path.clone(),
                bindings: v1_rt::rc_map_insert(
                    e.bindings.clone(),
                    intern(e.intern_table.clone(), tp_name.clone()).id.clone(),
                    tp_binding.clone(),
                ),
                str_bindings: v1_rt::rc_map_insert(
                    e.str_bindings.clone(),
                    tp_name.clone(),
                    tp_binding.clone(),
                ),
                ancestry_str_bindings: e.ancestry_str_bindings.clone(),
                parents: e.parents.clone(),
                recursive_types: e.recursive_types.clone(),
                recursive_type_set: e.recursive_type_set.clone(),
                inductive_fields: e.inductive_fields.clone(),
                source_indices: e.source_indices.clone(),
                intern_table: e.intern_table.clone(),
                source_visible_names: v1_rt::rc_map_insert(
                    e.source_visible_names.clone(),
                    tp_name.clone(),
                    true,
                ),
                symbol_index: e.symbol_index.clone(),
            })
        })
}
