// Generated tests for std_logic_generated_tests DAG.
// 
// Generated by gunbc-testgen
// DO NOT EDIT - regenerate with: make testgen
// Obligations: 13 obligations (0 discharged, 13 testable [0 compiler gaps]: A=4, B=9, C=0, D=0)
// Proven by construction: acyclicity, type compatibility, cardinality satisfaction.
// Content-Hash: 45e907650f5e1a8b283cc2d7b856597ce0b763bab802c7130358d92efe59a4a8
// 
// Probe-Observer Coverage:
//   Probes: 3
//     std.logic::classical_not (boundary mock)
//     std.logic::classical_and (boundary mock)
//     std.logic::classical_or (boundary mock)
//   
//   Observers: 3
//     std.logic::classical_and [terminal] (return: NonEmpty)
//     std.logic::classical_not [terminal] (return: NonEmpty)
//     std.logic::classical_or [terminal] (return: NonEmpty)
//   
//   Integration tests: 0


use gunbc_exec::{ExecuteConfig, ExecutionMode, execute_dag, lower};
use gunbc_ir::{Value, detect_boundaries};
use gunbc_test::{FermiCost, MockSpec, TestClass, assert_boundary_mockable, guard_test};

fn mock_spec() -> MockSpec {
    { let __r = gunbc_resolve::builder::build_dsl_graph("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build"); gunbc_test::auto_mock_spec(&__r.dag, "std-logic", Some(&__r.dsl_type_registry)) }
}

// =========================================================================
// Bucket A: Execution Semantics
// =========================================================================

// Proves: executor/boundary model correctness (runtime-only)
// 
// Determinism obligations: 3 pure nodes.
// To enable per-node determinism tests, use `execute_single_node`
// from gunbc_exec with baseline-derived inputs (Tier 1 infra).
// - 'std.logic::classical_not': same inputs → same outputs
// - 'std.logic::classical_and': same inputs → same outputs
// - 'std.logic::classical_or': same inputs → same outputs

/// DryRun execution completes without crash.
/// 
/// This is the minimal smoke test: build the DAG, run it in DryRun
/// with explicit boundary mocks, and verify it completes successfully.
#[test]
fn test_dryrun_completion() {
    if !guard_test("test_dryrun_completion", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mock_spec().to_boundary_mocks()), ..Default::default() }).expect("DryRun execution should complete without crash");
    assert!(!log.entries.is_empty(), "execution should produce log entries");
}

// =========================================================================
// Bucket B: Contract Obligations
// =========================================================================

// Tests for semantic compatibility when proof engine returns Unknown.
// 3 node contract compliance obligations.
// Per-node compliance tests use `execute_single_node` (Tier 1 infra).
// - 'std.logic::classical_not': valid inputs → valid outputs
// - 'std.logic::classical_and': valid inputs → valid outputs
// - 'std.logic::classical_or': valid inputs → valid outputs
// 3 optional input handling obligations.
// Optional inputs must accept missing values and reject wrong-typed inputs.

/// Variant coverage: std.logic::classical_not.return with variant 'True' of type Classical.
/// 
/// Proves: DAG handles variant 'True' at boundary port std.logic::classical_not.return.
#[test]
fn test_variant_std_logic_classical_not_return_true() {
    if !guard_test("test_variant_std_logic_classical_not_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_not", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for std.logic::classical_not.return should not crash");
}

/// Variant coverage: std.logic::classical_not.return with variant 'False' of type Classical.
/// 
/// Proves: DAG handles variant 'False' at boundary port std.logic::classical_not.return.
#[test]
fn test_variant_std_logic_classical_not_return_false() {
    if !guard_test("test_variant_std_logic_classical_not_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_not", "return", Value::Enum { ty: "Classical".to_string(), variant: "False".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for std.logic::classical_not.return should not crash");
}

/// Variant coverage: std.logic::classical_and.return with variant 'True' of type Classical.
/// 
/// Proves: DAG handles variant 'True' at boundary port std.logic::classical_and.return.
#[test]
fn test_variant_std_logic_classical_and_return_true() {
    if !guard_test("test_variant_std_logic_classical_and_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_and", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for std.logic::classical_and.return should not crash");
}

/// Variant coverage: std.logic::classical_and.return with variant 'False' of type Classical.
/// 
/// Proves: DAG handles variant 'False' at boundary port std.logic::classical_and.return.
#[test]
fn test_variant_std_logic_classical_and_return_false() {
    if !guard_test("test_variant_std_logic_classical_and_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_and", "return", Value::Enum { ty: "Classical".to_string(), variant: "False".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for std.logic::classical_and.return should not crash");
}

/// Variant coverage: std.logic::classical_or.return with variant 'True' of type Classical.
/// 
/// Proves: DAG handles variant 'True' at boundary port std.logic::classical_or.return.
#[test]
fn test_variant_std_logic_classical_or_return_true() {
    if !guard_test("test_variant_std_logic_classical_or_return_true", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_or", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant True for std.logic::classical_or.return should not crash");
}

/// Variant coverage: std.logic::classical_or.return with variant 'False' of type Classical.
/// 
/// Proves: DAG handles variant 'False' at boundary port std.logic::classical_or.return.
#[test]
fn test_variant_std_logic_classical_or_return_false() {
    if !guard_test("test_variant_std_logic_classical_or_return_false", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_or", "return", Value::Enum { ty: "Classical".to_string(), variant: "False".to_string() });
    let _log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).expect("variant False for std.logic::classical_or.return should not crash");
}

/// Optional input: std.logic::classical_not.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_logic_classical_not_deps() {
    if !guard_test("test_optional_missing_std_logic_classical_not_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_not", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.logic::classical_not.__deps missing should not error");
}

/// Optional input: std.logic::classical_and.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_logic_classical_and_deps() {
    if !guard_test("test_optional_missing_std_logic_classical_and_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("b".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_and", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.logic::classical_and.__deps missing should not error");
}

/// Optional input: std.logic::classical_or.__deps (cardinality: 0..*).
/// 
/// Proves: missing optional input does not crash.
#[test]
fn test_optional_missing_std_logic_classical_or_deps() {
    if !guard_test("test_optional_missing_std_logic_classical_or_deps", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("b".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let _outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_or", inputs, gunbc_exec::ExecutionMode::Real).expect("optional input std.logic::classical_or.__deps missing should not error");
}

// =========================================================================
// Boundary Tests (per-node mockability)
// =========================================================================

/// Test that all boundaries can be mocked.
#[test]
fn test_boundaries_mockable() {
    if !guard_test("test_boundaries_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let result = assert_boundary_mockable(&dag, mock_spec().to_boundary_mocks());
    assert!(result.is_ok(), "Boundaries should be mockable: {:?}", result.error);
}

/// Test that std.logic::classical_and boundary can be mocked.
#[test]
fn test_boundary_std_logic_classical_and_mockable() {
    if !guard_test("test_boundary_std_logic_classical_and_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.logic::classical_and".into()), "std.logic::classical_and should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_and", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.logic::classical_and").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.logic::classical_not boundary can be mocked.
#[test]
fn test_boundary_std_logic_classical_not_mockable() {
    if !guard_test("test_boundary_std_logic_classical_not_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.logic::classical_not".into()), "std.logic::classical_not should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_not", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.logic::classical_not").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

/// Test that std.logic::classical_or boundary can be mocked.
#[test]
fn test_boundary_std_logic_classical_or_mockable() {
    if !guard_test("test_boundary_std_logic_classical_or_mockable", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let lowered = lower(&dag).expect("lowering should succeed");
    let boundaries = detect_boundaries(&lowered.dag);
    assert!(boundaries.is_boundary_node(&"std.logic::classical_or".into()), "std.logic::classical_or should be a boundary");

    let mut mocks = mock_spec().to_boundary_mocks();
    mocks.set_value("std.logic::classical_or", "return", Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });

    let log = execute_dag(&dag, ExecuteConfig { mode: ExecutionMode::DryRun(mocks), ..Default::default() }).unwrap();
    let entry = log.get("std.logic::classical_or").expect("node should be in log");
    assert!(entry.was_intercepted, "boundary should be intercepted in dry-run");
}

// =========================================================================
// Chain Validation Tests
// =========================================================================

// These tests verify that mock outputs satisfy downstream input expectations.

/// Test that this tool's mock spec is self-consistent.
#[test]
fn test_mock_spec_self_consistent() {
    if !guard_test("test_mock_spec_self_consistent", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let spec = mock_spec();
    // Verify all boundary mocks are present
    assert!(spec.get_boundary_mock("std.logic::classical_not", "return").is_some(), "MockSpec should have boundary mock for std.logic::classical_not.return");
    assert!(spec.get_boundary_mock("std.logic::classical_and", "return").is_some(), "MockSpec should have boundary mock for std.logic::classical_and.return");
    assert!(spec.get_boundary_mock("std.logic::classical_or", "return").is_some(), "MockSpec should have boundary mock for std.logic::classical_or.return");
}

// =========================================================================
// Node I/O Example Tests
// =========================================================================

// These tests verify individual node behavior against specified examples.
// Each test executes a single node with given inputs and checks outputs.

/// Node example: std.logic::classical_not (example 0)
/// 
/// Tests that node 'std.logic::classical_not' produces expected outputs for given inputs.
#[test]
fn test_example_std_logic_classical_not_0() {
    if !guard_test("test_example_std_logic_classical_not_0", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__deps".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_not", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.logic::classical_not' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.logic::classical_and (example 1)
/// 
/// Tests that node 'std.logic::classical_and' produces expected outputs for given inputs.
#[test]
fn test_example_std_logic_classical_and_1() {
    if !guard_test("test_example_std_logic_classical_and_1", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__deps".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("b".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_and", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.logic::classical_and' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

/// Node example: std.logic::classical_or (example 2)
/// 
/// Tests that node 'std.logic::classical_or' produces expected outputs for given inputs.
#[test]
fn test_example_std_logic_classical_or_2() {
    if !guard_test("test_example_std_logic_classical_or_2", TestClass::Unit, FermiCost::XS, &[], &[]) {
    return;
};
    let dag = gunbc_resolve::builder::build_dsl_graph_dag("std/logic.dag", gunbc_resolve::BuildOpts::default()).expect("graph should build");
    let mut inputs = std::collections::HashMap::new();
    inputs.insert("__deps".to_string(), Value::List(vec![Value::Str("mock".to_string())]));
    inputs.insert("__out:return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("a".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("b".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    inputs.insert("return".to_string(), Value::Enum { ty: "Classical".to_string(), variant: "True".to_string() });
    let outputs = gunbc_exec::execute_single_node(&dag, "std.logic::classical_or", inputs, gunbc_exec::ExecutionMode::Real).expect("node 'std.logic::classical_or' should execute successfully");

    // Check output port 'return'
    let output_return = outputs.get("return").expect("output port 'return' should exist");
    assert!(!output_return.is_empty() || matches!(*output_return, Value::Skipped | Value::Unit), "expected non-empty value, Skipped, or Unit");
}

