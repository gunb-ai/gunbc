module v1.compiler.emit_rust_frontier

import std.roster_frontier {
  FrontierRow,
  TriggerProse,
  decl_ref,
  frontier_row_decl,
}

data strip_repeated_use_symbols_frontier_rows: List<FrontierRow> = [
  frontier_row_decl(
    ref: decl_ref(module_path: "v1.compiler.emit_rust", decl_name: "strip_repeated_use_symbols"),
    reason: "Post-hoc cross-line symbol-ownership pass over rendered use lines; construction fix is a single symbol-to-provider ownership map built before any use-line renders",
    trigger: TriggerProse {
      text: "import surface derives from ONE symbol-to-provider ownership map built BEFORE any use-line renders, OR resolver step-2 typed refusal replaces reference-derived synthesis entirely",
    },
  )
]
