# TODO_progress_display
#
# DAG progress visualization with "power flow" rendering.
# Replace verbose log-line dumps with a live DAG display showing nodes
# lighting up as execution progresses (SimCity 2013 power grid style).

## Problem

Current shell output dumps sequential `[node_name]` log lines with no
sense of overall progress, structure, or relationships. Long pipelines
produce walls of text that scroll off screen.

## Architecture

    ┌─────────────────────────────────────────────────┐
    │  Layer 4: Renderers (terminal, CI, web future)  │
    ├─────────────────────────────────────────────────┤
    │  Layer 3: DagProgress (renderer-agnostic state) │
    ├─────────────────────────────────────────────────┤
    │  Layer 2: Frame loop + ProgressObserver         │
    ├─────────────────────────────────────────────────┤
    │  Layer 1: Symbol system (core/ir visual vocab)  │
    └─────────────────────────────────────────────────┘

Integrates with existing infrastructure:
- `Renderable` trait (core/ir/src/render.rs) — generated file headers
- Language SubDag module (core/ir/src/language/) — same compositional pattern
- CI rendering (core/ir/src/transport/ci/) — CiContext feeds into observer
- `to_mermaid()` (core/ir/src/dag.rs) — progress Mermaid via SemanticColor

---------------------------------------------------------------------

## Layer 1: Symbol system (core/ir/src/symbols.rs)

DAG-modeled visual vocabulary for the entire codebase. Lives in core/ir
alongside render.rs and the language module — all three are "format-specific
visual concerns modeled as composable data."

### Core types

```rust
enum Tier { Emoji, Unicode, Ascii }       // Encoding capability
enum SemanticColor { Default, Success, Warning, Error, Info, Dim, Active, Accent }
enum SymbolId { NodePending, NodeRunning, NodeCompleted, /* ... ~30 ids */ }

struct Symbol {
    id: SymbolId,
    emoji: &'static str, unicode: &'static str, ascii: &'static str,
    color: SemanticColor,
}

struct SymbolSet {                         // "Theme" — lookup by SymbolId
    name: &'static str,
    default_tier: Tier,
    symbols: &'static [Symbol],
}
```

SymbolId mirrors TypeId, SymbolSet mirrors TypeRegistry — same pattern.
Full symbol table in Appendix A.

### Compositional model (SubDag pattern)

Symbols follow the same SubDag pattern as the language module:

- **Encoding tiers** are a SubDag: emoji → unicode → ascii fallback chain
  (same structure as language config → render chain)
- **Individual symbols** are SubDags: config + encoding SubDag + resolve node
  (parallels `build_html_subdag()`: config + render nodes)
- **Composite symbols** compose via SubDag nesting (boundary + completed =
  one composed SubDag, not a new flat entry)
- **Animation frames** are SubDags with cycle edges (spinner = 4 frame nodes
  + cycle edge from frame_3 → frame_0)

```rust
enum SymbolOp {           // Parallels LanguageOp
    Config,               // Symbol metadata (id, color)
    Atom { tier: Tier, value: String },  // Concrete encoding
    Resolve,              // Tier → atom selection
    Compose,              // Multi-symbol concatenation
    Frame { index: u8 },  // Animation frame
    Cycle,                // Frame loop control
}
```

`Dag<SymbolOp>` is a composable symbol definition graph, same as
`Dag<LanguageOp>` is a composable language definition graph. The static
STANDARD SymbolSet is the "compiled" fast path; the SubDag model is the
"definition" layer.

Files: core/ir/src/symbols.rs (NEW), core/ir/src/lib.rs (add pub mod)

---------------------------------------------------------------------

## Layer 2: Frame loop + ProgressObserver

### Game-engine style frame loop

The core rendering primitive is a generic update→render loop, similar to
a game engine's main loop. This decouples execution events from display
timing — the renderer runs at its own frame rate.

```rust
trait FrameLoop {
    /// Process pending state changes (from observer events).
    fn update(&mut self, progress: &DagProgress, dt: Duration);
    /// Produce a visual frame from current state.
    fn render(&mut self, progress: &DagProgress);
}
```

#### Frame scheduling policy

Different situations need different policies for how the frame loop
relates to execution:

```rust
enum FramePolicy {
    /// Always show latest state. Skip intermediate frames if execution
    /// outpaces the frame rate. Display never lags behind execution.
    /// Best for: fast pipelines, CI, non-interactive.
    Latest,

    /// Sequential: show every state transition, even if execution has
    /// moved ahead. Queue transitions and replay them at frame rate.
    /// Best for: dynamic mode where you want to see every animation.
    Sequential { max_queue: usize },

    /// Adaptive: show latest by default, but slow down when transitions
    /// are visually interesting (edge flow, node morph). Speeds back up
    /// when execution is idle or done.
    /// Best for: interactive terminal with dynamic mode.
    Adaptive { min_fps: u8, max_fps: u8 },
}
```

#### Execution ↔ frame loop interaction

```
    execute_flat() loop              Frame loop (separate concern)
    ┌──────────────────┐            ┌──────────────────────────┐
    │ for node in topo │  events    │ loop {                   │
    │   observer.on_*()│──────────→ │   update(progress, dt)   │
    │   execute(node)  │            │   render(progress)       │
    │ end              │            │   sleep(frame_interval)  │
    └──────────────────┘            │ }                        │
                                    └──────────────────────────┘
```

For synchronous execution (current), the frame loop runs between node
executions — update+render after each observer callback. For future
concurrent execution, the frame loop runs on its own thread/tick.

### ProgressObserver trait

Formalizes the existing CiContext ad-hoc callback pattern into a trait:

```rust
trait ProgressObserver: Send {
    fn on_dag_start(&mut self, snapshot: &DagSnapshot);
    fn on_node_start(&mut self, node_id: &NodeId);
    fn on_node_complete(&mut self, node_id: &NodeId, summary: OutputSummary);
    fn on_node_failed(&mut self, node_id: &NodeId, error: &str);
    fn on_node_skipped(&mut self, node_id: &NodeId);
    fn on_node_intercepted(&mut self, node_id: &NodeId, summary: OutputSummary);
    fn on_dag_complete(&mut self, elapsed: Duration);
}
```

### DagSnapshot — reuses existing types

```rust
struct DagSnapshot {
    node_ids: Vec<NodeId>,
    edges: Vec<Edge>,                    // Reuse existing Edge type
    topo_order: Vec<NodeId>,
    boundary_nodes: HashSet<NodeId>,     // From detect_boundaries()
    labels: HashMap<NodeId, String>,
}
```

### OutputSummary

Extracts summarization logic from existing `print_log_entry()`:

```rust
struct OutputSummary { fields: Vec<FieldSummary>, elapsed: Duration }
struct FieldSummary { name: String, kind: FieldKind, preview: String }
enum FieldKind { Scalar, Number, Boolean, List(usize), Map(usize), Secret, Url, Empty }
```

FieldKind adds semantic categorization on top of Value's structural types
(e.g., detecting URLs within Str values).

Files: core/exec/src/progress.rs (NEW), core/exec/src/execute.rs (add observer)

---------------------------------------------------------------------

## Layer 3: DagProgress state machine

Renderer-agnostic state. Knows nothing about terminals or HTML.

```rust
struct DagProgress {
    nodes: HashMap<NodeId, NodeProgress>,
    edges: HashMap<(NodeId, NodeId), EdgeProgress>,
    topo_order: Vec<NodeId>,
    phase: DagPhase,
    elapsed: Duration,
}

enum DagPhase { NotStarted, Running { current_node: NodeId },
                Completed { elapsed: Duration }, Failed { node: NodeId, error: String } }
enum NodeState { Pending, Running, Completed, Failed, Skipped, Intercepted }
enum EdgeState { Idle, Flowing, Done, Dead }
```

### Power flow transitions

    1. DAG starts:     all nodes=Pending, all edges=Idle
    2. Node A starts:  A=Running
    3. Node A done:    A=Completed, edges(A→*)=Flowing  ← the visual moment
    4. Node B starts:  B=Running, edges(*→B)=Done
    ...repeat until DAG=Completed or Failed

Files: core/exec/src/progress.rs (DagProgress, NodeProgress, EdgeProgress)

---------------------------------------------------------------------

## Layer 4: Renderers

### Render modes (orthogonal to encoding tier)

```rust
enum RenderMode {
    Standard,   // Symbol-per-state, clean layout
    Dynamic,    // Cellular automaton effects, wavefront animation
    Compact,    // Single-line summary
}
```

### Terminal renderer

Carriage-return based (`\r` + `\x1b[{N}A` cursor-up). Zero dependencies.
Falls back to append-only when piped (not a TTY).

The terminal renderer implements FrameLoop:
- `update()`: processes observer events, advances animations (dynamic mode)
- `render()`: builds display lines from layout + symbols, writes to stdout

Layout: leveled from topo order, left-to-right, wrapping at terminal width.
See Appendix B for visual mockups.

### Diamond / parallel branch layout

DagBuilder already tracks "generations" (topological levels) — nodes at the
same generation are parallel. `compute_levels()` (~10 lines) extracts this
from the topo order. Diamonds render as side-by-side branches:

```
  ● build ──┬── ● test ──┬── ○ report
             │            │
             └── ● lint ──┘
```

The level-grouping algorithm:
1. Compute `level[node] = max(level[predecessor] + 1)` over topo order
2. Group nodes by level → each level is a horizontal "band"
3. Nodes in the same band render side-by-side (parallel)
4. Use ConnectorTee (├/┬) and ConnectorCorner (└) for fan-out/fan-in

This naturally handles the CI graph's real diamond (build → test + lint →
report) and any future parallel DAG shapes. See Appendix B for mockups.

### Dynamic mode

Purely a rendering concern — same DagProgress data model.

### Unified animation model

All animations — spinners, edge wavefronts, node morphs, cellular automaton
patterns — share the same underlying structure: a **SubDag with Frame nodes
and a Cycle edge**. The renderer advances frames; the animation type just
determines what the frame content is.

```rust
/// Single animation state, shared by ALL animation types.
/// Spinners, wavefronts, morphs, and automaton patterns are
/// all frame sequences — they differ only in frame content.
struct Animation {
    frames: Vec<String>,     // Frame content (from SubDag Frame nodes)
    current: usize,          // Current frame index
    interval: Duration,      // Time between frames
    elapsed: Duration,       // Time since last frame advance
    mode: AnimationMode,     // How to loop
}

enum AnimationMode {
    Cycle,        // Loop forever (spinners, idle automaton)
    Once,         // Play once then hold last frame (node morph)
    Propagate {   // Advance position along a path (edge wavefront)
        path_len: usize,
        position: usize,
    },
}
```

This means:
- **Spinners** = Animation { frames: [🌑,🌒,🌓,🌔], mode: Cycle }
- **Node morph** = Animation { frames: [○,◔,◑,◐], mode: Once }
- **Edge wavefront** = Animation { frames: [═], mode: Propagate { path_len, .. } }
- **Idle automaton** = Animation { frames: [·pattern·], mode: Cycle }

No special cases. The `Animation` struct is the universal frame-cycling
primitive. Each animation type is just a different set of frames + mode
constructed from the same SymbolOp::Frame SubDags.

Frame scheduling uses Adaptive policy: slows for visually interesting
transitions, catches up when execution is fast. See Appendix C.

### CI renderer

CiProgressObserver wraps CiContext — formalizes existing ad-hoc callbacks.
Uses ASCII tier. Maps SemanticColor to CI annotation levels.

### Web renderer (future, deferred)

DagProgress has Serialize → JSON/SSE to browser → Mermaid + style classes.
Zero changes to Layers 1-3 needed.

---------------------------------------------------------------------

## Consolidation with existing codebase

| Proposed type      | Reuses                           | New?  |
|--------------------|----------------------------------|-------|
| Edge (in snapshot) | core/ir Edge directly            | No    |
| BoundaryInfo       | detect_boundaries() result       | No    |
| OutputSummary      | Extracts print_log_entry() logic | Partial |
| ProgressObserver   | Formalizes CiContext pattern      | Yes   |
| Symbol system      | Parallels language SubDag module | Yes   |
| DagProgress        | New state machine (different from ExecutionLog) | Yes |
| FrameLoop          | New (no existing frame concept)  | Yes   |

Key principle: no new types where existing ones suffice. DagSnapshot uses
Edge directly. Observer formalizes what CiContext already does ad-hoc.
Symbol system follows exact same patterns as language module.

---------------------------------------------------------------------

## Implementation phases

1. **Symbol system** — core/ir/src/symbols.rs: types, STANDARD set, tests
2. **Observer + snapshot** — core/exec/src/progress.rs: trait, DagSnapshot,
   OutputSummary; wire into execute_flat()
3. **Frame loop** — FrameLoop trait, FramePolicy enum, synchronous driver
4. **Terminal renderer** — standard mode: layout, carriage-return, emoji default
5. **Dynamic mode** — wavefront, morph, cellular automaton, adaptive timing
6. **CI integration** — CiProgressObserver wrapping CiContext
7. **Polish** — spinner animation, compact mode, TTY detection
8. **Web** (future) — JSON serialization, SSE endpoint, browser renderer

---------------------------------------------------------------------

## Key design decisions

- **Symbols in core/ir**: vocabulary not rendering engine; parallels
  language module; any crate can use without renderer dependency
- **Three tiers**: emoji default (we control env), unicode for edge cases,
  ASCII for CI/pipes — same vocab serves all contexts
- **Dynamic as RenderMode not Tier**: encoding (emoji/unicode/ascii) and
  temporal behavior (static/animated) are orthogonal dimensions
- **Carriage return not TUI**: zero deps, non-interactive, fixed-size
  display; DagProgress model supports swapping in TUI later
- **Observer not channel**: execution is synchronous single-threaded;
  observer is simpler; trait stays same if switching to channels later
- **Frame loop as trait**: decouples render timing from execution timing;
  same trait works for sync (between nodes) and async (own thread)
- **Frame policy choices**: Latest (never lag), Sequential (see everything),
  Adaptive (best of both) — renderer picks based on context
- **DagProgress separate from ExecutionLog**: live state machine vs post-hoc
  record; different concerns, don't couple them

=====================================================================

## Appendix A: Standard SymbolSet

    | SymbolId         | Emoji | Unicode | ASCII | Color   |
    |------------------|-------|---------|-------|---------|
    | NodePending      | ⏳    | ○       | [ ]   | Dim     |
    | NodeRunning      | 🔄    | ◐       | [~]   | Active  |
    | NodeCompleted    | ✅    | ●       | [x]   | Success |
    | NodeFailed       | ❌    | ✗       | [!]   | Error   |
    | NodeSkipped      | ⏭️    | ◌       | [-]   | Dim     |
    | NodeIntercepted  | 🔮    | ◇       | [m]   | Info    |
    | EdgeIdle         | ─     | ─       | -     | Dim     |
    | EdgeFlowing      | ⚡    | ═       | =     | Accent  |
    | EdgeDone         | ─     | ─       | -     | Success |
    | EdgeDead         | ┄     | ┄       | .     | Dim     |
    | DagNotStarted    | 🔲    | □       | [ ]   | Dim     |
    | DagRunning       | 🚀    | ▶       | [>]   | Active  |
    | DagCompleted     | 🏁    | ■       | [X]   | Success |
    | DagFailed        | 💥    | ■       | [!]   | Error   |
    | BoundaryMarker   | 🌐    | ◈       | [B]   | Info    |
    | Spinner0..3      | 🌑🌒🌓🌔 | ◐◓◑◒ | |/-\  | Active  |
    | Success          | ✅    | ✓       | OK    | Success |
    | Failure          | ❌    | ✗       | FAIL  | Error   |
    | Warning          | ⚠️    | ⚠       | WARN  | Warning |
    | Info             | ℹ️    | ℹ       | INFO  | Info    |
    | DataList         | 📋    | ≡       | [L]   | Default |
    | DataMap          | 🗂️    | ⊞       | [M]   | Default |
    | DataSecret       | 🔒    | ▪       | [*]   | Warning |
    | DataUrl          | 🔗    | ↗       | [U]   | Info    |
    | DataTimer        | ⏱️    | ⏱       | [T]   | Dim     |

SemanticColor → ANSI: Success=\x1b[32m, Error=\x1b[31m, Warning=\x1b[33m,
Active=\x1b[1;37m, Dim=\x1b[2m, Accent=\x1b[36m, Info=\x1b[34m

## Appendix B: Visual mockups

### Emoji tier (default) — mid-execution

```
  🚀 gist-diff                                        ⏱️  1.2s
  ─────────────────────────────────────────────────────────────

  ✅ read_files ──⚡── ✅ parse_read_files ──⚡── ✅ render_markdown
     0.02s              0.1s  📋 234 items        0.05s
                                    │
                                    ⚡
                                    │
                        ✅ prepare_gist_request ──⚡── 🔄 execute_gist
                           0.01s                        🌒 running...
                                                           │
                                                           ⏳
                                                           │
                                                   ⏳ parse_gist_response
```

### Emoji tier — completed

```
  🏁 gist-diff                                        ⏱️  2.4s
  ─────────────────────────────────────────────────────────────

  ✅ read_files ──── ✅ parse_read_files ──── ✅ render_markdown
     0.02s              0.1s  📋 234 items       0.05s
                                    │
                        ✅ prepare_gist_request ──── ✅ execute_gist
                           0.01s                       0.8s
                                                           │
                                                   ✅ parse_gist_response
                                                      🔗 https://gist.github.com/...
```

### ASCII tier (CI/pipes)

```
  [>] gist-diff                                        [T] 1.2s
  -----------------------------------------------------------------
  [x] read_files -- [x] parse_read_files -- [x] render_markdown
      0.02s            0.1s  [L] 234 items     0.05s
                                  |
                      [x] prepare_gist_request == [~] execute_gist
                          0.01s                      running...
                                                        |
                                              [ ] parse_gist_response
```

### Diamond / parallel pattern (emoji tier, CI graph)

```
  🚀 ci-pipeline                                      ⏱️  4.1s
  ─────────────────────────────────────────────────────────────

  ✅ build ──┬── ✅ test ───┬── 🔄 report
     1.2s    │    1.8s      │      🌒 running...
             │              │
             └── ✅ lint ───┘
                  0.9s
```

### Diamond — wider (3 parallel branches, ASCII tier)

```
  [>] wide-pipeline                                    [T] 3.0s
  -----------------------------------------------------------------
  [x] fetch ──┬── [x] validate ──┬── [~] deploy
      0.5s    │      0.8s        │      running...
              │                  │
              ├── [x] transform ─┤
              │      1.1s        │
              │                  │
              └── [x] audit ─────┘
                     0.6s
```

## Appendix C: Dynamic mode animation frames

### Edge wavefront propagation

```
  Frame 0:  ● A ──────── ○ B       (A completed)
  Frame 1:  ● A ═─────── ○ B       (wavefront starts)
  Frame 2:  ● A ═══───── ○ B       (propagating)
  Frame 4:  ● A ════════ ○ B       (fully energized)
  Frame 6:  ● A ────════ ● B       (B started, settling)
  Frame 7:  ● A ──────── ● B       (edge done)
```

### Node state morph (unicode tier)

```
  Pending→Running:   ○  →  ◔  →  ◑  →  ◐
  Running→Completed: ◐  →  ◕  →  ⬤  →  ●
  Running→Failed:    ◐  →  ◉  →  ✗
```

### Cellular automaton idle animation (Rule 110)

```
  Frame 0:  ● A ─·──·─── ○ B
  Frame 1:  ● A ──·──·── ○ B
  Frame 2:  ● A ───·──·─ ○ B       (dots shift along edge)
```
